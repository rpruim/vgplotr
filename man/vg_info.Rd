% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/info.R
\name{vg_info}
\alias{vg_info}
\title{Report the versions of Mosaic and DuckDB-Wasm this vgplotr bundles}
\usage{
vg_info()
}
\value{
An object of class \code{vg_info}, with a \code{print()} method, containing:
\describe{
\item{\code{vgplotr}}{This vgplotr version.}
\item{\code{mosaic}}{The bundled mosaic version.}
\item{\code{duckdb_wasm}}{The pinned DuckDB-Wasm version (see
\code{\link[=vg_duckdb_cache_status]{vg_duckdb_cache_status()}} for whether it's currently cached
locally or fetched from a CDN at view time).}
\item{\code{duckdb_cache}}{The result of \code{\link[=vg_duckdb_cache_status]{vg_duckdb_cache_status()}}.}
\item{\code{native_packages}}{A named character vector, the installed
version of each of \code{duckdb}/\code{nanoarrow}/\code{DBI} (used by
\code{\link[=vg_duckdb_connector]{vg_duckdb_connector()}}), or \code{NA} for any not installed.}
}
}
\description{
The mosaic JS runtime and DuckDB-Wasm's JS API are vendored into the
package at a fixed version, chosen when this vgplotr version was built
(not resolved at install time or view time) -- see \code{vignette("using-databases")}
and \code{data-raw/js/build.js}. \code{vg_info()} reports exactly which versions
that is, along with whether the (separately-cached, since it's too large
to vendor) DuckDB-Wasm engine binary itself is currently cached locally,
and -- since these aren't vendored at all, but come from whatever the
user has installed -- the \code{duckdb}/\code{nanoarrow} package versions used by
\code{\link[=vg_duckdb_connector]{vg_duckdb_connector()}}'s native rendering path, if installed.
}
\concept{info functions}
