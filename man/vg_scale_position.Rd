% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/scale.R
\name{vg_scale_position}
\alias{vg_scale_position}
\alias{vg_scale_x}
\alias{vg_scale_y}
\title{Set position scale properties (x or y)}
\usage{
vg_scale_position(
  spec = NULL,
  which = c("x", "y"),
  type = vg_unset,
  domain = vg_unset,
  range = vg_unset,
  nice = vg_unset,
  zero = vg_unset,
  reverse = vg_unset,
  clamp = vg_unset,
  round = vg_unset,
  padding = vg_unset,
  padding_inner = vg_unset,
  padding_outer = vg_unset,
  align = vg_unset,
  inset = vg_unset,
  inset_left = vg_unset,
  inset_right = vg_unset,
  inset_top = vg_unset,
  inset_bottom = vg_unset,
  base = vg_unset,
  exponent = vg_unset,
  constant = vg_unset,
  percent = vg_unset,
  ...
)

vg_scale_x(
  spec = NULL,
  type = vg_unset,
  domain = vg_unset,
  range = vg_unset,
  nice = vg_unset,
  zero = vg_unset,
  reverse = vg_unset,
  clamp = vg_unset,
  round = vg_unset,
  padding = vg_unset,
  padding_inner = vg_unset,
  padding_outer = vg_unset,
  align = vg_unset,
  inset = vg_unset,
  inset_left = vg_unset,
  inset_right = vg_unset,
  inset_top = vg_unset,
  inset_bottom = vg_unset,
  base = vg_unset,
  exponent = vg_unset,
  constant = vg_unset,
  percent = vg_unset,
  ...
)

vg_scale_y(
  spec = NULL,
  type = vg_unset,
  domain = vg_unset,
  range = vg_unset,
  nice = vg_unset,
  zero = vg_unset,
  reverse = vg_unset,
  clamp = vg_unset,
  round = vg_unset,
  padding = vg_unset,
  padding_inner = vg_unset,
  padding_outer = vg_unset,
  align = vg_unset,
  inset = vg_unset,
  inset_left = vg_unset,
  inset_right = vg_unset,
  inset_top = vg_unset,
  inset_bottom = vg_unset,
  base = vg_unset,
  exponent = vg_unset,
  constant = vg_unset,
  percent = vg_unset,
  ...
)
}
\arguments{
\item{spec}{A plot fragment or \code{vgspec} to set this scale on, or \code{NULL} to
start a new plot fragment with just these attributes.}

\item{which}{Which position scale this sets: \code{"x"} or \code{"y"}.}

\item{type}{Scale type, e.g. \code{"linear"}, \code{"log"}, \code{"pow"}, \code{"sqrt"},
\code{"symlog"}, \code{"utc"}, \code{"time"}, \code{"band"}, \code{"point"}, \code{"ordinal"}
(\code{xScale}/\code{yScale}).}

\item{domain}{Data domain extent \code{c(min, max)}, or \code{"fixed"} to preserve it
across updates (\code{xDomain}/\code{yDomain}).}

\item{range}{Output range in pixels \code{c(min, max)} (\code{xRange}/\code{yRange}).}

\item{nice}{Boolean; round the scale domain to human-friendly values
(\code{xNice}/\code{yNice}).}

\item{zero}{Boolean; force the domain to include zero (\code{xZero}/\code{yZero}).}

\item{reverse}{Boolean; reverse the scale direction (\code{xReverse}/\code{yReverse}).}

\item{clamp}{Boolean; clamp out-of-domain values to the range
(\code{xClamp}/\code{yClamp}).}

\item{round}{Boolean; round output values to the nearest pixel
(\code{xRound}/\code{yRound}).}

\item{padding}{Inner/outer padding fraction for band scales, 0 to 1
(\code{xPadding}/\code{yPadding}).}

\item{padding_inner}{Inner padding between bands, 0 to 1
(\code{xPaddingInner}/\code{yPaddingInner}).}

\item{padding_outer}{Outer padding at the scale's edges, 0 to 1
(\code{xPaddingOuter}/\code{yPaddingOuter}).}

\item{align}{Alignment of ordinal bands, 0 to 1 (\code{xAlign}/\code{yAlign}).}

\item{inset}{Pixel inset applied to both ends of the range
(\code{xInset}/\code{yInset}).}

\item{inset_left, inset_right}{Pixel inset at the left/right end of the
range; only meaningful for \code{which = "x"} (\code{xInsetLeft}/\code{xInsetRight}).}

\item{inset_top, inset_bottom}{Pixel inset at the top/bottom end of the
range; only meaningful for \code{which = "y"} (\code{yInsetTop}/\code{yInsetBottom}).}

\item{base}{Log/pow scale base (\code{xBase}/\code{yBase}).}

\item{exponent}{Pow/symlog scale exponent (\code{xExponent}/\code{yExponent}).}

\item{constant}{Symlog scale constant (\code{xConstant}/\code{yConstant}).}

\item{percent}{Boolean; format/scale values as percentages
(\code{xPercent}/\code{yPercent}).}

\item{...}{Additional plot-level attributes not covered above, by their
raw mosaic-spec camelCase name (e.g. \verb{xyDomain =}).}
}
\description{
\code{vg_scale_x()}/\code{vg_scale_y()} set the scale properties mosaic-spec exposes
per positional axis (\code{xScale}, \code{xDomain}, ... -- substitute \code{y} for the
vertical axis). These are already plot-level attributes that
\code{\link[=vg_plot]{vg_plot()}}/\code{\link[=vg_attributes]{vg_attributes()}} accept directly by their raw camelCase
names; this is a discoverable, snake_case-argument convenience layer on
top of that, following the same real-named-argument approach as the mark
constructors (see \code{\link[=vg_mark]{vg_mark()}}). \code{vg_scale_x()} and \code{vg_scale_y()} are thin
wrappers around the generic \code{vg_scale_position()}.
}
\details{
Like \code{\link[=vg_plot]{vg_plot()}}, this can be piped in alongside marks/interactors -- it
only ever sets attributes on the current plot fragment, so it never needs
to come last in a chain. For the analogous facet scales (\code{fx}/\code{fy}), see
\code{\link[=vg_scale_facet]{vg_scale_facet()}}.
}
\examples{
vg_mark_dot(x = ~a, y = ~b) |>
  vg_scale_x(type = "log") |>
  vg_scale_y(zero = TRUE, nice = TRUE)
}
\seealso{
Other scale functions:
\code{\link[=vg_scale_color]{vg_scale_color()}},
\code{\link[=vg_scale_facet]{vg_scale_facet()}},
\code{\link[=vg_scale_opacity]{vg_scale_opacity()}},
\code{\link[=vg_scale_r]{vg_scale_r()}}
}
\concept{scale functions}
