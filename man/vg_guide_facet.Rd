% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/guide.R
\name{vg_guide_facet}
\alias{vg_guide_facet}
\alias{vg_guide_fx}
\alias{vg_guide_fy}
\title{Set axis-guide properties for a facet scale (fx or fy)}
\usage{
vg_guide_facet(
  spec = NULL,
  which = c("fx", "fy"),
  position = vg_unset,
  ticks = vg_unset,
  tick_spacing = vg_unset,
  tick_size = vg_unset,
  tick_padding = vg_unset,
  tick_format = vg_unset,
  tick_rotate = vg_unset,
  grid = vg_unset,
  line = vg_unset,
  label = vg_unset,
  label_anchor = vg_unset,
  label_offset = vg_unset,
  font_variant = vg_unset,
  aria_label = vg_unset,
  aria_description = vg_unset,
  ...
)

vg_guide_fx(
  spec = NULL,
  position = vg_unset,
  ticks = vg_unset,
  tick_spacing = vg_unset,
  tick_size = vg_unset,
  tick_padding = vg_unset,
  tick_format = vg_unset,
  tick_rotate = vg_unset,
  grid = vg_unset,
  line = vg_unset,
  label = vg_unset,
  label_anchor = vg_unset,
  label_offset = vg_unset,
  font_variant = vg_unset,
  aria_label = vg_unset,
  aria_description = vg_unset,
  ...
)

vg_guide_fy(
  spec = NULL,
  position = vg_unset,
  ticks = vg_unset,
  tick_spacing = vg_unset,
  tick_size = vg_unset,
  tick_padding = vg_unset,
  tick_format = vg_unset,
  tick_rotate = vg_unset,
  grid = vg_unset,
  line = vg_unset,
  label = vg_unset,
  label_anchor = vg_unset,
  label_offset = vg_unset,
  font_variant = vg_unset,
  aria_label = vg_unset,
  aria_description = vg_unset,
  ...
)
}
\arguments{
\item{spec}{A plot fragment or \code{vgspec} to set this guide on, or \code{NULL} to
start a new plot fragment with just these attributes.}

\item{which}{Which facet axis this sets: \code{"fx"} or \code{"fy"}.}

\item{position}{Facet header position: \code{"top"}/\code{"bottom"} for \code{fx},
\code{"left"}/\code{"right"} for \code{fy}, or \code{NULL}/\code{FALSE} to hide it
(\code{fxAxis}/\code{fyAxis}).}

\item{ticks}{Number of ticks, or an array of explicit tick values
(\code{fxTicks}/\code{fyTicks}).}

\item{tick_spacing}{Target distance between ticks in pixels
(\code{fxTickSpacing}/\code{fyTickSpacing}).}

\item{tick_size}{Length of tick marks in pixels
(\code{fxTickSize}/\code{fyTickSize}).}

\item{tick_padding}{Distance between a tick mark and its label in pixels
(\code{fxTickPadding}/\code{fyTickPadding}).}

\item{tick_format}{Format specifier string or function for tick labels
(\code{fxTickFormat}/\code{fyTickFormat}).}

\item{tick_rotate}{Rotation angle of tick labels in degrees
(\code{fxTickRotate}/\code{fyTickRotate}).}

\item{grid}{Boolean, or a line stroke count, to render grid lines along
this facet axis (\code{fxGrid}/\code{fyGrid}).}

\item{line}{Boolean; whether to draw the facet axis line itself
(\code{fxLine}/\code{fyLine}).}

\item{label}{Facet axis title string (\code{fxLabel}/\code{fyLabel}).}

\item{label_anchor}{Position anchor for the label text: \code{"left"},
\code{"center"}, or \code{"right"} (\code{fxLabelAnchor}/\code{fyLabelAnchor}).}

\item{label_offset}{Distance between the facet axis and its label in
pixels (\code{fxLabelOffset}/\code{fyLabelOffset}).}

\item{font_variant}{CSS font-variant for tick labels
(\code{fxFontVariant}/\code{fyFontVariant}).}

\item{aria_label}{ARIA accessibility label for the facet axis
(\code{fxAriaLabel}/\code{fyAriaLabel}).}

\item{aria_description}{ARIA accessibility description for the facet axis
(\code{fxAriaDescription}/\code{fyAriaDescription}).}

\item{...}{Additional plot-level attributes not covered above, by their
raw mosaic-spec camelCase name.}
}
\description{
\code{vg_guide_fx()}/\code{vg_guide_fy()} set the axis-guide properties mosaic-spec
exposes per facet axis (\code{fxAxis}, \code{fxTicks}, ... -- substitute \code{fy} for
the row facet axis). This is the facet counterpart of
\code{\link[=vg_guide_position]{vg_guide_position()}}; it accepts the same properties minus
\code{label_arrow}, which mosaic doesn't define for facet guides.
\code{vg_guide_fx()} and \code{vg_guide_fy()} are thin wrappers around the generic
\code{vg_guide_facet()}.
}
\details{
See \code{\link[=vg_guide_position]{vg_guide_position()}} for why these are \verb{vg_guide_*()} rather than
\verb{vg_axis_*()} (a historical collision with mosaic's \code{axisFx}/\code{axisFy}
marks -- no longer live now that mark constructors are \code{vg_mark_}
prefixed, but the guide functions haven't been renamed back).
}
\examples{
vg_mark_dot(x = ~a, y = ~b, fx = ~g) |>
  vg_guide_fx(label = "Group")
}
\seealso{
Other guide functions:
\code{\link[=vg_guide_color]{vg_guide_color()}},
\code{\link[=vg_guide_opacity]{vg_guide_opacity()}},
\code{\link[=vg_guide_position]{vg_guide_position()}},
\code{\link[=vg_guide_r]{vg_guide_r()}}
}
\concept{guide functions}
