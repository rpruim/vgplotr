% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/guide-generated.R
\name{vg_guide_facet}
\alias{vg_guide_facet}
\alias{vg_guide_fx}
\alias{vg_guide_fy}
\title{Set axis-guide properties for a facet scale (fx or fy)}
\usage{
vg_guide_facet(
  spec = NULL,
  which = c("fx", "fy"),
  position = vg_unset,
  ticks = vg_unset,
  tick_spacing = vg_unset,
  tick_size = vg_unset,
  tick_padding = vg_unset,
  tick_format = vg_unset,
  tick_rotate = vg_unset,
  grid = vg_unset,
  line = vg_unset,
  label = vg_unset,
  label_anchor = vg_unset,
  label_offset = vg_unset,
  font_variant = vg_unset,
  aria_label = vg_unset,
  aria_description = vg_unset,
  ...
)

vg_guide_fx(
  spec = NULL,
  position = vg_unset,
  ticks = vg_unset,
  tick_spacing = vg_unset,
  tick_size = vg_unset,
  tick_padding = vg_unset,
  tick_format = vg_unset,
  tick_rotate = vg_unset,
  grid = vg_unset,
  line = vg_unset,
  label = vg_unset,
  label_anchor = vg_unset,
  label_offset = vg_unset,
  font_variant = vg_unset,
  aria_label = vg_unset,
  aria_description = vg_unset,
  ...
)

vg_guide_fy(
  spec = NULL,
  position = vg_unset,
  ticks = vg_unset,
  tick_spacing = vg_unset,
  tick_size = vg_unset,
  tick_padding = vg_unset,
  tick_format = vg_unset,
  tick_rotate = vg_unset,
  grid = vg_unset,
  line = vg_unset,
  label = vg_unset,
  label_anchor = vg_unset,
  label_offset = vg_unset,
  font_variant = vg_unset,
  aria_label = vg_unset,
  aria_description = vg_unset,
  ...
)
}
\arguments{
\item{spec}{A plot fragment or \code{vgspec} to set this guide on, or \code{NULL} to
start a new plot fragment with just these attributes.}

\item{which}{Which facet axis this sets: \code{"fx"} or \code{"fy"}.}

\item{position}{The side of the frame on which to place the implicit axis: \emph{top} or \emph{bottom} for \emph{fx}. (\code{fxAxis}/\code{fyAxis}).}

\item{ticks}{The desired approximate number of axis ticks, or an explicit array of tick values, or an interval such as \emph{day} or \emph{month}. (\code{fxTicks}/\code{fyTicks}).}

\item{tick_spacing}{The desired approximate spacing between adjacent axis ticks, affecting the default \strong{ticks}; defaults to 80 pixels for \emph{x} and \emph{fx}, and 35 pixels for \emph{y} and \emph{fy}. (\code{fxTickSpacing}/\code{fyTickSpacing}).}

\item{tick_size}{The length of axis tick marks in pixels; negative values extend in the opposite direction. (\code{fxTickSize}/\code{fyTickSize}).}

\item{tick_padding}{The distance between an axis tick mark and its associated text label (in pixels); often defaults to 3, but may be affected by \strong{fxTickSize} and \strong{fxTickRotate}. (\code{fxTickPadding}/\code{fyTickPadding}).}

\item{tick_format}{How to format inputs (abstract values) for axis tick labels; one of: - a d3-format string for numeric scales - a d3-time-format string for temporal scales : https://d3js.org/d3-time : https://d3js.org/d3-time-format (\code{fxTickFormat}/\code{fyTickFormat}).}

\item{tick_rotate}{The rotation angle of axis tick labels in degrees clocksize; defaults to 0. (\code{fxTickRotate}/\code{fyTickRotate}).}

\item{grid}{Whether to show a grid aligned with the scale’s ticks. (\code{fxGrid}/\code{fyGrid}).}

\item{line}{If true, draw a line along the axis; if false (default), do not. (\code{fxLine}/\code{fyLine}).}

\item{label}{A textual label to show on the axis or legend; if null, show no label. (\code{fxLabel}/\code{fyLabel}).}

\item{label_anchor}{Where to place the axis \strong{label} relative to the plot’s frame. (\code{fxLabelAnchor}/\code{fyLabelAnchor}).}

\item{label_offset}{The axis \strong{label} position offset (in pixels); default depends on margins and orientation. (\code{fxLabelOffset}/\code{fyLabelOffset}).}

\item{font_variant}{The font-variant attribute for axis ticks; defaults to \emph{tabular-nums} for quantitative axes. (\code{fxFontVariant}/\code{fyFontVariant}).}

\item{aria_label}{A short label representing the axis in the accessibility tree. (\code{fxAriaLabel}/\code{fyAriaLabel}).}

\item{aria_description}{A textual description for the axis in the accessibility tree. (\code{fxAriaDescription}/\code{fyAriaDescription}).}

\item{...}{Additional plot-level attributes not covered above, snake_case
(e.g. \verb{x_domain =}) -- translated to mosaic's own camelCase key.}
}
\description{
\code{vg_guide_fx()}/\code{vg_guide_fy()} set the axis-guide properties
mosaic-spec exposes per facet axis (\code{fxAxis}, \code{fxTicks}, ... --
substitute \code{fy} for the row facet axis). This is the facet
counterpart of \code{\link[=vg_guide_position]{vg_guide_position()}}; the property set (which lacks
\code{label_arrow}, among others) is derived from the schema, not
hand-picked.
}
\details{
See \code{\link[=vg_guide_position]{vg_guide_position()}} for why these are \verb{vg_guide_*()} rather
than \verb{vg_axis_*()} (a historical collision with mosaic's \code{axisFx}/
\code{axisFy} marks -- no longer live now that mark constructors are
\code{vg_mark_} prefixed, but the guide functions haven't been renamed
back).
}
\examples{
vg_mark_dot(x = ~a, y = ~b, fx = ~g) |>
  vg_guide_fx(label = "Group")
}
\seealso{
Other guide functions:
\code{\link[=vg_guide_all]{vg_guide_all()}},
\code{\link[=vg_guide_color]{vg_guide_color()}},
\code{\link[=vg_guide_opacity]{vg_guide_opacity()}},
\code{\link[=vg_guide_position]{vg_guide_position()}},
\code{\link[=vg_guide_r]{vg_guide_r()}}
}
\concept{guide functions}
