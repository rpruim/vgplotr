% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/scale.R
\name{vg_scale_opacity}
\alias{vg_scale_opacity}
\title{Set the opacity scale's properties}
\usage{
vg_scale_opacity(
  spec = NULL,
  type = vg_unset,
  domain = vg_unset,
  range = vg_unset,
  nice = vg_unset,
  zero = vg_unset,
  reverse = vg_unset,
  clamp = vg_unset,
  base = vg_unset,
  exponent = vg_unset,
  constant = vg_unset,
  percent = vg_unset,
  ...
)
}
\arguments{
\item{spec}{A plot fragment or \code{vgspec} to set this scale on, or \code{NULL}
to start a new plot fragment with just these attributes.}

\item{type}{Scale type, e.g. \code{"linear"}, \code{"sqrt"}, \code{"pow"}, \code{"log"}
(\code{opacityScale}).}

\item{domain}{Data domain extent \code{c(min, max)} (\code{opacityDomain}).}

\item{range}{Output opacity range, e.g. \code{c(0, 1)} (\code{opacityRange}).}

\item{nice}{Boolean; round the scale domain to human-friendly values
(\code{opacityNice}).}

\item{zero}{Boolean; force the domain to include zero (\code{opacityZero}).}

\item{reverse}{Boolean; reverse the scale direction (\code{opacityReverse}).}

\item{clamp}{Boolean; clamp out-of-domain values to the range
(\code{opacityClamp}).}

\item{base}{Log/pow scale base (\code{opacityBase}).}

\item{exponent}{Pow/symlog scale exponent (\code{opacityExponent}).}

\item{constant}{Symlog scale constant (\code{opacityConstant}).}

\item{percent}{Boolean; format/scale values as percentages
(\code{opacityPercent}).}

\item{...}{Additional plot-level attributes not covered above, by their
raw mosaic-spec camelCase name.}
}
\description{
\code{vg_scale_opacity()} sets the scale properties mosaic-spec exposes for
the \code{opacity} channel (\code{opacityScale}, \code{opacityDomain}, ... -- the scale
that \verb{opacity =}/\verb{fillOpacity =}/\verb{strokeOpacity =} encodings are bound
to unless they're a literal constant). These are already plot-level
attributes that \code{\link[=vg_plot]{vg_plot()}}/\code{\link[=vg_attributes]{vg_attributes()}} accept directly by their
raw camelCase names; this is a discoverable, snake_case-argument
convenience layer on top of that. For the axis-guide properties
(\code{opacityLabel}/\code{opacityTickFormat}), see \code{\link[=vg_guide_opacity]{vg_guide_opacity()}}; for an
actual rendered opacity legend, see \code{\link[=vg_legend_opacity]{vg_legend_opacity()}}.
}
\details{
Like \code{\link[=vg_plot]{vg_plot()}}, this can be piped in alongside marks/interactors -- it
only ever sets attributes on the current plot fragment, so it never
needs to come last in a chain.
}
\examples{
vg_mark_dot(x = ~a, y = ~b, opacity = ~g) |>
  vg_scale_opacity(range = c(0.2, 1))
}
\seealso{
Other scale functions:
\code{\link[=vg_scale_color]{vg_scale_color()}},
\code{\link[=vg_scale_facet]{vg_scale_facet()}},
\code{\link[=vg_scale_position]{vg_scale_position()}},
\code{\link[=vg_scale_r]{vg_scale_r()}}
}
\concept{scale functions}
