% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/scale.R
\name{vg_scale_color}
\alias{vg_scale_color}
\title{Set the color scale's properties}
\usage{
vg_scale_color(
  spec = NULL,
  type = vg_unset,
  domain = vg_unset,
  range = vg_unset,
  scheme = vg_unset,
  interpolate = vg_unset,
  pivot = vg_unset,
  symmetric = vg_unset,
  nice = vg_unset,
  zero = vg_unset,
  reverse = vg_unset,
  clamp = vg_unset,
  base = vg_unset,
  exponent = vg_unset,
  constant = vg_unset,
  percent = vg_unset,
  n = vg_unset,
  ...
)
}
\arguments{
\item{spec}{A plot fragment or \code{vgspec} to set this scale on, or \code{NULL}
to start a new plot fragment with just these attributes.}

\item{type}{Scale type, e.g. \code{"linear"}, \code{"log"}, \code{"pow"}, \code{"sqrt"},
\code{"symlog"}, \code{"ordinal"}, \code{"categorical"}, \code{"threshold"}, \code{"quantile"},
\code{"quantize"} (\code{colorScale}).}

\item{domain}{Data domain extent \code{c(min, max)}, or an array of
categories for an ordinal/categorical scale (\code{colorDomain}).}

\item{range}{Explicit array of output colors (\code{colorRange}).}

\item{scheme}{Named color palette, e.g. \code{"Viridis"}, \code{"Blues"},
\code{"YlOrRd"}, \code{"Tableau10"} (\code{colorScheme}).}

\item{interpolate}{Custom interpolation function/method for a continuous
scale (\code{colorInterpolate}).}

\item{pivot}{Midpoint value for a diverging color scheme
(\code{colorPivot}).}

\item{symmetric}{Boolean; force a continuous domain to be symmetric
around \code{pivot} (\code{colorSymmetric}).}

\item{nice}{Boolean; round the scale domain to human-friendly values
(\code{colorNice}).}

\item{zero}{Boolean; force the domain to include zero (\code{colorZero}).}

\item{reverse}{Boolean; reverse the scale/palette order
(\code{colorReverse}).}

\item{clamp}{Boolean; clamp out-of-domain values to the range
(\code{colorClamp}).}

\item{base}{Log/pow scale base (\code{colorBase}).}

\item{exponent}{Pow/symlog scale exponent (\code{colorExponent}).}

\item{constant}{Symlog scale constant (\code{colorConstant}).}

\item{percent}{Boolean; format/scale values as percentages
(\code{colorPercent}).}

\item{n}{Number of quantiles/buckets for a \code{"quantile"}/\code{"quantize"}
scale (\code{colorN}).}

\item{...}{Additional plot-level attributes not covered above, by their
raw mosaic-spec camelCase name.}
}
\description{
\code{vg_scale_color()} sets the scale properties mosaic-spec exposes for the
\code{color} channel (\code{colorScale}, \code{colorDomain}, ... -- the scale that
\verb{fill =}/\verb{stroke =} encodings are bound to unless they're a literal
constant). These are already plot-level attributes that
\code{\link[=vg_plot]{vg_plot()}}/\code{\link[=vg_attributes]{vg_attributes()}} accept directly by their raw camelCase
names; this is a discoverable, snake_case-argument convenience layer on
top of that. Unlike \code{vg_scale_x()}/\code{vg_scale_y()}, there's only one
color channel, so \code{vg_scale_color()} isn't built from a \verb{which =}
generic -- it's the whole implementation. For the axis-guide properties
(\code{colorLabel}/\code{colorTickFormat}), see \code{\link[=vg_guide_color]{vg_guide_color()}}; for an actual
rendered color legend, see \code{\link[=vg_legend_color]{vg_legend_color()}} -- a different (if
related) thing, a standalone/embedded legend mark rather than a plot
attribute.
}
\details{
Like \code{\link[=vg_plot]{vg_plot()}}, this can be piped in alongside marks/interactors -- it
only ever sets attributes on the current plot fragment, so it never
needs to come last in a chain.
}
\examples{
vg_mark_dot(x = ~a, y = ~b, fill = ~g) |>
  vg_scale_color(scheme = "Viridis", type = "linear")
}
\seealso{
Other scale functions:
\code{\link[=vg_scale_facet]{vg_scale_facet()}},
\code{\link[=vg_scale_opacity]{vg_scale_opacity()}},
\code{\link[=vg_scale_position]{vg_scale_position()}},
\code{\link[=vg_scale_r]{vg_scale_r()}}
}
\concept{scale functions}
