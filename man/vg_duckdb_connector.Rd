% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/connector.R
\name{vg_duckdb_connector}
\alias{vg_duckdb_connector}
\title{Render via a native DuckDB connection instead of DuckDB-Wasm}
\usage{
vg_duckdb_connector(con = NULL)
}
\arguments{
\item{con}{An existing DBI connection to a DuckDB database (e.g. from
\code{DBI::dbConnect(duckdb::duckdb())}), already set up with any extensions
or \code{ATTACH}ed external databases you want reachable. Left \code{NULL} (the
default), vgplotr creates and manages a private in-process connection
for you, closed when you call \code{\link[=vg_duckdb_server_stop]{vg_duckdb_server_stop()}}. A \code{con} you
supply yourself is never closed by vgplotr -- you own its lifecycle.}
}
\value{
A \code{vg_connector} object, for use as \code{\link[=vg_widget]{vg_widget()}}/\code{\link[=vg_render]{vg_render()}}'s
\verb{connector =} argument.
}
\description{
An opt-in alternative to the default \code{\link[=vg_wasm_connector]{vg_wasm_connector()}}: instead of
running DuckDB-Wasm inside the browser, the browser talks over a local
HTTP server (started automatically, see \code{\link[=vg_duckdb_server_stop]{vg_duckdb_server_stop()}}) to a
\emph{real} DuckDB process running in this R session. This removes
DuckDB-Wasm's size/extension limits -- in particular, it's the only way
to visualize data that actually lives in another database, via DuckDB's
own federation extensions (e.g. \verb{ATTACH '...' AS pg (TYPE postgres)},
\code{mysql_scanner}, \code{sqlite_scanner}): set those up yourself on \code{con} however
you like, vgplotr just proxies queries to it.
}
\details{
Only one native connection is served per R session (starting a second,
different \code{con} while one is already active is an error -- call
\code{\link[=vg_duckdb_server_stop]{vg_duckdb_server_stop()}} first if you need to switch).

\strong{Security note:} the local server executes whatever SQL the page sends
it, with no authentication -- mitigated by binding to \verb{127.0.0.1} only,
never a public interface, but this means (1) don't point \code{con} at a
connection with write access to anything you wouldn't want arbitrary SQL
run against from that browser tab (prefer a read-only role for real
external databases), and (2) unlike DuckDB-Wasm output, a page rendered
this way is only live on the machine/session that rendered it -- sharing
the HTML file (email, hosting it, opening it later) gives a plot that
can't connect to anything, since there's no server running for it to
reach on the recipient's machine.
}
\seealso{
Other connector functions:
\code{\link[=vg_duckdb_server_stop]{vg_duckdb_server_stop()}},
\code{\link[=vg_wasm_connector]{vg_wasm_connector()}}
}
\concept{connector functions}
