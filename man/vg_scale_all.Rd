% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/scale.R
\name{vg_scale_all}
\alias{vg_scale_all}
\title{Set global default scale properties (all ordinal position scales)}
\usage{
vg_scale_all(
  spec = NULL,
  align = vg_unset,
  inset = vg_unset,
  padding = vg_unset,
  ...
)
}
\arguments{
\item{spec}{A plot fragment or \code{vgspec} to set these on, or \code{NULL} to
start a new plot fragment with just these attributes.}

\item{align}{Default alignment of ordinal bands, 0 to 1, for any
position scale that doesn't set its own \code{xAlign}/\code{yAlign}/etc.
(\code{align}).}

\item{inset}{Default pixel inset applied to both ends of the range, for
any position scale that doesn't set its own \code{xInset}/\code{yInset}/etc.
(\code{inset}).}

\item{padding}{Default inner/outer padding fraction for band/point
scales, for any position scale that doesn't set its own
\code{xPadding}/\code{yPadding}/etc. (\code{padding}).}

\item{...}{Additional plot-level attributes not covered above, by their
raw mosaic-spec camelCase name.}
}
\description{
\code{vg_scale_all()} sets mosaic-spec's plot-wide fallback defaults for
\code{align}/\code{inset}/\code{padding} -- unlike \code{xAlign}/\code{xPadding}/etc. (set via
\code{\link[=vg_scale_position]{vg_scale_position()}}) or \code{fxAlign}/\code{fxPadding}/etc. (via
\code{\link[=vg_scale_facet]{vg_scale_facet()}}), which only affect one scale, these bare attributes
(\code{align}, \code{inset}, \code{padding}) are mosaic's own defaults applied to
\emph{every} ordinal position scale (\code{x}, \code{y}, \code{fx}, \code{fy}) that doesn't set
its own value. They're already plot-level attributes that
\code{\link[=vg_plot]{vg_plot()}}/\code{\link[=vg_attributes]{vg_attributes()}} accept directly by their raw camelCase
names; this is just a discoverable, named-argument alias for them (no
snake_case translation needed here, since these names have no camelCase
compound to convert).
}
\details{
Like \code{\link[=vg_plot]{vg_plot()}}, this can be piped in alongside marks/interactors -- it
only ever sets attributes on the current plot fragment, so it never
needs to come last in a chain. For the analogous axis-guide defaults
(\code{axis}, \code{grid}, \code{ariaLabel}, \code{ariaDescription}), see \code{\link[=vg_guide_all]{vg_guide_all()}}.
}
\examples{
vg_mark_dot(x = ~a, y = ~b) |>
  vg_scale_all(padding = 0.2)
}
\seealso{
Other scale functions:
\code{\link[=vg_scale_color]{vg_scale_color()}},
\code{\link[=vg_scale_facet]{vg_scale_facet()}},
\code{\link[=vg_scale_opacity]{vg_scale_opacity()}},
\code{\link[=vg_scale_position]{vg_scale_position()}},
\code{\link[=vg_scale_r]{vg_scale_r()}}
}
\concept{scale functions}
