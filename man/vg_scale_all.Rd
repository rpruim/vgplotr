% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/scale-generated.R
\name{vg_scale_all}
\alias{vg_scale_all}
\title{Set global default scale properties (all ordinal position scales)}
\usage{
vg_scale_all(
  spec = NULL,
  padding = vg_unset,
  align = vg_unset,
  inset = vg_unset,
  ...
)
}
\arguments{
\item{spec}{A plot fragment or \code{vgspec} to set this scale on, or \code{NULL} to
start a new plot fragment with just these attributes.}

\item{padding}{\verb{<number | param()>} For \emph{band} scales, how much of the \strong{range} to reserve to separate adjacent bands; defaults to 0.1 (10\%). (\code{padding}).}

\item{align}{\verb{<number | param()>} How to distribute unused space in the \strong{range} for \emph{point} and \emph{band} scales. (\code{align}).}

\item{inset}{\verb{<number | param()>} Shorthand to set the same default for all four insets: \strong{insetTop}, \strong{insetRight}, \strong{insetBottom}, and \strong{insetLeft}. (\code{inset}).}

\item{...}{Additional plot-level attributes not covered above, snake_case
(e.g., \verb{x_domain =}) -- translated to mosaic's own camelCase key.}
}
\description{
\code{vg_scale_all()} sets mosaic-spec's plot-wide fallback defaults --
unlike \code{xAlign}/\code{xPadding}/etc. (set via \code{\link[=vg_scale_position]{vg_scale_position()}}) or
\code{fxAlign}/etc. (via \code{\link[=vg_scale_facet]{vg_scale_facet()}}), which only affect one scale,
these bare attributes are mosaic's own defaults applied to \emph{every}
ordinal position scale (\code{x}, \code{y}, \code{fx}, \code{fy}) that doesn't set its
own value. For the analogous axis-guide defaults, see
\code{\link[=vg_guide_all]{vg_guide_all()}}.
}
\details{
See \link{vg_value_types} for what the \verb{<...>} notation below (\code{number}, \code{param()}, ...) means.
}
\examples{
vg_mark_dot(x = ~a, y = ~b) |>
  vg_scale_all(padding = 0.2)
}
\seealso{
Other scale functions:
\code{\link[=vg_scale_color]{vg_scale_color()}},
\code{\link[=vg_scale_facet]{vg_scale_facet()}},
\code{\link[=vg_scale_length]{vg_scale_length()}},
\code{\link[=vg_scale_opacity]{vg_scale_opacity()}},
\code{\link[=vg_scale_position]{vg_scale_position()}},
\code{\link[=vg_scale_r]{vg_scale_r()}},
\code{\link[=vg_scale_symbol]{vg_scale_symbol()}}
}
\concept{scale functions}
