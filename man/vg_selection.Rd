% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/selection.R
\name{vg_selection}
\alias{vg_selection}
\alias{vg_param_date}
\title{Define a Selection, or a date-valued Param, for \code{vg_params()}}
\usage{
vg_selection(
  select = c("crossfilter", "intersect", "single", "union"),
  cross = NULL,
  empty = NULL,
  include = NULL
)

vg_param_date(date)
}
\arguments{
\item{select}{How clauses combine: \code{"crossfilter"} (a cross-filtered
intersection: a selection made in a plot filters the others but not
itself), \code{"intersect"} (logical "and"), \code{"union"} (logical "or"), or
\code{"single"} (only the most recent clause is kept).}

\item{cross}{Whether selections made in a plot filter other plots but not
itself (default \code{FALSE}, except that a \code{"crossfilter"} selection always
does). Not allowed with \code{select = "crossfilter"}, which ignores it.}

\item{empty}{Whether a selection with no clauses selects no records (\code{TRUE})
or all of them (\code{FALSE}, the default).}

\item{include}{Upstream selections whose clauses are relayed into this one:
a \code{\link[=param]{param()}}, a list of them, or the selections' names as strings.}

\item{date}{A \code{Date}, a \code{POSIXct} date-time, or an ISO date/time string,
e.g. \code{"2020-01-01"}: the param's initial value.}
}
\value{
A \code{vg_selection} or \code{vg_param_date} object, for use as a named
argument to \code{\link[=vg_params]{vg_params()}}.
}
\description{
\code{vg_selection()} writes mosaic-spec's \code{Selection} definition,
\code{vg_selection("crossfilter", include = param(other))}, where
\code{vg_params(brush = list(select = "crossfilter"))} would otherwise be typed
out by hand. It checks the pieces and spells the \code{include} references for
you. \code{vg_param_date()} writes a date-valued param: a plain \code{Date} in
\code{vg_params()} reaches mosaic as an ordinary string, not a date.
}
\details{
A selection collects the clauses that interactors and inputs publish to it
(\code{as = param(brush)}), and filters whatever is bound to it (a mark's
\code{filter_by}). Its \code{select} type decides how the clauses combine.
}
\examples{
vg_create() |>
  vg_params(
    brush = vg_selection("crossfilter"),
    either = vg_selection("union", include = c("a", "b")),
    since = vg_param_date(as.Date("2020-01-01"))
  )
}
\seealso{
Other spec functions:
\code{\link[=param]{param()}},
\code{\link[=vg_attributes]{vg_attributes()}},
\code{\link[=vg_config]{vg_config()}},
\code{\link[=vg_create]{vg_create()}},
\code{\link[=vg_data]{vg_data()}},
\code{\link[=vg_data_registry]{vg_data_registry()}},
\code{\link[=vg_example_url]{vg_example_url()}},
\code{\link[=vg_meta]{vg_meta()}},
\code{\link[=vg_params]{vg_params()}},
\code{\link[=vg_plot_defaults]{vg_plot_defaults()}}
}
\concept{spec functions}
