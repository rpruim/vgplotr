% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/create.R
\name{vg_data_registry}
\alias{vg_data_registry}
\title{List every data source name used this session, and what it currently refers to}
\usage{
vg_data_registry()
}
\value{
A data frame with one row per distinct name: \code{name}; \code{kind}
(\code{"local data"}, \code{"file"}, \code{"url"}, \code{"query"}, or \code{"other"}); and
\code{source}, a short description (the data frame's dimensions, the file
path/URL, or the query text).
}
\description{
A session-wide log of every name \code{\link[=vg_data]{vg_data()}} has registered so far --
whether given explicitly or auto-generated (see its \code{name} argument) --
meant as a diagnostic aid. Since every \code{vg_render()} call on the same
page shares one DuckDB instance, two different specs that end up using
the same name for different data silently clobber each other once
rendered together, with nothing in the R console to say so (see
\code{vignette("getting-started")}'s "One DuckDB instance for an entire
page" callout). Checking \code{vg_data_registry()} before rendering several
specs onto the same page -- to confirm each name's \code{source} is really
the data meant for it -- is a quick way to catch that kind of mistake
before it happens instead of after.
}
\details{
Reflects only the \emph{most recent} registration for each name (matching
what would actually be live in the shared DuckDB instance if every spec
built so far were rendered onto one page right now), not a full history
of every past registration. Only covers sources registered through
\code{\link[=vg_data]{vg_data()}}/\link[=vg_create]{vg_create(data = )}/the \code{some_df |> vg_mark_dot(...)} shorthand -- a name that only appears inside a raw
JSON/YAML string passed straight to \code{\link[=vg_render]{vg_render()}}/\code{\link[=vg_widget]{vg_widget()}} isn't
tracked here, since that path never goes through \code{\link[=vg_data]{vg_data()}}.
}
\seealso{
Other spec functions:
\code{\link[=param]{param()}},
\code{\link[=vg_attributes]{vg_attributes()}},
\code{\link[=vg_config]{vg_config()}},
\code{\link[=vg_create]{vg_create()}},
\code{\link[=vg_data]{vg_data()}},
\code{\link[=vg_data_url]{vg_data_url()}},
\code{\link[=vg_meta]{vg_meta()}},
\code{\link[=vg_params]{vg_params()}},
\code{\link[=vg_plot_defaults]{vg_plot_defaults()}}
}
\concept{spec functions}
