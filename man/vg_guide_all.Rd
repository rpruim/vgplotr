% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/guide.R
\name{vg_guide_all}
\alias{vg_guide_all}
\title{Set global default axis-guide properties (all position axes)}
\usage{
vg_guide_all(
  spec = NULL,
  position = vg_unset,
  grid = vg_unset,
  aria_label = vg_unset,
  aria_description = vg_unset,
  ...
)
}
\arguments{
\item{spec}{A plot fragment or \code{vgspec} to set these on, or \code{NULL} to
start a new plot fragment with just these attributes.}

\item{position}{Default axis side -- \code{"top"}/\code{"bottom"} for \code{x}/\code{fx},
\code{"left"}/\code{"right"} for \code{y}/\code{fy}, \code{"both"} for both sides, or \code{NULL} to
suppress the axis -- for any position axis that doesn't set its own
\code{xAxis}/\code{yAxis}/etc. (\code{axis}).}

\item{grid}{Default grid setting (boolean, a stroke color string, or a
tick count/values), for any position axis that doesn't set its own
\code{xGrid}/\code{yGrid}/etc. (\code{grid}).}

\item{aria_label}{Default ARIA accessibility label for the plot's SVG
root (\code{ariaLabel}).}

\item{aria_description}{Default ARIA accessibility description for the
plot's SVG root (\code{ariaDescription}).}

\item{...}{Additional plot-level attributes not covered above, by their
raw mosaic-spec camelCase name.}
}
\description{
\code{vg_guide_all()} sets mosaic-spec's plot-wide fallback defaults for
\code{axis}/\code{grid}/\code{ariaLabel}/\code{ariaDescription} -- unlike \code{xAxis}/\code{xGrid}/
etc. (set via \code{\link[=vg_guide_position]{vg_guide_position()}}) or \code{fxAxis}/\code{fxGrid}/etc. (via
\code{\link[=vg_guide_facet]{vg_guide_facet()}}), which only affect one axis, these bare attributes
are mosaic's own defaults applied to \emph{every} position axis (\code{x}, \code{y},
\code{fx}, \code{fy}) that doesn't set its own value. They're already plot-level
attributes that \code{\link[=vg_plot]{vg_plot()}}/\code{\link[=vg_attributes]{vg_attributes()}} accept directly by their
raw camelCase names; this gives them the same discoverable,
snake_case-argument treatment as the other guide functions.
}
\details{
Like \code{\link[=vg_plot]{vg_plot()}}, this can be piped in alongside marks/interactors -- it
only ever sets attributes on the current plot fragment, so it never
needs to come last in a chain. For the analogous scale defaults
(\code{align}, \code{inset}, \code{padding}), see \code{\link[=vg_scale_all]{vg_scale_all()}}.
}
\examples{
vg_mark_dot(x = ~a, y = ~b) |>
  vg_guide_all(grid = TRUE)
}
\seealso{
Other guide functions:
\code{\link[=vg_guide_color]{vg_guide_color()}},
\code{\link[=vg_guide_facet]{vg_guide_facet()}},
\code{\link[=vg_guide_opacity]{vg_guide_opacity()}},
\code{\link[=vg_guide_position]{vg_guide_position()}},
\code{\link[=vg_guide_r]{vg_guide_r()}}
}
\concept{guide functions}
