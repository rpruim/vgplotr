% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/guide-generated.R
\name{vg_guide_all}
\alias{vg_guide_all}
\title{Set global default axis-guide properties (all position axes)}
\usage{
vg_guide_all(
  spec = NULL,
  position = vg_unset,
  grid = vg_unset,
  aria_label = vg_unset,
  aria_description = vg_unset,
  ...
)
}
\arguments{
\item{spec}{A plot fragment or \code{vgspec} to set this guide on, or \code{NULL} to
start a new plot fragment with just these attributes.}

\item{position}{The side of the frame on which to place the implicit axis: \emph{top} or \emph{bottom} for \emph{x} or \emph{fx}, or \emph{left} or \emph{right} for \emph{y} or \emph{fy}. (\code{axis}).}

\item{grid}{Whether to show a grid aligned with the scale’s ticks. (\code{grid}).}

\item{aria_label}{The aria-label attribute on the SVG root. (\code{ariaLabel}).}

\item{aria_description}{The aria-description attribute on the SVG root. (\code{ariaDescription}).}

\item{...}{Additional plot-level attributes not covered above, snake_case
(e.g. \verb{x_domain =}) -- translated to mosaic's own camelCase key.}
}
\description{
\code{vg_guide_all()} sets mosaic-spec's plot-wide fallback defaults for
the axis-guide properties -- unlike \code{xAxis}/\code{xGrid}/etc. (set via
\code{\link[=vg_guide_position]{vg_guide_position()}}) or \code{fxAxis}/etc. (via \code{\link[=vg_guide_facet]{vg_guide_facet()}}),
which only affect one axis, these bare attributes are mosaic's own
defaults applied to \emph{every} position axis (\code{x}, \code{y}, \code{fx}, \code{fy})
that doesn't set its own value. For the analogous scale defaults,
see \code{\link[=vg_scale_all]{vg_scale_all()}}.
}
\examples{
vg_mark_dot(x = ~a, y = ~b) |>
  vg_guide_all(grid = TRUE)
}
\seealso{
Other guide functions:
\code{\link[=vg_guide_color]{vg_guide_color()}},
\code{\link[=vg_guide_facet]{vg_guide_facet()}},
\code{\link[=vg_guide_opacity]{vg_guide_opacity()}},
\code{\link[=vg_guide_position]{vg_guide_position()}},
\code{\link[=vg_guide_r]{vg_guide_r()}}
}
\concept{guide functions}
