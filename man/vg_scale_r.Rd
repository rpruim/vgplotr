% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/scale.R
\name{vg_scale_r}
\alias{vg_scale_r}
\alias{vg_scale_radius}
\title{Set the radius scale's properties}
\usage{
vg_scale_r(
  spec = NULL,
  type = vg_unset,
  domain = vg_unset,
  range = vg_unset,
  nice = vg_unset,
  zero = vg_unset,
  clamp = vg_unset,
  base = vg_unset,
  exponent = vg_unset,
  constant = vg_unset,
  percent = vg_unset,
  ...
)

vg_scale_radius(
  spec = NULL,
  type = vg_unset,
  domain = vg_unset,
  range = vg_unset,
  nice = vg_unset,
  zero = vg_unset,
  clamp = vg_unset,
  base = vg_unset,
  exponent = vg_unset,
  constant = vg_unset,
  percent = vg_unset,
  ...
)
}
\arguments{
\item{spec}{A plot fragment or \code{vgspec} to set this scale on, or \code{NULL}
to start a new plot fragment with just these attributes.}

\item{type}{Scale type, e.g. \code{"sqrt"}, \code{"linear"}, \code{"pow"}, \code{"log"}
(\code{rScale}).}

\item{domain}{Data domain extent \code{c(min, max)} (\code{rDomain}).}

\item{range}{Output radius range in pixels, e.g. \code{c(0, 20)} (\code{rRange}).}

\item{nice}{Boolean; round the scale domain to human-friendly values
(\code{rNice}).}

\item{zero}{Boolean; force the domain to start at 0 (\code{rZero}).}

\item{clamp}{Boolean; clamp out-of-domain values to the range
(\code{rClamp}).}

\item{base}{Log/pow scale base (\code{rBase}).}

\item{exponent}{Pow/symlog scale exponent (\code{rExponent}).}

\item{constant}{Symlog scale constant (\code{rConstant}).}

\item{percent}{Boolean; format/scale values as percentages
(\code{rPercent}).}

\item{...}{Additional plot-level attributes not covered above, by their
raw mosaic-spec camelCase name.}
}
\description{
\code{vg_scale_r()} (aliased as \code{vg_scale_radius()}) sets the scale
properties mosaic-spec exposes for the \code{r} channel (\code{rScale},
\code{rDomain}, ... -- the scale that a \code{dot}/\code{circle} mark's \code{r} encoding is
bound to unless it's a literal constant). These are already
plot-level attributes that \code{\link[=vg_plot]{vg_plot()}}/\code{\link[=vg_attributes]{vg_attributes()}} accept
directly by their raw camelCase names; this is a discoverable,
snake_case-argument convenience layer on top of that. Unlike the color
and opacity scales, mosaic doesn't define an \code{rReverse} property, so
there's no \code{reverse} argument here. For the axis-guide property
(\code{rLabel}; there's no \code{rTickFormat}), see \code{\link[=vg_guide_r]{vg_guide_r()}}/
\code{\link[=vg_guide_radius]{vg_guide_radius()}}; for an actual rendered radius/size legend, see
\code{\link[=vg_legend_symbol]{vg_legend_symbol()}} (mosaic doesn't have a dedicated \code{r}-typed legend).
}
\details{
Like \code{\link[=vg_plot]{vg_plot()}}, this can be piped in alongside marks/interactors -- it
only ever sets attributes on the current plot fragment, so it never
needs to come last in a chain.
}
\examples{
vg_dot(x = ~a, y = ~b, r = ~g) |>
  vg_scale_r(range = c(0, 20), zero = TRUE)
}
\seealso{
Other scale functions:
\code{\link[=vg_scale_color]{vg_scale_color()}},
\code{\link[=vg_scale_facet]{vg_scale_facet()}},
\code{\link[=vg_scale_opacity]{vg_scale_opacity()}},
\code{\link[=vg_scale_position]{vg_scale_position()}}
}
\concept{scale functions}
