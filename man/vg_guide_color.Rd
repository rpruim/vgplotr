% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/guide.R
\name{vg_guide_color}
\alias{vg_guide_color}
\title{Set axis-guide properties for the color scale}
\usage{
vg_guide_color(spec = NULL, label = vg_unset, tick_format = vg_unset, ...)
}
\arguments{
\item{spec}{A plot fragment or \code{vgspec} to set this guide on, or \code{NULL}
to start a new plot fragment with just these attributes.}

\item{label}{Color scale title string, e.g. for a legend header
(\code{colorLabel}).}

\item{tick_format}{Format specifier string or function for color legend
ticks (\code{colorTickFormat}).}

\item{...}{Additional plot-level attributes not covered above, by their
raw mosaic-spec camelCase name.}
}
\description{
\code{vg_guide_color()} sets the axis-guide properties mosaic-spec exposes
for the \code{color} channel (\code{colorLabel}, \code{colorTickFormat}). These are
already plot-level attributes that \code{\link[=vg_plot]{vg_plot()}}/\code{\link[=vg_attributes]{vg_attributes()}} accept
directly by their raw camelCase names; this is a discoverable,
snake_case-argument convenience layer on top of that -- the counterpart
of \code{\link[=vg_scale_color]{vg_scale_color()}} for the color channel's label/tick formatting
rather than its domain/range/palette.
}
\details{
This is named \code{vg_guide_color()} rather than \code{vg_legend_color()} because
\code{vg_legend_color()} already exists and means something different: it
adds an actual rendered color legend (a standalone/embedded legend
mark, \code{\link[=vg_legend]{vg_legend()}}) to the spec. \code{vg_guide_color()} only sets these two
plot attributes -- it neither shows nor requires a legend to be present.

Like \code{\link[=vg_plot]{vg_plot()}}, this can be piped in alongside marks/interactors -- it
only ever sets attributes on the current plot fragment, so it never
needs to come last in a chain.
}
\examples{
vg_mark_dot(x = ~a, y = ~b, fill = ~g) |>
  vg_guide_color(label = "Group")
}
\seealso{
Other guide functions:
\code{\link[=vg_guide_all]{vg_guide_all()}},
\code{\link[=vg_guide_facet]{vg_guide_facet()}},
\code{\link[=vg_guide_opacity]{vg_guide_opacity()}},
\code{\link[=vg_guide_position]{vg_guide_position()}},
\code{\link[=vg_guide_r]{vg_guide_r()}}
}
\concept{guide functions}
