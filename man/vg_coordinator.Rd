% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/coordinator.R
\name{vg_coordinator}
\alias{vg_coordinator}
\title{Options for mosaic's client-side coordinator}
\usage{
vg_coordinator(
  cache = TRUE,
  consolidate = TRUE,
  preaggregate = TRUE,
  preaggregate_schema = "mosaic",
  logging = c("all", "errors", "none")
)
}
\arguments{
\item{cache}{Whether to keep query results in the browser and reuse them
when the same query is asked again (default \code{TRUE}). Turn off to see every
query run against the database.}

\item{consolidate}{Whether to combine queries that differ only in what
they select into one query (default \code{TRUE}).}

\item{preaggregate}{Whether to speed up interactive filtering of
aggregated marks (histograms, heatmaps, ...) by materializing data cubes
in the database (default \code{TRUE}). Those tables are written to the
database's \code{preaggregate_schema}, so set this to \code{FALSE} when the database
is a real one, e.g., from \code{\link[=vg_duckdb_connector]{vg_duckdb_connector()}}, that shouldn't get a
schema of mosaic's making.}

\item{preaggregate_schema}{The database schema, a simple name (letters,
digits and underscores), that holds the pre-aggregated tables (default
\code{"mosaic"}).}

\item{logging}{How much mosaic writes to the browser console: \code{"all"} (the
default; per-query timing among other messages), \code{"errors"} (warnings and
errors only), or \code{"none"}.}
}
\value{
A \code{vg_coordinator} object.
}
\description{
Every plot on a page talks to the database through one mosaic
\emph{coordinator}, which caches query results, merges queries that can share a
result, and speeds up interactive filtering by pre-aggregating. The defaults
suit most graphics; \code{vg_coordinator()} collects the options for the times
they don't: to turn a feature off while tracking down a problem, to keep
mosaic's console output quiet, or to keep pre-aggregation out of a database
you care about.
}
\details{
Pass the result to \code{\link[=vg_widget]{vg_widget()}}/\code{\link[=vg_render]{vg_render()}} as \verb{coordinator =}, or set
\code{options(vgplotr.coordinator = vg_coordinator(...))} once for a whole
document (like \code{vgplotr.use_cache}; see \code{\link[=vg_widget]{vg_widget()}}).

The coordinator is shared by every widget on a page, and the first widget
to render creates it. Its options are the ones that count: a later widget
that asks for different ones is told so in the browser console and
otherwise ignored. To use a setting, give it to every widget (the
\code{vgplotr.coordinator} option does that).
}
\examples{
vg_coordinator(logging = "errors")
vg_coordinator(cache = FALSE, preaggregate = FALSE)
}
\seealso{
Other rendering functions:
\code{\link[=vg_iframe]{vg_iframe()}},
\code{\link[=vg_render]{vg_render()}},
\code{\link[=vg_snapshot]{vg_snapshot()}},
\code{\link[=vg_widget]{vg_widget()}}
}
\concept{rendering functions}
