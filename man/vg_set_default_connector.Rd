% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/connector.R
\name{vg_set_default_connector}
\alias{vg_set_default_connector}
\title{Set (or reset) this session's default rendering connector}
\usage{
vg_set_default_connector(connector = vg_wasm_connector())
}
\arguments{
\item{connector}{A \code{vg_connector} (e.g. \code{vg_duckdb_connector(con)}) to
use as the default from now on. Left at the default
(\code{\link[=vg_wasm_connector]{vg_wasm_connector()}}), or called with no arguments, this resets the
session back to its own built-in default.}
}
\value{
Invisibly, the previous default connector.
}
\description{
Every \code{vg_widget()}/\code{vg_render()} call that doesn't pass its own
\verb{connector =} argument uses this session's default connector --
\code{\link[=vg_wasm_connector]{vg_wasm_connector()}} until you change it. Useful when you're working
with a native DuckDB connection across many plots in one session/script
and don't want to repeat \code{connector = vg_duckdb_connector(con)} on every
call; a specific call can still override the session default by passing
its own \verb{connector =} explicitly.
}
\details{
This changes what \emph{unspecified} \verb{connector =} arguments mean for the
rest of the session -- it's deliberately something you opt into
explicitly, rather than vgplotr inferring it from whether a native
server happens to already be running (which would make a plain
\code{vg_render(spec)} behave differently depending on unrelated earlier
code, silently losing DuckDB-Wasm's self-contained/shareable output).
}
\seealso{
Other connector functions:
\code{\link[=vg_duckdb_connector]{vg_duckdb_connector()}},
\code{\link[=vg_duckdb_server_stop]{vg_duckdb_server_stop()}},
\code{\link[=vg_wasm_connector]{vg_wasm_connector()}}
}
\concept{connector functions}
