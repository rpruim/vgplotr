% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/widget.R
\name{vg_widget}
\alias{vg_widget}
\title{Build a vgspec into a live mosaic vgplot widget}
\usage{
vg_widget(
  spec,
  width = NULL,
  height = NULL,
  elementId = NULL,
  use_cache = vg_duckdb_cache_status()$cached,
  connector = vg_default_connector(),
  ...
)
}
\arguments{
\item{spec}{A \code{vgspec} with a layout of plots/vconcat()/hconcat(), or a
single JSON or YAML string holding an already-complete mosaic spec --
e.g. copied from mosaic's own example gallery, or the output of
\code{\link[=to_json]{to_json()}}/\code{\link[=to_yaml]{to_yaml()}} -- to render it directly without building it up
through \verb{vg_*()} calls first. Format is auto-detected (JSON if the
trimmed text starts with an opening brace or bracket, YAML otherwise).
Data references in
such a spec are left exactly as given -- unlike a \code{vgspec}'s own local
\verb{file =}/inline-data-frame sources, nothing here is read or embedded,
so a local file path needs to actually be fetchable (an http(s) URL,
or a path relative to wherever the rendered page is ultimately opened
from) for duckdb-wasm to load it in the browser.}

\item{width, height}{Widget sizing, in CSS units (e.g. \code{"100\%"}) or pixels.}

\item{elementId}{Optional DOM element ID for the widget.}

\item{use_cache}{Whether this graphic should use the local duckdb-wasm
engine cache (see \code{\link[=vg_cache_duckdb]{vg_cache_duckdb()}}) if one exists. Defaults to
whatever \code{\link[=vg_duckdb_cache_status]{vg_duckdb_cache_status()}} currently reports, so it tracks the
cache automatically; set to \code{FALSE} to force this one graphic to fetch
the engine from the CDN even when a cache is present, or \code{TRUE} to
request the cache explicitly (harmless, and equivalent to the default,
when no cache exists -- it just falls back to the CDN). Ignored when
\code{connector} isn't the default (there's no duckdb-wasm engine to cache).}

\item{connector}{Which database this graphic's SQL actually runs against:
\code{\link[=vg_wasm_connector]{vg_wasm_connector()}} (the default -- DuckDB-Wasm in the browser, fully
self-contained) or \code{\link[=vg_duckdb_connector]{vg_duckdb_connector()}} (a real, native DuckDB,
reached over a local server started automatically -- see its docs for
when you'd want this and its sharing/security caveats). Defaults to
the session's default connector (\code{\link[=vg_set_default_connector]{vg_set_default_connector()}}), so you
don't need to repeat this on every call once you've set one.}

\item{...}{Not used by \code{vg_widget()} itself. Accepted (and silently
ignored) so that \code{\link[=vg_render]{vg_render()}} can forward its own \code{...} uniformly to
whichever of \code{\link[=vg_widget]{vg_widget()}}/\code{\link[=vg_snapshot]{vg_snapshot()}}/\code{\link[=vg_iframe]{vg_iframe()}} ends up
handling a request, without erroring on arguments meant for one of
the other two (e.g. \verb{delay =}, only meaningful for \code{\link[=vg_snapshot]{vg_snapshot()}}).}
}
\description{
Returns a live, interactive \code{htmlwidget} -- useful directly
when you know the result will be viewed somewhere JavaScript can
run (the console, RStudio's viewer, an HTML vignette, a Quarto HTML
page). For a document that might \emph{also} be knitted to an output
that can't run JavaScript (plain markdown, PDF, Word, ...),
see \code{\link[=vg_render]{vg_render()}}, which picks automatically between this function, \code{\link[=vg_snapshot]{vg_snapshot()}},
and \code{\link[=vg_iframe]{vg_iframe()}}.
}
\details{
Renders the spec in the browser using a client-side DuckDB (via
duckdb-wasm). Most of the JS runtime this needs
(mosaic-spec/mosaic-core) is vendored with the package
(\verb{inst/htmlwidgets/lib/}), so it works offline out of the box.
The one exception is duckdb-wasm's actual database engine --
a ~35 MB compiled WebAssembly binary, too large to
ship with the package -- which is fetched from a CDN each time a graphic
is \emph{viewed}, unless you've called \code{\link[=vg_cache_duckdb]{vg_cache_duckdb()}} to cache it locally
(in which case that cached copy is used instead, and no CDN is involved
at all). If the cache doesn't exist, this function will offer to set it
up for you the first time you call it in an interactive session; see
\code{\link[=vg_cache_duckdb]{vg_cache_duckdb()}} for details, including how to silence that offer.

Mosaic-spec's own \code{meta} (title/description/credit -- see \code{\link[=vg_meta]{vg_meta()}})
isn't rendered by mosaic's JS runtime at all; it's inert, spec-level
metadata (\verb{@uwdata/mosaic-spec}'s own
\code{astToDOM()} never reads it). Since a title is nonetheless the
most commonly expected use of it, \code{vg_widget()} renders \code{meta$title}
itself -- as a caption prepended above the widget -- as a vgplotr-level
convenience layered on top of what mosaic itself does; \code{description}/
\code{credit} remain inert for now.
}
\seealso{
\code{\link[=vg_cache_duckdb]{vg_cache_duckdb()}}, \code{\link[=vg_duckdb_cache_status]{vg_duckdb_cache_status()}}

Other rendering functions:
\code{\link[=vg_iframe]{vg_iframe()}},
\code{\link[=vg_render]{vg_render()}},
\code{\link[=vg_snapshot]{vg_snapshot()}}
}
\concept{rendering functions}
