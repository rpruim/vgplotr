% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/on-render.R
\name{vg_on_render}
\alias{vg_on_render}
\title{Run JavaScript after a graphic renders}
\usage{
vg_on_render(spec, code)
}
\arguments{
\item{spec}{A \code{vgspec}.}

\item{code}{JavaScript, written with \code{\link[=js]{js()}}, that evaluates to a function.}
}
\value{
The \code{vgspec}, with the hook added.
}
\description{
Some behavior can't be written in a mosaic spec: a custom \emph{client} that runs
its own query and reacts to a selection (a live count of the selected rows,
say), or a \emph{clause} published to a selection from your own controls.
\code{vg_on_render()} adds a JavaScript function that vgplotr calls in the browser
once the graphic has rendered, with what it needs to do such things. Like
\code{\link[=js]{js()}}, of which it is a use, the code runs with the page's own privileges,
so only use code you wrote or trust.
}
\details{
The function receives one object, with these properties:
\itemize{
\item \code{element}: the rendered graphic's root DOM element. Append your own
elements to it (or read from it).
\item \code{params}: a \code{Map} from each param/selection name (see \code{\link[=vg_params]{vg_params()}}) to
its mosaic \code{Param} or \code{Selection}. A name that is only referred to, such
as an interactor's \code{as = param(brush)}, is included.
\item \code{coordinator}: the graphic's mosaic coordinator, which runs its queries.
\item \code{vg}: the vgplot API bound to that coordinator, including the SQL builders
(\code{vg.Query}, \code{vg.sql}, \code{vg.count()}, ...).
\item \code{mosaic}: the \code{mosaic-core} module, for \code{MosaicClient}, \code{Selection},
\code{Priority} and the clause helpers (\code{mosaic.clausePoint()},
\code{mosaic.clauseInterval()}, \code{mosaic.clauseMatch()}, ...).
\item \code{makeClient(options)}: mosaic's \code{makeClient()}, already connected to this
graphic's coordinator. Prefer it to \code{mosaic.makeClient()}, which defaults to
mosaic's global coordinator, a different one.
}

The function may be \code{async}, and may be added more than once (they run in
order). If it throws, the error is shown under the graphic.

Hooks are not part of mosaic-spec, so \code{\link[=to_json]{to_json()}}/\code{\link[=to_yaml]{to_yaml()}} leave them
out (with a warning), and they only apply to a spec built with
\code{\link[=vg_create]{vg_create()}}, not to a JSON/YAML string given to \code{\link[=vg_widget]{vg_widget()}}.

See \code{vignette("custom-javascript")} for a worked example.
}
\examples{
# a live count of the rows inside a brush
set.seed(1)
pts <- data.frame(x = runif(100), y = runif(100))
vg_create() |>
  vg_data("pts", pts) |>
  vg_mark_dot(x = ~x, y = ~y, data_from = "pts") |>
  vg_interval_x(as = param(brush)) |>
  vg_on_render(js("({ element, params, makeClient, vg }) => {
    const label = element.appendChild(document.createElement('div'));
    makeClient({
      selection: params.get('brush'),
      query: filter => vg.Query.from('pts').select({ n: vg.count() }).where(filter),
      queryResult: data => { label.textContent = [...data][0].n + ' selected'; }
    });
  }"))
}
\seealso{
Other spec functions:
\code{\link[=param]{param()}},
\code{\link[=vg_attributes]{vg_attributes()}},
\code{\link[=vg_config]{vg_config()}},
\code{\link[=vg_create]{vg_create()}},
\code{\link[=vg_data]{vg_data()}},
\code{\link[=vg_data_registry]{vg_data_registry()}},
\code{\link[=vg_example_url]{vg_example_url()}},
\code{\link[=vg_meta]{vg_meta()}},
\code{\link[=vg_params]{vg_params()}},
\code{\link[=vg_plot_defaults]{vg_plot_defaults()}},
\code{\link[=vg_selection]{vg_selection()}}
}
\concept{spec functions}
