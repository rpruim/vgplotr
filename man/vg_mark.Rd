% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/mark.R
\name{vg_mark}
\alias{vg_mark}
\title{Add a mark (a single visual layer, e.g. points or a line) to a plot}
\usage{
vg_mark(spec = NULL, mark, ...)
}
\arguments{
\item{spec}{A plot fragment or \code{vgspec} to add this mark to, \code{NULL} to
start a new plot with just this mark, or a data frame -- shorthand for
\verb{vg_create() |> vg_data(data = spec) |> vg_mark_<mark>(...)}, i.e.
starting a brand-new spec with \code{spec} registered as its data source
(named per \code{\link[=vg_data]{vg_data()}}) and used automatically as this mark's own
\code{data_from} -- e.g. \code{some_data |> vg_mark_dot(x = ~a, y = ~b)}. A
\verb{data_from =} string given explicitly names the registered source
instead of the \code{\link[=vg_data]{vg_data()}} default (handy since it's also what later
marks in the chain need for their own \verb{data_from =}); giving anything
else there (an integer index, a literal vector, a \code{\link[=param]{param()}}) doesn't
name a source and is left alone, meant for a plot fragment/\code{vgspec}
\code{spec} instead.

Whether \code{spec} is a data frame or an existing \code{vgspec}, \verb{data_from =}
can also be a length-1 R integer (\code{1L}, \code{2L}, ...; note the \code{L} --
\code{data_from = 1} is a double, mosaic's own literal inline-data
shorthand, see below) giving a 1-based index into the spec's data
sources in registration order, negative counting from the end (e.g.
\code{-1L} for the most recently registered one). Left unset entirely, any
mark that takes data at all defaults to \code{data_from = 1L}, the first
registered source. This is what lets a whole multi-layer plot share
one data source without repeating \verb{data_from =} on every mark, e.g.
\code{some_data |> vg_mark_dot(x = ~a, y = ~b) |> vg_mark_line_y(x = ~a, y = ~b)}. Both only look at the data registered so far when \emph{this} mark is
added; they don't reach back to fill in earlier marks if a data source
is registered later.}

\item{mark}{The mosaic mark type, e.g. \code{"dot"}, \code{"lineY"}.}

\item{...}{Encodings (e.g. \code{x = ~var1}), mark options, and/or plot-level
attributes (\verb{width =}, \verb{name =}, ...). See \code{\link[=vg_plot]{vg_plot()}} for how
plot-level attributes from multiple marks are combined.}
}
\description{
This is the generic, low-level constructor that the \code{vg_mark_dot()},
\code{vg_mark_line_y()}, etc. convenience functions -- one per mark type in
mosaic's own JSON schema, generated by \code{data-raw/update-schema.R} into
\code{R/marks-generated.R} -- are built on top of.
}
