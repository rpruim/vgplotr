% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/create.R
\name{vg_create}
\alias{vg_create}
\title{Create a new mosaic vgplot specification}
\usage{
vg_create(data = NULL, ...)
}
\arguments{
\item{data}{Optional default data source name/data frame for the spec.
(Full support for passing an in-memory data frame is not yet
implemented -- see the design notes.)}

\item{...}{Top-level plot defaults (mosaic-spec's \code{plotDefaults}): applied
to every plot in the spec, as opposed to one plot's own attributes (set
via \code{\link[=vg_plot]{vg_plot()}}) or a single mark's encodings. Equivalent to calling
\code{\link[=vg_attributes]{vg_attributes()}} right after \code{vg_create()}.}
}
\description{
\code{vg_create()} starts a new \code{vgspec}, the top-level object that other
\verb{vg_*()} functions are piped through to add data sources, params, marks,
interactors, and layout. Marks and interactors can be piped directly onto
a freshly created spec (e.g. \code{vg_create() |> vg_dot(x = ~a, y = ~b)}) as
long as the spec only needs a single plot; a spec with multiple plots
needs an explicit layout (\code{vg_vconcat()}/\code{vg_hconcat()}, not yet
implemented).
}
