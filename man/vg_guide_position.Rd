% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/guide.R
\name{vg_guide_position}
\alias{vg_guide_position}
\alias{vg_guide_x}
\alias{vg_guide_y}
\title{Set axis-guide properties for a position scale (x or y)}
\usage{
vg_guide_position(
  spec = NULL,
  which = c("x", "y"),
  position = vg_unset,
  ticks = vg_unset,
  tick_spacing = vg_unset,
  tick_size = vg_unset,
  tick_padding = vg_unset,
  tick_format = vg_unset,
  tick_rotate = vg_unset,
  grid = vg_unset,
  line = vg_unset,
  label = vg_unset,
  label_anchor = vg_unset,
  label_offset = vg_unset,
  label_arrow = vg_unset,
  font_variant = vg_unset,
  aria_label = vg_unset,
  aria_description = vg_unset,
  ...
)

vg_guide_x(
  spec = NULL,
  position = vg_unset,
  ticks = vg_unset,
  tick_spacing = vg_unset,
  tick_size = vg_unset,
  tick_padding = vg_unset,
  tick_format = vg_unset,
  tick_rotate = vg_unset,
  grid = vg_unset,
  line = vg_unset,
  label = vg_unset,
  label_anchor = vg_unset,
  label_offset = vg_unset,
  label_arrow = vg_unset,
  font_variant = vg_unset,
  aria_label = vg_unset,
  aria_description = vg_unset,
  ...
)

vg_guide_y(
  spec = NULL,
  position = vg_unset,
  ticks = vg_unset,
  tick_spacing = vg_unset,
  tick_size = vg_unset,
  tick_padding = vg_unset,
  tick_format = vg_unset,
  tick_rotate = vg_unset,
  grid = vg_unset,
  line = vg_unset,
  label = vg_unset,
  label_anchor = vg_unset,
  label_offset = vg_unset,
  label_arrow = vg_unset,
  font_variant = vg_unset,
  aria_label = vg_unset,
  aria_description = vg_unset,
  ...
)
}
\arguments{
\item{spec}{A plot fragment or \code{vgspec} to set this guide on, or \code{NULL} to
start a new plot fragment with just these attributes.}

\item{which}{Which axis this sets: \code{"x"} or \code{"y"}.}

\item{position}{Axis position/visibility: \code{"top"}/\code{"bottom"} for \code{x},
\code{"left"}/\code{"right"} for \code{y}, or \code{NULL}/\code{FALSE} to hide it
(\code{xAxis}/\code{yAxis}).}

\item{ticks}{Number of ticks, or an array of explicit tick values
(\code{xTicks}/\code{yTicks}).}

\item{tick_spacing}{Target distance between ticks in pixels
(\code{xTickSpacing}/\code{yTickSpacing}).}

\item{tick_size}{Length of tick marks in pixels (\code{xTickSize}/\code{yTickSize}).}

\item{tick_padding}{Distance between a tick mark and its label in pixels
(\code{xTickPadding}/\code{yTickPadding}).}

\item{tick_format}{Format specifier string (e.g. \code{"\%Y"}, \code{"d"}, \code{".2f"})
or function for tick labels (\code{xTickFormat}/\code{yTickFormat}).}

\item{tick_rotate}{Rotation angle of tick labels in degrees
(\code{xTickRotate}/\code{yTickRotate}).}

\item{grid}{Boolean, or a line stroke count, to render grid lines along
this axis (\code{xGrid}/\code{yGrid}).}

\item{line}{Boolean; whether to draw the axis line itself
(\code{xLine}/\code{yLine}).}

\item{label}{Axis title string (\code{xLabel}/\code{yLabel}).}

\item{label_anchor}{Position anchor for the label text: \code{"left"},
\code{"center"}, or \code{"right"} (\code{xLabelAnchor}/\code{yLabelAnchor}).}

\item{label_offset}{Distance between the axis and its label in pixels
(\code{xLabelOffset}/\code{yLabelOffset}).}

\item{label_arrow}{Whether/how to draw an arrow on the axis label
(\code{xLabelArrow}/\code{yLabelArrow}).}

\item{font_variant}{CSS font-variant for tick labels, e.g.
\code{"tabular-nums"} (\code{xFontVariant}/\code{yFontVariant}).}

\item{aria_label}{ARIA accessibility label for the axis
(\code{xAriaLabel}/\code{yAriaLabel}).}

\item{aria_description}{ARIA accessibility description for the axis
(\code{xAriaDescription}/\code{yAriaDescription}).}

\item{...}{Additional plot-level attributes not covered above, by their
raw mosaic-spec camelCase name.}
}
\description{
\code{vg_guide_x()}/\code{vg_guide_y()} set the axis-guide properties mosaic-spec
exposes per positional axis (\code{xAxis}, \code{xTicks}, ... -- substitute \code{y} for
the vertical axis). Like the \code{xScale}/\code{yScale} properties handled by
\code{\link[=vg_scale_position]{vg_scale_position()}}, these are already plot-level attributes that
\code{\link[=vg_plot]{vg_plot()}}/\code{\link[=vg_attributes]{vg_attributes()}} accept directly by their raw camelCase
names; this is a discoverable, snake_case-argument convenience layer on
top of that. \code{vg_guide_x()} and \code{vg_guide_y()} are thin wrappers around
the generic \code{vg_guide_position()}.
}
\details{
These are named \verb{vg_guide_*()} rather than \verb{vg_axis_*()} for historical
reasons: mosaic-spec's \code{axisX}/\code{axisY} \emph{mark} (a drawable,
independently-styled axis) used to be generated as \code{vg_axis_x()}/
\code{vg_axis_y()}, colliding with the plot-attribute guide setters here.
Every mark constructor is now prefixed \code{vg_mark_} instead (see
\code{R/marks-generated.R}), specifically to avoid this kind of collision, so
\code{vg_axis_x()}/\code{vg_axis_y()} are free again -- but \code{vg_guide_x()}/
\code{vg_guide_y()} haven't been renamed back to them, to avoid further API
churn. \verb{vg_guide_*()} sets the plain \code{xAxis}/\code{yAxis} guide attributes
that appear automatically alongside a plot's marks, which is a
different (if related) thing from mosaic's \code{axisX}/\code{axisY} mark (now
\code{vg_mark_axis_x()}/\code{vg_mark_axis_y()}). Same story for the analogous
facet guides, \code{\link[=vg_guide_facet]{vg_guide_facet()}} (vs. \code{vg_mark_axis_fx()}/
\code{vg_mark_axis_fy()}, mosaic's \code{axisFx}/\code{axisFy} marks).

Like \code{\link[=vg_plot]{vg_plot()}}, this can be piped in alongside marks/interactors -- it
only ever sets attributes on the current plot fragment, so it never needs
to come last in a chain.
}
\examples{
vg_mark_dot(x = ~a, y = ~b) |>
  vg_guide_x(label = "A", grid = TRUE) |>
  vg_guide_y(label = "B", tick_format = ".0f")
}
\seealso{
Other guide functions:
\code{\link[=vg_guide_color]{vg_guide_color()}},
\code{\link[=vg_guide_facet]{vg_guide_facet()}},
\code{\link[=vg_guide_opacity]{vg_guide_opacity()}},
\code{\link[=vg_guide_r]{vg_guide_r()}}
}
\concept{guide functions}
