% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/intervals.R
\name{vg_intervals}
\alias{vg_intervals}
\alias{vg_years}
\alias{vg_months}
\alias{vg_days}
\alias{vg_hours}
\alias{vg_minutes}
\alias{vg_seconds}
\alias{vg_milliseconds}
\alias{vg_microseconds}
\title{Date/time intervals for window frames}
\usage{
vg_years(n)

vg_months(n)

vg_days(n)

vg_hours(n)

vg_minutes(n)

vg_seconds(n)

vg_milliseconds(n)

vg_microseconds(n)
}
\arguments{
\item{n}{A single non-negative number of units, or a \code{\link[=param]{param()}}.}
}
\value{
A \code{vg_interval} object, serialized as \verb{\{<unit>: n\}}.
}
\description{
\code{vg_years()}, \code{vg_months()}, \code{vg_days()}, \code{vg_hours()}, \code{vg_minutes()},
\code{vg_seconds()}, \code{vg_milliseconds()} and \code{vg_microseconds()} write a span of
time -- mosaic-spec's interval transforms (\code{{days: 7}}) -- where a window
frame wants an offset in time units instead of a row count. The frame
options \code{rows}, \code{range} and \code{groups} of a window or aggregate transform
(see \link{vg_transforms}) each take a pair of offsets, measured from the
current row: how far the frame reaches \emph{before} it, then how far \emph{after}
it. So \code{range = list(vg_days(6), vg_days(0))} is a trailing 7-day window:
the current day and the 6 before it.
}
\details{
Unlike the transforms in \link{vg_transforms}, the argument is a plain value,
not a column, so \code{vg_days(6)} is evaluated as ordinary R. An offset is a
distance, so it can't be negative. A \code{range} frame also needs \code{orderby}
(a column name, as a string) to say which column the distances are
measured along.

Either offset can also be a plain number (\code{0} means the current row) or
\code{NULL} (unbounded), so \code{range = list(vg_days(6), 0)} works too.
}
\examples{
# a 7-day trailing average
y <- ~ vg_avg(close, orderby = "date", range = list(vg_days(6), 0))
}
\seealso{
Other transform functions:
\code{\link[=js]{js()}},
\code{\link[=sql]{sql()}},
\code{\link{vg_transforms}}
}
\concept{transform functions}
