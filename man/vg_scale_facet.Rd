% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/scale.R
\name{vg_scale_facet}
\alias{vg_scale_facet}
\alias{vg_scale_fx}
\alias{vg_scale_fy}
\title{Set facet scale properties (fx or fy)}
\usage{
vg_scale_facet(
  spec = NULL,
  which = c("fx", "fy"),
  domain = vg_unset,
  range = vg_unset,
  reverse = vg_unset,
  round = vg_unset,
  padding = vg_unset,
  padding_inner = vg_unset,
  padding_outer = vg_unset,
  align = vg_unset,
  inset = vg_unset,
  inset_left = vg_unset,
  inset_right = vg_unset,
  inset_top = vg_unset,
  inset_bottom = vg_unset,
  ...
)

vg_scale_fx(
  spec = NULL,
  domain = vg_unset,
  range = vg_unset,
  reverse = vg_unset,
  round = vg_unset,
  padding = vg_unset,
  padding_inner = vg_unset,
  padding_outer = vg_unset,
  align = vg_unset,
  inset = vg_unset,
  inset_left = vg_unset,
  inset_right = vg_unset,
  inset_top = vg_unset,
  inset_bottom = vg_unset,
  ...
)

vg_scale_fy(
  spec = NULL,
  domain = vg_unset,
  range = vg_unset,
  reverse = vg_unset,
  round = vg_unset,
  padding = vg_unset,
  padding_inner = vg_unset,
  padding_outer = vg_unset,
  align = vg_unset,
  inset = vg_unset,
  inset_left = vg_unset,
  inset_right = vg_unset,
  inset_top = vg_unset,
  inset_bottom = vg_unset,
  ...
)
}
\arguments{
\item{spec}{A plot fragment or \code{vgspec} to set this scale on, or \code{NULL} to
start a new plot fragment with just these attributes.}

\item{which}{Which facet scale this sets: \code{"fx"} or \code{"fy"}.}

\item{domain}{Array of facet group categories (\code{fxDomain}/\code{fyDomain}).}

\item{range}{Pixel range for the overall faceting space
(\code{fxRange}/\code{fyRange}).}

\item{reverse}{Boolean; reverse the facet order (\code{fxReverse}/\code{fyReverse}).}

\item{round}{Boolean; round output values to the nearest pixel
(\code{fxRound}/\code{fyRound}).}

\item{padding}{Outer padding between facet subplots, 0 to 1
(\code{fxPadding}/\code{fyPadding}).}

\item{padding_inner}{Inner padding between facet subplots, 0 to 1
(\code{fxPaddingInner}/\code{fyPaddingInner}).}

\item{padding_outer}{Outer padding at the facet space's edges, 0 to 1
(\code{fxPaddingOuter}/\code{fyPaddingOuter}).}

\item{align}{Alignment of facet bands, 0 to 1 (\code{fxAlign}/\code{fyAlign}).}

\item{inset}{Pixel inset applied to both ends of the facet range
(\code{fxInset}/\code{fyInset}).}

\item{inset_left, inset_right}{Pixel inset at the left/right end of the
facet range; only meaningful for \code{which = "fx"}
(\code{fxInsetLeft}/\code{fxInsetRight}).}

\item{inset_top, inset_bottom}{Pixel inset at the top/bottom end of the
facet range; only meaningful for \code{which = "fy"}
(\code{fyInsetTop}/\code{fyInsetBottom}).}

\item{...}{Additional plot-level attributes not covered above, by their
raw mosaic-spec camelCase name.}
}
\description{
\code{vg_scale_fx()}/\code{vg_scale_fy()} set the scale properties mosaic-spec
exposes per facet axis (\code{fxDomain}, \code{fxPadding}, ... -- substitute \code{fy}
for the row facet axis). Facet scales are always band scales, so this is
a strict subset of \code{\link[=vg_scale_position]{vg_scale_position()}}'s properties: no \code{type}, and
none of the properties specific to continuous/log/pow scales (\code{nice},
\code{zero}, \code{clamp}, \code{base}, \code{exponent}, \code{constant}, \code{percent}).
\code{vg_scale_fx()} and \code{vg_scale_fy()} are thin wrappers around the generic
\code{vg_scale_facet()}.
}
\examples{
vg_dot(x = ~a, y = ~b, fx = ~g) |>
  vg_scale_fx(padding = 0.1)
}
\seealso{
Other scale functions:
\code{\link[=vg_scale_position]{vg_scale_position()}}
}
\concept{scale functions}
