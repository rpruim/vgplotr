% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/guide.R
\name{vg_guide_r}
\alias{vg_guide_r}
\alias{vg_guide_radius}
\title{Set the axis-guide property for the radius scale}
\usage{
vg_guide_r(spec = NULL, label = vg_unset, ...)

vg_guide_radius(spec = NULL, label = vg_unset, ...)
}
\arguments{
\item{spec}{A plot fragment or \code{vgspec} to set this guide on, or \code{NULL}
to start a new plot fragment with just these attributes.}

\item{label}{Radius scale title string, e.g. for a legend header
(\code{rLabel}).}

\item{...}{Additional plot-level attributes not covered above, by their
raw mosaic-spec camelCase name.}
}
\description{
\code{vg_guide_r()} (aliased as \code{vg_guide_radius()}) sets the axis-guide
property mosaic-spec exposes for the \code{r} channel (\code{rLabel} -- mosaic
doesn't define an \code{rTickFormat}). This is already a plot-level attribute
that \code{\link[=vg_plot]{vg_plot()}}/\code{\link[=vg_attributes]{vg_attributes()}} accepts directly by its raw camelCase
name; this is a discoverable, snake_case-argument convenience layer on
top of that -- the counterpart of \code{\link[=vg_scale_r]{vg_scale_r()}}/\code{\link[=vg_scale_radius]{vg_scale_radius()}} for
the radius channel's label rather than its domain/range.
}
\details{
See \code{\link[=vg_guide_color]{vg_guide_color()}} for why this is \code{vg_guide_r()}/\code{vg_guide_radius()}
rather than a \code{vg_legend_r()}/\code{vg_legend_radius()} -- mosaic doesn't
have a dedicated \code{r}-typed legend mark to collide with here (radius/size
is usually shown via \code{\link[=vg_legend_symbol]{vg_legend_symbol()}} instead), but the naming stays
consistent with \code{\link[=vg_guide_color]{vg_guide_color()}}/\code{\link[=vg_guide_opacity]{vg_guide_opacity()}}.

Like \code{\link[=vg_plot]{vg_plot()}}, this can be piped in alongside marks/interactors -- it
only ever sets attributes on the current plot fragment, so it never
needs to come last in a chain.
}
\examples{
vg_mark_dot(x = ~a, y = ~b, r = ~g) |>
  vg_guide_r(label = "Group")
}
\seealso{
Other guide functions:
\code{\link[=vg_guide_color]{vg_guide_color()}},
\code{\link[=vg_guide_facet]{vg_guide_facet()}},
\code{\link[=vg_guide_opacity]{vg_guide_opacity()}},
\code{\link[=vg_guide_position]{vg_guide_position()}}
}
\concept{guide functions}
