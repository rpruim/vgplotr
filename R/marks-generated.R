# Generated by data-raw/update-schema.R from mosaic's JSON schema.
# DO NOT EDIT BY HAND -- rerun that script instead.
#
# One vg_mark_<mark>() wrapper per mark type in the mosaic-spec schema,
# each with a real named argument per property that mark accepts (so tab
# completion and ?vg_mark_dot show the actual options) -- e.g.,
# vg_mark_dot(spec, x = ~a, y = ~b) is vg_mark(spec, "dot", x = ~a, y =
# ~b) with x/y (and every other dot property) as real, documented
# arguments instead of an opaque `...`. Every mark constructor starts
# with vg_mark_, so they can all be found by listing functions with that
# prefix -- this also keeps a mark name from ever colliding with an
# unrelated vg_<name>() function elsewhere in the package.

#' The area mark.
#'
#' See [vg_value_types] for what the `<...>` notation below (`column`, `param()`, ...) means.
#'
#' @param spec A plot fragment or `vgspec` to add this mark to, or `NULL` to start a new plot with just this mark.
#' @param formula A shorthand for this mark's position channels (`x`, `y`, `fx`, `fy`, and paired `x1`/`x2` or `y1`/`y2`), e.g., `Sepal.Length ~ Sepal.Width | ~ Species` for `y = ~Sepal.Length, x = ~Sepal.Width, fx = ~Species`. See [vg_mark()] for the full grammar.
#' @param aria_description `<string | param()>` The aria-description; a constant textual description.
#' @param aria_hidden `<string | param()>` The aria-hidden state; a constant indicating whether the element is exposed to an accessibility API.
#' @param aria_label `<column | literal | transform() | sql()/agg()>` The aria-label; a channel specifying short textual labels representing the value in the accessibility tree.
#' @param channels `<list>` Additional named channels, for example to include in a tooltip.
#' @param clip `<"frame" | "sphere" | boolean | NULL | param()>` How to clip the mark; one of: - *frame* or true - clip to the plot’s frame (inner area) - *sphere* - clip to the projected sphere (*e.g.*, front hemisphere) - null or false - do not clip The *sphere* clip option requires a geographic projection.
#' @param curve `<"basis" | "basis-closed" | "basis-open" | "bundle" | "bump-x" | "bump-y" | "cardinal" | "cardinal-closed" | "cardinal-open" | "catmull-rom" | ... | param()>` The curve (interpolation) method for connecting adjacent points.
#' @param dx `<number | param()>` The horizontal offset in pixels; a constant option.
#' @param dy `<number | param()>` The vertical offset in pixels; a constant option.
#' @param facet `<"auto" | "include" | "exclude" | "super" | boolean | NULL | param()>` Whether to enable or disable faceting; one of: - *auto* (default) - automatically determine if this mark should be faceted - *include* (or true) - draw the subset of the mark’s data in the current facet - *exclude* - draw the subset of the mark’s data *not* in the current facet - *super* - draw this mark in a single frame that covers all facets - null (or false) - repeat this mark’s data across all facets (*i.e.*, no faceting) When a mark uses *super* faceting, it is not allowed to use position scales (*x*, *y*, *fx*, or *fy*); *super* faceting is intended for decorations, such as labels and legends.
#' @param facet_anchor `<"top" | "right" | "bottom" | "left" | "top-left" | "top-right" | "bottom-left" | "bottom-right" | "top-empty" | "right-empty" | "bottom-empty" | "left-empty" | "empty" | NULL | param()>` How to place the mark with respect to facets; one of: - null (default for most marks) - display the mark in each non-empty facet - *top*, *right*, *bottom*, or *left* - display the mark only in facets on the given side - *top-empty*, *right-empty*, *bottom-empty*, or *left-empty* (default for axis marks) - display the mark only in facets that have empty space on the given side: either the margin, or an empty facet - *empty* - display the mark in empty facets only
#' @param fill `<column | literal | transform() | sql()/agg() | list(value=, ...) | param()>` The fill; a constant CSS color string, or a channel typically bound to the *color* scale.
#' @param fill_opacity `<column | literal | transform() | sql()/agg() | list(value=, ...) | param()>` The fill-opacity; a constant number between 0 and 1, or a channel typically bound to the *opacity* scale.
#' @param filter `<column | literal | transform() | sql()/agg()>` Applies a transform to filter the mark’s index according to the given channel values; only truthy values are retained.
#' @param fx `<column | literal | transform() | sql()/agg()>` The horizontal facet position channel, for mark-level faceting, bound to the *fx* scale.
#' @param fy `<column | literal | transform() | sql()/agg()>` The vertical facet position channel, for mark-level faceting, bound to the *fy* scale.
#' @param href `<column | literal | transform() | sql()/agg()>` The href; a channel specifying URLs for clickable links.
#' @param image_filter `<string | param()>` A CSS filter; a constant string used to adjust the rendering of images, such as *blur(5px)*.
#' @param margin `<number | param()>` Shorthand to set the same default for all four mark margins: **marginTop**, **marginRight**, **marginBottom**, and **marginLeft**; typically defaults to 0, except for axis marks.
#' @param margin_bottom `<number | param()>` The mark’s bottom margin; the minimum distance in pixels between the bottom edges of the inner and outer plot area.
#' @param margin_left `<number | param()>` The mark’s left margin; the minimum distance in pixels between the left edges of the inner and outer plot area.
#' @param margin_right `<number | param()>` The mark’s right margin; the minimum distance in pixels between the right edges of the mark’s inner and outer plot area.
#' @param margin_top `<number | param()>` The mark’s top margin; the minimum distance in pixels between the top edges of the inner and outer plot area.
#' @param mix_blend_mode `<string | param()>` The mix-blend-mode; a constant string specifying how to blend content such as *multiply*.
#' @param offset `<"center" | "normalize" | "wiggle" | NULL | param()>` After stacking, an optional **offset** can be applied to translate and scale stacks, say to produce a streamgraph; defaults to null for a zero baseline (**y** = 0 for stackY, and **x** = 0 for stackX).
#' @param opacity `<column | literal | transform() | sql()/agg() | list(value=, ...)>` The opacity; a constant between 0 and 1, or a channel typically bound to the *opacity* scale.
#' @param order `<"value" | "x" | "y" | "z" | "sum" | "appearance" | "inside-out" | "-value" | "-x" | "-y" | ... | NULL | param()>` The order in which stacks are layered; one of: - null (default) for input order - a named stack order method such as *inside-out* or *sum* - a field name, for natural order of the corresponding values - a function of data, for natural order of the corresponding values - an array of explicit **z** values in the desired order If the *wiggle* **offset** is used, as for a streamgraph, the default changes to *inside-out*.
#' @param paint_order `<string | param()>` The paint-order; a constant string specifying the order in which the **fill**, **stroke**, and any markers are drawn; defaults to *normal*, which draws the fill, then stroke, then markers; defaults to *stroke* for the text mark to create a “halo” around text to improve legibility.
#' @param pointer_events `<string | param()>` The pointer-events property; a constant string such as *none*.
#' @param reverse `<boolean | param()>` Applies a transform to reverse the order of the mark’s index, say for reverse input order.
#' @param select `<"first" | "last" | "maxX" | "maxY" | "minX" | "minY" | "nearest" | "nearestX" | "nearestY">` Applies a filter transform after data is loaded to highlight selected values only.
#' @param shape_rendering `<string | param()>` The shape-rendering; a constant string such as *crispEdges*.
#' @param sort `<column | literal | transform() | sql()/agg() | list>` Either applies a transform to sort the mark’s index by the specified channel values, or imputes ordinal scale domains from this mark’s channels.
#' @param stroke `<column | literal | transform() | sql()/agg() | list(value=, ...) | param()>` The stroke; a constant CSS color string, or a channel typically bound to the *color* scale.
#' @param stroke_dasharray `<string | number | param()>` The stroke-dasharray; a constant number indicating the length in pixels of alternating dashes and gaps, or a constant string of numbers separated by spaces or commas (_e.g._, *10 2* for dashes of 10 pixels separated by gaps of 2 pixels), or *none* (the default) for no dashing : https://developer.mozilla.org/en-US/docs/Web/SVG/Attribute/stroke-dasharray
#' @param stroke_dashoffset `<string | number | param()>` The stroke-dashoffset; a constant indicating the offset in pixels of the first dash along the stroke; defaults to zero.
#' @param stroke_linecap `<string | param()>` The stroke-linecap; a constant specifying how to cap stroked paths, such as *butt*, *round*, or *square*.
#' @param stroke_linejoin `<string | param()>` The stroke-linejoin; a constant specifying how to join stroked paths, such as *bevel*, *miter*, *miter-clip*, or *round*.
#' @param stroke_miterlimit `<number | param()>` The stroke-miterlimit; a constant number specifying how to limit the length of *miter* joins on stroked paths.
#' @param stroke_opacity `<column | literal | transform() | sql()/agg() | list(value=, ...)>` The stroke-opacity; a constant between 0 and 1, or a channel typically bound to the *opacity* scale.
#' @param stroke_width `<column | literal | transform() | sql()/agg() | list(value=, ...)>` The stroke-width; a constant number in pixels, or a channel.
#' @param target `<string | param()>` The target; a constant string specifying the target window (_e.g._, *_blank*) for clickable links; used in conjunction with the **href** option.
#' @param tension `<number | param()>` The tension option only has an effect on bundle, cardinal and Catmull–Rom splines (*bundle*, *cardinal*, *cardinal-open*, *cardinal-closed*, *catmull-rom*, *catmull-rom-open*, and *catmull-rom-closed*).
#' @param tip `<boolean | "x" | "y" | "xy" | list | param()>` Whether to generate a tooltip for this mark, and any tip options.
#' @param title `<column | literal | transform() | sql()/agg()>` The title; a channel specifying accessible, short textual descriptions as strings (possibly with newlines).
#' @param x1 `<column | literal | transform() | sql()/agg() | list(value=, ...)>` The required primary (starting, often left) horizontal position channel, representing the area’s baseline, typically bound to the *x* scale.
#' @param x2 `<column | literal | transform() | sql()/agg() | list(value=, ...)>` The optional secondary (ending, often right) horizontal position channel, representing the area’s topline, typically bound to the *x* scale; if not specified, **x1** is used.
#' @param y1 `<column | literal | transform() | sql()/agg() | list(value=, ...)>` The required primary (starting, often bottom) vertical position channel, representing the area’s baseline, typically bound to the *y* scale.
#' @param y2 `<column | literal | transform() | sql()/agg() | list(value=, ...)>` The optional secondary (ending, often top) vertical position channel, representing the area’s topline, typically bound to the *y* scale; if not specified, **y1** is used.
#' @param z `<column | literal | transform() | sql()/agg()>` An optional ordinal channel for grouping data into (possibly stacked) series to be drawn as separate areas; defaults to **fill** if a channel, or **stroke** if a channel.
#' @param ... Additional options or plot-level attributes.
#' @param data_from The name of the data source this mark reads from (see [vg_data()]); a length-1 nonzero R integer (`1L`, `-1L`, ...; note the `L`) giving a 1-based index into the spec's registered data sources instead, negative counting from the end (e.g., `-1L` for the most recently registered one); or a literal vector of values to use as inline data directly (mosaic-spec's `"data": [...]` shorthand, e.g., for a single reference line -- a bare double like `0`/`c(0)`, or `0L` itself (never a valid index), means this, not an index). Left unset, defaults to the first registered data source (equivalent to `data_from = 1L`) for any mark type that takes data at all.
#' @param filter_by A Param/Selection (e.g., from [param()]) to filter this mark's data by.
#' @param data_optimize A flag (default `TRUE`) to enable mark-specific query optimizations for this mark's data; set `FALSE` to disable them (mosaic-spec's `data: {optimize: false}`).
#' @family mark functions
#' @export
vg_mark_area <- function(spec = NULL, formula = vg_unset, aria_description = vg_unset, aria_hidden = vg_unset, aria_label = vg_unset, channels = vg_unset, clip = vg_unset, curve = vg_unset, dx = vg_unset, dy = vg_unset, facet = vg_unset, facet_anchor = vg_unset, fill = vg_unset, fill_opacity = vg_unset, filter = vg_unset, fx = vg_unset, fy = vg_unset, href = vg_unset, image_filter = vg_unset, margin = vg_unset, margin_bottom = vg_unset, margin_left = vg_unset, margin_right = vg_unset, margin_top = vg_unset, mix_blend_mode = vg_unset, offset = vg_unset, opacity = vg_unset, order = vg_unset, paint_order = vg_unset, pointer_events = vg_unset, reverse = vg_unset, select = vg_unset, shape_rendering = vg_unset, sort = vg_unset, stroke = vg_unset, stroke_dasharray = vg_unset, stroke_dashoffset = vg_unset, stroke_linecap = vg_unset, stroke_linejoin = vg_unset, stroke_miterlimit = vg_unset, stroke_opacity = vg_unset, stroke_width = vg_unset, target = vg_unset, tension = vg_unset, tip = vg_unset, title = vg_unset, x1 = vg_unset, x2 = vg_unset, y1 = vg_unset, y2 = vg_unset, z = vg_unset, ..., data_from = vg_unset, filter_by = vg_unset, data_optimize = vg_unset) {
  vg_mark_(spec, "area", formula = formula, ariaDescription = aria_description, ariaHidden = aria_hidden, ariaLabel = aria_label, channels = channels, clip = clip, curve = curve, dx = dx, dy = dy, facet = facet, facetAnchor = facet_anchor, fill = fill, fillOpacity = fill_opacity, filter = filter, fx = fx, fy = fy, href = href, imageFilter = image_filter, margin = margin, marginBottom = margin_bottom, marginLeft = margin_left, marginRight = margin_right, marginTop = margin_top, mixBlendMode = mix_blend_mode, offset = offset, opacity = opacity, order = order, paintOrder = paint_order, pointerEvents = pointer_events, reverse = reverse, select = select, shapeRendering = shape_rendering, sort = sort, stroke = stroke, strokeDasharray = stroke_dasharray, strokeDashoffset = stroke_dashoffset, strokeLinecap = stroke_linecap, strokeLinejoin = stroke_linejoin, strokeMiterlimit = stroke_miterlimit, strokeOpacity = stroke_opacity, strokeWidth = stroke_width, target = target, tension = tension, tip = tip, title = title, x1 = x1, x2 = x2, y1 = y1, y2 = y2, z = z, ..., data_from = data_from, filter_by = filter_by, data_optimize = data_optimize)
}

#' The areaX mark.
#'
#' See [vg_value_types] for what the `<...>` notation below (`column`, `param()`, ...) means.
#'
#' @param spec A plot fragment or `vgspec` to add this mark to, or `NULL` to start a new plot with just this mark.
#' @param formula A shorthand for this mark's position channels (`x`, `y`, `fx`, `fy`, and paired `x1`/`x2` or `y1`/`y2`), e.g., `Sepal.Length ~ Sepal.Width | ~ Species` for `y = ~Sepal.Length, x = ~Sepal.Width, fx = ~Species`. See [vg_mark()] for the full grammar.
#' @param aria_description `<string | param()>` The aria-description; a constant textual description.
#' @param aria_hidden `<string | param()>` The aria-hidden state; a constant indicating whether the element is exposed to an accessibility API.
#' @param aria_label `<column | literal | transform() | sql()/agg()>` The aria-label; a channel specifying short textual labels representing the value in the accessibility tree.
#' @param channels `<list>` Additional named channels, for example to include in a tooltip.
#' @param clip `<"frame" | "sphere" | boolean | NULL | param()>` How to clip the mark; one of: - *frame* or true - clip to the plot’s frame (inner area) - *sphere* - clip to the projected sphere (*e.g.*, front hemisphere) - null or false - do not clip The *sphere* clip option requires a geographic projection.
#' @param curve `<"basis" | "basis-closed" | "basis-open" | "bundle" | "bump-x" | "bump-y" | "cardinal" | "cardinal-closed" | "cardinal-open" | "catmull-rom" | ... | param()>` The curve (interpolation) method for connecting adjacent points.
#' @param dx `<number | param()>` The horizontal offset in pixels; a constant option.
#' @param dy `<number | param()>` The vertical offset in pixels; a constant option.
#' @param facet `<"auto" | "include" | "exclude" | "super" | boolean | NULL | param()>` Whether to enable or disable faceting; one of: - *auto* (default) - automatically determine if this mark should be faceted - *include* (or true) - draw the subset of the mark’s data in the current facet - *exclude* - draw the subset of the mark’s data *not* in the current facet - *super* - draw this mark in a single frame that covers all facets - null (or false) - repeat this mark’s data across all facets (*i.e.*, no faceting) When a mark uses *super* faceting, it is not allowed to use position scales (*x*, *y*, *fx*, or *fy*); *super* faceting is intended for decorations, such as labels and legends.
#' @param facet_anchor `<"top" | "right" | "bottom" | "left" | "top-left" | "top-right" | "bottom-left" | "bottom-right" | "top-empty" | "right-empty" | "bottom-empty" | "left-empty" | "empty" | NULL | param()>` How to place the mark with respect to facets; one of: - null (default for most marks) - display the mark in each non-empty facet - *top*, *right*, *bottom*, or *left* - display the mark only in facets on the given side - *top-empty*, *right-empty*, *bottom-empty*, or *left-empty* (default for axis marks) - display the mark only in facets that have empty space on the given side: either the margin, or an empty facet - *empty* - display the mark in empty facets only
#' @param fill `<column | literal | transform() | sql()/agg() | list(value=, ...) | param()>` The fill; a constant CSS color string, or a channel typically bound to the *color* scale.
#' @param fill_opacity `<column | literal | transform() | sql()/agg() | list(value=, ...) | param()>` The fill-opacity; a constant number between 0 and 1, or a channel typically bound to the *opacity* scale.
#' @param filter `<column | literal | transform() | sql()/agg()>` Applies a transform to filter the mark’s index according to the given channel values; only truthy values are retained.
#' @param fx `<column | literal | transform() | sql()/agg()>` The horizontal facet position channel, for mark-level faceting, bound to the *fx* scale.
#' @param fy `<column | literal | transform() | sql()/agg()>` The vertical facet position channel, for mark-level faceting, bound to the *fy* scale.
#' @param href `<column | literal | transform() | sql()/agg()>` The href; a channel specifying URLs for clickable links.
#' @param image_filter `<string | param()>` A CSS filter; a constant string used to adjust the rendering of images, such as *blur(5px)*.
#' @param margin `<number | param()>` Shorthand to set the same default for all four mark margins: **marginTop**, **marginRight**, **marginBottom**, and **marginLeft**; typically defaults to 0, except for axis marks.
#' @param margin_bottom `<number | param()>` The mark’s bottom margin; the minimum distance in pixels between the bottom edges of the inner and outer plot area.
#' @param margin_left `<number | param()>` The mark’s left margin; the minimum distance in pixels between the left edges of the inner and outer plot area.
#' @param margin_right `<number | param()>` The mark’s right margin; the minimum distance in pixels between the right edges of the mark’s inner and outer plot area.
#' @param margin_top `<number | param()>` The mark’s top margin; the minimum distance in pixels between the top edges of the inner and outer plot area.
#' @param mix_blend_mode `<string | param()>` The mix-blend-mode; a constant string specifying how to blend content such as *multiply*.
#' @param offset `<"center" | "normalize" | "wiggle" | NULL | param()>` After stacking, an optional **offset** can be applied to translate and scale stacks, say to produce a streamgraph; defaults to null for a zero baseline (**y** = 0 for stackY, and **x** = 0 for stackX).
#' @param opacity `<column | literal | transform() | sql()/agg() | list(value=, ...)>` The opacity; a constant between 0 and 1, or a channel typically bound to the *opacity* scale.
#' @param order `<"value" | "x" | "y" | "z" | "sum" | "appearance" | "inside-out" | "-value" | "-x" | "-y" | ... | NULL | param()>` The order in which stacks are layered; one of: - null (default) for input order - a named stack order method such as *inside-out* or *sum* - a field name, for natural order of the corresponding values - a function of data, for natural order of the corresponding values - an array of explicit **z** values in the desired order If the *wiggle* **offset** is used, as for a streamgraph, the default changes to *inside-out*.
#' @param paint_order `<string | param()>` The paint-order; a constant string specifying the order in which the **fill**, **stroke**, and any markers are drawn; defaults to *normal*, which draws the fill, then stroke, then markers; defaults to *stroke* for the text mark to create a “halo” around text to improve legibility.
#' @param pointer_events `<string | param()>` The pointer-events property; a constant string such as *none*.
#' @param reverse `<boolean | param()>` Applies a transform to reverse the order of the mark’s index, say for reverse input order.
#' @param select `<"first" | "last" | "maxX" | "maxY" | "minX" | "minY" | "nearest" | "nearestX" | "nearestY">` Applies a filter transform after data is loaded to highlight selected values only.
#' @param shape_rendering `<string | param()>` The shape-rendering; a constant string such as *crispEdges*.
#' @param sort `<column | literal | transform() | sql()/agg() | list>` Either applies a transform to sort the mark’s index by the specified channel values, or imputes ordinal scale domains from this mark’s channels.
#' @param stroke `<column | literal | transform() | sql()/agg() | list(value=, ...) | param()>` The stroke; a constant CSS color string, or a channel typically bound to the *color* scale.
#' @param stroke_dasharray `<string | number | param()>` The stroke-dasharray; a constant number indicating the length in pixels of alternating dashes and gaps, or a constant string of numbers separated by spaces or commas (_e.g._, *10 2* for dashes of 10 pixels separated by gaps of 2 pixels), or *none* (the default) for no dashing : https://developer.mozilla.org/en-US/docs/Web/SVG/Attribute/stroke-dasharray
#' @param stroke_dashoffset `<string | number | param()>` The stroke-dashoffset; a constant indicating the offset in pixels of the first dash along the stroke; defaults to zero.
#' @param stroke_linecap `<string | param()>` The stroke-linecap; a constant specifying how to cap stroked paths, such as *butt*, *round*, or *square*.
#' @param stroke_linejoin `<string | param()>` The stroke-linejoin; a constant specifying how to join stroked paths, such as *bevel*, *miter*, *miter-clip*, or *round*.
#' @param stroke_miterlimit `<number | param()>` The stroke-miterlimit; a constant number specifying how to limit the length of *miter* joins on stroked paths.
#' @param stroke_opacity `<column | literal | transform() | sql()/agg() | list(value=, ...)>` The stroke-opacity; a constant between 0 and 1, or a channel typically bound to the *opacity* scale.
#' @param stroke_width `<column | literal | transform() | sql()/agg() | list(value=, ...)>` The stroke-width; a constant number in pixels, or a channel.
#' @param target `<string | param()>` The target; a constant string specifying the target window (_e.g._, *_blank*) for clickable links; used in conjunction with the **href** option.
#' @param tension `<number | param()>` The tension option only has an effect on bundle, cardinal and Catmull–Rom splines (*bundle*, *cardinal*, *cardinal-open*, *cardinal-closed*, *catmull-rom*, *catmull-rom-open*, and *catmull-rom-closed*).
#' @param tip `<boolean | "x" | "y" | "xy" | list | param()>` Whether to generate a tooltip for this mark, and any tip options.
#' @param title `<column | literal | transform() | sql()/agg()>` The title; a channel specifying accessible, short textual descriptions as strings (possibly with newlines).
#' @param x `<column | literal | transform() | sql()/agg() | list(value=, ...)>` The horizontal position (or length) channel, typically bound to the *x* scale.
#' @param x1 `<column | literal | transform() | sql()/agg() | list(value=, ...)>` The required primary (starting, often left) horizontal position channel, representing the area’s baseline, typically bound to the *x* scale.
#' @param x2 `<column | literal | transform() | sql()/agg() | list(value=, ...)>` The optional secondary (ending, often right) horizontal position channel, representing the area’s topline, typically bound to the *x* scale; if not specified, **x1** is used.
#' @param y `<column | literal | transform() | sql()/agg() | list(value=, ...)>` The vertical position channel, typically bound to the *y* scale; defaults to the zero-based index of the data 0, 1, 2, ….
#' @param z `<column | literal | transform() | sql()/agg()>` An optional ordinal channel for grouping data into (possibly stacked) series to be drawn as separate areas; defaults to **fill** if a channel, or **stroke** if a channel.
#' @param ... Additional options or plot-level attributes.
#' @param data_from The name of the data source this mark reads from (see [vg_data()]); a length-1 nonzero R integer (`1L`, `-1L`, ...; note the `L`) giving a 1-based index into the spec's registered data sources instead, negative counting from the end (e.g., `-1L` for the most recently registered one); or a literal vector of values to use as inline data directly (mosaic-spec's `"data": [...]` shorthand, e.g., for a single reference line -- a bare double like `0`/`c(0)`, or `0L` itself (never a valid index), means this, not an index). Left unset, defaults to the first registered data source (equivalent to `data_from = 1L`) for any mark type that takes data at all.
#' @param filter_by A Param/Selection (e.g., from [param()]) to filter this mark's data by.
#' @param data_optimize A flag (default `TRUE`) to enable mark-specific query optimizations for this mark's data; set `FALSE` to disable them (mosaic-spec's `data: {optimize: false}`).
#' @family mark functions
#' @export
vg_mark_area_x <- function(spec = NULL, formula = vg_unset, aria_description = vg_unset, aria_hidden = vg_unset, aria_label = vg_unset, channels = vg_unset, clip = vg_unset, curve = vg_unset, dx = vg_unset, dy = vg_unset, facet = vg_unset, facet_anchor = vg_unset, fill = vg_unset, fill_opacity = vg_unset, filter = vg_unset, fx = vg_unset, fy = vg_unset, href = vg_unset, image_filter = vg_unset, margin = vg_unset, margin_bottom = vg_unset, margin_left = vg_unset, margin_right = vg_unset, margin_top = vg_unset, mix_blend_mode = vg_unset, offset = vg_unset, opacity = vg_unset, order = vg_unset, paint_order = vg_unset, pointer_events = vg_unset, reverse = vg_unset, select = vg_unset, shape_rendering = vg_unset, sort = vg_unset, stroke = vg_unset, stroke_dasharray = vg_unset, stroke_dashoffset = vg_unset, stroke_linecap = vg_unset, stroke_linejoin = vg_unset, stroke_miterlimit = vg_unset, stroke_opacity = vg_unset, stroke_width = vg_unset, target = vg_unset, tension = vg_unset, tip = vg_unset, title = vg_unset, x = vg_unset, x1 = vg_unset, x2 = vg_unset, y = vg_unset, z = vg_unset, ..., data_from = vg_unset, filter_by = vg_unset, data_optimize = vg_unset) {
  vg_mark_(spec, "areaX", formula = formula, ariaDescription = aria_description, ariaHidden = aria_hidden, ariaLabel = aria_label, channels = channels, clip = clip, curve = curve, dx = dx, dy = dy, facet = facet, facetAnchor = facet_anchor, fill = fill, fillOpacity = fill_opacity, filter = filter, fx = fx, fy = fy, href = href, imageFilter = image_filter, margin = margin, marginBottom = margin_bottom, marginLeft = margin_left, marginRight = margin_right, marginTop = margin_top, mixBlendMode = mix_blend_mode, offset = offset, opacity = opacity, order = order, paintOrder = paint_order, pointerEvents = pointer_events, reverse = reverse, select = select, shapeRendering = shape_rendering, sort = sort, stroke = stroke, strokeDasharray = stroke_dasharray, strokeDashoffset = stroke_dashoffset, strokeLinecap = stroke_linecap, strokeLinejoin = stroke_linejoin, strokeMiterlimit = stroke_miterlimit, strokeOpacity = stroke_opacity, strokeWidth = stroke_width, target = target, tension = tension, tip = tip, title = title, x = x, x1 = x1, x2 = x2, y = y, z = z, ..., data_from = data_from, filter_by = filter_by, data_optimize = data_optimize)
}

#' The areaY mark.
#'
#' See [vg_value_types] for what the `<...>` notation below (`column`, `param()`, ...) means.
#'
#' @param spec A plot fragment or `vgspec` to add this mark to, or `NULL` to start a new plot with just this mark.
#' @param formula A shorthand for this mark's position channels (`x`, `y`, `fx`, `fy`, and paired `x1`/`x2` or `y1`/`y2`), e.g., `Sepal.Length ~ Sepal.Width | ~ Species` for `y = ~Sepal.Length, x = ~Sepal.Width, fx = ~Species`. See [vg_mark()] for the full grammar.
#' @param aria_description `<string | param()>` The aria-description; a constant textual description.
#' @param aria_hidden `<string | param()>` The aria-hidden state; a constant indicating whether the element is exposed to an accessibility API.
#' @param aria_label `<column | literal | transform() | sql()/agg()>` The aria-label; a channel specifying short textual labels representing the value in the accessibility tree.
#' @param channels `<list>` Additional named channels, for example to include in a tooltip.
#' @param clip `<"frame" | "sphere" | boolean | NULL | param()>` How to clip the mark; one of: - *frame* or true - clip to the plot’s frame (inner area) - *sphere* - clip to the projected sphere (*e.g.*, front hemisphere) - null or false - do not clip The *sphere* clip option requires a geographic projection.
#' @param curve `<"basis" | "basis-closed" | "basis-open" | "bundle" | "bump-x" | "bump-y" | "cardinal" | "cardinal-closed" | "cardinal-open" | "catmull-rom" | ... | param()>` The curve (interpolation) method for connecting adjacent points.
#' @param dx `<number | param()>` The horizontal offset in pixels; a constant option.
#' @param dy `<number | param()>` The vertical offset in pixels; a constant option.
#' @param facet `<"auto" | "include" | "exclude" | "super" | boolean | NULL | param()>` Whether to enable or disable faceting; one of: - *auto* (default) - automatically determine if this mark should be faceted - *include* (or true) - draw the subset of the mark’s data in the current facet - *exclude* - draw the subset of the mark’s data *not* in the current facet - *super* - draw this mark in a single frame that covers all facets - null (or false) - repeat this mark’s data across all facets (*i.e.*, no faceting) When a mark uses *super* faceting, it is not allowed to use position scales (*x*, *y*, *fx*, or *fy*); *super* faceting is intended for decorations, such as labels and legends.
#' @param facet_anchor `<"top" | "right" | "bottom" | "left" | "top-left" | "top-right" | "bottom-left" | "bottom-right" | "top-empty" | "right-empty" | "bottom-empty" | "left-empty" | "empty" | NULL | param()>` How to place the mark with respect to facets; one of: - null (default for most marks) - display the mark in each non-empty facet - *top*, *right*, *bottom*, or *left* - display the mark only in facets on the given side - *top-empty*, *right-empty*, *bottom-empty*, or *left-empty* (default for axis marks) - display the mark only in facets that have empty space on the given side: either the margin, or an empty facet - *empty* - display the mark in empty facets only
#' @param fill `<column | literal | transform() | sql()/agg() | list(value=, ...) | param()>` The fill; a constant CSS color string, or a channel typically bound to the *color* scale.
#' @param fill_opacity `<column | literal | transform() | sql()/agg() | list(value=, ...) | param()>` The fill-opacity; a constant number between 0 and 1, or a channel typically bound to the *opacity* scale.
#' @param filter `<column | literal | transform() | sql()/agg()>` Applies a transform to filter the mark’s index according to the given channel values; only truthy values are retained.
#' @param fx `<column | literal | transform() | sql()/agg()>` The horizontal facet position channel, for mark-level faceting, bound to the *fx* scale.
#' @param fy `<column | literal | transform() | sql()/agg()>` The vertical facet position channel, for mark-level faceting, bound to the *fy* scale.
#' @param href `<column | literal | transform() | sql()/agg()>` The href; a channel specifying URLs for clickable links.
#' @param image_filter `<string | param()>` A CSS filter; a constant string used to adjust the rendering of images, such as *blur(5px)*.
#' @param margin `<number | param()>` Shorthand to set the same default for all four mark margins: **marginTop**, **marginRight**, **marginBottom**, and **marginLeft**; typically defaults to 0, except for axis marks.
#' @param margin_bottom `<number | param()>` The mark’s bottom margin; the minimum distance in pixels between the bottom edges of the inner and outer plot area.
#' @param margin_left `<number | param()>` The mark’s left margin; the minimum distance in pixels between the left edges of the inner and outer plot area.
#' @param margin_right `<number | param()>` The mark’s right margin; the minimum distance in pixels between the right edges of the mark’s inner and outer plot area.
#' @param margin_top `<number | param()>` The mark’s top margin; the minimum distance in pixels between the top edges of the inner and outer plot area.
#' @param mix_blend_mode `<string | param()>` The mix-blend-mode; a constant string specifying how to blend content such as *multiply*.
#' @param offset `<"center" | "normalize" | "wiggle" | NULL | param()>` After stacking, an optional **offset** can be applied to translate and scale stacks, say to produce a streamgraph; defaults to null for a zero baseline (**y** = 0 for stackY, and **x** = 0 for stackX).
#' @param opacity `<column | literal | transform() | sql()/agg() | list(value=, ...)>` The opacity; a constant between 0 and 1, or a channel typically bound to the *opacity* scale.
#' @param order `<"value" | "x" | "y" | "z" | "sum" | "appearance" | "inside-out" | "-value" | "-x" | "-y" | ... | NULL | param()>` The order in which stacks are layered; one of: - null (default) for input order - a named stack order method such as *inside-out* or *sum* - a field name, for natural order of the corresponding values - a function of data, for natural order of the corresponding values - an array of explicit **z** values in the desired order If the *wiggle* **offset** is used, as for a streamgraph, the default changes to *inside-out*.
#' @param paint_order `<string | param()>` The paint-order; a constant string specifying the order in which the **fill**, **stroke**, and any markers are drawn; defaults to *normal*, which draws the fill, then stroke, then markers; defaults to *stroke* for the text mark to create a “halo” around text to improve legibility.
#' @param pointer_events `<string | param()>` The pointer-events property; a constant string such as *none*.
#' @param reverse `<boolean | param()>` Applies a transform to reverse the order of the mark’s index, say for reverse input order.
#' @param select `<"first" | "last" | "maxX" | "maxY" | "minX" | "minY" | "nearest" | "nearestX" | "nearestY">` Applies a filter transform after data is loaded to highlight selected values only.
#' @param shape_rendering `<string | param()>` The shape-rendering; a constant string such as *crispEdges*.
#' @param sort `<column | literal | transform() | sql()/agg() | list>` Either applies a transform to sort the mark’s index by the specified channel values, or imputes ordinal scale domains from this mark’s channels.
#' @param stroke `<column | literal | transform() | sql()/agg() | list(value=, ...) | param()>` The stroke; a constant CSS color string, or a channel typically bound to the *color* scale.
#' @param stroke_dasharray `<string | number | param()>` The stroke-dasharray; a constant number indicating the length in pixels of alternating dashes and gaps, or a constant string of numbers separated by spaces or commas (_e.g._, *10 2* for dashes of 10 pixels separated by gaps of 2 pixels), or *none* (the default) for no dashing : https://developer.mozilla.org/en-US/docs/Web/SVG/Attribute/stroke-dasharray
#' @param stroke_dashoffset `<string | number | param()>` The stroke-dashoffset; a constant indicating the offset in pixels of the first dash along the stroke; defaults to zero.
#' @param stroke_linecap `<string | param()>` The stroke-linecap; a constant specifying how to cap stroked paths, such as *butt*, *round*, or *square*.
#' @param stroke_linejoin `<string | param()>` The stroke-linejoin; a constant specifying how to join stroked paths, such as *bevel*, *miter*, *miter-clip*, or *round*.
#' @param stroke_miterlimit `<number | param()>` The stroke-miterlimit; a constant number specifying how to limit the length of *miter* joins on stroked paths.
#' @param stroke_opacity `<column | literal | transform() | sql()/agg() | list(value=, ...)>` The stroke-opacity; a constant between 0 and 1, or a channel typically bound to the *opacity* scale.
#' @param stroke_width `<column | literal | transform() | sql()/agg() | list(value=, ...)>` The stroke-width; a constant number in pixels, or a channel.
#' @param target `<string | param()>` The target; a constant string specifying the target window (_e.g._, *_blank*) for clickable links; used in conjunction with the **href** option.
#' @param tension `<number | param()>` The tension option only has an effect on bundle, cardinal and Catmull–Rom splines (*bundle*, *cardinal*, *cardinal-open*, *cardinal-closed*, *catmull-rom*, *catmull-rom-open*, and *catmull-rom-closed*).
#' @param tip `<boolean | "x" | "y" | "xy" | list | param()>` Whether to generate a tooltip for this mark, and any tip options.
#' @param title `<column | literal | transform() | sql()/agg()>` The title; a channel specifying accessible, short textual descriptions as strings (possibly with newlines).
#' @param x `<column | literal | transform() | sql()/agg() | list(value=, ...)>` The horizontal position (or length) channel, typically bound to the *x* scale.
#' @param y `<column | literal | transform() | sql()/agg() | list(value=, ...)>` The vertical position channel, typically bound to the *y* scale; defaults to the zero-based index of the data 0, 1, 2, ….
#' @param y1 `<column | literal | transform() | sql()/agg() | list(value=, ...)>` The required primary (starting, often bottom) vertical position channel, representing the area’s baseline, typically bound to the *y* scale.
#' @param y2 `<column | literal | transform() | sql()/agg() | list(value=, ...)>` The optional secondary (ending, often top) vertical position channel, representing the area’s topline, typically bound to the *y* scale; if not specified, **y1** is used.
#' @param z `<column | literal | transform() | sql()/agg()>` An optional ordinal channel for grouping data into (possibly stacked) series to be drawn as separate areas; defaults to **fill** if a channel, or **stroke** if a channel.
#' @param ... Additional options or plot-level attributes.
#' @param data_from The name of the data source this mark reads from (see [vg_data()]); a length-1 nonzero R integer (`1L`, `-1L`, ...; note the `L`) giving a 1-based index into the spec's registered data sources instead, negative counting from the end (e.g., `-1L` for the most recently registered one); or a literal vector of values to use as inline data directly (mosaic-spec's `"data": [...]` shorthand, e.g., for a single reference line -- a bare double like `0`/`c(0)`, or `0L` itself (never a valid index), means this, not an index). Left unset, defaults to the first registered data source (equivalent to `data_from = 1L`) for any mark type that takes data at all.
#' @param filter_by A Param/Selection (e.g., from [param()]) to filter this mark's data by.
#' @param data_optimize A flag (default `TRUE`) to enable mark-specific query optimizations for this mark's data; set `FALSE` to disable them (mosaic-spec's `data: {optimize: false}`).
#' @family mark functions
#' @export
vg_mark_area_y <- function(spec = NULL, formula = vg_unset, aria_description = vg_unset, aria_hidden = vg_unset, aria_label = vg_unset, channels = vg_unset, clip = vg_unset, curve = vg_unset, dx = vg_unset, dy = vg_unset, facet = vg_unset, facet_anchor = vg_unset, fill = vg_unset, fill_opacity = vg_unset, filter = vg_unset, fx = vg_unset, fy = vg_unset, href = vg_unset, image_filter = vg_unset, margin = vg_unset, margin_bottom = vg_unset, margin_left = vg_unset, margin_right = vg_unset, margin_top = vg_unset, mix_blend_mode = vg_unset, offset = vg_unset, opacity = vg_unset, order = vg_unset, paint_order = vg_unset, pointer_events = vg_unset, reverse = vg_unset, select = vg_unset, shape_rendering = vg_unset, sort = vg_unset, stroke = vg_unset, stroke_dasharray = vg_unset, stroke_dashoffset = vg_unset, stroke_linecap = vg_unset, stroke_linejoin = vg_unset, stroke_miterlimit = vg_unset, stroke_opacity = vg_unset, stroke_width = vg_unset, target = vg_unset, tension = vg_unset, tip = vg_unset, title = vg_unset, x = vg_unset, y = vg_unset, y1 = vg_unset, y2 = vg_unset, z = vg_unset, ..., data_from = vg_unset, filter_by = vg_unset, data_optimize = vg_unset) {
  vg_mark_(spec, "areaY", formula = formula, ariaDescription = aria_description, ariaHidden = aria_hidden, ariaLabel = aria_label, channels = channels, clip = clip, curve = curve, dx = dx, dy = dy, facet = facet, facetAnchor = facet_anchor, fill = fill, fillOpacity = fill_opacity, filter = filter, fx = fx, fy = fy, href = href, imageFilter = image_filter, margin = margin, marginBottom = margin_bottom, marginLeft = margin_left, marginRight = margin_right, marginTop = margin_top, mixBlendMode = mix_blend_mode, offset = offset, opacity = opacity, order = order, paintOrder = paint_order, pointerEvents = pointer_events, reverse = reverse, select = select, shapeRendering = shape_rendering, sort = sort, stroke = stroke, strokeDasharray = stroke_dasharray, strokeDashoffset = stroke_dashoffset, strokeLinecap = stroke_linecap, strokeLinejoin = stroke_linejoin, strokeMiterlimit = stroke_miterlimit, strokeOpacity = stroke_opacity, strokeWidth = stroke_width, target = target, tension = tension, tip = tip, title = title, x = x, y = y, y1 = y1, y2 = y2, z = z, ..., data_from = data_from, filter_by = filter_by, data_optimize = data_optimize)
}

#' The arrow mark.
#'
#' See [vg_value_types] for what the `<...>` notation below (`column`, `param()`, ...) means.
#'
#' @param spec A plot fragment or `vgspec` to add this mark to, or `NULL` to start a new plot with just this mark.
#' @param formula A shorthand for this mark's position channels (`x`, `y`, `fx`, `fy`, and paired `x1`/`x2` or `y1`/`y2`), e.g., `Sepal.Length ~ Sepal.Width | ~ Species` for `y = ~Sepal.Length, x = ~Sepal.Width, fx = ~Species`. See [vg_mark()] for the full grammar.
#' @param aria_description `<string | param()>` The aria-description; a constant textual description.
#' @param aria_hidden `<string | param()>` The aria-hidden state; a constant indicating whether the element is exposed to an accessibility API.
#' @param aria_label `<column | literal | transform() | sql()/agg()>` The aria-label; a channel specifying short textual labels representing the value in the accessibility tree.
#' @param bend `<number | boolean | param()>` The angle, a constant in degrees, between the straight line intersecting the arrow’s two control points and the outgoing tangent direction of the arrow from the start point.
#' @param channels `<list>` Additional named channels, for example to include in a tooltip.
#' @param clip `<"frame" | "sphere" | boolean | NULL | param()>` How to clip the mark; one of: - *frame* or true - clip to the plot’s frame (inner area) - *sphere* - clip to the projected sphere (*e.g.*, front hemisphere) - null or false - do not clip The *sphere* clip option requires a geographic projection.
#' @param dx `<number | param()>` The horizontal offset in pixels; a constant option.
#' @param dy `<number | param()>` The vertical offset in pixels; a constant option.
#' @param facet `<"auto" | "include" | "exclude" | "super" | boolean | NULL | param()>` Whether to enable or disable faceting; one of: - *auto* (default) - automatically determine if this mark should be faceted - *include* (or true) - draw the subset of the mark’s data in the current facet - *exclude* - draw the subset of the mark’s data *not* in the current facet - *super* - draw this mark in a single frame that covers all facets - null (or false) - repeat this mark’s data across all facets (*i.e.*, no faceting) When a mark uses *super* faceting, it is not allowed to use position scales (*x*, *y*, *fx*, or *fy*); *super* faceting is intended for decorations, such as labels and legends.
#' @param facet_anchor `<"top" | "right" | "bottom" | "left" | "top-left" | "top-right" | "bottom-left" | "bottom-right" | "top-empty" | "right-empty" | "bottom-empty" | "left-empty" | "empty" | NULL | param()>` How to place the mark with respect to facets; one of: - null (default for most marks) - display the mark in each non-empty facet - *top*, *right*, *bottom*, or *left* - display the mark only in facets on the given side - *top-empty*, *right-empty*, *bottom-empty*, or *left-empty* (default for axis marks) - display the mark only in facets that have empty space on the given side: either the margin, or an empty facet - *empty* - display the mark in empty facets only
#' @param fill `<column | literal | transform() | sql()/agg() | list(value=, ...) | param()>` The fill; a constant CSS color string, or a channel typically bound to the *color* scale.
#' @param fill_opacity `<column | literal | transform() | sql()/agg() | list(value=, ...) | param()>` The fill-opacity; a constant number between 0 and 1, or a channel typically bound to the *opacity* scale.
#' @param filter `<column | literal | transform() | sql()/agg()>` Applies a transform to filter the mark’s index according to the given channel values; only truthy values are retained.
#' @param fx `<column | literal | transform() | sql()/agg()>` The horizontal facet position channel, for mark-level faceting, bound to the *fx* scale.
#' @param fy `<column | literal | transform() | sql()/agg()>` The vertical facet position channel, for mark-level faceting, bound to the *fy* scale.
#' @param head_angle `<number | param()>` How pointy the arrowhead is, in degrees; a constant typically between 0° and 180°, and defaults to 60°.
#' @param head_length `<number | param()>` The size of the arrowhead relative to the **strokeWidth**; a constant.
#' @param href `<column | literal | transform() | sql()/agg()>` The href; a channel specifying URLs for clickable links.
#' @param image_filter `<string | param()>` A CSS filter; a constant string used to adjust the rendering of images, such as *blur(5px)*.
#' @param inset `<number | param()>` Shorthand to set the same default for **insetStart** and **insetEnd**.
#' @param inset_end `<number | param()>` The ending inset, a constant in pixels; defaults to 0.
#' @param inset_start `<number | param()>` The starting inset, a constant in pixels; defaults to 0.
#' @param margin `<number | param()>` Shorthand to set the same default for all four mark margins: **marginTop**, **marginRight**, **marginBottom**, and **marginLeft**; typically defaults to 0, except for axis marks.
#' @param margin_bottom `<number | param()>` The mark’s bottom margin; the minimum distance in pixels between the bottom edges of the inner and outer plot area.
#' @param margin_left `<number | param()>` The mark’s left margin; the minimum distance in pixels between the left edges of the inner and outer plot area.
#' @param margin_right `<number | param()>` The mark’s right margin; the minimum distance in pixels between the right edges of the mark’s inner and outer plot area.
#' @param margin_top `<number | param()>` The mark’s top margin; the minimum distance in pixels between the top edges of the inner and outer plot area.
#' @param mix_blend_mode `<string | param()>` The mix-blend-mode; a constant string specifying how to blend content such as *multiply*.
#' @param opacity `<column | literal | transform() | sql()/agg() | list(value=, ...)>` The opacity; a constant between 0 and 1, or a channel typically bound to the *opacity* scale.
#' @param paint_order `<string | param()>` The paint-order; a constant string specifying the order in which the **fill**, **stroke**, and any markers are drawn; defaults to *normal*, which draws the fill, then stroke, then markers; defaults to *stroke* for the text mark to create a “halo” around text to improve legibility.
#' @param pointer_events `<string | param()>` The pointer-events property; a constant string such as *none*.
#' @param reverse `<boolean | param()>` Applies a transform to reverse the order of the mark’s index, say for reverse input order.
#' @param select `<"first" | "last" | "maxX" | "maxY" | "minX" | "minY" | "nearest" | "nearestX" | "nearestY">` Applies a filter transform after data is loaded to highlight selected values only.
#' @param shape_rendering `<string | param()>` The shape-rendering; a constant string such as *crispEdges*.
#' @param sort `<column | literal | transform() | sql()/agg() | list>` Either applies a transform to sort the mark’s index by the specified channel values, or imputes ordinal scale domains from this mark’s channels.
#' @param stroke `<column | literal | transform() | sql()/agg() | list(value=, ...) | param()>` The stroke; a constant CSS color string, or a channel typically bound to the *color* scale.
#' @param stroke_dasharray `<string | number | param()>` The stroke-dasharray; a constant number indicating the length in pixels of alternating dashes and gaps, or a constant string of numbers separated by spaces or commas (_e.g._, *10 2* for dashes of 10 pixels separated by gaps of 2 pixels), or *none* (the default) for no dashing : https://developer.mozilla.org/en-US/docs/Web/SVG/Attribute/stroke-dasharray
#' @param stroke_dashoffset `<string | number | param()>` The stroke-dashoffset; a constant indicating the offset in pixels of the first dash along the stroke; defaults to zero.
#' @param stroke_linecap `<string | param()>` The stroke-linecap; a constant specifying how to cap stroked paths, such as *butt*, *round*, or *square*.
#' @param stroke_linejoin `<string | param()>` The stroke-linejoin; a constant specifying how to join stroked paths, such as *bevel*, *miter*, *miter-clip*, or *round*.
#' @param stroke_miterlimit `<number | param()>` The stroke-miterlimit; a constant number specifying how to limit the length of *miter* joins on stroked paths.
#' @param stroke_opacity `<column | literal | transform() | sql()/agg() | list(value=, ...)>` The stroke-opacity; a constant between 0 and 1, or a channel typically bound to the *opacity* scale.
#' @param stroke_width `<column | literal | transform() | sql()/agg() | list(value=, ...)>` The stroke-width; a constant number in pixels, or a channel.
#' @param sweep `<number | "+x" | "-x" | "+y" | "-y" | param()>` The sweep order; defaults to 1 indicating a positive (clockwise) bend angle; -1 indicates a negative (anticlockwise) bend angle; 0 effectively clears the bend angle.
#' @param target `<string | param()>` The target; a constant string specifying the target window (_e.g._, *_blank*) for clickable links; used in conjunction with the **href** option.
#' @param tip `<boolean | "x" | "y" | "xy" | list | param()>` Whether to generate a tooltip for this mark, and any tip options.
#' @param title `<column | literal | transform() | sql()/agg()>` The title; a channel specifying accessible, short textual descriptions as strings (possibly with newlines).
#' @param x `<column | literal | transform() | sql()/agg() | list(value=, ...)>` The horizontal position (or length) channel, typically bound to the *x* scale.
#' @param x1 `<column | literal | transform() | sql()/agg() | list(value=, ...)>` The required primary (starting, often left) horizontal position channel, representing the area’s baseline, typically bound to the *x* scale.
#' @param x2 `<column | literal | transform() | sql()/agg() | list(value=, ...)>` The optional secondary (ending, often right) horizontal position channel, representing the area’s topline, typically bound to the *x* scale; if not specified, **x1** is used.
#' @param y `<column | literal | transform() | sql()/agg() | list(value=, ...)>` The vertical position channel, typically bound to the *y* scale; defaults to the zero-based index of the data 0, 1, 2, ….
#' @param y1 `<column | literal | transform() | sql()/agg() | list(value=, ...)>` The required primary (starting, often bottom) vertical position channel, representing the area’s baseline, typically bound to the *y* scale.
#' @param y2 `<column | literal | transform() | sql()/agg() | list(value=, ...)>` The optional secondary (ending, often top) vertical position channel, representing the area’s topline, typically bound to the *y* scale; if not specified, **y1** is used.
#' @param ... Additional options or plot-level attributes.
#' @param data_from The name of the data source this mark reads from (see [vg_data()]); a length-1 nonzero R integer (`1L`, `-1L`, ...; note the `L`) giving a 1-based index into the spec's registered data sources instead, negative counting from the end (e.g., `-1L` for the most recently registered one); or a literal vector of values to use as inline data directly (mosaic-spec's `"data": [...]` shorthand, e.g., for a single reference line -- a bare double like `0`/`c(0)`, or `0L` itself (never a valid index), means this, not an index). Left unset, defaults to the first registered data source (equivalent to `data_from = 1L`) for any mark type that takes data at all.
#' @param filter_by A Param/Selection (e.g., from [param()]) to filter this mark's data by.
#' @param data_optimize A flag (default `TRUE`) to enable mark-specific query optimizations for this mark's data; set `FALSE` to disable them (mosaic-spec's `data: {optimize: false}`).
#' @family mark functions
#' @export
vg_mark_arrow <- function(spec = NULL, formula = vg_unset, aria_description = vg_unset, aria_hidden = vg_unset, aria_label = vg_unset, bend = vg_unset, channels = vg_unset, clip = vg_unset, dx = vg_unset, dy = vg_unset, facet = vg_unset, facet_anchor = vg_unset, fill = vg_unset, fill_opacity = vg_unset, filter = vg_unset, fx = vg_unset, fy = vg_unset, head_angle = vg_unset, head_length = vg_unset, href = vg_unset, image_filter = vg_unset, inset = vg_unset, inset_end = vg_unset, inset_start = vg_unset, margin = vg_unset, margin_bottom = vg_unset, margin_left = vg_unset, margin_right = vg_unset, margin_top = vg_unset, mix_blend_mode = vg_unset, opacity = vg_unset, paint_order = vg_unset, pointer_events = vg_unset, reverse = vg_unset, select = vg_unset, shape_rendering = vg_unset, sort = vg_unset, stroke = vg_unset, stroke_dasharray = vg_unset, stroke_dashoffset = vg_unset, stroke_linecap = vg_unset, stroke_linejoin = vg_unset, stroke_miterlimit = vg_unset, stroke_opacity = vg_unset, stroke_width = vg_unset, sweep = vg_unset, target = vg_unset, tip = vg_unset, title = vg_unset, x = vg_unset, x1 = vg_unset, x2 = vg_unset, y = vg_unset, y1 = vg_unset, y2 = vg_unset, ..., data_from = vg_unset, filter_by = vg_unset, data_optimize = vg_unset) {
  vg_mark_(spec, "arrow", formula = formula, ariaDescription = aria_description, ariaHidden = aria_hidden, ariaLabel = aria_label, bend = bend, channels = channels, clip = clip, dx = dx, dy = dy, facet = facet, facetAnchor = facet_anchor, fill = fill, fillOpacity = fill_opacity, filter = filter, fx = fx, fy = fy, headAngle = head_angle, headLength = head_length, href = href, imageFilter = image_filter, inset = inset, insetEnd = inset_end, insetStart = inset_start, margin = margin, marginBottom = margin_bottom, marginLeft = margin_left, marginRight = margin_right, marginTop = margin_top, mixBlendMode = mix_blend_mode, opacity = opacity, paintOrder = paint_order, pointerEvents = pointer_events, reverse = reverse, select = select, shapeRendering = shape_rendering, sort = sort, stroke = stroke, strokeDasharray = stroke_dasharray, strokeDashoffset = stroke_dashoffset, strokeLinecap = stroke_linecap, strokeLinejoin = stroke_linejoin, strokeMiterlimit = stroke_miterlimit, strokeOpacity = stroke_opacity, strokeWidth = stroke_width, sweep = sweep, target = target, tip = tip, title = title, x = x, x1 = x1, x2 = x2, y = y, y1 = y1, y2 = y2, ..., data_from = data_from, filter_by = filter_by, data_optimize = data_optimize)
}

#' The axisFx mark.
#'
#' See [vg_value_types] for what the `<...>` notation below (`column`, `param()`, ...) means.
#'
#' @param spec A plot fragment or `vgspec` to add this mark to, or `NULL` to start a new plot with just this mark.
#' @param formula A shorthand for this mark's position channels (`x`, `y`, `fx`, `fy`, and paired `x1`/`x2` or `y1`/`y2`), e.g., `Sepal.Length ~ Sepal.Width | ~ Species` for `y = ~Sepal.Length, x = ~Sepal.Width, fx = ~Species`. See [vg_mark()] for the full grammar.
#' @param anchor `<"top" | "right" | "bottom" | "left" | param()>` The side of the frame on which to place the axis: *top* or *bottom* for horizontal axes (axisX and axisFx) and their associated vertical grids (gridX and gridFx), or *left* or *right* for vertical axes (axisY and axisFY) and their associated horizontal grids (gridY and gridFy).
#' @param aria_description `<string | param()>` The aria-description; a constant textual description.
#' @param aria_hidden `<string | param()>` The aria-hidden state; a constant indicating whether the element is exposed to an accessibility API.
#' @param aria_label `<column | literal | transform() | sql()/agg()>` The aria-label; a channel specifying short textual labels representing the value in the accessibility tree.
#' @param channels `<list>` Additional named channels, for example to include in a tooltip.
#' @param clip `<"frame" | "sphere" | boolean | NULL | param()>` How to clip the mark; one of: - *frame* or true - clip to the plot’s frame (inner area) - *sphere* - clip to the projected sphere (*e.g.*, front hemisphere) - null or false - do not clip The *sphere* clip option requires a geographic projection.
#' @param color `<column | literal | transform() | sql()/agg() | list(value=, ...) | param()>` A shorthand for setting both **fill** and **stroke**; affects the stroke of tick vectors and grid rules, and the fill of tick texts and axis label texts; defaults to *currentColor*.
#' @param dx `<number | param()>` The horizontal offset in pixels; a constant option.
#' @param dy `<number | param()>` The vertical offset in pixels; a constant option.
#' @param facet `<"auto" | "include" | "exclude" | "super" | boolean | NULL | param()>` Whether to enable or disable faceting; one of: - *auto* (default) - automatically determine if this mark should be faceted - *include* (or true) - draw the subset of the mark’s data in the current facet - *exclude* - draw the subset of the mark’s data *not* in the current facet - *super* - draw this mark in a single frame that covers all facets - null (or false) - repeat this mark’s data across all facets (*i.e.*, no faceting) When a mark uses *super* faceting, it is not allowed to use position scales (*x*, *y*, *fx*, or *fy*); *super* faceting is intended for decorations, such as labels and legends.
#' @param facet_anchor `<"top" | "right" | "bottom" | "left" | "top-left" | "top-right" | "bottom-left" | "bottom-right" | "top-empty" | "right-empty" | "bottom-empty" | "left-empty" | "empty" | NULL | param()>` How to place the mark with respect to facets; one of: - null (default for most marks) - display the mark in each non-empty facet - *top*, *right*, *bottom*, or *left* - display the mark only in facets on the given side - *top-empty*, *right-empty*, *bottom-empty*, or *left-empty* (default for axis marks) - display the mark only in facets that have empty space on the given side: either the margin, or an empty facet - *empty* - display the mark in empty facets only
#' @param fill `<column | literal | transform() | sql()/agg() | list(value=, ...) | param()>` The fill; a constant CSS color string, or a channel typically bound to the *color* scale.
#' @param fill_opacity `<column | literal | transform() | sql()/agg() | list(value=, ...) | param()>` The fill-opacity; a constant number between 0 and 1, or a channel typically bound to the *opacity* scale.
#' @param filter `<column | literal | transform() | sql()/agg()>` Applies a transform to filter the mark’s index according to the given channel values; only truthy values are retained.
#' @param font_family `<string | param()>` The font-family; a constant; defaults to the plot’s font family, which is typically *system-ui*.
#' @param font_size `<column | literal | transform() | sql()/agg() | param()>` The font size in pixels; either a constant or a channel; defaults to the plot’s font size, which is typically 10.
#' @param font_style `<string | param()>` The font style; a constant; defaults to the plot’s font style, which is typically *normal*.
#' @param font_variant `<string | param()>` The font variant; a constant; if the **text** channel contains numbers or dates, defaults to *tabular-nums* to facilitate comparing numbers; otherwise defaults to the plot’s font style, which is typically *normal*.
#' @param font_weight `<string | number | param()>` The font weight; a constant; defaults to the plot’s font weight, which is typically *normal*.
#' @param frame_anchor `<"middle" | "top-left" | "top" | "top-right" | "right" | "bottom-right" | "bottom" | "bottom-left" | "left" | param()>` The frame anchor specifies defaults for **x** and **y**, along with **textAnchor** and **lineAnchor**, based on the plot’s frame; it may be one of the four sides (*top*, *right*, *bottom*, *left*), one of the four corners (*top-left*, *top-right*, *bottom-right*, *bottom-left*), or the *middle* of the frame.
#' @param fx `<column | literal | transform() | sql()/agg()>` The horizontal facet position channel, for mark-level faceting, bound to the *fx* scale.
#' @param fy `<column | literal | transform() | sql()/agg()>` The vertical facet position channel, for mark-level faceting, bound to the *fy* scale.
#' @param href `<column | literal | transform() | sql()/agg()>` The href; a channel specifying URLs for clickable links.
#' @param image_filter `<string | param()>` A CSS filter; a constant string used to adjust the rendering of images, such as *blur(5px)*.
#' @param inset `<number | param()>` Shorthand to set the same default for **insetStart** and **insetEnd**.
#' @param inset_bottom `<number | param()>` Insets the bottom edge by the specified number of pixels.
#' @param inset_top `<number | param()>` Insets the top edge by the specified number of pixels.
#' @param interval `<"day"/"week"/"month"/... | number | param()>` Enforces uniformity for data at regular intervals, such as integer values or daily samples.
#' @param label `<string | NULL | param()>` A textual label to show on the axis or legend; if null, show no label.
#' @param label_anchor `<"top" | "right" | "bottom" | "left" | "center" | param()>` Where to place the axis **label** relative to the plot’s frame.
#' @param label_arrow `<"auto" | "up" | "right" | "down" | "left" | "none" | TRUE | FALSE | NULL | param()>` Whether to apply a directional arrow such as → or ↑ to the scale label.
#' @param label_offset `<number | param()>` The axis **label** position offset (in pixels); default depends on margins and orientation.
#' @param line_anchor `<"top" | "middle" | "bottom" | param()>` The line anchor controls how text is aligned (typically vertically) relative to its anchor point; it is one of *top*, *bottom*, or *middle*.
#' @param line_height `<number | param()>` The line height in ems; defaults to 1.
#' @param line_width `<number | param()>` The line width in ems (e.g., 10 for about 20 characters); defaults to infinity, disabling wrapping and clipping.
#' @param margin `<number | param()>` Shorthand to set the same default for all four mark margins: **marginTop**, **marginRight**, **marginBottom**, and **marginLeft**; typically defaults to 0, except for axis marks.
#' @param margin_bottom `<number | param()>` The mark’s bottom margin; the minimum distance in pixels between the bottom edges of the inner and outer plot area.
#' @param margin_left `<number | param()>` The mark’s left margin; the minimum distance in pixels between the left edges of the inner and outer plot area.
#' @param margin_right `<number | param()>` The mark’s right margin; the minimum distance in pixels between the right edges of the mark’s inner and outer plot area.
#' @param margin_top `<number | param()>` The mark’s top margin; the minimum distance in pixels between the top edges of the inner and outer plot area.
#' @param marker `<"arrow" | "arrow-reverse" | "dot" | "circle" | "circle-fill" | "circle-stroke" | "tick" | "tick-x" | "tick-y" | "none" | boolean | NULL | param()>` Shorthand to set the same default for markerStart, markerMid, and markerEnd; one of: - a marker name such as *arrow* or *circle* - *none* (default) - no marker * true - alias for *circle-fill* * false or null - alias for *none*
#' @param marker_end `<"arrow" | "arrow-reverse" | "dot" | "circle" | "circle-fill" | "circle-stroke" | "tick" | "tick-x" | "tick-y" | "none" | boolean | NULL | param()>` The marker for the ending point of a line segment; one of: - a marker name such as *arrow* or *circle* * *none* (default) - no marker * true - alias for *circle-fill* * false or null - alias for *none*
#' @param marker_mid `<"arrow" | "arrow-reverse" | "dot" | "circle" | "circle-fill" | "circle-stroke" | "tick" | "tick-x" | "tick-y" | "none" | boolean | NULL | param()>` The marker for any middle (interior) points of a line segment.
#' @param marker_start `<"arrow" | "arrow-reverse" | "dot" | "circle" | "circle-fill" | "circle-stroke" | "tick" | "tick-x" | "tick-y" | "none" | boolean | NULL | param()>` The marker for the starting point of a line segment; one of: - a marker name such as *arrow* or *circle* * *none* (default) - no marker * true - alias for *circle-fill* * false or null - alias for *none*
#' @param mix_blend_mode `<string | param()>` The mix-blend-mode; a constant string specifying how to blend content such as *multiply*.
#' @param monospace `<boolean | param()>` If true, changes the default **fontFamily** to *monospace*, and uses simplified monospaced text metrics calculations.
#' @param opacity `<column | literal | transform() | sql()/agg() | list(value=, ...)>` The opacity; a constant between 0 and 1, or a channel typically bound to the *opacity* scale.
#' @param paint_order `<string | param()>` The paint-order; a constant string specifying the order in which the **fill**, **stroke**, and any markers are drawn; defaults to *normal*, which draws the fill, then stroke, then markers; defaults to *stroke* for the text mark to create a “halo” around text to improve legibility.
#' @param pointer_events `<string | param()>` The pointer-events property; a constant string such as *none*.
#' @param reverse `<boolean | param()>` Applies a transform to reverse the order of the mark’s index, say for reverse input order.
#' @param rotate `<column | literal | transform() | sql()/agg() | param()>` The rotation angle in degrees clockwise; a constant or a channel; defaults to 0°.
#' @param select `<"first" | "last" | "maxX" | "maxY" | "minX" | "minY" | "nearest" | "nearestX" | "nearestY">` Applies a filter transform after data is loaded to highlight selected values only.
#' @param shape_rendering `<string | param()>` The shape-rendering; a constant string such as *crispEdges*.
#' @param sort `<column | literal | transform() | sql()/agg() | list>` Either applies a transform to sort the mark’s index by the specified channel values, or imputes ordinal scale domains from this mark’s channels.
#' @param stroke `<column | literal | transform() | sql()/agg() | list(value=, ...) | param()>` The stroke; a constant CSS color string, or a channel typically bound to the *color* scale.
#' @param stroke_dasharray `<string | number | param()>` The stroke-dasharray; a constant number indicating the length in pixels of alternating dashes and gaps, or a constant string of numbers separated by spaces or commas (_e.g._, *10 2* for dashes of 10 pixels separated by gaps of 2 pixels), or *none* (the default) for no dashing : https://developer.mozilla.org/en-US/docs/Web/SVG/Attribute/stroke-dasharray
#' @param stroke_dashoffset `<string | number | param()>` The stroke-dashoffset; a constant indicating the offset in pixels of the first dash along the stroke; defaults to zero.
#' @param stroke_linecap `<string | param()>` The stroke-linecap; a constant specifying how to cap stroked paths, such as *butt*, *round*, or *square*.
#' @param stroke_linejoin `<string | param()>` The stroke-linejoin; a constant specifying how to join stroked paths, such as *bevel*, *miter*, *miter-clip*, or *round*.
#' @param stroke_miterlimit `<number | param()>` The stroke-miterlimit; a constant number specifying how to limit the length of *miter* joins on stroked paths.
#' @param stroke_opacity `<column | literal | transform() | sql()/agg() | list(value=, ...)>` The stroke-opacity; a constant between 0 and 1, or a channel typically bound to the *opacity* scale.
#' @param stroke_width `<column | literal | transform() | sql()/agg() | list(value=, ...)>` The stroke-width; a constant number in pixels, or a channel.
#' @param target `<string | param()>` The target; a constant string specifying the target window (_e.g._, *_blank*) for clickable links; used in conjunction with the **href** option.
#' @param text `<column | literal | transform() | sql()/agg()>` The text contents channel, possibly with line breaks (\\n, \\r\\n, or \\r).
#' @param text_anchor `<"start" | "middle" | "end" | param()>` The text anchor controls how text is aligned (typically horizontally) relative to its anchor point; it is one of *start*, *end*, or *middle*.
#' @param text_overflow `<NULL | "clip" | "ellipsis" | "clip-start" | "clip-end" | "ellipsis-start" | "ellipsis-middle" | "ellipsis-end" | param()>` How truncate (or wrap) lines of text longer than the given **lineWidth**; one of: - null (default) - preserve overflowing characters (and wrap if needed) - *clip* or *clip-end* - remove characters from the end - *clip-start* - remove characters from the start - *ellipsis* or *ellipsis-end* - replace characters from the end with an ellipsis (…) - *ellipsis-start* - replace characters from the start with an ellipsis (…) - *ellipsis-middle* - replace characters from the middle with an ellipsis (…) If no **title** was specified, if text requires truncation, a title containing the non-truncated text will be implicitly added.
#' @param text_stroke `<column | literal | transform() | sql()/agg() | list(value=, ...) | param()>` The tick text **stroke**, say for a *white* outline to improve legibility; defaults to null.
#' @param text_stroke_opacity `<column | literal | transform() | sql()/agg() | list(value=, ...)>` The tick text **strokeOpacity**; defaults to 1; has no effect unless **textStroke** is set.
#' @param text_stroke_width `<column | literal | transform() | sql()/agg() | list(value=, ...)>` The tick text **strokeWidth**; defaults to 4; has no effect unless **textStroke** is set.
#' @param tick_format `<string | NULL | param()>` How to format inputs (abstract values) for axis tick labels; one of: - a d3-format string for numeric scales - a d3-time-format string for temporal scales : https://d3js.org/d3-time : https://d3js.org/d3-time-format
#' @param tick_padding `<number | param()>` The distance between an axis tick mark and its associated text label (in pixels); often defaults to 3, but may be affected by **xTickSize** and **xTickRotate**.
#' @param tick_rotate `<number | param()>` The rotation angle of axis tick labels in degrees clocksize; defaults to 0.
#' @param tick_size `<number | param()>` The length of axis tick marks in pixels; negative values extend in the opposite direction.
#' @param tick_spacing `<number | param()>` The desired approximate spacing between adjacent axis ticks, affecting the default **ticks**; defaults to 80 pixels for *x* and *fx*, and 35 pixels for *y* and *fy*.
#' @param ticks `<number | "day"/"week"/"month"/... | vector | param()>` The desired approximate number of axis ticks, or an explicit array of tick values, or an interval such as *day* or *month*.
#' @param tip `<boolean | "x" | "y" | "xy" | list | param()>` Whether to generate a tooltip for this mark, and any tip options.
#' @param title `<column | literal | transform() | sql()/agg()>` The title; a channel specifying accessible, short textual descriptions as strings (possibly with newlines).
#' @param x `<column | literal | transform() | sql()/agg() | list(value=, ...)>` The horizontal position (or length) channel, typically bound to the *x* scale.
#' @param y `<column | literal | transform() | sql()/agg() | list(value=, ...)>` The vertical position channel, typically bound to the *y* scale; defaults to the zero-based index of the data 0, 1, 2, ….
#' @param z `<column | literal | transform() | sql()/agg()>` An optional ordinal channel for grouping data into (possibly stacked) series to be drawn as separate areas; defaults to **fill** if a channel, or **stroke** if a channel.
#' @param ... Additional options or plot-level attributes.
#' @param data_from The name of the data source this mark reads from (see [vg_data()]); a length-1 nonzero R integer (`1L`, `-1L`, ...; note the `L`) giving a 1-based index into the spec's registered data sources instead, negative counting from the end (e.g., `-1L` for the most recently registered one); or a literal vector of values to use as inline data directly (mosaic-spec's `"data": [...]` shorthand, e.g., for a single reference line -- a bare double like `0`/`c(0)`, or `0L` itself (never a valid index), means this, not an index). Left unset, defaults to the first registered data source (equivalent to `data_from = 1L`) for any mark type that takes data at all.
#' @param filter_by A Param/Selection (e.g., from [param()]) to filter this mark's data by.
#' @param data_optimize A flag (default `TRUE`) to enable mark-specific query optimizations for this mark's data; set `FALSE` to disable them (mosaic-spec's `data: {optimize: false}`).
#' @family mark functions
#' @export
vg_mark_axis_fx <- function(spec = NULL, formula = vg_unset, anchor = vg_unset, aria_description = vg_unset, aria_hidden = vg_unset, aria_label = vg_unset, channels = vg_unset, clip = vg_unset, color = vg_unset, dx = vg_unset, dy = vg_unset, facet = vg_unset, facet_anchor = vg_unset, fill = vg_unset, fill_opacity = vg_unset, filter = vg_unset, font_family = vg_unset, font_size = vg_unset, font_style = vg_unset, font_variant = vg_unset, font_weight = vg_unset, frame_anchor = vg_unset, fx = vg_unset, fy = vg_unset, href = vg_unset, image_filter = vg_unset, inset = vg_unset, inset_bottom = vg_unset, inset_top = vg_unset, interval = vg_unset, label = vg_unset, label_anchor = vg_unset, label_arrow = vg_unset, label_offset = vg_unset, line_anchor = vg_unset, line_height = vg_unset, line_width = vg_unset, margin = vg_unset, margin_bottom = vg_unset, margin_left = vg_unset, margin_right = vg_unset, margin_top = vg_unset, marker = vg_unset, marker_end = vg_unset, marker_mid = vg_unset, marker_start = vg_unset, mix_blend_mode = vg_unset, monospace = vg_unset, opacity = vg_unset, paint_order = vg_unset, pointer_events = vg_unset, reverse = vg_unset, rotate = vg_unset, select = vg_unset, shape_rendering = vg_unset, sort = vg_unset, stroke = vg_unset, stroke_dasharray = vg_unset, stroke_dashoffset = vg_unset, stroke_linecap = vg_unset, stroke_linejoin = vg_unset, stroke_miterlimit = vg_unset, stroke_opacity = vg_unset, stroke_width = vg_unset, target = vg_unset, text = vg_unset, text_anchor = vg_unset, text_overflow = vg_unset, text_stroke = vg_unset, text_stroke_opacity = vg_unset, text_stroke_width = vg_unset, tick_format = vg_unset, tick_padding = vg_unset, tick_rotate = vg_unset, tick_size = vg_unset, tick_spacing = vg_unset, ticks = vg_unset, tip = vg_unset, title = vg_unset, x = vg_unset, y = vg_unset, z = vg_unset, ..., data_from = vg_unset, filter_by = vg_unset, data_optimize = vg_unset) {
  vg_mark_(spec, "axisFx", formula = formula, anchor = anchor, ariaDescription = aria_description, ariaHidden = aria_hidden, ariaLabel = aria_label, channels = channels, clip = clip, color = color, dx = dx, dy = dy, facet = facet, facetAnchor = facet_anchor, fill = fill, fillOpacity = fill_opacity, filter = filter, fontFamily = font_family, fontSize = font_size, fontStyle = font_style, fontVariant = font_variant, fontWeight = font_weight, frameAnchor = frame_anchor, fx = fx, fy = fy, href = href, imageFilter = image_filter, inset = inset, insetBottom = inset_bottom, insetTop = inset_top, interval = interval, label = label, labelAnchor = label_anchor, labelArrow = label_arrow, labelOffset = label_offset, lineAnchor = line_anchor, lineHeight = line_height, lineWidth = line_width, margin = margin, marginBottom = margin_bottom, marginLeft = margin_left, marginRight = margin_right, marginTop = margin_top, marker = marker, markerEnd = marker_end, markerMid = marker_mid, markerStart = marker_start, mixBlendMode = mix_blend_mode, monospace = monospace, opacity = opacity, paintOrder = paint_order, pointerEvents = pointer_events, reverse = reverse, rotate = rotate, select = select, shapeRendering = shape_rendering, sort = sort, stroke = stroke, strokeDasharray = stroke_dasharray, strokeDashoffset = stroke_dashoffset, strokeLinecap = stroke_linecap, strokeLinejoin = stroke_linejoin, strokeMiterlimit = stroke_miterlimit, strokeOpacity = stroke_opacity, strokeWidth = stroke_width, target = target, text = text, textAnchor = text_anchor, textOverflow = text_overflow, textStroke = text_stroke, textStrokeOpacity = text_stroke_opacity, textStrokeWidth = text_stroke_width, tickFormat = tick_format, tickPadding = tick_padding, tickRotate = tick_rotate, tickSize = tick_size, tickSpacing = tick_spacing, ticks = ticks, tip = tip, title = title, x = x, y = y, z = z, ..., data_from = data_from, filter_by = filter_by, data_optimize = data_optimize)
}

#' The axisFy mark.
#'
#' See [vg_value_types] for what the `<...>` notation below (`column`, `param()`, ...) means.
#'
#' @param spec A plot fragment or `vgspec` to add this mark to, or `NULL` to start a new plot with just this mark.
#' @param formula A shorthand for this mark's position channels (`x`, `y`, `fx`, `fy`, and paired `x1`/`x2` or `y1`/`y2`), e.g., `Sepal.Length ~ Sepal.Width | ~ Species` for `y = ~Sepal.Length, x = ~Sepal.Width, fx = ~Species`. See [vg_mark()] for the full grammar.
#' @param anchor `<"top" | "right" | "bottom" | "left" | param()>` The side of the frame on which to place the axis: *top* or *bottom* for horizontal axes (axisX and axisFx) and their associated vertical grids (gridX and gridFx), or *left* or *right* for vertical axes (axisY and axisFY) and their associated horizontal grids (gridY and gridFy).
#' @param aria_description `<string | param()>` The aria-description; a constant textual description.
#' @param aria_hidden `<string | param()>` The aria-hidden state; a constant indicating whether the element is exposed to an accessibility API.
#' @param aria_label `<column | literal | transform() | sql()/agg()>` The aria-label; a channel specifying short textual labels representing the value in the accessibility tree.
#' @param channels `<list>` Additional named channels, for example to include in a tooltip.
#' @param clip `<"frame" | "sphere" | boolean | NULL | param()>` How to clip the mark; one of: - *frame* or true - clip to the plot’s frame (inner area) - *sphere* - clip to the projected sphere (*e.g.*, front hemisphere) - null or false - do not clip The *sphere* clip option requires a geographic projection.
#' @param color `<column | literal | transform() | sql()/agg() | list(value=, ...) | param()>` A shorthand for setting both **fill** and **stroke**; affects the stroke of tick vectors and grid rules, and the fill of tick texts and axis label texts; defaults to *currentColor*.
#' @param dx `<number | param()>` The horizontal offset in pixels; a constant option.
#' @param dy `<number | param()>` The vertical offset in pixels; a constant option.
#' @param facet `<"auto" | "include" | "exclude" | "super" | boolean | NULL | param()>` Whether to enable or disable faceting; one of: - *auto* (default) - automatically determine if this mark should be faceted - *include* (or true) - draw the subset of the mark’s data in the current facet - *exclude* - draw the subset of the mark’s data *not* in the current facet - *super* - draw this mark in a single frame that covers all facets - null (or false) - repeat this mark’s data across all facets (*i.e.*, no faceting) When a mark uses *super* faceting, it is not allowed to use position scales (*x*, *y*, *fx*, or *fy*); *super* faceting is intended for decorations, such as labels and legends.
#' @param facet_anchor `<"top" | "right" | "bottom" | "left" | "top-left" | "top-right" | "bottom-left" | "bottom-right" | "top-empty" | "right-empty" | "bottom-empty" | "left-empty" | "empty" | NULL | param()>` How to place the mark with respect to facets; one of: - null (default for most marks) - display the mark in each non-empty facet - *top*, *right*, *bottom*, or *left* - display the mark only in facets on the given side - *top-empty*, *right-empty*, *bottom-empty*, or *left-empty* (default for axis marks) - display the mark only in facets that have empty space on the given side: either the margin, or an empty facet - *empty* - display the mark in empty facets only
#' @param fill `<column | literal | transform() | sql()/agg() | list(value=, ...) | param()>` The fill; a constant CSS color string, or a channel typically bound to the *color* scale.
#' @param fill_opacity `<column | literal | transform() | sql()/agg() | list(value=, ...) | param()>` The fill-opacity; a constant number between 0 and 1, or a channel typically bound to the *opacity* scale.
#' @param filter `<column | literal | transform() | sql()/agg()>` Applies a transform to filter the mark’s index according to the given channel values; only truthy values are retained.
#' @param font_family `<string | param()>` The font-family; a constant; defaults to the plot’s font family, which is typically *system-ui*.
#' @param font_size `<column | literal | transform() | sql()/agg() | param()>` The font size in pixels; either a constant or a channel; defaults to the plot’s font size, which is typically 10.
#' @param font_style `<string | param()>` The font style; a constant; defaults to the plot’s font style, which is typically *normal*.
#' @param font_variant `<string | param()>` The font variant; a constant; if the **text** channel contains numbers or dates, defaults to *tabular-nums* to facilitate comparing numbers; otherwise defaults to the plot’s font style, which is typically *normal*.
#' @param font_weight `<string | number | param()>` The font weight; a constant; defaults to the plot’s font weight, which is typically *normal*.
#' @param frame_anchor `<"middle" | "top-left" | "top" | "top-right" | "right" | "bottom-right" | "bottom" | "bottom-left" | "left" | param()>` The frame anchor specifies defaults for **x** and **y**, along with **textAnchor** and **lineAnchor**, based on the plot’s frame; it may be one of the four sides (*top*, *right*, *bottom*, *left*), one of the four corners (*top-left*, *top-right*, *bottom-right*, *bottom-left*), or the *middle* of the frame.
#' @param fx `<column | literal | transform() | sql()/agg()>` The horizontal facet position channel, for mark-level faceting, bound to the *fx* scale.
#' @param fy `<column | literal | transform() | sql()/agg()>` The vertical facet position channel, for mark-level faceting, bound to the *fy* scale.
#' @param href `<column | literal | transform() | sql()/agg()>` The href; a channel specifying URLs for clickable links.
#' @param image_filter `<string | param()>` A CSS filter; a constant string used to adjust the rendering of images, such as *blur(5px)*.
#' @param inset `<number | param()>` Shorthand to set the same default for **insetStart** and **insetEnd**.
#' @param inset_left `<number | param()>` Insets the left edge by the specified number of pixels.
#' @param inset_right `<number | param()>` Insets the right edge by the specified number of pixels.
#' @param interval `<"day"/"week"/"month"/... | number | param()>` Enforces uniformity for data at regular intervals, such as integer values or daily samples.
#' @param label `<string | NULL | param()>` A textual label to show on the axis or legend; if null, show no label.
#' @param label_anchor `<"top" | "right" | "bottom" | "left" | "center" | param()>` Where to place the axis **label** relative to the plot’s frame.
#' @param label_arrow `<"auto" | "up" | "right" | "down" | "left" | "none" | TRUE | FALSE | NULL | param()>` Whether to apply a directional arrow such as → or ↑ to the scale label.
#' @param label_offset `<number | param()>` The axis **label** position offset (in pixels); default depends on margins and orientation.
#' @param line_anchor `<"top" | "middle" | "bottom" | param()>` The line anchor controls how text is aligned (typically vertically) relative to its anchor point; it is one of *top*, *bottom*, or *middle*.
#' @param line_height `<number | param()>` The line height in ems; defaults to 1.
#' @param line_width `<number | param()>` The line width in ems (e.g., 10 for about 20 characters); defaults to infinity, disabling wrapping and clipping.
#' @param margin `<number | param()>` Shorthand to set the same default for all four mark margins: **marginTop**, **marginRight**, **marginBottom**, and **marginLeft**; typically defaults to 0, except for axis marks.
#' @param margin_bottom `<number | param()>` The mark’s bottom margin; the minimum distance in pixels between the bottom edges of the inner and outer plot area.
#' @param margin_left `<number | param()>` The mark’s left margin; the minimum distance in pixels between the left edges of the inner and outer plot area.
#' @param margin_right `<number | param()>` The mark’s right margin; the minimum distance in pixels between the right edges of the mark’s inner and outer plot area.
#' @param margin_top `<number | param()>` The mark’s top margin; the minimum distance in pixels between the top edges of the inner and outer plot area.
#' @param marker `<"arrow" | "arrow-reverse" | "dot" | "circle" | "circle-fill" | "circle-stroke" | "tick" | "tick-x" | "tick-y" | "none" | boolean | NULL | param()>` Shorthand to set the same default for markerStart, markerMid, and markerEnd; one of: - a marker name such as *arrow* or *circle* - *none* (default) - no marker * true - alias for *circle-fill* * false or null - alias for *none*
#' @param marker_end `<"arrow" | "arrow-reverse" | "dot" | "circle" | "circle-fill" | "circle-stroke" | "tick" | "tick-x" | "tick-y" | "none" | boolean | NULL | param()>` The marker for the ending point of a line segment; one of: - a marker name such as *arrow* or *circle* * *none* (default) - no marker * true - alias for *circle-fill* * false or null - alias for *none*
#' @param marker_mid `<"arrow" | "arrow-reverse" | "dot" | "circle" | "circle-fill" | "circle-stroke" | "tick" | "tick-x" | "tick-y" | "none" | boolean | NULL | param()>` The marker for any middle (interior) points of a line segment.
#' @param marker_start `<"arrow" | "arrow-reverse" | "dot" | "circle" | "circle-fill" | "circle-stroke" | "tick" | "tick-x" | "tick-y" | "none" | boolean | NULL | param()>` The marker for the starting point of a line segment; one of: - a marker name such as *arrow* or *circle* * *none* (default) - no marker * true - alias for *circle-fill* * false or null - alias for *none*
#' @param mix_blend_mode `<string | param()>` The mix-blend-mode; a constant string specifying how to blend content such as *multiply*.
#' @param monospace `<boolean | param()>` If true, changes the default **fontFamily** to *monospace*, and uses simplified monospaced text metrics calculations.
#' @param opacity `<column | literal | transform() | sql()/agg() | list(value=, ...)>` The opacity; a constant between 0 and 1, or a channel typically bound to the *opacity* scale.
#' @param paint_order `<string | param()>` The paint-order; a constant string specifying the order in which the **fill**, **stroke**, and any markers are drawn; defaults to *normal*, which draws the fill, then stroke, then markers; defaults to *stroke* for the text mark to create a “halo” around text to improve legibility.
#' @param pointer_events `<string | param()>` The pointer-events property; a constant string such as *none*.
#' @param reverse `<boolean | param()>` Applies a transform to reverse the order of the mark’s index, say for reverse input order.
#' @param rotate `<column | literal | transform() | sql()/agg() | param()>` The rotation angle in degrees clockwise; a constant or a channel; defaults to 0°.
#' @param select `<"first" | "last" | "maxX" | "maxY" | "minX" | "minY" | "nearest" | "nearestX" | "nearestY">` Applies a filter transform after data is loaded to highlight selected values only.
#' @param shape_rendering `<string | param()>` The shape-rendering; a constant string such as *crispEdges*.
#' @param sort `<column | literal | transform() | sql()/agg() | list>` Either applies a transform to sort the mark’s index by the specified channel values, or imputes ordinal scale domains from this mark’s channels.
#' @param stroke `<column | literal | transform() | sql()/agg() | list(value=, ...) | param()>` The stroke; a constant CSS color string, or a channel typically bound to the *color* scale.
#' @param stroke_dasharray `<string | number | param()>` The stroke-dasharray; a constant number indicating the length in pixels of alternating dashes and gaps, or a constant string of numbers separated by spaces or commas (_e.g._, *10 2* for dashes of 10 pixels separated by gaps of 2 pixels), or *none* (the default) for no dashing : https://developer.mozilla.org/en-US/docs/Web/SVG/Attribute/stroke-dasharray
#' @param stroke_dashoffset `<string | number | param()>` The stroke-dashoffset; a constant indicating the offset in pixels of the first dash along the stroke; defaults to zero.
#' @param stroke_linecap `<string | param()>` The stroke-linecap; a constant specifying how to cap stroked paths, such as *butt*, *round*, or *square*.
#' @param stroke_linejoin `<string | param()>` The stroke-linejoin; a constant specifying how to join stroked paths, such as *bevel*, *miter*, *miter-clip*, or *round*.
#' @param stroke_miterlimit `<number | param()>` The stroke-miterlimit; a constant number specifying how to limit the length of *miter* joins on stroked paths.
#' @param stroke_opacity `<column | literal | transform() | sql()/agg() | list(value=, ...)>` The stroke-opacity; a constant between 0 and 1, or a channel typically bound to the *opacity* scale.
#' @param stroke_width `<column | literal | transform() | sql()/agg() | list(value=, ...)>` The stroke-width; a constant number in pixels, or a channel.
#' @param target `<string | param()>` The target; a constant string specifying the target window (_e.g._, *_blank*) for clickable links; used in conjunction with the **href** option.
#' @param text `<column | literal | transform() | sql()/agg()>` The text contents channel, possibly with line breaks (\\n, \\r\\n, or \\r).
#' @param text_anchor `<"start" | "middle" | "end" | param()>` The text anchor controls how text is aligned (typically horizontally) relative to its anchor point; it is one of *start*, *end*, or *middle*.
#' @param text_overflow `<NULL | "clip" | "ellipsis" | "clip-start" | "clip-end" | "ellipsis-start" | "ellipsis-middle" | "ellipsis-end" | param()>` How truncate (or wrap) lines of text longer than the given **lineWidth**; one of: - null (default) - preserve overflowing characters (and wrap if needed) - *clip* or *clip-end* - remove characters from the end - *clip-start* - remove characters from the start - *ellipsis* or *ellipsis-end* - replace characters from the end with an ellipsis (…) - *ellipsis-start* - replace characters from the start with an ellipsis (…) - *ellipsis-middle* - replace characters from the middle with an ellipsis (…) If no **title** was specified, if text requires truncation, a title containing the non-truncated text will be implicitly added.
#' @param text_stroke `<column | literal | transform() | sql()/agg() | list(value=, ...) | param()>` The tick text **stroke**, say for a *white* outline to improve legibility; defaults to null.
#' @param text_stroke_opacity `<column | literal | transform() | sql()/agg() | list(value=, ...)>` The tick text **strokeOpacity**; defaults to 1; has no effect unless **textStroke** is set.
#' @param text_stroke_width `<column | literal | transform() | sql()/agg() | list(value=, ...)>` The tick text **strokeWidth**; defaults to 4; has no effect unless **textStroke** is set.
#' @param tick_format `<string | NULL | param()>` How to format inputs (abstract values) for axis tick labels; one of: - a d3-format string for numeric scales - a d3-time-format string for temporal scales : https://d3js.org/d3-time : https://d3js.org/d3-time-format
#' @param tick_padding `<number | param()>` The distance between an axis tick mark and its associated text label (in pixels); often defaults to 3, but may be affected by **xTickSize** and **xTickRotate**.
#' @param tick_rotate `<number | param()>` The rotation angle of axis tick labels in degrees clocksize; defaults to 0.
#' @param tick_size `<number | param()>` The length of axis tick marks in pixels; negative values extend in the opposite direction.
#' @param tick_spacing `<number | param()>` The desired approximate spacing between adjacent axis ticks, affecting the default **ticks**; defaults to 80 pixels for *x* and *fx*, and 35 pixels for *y* and *fy*.
#' @param ticks `<number | "day"/"week"/"month"/... | vector | param()>` The desired approximate number of axis ticks, or an explicit array of tick values, or an interval such as *day* or *month*.
#' @param tip `<boolean | "x" | "y" | "xy" | list | param()>` Whether to generate a tooltip for this mark, and any tip options.
#' @param title `<column | literal | transform() | sql()/agg()>` The title; a channel specifying accessible, short textual descriptions as strings (possibly with newlines).
#' @param x `<column | literal | transform() | sql()/agg() | list(value=, ...)>` The horizontal position (or length) channel, typically bound to the *x* scale.
#' @param y `<column | literal | transform() | sql()/agg() | list(value=, ...)>` The vertical position channel, typically bound to the *y* scale; defaults to the zero-based index of the data 0, 1, 2, ….
#' @param z `<column | literal | transform() | sql()/agg()>` An optional ordinal channel for grouping data into (possibly stacked) series to be drawn as separate areas; defaults to **fill** if a channel, or **stroke** if a channel.
#' @param ... Additional options or plot-level attributes.
#' @param data_from The name of the data source this mark reads from (see [vg_data()]); a length-1 nonzero R integer (`1L`, `-1L`, ...; note the `L`) giving a 1-based index into the spec's registered data sources instead, negative counting from the end (e.g., `-1L` for the most recently registered one); or a literal vector of values to use as inline data directly (mosaic-spec's `"data": [...]` shorthand, e.g., for a single reference line -- a bare double like `0`/`c(0)`, or `0L` itself (never a valid index), means this, not an index). Left unset, defaults to the first registered data source (equivalent to `data_from = 1L`) for any mark type that takes data at all.
#' @param filter_by A Param/Selection (e.g., from [param()]) to filter this mark's data by.
#' @param data_optimize A flag (default `TRUE`) to enable mark-specific query optimizations for this mark's data; set `FALSE` to disable them (mosaic-spec's `data: {optimize: false}`).
#' @family mark functions
#' @export
vg_mark_axis_fy <- function(spec = NULL, formula = vg_unset, anchor = vg_unset, aria_description = vg_unset, aria_hidden = vg_unset, aria_label = vg_unset, channels = vg_unset, clip = vg_unset, color = vg_unset, dx = vg_unset, dy = vg_unset, facet = vg_unset, facet_anchor = vg_unset, fill = vg_unset, fill_opacity = vg_unset, filter = vg_unset, font_family = vg_unset, font_size = vg_unset, font_style = vg_unset, font_variant = vg_unset, font_weight = vg_unset, frame_anchor = vg_unset, fx = vg_unset, fy = vg_unset, href = vg_unset, image_filter = vg_unset, inset = vg_unset, inset_left = vg_unset, inset_right = vg_unset, interval = vg_unset, label = vg_unset, label_anchor = vg_unset, label_arrow = vg_unset, label_offset = vg_unset, line_anchor = vg_unset, line_height = vg_unset, line_width = vg_unset, margin = vg_unset, margin_bottom = vg_unset, margin_left = vg_unset, margin_right = vg_unset, margin_top = vg_unset, marker = vg_unset, marker_end = vg_unset, marker_mid = vg_unset, marker_start = vg_unset, mix_blend_mode = vg_unset, monospace = vg_unset, opacity = vg_unset, paint_order = vg_unset, pointer_events = vg_unset, reverse = vg_unset, rotate = vg_unset, select = vg_unset, shape_rendering = vg_unset, sort = vg_unset, stroke = vg_unset, stroke_dasharray = vg_unset, stroke_dashoffset = vg_unset, stroke_linecap = vg_unset, stroke_linejoin = vg_unset, stroke_miterlimit = vg_unset, stroke_opacity = vg_unset, stroke_width = vg_unset, target = vg_unset, text = vg_unset, text_anchor = vg_unset, text_overflow = vg_unset, text_stroke = vg_unset, text_stroke_opacity = vg_unset, text_stroke_width = vg_unset, tick_format = vg_unset, tick_padding = vg_unset, tick_rotate = vg_unset, tick_size = vg_unset, tick_spacing = vg_unset, ticks = vg_unset, tip = vg_unset, title = vg_unset, x = vg_unset, y = vg_unset, z = vg_unset, ..., data_from = vg_unset, filter_by = vg_unset, data_optimize = vg_unset) {
  vg_mark_(spec, "axisFy", formula = formula, anchor = anchor, ariaDescription = aria_description, ariaHidden = aria_hidden, ariaLabel = aria_label, channels = channels, clip = clip, color = color, dx = dx, dy = dy, facet = facet, facetAnchor = facet_anchor, fill = fill, fillOpacity = fill_opacity, filter = filter, fontFamily = font_family, fontSize = font_size, fontStyle = font_style, fontVariant = font_variant, fontWeight = font_weight, frameAnchor = frame_anchor, fx = fx, fy = fy, href = href, imageFilter = image_filter, inset = inset, insetLeft = inset_left, insetRight = inset_right, interval = interval, label = label, labelAnchor = label_anchor, labelArrow = label_arrow, labelOffset = label_offset, lineAnchor = line_anchor, lineHeight = line_height, lineWidth = line_width, margin = margin, marginBottom = margin_bottom, marginLeft = margin_left, marginRight = margin_right, marginTop = margin_top, marker = marker, markerEnd = marker_end, markerMid = marker_mid, markerStart = marker_start, mixBlendMode = mix_blend_mode, monospace = monospace, opacity = opacity, paintOrder = paint_order, pointerEvents = pointer_events, reverse = reverse, rotate = rotate, select = select, shapeRendering = shape_rendering, sort = sort, stroke = stroke, strokeDasharray = stroke_dasharray, strokeDashoffset = stroke_dashoffset, strokeLinecap = stroke_linecap, strokeLinejoin = stroke_linejoin, strokeMiterlimit = stroke_miterlimit, strokeOpacity = stroke_opacity, strokeWidth = stroke_width, target = target, text = text, textAnchor = text_anchor, textOverflow = text_overflow, textStroke = text_stroke, textStrokeOpacity = text_stroke_opacity, textStrokeWidth = text_stroke_width, tickFormat = tick_format, tickPadding = tick_padding, tickRotate = tick_rotate, tickSize = tick_size, tickSpacing = tick_spacing, ticks = ticks, tip = tip, title = title, x = x, y = y, z = z, ..., data_from = data_from, filter_by = filter_by, data_optimize = data_optimize)
}

#' The axisX mark.
#'
#' See [vg_value_types] for what the `<...>` notation below (`column`, `param()`, ...) means.
#'
#' @param spec A plot fragment or `vgspec` to add this mark to, or `NULL` to start a new plot with just this mark.
#' @param formula A shorthand for this mark's position channels (`x`, `y`, `fx`, `fy`, and paired `x1`/`x2` or `y1`/`y2`), e.g., `Sepal.Length ~ Sepal.Width | ~ Species` for `y = ~Sepal.Length, x = ~Sepal.Width, fx = ~Species`. See [vg_mark()] for the full grammar.
#' @param anchor `<"top" | "right" | "bottom" | "left" | param()>` The side of the frame on which to place the axis: *top* or *bottom* for horizontal axes (axisX and axisFx) and their associated vertical grids (gridX and gridFx), or *left* or *right* for vertical axes (axisY and axisFY) and their associated horizontal grids (gridY and gridFy).
#' @param aria_description `<string | param()>` The aria-description; a constant textual description.
#' @param aria_hidden `<string | param()>` The aria-hidden state; a constant indicating whether the element is exposed to an accessibility API.
#' @param aria_label `<column | literal | transform() | sql()/agg()>` The aria-label; a channel specifying short textual labels representing the value in the accessibility tree.
#' @param channels `<list>` Additional named channels, for example to include in a tooltip.
#' @param clip `<"frame" | "sphere" | boolean | NULL | param()>` How to clip the mark; one of: - *frame* or true - clip to the plot’s frame (inner area) - *sphere* - clip to the projected sphere (*e.g.*, front hemisphere) - null or false - do not clip The *sphere* clip option requires a geographic projection.
#' @param color `<column | literal | transform() | sql()/agg() | list(value=, ...) | param()>` A shorthand for setting both **fill** and **stroke**; affects the stroke of tick vectors and grid rules, and the fill of tick texts and axis label texts; defaults to *currentColor*.
#' @param dx `<number | param()>` The horizontal offset in pixels; a constant option.
#' @param dy `<number | param()>` The vertical offset in pixels; a constant option.
#' @param facet `<"auto" | "include" | "exclude" | "super" | boolean | NULL | param()>` Whether to enable or disable faceting; one of: - *auto* (default) - automatically determine if this mark should be faceted - *include* (or true) - draw the subset of the mark’s data in the current facet - *exclude* - draw the subset of the mark’s data *not* in the current facet - *super* - draw this mark in a single frame that covers all facets - null (or false) - repeat this mark’s data across all facets (*i.e.*, no faceting) When a mark uses *super* faceting, it is not allowed to use position scales (*x*, *y*, *fx*, or *fy*); *super* faceting is intended for decorations, such as labels and legends.
#' @param facet_anchor `<"top" | "right" | "bottom" | "left" | "top-left" | "top-right" | "bottom-left" | "bottom-right" | "top-empty" | "right-empty" | "bottom-empty" | "left-empty" | "empty" | NULL | param()>` How to place the mark with respect to facets; one of: - null (default for most marks) - display the mark in each non-empty facet - *top*, *right*, *bottom*, or *left* - display the mark only in facets on the given side - *top-empty*, *right-empty*, *bottom-empty*, or *left-empty* (default for axis marks) - display the mark only in facets that have empty space on the given side: either the margin, or an empty facet - *empty* - display the mark in empty facets only
#' @param fill `<column | literal | transform() | sql()/agg() | list(value=, ...) | param()>` The fill; a constant CSS color string, or a channel typically bound to the *color* scale.
#' @param fill_opacity `<column | literal | transform() | sql()/agg() | list(value=, ...) | param()>` The fill-opacity; a constant number between 0 and 1, or a channel typically bound to the *opacity* scale.
#' @param filter `<column | literal | transform() | sql()/agg()>` Applies a transform to filter the mark’s index according to the given channel values; only truthy values are retained.
#' @param font_family `<string | param()>` The font-family; a constant; defaults to the plot’s font family, which is typically *system-ui*.
#' @param font_size `<column | literal | transform() | sql()/agg() | param()>` The font size in pixels; either a constant or a channel; defaults to the plot’s font size, which is typically 10.
#' @param font_style `<string | param()>` The font style; a constant; defaults to the plot’s font style, which is typically *normal*.
#' @param font_variant `<string | param()>` The font variant; a constant; if the **text** channel contains numbers or dates, defaults to *tabular-nums* to facilitate comparing numbers; otherwise defaults to the plot’s font style, which is typically *normal*.
#' @param font_weight `<string | number | param()>` The font weight; a constant; defaults to the plot’s font weight, which is typically *normal*.
#' @param frame_anchor `<"middle" | "top-left" | "top" | "top-right" | "right" | "bottom-right" | "bottom" | "bottom-left" | "left" | param()>` The frame anchor specifies defaults for **x** and **y**, along with **textAnchor** and **lineAnchor**, based on the plot’s frame; it may be one of the four sides (*top*, *right*, *bottom*, *left*), one of the four corners (*top-left*, *top-right*, *bottom-right*, *bottom-left*), or the *middle* of the frame.
#' @param fx `<column | literal | transform() | sql()/agg()>` The horizontal facet position channel, for mark-level faceting, bound to the *fx* scale.
#' @param fy `<column | literal | transform() | sql()/agg()>` The vertical facet position channel, for mark-level faceting, bound to the *fy* scale.
#' @param href `<column | literal | transform() | sql()/agg()>` The href; a channel specifying URLs for clickable links.
#' @param image_filter `<string | param()>` A CSS filter; a constant string used to adjust the rendering of images, such as *blur(5px)*.
#' @param inset `<number | param()>` Shorthand to set the same default for **insetStart** and **insetEnd**.
#' @param inset_bottom `<number | param()>` Insets the bottom edge by the specified number of pixels.
#' @param inset_top `<number | param()>` Insets the top edge by the specified number of pixels.
#' @param interval `<"day"/"week"/"month"/... | number | param()>` Enforces uniformity for data at regular intervals, such as integer values or daily samples.
#' @param label `<string | NULL | param()>` A textual label to show on the axis or legend; if null, show no label.
#' @param label_anchor `<"top" | "right" | "bottom" | "left" | "center" | param()>` Where to place the axis **label** relative to the plot’s frame.
#' @param label_arrow `<"auto" | "up" | "right" | "down" | "left" | "none" | TRUE | FALSE | NULL | param()>` Whether to apply a directional arrow such as → or ↑ to the scale label.
#' @param label_offset `<number | param()>` The axis **label** position offset (in pixels); default depends on margins and orientation.
#' @param line_anchor `<"top" | "middle" | "bottom" | param()>` The line anchor controls how text is aligned (typically vertically) relative to its anchor point; it is one of *top*, *bottom*, or *middle*.
#' @param line_height `<number | param()>` The line height in ems; defaults to 1.
#' @param line_width `<number | param()>` The line width in ems (e.g., 10 for about 20 characters); defaults to infinity, disabling wrapping and clipping.
#' @param margin `<number | param()>` Shorthand to set the same default for all four mark margins: **marginTop**, **marginRight**, **marginBottom**, and **marginLeft**; typically defaults to 0, except for axis marks.
#' @param margin_bottom `<number | param()>` The mark’s bottom margin; the minimum distance in pixels between the bottom edges of the inner and outer plot area.
#' @param margin_left `<number | param()>` The mark’s left margin; the minimum distance in pixels between the left edges of the inner and outer plot area.
#' @param margin_right `<number | param()>` The mark’s right margin; the minimum distance in pixels between the right edges of the mark’s inner and outer plot area.
#' @param margin_top `<number | param()>` The mark’s top margin; the minimum distance in pixels between the top edges of the inner and outer plot area.
#' @param marker `<"arrow" | "arrow-reverse" | "dot" | "circle" | "circle-fill" | "circle-stroke" | "tick" | "tick-x" | "tick-y" | "none" | boolean | NULL | param()>` Shorthand to set the same default for markerStart, markerMid, and markerEnd; one of: - a marker name such as *arrow* or *circle* - *none* (default) - no marker * true - alias for *circle-fill* * false or null - alias for *none*
#' @param marker_end `<"arrow" | "arrow-reverse" | "dot" | "circle" | "circle-fill" | "circle-stroke" | "tick" | "tick-x" | "tick-y" | "none" | boolean | NULL | param()>` The marker for the ending point of a line segment; one of: - a marker name such as *arrow* or *circle* * *none* (default) - no marker * true - alias for *circle-fill* * false or null - alias for *none*
#' @param marker_mid `<"arrow" | "arrow-reverse" | "dot" | "circle" | "circle-fill" | "circle-stroke" | "tick" | "tick-x" | "tick-y" | "none" | boolean | NULL | param()>` The marker for any middle (interior) points of a line segment.
#' @param marker_start `<"arrow" | "arrow-reverse" | "dot" | "circle" | "circle-fill" | "circle-stroke" | "tick" | "tick-x" | "tick-y" | "none" | boolean | NULL | param()>` The marker for the starting point of a line segment; one of: - a marker name such as *arrow* or *circle* * *none* (default) - no marker * true - alias for *circle-fill* * false or null - alias for *none*
#' @param mix_blend_mode `<string | param()>` The mix-blend-mode; a constant string specifying how to blend content such as *multiply*.
#' @param monospace `<boolean | param()>` If true, changes the default **fontFamily** to *monospace*, and uses simplified monospaced text metrics calculations.
#' @param opacity `<column | literal | transform() | sql()/agg() | list(value=, ...)>` The opacity; a constant between 0 and 1, or a channel typically bound to the *opacity* scale.
#' @param paint_order `<string | param()>` The paint-order; a constant string specifying the order in which the **fill**, **stroke**, and any markers are drawn; defaults to *normal*, which draws the fill, then stroke, then markers; defaults to *stroke* for the text mark to create a “halo” around text to improve legibility.
#' @param pointer_events `<string | param()>` The pointer-events property; a constant string such as *none*.
#' @param reverse `<boolean | param()>` Applies a transform to reverse the order of the mark’s index, say for reverse input order.
#' @param rotate `<column | literal | transform() | sql()/agg() | param()>` The rotation angle in degrees clockwise; a constant or a channel; defaults to 0°.
#' @param select `<"first" | "last" | "maxX" | "maxY" | "minX" | "minY" | "nearest" | "nearestX" | "nearestY">` Applies a filter transform after data is loaded to highlight selected values only.
#' @param shape_rendering `<string | param()>` The shape-rendering; a constant string such as *crispEdges*.
#' @param sort `<column | literal | transform() | sql()/agg() | list>` Either applies a transform to sort the mark’s index by the specified channel values, or imputes ordinal scale domains from this mark’s channels.
#' @param stroke `<column | literal | transform() | sql()/agg() | list(value=, ...) | param()>` The stroke; a constant CSS color string, or a channel typically bound to the *color* scale.
#' @param stroke_dasharray `<string | number | param()>` The stroke-dasharray; a constant number indicating the length in pixels of alternating dashes and gaps, or a constant string of numbers separated by spaces or commas (_e.g._, *10 2* for dashes of 10 pixels separated by gaps of 2 pixels), or *none* (the default) for no dashing : https://developer.mozilla.org/en-US/docs/Web/SVG/Attribute/stroke-dasharray
#' @param stroke_dashoffset `<string | number | param()>` The stroke-dashoffset; a constant indicating the offset in pixels of the first dash along the stroke; defaults to zero.
#' @param stroke_linecap `<string | param()>` The stroke-linecap; a constant specifying how to cap stroked paths, such as *butt*, *round*, or *square*.
#' @param stroke_linejoin `<string | param()>` The stroke-linejoin; a constant specifying how to join stroked paths, such as *bevel*, *miter*, *miter-clip*, or *round*.
#' @param stroke_miterlimit `<number | param()>` The stroke-miterlimit; a constant number specifying how to limit the length of *miter* joins on stroked paths.
#' @param stroke_opacity `<column | literal | transform() | sql()/agg() | list(value=, ...)>` The stroke-opacity; a constant between 0 and 1, or a channel typically bound to the *opacity* scale.
#' @param stroke_width `<column | literal | transform() | sql()/agg() | list(value=, ...)>` The stroke-width; a constant number in pixels, or a channel.
#' @param target `<string | param()>` The target; a constant string specifying the target window (_e.g._, *_blank*) for clickable links; used in conjunction with the **href** option.
#' @param text `<column | literal | transform() | sql()/agg()>` The text contents channel, possibly with line breaks (\\n, \\r\\n, or \\r).
#' @param text_anchor `<"start" | "middle" | "end" | param()>` The text anchor controls how text is aligned (typically horizontally) relative to its anchor point; it is one of *start*, *end*, or *middle*.
#' @param text_overflow `<NULL | "clip" | "ellipsis" | "clip-start" | "clip-end" | "ellipsis-start" | "ellipsis-middle" | "ellipsis-end" | param()>` How truncate (or wrap) lines of text longer than the given **lineWidth**; one of: - null (default) - preserve overflowing characters (and wrap if needed) - *clip* or *clip-end* - remove characters from the end - *clip-start* - remove characters from the start - *ellipsis* or *ellipsis-end* - replace characters from the end with an ellipsis (…) - *ellipsis-start* - replace characters from the start with an ellipsis (…) - *ellipsis-middle* - replace characters from the middle with an ellipsis (…) If no **title** was specified, if text requires truncation, a title containing the non-truncated text will be implicitly added.
#' @param text_stroke `<column | literal | transform() | sql()/agg() | list(value=, ...) | param()>` The tick text **stroke**, say for a *white* outline to improve legibility; defaults to null.
#' @param text_stroke_opacity `<column | literal | transform() | sql()/agg() | list(value=, ...)>` The tick text **strokeOpacity**; defaults to 1; has no effect unless **textStroke** is set.
#' @param text_stroke_width `<column | literal | transform() | sql()/agg() | list(value=, ...)>` The tick text **strokeWidth**; defaults to 4; has no effect unless **textStroke** is set.
#' @param tick_format `<string | NULL | param()>` How to format inputs (abstract values) for axis tick labels; one of: - a d3-format string for numeric scales - a d3-time-format string for temporal scales : https://d3js.org/d3-time : https://d3js.org/d3-time-format
#' @param tick_padding `<number | param()>` The distance between an axis tick mark and its associated text label (in pixels); often defaults to 3, but may be affected by **xTickSize** and **xTickRotate**.
#' @param tick_rotate `<number | param()>` The rotation angle of axis tick labels in degrees clocksize; defaults to 0.
#' @param tick_size `<number | param()>` The length of axis tick marks in pixels; negative values extend in the opposite direction.
#' @param tick_spacing `<number | param()>` The desired approximate spacing between adjacent axis ticks, affecting the default **ticks**; defaults to 80 pixels for *x* and *fx*, and 35 pixels for *y* and *fy*.
#' @param ticks `<number | "day"/"week"/"month"/... | vector | param()>` The desired approximate number of axis ticks, or an explicit array of tick values, or an interval such as *day* or *month*.
#' @param tip `<boolean | "x" | "y" | "xy" | list | param()>` Whether to generate a tooltip for this mark, and any tip options.
#' @param title `<column | literal | transform() | sql()/agg()>` The title; a channel specifying accessible, short textual descriptions as strings (possibly with newlines).
#' @param x `<column | literal | transform() | sql()/agg() | list(value=, ...)>` The horizontal position (or length) channel, typically bound to the *x* scale.
#' @param y `<column | literal | transform() | sql()/agg() | list(value=, ...)>` The vertical position channel, typically bound to the *y* scale; defaults to the zero-based index of the data 0, 1, 2, ….
#' @param z `<column | literal | transform() | sql()/agg()>` An optional ordinal channel for grouping data into (possibly stacked) series to be drawn as separate areas; defaults to **fill** if a channel, or **stroke** if a channel.
#' @param ... Additional options or plot-level attributes.
#' @param data_from The name of the data source this mark reads from (see [vg_data()]); a length-1 nonzero R integer (`1L`, `-1L`, ...; note the `L`) giving a 1-based index into the spec's registered data sources instead, negative counting from the end (e.g., `-1L` for the most recently registered one); or a literal vector of values to use as inline data directly (mosaic-spec's `"data": [...]` shorthand, e.g., for a single reference line -- a bare double like `0`/`c(0)`, or `0L` itself (never a valid index), means this, not an index). Left unset, defaults to the first registered data source (equivalent to `data_from = 1L`) for any mark type that takes data at all.
#' @param filter_by A Param/Selection (e.g., from [param()]) to filter this mark's data by.
#' @param data_optimize A flag (default `TRUE`) to enable mark-specific query optimizations for this mark's data; set `FALSE` to disable them (mosaic-spec's `data: {optimize: false}`).
#' @family mark functions
#' @export
vg_mark_axis_x <- function(spec = NULL, formula = vg_unset, anchor = vg_unset, aria_description = vg_unset, aria_hidden = vg_unset, aria_label = vg_unset, channels = vg_unset, clip = vg_unset, color = vg_unset, dx = vg_unset, dy = vg_unset, facet = vg_unset, facet_anchor = vg_unset, fill = vg_unset, fill_opacity = vg_unset, filter = vg_unset, font_family = vg_unset, font_size = vg_unset, font_style = vg_unset, font_variant = vg_unset, font_weight = vg_unset, frame_anchor = vg_unset, fx = vg_unset, fy = vg_unset, href = vg_unset, image_filter = vg_unset, inset = vg_unset, inset_bottom = vg_unset, inset_top = vg_unset, interval = vg_unset, label = vg_unset, label_anchor = vg_unset, label_arrow = vg_unset, label_offset = vg_unset, line_anchor = vg_unset, line_height = vg_unset, line_width = vg_unset, margin = vg_unset, margin_bottom = vg_unset, margin_left = vg_unset, margin_right = vg_unset, margin_top = vg_unset, marker = vg_unset, marker_end = vg_unset, marker_mid = vg_unset, marker_start = vg_unset, mix_blend_mode = vg_unset, monospace = vg_unset, opacity = vg_unset, paint_order = vg_unset, pointer_events = vg_unset, reverse = vg_unset, rotate = vg_unset, select = vg_unset, shape_rendering = vg_unset, sort = vg_unset, stroke = vg_unset, stroke_dasharray = vg_unset, stroke_dashoffset = vg_unset, stroke_linecap = vg_unset, stroke_linejoin = vg_unset, stroke_miterlimit = vg_unset, stroke_opacity = vg_unset, stroke_width = vg_unset, target = vg_unset, text = vg_unset, text_anchor = vg_unset, text_overflow = vg_unset, text_stroke = vg_unset, text_stroke_opacity = vg_unset, text_stroke_width = vg_unset, tick_format = vg_unset, tick_padding = vg_unset, tick_rotate = vg_unset, tick_size = vg_unset, tick_spacing = vg_unset, ticks = vg_unset, tip = vg_unset, title = vg_unset, x = vg_unset, y = vg_unset, z = vg_unset, ..., data_from = vg_unset, filter_by = vg_unset, data_optimize = vg_unset) {
  vg_mark_(spec, "axisX", formula = formula, anchor = anchor, ariaDescription = aria_description, ariaHidden = aria_hidden, ariaLabel = aria_label, channels = channels, clip = clip, color = color, dx = dx, dy = dy, facet = facet, facetAnchor = facet_anchor, fill = fill, fillOpacity = fill_opacity, filter = filter, fontFamily = font_family, fontSize = font_size, fontStyle = font_style, fontVariant = font_variant, fontWeight = font_weight, frameAnchor = frame_anchor, fx = fx, fy = fy, href = href, imageFilter = image_filter, inset = inset, insetBottom = inset_bottom, insetTop = inset_top, interval = interval, label = label, labelAnchor = label_anchor, labelArrow = label_arrow, labelOffset = label_offset, lineAnchor = line_anchor, lineHeight = line_height, lineWidth = line_width, margin = margin, marginBottom = margin_bottom, marginLeft = margin_left, marginRight = margin_right, marginTop = margin_top, marker = marker, markerEnd = marker_end, markerMid = marker_mid, markerStart = marker_start, mixBlendMode = mix_blend_mode, monospace = monospace, opacity = opacity, paintOrder = paint_order, pointerEvents = pointer_events, reverse = reverse, rotate = rotate, select = select, shapeRendering = shape_rendering, sort = sort, stroke = stroke, strokeDasharray = stroke_dasharray, strokeDashoffset = stroke_dashoffset, strokeLinecap = stroke_linecap, strokeLinejoin = stroke_linejoin, strokeMiterlimit = stroke_miterlimit, strokeOpacity = stroke_opacity, strokeWidth = stroke_width, target = target, text = text, textAnchor = text_anchor, textOverflow = text_overflow, textStroke = text_stroke, textStrokeOpacity = text_stroke_opacity, textStrokeWidth = text_stroke_width, tickFormat = tick_format, tickPadding = tick_padding, tickRotate = tick_rotate, tickSize = tick_size, tickSpacing = tick_spacing, ticks = ticks, tip = tip, title = title, x = x, y = y, z = z, ..., data_from = data_from, filter_by = filter_by, data_optimize = data_optimize)
}

#' The axisY mark.
#'
#' See [vg_value_types] for what the `<...>` notation below (`column`, `param()`, ...) means.
#'
#' @param spec A plot fragment or `vgspec` to add this mark to, or `NULL` to start a new plot with just this mark.
#' @param formula A shorthand for this mark's position channels (`x`, `y`, `fx`, `fy`, and paired `x1`/`x2` or `y1`/`y2`), e.g., `Sepal.Length ~ Sepal.Width | ~ Species` for `y = ~Sepal.Length, x = ~Sepal.Width, fx = ~Species`. See [vg_mark()] for the full grammar.
#' @param anchor `<"top" | "right" | "bottom" | "left" | param()>` The side of the frame on which to place the axis: *top* or *bottom* for horizontal axes (axisX and axisFx) and their associated vertical grids (gridX and gridFx), or *left* or *right* for vertical axes (axisY and axisFY) and their associated horizontal grids (gridY and gridFy).
#' @param aria_description `<string | param()>` The aria-description; a constant textual description.
#' @param aria_hidden `<string | param()>` The aria-hidden state; a constant indicating whether the element is exposed to an accessibility API.
#' @param aria_label `<column | literal | transform() | sql()/agg()>` The aria-label; a channel specifying short textual labels representing the value in the accessibility tree.
#' @param channels `<list>` Additional named channels, for example to include in a tooltip.
#' @param clip `<"frame" | "sphere" | boolean | NULL | param()>` How to clip the mark; one of: - *frame* or true - clip to the plot’s frame (inner area) - *sphere* - clip to the projected sphere (*e.g.*, front hemisphere) - null or false - do not clip The *sphere* clip option requires a geographic projection.
#' @param color `<column | literal | transform() | sql()/agg() | list(value=, ...) | param()>` A shorthand for setting both **fill** and **stroke**; affects the stroke of tick vectors and grid rules, and the fill of tick texts and axis label texts; defaults to *currentColor*.
#' @param dx `<number | param()>` The horizontal offset in pixels; a constant option.
#' @param dy `<number | param()>` The vertical offset in pixels; a constant option.
#' @param facet `<"auto" | "include" | "exclude" | "super" | boolean | NULL | param()>` Whether to enable or disable faceting; one of: - *auto* (default) - automatically determine if this mark should be faceted - *include* (or true) - draw the subset of the mark’s data in the current facet - *exclude* - draw the subset of the mark’s data *not* in the current facet - *super* - draw this mark in a single frame that covers all facets - null (or false) - repeat this mark’s data across all facets (*i.e.*, no faceting) When a mark uses *super* faceting, it is not allowed to use position scales (*x*, *y*, *fx*, or *fy*); *super* faceting is intended for decorations, such as labels and legends.
#' @param facet_anchor `<"top" | "right" | "bottom" | "left" | "top-left" | "top-right" | "bottom-left" | "bottom-right" | "top-empty" | "right-empty" | "bottom-empty" | "left-empty" | "empty" | NULL | param()>` How to place the mark with respect to facets; one of: - null (default for most marks) - display the mark in each non-empty facet - *top*, *right*, *bottom*, or *left* - display the mark only in facets on the given side - *top-empty*, *right-empty*, *bottom-empty*, or *left-empty* (default for axis marks) - display the mark only in facets that have empty space on the given side: either the margin, or an empty facet - *empty* - display the mark in empty facets only
#' @param fill `<column | literal | transform() | sql()/agg() | list(value=, ...) | param()>` The fill; a constant CSS color string, or a channel typically bound to the *color* scale.
#' @param fill_opacity `<column | literal | transform() | sql()/agg() | list(value=, ...) | param()>` The fill-opacity; a constant number between 0 and 1, or a channel typically bound to the *opacity* scale.
#' @param filter `<column | literal | transform() | sql()/agg()>` Applies a transform to filter the mark’s index according to the given channel values; only truthy values are retained.
#' @param font_family `<string | param()>` The font-family; a constant; defaults to the plot’s font family, which is typically *system-ui*.
#' @param font_size `<column | literal | transform() | sql()/agg() | param()>` The font size in pixels; either a constant or a channel; defaults to the plot’s font size, which is typically 10.
#' @param font_style `<string | param()>` The font style; a constant; defaults to the plot’s font style, which is typically *normal*.
#' @param font_variant `<string | param()>` The font variant; a constant; if the **text** channel contains numbers or dates, defaults to *tabular-nums* to facilitate comparing numbers; otherwise defaults to the plot’s font style, which is typically *normal*.
#' @param font_weight `<string | number | param()>` The font weight; a constant; defaults to the plot’s font weight, which is typically *normal*.
#' @param frame_anchor `<"middle" | "top-left" | "top" | "top-right" | "right" | "bottom-right" | "bottom" | "bottom-left" | "left" | param()>` The frame anchor specifies defaults for **x** and **y**, along with **textAnchor** and **lineAnchor**, based on the plot’s frame; it may be one of the four sides (*top*, *right*, *bottom*, *left*), one of the four corners (*top-left*, *top-right*, *bottom-right*, *bottom-left*), or the *middle* of the frame.
#' @param fx `<column | literal | transform() | sql()/agg()>` The horizontal facet position channel, for mark-level faceting, bound to the *fx* scale.
#' @param fy `<column | literal | transform() | sql()/agg()>` The vertical facet position channel, for mark-level faceting, bound to the *fy* scale.
#' @param href `<column | literal | transform() | sql()/agg()>` The href; a channel specifying URLs for clickable links.
#' @param image_filter `<string | param()>` A CSS filter; a constant string used to adjust the rendering of images, such as *blur(5px)*.
#' @param inset `<number | param()>` Shorthand to set the same default for **insetStart** and **insetEnd**.
#' @param inset_left `<number | param()>` Insets the left edge by the specified number of pixels.
#' @param inset_right `<number | param()>` Insets the right edge by the specified number of pixels.
#' @param interval `<"day"/"week"/"month"/... | number | param()>` Enforces uniformity for data at regular intervals, such as integer values or daily samples.
#' @param label `<string | NULL | param()>` A textual label to show on the axis or legend; if null, show no label.
#' @param label_anchor `<"top" | "right" | "bottom" | "left" | "center" | param()>` Where to place the axis **label** relative to the plot’s frame.
#' @param label_arrow `<"auto" | "up" | "right" | "down" | "left" | "none" | TRUE | FALSE | NULL | param()>` Whether to apply a directional arrow such as → or ↑ to the scale label.
#' @param label_offset `<number | param()>` The axis **label** position offset (in pixels); default depends on margins and orientation.
#' @param line_anchor `<"top" | "middle" | "bottom" | param()>` The line anchor controls how text is aligned (typically vertically) relative to its anchor point; it is one of *top*, *bottom*, or *middle*.
#' @param line_height `<number | param()>` The line height in ems; defaults to 1.
#' @param line_width `<number | param()>` The line width in ems (e.g., 10 for about 20 characters); defaults to infinity, disabling wrapping and clipping.
#' @param margin `<number | param()>` Shorthand to set the same default for all four mark margins: **marginTop**, **marginRight**, **marginBottom**, and **marginLeft**; typically defaults to 0, except for axis marks.
#' @param margin_bottom `<number | param()>` The mark’s bottom margin; the minimum distance in pixels between the bottom edges of the inner and outer plot area.
#' @param margin_left `<number | param()>` The mark’s left margin; the minimum distance in pixels between the left edges of the inner and outer plot area.
#' @param margin_right `<number | param()>` The mark’s right margin; the minimum distance in pixels between the right edges of the mark’s inner and outer plot area.
#' @param margin_top `<number | param()>` The mark’s top margin; the minimum distance in pixels between the top edges of the inner and outer plot area.
#' @param marker `<"arrow" | "arrow-reverse" | "dot" | "circle" | "circle-fill" | "circle-stroke" | "tick" | "tick-x" | "tick-y" | "none" | boolean | NULL | param()>` Shorthand to set the same default for markerStart, markerMid, and markerEnd; one of: - a marker name such as *arrow* or *circle* - *none* (default) - no marker * true - alias for *circle-fill* * false or null - alias for *none*
#' @param marker_end `<"arrow" | "arrow-reverse" | "dot" | "circle" | "circle-fill" | "circle-stroke" | "tick" | "tick-x" | "tick-y" | "none" | boolean | NULL | param()>` The marker for the ending point of a line segment; one of: - a marker name such as *arrow* or *circle* * *none* (default) - no marker * true - alias for *circle-fill* * false or null - alias for *none*
#' @param marker_mid `<"arrow" | "arrow-reverse" | "dot" | "circle" | "circle-fill" | "circle-stroke" | "tick" | "tick-x" | "tick-y" | "none" | boolean | NULL | param()>` The marker for any middle (interior) points of a line segment.
#' @param marker_start `<"arrow" | "arrow-reverse" | "dot" | "circle" | "circle-fill" | "circle-stroke" | "tick" | "tick-x" | "tick-y" | "none" | boolean | NULL | param()>` The marker for the starting point of a line segment; one of: - a marker name such as *arrow* or *circle* * *none* (default) - no marker * true - alias for *circle-fill* * false or null - alias for *none*
#' @param mix_blend_mode `<string | param()>` The mix-blend-mode; a constant string specifying how to blend content such as *multiply*.
#' @param monospace `<boolean | param()>` If true, changes the default **fontFamily** to *monospace*, and uses simplified monospaced text metrics calculations.
#' @param opacity `<column | literal | transform() | sql()/agg() | list(value=, ...)>` The opacity; a constant between 0 and 1, or a channel typically bound to the *opacity* scale.
#' @param paint_order `<string | param()>` The paint-order; a constant string specifying the order in which the **fill**, **stroke**, and any markers are drawn; defaults to *normal*, which draws the fill, then stroke, then markers; defaults to *stroke* for the text mark to create a “halo” around text to improve legibility.
#' @param pointer_events `<string | param()>` The pointer-events property; a constant string such as *none*.
#' @param reverse `<boolean | param()>` Applies a transform to reverse the order of the mark’s index, say for reverse input order.
#' @param rotate `<column | literal | transform() | sql()/agg() | param()>` The rotation angle in degrees clockwise; a constant or a channel; defaults to 0°.
#' @param select `<"first" | "last" | "maxX" | "maxY" | "minX" | "minY" | "nearest" | "nearestX" | "nearestY">` Applies a filter transform after data is loaded to highlight selected values only.
#' @param shape_rendering `<string | param()>` The shape-rendering; a constant string such as *crispEdges*.
#' @param sort `<column | literal | transform() | sql()/agg() | list>` Either applies a transform to sort the mark’s index by the specified channel values, or imputes ordinal scale domains from this mark’s channels.
#' @param stroke `<column | literal | transform() | sql()/agg() | list(value=, ...) | param()>` The stroke; a constant CSS color string, or a channel typically bound to the *color* scale.
#' @param stroke_dasharray `<string | number | param()>` The stroke-dasharray; a constant number indicating the length in pixels of alternating dashes and gaps, or a constant string of numbers separated by spaces or commas (_e.g._, *10 2* for dashes of 10 pixels separated by gaps of 2 pixels), or *none* (the default) for no dashing : https://developer.mozilla.org/en-US/docs/Web/SVG/Attribute/stroke-dasharray
#' @param stroke_dashoffset `<string | number | param()>` The stroke-dashoffset; a constant indicating the offset in pixels of the first dash along the stroke; defaults to zero.
#' @param stroke_linecap `<string | param()>` The stroke-linecap; a constant specifying how to cap stroked paths, such as *butt*, *round*, or *square*.
#' @param stroke_linejoin `<string | param()>` The stroke-linejoin; a constant specifying how to join stroked paths, such as *bevel*, *miter*, *miter-clip*, or *round*.
#' @param stroke_miterlimit `<number | param()>` The stroke-miterlimit; a constant number specifying how to limit the length of *miter* joins on stroked paths.
#' @param stroke_opacity `<column | literal | transform() | sql()/agg() | list(value=, ...)>` The stroke-opacity; a constant between 0 and 1, or a channel typically bound to the *opacity* scale.
#' @param stroke_width `<column | literal | transform() | sql()/agg() | list(value=, ...)>` The stroke-width; a constant number in pixels, or a channel.
#' @param target `<string | param()>` The target; a constant string specifying the target window (_e.g._, *_blank*) for clickable links; used in conjunction with the **href** option.
#' @param text `<column | literal | transform() | sql()/agg()>` The text contents channel, possibly with line breaks (\\n, \\r\\n, or \\r).
#' @param text_anchor `<"start" | "middle" | "end" | param()>` The text anchor controls how text is aligned (typically horizontally) relative to its anchor point; it is one of *start*, *end*, or *middle*.
#' @param text_overflow `<NULL | "clip" | "ellipsis" | "clip-start" | "clip-end" | "ellipsis-start" | "ellipsis-middle" | "ellipsis-end" | param()>` How truncate (or wrap) lines of text longer than the given **lineWidth**; one of: - null (default) - preserve overflowing characters (and wrap if needed) - *clip* or *clip-end* - remove characters from the end - *clip-start* - remove characters from the start - *ellipsis* or *ellipsis-end* - replace characters from the end with an ellipsis (…) - *ellipsis-start* - replace characters from the start with an ellipsis (…) - *ellipsis-middle* - replace characters from the middle with an ellipsis (…) If no **title** was specified, if text requires truncation, a title containing the non-truncated text will be implicitly added.
#' @param text_stroke `<column | literal | transform() | sql()/agg() | list(value=, ...) | param()>` The tick text **stroke**, say for a *white* outline to improve legibility; defaults to null.
#' @param text_stroke_opacity `<column | literal | transform() | sql()/agg() | list(value=, ...)>` The tick text **strokeOpacity**; defaults to 1; has no effect unless **textStroke** is set.
#' @param text_stroke_width `<column | literal | transform() | sql()/agg() | list(value=, ...)>` The tick text **strokeWidth**; defaults to 4; has no effect unless **textStroke** is set.
#' @param tick_format `<string | NULL | param()>` How to format inputs (abstract values) for axis tick labels; one of: - a d3-format string for numeric scales - a d3-time-format string for temporal scales : https://d3js.org/d3-time : https://d3js.org/d3-time-format
#' @param tick_padding `<number | param()>` The distance between an axis tick mark and its associated text label (in pixels); often defaults to 3, but may be affected by **xTickSize** and **xTickRotate**.
#' @param tick_rotate `<number | param()>` The rotation angle of axis tick labels in degrees clocksize; defaults to 0.
#' @param tick_size `<number | param()>` The length of axis tick marks in pixels; negative values extend in the opposite direction.
#' @param tick_spacing `<number | param()>` The desired approximate spacing between adjacent axis ticks, affecting the default **ticks**; defaults to 80 pixels for *x* and *fx*, and 35 pixels for *y* and *fy*.
#' @param ticks `<number | "day"/"week"/"month"/... | vector | param()>` The desired approximate number of axis ticks, or an explicit array of tick values, or an interval such as *day* or *month*.
#' @param tip `<boolean | "x" | "y" | "xy" | list | param()>` Whether to generate a tooltip for this mark, and any tip options.
#' @param title `<column | literal | transform() | sql()/agg()>` The title; a channel specifying accessible, short textual descriptions as strings (possibly with newlines).
#' @param x `<column | literal | transform() | sql()/agg() | list(value=, ...)>` The horizontal position (or length) channel, typically bound to the *x* scale.
#' @param y `<column | literal | transform() | sql()/agg() | list(value=, ...)>` The vertical position channel, typically bound to the *y* scale; defaults to the zero-based index of the data 0, 1, 2, ….
#' @param z `<column | literal | transform() | sql()/agg()>` An optional ordinal channel for grouping data into (possibly stacked) series to be drawn as separate areas; defaults to **fill** if a channel, or **stroke** if a channel.
#' @param ... Additional options or plot-level attributes.
#' @param data_from The name of the data source this mark reads from (see [vg_data()]); a length-1 nonzero R integer (`1L`, `-1L`, ...; note the `L`) giving a 1-based index into the spec's registered data sources instead, negative counting from the end (e.g., `-1L` for the most recently registered one); or a literal vector of values to use as inline data directly (mosaic-spec's `"data": [...]` shorthand, e.g., for a single reference line -- a bare double like `0`/`c(0)`, or `0L` itself (never a valid index), means this, not an index). Left unset, defaults to the first registered data source (equivalent to `data_from = 1L`) for any mark type that takes data at all.
#' @param filter_by A Param/Selection (e.g., from [param()]) to filter this mark's data by.
#' @param data_optimize A flag (default `TRUE`) to enable mark-specific query optimizations for this mark's data; set `FALSE` to disable them (mosaic-spec's `data: {optimize: false}`).
#' @family mark functions
#' @export
vg_mark_axis_y <- function(spec = NULL, formula = vg_unset, anchor = vg_unset, aria_description = vg_unset, aria_hidden = vg_unset, aria_label = vg_unset, channels = vg_unset, clip = vg_unset, color = vg_unset, dx = vg_unset, dy = vg_unset, facet = vg_unset, facet_anchor = vg_unset, fill = vg_unset, fill_opacity = vg_unset, filter = vg_unset, font_family = vg_unset, font_size = vg_unset, font_style = vg_unset, font_variant = vg_unset, font_weight = vg_unset, frame_anchor = vg_unset, fx = vg_unset, fy = vg_unset, href = vg_unset, image_filter = vg_unset, inset = vg_unset, inset_left = vg_unset, inset_right = vg_unset, interval = vg_unset, label = vg_unset, label_anchor = vg_unset, label_arrow = vg_unset, label_offset = vg_unset, line_anchor = vg_unset, line_height = vg_unset, line_width = vg_unset, margin = vg_unset, margin_bottom = vg_unset, margin_left = vg_unset, margin_right = vg_unset, margin_top = vg_unset, marker = vg_unset, marker_end = vg_unset, marker_mid = vg_unset, marker_start = vg_unset, mix_blend_mode = vg_unset, monospace = vg_unset, opacity = vg_unset, paint_order = vg_unset, pointer_events = vg_unset, reverse = vg_unset, rotate = vg_unset, select = vg_unset, shape_rendering = vg_unset, sort = vg_unset, stroke = vg_unset, stroke_dasharray = vg_unset, stroke_dashoffset = vg_unset, stroke_linecap = vg_unset, stroke_linejoin = vg_unset, stroke_miterlimit = vg_unset, stroke_opacity = vg_unset, stroke_width = vg_unset, target = vg_unset, text = vg_unset, text_anchor = vg_unset, text_overflow = vg_unset, text_stroke = vg_unset, text_stroke_opacity = vg_unset, text_stroke_width = vg_unset, tick_format = vg_unset, tick_padding = vg_unset, tick_rotate = vg_unset, tick_size = vg_unset, tick_spacing = vg_unset, ticks = vg_unset, tip = vg_unset, title = vg_unset, x = vg_unset, y = vg_unset, z = vg_unset, ..., data_from = vg_unset, filter_by = vg_unset, data_optimize = vg_unset) {
  vg_mark_(spec, "axisY", formula = formula, anchor = anchor, ariaDescription = aria_description, ariaHidden = aria_hidden, ariaLabel = aria_label, channels = channels, clip = clip, color = color, dx = dx, dy = dy, facet = facet, facetAnchor = facet_anchor, fill = fill, fillOpacity = fill_opacity, filter = filter, fontFamily = font_family, fontSize = font_size, fontStyle = font_style, fontVariant = font_variant, fontWeight = font_weight, frameAnchor = frame_anchor, fx = fx, fy = fy, href = href, imageFilter = image_filter, inset = inset, insetLeft = inset_left, insetRight = inset_right, interval = interval, label = label, labelAnchor = label_anchor, labelArrow = label_arrow, labelOffset = label_offset, lineAnchor = line_anchor, lineHeight = line_height, lineWidth = line_width, margin = margin, marginBottom = margin_bottom, marginLeft = margin_left, marginRight = margin_right, marginTop = margin_top, marker = marker, markerEnd = marker_end, markerMid = marker_mid, markerStart = marker_start, mixBlendMode = mix_blend_mode, monospace = monospace, opacity = opacity, paintOrder = paint_order, pointerEvents = pointer_events, reverse = reverse, rotate = rotate, select = select, shapeRendering = shape_rendering, sort = sort, stroke = stroke, strokeDasharray = stroke_dasharray, strokeDashoffset = stroke_dashoffset, strokeLinecap = stroke_linecap, strokeLinejoin = stroke_linejoin, strokeMiterlimit = stroke_miterlimit, strokeOpacity = stroke_opacity, strokeWidth = stroke_width, target = target, text = text, textAnchor = text_anchor, textOverflow = text_overflow, textStroke = text_stroke, textStrokeOpacity = text_stroke_opacity, textStrokeWidth = text_stroke_width, tickFormat = tick_format, tickPadding = tick_padding, tickRotate = tick_rotate, tickSize = tick_size, tickSpacing = tick_spacing, ticks = ticks, tip = tip, title = title, x = x, y = y, z = z, ..., data_from = data_from, filter_by = filter_by, data_optimize = data_optimize)
}

#' The barX mark.
#'
#' See [vg_value_types] for what the `<...>` notation below (`column`, `param()`, ...) means.
#'
#' @param spec A plot fragment or `vgspec` to add this mark to, or `NULL` to start a new plot with just this mark.
#' @param formula A shorthand for this mark's position channels (`x`, `y`, `fx`, `fy`, and paired `x1`/`x2` or `y1`/`y2`), e.g., `Sepal.Length ~ Sepal.Width | ~ Species` for `y = ~Sepal.Length, x = ~Sepal.Width, fx = ~Species`. See [vg_mark()] for the full grammar.
#' @param aria_description `<string | param()>` The aria-description; a constant textual description.
#' @param aria_hidden `<string | param()>` The aria-hidden state; a constant indicating whether the element is exposed to an accessibility API.
#' @param aria_label `<column | literal | transform() | sql()/agg()>` The aria-label; a channel specifying short textual labels representing the value in the accessibility tree.
#' @param channels `<list>` Additional named channels, for example to include in a tooltip.
#' @param clip `<"frame" | "sphere" | boolean | NULL | param()>` How to clip the mark; one of: - *frame* or true - clip to the plot’s frame (inner area) - *sphere* - clip to the projected sphere (*e.g.*, front hemisphere) - null or false - do not clip The *sphere* clip option requires a geographic projection.
#' @param dx `<number | param()>` The horizontal offset in pixels; a constant option.
#' @param dy `<number | param()>` The vertical offset in pixels; a constant option.
#' @param facet `<"auto" | "include" | "exclude" | "super" | boolean | NULL | param()>` Whether to enable or disable faceting; one of: - *auto* (default) - automatically determine if this mark should be faceted - *include* (or true) - draw the subset of the mark’s data in the current facet - *exclude* - draw the subset of the mark’s data *not* in the current facet - *super* - draw this mark in a single frame that covers all facets - null (or false) - repeat this mark’s data across all facets (*i.e.*, no faceting) When a mark uses *super* faceting, it is not allowed to use position scales (*x*, *y*, *fx*, or *fy*); *super* faceting is intended for decorations, such as labels and legends.
#' @param facet_anchor `<"top" | "right" | "bottom" | "left" | "top-left" | "top-right" | "bottom-left" | "bottom-right" | "top-empty" | "right-empty" | "bottom-empty" | "left-empty" | "empty" | NULL | param()>` How to place the mark with respect to facets; one of: - null (default for most marks) - display the mark in each non-empty facet - *top*, *right*, *bottom*, or *left* - display the mark only in facets on the given side - *top-empty*, *right-empty*, *bottom-empty*, or *left-empty* (default for axis marks) - display the mark only in facets that have empty space on the given side: either the margin, or an empty facet - *empty* - display the mark in empty facets only
#' @param fill `<column | literal | transform() | sql()/agg() | list(value=, ...) | param()>` The fill; a constant CSS color string, or a channel typically bound to the *color* scale.
#' @param fill_opacity `<column | literal | transform() | sql()/agg() | list(value=, ...) | param()>` The fill-opacity; a constant number between 0 and 1, or a channel typically bound to the *opacity* scale.
#' @param filter `<column | literal | transform() | sql()/agg()>` Applies a transform to filter the mark’s index according to the given channel values; only truthy values are retained.
#' @param fx `<column | literal | transform() | sql()/agg()>` The horizontal facet position channel, for mark-level faceting, bound to the *fx* scale.
#' @param fy `<column | literal | transform() | sql()/agg()>` The vertical facet position channel, for mark-level faceting, bound to the *fy* scale.
#' @param href `<column | literal | transform() | sql()/agg()>` The href; a channel specifying URLs for clickable links.
#' @param image_filter `<string | param()>` A CSS filter; a constant string used to adjust the rendering of images, such as *blur(5px)*.
#' @param inset `<number | param()>` Shorthand to set the same default for **insetStart** and **insetEnd**.
#' @param inset_bottom `<number | param()>` Insets the bottom edge by the specified number of pixels.
#' @param inset_left `<number | param()>` Insets the left edge by the specified number of pixels.
#' @param inset_right `<number | param()>` Insets the right edge by the specified number of pixels.
#' @param inset_top `<number | param()>` Insets the top edge by the specified number of pixels.
#' @param interval `<"day"/"week"/"month"/... | number | param()>` Enforces uniformity for data at regular intervals, such as integer values or daily samples.
#' @param margin `<number | param()>` Shorthand to set the same default for all four mark margins: **marginTop**, **marginRight**, **marginBottom**, and **marginLeft**; typically defaults to 0, except for axis marks.
#' @param margin_bottom `<number | param()>` The mark’s bottom margin; the minimum distance in pixels between the bottom edges of the inner and outer plot area.
#' @param margin_left `<number | param()>` The mark’s left margin; the minimum distance in pixels between the left edges of the inner and outer plot area.
#' @param margin_right `<number | param()>` The mark’s right margin; the minimum distance in pixels between the right edges of the mark’s inner and outer plot area.
#' @param margin_top `<number | param()>` The mark’s top margin; the minimum distance in pixels between the top edges of the inner and outer plot area.
#' @param mix_blend_mode `<string | param()>` The mix-blend-mode; a constant string specifying how to blend content such as *multiply*.
#' @param offset `<"center" | "normalize" | "wiggle" | NULL | param()>` After stacking, an optional **offset** can be applied to translate and scale stacks, say to produce a streamgraph; defaults to null for a zero baseline (**y** = 0 for stackY, and **x** = 0 for stackX).
#' @param opacity `<column | literal | transform() | sql()/agg() | list(value=, ...)>` The opacity; a constant between 0 and 1, or a channel typically bound to the *opacity* scale.
#' @param order `<"value" | "x" | "y" | "z" | "sum" | "appearance" | "inside-out" | "-value" | "-x" | "-y" | ... | NULL | param()>` The order in which stacks are layered; one of: - null (default) for input order - a named stack order method such as *inside-out* or *sum* - a field name, for natural order of the corresponding values - a function of data, for natural order of the corresponding values - an array of explicit **z** values in the desired order If the *wiggle* **offset** is used, as for a streamgraph, the default changes to *inside-out*.
#' @param paint_order `<string | param()>` The paint-order; a constant string specifying the order in which the **fill**, **stroke**, and any markers are drawn; defaults to *normal*, which draws the fill, then stroke, then markers; defaults to *stroke* for the text mark to create a “halo” around text to improve legibility.
#' @param pointer_events `<string | param()>` The pointer-events property; a constant string such as *none*.
#' @param reverse `<boolean | param()>` Applies a transform to reverse the order of the mark’s index, say for reverse input order.
#' @param rx `<number | string | param()>` The rounded corner *x*-radius, either in pixels or as a percentage of the rect width.
#' @param ry `<number | string | param()>` The rounded corner *y*-radius, either in pixels or as a percentage of the rect height.
#' @param select `<"first" | "last" | "maxX" | "maxY" | "minX" | "minY" | "nearest" | "nearestX" | "nearestY">` Applies a filter transform after data is loaded to highlight selected values only.
#' @param shape_rendering `<string | param()>` The shape-rendering; a constant string such as *crispEdges*.
#' @param sort `<column | literal | transform() | sql()/agg() | list>` Either applies a transform to sort the mark’s index by the specified channel values, or imputes ordinal scale domains from this mark’s channels.
#' @param stroke `<column | literal | transform() | sql()/agg() | list(value=, ...) | param()>` The stroke; a constant CSS color string, or a channel typically bound to the *color* scale.
#' @param stroke_dasharray `<string | number | param()>` The stroke-dasharray; a constant number indicating the length in pixels of alternating dashes and gaps, or a constant string of numbers separated by spaces or commas (_e.g._, *10 2* for dashes of 10 pixels separated by gaps of 2 pixels), or *none* (the default) for no dashing : https://developer.mozilla.org/en-US/docs/Web/SVG/Attribute/stroke-dasharray
#' @param stroke_dashoffset `<string | number | param()>` The stroke-dashoffset; a constant indicating the offset in pixels of the first dash along the stroke; defaults to zero.
#' @param stroke_linecap `<string | param()>` The stroke-linecap; a constant specifying how to cap stroked paths, such as *butt*, *round*, or *square*.
#' @param stroke_linejoin `<string | param()>` The stroke-linejoin; a constant specifying how to join stroked paths, such as *bevel*, *miter*, *miter-clip*, or *round*.
#' @param stroke_miterlimit `<number | param()>` The stroke-miterlimit; a constant number specifying how to limit the length of *miter* joins on stroked paths.
#' @param stroke_opacity `<column | literal | transform() | sql()/agg() | list(value=, ...)>` The stroke-opacity; a constant between 0 and 1, or a channel typically bound to the *opacity* scale.
#' @param stroke_width `<column | literal | transform() | sql()/agg() | list(value=, ...)>` The stroke-width; a constant number in pixels, or a channel.
#' @param target `<string | param()>` The target; a constant string specifying the target window (_e.g._, *_blank*) for clickable links; used in conjunction with the **href** option.
#' @param tip `<boolean | "x" | "y" | "xy" | list | param()>` Whether to generate a tooltip for this mark, and any tip options.
#' @param title `<column | literal | transform() | sql()/agg()>` The title; a channel specifying accessible, short textual descriptions as strings (possibly with newlines).
#' @param x `<column | literal | transform() | sql()/agg() | list(value=, ...)>` The horizontal position (or length) channel, typically bound to the *x* scale.
#' @param x1 `<column | literal | transform() | sql()/agg() | list(value=, ...)>` The required primary (starting, often left) horizontal position channel, representing the area’s baseline, typically bound to the *x* scale.
#' @param x2 `<column | literal | transform() | sql()/agg() | list(value=, ...)>` The optional secondary (ending, often right) horizontal position channel, representing the area’s topline, typically bound to the *x* scale; if not specified, **x1** is used.
#' @param y `<column | literal | transform() | sql()/agg() | list(value=, ...)>` The vertical position channel, typically bound to the *y* scale; defaults to the zero-based index of the data 0, 1, 2, ….
#' @param z `<column | literal | transform() | sql()/agg()>` An optional ordinal channel for grouping data into (possibly stacked) series to be drawn as separate areas; defaults to **fill** if a channel, or **stroke** if a channel.
#' @param ... Additional options or plot-level attributes.
#' @param data_from The name of the data source this mark reads from (see [vg_data()]); a length-1 nonzero R integer (`1L`, `-1L`, ...; note the `L`) giving a 1-based index into the spec's registered data sources instead, negative counting from the end (e.g., `-1L` for the most recently registered one); or a literal vector of values to use as inline data directly (mosaic-spec's `"data": [...]` shorthand, e.g., for a single reference line -- a bare double like `0`/`c(0)`, or `0L` itself (never a valid index), means this, not an index). Left unset, defaults to the first registered data source (equivalent to `data_from = 1L`) for any mark type that takes data at all.
#' @param filter_by A Param/Selection (e.g., from [param()]) to filter this mark's data by.
#' @param data_optimize A flag (default `TRUE`) to enable mark-specific query optimizations for this mark's data; set `FALSE` to disable them (mosaic-spec's `data: {optimize: false}`).
#' @family mark functions
#' @export
vg_mark_bar_x <- function(spec = NULL, formula = vg_unset, aria_description = vg_unset, aria_hidden = vg_unset, aria_label = vg_unset, channels = vg_unset, clip = vg_unset, dx = vg_unset, dy = vg_unset, facet = vg_unset, facet_anchor = vg_unset, fill = vg_unset, fill_opacity = vg_unset, filter = vg_unset, fx = vg_unset, fy = vg_unset, href = vg_unset, image_filter = vg_unset, inset = vg_unset, inset_bottom = vg_unset, inset_left = vg_unset, inset_right = vg_unset, inset_top = vg_unset, interval = vg_unset, margin = vg_unset, margin_bottom = vg_unset, margin_left = vg_unset, margin_right = vg_unset, margin_top = vg_unset, mix_blend_mode = vg_unset, offset = vg_unset, opacity = vg_unset, order = vg_unset, paint_order = vg_unset, pointer_events = vg_unset, reverse = vg_unset, rx = vg_unset, ry = vg_unset, select = vg_unset, shape_rendering = vg_unset, sort = vg_unset, stroke = vg_unset, stroke_dasharray = vg_unset, stroke_dashoffset = vg_unset, stroke_linecap = vg_unset, stroke_linejoin = vg_unset, stroke_miterlimit = vg_unset, stroke_opacity = vg_unset, stroke_width = vg_unset, target = vg_unset, tip = vg_unset, title = vg_unset, x = vg_unset, x1 = vg_unset, x2 = vg_unset, y = vg_unset, z = vg_unset, ..., data_from = vg_unset, filter_by = vg_unset, data_optimize = vg_unset) {
  vg_mark_(spec, "barX", formula = formula, ariaDescription = aria_description, ariaHidden = aria_hidden, ariaLabel = aria_label, channels = channels, clip = clip, dx = dx, dy = dy, facet = facet, facetAnchor = facet_anchor, fill = fill, fillOpacity = fill_opacity, filter = filter, fx = fx, fy = fy, href = href, imageFilter = image_filter, inset = inset, insetBottom = inset_bottom, insetLeft = inset_left, insetRight = inset_right, insetTop = inset_top, interval = interval, margin = margin, marginBottom = margin_bottom, marginLeft = margin_left, marginRight = margin_right, marginTop = margin_top, mixBlendMode = mix_blend_mode, offset = offset, opacity = opacity, order = order, paintOrder = paint_order, pointerEvents = pointer_events, reverse = reverse, rx = rx, ry = ry, select = select, shapeRendering = shape_rendering, sort = sort, stroke = stroke, strokeDasharray = stroke_dasharray, strokeDashoffset = stroke_dashoffset, strokeLinecap = stroke_linecap, strokeLinejoin = stroke_linejoin, strokeMiterlimit = stroke_miterlimit, strokeOpacity = stroke_opacity, strokeWidth = stroke_width, target = target, tip = tip, title = title, x = x, x1 = x1, x2 = x2, y = y, z = z, ..., data_from = data_from, filter_by = filter_by, data_optimize = data_optimize)
}

#' The barY mark.
#'
#' See [vg_value_types] for what the `<...>` notation below (`column`, `param()`, ...) means.
#'
#' @param spec A plot fragment or `vgspec` to add this mark to, or `NULL` to start a new plot with just this mark.
#' @param formula A shorthand for this mark's position channels (`x`, `y`, `fx`, `fy`, and paired `x1`/`x2` or `y1`/`y2`), e.g., `Sepal.Length ~ Sepal.Width | ~ Species` for `y = ~Sepal.Length, x = ~Sepal.Width, fx = ~Species`. See [vg_mark()] for the full grammar.
#' @param aria_description `<string | param()>` The aria-description; a constant textual description.
#' @param aria_hidden `<string | param()>` The aria-hidden state; a constant indicating whether the element is exposed to an accessibility API.
#' @param aria_label `<column | literal | transform() | sql()/agg()>` The aria-label; a channel specifying short textual labels representing the value in the accessibility tree.
#' @param channels `<list>` Additional named channels, for example to include in a tooltip.
#' @param clip `<"frame" | "sphere" | boolean | NULL | param()>` How to clip the mark; one of: - *frame* or true - clip to the plot’s frame (inner area) - *sphere* - clip to the projected sphere (*e.g.*, front hemisphere) - null or false - do not clip The *sphere* clip option requires a geographic projection.
#' @param dx `<number | param()>` The horizontal offset in pixels; a constant option.
#' @param dy `<number | param()>` The vertical offset in pixels; a constant option.
#' @param facet `<"auto" | "include" | "exclude" | "super" | boolean | NULL | param()>` Whether to enable or disable faceting; one of: - *auto* (default) - automatically determine if this mark should be faceted - *include* (or true) - draw the subset of the mark’s data in the current facet - *exclude* - draw the subset of the mark’s data *not* in the current facet - *super* - draw this mark in a single frame that covers all facets - null (or false) - repeat this mark’s data across all facets (*i.e.*, no faceting) When a mark uses *super* faceting, it is not allowed to use position scales (*x*, *y*, *fx*, or *fy*); *super* faceting is intended for decorations, such as labels and legends.
#' @param facet_anchor `<"top" | "right" | "bottom" | "left" | "top-left" | "top-right" | "bottom-left" | "bottom-right" | "top-empty" | "right-empty" | "bottom-empty" | "left-empty" | "empty" | NULL | param()>` How to place the mark with respect to facets; one of: - null (default for most marks) - display the mark in each non-empty facet - *top*, *right*, *bottom*, or *left* - display the mark only in facets on the given side - *top-empty*, *right-empty*, *bottom-empty*, or *left-empty* (default for axis marks) - display the mark only in facets that have empty space on the given side: either the margin, or an empty facet - *empty* - display the mark in empty facets only
#' @param fill `<column | literal | transform() | sql()/agg() | list(value=, ...) | param()>` The fill; a constant CSS color string, or a channel typically bound to the *color* scale.
#' @param fill_opacity `<column | literal | transform() | sql()/agg() | list(value=, ...) | param()>` The fill-opacity; a constant number between 0 and 1, or a channel typically bound to the *opacity* scale.
#' @param filter `<column | literal | transform() | sql()/agg()>` Applies a transform to filter the mark’s index according to the given channel values; only truthy values are retained.
#' @param fx `<column | literal | transform() | sql()/agg()>` The horizontal facet position channel, for mark-level faceting, bound to the *fx* scale.
#' @param fy `<column | literal | transform() | sql()/agg()>` The vertical facet position channel, for mark-level faceting, bound to the *fy* scale.
#' @param href `<column | literal | transform() | sql()/agg()>` The href; a channel specifying URLs for clickable links.
#' @param image_filter `<string | param()>` A CSS filter; a constant string used to adjust the rendering of images, such as *blur(5px)*.
#' @param inset `<number | param()>` Shorthand to set the same default for **insetStart** and **insetEnd**.
#' @param inset_bottom `<number | param()>` Insets the bottom edge by the specified number of pixels.
#' @param inset_left `<number | param()>` Insets the left edge by the specified number of pixels.
#' @param inset_right `<number | param()>` Insets the right edge by the specified number of pixels.
#' @param inset_top `<number | param()>` Insets the top edge by the specified number of pixels.
#' @param interval `<"day"/"week"/"month"/... | number | param()>` Enforces uniformity for data at regular intervals, such as integer values or daily samples.
#' @param margin `<number | param()>` Shorthand to set the same default for all four mark margins: **marginTop**, **marginRight**, **marginBottom**, and **marginLeft**; typically defaults to 0, except for axis marks.
#' @param margin_bottom `<number | param()>` The mark’s bottom margin; the minimum distance in pixels between the bottom edges of the inner and outer plot area.
#' @param margin_left `<number | param()>` The mark’s left margin; the minimum distance in pixels between the left edges of the inner and outer plot area.
#' @param margin_right `<number | param()>` The mark’s right margin; the minimum distance in pixels between the right edges of the mark’s inner and outer plot area.
#' @param margin_top `<number | param()>` The mark’s top margin; the minimum distance in pixels between the top edges of the inner and outer plot area.
#' @param mix_blend_mode `<string | param()>` The mix-blend-mode; a constant string specifying how to blend content such as *multiply*.
#' @param offset `<"center" | "normalize" | "wiggle" | NULL | param()>` After stacking, an optional **offset** can be applied to translate and scale stacks, say to produce a streamgraph; defaults to null for a zero baseline (**y** = 0 for stackY, and **x** = 0 for stackX).
#' @param opacity `<column | literal | transform() | sql()/agg() | list(value=, ...)>` The opacity; a constant between 0 and 1, or a channel typically bound to the *opacity* scale.
#' @param order `<"value" | "x" | "y" | "z" | "sum" | "appearance" | "inside-out" | "-value" | "-x" | "-y" | ... | NULL | param()>` The order in which stacks are layered; one of: - null (default) for input order - a named stack order method such as *inside-out* or *sum* - a field name, for natural order of the corresponding values - a function of data, for natural order of the corresponding values - an array of explicit **z** values in the desired order If the *wiggle* **offset** is used, as for a streamgraph, the default changes to *inside-out*.
#' @param paint_order `<string | param()>` The paint-order; a constant string specifying the order in which the **fill**, **stroke**, and any markers are drawn; defaults to *normal*, which draws the fill, then stroke, then markers; defaults to *stroke* for the text mark to create a “halo” around text to improve legibility.
#' @param pointer_events `<string | param()>` The pointer-events property; a constant string such as *none*.
#' @param reverse `<boolean | param()>` Applies a transform to reverse the order of the mark’s index, say for reverse input order.
#' @param rx `<number | string | param()>` The rounded corner *x*-radius, either in pixels or as a percentage of the rect width.
#' @param ry `<number | string | param()>` The rounded corner *y*-radius, either in pixels or as a percentage of the rect height.
#' @param select `<"first" | "last" | "maxX" | "maxY" | "minX" | "minY" | "nearest" | "nearestX" | "nearestY">` Applies a filter transform after data is loaded to highlight selected values only.
#' @param shape_rendering `<string | param()>` The shape-rendering; a constant string such as *crispEdges*.
#' @param sort `<column | literal | transform() | sql()/agg() | list>` Either applies a transform to sort the mark’s index by the specified channel values, or imputes ordinal scale domains from this mark’s channels.
#' @param stroke `<column | literal | transform() | sql()/agg() | list(value=, ...) | param()>` The stroke; a constant CSS color string, or a channel typically bound to the *color* scale.
#' @param stroke_dasharray `<string | number | param()>` The stroke-dasharray; a constant number indicating the length in pixels of alternating dashes and gaps, or a constant string of numbers separated by spaces or commas (_e.g._, *10 2* for dashes of 10 pixels separated by gaps of 2 pixels), or *none* (the default) for no dashing : https://developer.mozilla.org/en-US/docs/Web/SVG/Attribute/stroke-dasharray
#' @param stroke_dashoffset `<string | number | param()>` The stroke-dashoffset; a constant indicating the offset in pixels of the first dash along the stroke; defaults to zero.
#' @param stroke_linecap `<string | param()>` The stroke-linecap; a constant specifying how to cap stroked paths, such as *butt*, *round*, or *square*.
#' @param stroke_linejoin `<string | param()>` The stroke-linejoin; a constant specifying how to join stroked paths, such as *bevel*, *miter*, *miter-clip*, or *round*.
#' @param stroke_miterlimit `<number | param()>` The stroke-miterlimit; a constant number specifying how to limit the length of *miter* joins on stroked paths.
#' @param stroke_opacity `<column | literal | transform() | sql()/agg() | list(value=, ...)>` The stroke-opacity; a constant between 0 and 1, or a channel typically bound to the *opacity* scale.
#' @param stroke_width `<column | literal | transform() | sql()/agg() | list(value=, ...)>` The stroke-width; a constant number in pixels, or a channel.
#' @param target `<string | param()>` The target; a constant string specifying the target window (_e.g._, *_blank*) for clickable links; used in conjunction with the **href** option.
#' @param tip `<boolean | "x" | "y" | "xy" | list | param()>` Whether to generate a tooltip for this mark, and any tip options.
#' @param title `<column | literal | transform() | sql()/agg()>` The title; a channel specifying accessible, short textual descriptions as strings (possibly with newlines).
#' @param x `<column | literal | transform() | sql()/agg() | list(value=, ...)>` The horizontal position (or length) channel, typically bound to the *x* scale.
#' @param y `<column | literal | transform() | sql()/agg() | list(value=, ...)>` The vertical position channel, typically bound to the *y* scale; defaults to the zero-based index of the data 0, 1, 2, ….
#' @param y1 `<column | literal | transform() | sql()/agg() | list(value=, ...)>` The required primary (starting, often bottom) vertical position channel, representing the area’s baseline, typically bound to the *y* scale.
#' @param y2 `<column | literal | transform() | sql()/agg() | list(value=, ...)>` The optional secondary (ending, often top) vertical position channel, representing the area’s topline, typically bound to the *y* scale; if not specified, **y1** is used.
#' @param z `<column | literal | transform() | sql()/agg()>` An optional ordinal channel for grouping data into (possibly stacked) series to be drawn as separate areas; defaults to **fill** if a channel, or **stroke** if a channel.
#' @param ... Additional options or plot-level attributes.
#' @param data_from The name of the data source this mark reads from (see [vg_data()]); a length-1 nonzero R integer (`1L`, `-1L`, ...; note the `L`) giving a 1-based index into the spec's registered data sources instead, negative counting from the end (e.g., `-1L` for the most recently registered one); or a literal vector of values to use as inline data directly (mosaic-spec's `"data": [...]` shorthand, e.g., for a single reference line -- a bare double like `0`/`c(0)`, or `0L` itself (never a valid index), means this, not an index). Left unset, defaults to the first registered data source (equivalent to `data_from = 1L`) for any mark type that takes data at all.
#' @param filter_by A Param/Selection (e.g., from [param()]) to filter this mark's data by.
#' @param data_optimize A flag (default `TRUE`) to enable mark-specific query optimizations for this mark's data; set `FALSE` to disable them (mosaic-spec's `data: {optimize: false}`).
#' @family mark functions
#' @export
vg_mark_bar_y <- function(spec = NULL, formula = vg_unset, aria_description = vg_unset, aria_hidden = vg_unset, aria_label = vg_unset, channels = vg_unset, clip = vg_unset, dx = vg_unset, dy = vg_unset, facet = vg_unset, facet_anchor = vg_unset, fill = vg_unset, fill_opacity = vg_unset, filter = vg_unset, fx = vg_unset, fy = vg_unset, href = vg_unset, image_filter = vg_unset, inset = vg_unset, inset_bottom = vg_unset, inset_left = vg_unset, inset_right = vg_unset, inset_top = vg_unset, interval = vg_unset, margin = vg_unset, margin_bottom = vg_unset, margin_left = vg_unset, margin_right = vg_unset, margin_top = vg_unset, mix_blend_mode = vg_unset, offset = vg_unset, opacity = vg_unset, order = vg_unset, paint_order = vg_unset, pointer_events = vg_unset, reverse = vg_unset, rx = vg_unset, ry = vg_unset, select = vg_unset, shape_rendering = vg_unset, sort = vg_unset, stroke = vg_unset, stroke_dasharray = vg_unset, stroke_dashoffset = vg_unset, stroke_linecap = vg_unset, stroke_linejoin = vg_unset, stroke_miterlimit = vg_unset, stroke_opacity = vg_unset, stroke_width = vg_unset, target = vg_unset, tip = vg_unset, title = vg_unset, x = vg_unset, y = vg_unset, y1 = vg_unset, y2 = vg_unset, z = vg_unset, ..., data_from = vg_unset, filter_by = vg_unset, data_optimize = vg_unset) {
  vg_mark_(spec, "barY", formula = formula, ariaDescription = aria_description, ariaHidden = aria_hidden, ariaLabel = aria_label, channels = channels, clip = clip, dx = dx, dy = dy, facet = facet, facetAnchor = facet_anchor, fill = fill, fillOpacity = fill_opacity, filter = filter, fx = fx, fy = fy, href = href, imageFilter = image_filter, inset = inset, insetBottom = inset_bottom, insetLeft = inset_left, insetRight = inset_right, insetTop = inset_top, interval = interval, margin = margin, marginBottom = margin_bottom, marginLeft = margin_left, marginRight = margin_right, marginTop = margin_top, mixBlendMode = mix_blend_mode, offset = offset, opacity = opacity, order = order, paintOrder = paint_order, pointerEvents = pointer_events, reverse = reverse, rx = rx, ry = ry, select = select, shapeRendering = shape_rendering, sort = sort, stroke = stroke, strokeDasharray = stroke_dasharray, strokeDashoffset = stroke_dashoffset, strokeLinecap = stroke_linecap, strokeLinejoin = stroke_linejoin, strokeMiterlimit = stroke_miterlimit, strokeOpacity = stroke_opacity, strokeWidth = stroke_width, target = target, tip = tip, title = title, x = x, y = y, y1 = y1, y2 = y2, z = z, ..., data_from = data_from, filter_by = filter_by, data_optimize = data_optimize)
}

#' The cell mark.
#'
#' See [vg_value_types] for what the `<...>` notation below (`column`, `param()`, ...) means.
#'
#' @param spec A plot fragment or `vgspec` to add this mark to, or `NULL` to start a new plot with just this mark.
#' @param formula A shorthand for this mark's position channels (`x`, `y`, `fx`, `fy`, and paired `x1`/`x2` or `y1`/`y2`), e.g., `Sepal.Length ~ Sepal.Width | ~ Species` for `y = ~Sepal.Length, x = ~Sepal.Width, fx = ~Species`. See [vg_mark()] for the full grammar.
#' @param aria_description `<string | param()>` The aria-description; a constant textual description.
#' @param aria_hidden `<string | param()>` The aria-hidden state; a constant indicating whether the element is exposed to an accessibility API.
#' @param aria_label `<column | literal | transform() | sql()/agg()>` The aria-label; a channel specifying short textual labels representing the value in the accessibility tree.
#' @param channels `<list>` Additional named channels, for example to include in a tooltip.
#' @param clip `<"frame" | "sphere" | boolean | NULL | param()>` How to clip the mark; one of: - *frame* or true - clip to the plot’s frame (inner area) - *sphere* - clip to the projected sphere (*e.g.*, front hemisphere) - null or false - do not clip The *sphere* clip option requires a geographic projection.
#' @param dx `<number | param()>` The horizontal offset in pixels; a constant option.
#' @param dy `<number | param()>` The vertical offset in pixels; a constant option.
#' @param facet `<"auto" | "include" | "exclude" | "super" | boolean | NULL | param()>` Whether to enable or disable faceting; one of: - *auto* (default) - automatically determine if this mark should be faceted - *include* (or true) - draw the subset of the mark’s data in the current facet - *exclude* - draw the subset of the mark’s data *not* in the current facet - *super* - draw this mark in a single frame that covers all facets - null (or false) - repeat this mark’s data across all facets (*i.e.*, no faceting) When a mark uses *super* faceting, it is not allowed to use position scales (*x*, *y*, *fx*, or *fy*); *super* faceting is intended for decorations, such as labels and legends.
#' @param facet_anchor `<"top" | "right" | "bottom" | "left" | "top-left" | "top-right" | "bottom-left" | "bottom-right" | "top-empty" | "right-empty" | "bottom-empty" | "left-empty" | "empty" | NULL | param()>` How to place the mark with respect to facets; one of: - null (default for most marks) - display the mark in each non-empty facet - *top*, *right*, *bottom*, or *left* - display the mark only in facets on the given side - *top-empty*, *right-empty*, *bottom-empty*, or *left-empty* (default for axis marks) - display the mark only in facets that have empty space on the given side: either the margin, or an empty facet - *empty* - display the mark in empty facets only
#' @param fill `<column | literal | transform() | sql()/agg() | list(value=, ...) | param()>` The fill; a constant CSS color string, or a channel typically bound to the *color* scale.
#' @param fill_opacity `<column | literal | transform() | sql()/agg() | list(value=, ...) | param()>` The fill-opacity; a constant number between 0 and 1, or a channel typically bound to the *opacity* scale.
#' @param filter `<column | literal | transform() | sql()/agg()>` Applies a transform to filter the mark’s index according to the given channel values; only truthy values are retained.
#' @param fx `<column | literal | transform() | sql()/agg()>` The horizontal facet position channel, for mark-level faceting, bound to the *fx* scale.
#' @param fy `<column | literal | transform() | sql()/agg()>` The vertical facet position channel, for mark-level faceting, bound to the *fy* scale.
#' @param href `<column | literal | transform() | sql()/agg()>` The href; a channel specifying URLs for clickable links.
#' @param image_filter `<string | param()>` A CSS filter; a constant string used to adjust the rendering of images, such as *blur(5px)*.
#' @param inset `<number | param()>` Shorthand to set the same default for **insetStart** and **insetEnd**.
#' @param inset_bottom `<number | param()>` Insets the bottom edge by the specified number of pixels.
#' @param inset_left `<number | param()>` Insets the left edge by the specified number of pixels.
#' @param inset_right `<number | param()>` Insets the right edge by the specified number of pixels.
#' @param inset_top `<number | param()>` Insets the top edge by the specified number of pixels.
#' @param margin `<number | param()>` Shorthand to set the same default for all four mark margins: **marginTop**, **marginRight**, **marginBottom**, and **marginLeft**; typically defaults to 0, except for axis marks.
#' @param margin_bottom `<number | param()>` The mark’s bottom margin; the minimum distance in pixels between the bottom edges of the inner and outer plot area.
#' @param margin_left `<number | param()>` The mark’s left margin; the minimum distance in pixels between the left edges of the inner and outer plot area.
#' @param margin_right `<number | param()>` The mark’s right margin; the minimum distance in pixels between the right edges of the mark’s inner and outer plot area.
#' @param margin_top `<number | param()>` The mark’s top margin; the minimum distance in pixels between the top edges of the inner and outer plot area.
#' @param mix_blend_mode `<string | param()>` The mix-blend-mode; a constant string specifying how to blend content such as *multiply*.
#' @param opacity `<column | literal | transform() | sql()/agg() | list(value=, ...)>` The opacity; a constant between 0 and 1, or a channel typically bound to the *opacity* scale.
#' @param paint_order `<string | param()>` The paint-order; a constant string specifying the order in which the **fill**, **stroke**, and any markers are drawn; defaults to *normal*, which draws the fill, then stroke, then markers; defaults to *stroke* for the text mark to create a “halo” around text to improve legibility.
#' @param pointer_events `<string | param()>` The pointer-events property; a constant string such as *none*.
#' @param reverse `<boolean | param()>` Applies a transform to reverse the order of the mark’s index, say for reverse input order.
#' @param rx `<number | string | param()>` The rounded corner *x*-radius, either in pixels or as a percentage of the rect width.
#' @param ry `<number | string | param()>` The rounded corner *y*-radius, either in pixels or as a percentage of the rect height.
#' @param select `<"first" | "last" | "maxX" | "maxY" | "minX" | "minY" | "nearest" | "nearestX" | "nearestY">` Applies a filter transform after data is loaded to highlight selected values only.
#' @param shape_rendering `<string | param()>` The shape-rendering; a constant string such as *crispEdges*.
#' @param sort `<column | literal | transform() | sql()/agg() | list>` Either applies a transform to sort the mark’s index by the specified channel values, or imputes ordinal scale domains from this mark’s channels.
#' @param stroke `<column | literal | transform() | sql()/agg() | list(value=, ...) | param()>` The stroke; a constant CSS color string, or a channel typically bound to the *color* scale.
#' @param stroke_dasharray `<string | number | param()>` The stroke-dasharray; a constant number indicating the length in pixels of alternating dashes and gaps, or a constant string of numbers separated by spaces or commas (_e.g._, *10 2* for dashes of 10 pixels separated by gaps of 2 pixels), or *none* (the default) for no dashing : https://developer.mozilla.org/en-US/docs/Web/SVG/Attribute/stroke-dasharray
#' @param stroke_dashoffset `<string | number | param()>` The stroke-dashoffset; a constant indicating the offset in pixels of the first dash along the stroke; defaults to zero.
#' @param stroke_linecap `<string | param()>` The stroke-linecap; a constant specifying how to cap stroked paths, such as *butt*, *round*, or *square*.
#' @param stroke_linejoin `<string | param()>` The stroke-linejoin; a constant specifying how to join stroked paths, such as *bevel*, *miter*, *miter-clip*, or *round*.
#' @param stroke_miterlimit `<number | param()>` The stroke-miterlimit; a constant number specifying how to limit the length of *miter* joins on stroked paths.
#' @param stroke_opacity `<column | literal | transform() | sql()/agg() | list(value=, ...)>` The stroke-opacity; a constant between 0 and 1, or a channel typically bound to the *opacity* scale.
#' @param stroke_width `<column | literal | transform() | sql()/agg() | list(value=, ...)>` The stroke-width; a constant number in pixels, or a channel.
#' @param target `<string | param()>` The target; a constant string specifying the target window (_e.g._, *_blank*) for clickable links; used in conjunction with the **href** option.
#' @param tip `<boolean | "x" | "y" | "xy" | list | param()>` Whether to generate a tooltip for this mark, and any tip options.
#' @param title `<column | literal | transform() | sql()/agg()>` The title; a channel specifying accessible, short textual descriptions as strings (possibly with newlines).
#' @param x `<column | literal | transform() | sql()/agg() | list(value=, ...)>` The horizontal position (or length) channel, typically bound to the *x* scale.
#' @param y `<column | literal | transform() | sql()/agg() | list(value=, ...)>` The vertical position channel, typically bound to the *y* scale; defaults to the zero-based index of the data 0, 1, 2, ….
#' @param ... Additional options or plot-level attributes.
#' @param data_from The name of the data source this mark reads from (see [vg_data()]); a length-1 nonzero R integer (`1L`, `-1L`, ...; note the `L`) giving a 1-based index into the spec's registered data sources instead, negative counting from the end (e.g., `-1L` for the most recently registered one); or a literal vector of values to use as inline data directly (mosaic-spec's `"data": [...]` shorthand, e.g., for a single reference line -- a bare double like `0`/`c(0)`, or `0L` itself (never a valid index), means this, not an index). Left unset, defaults to the first registered data source (equivalent to `data_from = 1L`) for any mark type that takes data at all.
#' @param filter_by A Param/Selection (e.g., from [param()]) to filter this mark's data by.
#' @param data_optimize A flag (default `TRUE`) to enable mark-specific query optimizations for this mark's data; set `FALSE` to disable them (mosaic-spec's `data: {optimize: false}`).
#' @family mark functions
#' @export
vg_mark_cell <- function(spec = NULL, formula = vg_unset, aria_description = vg_unset, aria_hidden = vg_unset, aria_label = vg_unset, channels = vg_unset, clip = vg_unset, dx = vg_unset, dy = vg_unset, facet = vg_unset, facet_anchor = vg_unset, fill = vg_unset, fill_opacity = vg_unset, filter = vg_unset, fx = vg_unset, fy = vg_unset, href = vg_unset, image_filter = vg_unset, inset = vg_unset, inset_bottom = vg_unset, inset_left = vg_unset, inset_right = vg_unset, inset_top = vg_unset, margin = vg_unset, margin_bottom = vg_unset, margin_left = vg_unset, margin_right = vg_unset, margin_top = vg_unset, mix_blend_mode = vg_unset, opacity = vg_unset, paint_order = vg_unset, pointer_events = vg_unset, reverse = vg_unset, rx = vg_unset, ry = vg_unset, select = vg_unset, shape_rendering = vg_unset, sort = vg_unset, stroke = vg_unset, stroke_dasharray = vg_unset, stroke_dashoffset = vg_unset, stroke_linecap = vg_unset, stroke_linejoin = vg_unset, stroke_miterlimit = vg_unset, stroke_opacity = vg_unset, stroke_width = vg_unset, target = vg_unset, tip = vg_unset, title = vg_unset, x = vg_unset, y = vg_unset, ..., data_from = vg_unset, filter_by = vg_unset, data_optimize = vg_unset) {
  vg_mark_(spec, "cell", formula = formula, ariaDescription = aria_description, ariaHidden = aria_hidden, ariaLabel = aria_label, channels = channels, clip = clip, dx = dx, dy = dy, facet = facet, facetAnchor = facet_anchor, fill = fill, fillOpacity = fill_opacity, filter = filter, fx = fx, fy = fy, href = href, imageFilter = image_filter, inset = inset, insetBottom = inset_bottom, insetLeft = inset_left, insetRight = inset_right, insetTop = inset_top, margin = margin, marginBottom = margin_bottom, marginLeft = margin_left, marginRight = margin_right, marginTop = margin_top, mixBlendMode = mix_blend_mode, opacity = opacity, paintOrder = paint_order, pointerEvents = pointer_events, reverse = reverse, rx = rx, ry = ry, select = select, shapeRendering = shape_rendering, sort = sort, stroke = stroke, strokeDasharray = stroke_dasharray, strokeDashoffset = stroke_dashoffset, strokeLinecap = stroke_linecap, strokeLinejoin = stroke_linejoin, strokeMiterlimit = stroke_miterlimit, strokeOpacity = stroke_opacity, strokeWidth = stroke_width, target = target, tip = tip, title = title, x = x, y = y, ..., data_from = data_from, filter_by = filter_by, data_optimize = data_optimize)
}

#' The cellX mark.
#'
#' See [vg_value_types] for what the `<...>` notation below (`column`, `param()`, ...) means.
#'
#' @param spec A plot fragment or `vgspec` to add this mark to, or `NULL` to start a new plot with just this mark.
#' @param formula A shorthand for this mark's position channels (`x`, `y`, `fx`, `fy`, and paired `x1`/`x2` or `y1`/`y2`), e.g., `Sepal.Length ~ Sepal.Width | ~ Species` for `y = ~Sepal.Length, x = ~Sepal.Width, fx = ~Species`. See [vg_mark()] for the full grammar.
#' @param aria_description `<string | param()>` The aria-description; a constant textual description.
#' @param aria_hidden `<string | param()>` The aria-hidden state; a constant indicating whether the element is exposed to an accessibility API.
#' @param aria_label `<column | literal | transform() | sql()/agg()>` The aria-label; a channel specifying short textual labels representing the value in the accessibility tree.
#' @param channels `<list>` Additional named channels, for example to include in a tooltip.
#' @param clip `<"frame" | "sphere" | boolean | NULL | param()>` How to clip the mark; one of: - *frame* or true - clip to the plot’s frame (inner area) - *sphere* - clip to the projected sphere (*e.g.*, front hemisphere) - null or false - do not clip The *sphere* clip option requires a geographic projection.
#' @param dx `<number | param()>` The horizontal offset in pixels; a constant option.
#' @param dy `<number | param()>` The vertical offset in pixels; a constant option.
#' @param facet `<"auto" | "include" | "exclude" | "super" | boolean | NULL | param()>` Whether to enable or disable faceting; one of: - *auto* (default) - automatically determine if this mark should be faceted - *include* (or true) - draw the subset of the mark’s data in the current facet - *exclude* - draw the subset of the mark’s data *not* in the current facet - *super* - draw this mark in a single frame that covers all facets - null (or false) - repeat this mark’s data across all facets (*i.e.*, no faceting) When a mark uses *super* faceting, it is not allowed to use position scales (*x*, *y*, *fx*, or *fy*); *super* faceting is intended for decorations, such as labels and legends.
#' @param facet_anchor `<"top" | "right" | "bottom" | "left" | "top-left" | "top-right" | "bottom-left" | "bottom-right" | "top-empty" | "right-empty" | "bottom-empty" | "left-empty" | "empty" | NULL | param()>` How to place the mark with respect to facets; one of: - null (default for most marks) - display the mark in each non-empty facet - *top*, *right*, *bottom*, or *left* - display the mark only in facets on the given side - *top-empty*, *right-empty*, *bottom-empty*, or *left-empty* (default for axis marks) - display the mark only in facets that have empty space on the given side: either the margin, or an empty facet - *empty* - display the mark in empty facets only
#' @param fill `<column | literal | transform() | sql()/agg() | list(value=, ...) | param()>` The fill; a constant CSS color string, or a channel typically bound to the *color* scale.
#' @param fill_opacity `<column | literal | transform() | sql()/agg() | list(value=, ...) | param()>` The fill-opacity; a constant number between 0 and 1, or a channel typically bound to the *opacity* scale.
#' @param filter `<column | literal | transform() | sql()/agg()>` Applies a transform to filter the mark’s index according to the given channel values; only truthy values are retained.
#' @param fx `<column | literal | transform() | sql()/agg()>` The horizontal facet position channel, for mark-level faceting, bound to the *fx* scale.
#' @param fy `<column | literal | transform() | sql()/agg()>` The vertical facet position channel, for mark-level faceting, bound to the *fy* scale.
#' @param href `<column | literal | transform() | sql()/agg()>` The href; a channel specifying URLs for clickable links.
#' @param image_filter `<string | param()>` A CSS filter; a constant string used to adjust the rendering of images, such as *blur(5px)*.
#' @param inset `<number | param()>` Shorthand to set the same default for **insetStart** and **insetEnd**.
#' @param inset_bottom `<number | param()>` Insets the bottom edge by the specified number of pixels.
#' @param inset_left `<number | param()>` Insets the left edge by the specified number of pixels.
#' @param inset_right `<number | param()>` Insets the right edge by the specified number of pixels.
#' @param inset_top `<number | param()>` Insets the top edge by the specified number of pixels.
#' @param margin `<number | param()>` Shorthand to set the same default for all four mark margins: **marginTop**, **marginRight**, **marginBottom**, and **marginLeft**; typically defaults to 0, except for axis marks.
#' @param margin_bottom `<number | param()>` The mark’s bottom margin; the minimum distance in pixels between the bottom edges of the inner and outer plot area.
#' @param margin_left `<number | param()>` The mark’s left margin; the minimum distance in pixels between the left edges of the inner and outer plot area.
#' @param margin_right `<number | param()>` The mark’s right margin; the minimum distance in pixels between the right edges of the mark’s inner and outer plot area.
#' @param margin_top `<number | param()>` The mark’s top margin; the minimum distance in pixels between the top edges of the inner and outer plot area.
#' @param mix_blend_mode `<string | param()>` The mix-blend-mode; a constant string specifying how to blend content such as *multiply*.
#' @param opacity `<column | literal | transform() | sql()/agg() | list(value=, ...)>` The opacity; a constant between 0 and 1, or a channel typically bound to the *opacity* scale.
#' @param paint_order `<string | param()>` The paint-order; a constant string specifying the order in which the **fill**, **stroke**, and any markers are drawn; defaults to *normal*, which draws the fill, then stroke, then markers; defaults to *stroke* for the text mark to create a “halo” around text to improve legibility.
#' @param pointer_events `<string | param()>` The pointer-events property; a constant string such as *none*.
#' @param reverse `<boolean | param()>` Applies a transform to reverse the order of the mark’s index, say for reverse input order.
#' @param rx `<number | string | param()>` The rounded corner *x*-radius, either in pixels or as a percentage of the rect width.
#' @param ry `<number | string | param()>` The rounded corner *y*-radius, either in pixels or as a percentage of the rect height.
#' @param select `<"first" | "last" | "maxX" | "maxY" | "minX" | "minY" | "nearest" | "nearestX" | "nearestY">` Applies a filter transform after data is loaded to highlight selected values only.
#' @param shape_rendering `<string | param()>` The shape-rendering; a constant string such as *crispEdges*.
#' @param sort `<column | literal | transform() | sql()/agg() | list>` Either applies a transform to sort the mark’s index by the specified channel values, or imputes ordinal scale domains from this mark’s channels.
#' @param stroke `<column | literal | transform() | sql()/agg() | list(value=, ...) | param()>` The stroke; a constant CSS color string, or a channel typically bound to the *color* scale.
#' @param stroke_dasharray `<string | number | param()>` The stroke-dasharray; a constant number indicating the length in pixels of alternating dashes and gaps, or a constant string of numbers separated by spaces or commas (_e.g._, *10 2* for dashes of 10 pixels separated by gaps of 2 pixels), or *none* (the default) for no dashing : https://developer.mozilla.org/en-US/docs/Web/SVG/Attribute/stroke-dasharray
#' @param stroke_dashoffset `<string | number | param()>` The stroke-dashoffset; a constant indicating the offset in pixels of the first dash along the stroke; defaults to zero.
#' @param stroke_linecap `<string | param()>` The stroke-linecap; a constant specifying how to cap stroked paths, such as *butt*, *round*, or *square*.
#' @param stroke_linejoin `<string | param()>` The stroke-linejoin; a constant specifying how to join stroked paths, such as *bevel*, *miter*, *miter-clip*, or *round*.
#' @param stroke_miterlimit `<number | param()>` The stroke-miterlimit; a constant number specifying how to limit the length of *miter* joins on stroked paths.
#' @param stroke_opacity `<column | literal | transform() | sql()/agg() | list(value=, ...)>` The stroke-opacity; a constant between 0 and 1, or a channel typically bound to the *opacity* scale.
#' @param stroke_width `<column | literal | transform() | sql()/agg() | list(value=, ...)>` The stroke-width; a constant number in pixels, or a channel.
#' @param target `<string | param()>` The target; a constant string specifying the target window (_e.g._, *_blank*) for clickable links; used in conjunction with the **href** option.
#' @param tip `<boolean | "x" | "y" | "xy" | list | param()>` Whether to generate a tooltip for this mark, and any tip options.
#' @param title `<column | literal | transform() | sql()/agg()>` The title; a channel specifying accessible, short textual descriptions as strings (possibly with newlines).
#' @param x `<column | literal | transform() | sql()/agg() | list(value=, ...)>` The horizontal position (or length) channel, typically bound to the *x* scale.
#' @param y `<column | literal | transform() | sql()/agg() | list(value=, ...)>` The vertical position channel, typically bound to the *y* scale; defaults to the zero-based index of the data 0, 1, 2, ….
#' @param ... Additional options or plot-level attributes.
#' @param data_from The name of the data source this mark reads from (see [vg_data()]); a length-1 nonzero R integer (`1L`, `-1L`, ...; note the `L`) giving a 1-based index into the spec's registered data sources instead, negative counting from the end (e.g., `-1L` for the most recently registered one); or a literal vector of values to use as inline data directly (mosaic-spec's `"data": [...]` shorthand, e.g., for a single reference line -- a bare double like `0`/`c(0)`, or `0L` itself (never a valid index), means this, not an index). Left unset, defaults to the first registered data source (equivalent to `data_from = 1L`) for any mark type that takes data at all.
#' @param filter_by A Param/Selection (e.g., from [param()]) to filter this mark's data by.
#' @param data_optimize A flag (default `TRUE`) to enable mark-specific query optimizations for this mark's data; set `FALSE` to disable them (mosaic-spec's `data: {optimize: false}`).
#' @family mark functions
#' @export
vg_mark_cell_x <- function(spec = NULL, formula = vg_unset, aria_description = vg_unset, aria_hidden = vg_unset, aria_label = vg_unset, channels = vg_unset, clip = vg_unset, dx = vg_unset, dy = vg_unset, facet = vg_unset, facet_anchor = vg_unset, fill = vg_unset, fill_opacity = vg_unset, filter = vg_unset, fx = vg_unset, fy = vg_unset, href = vg_unset, image_filter = vg_unset, inset = vg_unset, inset_bottom = vg_unset, inset_left = vg_unset, inset_right = vg_unset, inset_top = vg_unset, margin = vg_unset, margin_bottom = vg_unset, margin_left = vg_unset, margin_right = vg_unset, margin_top = vg_unset, mix_blend_mode = vg_unset, opacity = vg_unset, paint_order = vg_unset, pointer_events = vg_unset, reverse = vg_unset, rx = vg_unset, ry = vg_unset, select = vg_unset, shape_rendering = vg_unset, sort = vg_unset, stroke = vg_unset, stroke_dasharray = vg_unset, stroke_dashoffset = vg_unset, stroke_linecap = vg_unset, stroke_linejoin = vg_unset, stroke_miterlimit = vg_unset, stroke_opacity = vg_unset, stroke_width = vg_unset, target = vg_unset, tip = vg_unset, title = vg_unset, x = vg_unset, y = vg_unset, ..., data_from = vg_unset, filter_by = vg_unset, data_optimize = vg_unset) {
  vg_mark_(spec, "cellX", formula = formula, ariaDescription = aria_description, ariaHidden = aria_hidden, ariaLabel = aria_label, channels = channels, clip = clip, dx = dx, dy = dy, facet = facet, facetAnchor = facet_anchor, fill = fill, fillOpacity = fill_opacity, filter = filter, fx = fx, fy = fy, href = href, imageFilter = image_filter, inset = inset, insetBottom = inset_bottom, insetLeft = inset_left, insetRight = inset_right, insetTop = inset_top, margin = margin, marginBottom = margin_bottom, marginLeft = margin_left, marginRight = margin_right, marginTop = margin_top, mixBlendMode = mix_blend_mode, opacity = opacity, paintOrder = paint_order, pointerEvents = pointer_events, reverse = reverse, rx = rx, ry = ry, select = select, shapeRendering = shape_rendering, sort = sort, stroke = stroke, strokeDasharray = stroke_dasharray, strokeDashoffset = stroke_dashoffset, strokeLinecap = stroke_linecap, strokeLinejoin = stroke_linejoin, strokeMiterlimit = stroke_miterlimit, strokeOpacity = stroke_opacity, strokeWidth = stroke_width, target = target, tip = tip, title = title, x = x, y = y, ..., data_from = data_from, filter_by = filter_by, data_optimize = data_optimize)
}

#' The cellY mark.
#'
#' See [vg_value_types] for what the `<...>` notation below (`column`, `param()`, ...) means.
#'
#' @param spec A plot fragment or `vgspec` to add this mark to, or `NULL` to start a new plot with just this mark.
#' @param formula A shorthand for this mark's position channels (`x`, `y`, `fx`, `fy`, and paired `x1`/`x2` or `y1`/`y2`), e.g., `Sepal.Length ~ Sepal.Width | ~ Species` for `y = ~Sepal.Length, x = ~Sepal.Width, fx = ~Species`. See [vg_mark()] for the full grammar.
#' @param aria_description `<string | param()>` The aria-description; a constant textual description.
#' @param aria_hidden `<string | param()>` The aria-hidden state; a constant indicating whether the element is exposed to an accessibility API.
#' @param aria_label `<column | literal | transform() | sql()/agg()>` The aria-label; a channel specifying short textual labels representing the value in the accessibility tree.
#' @param channels `<list>` Additional named channels, for example to include in a tooltip.
#' @param clip `<"frame" | "sphere" | boolean | NULL | param()>` How to clip the mark; one of: - *frame* or true - clip to the plot’s frame (inner area) - *sphere* - clip to the projected sphere (*e.g.*, front hemisphere) - null or false - do not clip The *sphere* clip option requires a geographic projection.
#' @param dx `<number | param()>` The horizontal offset in pixels; a constant option.
#' @param dy `<number | param()>` The vertical offset in pixels; a constant option.
#' @param facet `<"auto" | "include" | "exclude" | "super" | boolean | NULL | param()>` Whether to enable or disable faceting; one of: - *auto* (default) - automatically determine if this mark should be faceted - *include* (or true) - draw the subset of the mark’s data in the current facet - *exclude* - draw the subset of the mark’s data *not* in the current facet - *super* - draw this mark in a single frame that covers all facets - null (or false) - repeat this mark’s data across all facets (*i.e.*, no faceting) When a mark uses *super* faceting, it is not allowed to use position scales (*x*, *y*, *fx*, or *fy*); *super* faceting is intended for decorations, such as labels and legends.
#' @param facet_anchor `<"top" | "right" | "bottom" | "left" | "top-left" | "top-right" | "bottom-left" | "bottom-right" | "top-empty" | "right-empty" | "bottom-empty" | "left-empty" | "empty" | NULL | param()>` How to place the mark with respect to facets; one of: - null (default for most marks) - display the mark in each non-empty facet - *top*, *right*, *bottom*, or *left* - display the mark only in facets on the given side - *top-empty*, *right-empty*, *bottom-empty*, or *left-empty* (default for axis marks) - display the mark only in facets that have empty space on the given side: either the margin, or an empty facet - *empty* - display the mark in empty facets only
#' @param fill `<column | literal | transform() | sql()/agg() | list(value=, ...) | param()>` The fill; a constant CSS color string, or a channel typically bound to the *color* scale.
#' @param fill_opacity `<column | literal | transform() | sql()/agg() | list(value=, ...) | param()>` The fill-opacity; a constant number between 0 and 1, or a channel typically bound to the *opacity* scale.
#' @param filter `<column | literal | transform() | sql()/agg()>` Applies a transform to filter the mark’s index according to the given channel values; only truthy values are retained.
#' @param fx `<column | literal | transform() | sql()/agg()>` The horizontal facet position channel, for mark-level faceting, bound to the *fx* scale.
#' @param fy `<column | literal | transform() | sql()/agg()>` The vertical facet position channel, for mark-level faceting, bound to the *fy* scale.
#' @param href `<column | literal | transform() | sql()/agg()>` The href; a channel specifying URLs for clickable links.
#' @param image_filter `<string | param()>` A CSS filter; a constant string used to adjust the rendering of images, such as *blur(5px)*.
#' @param inset `<number | param()>` Shorthand to set the same default for **insetStart** and **insetEnd**.
#' @param inset_bottom `<number | param()>` Insets the bottom edge by the specified number of pixels.
#' @param inset_left `<number | param()>` Insets the left edge by the specified number of pixels.
#' @param inset_right `<number | param()>` Insets the right edge by the specified number of pixels.
#' @param inset_top `<number | param()>` Insets the top edge by the specified number of pixels.
#' @param margin `<number | param()>` Shorthand to set the same default for all four mark margins: **marginTop**, **marginRight**, **marginBottom**, and **marginLeft**; typically defaults to 0, except for axis marks.
#' @param margin_bottom `<number | param()>` The mark’s bottom margin; the minimum distance in pixels between the bottom edges of the inner and outer plot area.
#' @param margin_left `<number | param()>` The mark’s left margin; the minimum distance in pixels between the left edges of the inner and outer plot area.
#' @param margin_right `<number | param()>` The mark’s right margin; the minimum distance in pixels between the right edges of the mark’s inner and outer plot area.
#' @param margin_top `<number | param()>` The mark’s top margin; the minimum distance in pixels between the top edges of the inner and outer plot area.
#' @param mix_blend_mode `<string | param()>` The mix-blend-mode; a constant string specifying how to blend content such as *multiply*.
#' @param opacity `<column | literal | transform() | sql()/agg() | list(value=, ...)>` The opacity; a constant between 0 and 1, or a channel typically bound to the *opacity* scale.
#' @param paint_order `<string | param()>` The paint-order; a constant string specifying the order in which the **fill**, **stroke**, and any markers are drawn; defaults to *normal*, which draws the fill, then stroke, then markers; defaults to *stroke* for the text mark to create a “halo” around text to improve legibility.
#' @param pointer_events `<string | param()>` The pointer-events property; a constant string such as *none*.
#' @param reverse `<boolean | param()>` Applies a transform to reverse the order of the mark’s index, say for reverse input order.
#' @param rx `<number | string | param()>` The rounded corner *x*-radius, either in pixels or as a percentage of the rect width.
#' @param ry `<number | string | param()>` The rounded corner *y*-radius, either in pixels or as a percentage of the rect height.
#' @param select `<"first" | "last" | "maxX" | "maxY" | "minX" | "minY" | "nearest" | "nearestX" | "nearestY">` Applies a filter transform after data is loaded to highlight selected values only.
#' @param shape_rendering `<string | param()>` The shape-rendering; a constant string such as *crispEdges*.
#' @param sort `<column | literal | transform() | sql()/agg() | list>` Either applies a transform to sort the mark’s index by the specified channel values, or imputes ordinal scale domains from this mark’s channels.
#' @param stroke `<column | literal | transform() | sql()/agg() | list(value=, ...) | param()>` The stroke; a constant CSS color string, or a channel typically bound to the *color* scale.
#' @param stroke_dasharray `<string | number | param()>` The stroke-dasharray; a constant number indicating the length in pixels of alternating dashes and gaps, or a constant string of numbers separated by spaces or commas (_e.g._, *10 2* for dashes of 10 pixels separated by gaps of 2 pixels), or *none* (the default) for no dashing : https://developer.mozilla.org/en-US/docs/Web/SVG/Attribute/stroke-dasharray
#' @param stroke_dashoffset `<string | number | param()>` The stroke-dashoffset; a constant indicating the offset in pixels of the first dash along the stroke; defaults to zero.
#' @param stroke_linecap `<string | param()>` The stroke-linecap; a constant specifying how to cap stroked paths, such as *butt*, *round*, or *square*.
#' @param stroke_linejoin `<string | param()>` The stroke-linejoin; a constant specifying how to join stroked paths, such as *bevel*, *miter*, *miter-clip*, or *round*.
#' @param stroke_miterlimit `<number | param()>` The stroke-miterlimit; a constant number specifying how to limit the length of *miter* joins on stroked paths.
#' @param stroke_opacity `<column | literal | transform() | sql()/agg() | list(value=, ...)>` The stroke-opacity; a constant between 0 and 1, or a channel typically bound to the *opacity* scale.
#' @param stroke_width `<column | literal | transform() | sql()/agg() | list(value=, ...)>` The stroke-width; a constant number in pixels, or a channel.
#' @param target `<string | param()>` The target; a constant string specifying the target window (_e.g._, *_blank*) for clickable links; used in conjunction with the **href** option.
#' @param tip `<boolean | "x" | "y" | "xy" | list | param()>` Whether to generate a tooltip for this mark, and any tip options.
#' @param title `<column | literal | transform() | sql()/agg()>` The title; a channel specifying accessible, short textual descriptions as strings (possibly with newlines).
#' @param x `<column | literal | transform() | sql()/agg() | list(value=, ...)>` The horizontal position (or length) channel, typically bound to the *x* scale.
#' @param y `<column | literal | transform() | sql()/agg() | list(value=, ...)>` The vertical position channel, typically bound to the *y* scale; defaults to the zero-based index of the data 0, 1, 2, ….
#' @param ... Additional options or plot-level attributes.
#' @param data_from The name of the data source this mark reads from (see [vg_data()]); a length-1 nonzero R integer (`1L`, `-1L`, ...; note the `L`) giving a 1-based index into the spec's registered data sources instead, negative counting from the end (e.g., `-1L` for the most recently registered one); or a literal vector of values to use as inline data directly (mosaic-spec's `"data": [...]` shorthand, e.g., for a single reference line -- a bare double like `0`/`c(0)`, or `0L` itself (never a valid index), means this, not an index). Left unset, defaults to the first registered data source (equivalent to `data_from = 1L`) for any mark type that takes data at all.
#' @param filter_by A Param/Selection (e.g., from [param()]) to filter this mark's data by.
#' @param data_optimize A flag (default `TRUE`) to enable mark-specific query optimizations for this mark's data; set `FALSE` to disable them (mosaic-spec's `data: {optimize: false}`).
#' @family mark functions
#' @export
vg_mark_cell_y <- function(spec = NULL, formula = vg_unset, aria_description = vg_unset, aria_hidden = vg_unset, aria_label = vg_unset, channels = vg_unset, clip = vg_unset, dx = vg_unset, dy = vg_unset, facet = vg_unset, facet_anchor = vg_unset, fill = vg_unset, fill_opacity = vg_unset, filter = vg_unset, fx = vg_unset, fy = vg_unset, href = vg_unset, image_filter = vg_unset, inset = vg_unset, inset_bottom = vg_unset, inset_left = vg_unset, inset_right = vg_unset, inset_top = vg_unset, margin = vg_unset, margin_bottom = vg_unset, margin_left = vg_unset, margin_right = vg_unset, margin_top = vg_unset, mix_blend_mode = vg_unset, opacity = vg_unset, paint_order = vg_unset, pointer_events = vg_unset, reverse = vg_unset, rx = vg_unset, ry = vg_unset, select = vg_unset, shape_rendering = vg_unset, sort = vg_unset, stroke = vg_unset, stroke_dasharray = vg_unset, stroke_dashoffset = vg_unset, stroke_linecap = vg_unset, stroke_linejoin = vg_unset, stroke_miterlimit = vg_unset, stroke_opacity = vg_unset, stroke_width = vg_unset, target = vg_unset, tip = vg_unset, title = vg_unset, x = vg_unset, y = vg_unset, ..., data_from = vg_unset, filter_by = vg_unset, data_optimize = vg_unset) {
  vg_mark_(spec, "cellY", formula = formula, ariaDescription = aria_description, ariaHidden = aria_hidden, ariaLabel = aria_label, channels = channels, clip = clip, dx = dx, dy = dy, facet = facet, facetAnchor = facet_anchor, fill = fill, fillOpacity = fill_opacity, filter = filter, fx = fx, fy = fy, href = href, imageFilter = image_filter, inset = inset, insetBottom = inset_bottom, insetLeft = inset_left, insetRight = inset_right, insetTop = inset_top, margin = margin, marginBottom = margin_bottom, marginLeft = margin_left, marginRight = margin_right, marginTop = margin_top, mixBlendMode = mix_blend_mode, opacity = opacity, paintOrder = paint_order, pointerEvents = pointer_events, reverse = reverse, rx = rx, ry = ry, select = select, shapeRendering = shape_rendering, sort = sort, stroke = stroke, strokeDasharray = stroke_dasharray, strokeDashoffset = stroke_dashoffset, strokeLinecap = stroke_linecap, strokeLinejoin = stroke_linejoin, strokeMiterlimit = stroke_miterlimit, strokeOpacity = stroke_opacity, strokeWidth = stroke_width, target = target, tip = tip, title = title, x = x, y = y, ..., data_from = data_from, filter_by = filter_by, data_optimize = data_optimize)
}

#' The circle mark.
#'
#' See [vg_value_types] for what the `<...>` notation below (`column`, `param()`, ...) means.
#'
#' @param spec A plot fragment or `vgspec` to add this mark to, or `NULL` to start a new plot with just this mark.
#' @param formula A shorthand for this mark's position channels (`x`, `y`, `fx`, `fy`, and paired `x1`/`x2` or `y1`/`y2`), e.g., `Sepal.Length ~ Sepal.Width | ~ Species` for `y = ~Sepal.Length, x = ~Sepal.Width, fx = ~Species`. See [vg_mark()] for the full grammar.
#' @param aria_description `<string | param()>` The aria-description; a constant textual description.
#' @param aria_hidden `<string | param()>` The aria-hidden state; a constant indicating whether the element is exposed to an accessibility API.
#' @param aria_label `<column | literal | transform() | sql()/agg()>` The aria-label; a channel specifying short textual labels representing the value in the accessibility tree.
#' @param channels `<list>` Additional named channels, for example to include in a tooltip.
#' @param clip `<"frame" | "sphere" | boolean | NULL | param()>` How to clip the mark; one of: - *frame* or true - clip to the plot’s frame (inner area) - *sphere* - clip to the projected sphere (*e.g.*, front hemisphere) - null or false - do not clip The *sphere* clip option requires a geographic projection.
#' @param dx `<number | param()>` The horizontal offset in pixels; a constant option.
#' @param dy `<number | param()>` The vertical offset in pixels; a constant option.
#' @param facet `<"auto" | "include" | "exclude" | "super" | boolean | NULL | param()>` Whether to enable or disable faceting; one of: - *auto* (default) - automatically determine if this mark should be faceted - *include* (or true) - draw the subset of the mark’s data in the current facet - *exclude* - draw the subset of the mark’s data *not* in the current facet - *super* - draw this mark in a single frame that covers all facets - null (or false) - repeat this mark’s data across all facets (*i.e.*, no faceting) When a mark uses *super* faceting, it is not allowed to use position scales (*x*, *y*, *fx*, or *fy*); *super* faceting is intended for decorations, such as labels and legends.
#' @param facet_anchor `<"top" | "right" | "bottom" | "left" | "top-left" | "top-right" | "bottom-left" | "bottom-right" | "top-empty" | "right-empty" | "bottom-empty" | "left-empty" | "empty" | NULL | param()>` How to place the mark with respect to facets; one of: - null (default for most marks) - display the mark in each non-empty facet - *top*, *right*, *bottom*, or *left* - display the mark only in facets on the given side - *top-empty*, *right-empty*, *bottom-empty*, or *left-empty* (default for axis marks) - display the mark only in facets that have empty space on the given side: either the margin, or an empty facet - *empty* - display the mark in empty facets only
#' @param fill `<column | literal | transform() | sql()/agg() | list(value=, ...) | param()>` The fill; a constant CSS color string, or a channel typically bound to the *color* scale.
#' @param fill_opacity `<column | literal | transform() | sql()/agg() | list(value=, ...) | param()>` The fill-opacity; a constant number between 0 and 1, or a channel typically bound to the *opacity* scale.
#' @param filter `<column | literal | transform() | sql()/agg()>` Applies a transform to filter the mark’s index according to the given channel values; only truthy values are retained.
#' @param frame_anchor `<"middle" | "top-left" | "top" | "top-right" | "right" | "bottom-right" | "bottom" | "bottom-left" | "left" | param()>` The frame anchor specifies defaults for **x** and **y**, along with **textAnchor** and **lineAnchor**, based on the plot’s frame; it may be one of the four sides (*top*, *right*, *bottom*, *left*), one of the four corners (*top-left*, *top-right*, *bottom-right*, *bottom-left*), or the *middle* of the frame.
#' @param fx `<column | literal | transform() | sql()/agg()>` The horizontal facet position channel, for mark-level faceting, bound to the *fx* scale.
#' @param fy `<column | literal | transform() | sql()/agg()>` The vertical facet position channel, for mark-level faceting, bound to the *fy* scale.
#' @param href `<column | literal | transform() | sql()/agg()>` The href; a channel specifying URLs for clickable links.
#' @param image_filter `<string | param()>` A CSS filter; a constant string used to adjust the rendering of images, such as *blur(5px)*.
#' @param margin `<number | param()>` Shorthand to set the same default for all four mark margins: **marginTop**, **marginRight**, **marginBottom**, and **marginLeft**; typically defaults to 0, except for axis marks.
#' @param margin_bottom `<number | param()>` The mark’s bottom margin; the minimum distance in pixels between the bottom edges of the inner and outer plot area.
#' @param margin_left `<number | param()>` The mark’s left margin; the minimum distance in pixels between the left edges of the inner and outer plot area.
#' @param margin_right `<number | param()>` The mark’s right margin; the minimum distance in pixels between the right edges of the mark’s inner and outer plot area.
#' @param margin_top `<number | param()>` The mark’s top margin; the minimum distance in pixels between the top edges of the inner and outer plot area.
#' @param mix_blend_mode `<string | param()>` The mix-blend-mode; a constant string specifying how to blend content such as *multiply*.
#' @param opacity `<column | literal | transform() | sql()/agg() | list(value=, ...)>` The opacity; a constant between 0 and 1, or a channel typically bound to the *opacity* scale.
#' @param paint_order `<string | param()>` The paint-order; a constant string specifying the order in which the **fill**, **stroke**, and any markers are drawn; defaults to *normal*, which draws the fill, then stroke, then markers; defaults to *stroke* for the text mark to create a “halo” around text to improve legibility.
#' @param pointer_events `<string | param()>` The pointer-events property; a constant string such as *none*.
#' @param r `<column | literal | transform() | sql()/agg() | list(value=, ...) | number | param()>` The radius of dots; either a channel or constant.
#' @param reverse `<boolean | param()>` Applies a transform to reverse the order of the mark’s index, say for reverse input order.
#' @param rotate `<column | literal | transform() | sql()/agg() | param()>` The rotation angle in degrees clockwise; a constant or a channel; defaults to 0°.
#' @param select `<"first" | "last" | "maxX" | "maxY" | "minX" | "minY" | "nearest" | "nearestX" | "nearestY">` Applies a filter transform after data is loaded to highlight selected values only.
#' @param shape_rendering `<string | param()>` The shape-rendering; a constant string such as *crispEdges*.
#' @param sort `<column | literal | transform() | sql()/agg() | list>` Either applies a transform to sort the mark’s index by the specified channel values, or imputes ordinal scale domains from this mark’s channels.
#' @param stroke `<column | literal | transform() | sql()/agg() | list(value=, ...) | param()>` The stroke; a constant CSS color string, or a channel typically bound to the *color* scale.
#' @param stroke_dasharray `<string | number | param()>` The stroke-dasharray; a constant number indicating the length in pixels of alternating dashes and gaps, or a constant string of numbers separated by spaces or commas (_e.g._, *10 2* for dashes of 10 pixels separated by gaps of 2 pixels), or *none* (the default) for no dashing : https://developer.mozilla.org/en-US/docs/Web/SVG/Attribute/stroke-dasharray
#' @param stroke_dashoffset `<string | number | param()>` The stroke-dashoffset; a constant indicating the offset in pixels of the first dash along the stroke; defaults to zero.
#' @param stroke_linecap `<string | param()>` The stroke-linecap; a constant specifying how to cap stroked paths, such as *butt*, *round*, or *square*.
#' @param stroke_linejoin `<string | param()>` The stroke-linejoin; a constant specifying how to join stroked paths, such as *bevel*, *miter*, *miter-clip*, or *round*.
#' @param stroke_miterlimit `<number | param()>` The stroke-miterlimit; a constant number specifying how to limit the length of *miter* joins on stroked paths.
#' @param stroke_opacity `<column | literal | transform() | sql()/agg() | list(value=, ...)>` The stroke-opacity; a constant between 0 and 1, or a channel typically bound to the *opacity* scale.
#' @param stroke_width `<column | literal | transform() | sql()/agg() | list(value=, ...)>` The stroke-width; a constant number in pixels, or a channel.
#' @param symbol `<column | literal | transform() | sql()/agg() | list(value=, ...) | "asterisk" | "circle" | "cross" | "diamond" | "diamond2" | "hexagon" | "plus" | "square" | "square2" | "star" | ... | param()>` The categorical symbol; either a channel or a constant.
#' @param target `<string | param()>` The target; a constant string specifying the target window (_e.g._, *_blank*) for clickable links; used in conjunction with the **href** option.
#' @param tip `<boolean | "x" | "y" | "xy" | list | param()>` Whether to generate a tooltip for this mark, and any tip options.
#' @param title `<column | literal | transform() | sql()/agg()>` The title; a channel specifying accessible, short textual descriptions as strings (possibly with newlines).
#' @param x `<column | literal | transform() | sql()/agg() | list(value=, ...)>` The horizontal position (or length) channel, typically bound to the *x* scale.
#' @param y `<column | literal | transform() | sql()/agg() | list(value=, ...)>` The vertical position channel, typically bound to the *y* scale; defaults to the zero-based index of the data 0, 1, 2, ….
#' @param z `<column | literal | transform() | sql()/agg()>` An optional ordinal channel for grouping data into (possibly stacked) series to be drawn as separate areas; defaults to **fill** if a channel, or **stroke** if a channel.
#' @param ... Additional options or plot-level attributes.
#' @param data_from The name of the data source this mark reads from (see [vg_data()]); a length-1 nonzero R integer (`1L`, `-1L`, ...; note the `L`) giving a 1-based index into the spec's registered data sources instead, negative counting from the end (e.g., `-1L` for the most recently registered one); or a literal vector of values to use as inline data directly (mosaic-spec's `"data": [...]` shorthand, e.g., for a single reference line -- a bare double like `0`/`c(0)`, or `0L` itself (never a valid index), means this, not an index). Left unset, defaults to the first registered data source (equivalent to `data_from = 1L`) for any mark type that takes data at all.
#' @param filter_by A Param/Selection (e.g., from [param()]) to filter this mark's data by.
#' @param data_optimize A flag (default `TRUE`) to enable mark-specific query optimizations for this mark's data; set `FALSE` to disable them (mosaic-spec's `data: {optimize: false}`).
#' @family mark functions
#' @export
vg_mark_circle <- function(spec = NULL, formula = vg_unset, aria_description = vg_unset, aria_hidden = vg_unset, aria_label = vg_unset, channels = vg_unset, clip = vg_unset, dx = vg_unset, dy = vg_unset, facet = vg_unset, facet_anchor = vg_unset, fill = vg_unset, fill_opacity = vg_unset, filter = vg_unset, frame_anchor = vg_unset, fx = vg_unset, fy = vg_unset, href = vg_unset, image_filter = vg_unset, margin = vg_unset, margin_bottom = vg_unset, margin_left = vg_unset, margin_right = vg_unset, margin_top = vg_unset, mix_blend_mode = vg_unset, opacity = vg_unset, paint_order = vg_unset, pointer_events = vg_unset, r = vg_unset, reverse = vg_unset, rotate = vg_unset, select = vg_unset, shape_rendering = vg_unset, sort = vg_unset, stroke = vg_unset, stroke_dasharray = vg_unset, stroke_dashoffset = vg_unset, stroke_linecap = vg_unset, stroke_linejoin = vg_unset, stroke_miterlimit = vg_unset, stroke_opacity = vg_unset, stroke_width = vg_unset, symbol = vg_unset, target = vg_unset, tip = vg_unset, title = vg_unset, x = vg_unset, y = vg_unset, z = vg_unset, ..., data_from = vg_unset, filter_by = vg_unset, data_optimize = vg_unset) {
  vg_mark_(spec, "circle", formula = formula, ariaDescription = aria_description, ariaHidden = aria_hidden, ariaLabel = aria_label, channels = channels, clip = clip, dx = dx, dy = dy, facet = facet, facetAnchor = facet_anchor, fill = fill, fillOpacity = fill_opacity, filter = filter, frameAnchor = frame_anchor, fx = fx, fy = fy, href = href, imageFilter = image_filter, margin = margin, marginBottom = margin_bottom, marginLeft = margin_left, marginRight = margin_right, marginTop = margin_top, mixBlendMode = mix_blend_mode, opacity = opacity, paintOrder = paint_order, pointerEvents = pointer_events, r = r, reverse = reverse, rotate = rotate, select = select, shapeRendering = shape_rendering, sort = sort, stroke = stroke, strokeDasharray = stroke_dasharray, strokeDashoffset = stroke_dashoffset, strokeLinecap = stroke_linecap, strokeLinejoin = stroke_linejoin, strokeMiterlimit = stroke_miterlimit, strokeOpacity = stroke_opacity, strokeWidth = stroke_width, symbol = symbol, target = target, tip = tip, title = title, x = x, y = y, z = z, ..., data_from = data_from, filter_by = filter_by, data_optimize = data_optimize)
}

#' The contour mark.
#'
#' See [vg_value_types] for what the `<...>` notation below (`column`, `param()`, ...) means.
#'
#' @param spec A plot fragment or `vgspec` to add this mark to, or `NULL` to start a new plot with just this mark.
#' @param formula A shorthand for this mark's position channels (`x`, `y`, `fx`, `fy`, and paired `x1`/`x2` or `y1`/`y2`), e.g., `Sepal.Length ~ Sepal.Width | ~ Species` for `y = ~Sepal.Length, x = ~Sepal.Width, fx = ~Species`. See [vg_mark()] for the full grammar.
#' @param aria_description `<string | param()>` The aria-description; a constant textual description.
#' @param aria_hidden `<string | param()>` The aria-hidden state; a constant indicating whether the element is exposed to an accessibility API.
#' @param aria_label `<column | literal | transform() | sql()/agg()>` The aria-label; a channel specifying short textual labels representing the value in the accessibility tree.
#' @param bandwidth `<number | param()>` The kernel density bandwidth for smoothing, in pixels.
#' @param channels `<list>` Additional named channels, for example to include in a tooltip.
#' @param clip `<"frame" | "sphere" | boolean | NULL | param()>` How to clip the mark; one of: - *frame* or true - clip to the plot’s frame (inner area) - *sphere* - clip to the projected sphere (*e.g.*, front hemisphere) - null or false - do not clip The *sphere* clip option requires a geographic projection.
#' @param dx `<number | param()>` The horizontal offset in pixels; a constant option.
#' @param dy `<number | param()>` The vertical offset in pixels; a constant option.
#' @param facet `<"auto" | "include" | "exclude" | "super" | boolean | NULL | param()>` Whether to enable or disable faceting; one of: - *auto* (default) - automatically determine if this mark should be faceted - *include* (or true) - draw the subset of the mark’s data in the current facet - *exclude* - draw the subset of the mark’s data *not* in the current facet - *super* - draw this mark in a single frame that covers all facets - null (or false) - repeat this mark’s data across all facets (*i.e.*, no faceting) When a mark uses *super* faceting, it is not allowed to use position scales (*x*, *y*, *fx*, or *fy*); *super* faceting is intended for decorations, such as labels and legends.
#' @param facet_anchor `<"top" | "right" | "bottom" | "left" | "top-left" | "top-right" | "bottom-left" | "bottom-right" | "top-empty" | "right-empty" | "bottom-empty" | "left-empty" | "empty" | NULL | param()>` How to place the mark with respect to facets; one of: - null (default for most marks) - display the mark in each non-empty facet - *top*, *right*, *bottom*, or *left* - display the mark only in facets on the given side - *top-empty*, *right-empty*, *bottom-empty*, or *left-empty* (default for axis marks) - display the mark only in facets that have empty space on the given side: either the margin, or an empty facet - *empty* - display the mark in empty facets only
#' @param fill `<column | literal | transform() | sql()/agg() | list(value=, ...) | param()>` The fill; a constant CSS color string, or a channel typically bound to the *color* scale.
#' @param fill_opacity `<column | literal | transform() | sql()/agg() | list(value=, ...) | param()>` The fill-opacity; a constant number between 0 and 1, or a channel typically bound to the *opacity* scale.
#' @param filter `<column | literal | transform() | sql()/agg()>` Applies a transform to filter the mark’s index according to the given channel values; only truthy values are retained.
#' @param fx `<column | literal | transform() | sql()/agg()>` The horizontal facet position channel, for mark-level faceting, bound to the *fx* scale.
#' @param fy `<column | literal | transform() | sql()/agg()>` The vertical facet position channel, for mark-level faceting, bound to the *fy* scale.
#' @param height `<number | param()>` The height (number of rows) of the grid, in actual pixels.
#' @param href `<column | literal | transform() | sql()/agg()>` The href; a channel specifying URLs for clickable links.
#' @param image_filter `<string | param()>` A CSS filter; a constant string used to adjust the rendering of images, such as *blur(5px)*.
#' @param interpolate `<"none" | "linear" | "nearest" | "barycentric" | "random-walk" | NULL | param()>` The spatial interpolation method; one of: - *none* - do not perform interpolation (the default), maps samples to single bins - *linear* - apply proportional linear interpolation across adjacent bins - *nearest* - assign each pixel to the closest sample’s value (Voronoi diagram) - *barycentric* - apply barycentric interpolation over the Delaunay triangulation - *random-walk* - apply a random walk from each pixel, stopping when near a sample
#' @param margin `<number | param()>` Shorthand to set the same default for all four mark margins: **marginTop**, **marginRight**, **marginBottom**, and **marginLeft**; typically defaults to 0, except for axis marks.
#' @param margin_bottom `<number | param()>` The mark’s bottom margin; the minimum distance in pixels between the bottom edges of the inner and outer plot area.
#' @param margin_left `<number | param()>` The mark’s left margin; the minimum distance in pixels between the left edges of the inner and outer plot area.
#' @param margin_right `<number | param()>` The mark’s right margin; the minimum distance in pixels between the right edges of the mark’s inner and outer plot area.
#' @param margin_top `<number | param()>` The mark’s top margin; the minimum distance in pixels between the top edges of the inner and outer plot area.
#' @param mix_blend_mode `<string | param()>` The mix-blend-mode; a constant string specifying how to blend content such as *multiply*.
#' @param opacity `<column | literal | transform() | sql()/agg() | list(value=, ...)>` The opacity; a constant between 0 and 1, or a channel typically bound to the *opacity* scale.
#' @param pad `<number | param()>` The bin padding, one of 1 (default) to include extra padding for the final bin, or 0 to make the bins flush with the maximum domain value.
#' @param paint_order `<string | param()>` The paint-order; a constant string specifying the order in which the **fill**, **stroke**, and any markers are drawn; defaults to *normal*, which draws the fill, then stroke, then markers; defaults to *stroke* for the text mark to create a “halo” around text to improve legibility.
#' @param pixel_size `<number | param()>` The effective screen size of a raster pixel, used to determine the height and width of the raster from the frame’s dimensions; defaults to 1.
#' @param pointer_events `<string | param()>` The pointer-events property; a constant string such as *none*.
#' @param reverse `<boolean | param()>` Applies a transform to reverse the order of the mark’s index, say for reverse input order.
#' @param select `<"first" | "last" | "maxX" | "maxY" | "minX" | "minY" | "nearest" | "nearestX" | "nearestY">` Applies a filter transform after data is loaded to highlight selected values only.
#' @param shape_rendering `<string | param()>` The shape-rendering; a constant string such as *crispEdges*.
#' @param sort `<column | literal | transform() | sql()/agg() | list>` Either applies a transform to sort the mark’s index by the specified channel values, or imputes ordinal scale domains from this mark’s channels.
#' @param stroke `<column | literal | transform() | sql()/agg() | list(value=, ...) | param()>` The stroke; a constant CSS color string, or a channel typically bound to the *color* scale.
#' @param stroke_dasharray `<string | number | param()>` The stroke-dasharray; a constant number indicating the length in pixels of alternating dashes and gaps, or a constant string of numbers separated by spaces or commas (_e.g._, *10 2* for dashes of 10 pixels separated by gaps of 2 pixels), or *none* (the default) for no dashing : https://developer.mozilla.org/en-US/docs/Web/SVG/Attribute/stroke-dasharray
#' @param stroke_dashoffset `<string | number | param()>` The stroke-dashoffset; a constant indicating the offset in pixels of the first dash along the stroke; defaults to zero.
#' @param stroke_linecap `<string | param()>` The stroke-linecap; a constant specifying how to cap stroked paths, such as *butt*, *round*, or *square*.
#' @param stroke_linejoin `<string | param()>` The stroke-linejoin; a constant specifying how to join stroked paths, such as *bevel*, *miter*, *miter-clip*, or *round*.
#' @param stroke_miterlimit `<number | param()>` The stroke-miterlimit; a constant number specifying how to limit the length of *miter* joins on stroked paths.
#' @param stroke_opacity `<column | literal | transform() | sql()/agg() | list(value=, ...)>` The stroke-opacity; a constant between 0 and 1, or a channel typically bound to the *opacity* scale.
#' @param stroke_width `<column | literal | transform() | sql()/agg() | list(value=, ...)>` The stroke-width; a constant number in pixels, or a channel.
#' @param target `<string | param()>` The target; a constant string specifying the target window (_e.g._, *_blank*) for clickable links; used in conjunction with the **href** option.
#' @param thresholds `<number | numeric vector | param()>` The number of contour thresholds to subdivide the domain into discrete level sets; defaults to 10.
#' @param tip `<boolean | "x" | "y" | "xy" | list | param()>` Whether to generate a tooltip for this mark, and any tip options.
#' @param title `<column | literal | transform() | sql()/agg()>` The title; a channel specifying accessible, short textual descriptions as strings (possibly with newlines).
#' @param width `<number | param()>` The width (number of columns) of the grid, in actual pixels.
#' @param x `<column | literal | transform() | sql()/agg() | list(value=, ...)>` The horizontal position (or length) channel, typically bound to the *x* scale.
#' @param y `<column | literal | transform() | sql()/agg() | list(value=, ...)>` The vertical position channel, typically bound to the *y* scale; defaults to the zero-based index of the data 0, 1, 2, ….
#' @param ... Additional options or plot-level attributes.
#' @param data_from The name of the data source this mark reads from (see [vg_data()]); a length-1 nonzero R integer (`1L`, `-1L`, ...; note the `L`) giving a 1-based index into the spec's registered data sources instead, negative counting from the end (e.g., `-1L` for the most recently registered one); or a literal vector of values to use as inline data directly (mosaic-spec's `"data": [...]` shorthand, e.g., for a single reference line -- a bare double like `0`/`c(0)`, or `0L` itself (never a valid index), means this, not an index). Left unset, defaults to the first registered data source (equivalent to `data_from = 1L`) for any mark type that takes data at all.
#' @param filter_by A Param/Selection (e.g., from [param()]) to filter this mark's data by.
#' @param data_optimize A flag (default `TRUE`) to enable mark-specific query optimizations for this mark's data; set `FALSE` to disable them (mosaic-spec's `data: {optimize: false}`).
#' @family mark functions
#' @export
vg_mark_contour <- function(spec = NULL, formula = vg_unset, aria_description = vg_unset, aria_hidden = vg_unset, aria_label = vg_unset, bandwidth = vg_unset, channels = vg_unset, clip = vg_unset, dx = vg_unset, dy = vg_unset, facet = vg_unset, facet_anchor = vg_unset, fill = vg_unset, fill_opacity = vg_unset, filter = vg_unset, fx = vg_unset, fy = vg_unset, height = vg_unset, href = vg_unset, image_filter = vg_unset, interpolate = vg_unset, margin = vg_unset, margin_bottom = vg_unset, margin_left = vg_unset, margin_right = vg_unset, margin_top = vg_unset, mix_blend_mode = vg_unset, opacity = vg_unset, pad = vg_unset, paint_order = vg_unset, pixel_size = vg_unset, pointer_events = vg_unset, reverse = vg_unset, select = vg_unset, shape_rendering = vg_unset, sort = vg_unset, stroke = vg_unset, stroke_dasharray = vg_unset, stroke_dashoffset = vg_unset, stroke_linecap = vg_unset, stroke_linejoin = vg_unset, stroke_miterlimit = vg_unset, stroke_opacity = vg_unset, stroke_width = vg_unset, target = vg_unset, thresholds = vg_unset, tip = vg_unset, title = vg_unset, width = vg_unset, x = vg_unset, y = vg_unset, ..., data_from = vg_unset, filter_by = vg_unset, data_optimize = vg_unset) {
  vg_mark_(spec, "contour", formula = formula, ariaDescription = aria_description, ariaHidden = aria_hidden, ariaLabel = aria_label, bandwidth = bandwidth, channels = channels, clip = clip, dx = dx, dy = dy, facet = facet, facetAnchor = facet_anchor, fill = fill, fillOpacity = fill_opacity, filter = filter, fx = fx, fy = fy, height = height, href = href, imageFilter = image_filter, interpolate = interpolate, margin = margin, marginBottom = margin_bottom, marginLeft = margin_left, marginRight = margin_right, marginTop = margin_top, mixBlendMode = mix_blend_mode, opacity = opacity, pad = pad, paintOrder = paint_order, pixelSize = pixel_size, pointerEvents = pointer_events, reverse = reverse, select = select, shapeRendering = shape_rendering, sort = sort, stroke = stroke, strokeDasharray = stroke_dasharray, strokeDashoffset = stroke_dashoffset, strokeLinecap = stroke_linecap, strokeLinejoin = stroke_linejoin, strokeMiterlimit = stroke_miterlimit, strokeOpacity = stroke_opacity, strokeWidth = stroke_width, target = target, thresholds = thresholds, tip = tip, title = title, width = width, x = x, y = y, ..., data_from = data_from, filter_by = filter_by, data_optimize = data_optimize)
}

#' The delaunayLink mark.
#'
#' See [vg_value_types] for what the `<...>` notation below (`column`, `param()`, ...) means.
#'
#' @param spec A plot fragment or `vgspec` to add this mark to, or `NULL` to start a new plot with just this mark.
#' @param formula A shorthand for this mark's position channels (`x`, `y`, `fx`, `fy`, and paired `x1`/`x2` or `y1`/`y2`), e.g., `Sepal.Length ~ Sepal.Width | ~ Species` for `y = ~Sepal.Length, x = ~Sepal.Width, fx = ~Species`. See [vg_mark()] for the full grammar.
#' @param aria_description `<string | param()>` The aria-description; a constant textual description.
#' @param aria_hidden `<string | param()>` The aria-hidden state; a constant indicating whether the element is exposed to an accessibility API.
#' @param aria_label `<column | literal | transform() | sql()/agg()>` The aria-label; a channel specifying short textual labels representing the value in the accessibility tree.
#' @param channels `<list>` Additional named channels, for example to include in a tooltip.
#' @param clip `<"frame" | "sphere" | boolean | NULL | param()>` How to clip the mark; one of: - *frame* or true - clip to the plot’s frame (inner area) - *sphere* - clip to the projected sphere (*e.g.*, front hemisphere) - null or false - do not clip The *sphere* clip option requires a geographic projection.
#' @param curve `<"basis" | "basis-closed" | "basis-open" | "bundle" | "bump-x" | "bump-y" | "cardinal" | "cardinal-closed" | "cardinal-open" | "catmull-rom" | ... | param()>` The curve (interpolation) method for connecting adjacent points.
#' @param dx `<number | param()>` The horizontal offset in pixels; a constant option.
#' @param dy `<number | param()>` The vertical offset in pixels; a constant option.
#' @param facet `<"auto" | "include" | "exclude" | "super" | boolean | NULL | param()>` Whether to enable or disable faceting; one of: - *auto* (default) - automatically determine if this mark should be faceted - *include* (or true) - draw the subset of the mark’s data in the current facet - *exclude* - draw the subset of the mark’s data *not* in the current facet - *super* - draw this mark in a single frame that covers all facets - null (or false) - repeat this mark’s data across all facets (*i.e.*, no faceting) When a mark uses *super* faceting, it is not allowed to use position scales (*x*, *y*, *fx*, or *fy*); *super* faceting is intended for decorations, such as labels and legends.
#' @param facet_anchor `<"top" | "right" | "bottom" | "left" | "top-left" | "top-right" | "bottom-left" | "bottom-right" | "top-empty" | "right-empty" | "bottom-empty" | "left-empty" | "empty" | NULL | param()>` How to place the mark with respect to facets; one of: - null (default for most marks) - display the mark in each non-empty facet - *top*, *right*, *bottom*, or *left* - display the mark only in facets on the given side - *top-empty*, *right-empty*, *bottom-empty*, or *left-empty* (default for axis marks) - display the mark only in facets that have empty space on the given side: either the margin, or an empty facet - *empty* - display the mark in empty facets only
#' @param fill `<column | literal | transform() | sql()/agg() | list(value=, ...) | param()>` The fill; a constant CSS color string, or a channel typically bound to the *color* scale.
#' @param fill_opacity `<column | literal | transform() | sql()/agg() | list(value=, ...) | param()>` The fill-opacity; a constant number between 0 and 1, or a channel typically bound to the *opacity* scale.
#' @param filter `<column | literal | transform() | sql()/agg()>` Applies a transform to filter the mark’s index according to the given channel values; only truthy values are retained.
#' @param fx `<column | literal | transform() | sql()/agg()>` The horizontal facet position channel, for mark-level faceting, bound to the *fx* scale.
#' @param fy `<column | literal | transform() | sql()/agg()>` The vertical facet position channel, for mark-level faceting, bound to the *fy* scale.
#' @param href `<column | literal | transform() | sql()/agg()>` The href; a channel specifying URLs for clickable links.
#' @param image_filter `<string | param()>` A CSS filter; a constant string used to adjust the rendering of images, such as *blur(5px)*.
#' @param margin `<number | param()>` Shorthand to set the same default for all four mark margins: **marginTop**, **marginRight**, **marginBottom**, and **marginLeft**; typically defaults to 0, except for axis marks.
#' @param margin_bottom `<number | param()>` The mark’s bottom margin; the minimum distance in pixels between the bottom edges of the inner and outer plot area.
#' @param margin_left `<number | param()>` The mark’s left margin; the minimum distance in pixels between the left edges of the inner and outer plot area.
#' @param margin_right `<number | param()>` The mark’s right margin; the minimum distance in pixels between the right edges of the mark’s inner and outer plot area.
#' @param margin_top `<number | param()>` The mark’s top margin; the minimum distance in pixels between the top edges of the inner and outer plot area.
#' @param marker `<"arrow" | "arrow-reverse" | "dot" | "circle" | "circle-fill" | "circle-stroke" | "tick" | "tick-x" | "tick-y" | "none" | boolean | NULL | param()>` Shorthand to set the same default for markerStart, markerMid, and markerEnd; one of: - a marker name such as *arrow* or *circle* - *none* (default) - no marker * true - alias for *circle-fill* * false or null - alias for *none*
#' @param marker_end `<"arrow" | "arrow-reverse" | "dot" | "circle" | "circle-fill" | "circle-stroke" | "tick" | "tick-x" | "tick-y" | "none" | boolean | NULL | param()>` The marker for the ending point of a line segment; one of: - a marker name such as *arrow* or *circle* * *none* (default) - no marker * true - alias for *circle-fill* * false or null - alias for *none*
#' @param marker_mid `<"arrow" | "arrow-reverse" | "dot" | "circle" | "circle-fill" | "circle-stroke" | "tick" | "tick-x" | "tick-y" | "none" | boolean | NULL | param()>` The marker for any middle (interior) points of a line segment.
#' @param marker_start `<"arrow" | "arrow-reverse" | "dot" | "circle" | "circle-fill" | "circle-stroke" | "tick" | "tick-x" | "tick-y" | "none" | boolean | NULL | param()>` The marker for the starting point of a line segment; one of: - a marker name such as *arrow* or *circle* * *none* (default) - no marker * true - alias for *circle-fill* * false or null - alias for *none*
#' @param mix_blend_mode `<string | param()>` The mix-blend-mode; a constant string specifying how to blend content such as *multiply*.
#' @param opacity `<column | literal | transform() | sql()/agg() | list(value=, ...)>` The opacity; a constant between 0 and 1, or a channel typically bound to the *opacity* scale.
#' @param paint_order `<string | param()>` The paint-order; a constant string specifying the order in which the **fill**, **stroke**, and any markers are drawn; defaults to *normal*, which draws the fill, then stroke, then markers; defaults to *stroke* for the text mark to create a “halo” around text to improve legibility.
#' @param pointer_events `<string | param()>` The pointer-events property; a constant string such as *none*.
#' @param reverse `<boolean | param()>` Applies a transform to reverse the order of the mark’s index, say for reverse input order.
#' @param select `<"first" | "last" | "maxX" | "maxY" | "minX" | "minY" | "nearest" | "nearestX" | "nearestY">` Applies a filter transform after data is loaded to highlight selected values only.
#' @param shape_rendering `<string | param()>` The shape-rendering; a constant string such as *crispEdges*.
#' @param sort `<column | literal | transform() | sql()/agg() | list>` Either applies a transform to sort the mark’s index by the specified channel values, or imputes ordinal scale domains from this mark’s channels.
#' @param stroke `<column | literal | transform() | sql()/agg() | list(value=, ...) | param()>` The stroke; a constant CSS color string, or a channel typically bound to the *color* scale.
#' @param stroke_dasharray `<string | number | param()>` The stroke-dasharray; a constant number indicating the length in pixels of alternating dashes and gaps, or a constant string of numbers separated by spaces or commas (_e.g._, *10 2* for dashes of 10 pixels separated by gaps of 2 pixels), or *none* (the default) for no dashing : https://developer.mozilla.org/en-US/docs/Web/SVG/Attribute/stroke-dasharray
#' @param stroke_dashoffset `<string | number | param()>` The stroke-dashoffset; a constant indicating the offset in pixels of the first dash along the stroke; defaults to zero.
#' @param stroke_linecap `<string | param()>` The stroke-linecap; a constant specifying how to cap stroked paths, such as *butt*, *round*, or *square*.
#' @param stroke_linejoin `<string | param()>` The stroke-linejoin; a constant specifying how to join stroked paths, such as *bevel*, *miter*, *miter-clip*, or *round*.
#' @param stroke_miterlimit `<number | param()>` The stroke-miterlimit; a constant number specifying how to limit the length of *miter* joins on stroked paths.
#' @param stroke_opacity `<column | literal | transform() | sql()/agg() | list(value=, ...)>` The stroke-opacity; a constant between 0 and 1, or a channel typically bound to the *opacity* scale.
#' @param stroke_width `<column | literal | transform() | sql()/agg() | list(value=, ...)>` The stroke-width; a constant number in pixels, or a channel.
#' @param target `<string | param()>` The target; a constant string specifying the target window (_e.g._, *_blank*) for clickable links; used in conjunction with the **href** option.
#' @param tension `<number | param()>` The tension option only has an effect on bundle, cardinal and Catmull–Rom splines (*bundle*, *cardinal*, *cardinal-open*, *cardinal-closed*, *catmull-rom*, *catmull-rom-open*, and *catmull-rom-closed*).
#' @param tip `<boolean | "x" | "y" | "xy" | list | param()>` Whether to generate a tooltip for this mark, and any tip options.
#' @param title `<column | literal | transform() | sql()/agg()>` The title; a channel specifying accessible, short textual descriptions as strings (possibly with newlines).
#' @param x `<column | literal | transform() | sql()/agg() | list(value=, ...)>` The horizontal position (or length) channel, typically bound to the *x* scale.
#' @param y `<column | literal | transform() | sql()/agg() | list(value=, ...)>` The vertical position channel, typically bound to the *y* scale; defaults to the zero-based index of the data 0, 1, 2, ….
#' @param z `<column | literal | transform() | sql()/agg()>` An optional ordinal channel for grouping data into (possibly stacked) series to be drawn as separate areas; defaults to **fill** if a channel, or **stroke** if a channel.
#' @param ... Additional options or plot-level attributes.
#' @param data_from The name of the data source this mark reads from (see [vg_data()]); a length-1 nonzero R integer (`1L`, `-1L`, ...; note the `L`) giving a 1-based index into the spec's registered data sources instead, negative counting from the end (e.g., `-1L` for the most recently registered one); or a literal vector of values to use as inline data directly (mosaic-spec's `"data": [...]` shorthand, e.g., for a single reference line -- a bare double like `0`/`c(0)`, or `0L` itself (never a valid index), means this, not an index). Left unset, defaults to the first registered data source (equivalent to `data_from = 1L`) for any mark type that takes data at all.
#' @param filter_by A Param/Selection (e.g., from [param()]) to filter this mark's data by.
#' @param data_optimize A flag (default `TRUE`) to enable mark-specific query optimizations for this mark's data; set `FALSE` to disable them (mosaic-spec's `data: {optimize: false}`).
#' @family mark functions
#' @export
vg_mark_delaunay_link <- function(spec = NULL, formula = vg_unset, aria_description = vg_unset, aria_hidden = vg_unset, aria_label = vg_unset, channels = vg_unset, clip = vg_unset, curve = vg_unset, dx = vg_unset, dy = vg_unset, facet = vg_unset, facet_anchor = vg_unset, fill = vg_unset, fill_opacity = vg_unset, filter = vg_unset, fx = vg_unset, fy = vg_unset, href = vg_unset, image_filter = vg_unset, margin = vg_unset, margin_bottom = vg_unset, margin_left = vg_unset, margin_right = vg_unset, margin_top = vg_unset, marker = vg_unset, marker_end = vg_unset, marker_mid = vg_unset, marker_start = vg_unset, mix_blend_mode = vg_unset, opacity = vg_unset, paint_order = vg_unset, pointer_events = vg_unset, reverse = vg_unset, select = vg_unset, shape_rendering = vg_unset, sort = vg_unset, stroke = vg_unset, stroke_dasharray = vg_unset, stroke_dashoffset = vg_unset, stroke_linecap = vg_unset, stroke_linejoin = vg_unset, stroke_miterlimit = vg_unset, stroke_opacity = vg_unset, stroke_width = vg_unset, target = vg_unset, tension = vg_unset, tip = vg_unset, title = vg_unset, x = vg_unset, y = vg_unset, z = vg_unset, ..., data_from = vg_unset, filter_by = vg_unset, data_optimize = vg_unset) {
  vg_mark_(spec, "delaunayLink", formula = formula, ariaDescription = aria_description, ariaHidden = aria_hidden, ariaLabel = aria_label, channels = channels, clip = clip, curve = curve, dx = dx, dy = dy, facet = facet, facetAnchor = facet_anchor, fill = fill, fillOpacity = fill_opacity, filter = filter, fx = fx, fy = fy, href = href, imageFilter = image_filter, margin = margin, marginBottom = margin_bottom, marginLeft = margin_left, marginRight = margin_right, marginTop = margin_top, marker = marker, markerEnd = marker_end, markerMid = marker_mid, markerStart = marker_start, mixBlendMode = mix_blend_mode, opacity = opacity, paintOrder = paint_order, pointerEvents = pointer_events, reverse = reverse, select = select, shapeRendering = shape_rendering, sort = sort, stroke = stroke, strokeDasharray = stroke_dasharray, strokeDashoffset = stroke_dashoffset, strokeLinecap = stroke_linecap, strokeLinejoin = stroke_linejoin, strokeMiterlimit = stroke_miterlimit, strokeOpacity = stroke_opacity, strokeWidth = stroke_width, target = target, tension = tension, tip = tip, title = title, x = x, y = y, z = z, ..., data_from = data_from, filter_by = filter_by, data_optimize = data_optimize)
}

#' The delaunayMesh mark.
#'
#' See [vg_value_types] for what the `<...>` notation below (`column`, `param()`, ...) means.
#'
#' @param spec A plot fragment or `vgspec` to add this mark to, or `NULL` to start a new plot with just this mark.
#' @param formula A shorthand for this mark's position channels (`x`, `y`, `fx`, `fy`, and paired `x1`/`x2` or `y1`/`y2`), e.g., `Sepal.Length ~ Sepal.Width | ~ Species` for `y = ~Sepal.Length, x = ~Sepal.Width, fx = ~Species`. See [vg_mark()] for the full grammar.
#' @param aria_description `<string | param()>` The aria-description; a constant textual description.
#' @param aria_hidden `<string | param()>` The aria-hidden state; a constant indicating whether the element is exposed to an accessibility API.
#' @param aria_label `<column | literal | transform() | sql()/agg()>` The aria-label; a channel specifying short textual labels representing the value in the accessibility tree.
#' @param channels `<list>` Additional named channels, for example to include in a tooltip.
#' @param clip `<"frame" | "sphere" | boolean | NULL | param()>` How to clip the mark; one of: - *frame* or true - clip to the plot’s frame (inner area) - *sphere* - clip to the projected sphere (*e.g.*, front hemisphere) - null or false - do not clip The *sphere* clip option requires a geographic projection.
#' @param curve `<"basis" | "basis-closed" | "basis-open" | "bundle" | "bump-x" | "bump-y" | "cardinal" | "cardinal-closed" | "cardinal-open" | "catmull-rom" | ... | param()>` The curve (interpolation) method for connecting adjacent points.
#' @param dx `<number | param()>` The horizontal offset in pixels; a constant option.
#' @param dy `<number | param()>` The vertical offset in pixels; a constant option.
#' @param facet `<"auto" | "include" | "exclude" | "super" | boolean | NULL | param()>` Whether to enable or disable faceting; one of: - *auto* (default) - automatically determine if this mark should be faceted - *include* (or true) - draw the subset of the mark’s data in the current facet - *exclude* - draw the subset of the mark’s data *not* in the current facet - *super* - draw this mark in a single frame that covers all facets - null (or false) - repeat this mark’s data across all facets (*i.e.*, no faceting) When a mark uses *super* faceting, it is not allowed to use position scales (*x*, *y*, *fx*, or *fy*); *super* faceting is intended for decorations, such as labels and legends.
#' @param facet_anchor `<"top" | "right" | "bottom" | "left" | "top-left" | "top-right" | "bottom-left" | "bottom-right" | "top-empty" | "right-empty" | "bottom-empty" | "left-empty" | "empty" | NULL | param()>` How to place the mark with respect to facets; one of: - null (default for most marks) - display the mark in each non-empty facet - *top*, *right*, *bottom*, or *left* - display the mark only in facets on the given side - *top-empty*, *right-empty*, *bottom-empty*, or *left-empty* (default for axis marks) - display the mark only in facets that have empty space on the given side: either the margin, or an empty facet - *empty* - display the mark in empty facets only
#' @param fill `<column | literal | transform() | sql()/agg() | list(value=, ...) | param()>` The fill; a constant CSS color string, or a channel typically bound to the *color* scale.
#' @param fill_opacity `<column | literal | transform() | sql()/agg() | list(value=, ...) | param()>` The fill-opacity; a constant number between 0 and 1, or a channel typically bound to the *opacity* scale.
#' @param filter `<column | literal | transform() | sql()/agg()>` Applies a transform to filter the mark’s index according to the given channel values; only truthy values are retained.
#' @param fx `<column | literal | transform() | sql()/agg()>` The horizontal facet position channel, for mark-level faceting, bound to the *fx* scale.
#' @param fy `<column | literal | transform() | sql()/agg()>` The vertical facet position channel, for mark-level faceting, bound to the *fy* scale.
#' @param href `<column | literal | transform() | sql()/agg()>` The href; a channel specifying URLs for clickable links.
#' @param image_filter `<string | param()>` A CSS filter; a constant string used to adjust the rendering of images, such as *blur(5px)*.
#' @param margin `<number | param()>` Shorthand to set the same default for all four mark margins: **marginTop**, **marginRight**, **marginBottom**, and **marginLeft**; typically defaults to 0, except for axis marks.
#' @param margin_bottom `<number | param()>` The mark’s bottom margin; the minimum distance in pixels between the bottom edges of the inner and outer plot area.
#' @param margin_left `<number | param()>` The mark’s left margin; the minimum distance in pixels between the left edges of the inner and outer plot area.
#' @param margin_right `<number | param()>` The mark’s right margin; the minimum distance in pixels between the right edges of the mark’s inner and outer plot area.
#' @param margin_top `<number | param()>` The mark’s top margin; the minimum distance in pixels between the top edges of the inner and outer plot area.
#' @param marker `<"arrow" | "arrow-reverse" | "dot" | "circle" | "circle-fill" | "circle-stroke" | "tick" | "tick-x" | "tick-y" | "none" | boolean | NULL | param()>` Shorthand to set the same default for markerStart, markerMid, and markerEnd; one of: - a marker name such as *arrow* or *circle* - *none* (default) - no marker * true - alias for *circle-fill* * false or null - alias for *none*
#' @param marker_end `<"arrow" | "arrow-reverse" | "dot" | "circle" | "circle-fill" | "circle-stroke" | "tick" | "tick-x" | "tick-y" | "none" | boolean | NULL | param()>` The marker for the ending point of a line segment; one of: - a marker name such as *arrow* or *circle* * *none* (default) - no marker * true - alias for *circle-fill* * false or null - alias for *none*
#' @param marker_mid `<"arrow" | "arrow-reverse" | "dot" | "circle" | "circle-fill" | "circle-stroke" | "tick" | "tick-x" | "tick-y" | "none" | boolean | NULL | param()>` The marker for any middle (interior) points of a line segment.
#' @param marker_start `<"arrow" | "arrow-reverse" | "dot" | "circle" | "circle-fill" | "circle-stroke" | "tick" | "tick-x" | "tick-y" | "none" | boolean | NULL | param()>` The marker for the starting point of a line segment; one of: - a marker name such as *arrow* or *circle* * *none* (default) - no marker * true - alias for *circle-fill* * false or null - alias for *none*
#' @param mix_blend_mode `<string | param()>` The mix-blend-mode; a constant string specifying how to blend content such as *multiply*.
#' @param opacity `<column | literal | transform() | sql()/agg() | list(value=, ...)>` The opacity; a constant between 0 and 1, or a channel typically bound to the *opacity* scale.
#' @param paint_order `<string | param()>` The paint-order; a constant string specifying the order in which the **fill**, **stroke**, and any markers are drawn; defaults to *normal*, which draws the fill, then stroke, then markers; defaults to *stroke* for the text mark to create a “halo” around text to improve legibility.
#' @param pointer_events `<string | param()>` The pointer-events property; a constant string such as *none*.
#' @param reverse `<boolean | param()>` Applies a transform to reverse the order of the mark’s index, say for reverse input order.
#' @param select `<"first" | "last" | "maxX" | "maxY" | "minX" | "minY" | "nearest" | "nearestX" | "nearestY">` Applies a filter transform after data is loaded to highlight selected values only.
#' @param shape_rendering `<string | param()>` The shape-rendering; a constant string such as *crispEdges*.
#' @param sort `<column | literal | transform() | sql()/agg() | list>` Either applies a transform to sort the mark’s index by the specified channel values, or imputes ordinal scale domains from this mark’s channels.
#' @param stroke `<column | literal | transform() | sql()/agg() | list(value=, ...) | param()>` The stroke; a constant CSS color string, or a channel typically bound to the *color* scale.
#' @param stroke_dasharray `<string | number | param()>` The stroke-dasharray; a constant number indicating the length in pixels of alternating dashes and gaps, or a constant string of numbers separated by spaces or commas (_e.g._, *10 2* for dashes of 10 pixels separated by gaps of 2 pixels), or *none* (the default) for no dashing : https://developer.mozilla.org/en-US/docs/Web/SVG/Attribute/stroke-dasharray
#' @param stroke_dashoffset `<string | number | param()>` The stroke-dashoffset; a constant indicating the offset in pixels of the first dash along the stroke; defaults to zero.
#' @param stroke_linecap `<string | param()>` The stroke-linecap; a constant specifying how to cap stroked paths, such as *butt*, *round*, or *square*.
#' @param stroke_linejoin `<string | param()>` The stroke-linejoin; a constant specifying how to join stroked paths, such as *bevel*, *miter*, *miter-clip*, or *round*.
#' @param stroke_miterlimit `<number | param()>` The stroke-miterlimit; a constant number specifying how to limit the length of *miter* joins on stroked paths.
#' @param stroke_opacity `<column | literal | transform() | sql()/agg() | list(value=, ...)>` The stroke-opacity; a constant between 0 and 1, or a channel typically bound to the *opacity* scale.
#' @param stroke_width `<column | literal | transform() | sql()/agg() | list(value=, ...)>` The stroke-width; a constant number in pixels, or a channel.
#' @param target `<string | param()>` The target; a constant string specifying the target window (_e.g._, *_blank*) for clickable links; used in conjunction with the **href** option.
#' @param tension `<number | param()>` The tension option only has an effect on bundle, cardinal and Catmull–Rom splines (*bundle*, *cardinal*, *cardinal-open*, *cardinal-closed*, *catmull-rom*, *catmull-rom-open*, and *catmull-rom-closed*).
#' @param tip `<boolean | "x" | "y" | "xy" | list | param()>` Whether to generate a tooltip for this mark, and any tip options.
#' @param title `<column | literal | transform() | sql()/agg()>` The title; a channel specifying accessible, short textual descriptions as strings (possibly with newlines).
#' @param x `<column | literal | transform() | sql()/agg() | list(value=, ...)>` The horizontal position (or length) channel, typically bound to the *x* scale.
#' @param y `<column | literal | transform() | sql()/agg() | list(value=, ...)>` The vertical position channel, typically bound to the *y* scale; defaults to the zero-based index of the data 0, 1, 2, ….
#' @param z `<column | literal | transform() | sql()/agg()>` An optional ordinal channel for grouping data into (possibly stacked) series to be drawn as separate areas; defaults to **fill** if a channel, or **stroke** if a channel.
#' @param ... Additional options or plot-level attributes.
#' @param data_from The name of the data source this mark reads from (see [vg_data()]); a length-1 nonzero R integer (`1L`, `-1L`, ...; note the `L`) giving a 1-based index into the spec's registered data sources instead, negative counting from the end (e.g., `-1L` for the most recently registered one); or a literal vector of values to use as inline data directly (mosaic-spec's `"data": [...]` shorthand, e.g., for a single reference line -- a bare double like `0`/`c(0)`, or `0L` itself (never a valid index), means this, not an index). Left unset, defaults to the first registered data source (equivalent to `data_from = 1L`) for any mark type that takes data at all.
#' @param filter_by A Param/Selection (e.g., from [param()]) to filter this mark's data by.
#' @param data_optimize A flag (default `TRUE`) to enable mark-specific query optimizations for this mark's data; set `FALSE` to disable them (mosaic-spec's `data: {optimize: false}`).
#' @family mark functions
#' @export
vg_mark_delaunay_mesh <- function(spec = NULL, formula = vg_unset, aria_description = vg_unset, aria_hidden = vg_unset, aria_label = vg_unset, channels = vg_unset, clip = vg_unset, curve = vg_unset, dx = vg_unset, dy = vg_unset, facet = vg_unset, facet_anchor = vg_unset, fill = vg_unset, fill_opacity = vg_unset, filter = vg_unset, fx = vg_unset, fy = vg_unset, href = vg_unset, image_filter = vg_unset, margin = vg_unset, margin_bottom = vg_unset, margin_left = vg_unset, margin_right = vg_unset, margin_top = vg_unset, marker = vg_unset, marker_end = vg_unset, marker_mid = vg_unset, marker_start = vg_unset, mix_blend_mode = vg_unset, opacity = vg_unset, paint_order = vg_unset, pointer_events = vg_unset, reverse = vg_unset, select = vg_unset, shape_rendering = vg_unset, sort = vg_unset, stroke = vg_unset, stroke_dasharray = vg_unset, stroke_dashoffset = vg_unset, stroke_linecap = vg_unset, stroke_linejoin = vg_unset, stroke_miterlimit = vg_unset, stroke_opacity = vg_unset, stroke_width = vg_unset, target = vg_unset, tension = vg_unset, tip = vg_unset, title = vg_unset, x = vg_unset, y = vg_unset, z = vg_unset, ..., data_from = vg_unset, filter_by = vg_unset, data_optimize = vg_unset) {
  vg_mark_(spec, "delaunayMesh", formula = formula, ariaDescription = aria_description, ariaHidden = aria_hidden, ariaLabel = aria_label, channels = channels, clip = clip, curve = curve, dx = dx, dy = dy, facet = facet, facetAnchor = facet_anchor, fill = fill, fillOpacity = fill_opacity, filter = filter, fx = fx, fy = fy, href = href, imageFilter = image_filter, margin = margin, marginBottom = margin_bottom, marginLeft = margin_left, marginRight = margin_right, marginTop = margin_top, marker = marker, markerEnd = marker_end, markerMid = marker_mid, markerStart = marker_start, mixBlendMode = mix_blend_mode, opacity = opacity, paintOrder = paint_order, pointerEvents = pointer_events, reverse = reverse, select = select, shapeRendering = shape_rendering, sort = sort, stroke = stroke, strokeDasharray = stroke_dasharray, strokeDashoffset = stroke_dashoffset, strokeLinecap = stroke_linecap, strokeLinejoin = stroke_linejoin, strokeMiterlimit = stroke_miterlimit, strokeOpacity = stroke_opacity, strokeWidth = stroke_width, target = target, tension = tension, tip = tip, title = title, x = x, y = y, z = z, ..., data_from = data_from, filter_by = filter_by, data_optimize = data_optimize)
}

#' The denseLine mark.
#'
#' See [vg_value_types] for what the `<...>` notation below (`column`, `param()`, ...) means.
#'
#' @param spec A plot fragment or `vgspec` to add this mark to, or `NULL` to start a new plot with just this mark.
#' @param formula A shorthand for this mark's position channels (`x`, `y`, `fx`, `fy`, and paired `x1`/`x2` or `y1`/`y2`), e.g., `Sepal.Length ~ Sepal.Width | ~ Species` for `y = ~Sepal.Length, x = ~Sepal.Width, fx = ~Species`. See [vg_mark()] for the full grammar.
#' @param aria_description `<string | param()>` The aria-description; a constant textual description.
#' @param aria_hidden `<string | param()>` The aria-hidden state; a constant indicating whether the element is exposed to an accessibility API.
#' @param aria_label `<column | literal | transform() | sql()/agg()>` The aria-label; a channel specifying short textual labels representing the value in the accessibility tree.
#' @param bandwidth `<number | param()>` The kernel density bandwidth for smoothing, in pixels.
#' @param channels `<list>` Additional named channels, for example to include in a tooltip.
#' @param clip `<"frame" | "sphere" | boolean | NULL | param()>` How to clip the mark; one of: - *frame* or true - clip to the plot’s frame (inner area) - *sphere* - clip to the projected sphere (*e.g.*, front hemisphere) - null or false - do not clip The *sphere* clip option requires a geographic projection.
#' @param dx `<number | param()>` The horizontal offset in pixels; a constant option.
#' @param dy `<number | param()>` The vertical offset in pixels; a constant option.
#' @param facet `<"auto" | "include" | "exclude" | "super" | boolean | NULL | param()>` Whether to enable or disable faceting; one of: - *auto* (default) - automatically determine if this mark should be faceted - *include* (or true) - draw the subset of the mark’s data in the current facet - *exclude* - draw the subset of the mark’s data *not* in the current facet - *super* - draw this mark in a single frame that covers all facets - null (or false) - repeat this mark’s data across all facets (*i.e.*, no faceting) When a mark uses *super* faceting, it is not allowed to use position scales (*x*, *y*, *fx*, or *fy*); *super* faceting is intended for decorations, such as labels and legends.
#' @param facet_anchor `<"top" | "right" | "bottom" | "left" | "top-left" | "top-right" | "bottom-left" | "bottom-right" | "top-empty" | "right-empty" | "bottom-empty" | "left-empty" | "empty" | NULL | param()>` How to place the mark with respect to facets; one of: - null (default for most marks) - display the mark in each non-empty facet - *top*, *right*, *bottom*, or *left* - display the mark only in facets on the given side - *top-empty*, *right-empty*, *bottom-empty*, or *left-empty* (default for axis marks) - display the mark only in facets that have empty space on the given side: either the margin, or an empty facet - *empty* - display the mark in empty facets only
#' @param fill `<column | literal | transform() | sql()/agg() | list(value=, ...) | param()>` The fill; a constant CSS color string, or a channel typically bound to the *color* scale.
#' @param fill_opacity `<column | literal | transform() | sql()/agg() | list(value=, ...) | param()>` The fill-opacity; a constant number between 0 and 1, or a channel typically bound to the *opacity* scale.
#' @param filter `<column | literal | transform() | sql()/agg()>` Applies a transform to filter the mark’s index according to the given channel values; only truthy values are retained.
#' @param fx `<column | literal | transform() | sql()/agg()>` The horizontal facet position channel, for mark-level faceting, bound to the *fx* scale.
#' @param fy `<column | literal | transform() | sql()/agg()>` The vertical facet position channel, for mark-level faceting, bound to the *fy* scale.
#' @param height `<number | param()>` The height (number of rows) of the grid, in actual pixels.
#' @param href `<column | literal | transform() | sql()/agg()>` The href; a channel specifying URLs for clickable links.
#' @param image_filter `<string | param()>` A CSS filter; a constant string used to adjust the rendering of images, such as *blur(5px)*.
#' @param image_rendering `<string | param()>` The image-rendering attribute; defaults to *auto* (bilinear).
#' @param interpolate `<"none" | "linear" | "nearest" | "barycentric" | "random-walk" | NULL | param()>` The spatial interpolation method; one of: - *none* - do not perform interpolation (the default), maps samples to single bins - *linear* - apply proportional linear interpolation across adjacent bins - *nearest* - assign each pixel to the closest sample’s value (Voronoi diagram) - *barycentric* - apply barycentric interpolation over the Delaunay triangulation - *random-walk* - apply a random walk from each pixel, stopping when near a sample
#' @param margin `<number | param()>` Shorthand to set the same default for all four mark margins: **marginTop**, **marginRight**, **marginBottom**, and **marginLeft**; typically defaults to 0, except for axis marks.
#' @param margin_bottom `<number | param()>` The mark’s bottom margin; the minimum distance in pixels between the bottom edges of the inner and outer plot area.
#' @param margin_left `<number | param()>` The mark’s left margin; the minimum distance in pixels between the left edges of the inner and outer plot area.
#' @param margin_right `<number | param()>` The mark’s right margin; the minimum distance in pixels between the right edges of the mark’s inner and outer plot area.
#' @param margin_top `<number | param()>` The mark’s top margin; the minimum distance in pixels between the top edges of the inner and outer plot area.
#' @param mix_blend_mode `<string | param()>` The mix-blend-mode; a constant string specifying how to blend content such as *multiply*.
#' @param normalize `<boolean | param()>` Flag to perform approximate arc length normalization of line segments to prevent artifacts due to overcounting steep lines.
#' @param opacity `<column | literal | transform() | sql()/agg() | list(value=, ...)>` The opacity; a constant between 0 and 1, or a channel typically bound to the *opacity* scale.
#' @param pad `<number | param()>` The bin padding, one of 1 (default) to include extra padding for the final bin, or 0 to make the bins flush with the maximum domain value.
#' @param paint_order `<string | param()>` The paint-order; a constant string specifying the order in which the **fill**, **stroke**, and any markers are drawn; defaults to *normal*, which draws the fill, then stroke, then markers; defaults to *stroke* for the text mark to create a “halo” around text to improve legibility.
#' @param pixel_size `<number | param()>` The effective screen size of a raster pixel, used to determine the height and width of the raster from the frame’s dimensions; defaults to 1.
#' @param pointer_events `<string | param()>` The pointer-events property; a constant string such as *none*.
#' @param reverse `<boolean | param()>` Applies a transform to reverse the order of the mark’s index, say for reverse input order.
#' @param select `<"first" | "last" | "maxX" | "maxY" | "minX" | "minY" | "nearest" | "nearestX" | "nearestY">` Applies a filter transform after data is loaded to highlight selected values only.
#' @param shape_rendering `<string | param()>` The shape-rendering; a constant string such as *crispEdges*.
#' @param sort `<column | literal | transform() | sql()/agg() | list>` Either applies a transform to sort the mark’s index by the specified channel values, or imputes ordinal scale domains from this mark’s channels.
#' @param stroke `<column | literal | transform() | sql()/agg() | list(value=, ...) | param()>` The stroke; a constant CSS color string, or a channel typically bound to the *color* scale.
#' @param stroke_dasharray `<string | number | param()>` The stroke-dasharray; a constant number indicating the length in pixels of alternating dashes and gaps, or a constant string of numbers separated by spaces or commas (_e.g._, *10 2* for dashes of 10 pixels separated by gaps of 2 pixels), or *none* (the default) for no dashing : https://developer.mozilla.org/en-US/docs/Web/SVG/Attribute/stroke-dasharray
#' @param stroke_dashoffset `<string | number | param()>` The stroke-dashoffset; a constant indicating the offset in pixels of the first dash along the stroke; defaults to zero.
#' @param stroke_linecap `<string | param()>` The stroke-linecap; a constant specifying how to cap stroked paths, such as *butt*, *round*, or *square*.
#' @param stroke_linejoin `<string | param()>` The stroke-linejoin; a constant specifying how to join stroked paths, such as *bevel*, *miter*, *miter-clip*, or *round*.
#' @param stroke_miterlimit `<number | param()>` The stroke-miterlimit; a constant number specifying how to limit the length of *miter* joins on stroked paths.
#' @param stroke_opacity `<column | literal | transform() | sql()/agg() | list(value=, ...)>` The stroke-opacity; a constant between 0 and 1, or a channel typically bound to the *opacity* scale.
#' @param stroke_width `<column | literal | transform() | sql()/agg() | list(value=, ...)>` The stroke-width; a constant number in pixels, or a channel.
#' @param target `<string | param()>` The target; a constant string specifying the target window (_e.g._, *_blank*) for clickable links; used in conjunction with the **href** option.
#' @param tip `<boolean | "x" | "y" | "xy" | list | param()>` Whether to generate a tooltip for this mark, and any tip options.
#' @param title `<column | literal | transform() | sql()/agg()>` The title; a channel specifying accessible, short textual descriptions as strings (possibly with newlines).
#' @param width `<number | param()>` The width (number of columns) of the grid, in actual pixels.
#' @param x `<column | literal | transform() | sql()/agg() | list(value=, ...)>` The horizontal position (or length) channel, typically bound to the *x* scale.
#' @param y `<column | literal | transform() | sql()/agg() | list(value=, ...)>` The vertical position channel, typically bound to the *y* scale; defaults to the zero-based index of the data 0, 1, 2, ….
#' @param z `<column | literal | transform() | sql()/agg()>` An optional ordinal channel for grouping data into (possibly stacked) series to be drawn as separate areas; defaults to **fill** if a channel, or **stroke** if a channel.
#' @param ... Additional options or plot-level attributes.
#' @param data_from The name of the data source this mark reads from (see [vg_data()]); a length-1 nonzero R integer (`1L`, `-1L`, ...; note the `L`) giving a 1-based index into the spec's registered data sources instead, negative counting from the end (e.g., `-1L` for the most recently registered one); or a literal vector of values to use as inline data directly (mosaic-spec's `"data": [...]` shorthand, e.g., for a single reference line -- a bare double like `0`/`c(0)`, or `0L` itself (never a valid index), means this, not an index). Left unset, defaults to the first registered data source (equivalent to `data_from = 1L`) for any mark type that takes data at all.
#' @param filter_by A Param/Selection (e.g., from [param()]) to filter this mark's data by.
#' @param data_optimize A flag (default `TRUE`) to enable mark-specific query optimizations for this mark's data; set `FALSE` to disable them (mosaic-spec's `data: {optimize: false}`).
#' @family mark functions
#' @export
vg_mark_dense_line <- function(spec = NULL, formula = vg_unset, aria_description = vg_unset, aria_hidden = vg_unset, aria_label = vg_unset, bandwidth = vg_unset, channels = vg_unset, clip = vg_unset, dx = vg_unset, dy = vg_unset, facet = vg_unset, facet_anchor = vg_unset, fill = vg_unset, fill_opacity = vg_unset, filter = vg_unset, fx = vg_unset, fy = vg_unset, height = vg_unset, href = vg_unset, image_filter = vg_unset, image_rendering = vg_unset, interpolate = vg_unset, margin = vg_unset, margin_bottom = vg_unset, margin_left = vg_unset, margin_right = vg_unset, margin_top = vg_unset, mix_blend_mode = vg_unset, normalize = vg_unset, opacity = vg_unset, pad = vg_unset, paint_order = vg_unset, pixel_size = vg_unset, pointer_events = vg_unset, reverse = vg_unset, select = vg_unset, shape_rendering = vg_unset, sort = vg_unset, stroke = vg_unset, stroke_dasharray = vg_unset, stroke_dashoffset = vg_unset, stroke_linecap = vg_unset, stroke_linejoin = vg_unset, stroke_miterlimit = vg_unset, stroke_opacity = vg_unset, stroke_width = vg_unset, target = vg_unset, tip = vg_unset, title = vg_unset, width = vg_unset, x = vg_unset, y = vg_unset, z = vg_unset, ..., data_from = vg_unset, filter_by = vg_unset, data_optimize = vg_unset) {
  vg_mark_(spec, "denseLine", formula = formula, ariaDescription = aria_description, ariaHidden = aria_hidden, ariaLabel = aria_label, bandwidth = bandwidth, channels = channels, clip = clip, dx = dx, dy = dy, facet = facet, facetAnchor = facet_anchor, fill = fill, fillOpacity = fill_opacity, filter = filter, fx = fx, fy = fy, height = height, href = href, imageFilter = image_filter, imageRendering = image_rendering, interpolate = interpolate, margin = margin, marginBottom = margin_bottom, marginLeft = margin_left, marginRight = margin_right, marginTop = margin_top, mixBlendMode = mix_blend_mode, normalize = normalize, opacity = opacity, pad = pad, paintOrder = paint_order, pixelSize = pixel_size, pointerEvents = pointer_events, reverse = reverse, select = select, shapeRendering = shape_rendering, sort = sort, stroke = stroke, strokeDasharray = stroke_dasharray, strokeDashoffset = stroke_dashoffset, strokeLinecap = stroke_linecap, strokeLinejoin = stroke_linejoin, strokeMiterlimit = stroke_miterlimit, strokeOpacity = stroke_opacity, strokeWidth = stroke_width, target = target, tip = tip, title = title, width = width, x = x, y = y, z = z, ..., data_from = data_from, filter_by = filter_by, data_optimize = data_optimize)
}

#' The density mark for 2D densities.
#'
#' See [vg_value_types] for what the `<...>` notation below (`column`, `param()`, ...) means.
#'
#' @param spec A plot fragment or `vgspec` to add this mark to, or `NULL` to start a new plot with just this mark.
#' @param formula A shorthand for this mark's position channels (`x`, `y`, `fx`, `fy`, and paired `x1`/`x2` or `y1`/`y2`), e.g., `Sepal.Length ~ Sepal.Width | ~ Species` for `y = ~Sepal.Length, x = ~Sepal.Width, fx = ~Species`. See [vg_mark()] for the full grammar.
#' @param aria_description `<string | param()>` The aria-description; a constant textual description.
#' @param aria_hidden `<string | param()>` The aria-hidden state; a constant indicating whether the element is exposed to an accessibility API.
#' @param aria_label `<column | literal | transform() | sql()/agg()>` The aria-label; a channel specifying short textual labels representing the value in the accessibility tree.
#' @param bandwidth `<number | param()>` The kernel density bandwidth for smoothing, in pixels.
#' @param channels `<list>` Additional named channels, for example to include in a tooltip.
#' @param clip `<"frame" | "sphere" | boolean | NULL | param()>` How to clip the mark; one of: - *frame* or true - clip to the plot’s frame (inner area) - *sphere* - clip to the projected sphere (*e.g.*, front hemisphere) - null or false - do not clip The *sphere* clip option requires a geographic projection.
#' @param dx `<number | param()>` The horizontal offset in pixels; a constant option.
#' @param dy `<number | param()>` The vertical offset in pixels; a constant option.
#' @param facet `<"auto" | "include" | "exclude" | "super" | boolean | NULL | param()>` Whether to enable or disable faceting; one of: - *auto* (default) - automatically determine if this mark should be faceted - *include* (or true) - draw the subset of the mark’s data in the current facet - *exclude* - draw the subset of the mark’s data *not* in the current facet - *super* - draw this mark in a single frame that covers all facets - null (or false) - repeat this mark’s data across all facets (*i.e.*, no faceting) When a mark uses *super* faceting, it is not allowed to use position scales (*x*, *y*, *fx*, or *fy*); *super* faceting is intended for decorations, such as labels and legends.
#' @param facet_anchor `<"top" | "right" | "bottom" | "left" | "top-left" | "top-right" | "bottom-left" | "bottom-right" | "top-empty" | "right-empty" | "bottom-empty" | "left-empty" | "empty" | NULL | param()>` How to place the mark with respect to facets; one of: - null (default for most marks) - display the mark in each non-empty facet - *top*, *right*, *bottom*, or *left* - display the mark only in facets on the given side - *top-empty*, *right-empty*, *bottom-empty*, or *left-empty* (default for axis marks) - display the mark only in facets that have empty space on the given side: either the margin, or an empty facet - *empty* - display the mark in empty facets only
#' @param fill `<column | literal | transform() | sql()/agg() | list(value=, ...) | param()>` The fill; a constant CSS color string, or a channel typically bound to the *color* scale.
#' @param fill_opacity `<column | literal | transform() | sql()/agg() | list(value=, ...) | param()>` The fill-opacity; a constant number between 0 and 1, or a channel typically bound to the *opacity* scale.
#' @param filter `<column | literal | transform() | sql()/agg()>` Applies a transform to filter the mark’s index according to the given channel values; only truthy values are retained.
#' @param font_family `<string | param()>` The font-family; a constant; defaults to the plot’s font family, which is typically *system-ui*.
#' @param font_size `<column | literal | transform() | sql()/agg() | param()>` The font size in pixels; either a constant or a channel; defaults to the plot’s font size, which is typically 10.
#' @param font_style `<string | param()>` The font style; a constant; defaults to the plot’s font style, which is typically *normal*.
#' @param font_variant `<string | param()>` The font variant; a constant; if the **text** channel contains numbers or dates, defaults to *tabular-nums* to facilitate comparing numbers; otherwise defaults to the plot’s font style, which is typically *normal*.
#' @param font_weight `<string | number | param()>` The font weight; a constant; defaults to the plot’s font weight, which is typically *normal*.
#' @param frame_anchor `<"middle" | "top-left" | "top" | "top-right" | "right" | "bottom-right" | "bottom" | "bottom-left" | "left" | param()>` The frame anchor specifies defaults for **x** and **y**, along with **textAnchor** and **lineAnchor**, based on the plot’s frame; it may be one of the four sides (*top*, *right*, *bottom*, *left*), one of the four corners (*top-left*, *top-right*, *bottom-right*, *bottom-left*), or the *middle* of the frame.
#' @param fx `<column | literal | transform() | sql()/agg()>` The horizontal facet position channel, for mark-level faceting, bound to the *fx* scale.
#' @param fy `<column | literal | transform() | sql()/agg()>` The vertical facet position channel, for mark-level faceting, bound to the *fy* scale.
#' @param height `<number | param()>` The height (number of rows) of the grid, in actual pixels.
#' @param href `<column | literal | transform() | sql()/agg()>` The href; a channel specifying URLs for clickable links.
#' @param image_filter `<string | param()>` A CSS filter; a constant string used to adjust the rendering of images, such as *blur(5px)*.
#' @param interpolate `<"none" | "linear" | "nearest" | "barycentric" | "random-walk" | NULL | param()>` The spatial interpolation method; one of: - *none* - do not perform interpolation (the default), maps samples to single bins - *linear* - apply proportional linear interpolation across adjacent bins - *nearest* - assign each pixel to the closest sample’s value (Voronoi diagram) - *barycentric* - apply barycentric interpolation over the Delaunay triangulation - *random-walk* - apply a random walk from each pixel, stopping when near a sample
#' @param line_height `<number | param()>` The line height in ems; defaults to 1.
#' @param line_width `<number | param()>` The line width in ems (e.g., 10 for about 20 characters); defaults to infinity, disabling wrapping and clipping.
#' @param margin `<number | param()>` Shorthand to set the same default for all four mark margins: **marginTop**, **marginRight**, **marginBottom**, and **marginLeft**; typically defaults to 0, except for axis marks.
#' @param margin_bottom `<number | param()>` The mark’s bottom margin; the minimum distance in pixels between the bottom edges of the inner and outer plot area.
#' @param margin_left `<number | param()>` The mark’s left margin; the minimum distance in pixels between the left edges of the inner and outer plot area.
#' @param margin_right `<number | param()>` The mark’s right margin; the minimum distance in pixels between the right edges of the mark’s inner and outer plot area.
#' @param margin_top `<number | param()>` The mark’s top margin; the minimum distance in pixels between the top edges of the inner and outer plot area.
#' @param mix_blend_mode `<string | param()>` The mix-blend-mode; a constant string specifying how to blend content such as *multiply*.
#' @param monospace `<boolean | param()>` If true, changes the default **fontFamily** to *monospace*, and uses simplified monospaced text metrics calculations.
#' @param opacity `<column | literal | transform() | sql()/agg() | list(value=, ...)>` The opacity; a constant between 0 and 1, or a channel typically bound to the *opacity* scale.
#' @param pad `<number | param()>` The bin padding, one of 1 (default) to include extra padding for the final bin, or 0 to make the bins flush with the maximum domain value.
#' @param paint_order `<string | param()>` The paint-order; a constant string specifying the order in which the **fill**, **stroke**, and any markers are drawn; defaults to *normal*, which draws the fill, then stroke, then markers; defaults to *stroke* for the text mark to create a “halo” around text to improve legibility.
#' @param pixel_size `<number | param()>` The effective screen size of a raster pixel, used to determine the height and width of the raster from the frame’s dimensions; defaults to 1.
#' @param pointer_events `<string | param()>` The pointer-events property; a constant string such as *none*.
#' @param r `<column | literal | transform() | sql()/agg() | list(value=, ...) | number | param()>` The radius of dots; either a channel or constant.
#' @param reverse `<boolean | param()>` Applies a transform to reverse the order of the mark’s index, say for reverse input order.
#' @param rotate `<column | literal | transform() | sql()/agg() | param()>` The rotation angle in degrees clockwise; a constant or a channel; defaults to 0°.
#' @param select `<"first" | "last" | "maxX" | "maxY" | "minX" | "minY" | "nearest" | "nearestX" | "nearestY">` Applies a filter transform after data is loaded to highlight selected values only.
#' @param shape_rendering `<string | param()>` The shape-rendering; a constant string such as *crispEdges*.
#' @param sort `<column | literal | transform() | sql()/agg() | list>` Either applies a transform to sort the mark’s index by the specified channel values, or imputes ordinal scale domains from this mark’s channels.
#' @param stroke `<column | literal | transform() | sql()/agg() | list(value=, ...) | param()>` The stroke; a constant CSS color string, or a channel typically bound to the *color* scale.
#' @param stroke_dasharray `<string | number | param()>` The stroke-dasharray; a constant number indicating the length in pixels of alternating dashes and gaps, or a constant string of numbers separated by spaces or commas (_e.g._, *10 2* for dashes of 10 pixels separated by gaps of 2 pixels), or *none* (the default) for no dashing : https://developer.mozilla.org/en-US/docs/Web/SVG/Attribute/stroke-dasharray
#' @param stroke_dashoffset `<string | number | param()>` The stroke-dashoffset; a constant indicating the offset in pixels of the first dash along the stroke; defaults to zero.
#' @param stroke_linecap `<string | param()>` The stroke-linecap; a constant specifying how to cap stroked paths, such as *butt*, *round*, or *square*.
#' @param stroke_linejoin `<string | param()>` The stroke-linejoin; a constant specifying how to join stroked paths, such as *bevel*, *miter*, *miter-clip*, or *round*.
#' @param stroke_miterlimit `<number | param()>` The stroke-miterlimit; a constant number specifying how to limit the length of *miter* joins on stroked paths.
#' @param stroke_opacity `<column | literal | transform() | sql()/agg() | list(value=, ...)>` The stroke-opacity; a constant between 0 and 1, or a channel typically bound to the *opacity* scale.
#' @param stroke_width `<column | literal | transform() | sql()/agg() | list(value=, ...)>` The stroke-width; a constant number in pixels, or a channel.
#' @param symbol `<column | literal | transform() | sql()/agg() | list(value=, ...) | "asterisk" | "circle" | "cross" | "diamond" | "diamond2" | "hexagon" | "plus" | "square" | "square2" | "star" | ... | param()>` The categorical symbol; either a channel or a constant.
#' @param target `<string | param()>` The target; a constant string specifying the target window (_e.g._, *_blank*) for clickable links; used in conjunction with the **href** option.
#' @param text_anchor `<"start" | "middle" | "end" | param()>` The text anchor controls how text is aligned (typically horizontally) relative to its anchor point; it is one of *start*, *end*, or *middle*.
#' @param text_overflow `<NULL | "clip" | "ellipsis" | "clip-start" | "clip-end" | "ellipsis-start" | "ellipsis-middle" | "ellipsis-end" | param()>` How truncate (or wrap) lines of text longer than the given **lineWidth**; one of: - null (default) - preserve overflowing characters (and wrap if needed) - *clip* or *clip-end* - remove characters from the end - *clip-start* - remove characters from the start - *ellipsis* or *ellipsis-end* - replace characters from the end with an ellipsis (…) - *ellipsis-start* - replace characters from the start with an ellipsis (…) - *ellipsis-middle* - replace characters from the middle with an ellipsis (…) If no **title** was specified, if text requires truncation, a title containing the non-truncated text will be implicitly added.
#' @param tip `<boolean | "x" | "y" | "xy" | list | param()>` Whether to generate a tooltip for this mark, and any tip options.
#' @param title `<column | literal | transform() | sql()/agg()>` The title; a channel specifying accessible, short textual descriptions as strings (possibly with newlines).
#' @param type `<"dot" | "circle" | "hexagon" | "cell" | "text" | param()>` The basic mark type to use to render 2D density values.
#' @param width `<number | param()>` The width (number of columns) of the grid, in actual pixels.
#' @param x `<column | literal | transform() | sql()/agg() | list(value=, ...)>` The horizontal position (or length) channel, typically bound to the *x* scale.
#' @param y `<column | literal | transform() | sql()/agg() | list(value=, ...)>` The vertical position channel, typically bound to the *y* scale; defaults to the zero-based index of the data 0, 1, 2, ….
#' @param z `<column | literal | transform() | sql()/agg()>` An optional ordinal channel for grouping data into (possibly stacked) series to be drawn as separate areas; defaults to **fill** if a channel, or **stroke** if a channel.
#' @param ... Additional options or plot-level attributes.
#' @param data_from The name of the data source this mark reads from (see [vg_data()]); a length-1 nonzero R integer (`1L`, `-1L`, ...; note the `L`) giving a 1-based index into the spec's registered data sources instead, negative counting from the end (e.g., `-1L` for the most recently registered one); or a literal vector of values to use as inline data directly (mosaic-spec's `"data": [...]` shorthand, e.g., for a single reference line -- a bare double like `0`/`c(0)`, or `0L` itself (never a valid index), means this, not an index). Left unset, defaults to the first registered data source (equivalent to `data_from = 1L`) for any mark type that takes data at all.
#' @param filter_by A Param/Selection (e.g., from [param()]) to filter this mark's data by.
#' @param data_optimize A flag (default `TRUE`) to enable mark-specific query optimizations for this mark's data; set `FALSE` to disable them (mosaic-spec's `data: {optimize: false}`).
#' @family mark functions
#' @export
vg_mark_density <- function(spec = NULL, formula = vg_unset, aria_description = vg_unset, aria_hidden = vg_unset, aria_label = vg_unset, bandwidth = vg_unset, channels = vg_unset, clip = vg_unset, dx = vg_unset, dy = vg_unset, facet = vg_unset, facet_anchor = vg_unset, fill = vg_unset, fill_opacity = vg_unset, filter = vg_unset, font_family = vg_unset, font_size = vg_unset, font_style = vg_unset, font_variant = vg_unset, font_weight = vg_unset, frame_anchor = vg_unset, fx = vg_unset, fy = vg_unset, height = vg_unset, href = vg_unset, image_filter = vg_unset, interpolate = vg_unset, line_height = vg_unset, line_width = vg_unset, margin = vg_unset, margin_bottom = vg_unset, margin_left = vg_unset, margin_right = vg_unset, margin_top = vg_unset, mix_blend_mode = vg_unset, monospace = vg_unset, opacity = vg_unset, pad = vg_unset, paint_order = vg_unset, pixel_size = vg_unset, pointer_events = vg_unset, r = vg_unset, reverse = vg_unset, rotate = vg_unset, select = vg_unset, shape_rendering = vg_unset, sort = vg_unset, stroke = vg_unset, stroke_dasharray = vg_unset, stroke_dashoffset = vg_unset, stroke_linecap = vg_unset, stroke_linejoin = vg_unset, stroke_miterlimit = vg_unset, stroke_opacity = vg_unset, stroke_width = vg_unset, symbol = vg_unset, target = vg_unset, text_anchor = vg_unset, text_overflow = vg_unset, tip = vg_unset, title = vg_unset, type = vg_unset, width = vg_unset, x = vg_unset, y = vg_unset, z = vg_unset, ..., data_from = vg_unset, filter_by = vg_unset, data_optimize = vg_unset) {
  vg_mark_(spec, "density", formula = formula, ariaDescription = aria_description, ariaHidden = aria_hidden, ariaLabel = aria_label, bandwidth = bandwidth, channels = channels, clip = clip, dx = dx, dy = dy, facet = facet, facetAnchor = facet_anchor, fill = fill, fillOpacity = fill_opacity, filter = filter, fontFamily = font_family, fontSize = font_size, fontStyle = font_style, fontVariant = font_variant, fontWeight = font_weight, frameAnchor = frame_anchor, fx = fx, fy = fy, height = height, href = href, imageFilter = image_filter, interpolate = interpolate, lineHeight = line_height, lineWidth = line_width, margin = margin, marginBottom = margin_bottom, marginLeft = margin_left, marginRight = margin_right, marginTop = margin_top, mixBlendMode = mix_blend_mode, monospace = monospace, opacity = opacity, pad = pad, paintOrder = paint_order, pixelSize = pixel_size, pointerEvents = pointer_events, r = r, reverse = reverse, rotate = rotate, select = select, shapeRendering = shape_rendering, sort = sort, stroke = stroke, strokeDasharray = stroke_dasharray, strokeDashoffset = stroke_dashoffset, strokeLinecap = stroke_linecap, strokeLinejoin = stroke_linejoin, strokeMiterlimit = stroke_miterlimit, strokeOpacity = stroke_opacity, strokeWidth = stroke_width, symbol = symbol, target = target, textAnchor = text_anchor, textOverflow = text_overflow, tip = tip, title = title, type = type, width = width, x = x, y = y, z = z, ..., data_from = data_from, filter_by = filter_by, data_optimize = data_optimize)
}

#' The densityX mark.
#'
#' See [vg_value_types] for what the `<...>` notation below (`column`, `param()`, ...) means.
#'
#' @param spec A plot fragment or `vgspec` to add this mark to, or `NULL` to start a new plot with just this mark.
#' @param formula A shorthand for this mark's position channels (`x`, `y`, `fx`, `fy`, and paired `x1`/`x2` or `y1`/`y2`), e.g., `Sepal.Length ~ Sepal.Width | ~ Species` for `y = ~Sepal.Length, x = ~Sepal.Width, fx = ~Species`. See [vg_mark()] for the full grammar.
#' @param aria_description `<string | param()>` The aria-description; a constant textual description.
#' @param aria_hidden `<string | param()>` The aria-hidden state; a constant indicating whether the element is exposed to an accessibility API.
#' @param aria_label `<column | literal | transform() | sql()/agg()>` The aria-label; a channel specifying short textual labels representing the value in the accessibility tree.
#' @param bandwidth `<number | param()>` The kernel density bandwidth for smoothing, in pixels.
#' @param bins `<number | param()>` The number of bins over which to discretize the data prior to smoothing.
#' @param channels `<list>` Additional named channels, for example to include in a tooltip.
#' @param clip `<"frame" | "sphere" | boolean | NULL | param()>` How to clip the mark; one of: - *frame* or true - clip to the plot’s frame (inner area) - *sphere* - clip to the projected sphere (*e.g.*, front hemisphere) - null or false - do not clip The *sphere* clip option requires a geographic projection.
#' @param curve `<"basis" | "basis-closed" | "basis-open" | "bundle" | "bump-x" | "bump-y" | "cardinal" | "cardinal-closed" | "cardinal-open" | "catmull-rom" | ... | param()>` The curve (interpolation) method for connecting adjacent points.
#' @param dx `<number | param()>` The horizontal offset in pixels; a constant option.
#' @param dy `<number | param()>` The vertical offset in pixels; a constant option.
#' @param facet `<"auto" | "include" | "exclude" | "super" | boolean | NULL | param()>` Whether to enable or disable faceting; one of: - *auto* (default) - automatically determine if this mark should be faceted - *include* (or true) - draw the subset of the mark’s data in the current facet - *exclude* - draw the subset of the mark’s data *not* in the current facet - *super* - draw this mark in a single frame that covers all facets - null (or false) - repeat this mark’s data across all facets (*i.e.*, no faceting) When a mark uses *super* faceting, it is not allowed to use position scales (*x*, *y*, *fx*, or *fy*); *super* faceting is intended for decorations, such as labels and legends.
#' @param facet_anchor `<"top" | "right" | "bottom" | "left" | "top-left" | "top-right" | "bottom-left" | "bottom-right" | "top-empty" | "right-empty" | "bottom-empty" | "left-empty" | "empty" | NULL | param()>` How to place the mark with respect to facets; one of: - null (default for most marks) - display the mark in each non-empty facet - *top*, *right*, *bottom*, or *left* - display the mark only in facets on the given side - *top-empty*, *right-empty*, *bottom-empty*, or *left-empty* (default for axis marks) - display the mark only in facets that have empty space on the given side: either the margin, or an empty facet - *empty* - display the mark in empty facets only
#' @param fill `<column | literal | transform() | sql()/agg() | list(value=, ...) | param()>` The fill; a constant CSS color string, or a channel typically bound to the *color* scale.
#' @param fill_opacity `<column | literal | transform() | sql()/agg() | list(value=, ...) | param()>` The fill-opacity; a constant number between 0 and 1, or a channel typically bound to the *opacity* scale.
#' @param filter `<column | literal | transform() | sql()/agg()>` Applies a transform to filter the mark’s index according to the given channel values; only truthy values are retained.
#' @param fx `<column | literal | transform() | sql()/agg()>` The horizontal facet position channel, for mark-level faceting, bound to the *fx* scale.
#' @param fy `<column | literal | transform() | sql()/agg()>` The vertical facet position channel, for mark-level faceting, bound to the *fy* scale.
#' @param href `<column | literal | transform() | sql()/agg()>` The href; a channel specifying URLs for clickable links.
#' @param image_filter `<string | param()>` A CSS filter; a constant string used to adjust the rendering of images, such as *blur(5px)*.
#' @param margin `<number | param()>` Shorthand to set the same default for all four mark margins: **marginTop**, **marginRight**, **marginBottom**, and **marginLeft**; typically defaults to 0, except for axis marks.
#' @param margin_bottom `<number | param()>` The mark’s bottom margin; the minimum distance in pixels between the bottom edges of the inner and outer plot area.
#' @param margin_left `<number | param()>` The mark’s left margin; the minimum distance in pixels between the left edges of the inner and outer plot area.
#' @param margin_right `<number | param()>` The mark’s right margin; the minimum distance in pixels between the right edges of the mark’s inner and outer plot area.
#' @param margin_top `<number | param()>` The mark’s top margin; the minimum distance in pixels between the top edges of the inner and outer plot area.
#' @param mix_blend_mode `<string | param()>` The mix-blend-mode; a constant string specifying how to blend content such as *multiply*.
#' @param normalize `<boolean | param()>` Flag to perform approximate arc length normalization of line segments to prevent artifacts due to overcounting steep lines.
#' @param offset `<"center" | "normalize" | "wiggle" | NULL | param()>` After stacking, an optional **offset** can be applied to translate and scale stacks, say to produce a streamgraph; defaults to null for a zero baseline (**y** = 0 for stackY, and **x** = 0 for stackX).
#' @param opacity `<column | literal | transform() | sql()/agg() | list(value=, ...)>` The opacity; a constant between 0 and 1, or a channel typically bound to the *opacity* scale.
#' @param order `<"value" | "x" | "y" | "z" | "sum" | "appearance" | "inside-out" | "-value" | "-x" | "-y" | ... | NULL | param()>` The order in which stacks are layered; one of: - null (default) for input order - a named stack order method such as *inside-out* or *sum* - a field name, for natural order of the corresponding values - a function of data, for natural order of the corresponding values - an array of explicit **z** values in the desired order If the *wiggle* **offset** is used, as for a streamgraph, the default changes to *inside-out*.
#' @param paint_order `<string | param()>` The paint-order; a constant string specifying the order in which the **fill**, **stroke**, and any markers are drawn; defaults to *normal*, which draws the fill, then stroke, then markers; defaults to *stroke* for the text mark to create a “halo” around text to improve legibility.
#' @param pointer_events `<string | param()>` The pointer-events property; a constant string such as *none*.
#' @param reverse `<boolean | param()>` Applies a transform to reverse the order of the mark’s index, say for reverse input order.
#' @param select `<"first" | "last" | "maxX" | "maxY" | "minX" | "minY" | "nearest" | "nearestX" | "nearestY">` Applies a filter transform after data is loaded to highlight selected values only.
#' @param shape_rendering `<string | param()>` The shape-rendering; a constant string such as *crispEdges*.
#' @param sort `<column | literal | transform() | sql()/agg() | list>` Either applies a transform to sort the mark’s index by the specified channel values, or imputes ordinal scale domains from this mark’s channels.
#' @param stack `<boolean | param()>` Flag indicating if densities should be stacked.
#' @param stroke `<column | literal | transform() | sql()/agg() | list(value=, ...) | param()>` The stroke; a constant CSS color string, or a channel typically bound to the *color* scale.
#' @param stroke_dasharray `<string | number | param()>` The stroke-dasharray; a constant number indicating the length in pixels of alternating dashes and gaps, or a constant string of numbers separated by spaces or commas (_e.g._, *10 2* for dashes of 10 pixels separated by gaps of 2 pixels), or *none* (the default) for no dashing : https://developer.mozilla.org/en-US/docs/Web/SVG/Attribute/stroke-dasharray
#' @param stroke_dashoffset `<string | number | param()>` The stroke-dashoffset; a constant indicating the offset in pixels of the first dash along the stroke; defaults to zero.
#' @param stroke_linecap `<string | param()>` The stroke-linecap; a constant specifying how to cap stroked paths, such as *butt*, *round*, or *square*.
#' @param stroke_linejoin `<string | param()>` The stroke-linejoin; a constant specifying how to join stroked paths, such as *bevel*, *miter*, *miter-clip*, or *round*.
#' @param stroke_miterlimit `<number | param()>` The stroke-miterlimit; a constant number specifying how to limit the length of *miter* joins on stroked paths.
#' @param stroke_opacity `<column | literal | transform() | sql()/agg() | list(value=, ...)>` The stroke-opacity; a constant between 0 and 1, or a channel typically bound to the *opacity* scale.
#' @param stroke_width `<column | literal | transform() | sql()/agg() | list(value=, ...)>` The stroke-width; a constant number in pixels, or a channel.
#' @param target `<string | param()>` The target; a constant string specifying the target window (_e.g._, *_blank*) for clickable links; used in conjunction with the **href** option.
#' @param tension `<number | param()>` The tension option only has an effect on bundle, cardinal and Catmull–Rom splines (*bundle*, *cardinal*, *cardinal-open*, *cardinal-closed*, *catmull-rom*, *catmull-rom-open*, and *catmull-rom-closed*).
#' @param tip `<boolean | "x" | "y" | "xy" | list | param()>` Whether to generate a tooltip for this mark, and any tip options.
#' @param title `<column | literal | transform() | sql()/agg()>` The title; a channel specifying accessible, short textual descriptions as strings (possibly with newlines).
#' @param type `<"dot" | "circle" | "hexagon" | "cell" | "text" | param()>` The basic mark type to use to render 2D density values.
#' @param y `<column | literal | transform() | sql()/agg() | list(value=, ...)>` The vertical position channel, typically bound to the *y* scale; defaults to the zero-based index of the data 0, 1, 2, ….
#' @param z `<column | literal | transform() | sql()/agg()>` An optional ordinal channel for grouping data into (possibly stacked) series to be drawn as separate areas; defaults to **fill** if a channel, or **stroke** if a channel.
#' @param marker `<"arrow" | "arrow-reverse" | "dot" | "circle" | "circle-fill" | "circle-stroke" | "tick" | "tick-x" | "tick-y" | "none" | boolean | NULL | param()>` Shorthand to set the same default for markerStart, markerMid, and markerEnd; one of: - a marker name such as *arrow* or *circle* - *none* (default) - no marker * true - alias for *circle-fill* * false or null - alias for *none*
#' @param marker_end `<"arrow" | "arrow-reverse" | "dot" | "circle" | "circle-fill" | "circle-stroke" | "tick" | "tick-x" | "tick-y" | "none" | boolean | NULL | param()>` The marker for the ending point of a line segment; one of: - a marker name such as *arrow* or *circle* * *none* (default) - no marker * true - alias for *circle-fill* * false or null - alias for *none*
#' @param marker_mid `<"arrow" | "arrow-reverse" | "dot" | "circle" | "circle-fill" | "circle-stroke" | "tick" | "tick-x" | "tick-y" | "none" | boolean | NULL | param()>` The marker for any middle (interior) points of a line segment.
#' @param marker_start `<"arrow" | "arrow-reverse" | "dot" | "circle" | "circle-fill" | "circle-stroke" | "tick" | "tick-x" | "tick-y" | "none" | boolean | NULL | param()>` The marker for the starting point of a line segment; one of: - a marker name such as *arrow* or *circle* * *none* (default) - no marker * true - alias for *circle-fill* * false or null - alias for *none*
#' @param frame_anchor `<"middle" | "top-left" | "top" | "top-right" | "right" | "bottom-right" | "bottom" | "bottom-left" | "left" | param()>` The frame anchor specifies defaults for **x** and **y**, along with **textAnchor** and **lineAnchor**, based on the plot’s frame; it may be one of the four sides (*top*, *right*, *bottom*, *left*), one of the four corners (*top-left*, *top-right*, *bottom-right*, *bottom-left*), or the *middle* of the frame.
#' @param r `<column | literal | transform() | sql()/agg() | list(value=, ...) | number | param()>` The radius of dots; either a channel or constant.
#' @param rotate `<column | literal | transform() | sql()/agg() | param()>` The rotation angle in degrees clockwise; a constant or a channel; defaults to 0°.
#' @param symbol `<column | literal | transform() | sql()/agg() | list(value=, ...) | "asterisk" | "circle" | "cross" | "diamond" | "diamond2" | "hexagon" | "plus" | "square" | "square2" | "star" | ... | param()>` The categorical symbol; either a channel or a constant.
#' @param font_family `<string | param()>` The font-family; a constant; defaults to the plot’s font family, which is typically *system-ui*.
#' @param font_size `<column | literal | transform() | sql()/agg() | param()>` The font size in pixels; either a constant or a channel; defaults to the plot’s font size, which is typically 10.
#' @param font_style `<string | param()>` The font style; a constant; defaults to the plot’s font style, which is typically *normal*.
#' @param font_variant `<string | param()>` The font variant; a constant; if the **text** channel contains numbers or dates, defaults to *tabular-nums* to facilitate comparing numbers; otherwise defaults to the plot’s font style, which is typically *normal*.
#' @param font_weight `<string | number | param()>` The font weight; a constant; defaults to the plot’s font weight, which is typically *normal*.
#' @param line_anchor `<"top" | "middle" | "bottom" | param()>` The line anchor controls how text is aligned (typically vertically) relative to its anchor point; it is one of *top*, *bottom*, or *middle*.
#' @param line_height `<number | param()>` The line height in ems; defaults to 1.
#' @param line_width `<number | param()>` The line width in ems (e.g., 10 for about 20 characters); defaults to infinity, disabling wrapping and clipping.
#' @param monospace `<boolean | param()>` If true, changes the default **fontFamily** to *monospace*, and uses simplified monospaced text metrics calculations.
#' @param text `<column | literal | transform() | sql()/agg()>` The text contents channel, possibly with line breaks (\\n, \\r\\n, or \\r).
#' @param text_anchor `<"start" | "middle" | "end" | param()>` The text anchor controls how text is aligned (typically horizontally) relative to its anchor point; it is one of *start*, *end*, or *middle*.
#' @param text_overflow `<NULL | "clip" | "ellipsis" | "clip-start" | "clip-end" | "ellipsis-start" | "ellipsis-middle" | "ellipsis-end" | param()>` How truncate (or wrap) lines of text longer than the given **lineWidth**; one of: - null (default) - preserve overflowing characters (and wrap if needed) - *clip* or *clip-end* - remove characters from the end - *clip-start* - remove characters from the start - *ellipsis* or *ellipsis-end* - replace characters from the end with an ellipsis (…) - *ellipsis-start* - replace characters from the start with an ellipsis (…) - *ellipsis-middle* - replace characters from the middle with an ellipsis (…) If no **title** was specified, if text requires truncation, a title containing the non-truncated text will be implicitly added.
#' @param ... Additional options or plot-level attributes.
#' @param data_from The name of the data source this mark reads from (see [vg_data()]); a length-1 nonzero R integer (`1L`, `-1L`, ...; note the `L`) giving a 1-based index into the spec's registered data sources instead, negative counting from the end (e.g., `-1L` for the most recently registered one); or a literal vector of values to use as inline data directly (mosaic-spec's `"data": [...]` shorthand, e.g., for a single reference line -- a bare double like `0`/`c(0)`, or `0L` itself (never a valid index), means this, not an index). Left unset, defaults to the first registered data source (equivalent to `data_from = 1L`) for any mark type that takes data at all.
#' @param filter_by A Param/Selection (e.g., from [param()]) to filter this mark's data by.
#' @param data_optimize A flag (default `TRUE`) to enable mark-specific query optimizations for this mark's data; set `FALSE` to disable them (mosaic-spec's `data: {optimize: false}`).
#' @family mark functions
#' @export
vg_mark_density_x <- function(spec = NULL, formula = vg_unset, aria_description = vg_unset, aria_hidden = vg_unset, aria_label = vg_unset, bandwidth = vg_unset, bins = vg_unset, channels = vg_unset, clip = vg_unset, curve = vg_unset, dx = vg_unset, dy = vg_unset, facet = vg_unset, facet_anchor = vg_unset, fill = vg_unset, fill_opacity = vg_unset, filter = vg_unset, fx = vg_unset, fy = vg_unset, href = vg_unset, image_filter = vg_unset, margin = vg_unset, margin_bottom = vg_unset, margin_left = vg_unset, margin_right = vg_unset, margin_top = vg_unset, mix_blend_mode = vg_unset, normalize = vg_unset, offset = vg_unset, opacity = vg_unset, order = vg_unset, paint_order = vg_unset, pointer_events = vg_unset, reverse = vg_unset, select = vg_unset, shape_rendering = vg_unset, sort = vg_unset, stack = vg_unset, stroke = vg_unset, stroke_dasharray = vg_unset, stroke_dashoffset = vg_unset, stroke_linecap = vg_unset, stroke_linejoin = vg_unset, stroke_miterlimit = vg_unset, stroke_opacity = vg_unset, stroke_width = vg_unset, target = vg_unset, tension = vg_unset, tip = vg_unset, title = vg_unset, type = vg_unset, y = vg_unset, z = vg_unset, marker = vg_unset, marker_end = vg_unset, marker_mid = vg_unset, marker_start = vg_unset, frame_anchor = vg_unset, r = vg_unset, rotate = vg_unset, symbol = vg_unset, font_family = vg_unset, font_size = vg_unset, font_style = vg_unset, font_variant = vg_unset, font_weight = vg_unset, line_anchor = vg_unset, line_height = vg_unset, line_width = vg_unset, monospace = vg_unset, text = vg_unset, text_anchor = vg_unset, text_overflow = vg_unset, ..., data_from = vg_unset, filter_by = vg_unset, data_optimize = vg_unset) {
  vg_mark_(spec, "densityX", formula = formula, ariaDescription = aria_description, ariaHidden = aria_hidden, ariaLabel = aria_label, bandwidth = bandwidth, bins = bins, channels = channels, clip = clip, curve = curve, dx = dx, dy = dy, facet = facet, facetAnchor = facet_anchor, fill = fill, fillOpacity = fill_opacity, filter = filter, fx = fx, fy = fy, href = href, imageFilter = image_filter, margin = margin, marginBottom = margin_bottom, marginLeft = margin_left, marginRight = margin_right, marginTop = margin_top, mixBlendMode = mix_blend_mode, normalize = normalize, offset = offset, opacity = opacity, order = order, paintOrder = paint_order, pointerEvents = pointer_events, reverse = reverse, select = select, shapeRendering = shape_rendering, sort = sort, stack = stack, stroke = stroke, strokeDasharray = stroke_dasharray, strokeDashoffset = stroke_dashoffset, strokeLinecap = stroke_linecap, strokeLinejoin = stroke_linejoin, strokeMiterlimit = stroke_miterlimit, strokeOpacity = stroke_opacity, strokeWidth = stroke_width, target = target, tension = tension, tip = tip, title = title, type = type, y = y, z = z, marker = marker, markerEnd = marker_end, markerMid = marker_mid, markerStart = marker_start, frameAnchor = frame_anchor, r = r, rotate = rotate, symbol = symbol, fontFamily = font_family, fontSize = font_size, fontStyle = font_style, fontVariant = font_variant, fontWeight = font_weight, lineAnchor = line_anchor, lineHeight = line_height, lineWidth = line_width, monospace = monospace, text = text, textAnchor = text_anchor, textOverflow = text_overflow, ..., data_from = data_from, filter_by = filter_by, data_optimize = data_optimize)
}

#' The densityY mark.
#'
#' See [vg_value_types] for what the `<...>` notation below (`column`, `param()`, ...) means.
#'
#' @param spec A plot fragment or `vgspec` to add this mark to, or `NULL` to start a new plot with just this mark.
#' @param formula A shorthand for this mark's position channels (`x`, `y`, `fx`, `fy`, and paired `x1`/`x2` or `y1`/`y2`), e.g., `Sepal.Length ~ Sepal.Width | ~ Species` for `y = ~Sepal.Length, x = ~Sepal.Width, fx = ~Species`. See [vg_mark()] for the full grammar.
#' @param aria_description `<string | param()>` The aria-description; a constant textual description.
#' @param aria_hidden `<string | param()>` The aria-hidden state; a constant indicating whether the element is exposed to an accessibility API.
#' @param aria_label `<column | literal | transform() | sql()/agg()>` The aria-label; a channel specifying short textual labels representing the value in the accessibility tree.
#' @param bandwidth `<number | param()>` The kernel density bandwidth for smoothing, in pixels.
#' @param bins `<number | param()>` The number of bins over which to discretize the data prior to smoothing.
#' @param channels `<list>` Additional named channels, for example to include in a tooltip.
#' @param clip `<"frame" | "sphere" | boolean | NULL | param()>` How to clip the mark; one of: - *frame* or true - clip to the plot’s frame (inner area) - *sphere* - clip to the projected sphere (*e.g.*, front hemisphere) - null or false - do not clip The *sphere* clip option requires a geographic projection.
#' @param curve `<"basis" | "basis-closed" | "basis-open" | "bundle" | "bump-x" | "bump-y" | "cardinal" | "cardinal-closed" | "cardinal-open" | "catmull-rom" | ... | param()>` The curve (interpolation) method for connecting adjacent points.
#' @param dx `<number | param()>` The horizontal offset in pixels; a constant option.
#' @param dy `<number | param()>` The vertical offset in pixels; a constant option.
#' @param facet `<"auto" | "include" | "exclude" | "super" | boolean | NULL | param()>` Whether to enable or disable faceting; one of: - *auto* (default) - automatically determine if this mark should be faceted - *include* (or true) - draw the subset of the mark’s data in the current facet - *exclude* - draw the subset of the mark’s data *not* in the current facet - *super* - draw this mark in a single frame that covers all facets - null (or false) - repeat this mark’s data across all facets (*i.e.*, no faceting) When a mark uses *super* faceting, it is not allowed to use position scales (*x*, *y*, *fx*, or *fy*); *super* faceting is intended for decorations, such as labels and legends.
#' @param facet_anchor `<"top" | "right" | "bottom" | "left" | "top-left" | "top-right" | "bottom-left" | "bottom-right" | "top-empty" | "right-empty" | "bottom-empty" | "left-empty" | "empty" | NULL | param()>` How to place the mark with respect to facets; one of: - null (default for most marks) - display the mark in each non-empty facet - *top*, *right*, *bottom*, or *left* - display the mark only in facets on the given side - *top-empty*, *right-empty*, *bottom-empty*, or *left-empty* (default for axis marks) - display the mark only in facets that have empty space on the given side: either the margin, or an empty facet - *empty* - display the mark in empty facets only
#' @param fill `<column | literal | transform() | sql()/agg() | list(value=, ...) | param()>` The fill; a constant CSS color string, or a channel typically bound to the *color* scale.
#' @param fill_opacity `<column | literal | transform() | sql()/agg() | list(value=, ...) | param()>` The fill-opacity; a constant number between 0 and 1, or a channel typically bound to the *opacity* scale.
#' @param filter `<column | literal | transform() | sql()/agg()>` Applies a transform to filter the mark’s index according to the given channel values; only truthy values are retained.
#' @param fx `<column | literal | transform() | sql()/agg()>` The horizontal facet position channel, for mark-level faceting, bound to the *fx* scale.
#' @param fy `<column | literal | transform() | sql()/agg()>` The vertical facet position channel, for mark-level faceting, bound to the *fy* scale.
#' @param href `<column | literal | transform() | sql()/agg()>` The href; a channel specifying URLs for clickable links.
#' @param image_filter `<string | param()>` A CSS filter; a constant string used to adjust the rendering of images, such as *blur(5px)*.
#' @param margin `<number | param()>` Shorthand to set the same default for all four mark margins: **marginTop**, **marginRight**, **marginBottom**, and **marginLeft**; typically defaults to 0, except for axis marks.
#' @param margin_bottom `<number | param()>` The mark’s bottom margin; the minimum distance in pixels between the bottom edges of the inner and outer plot area.
#' @param margin_left `<number | param()>` The mark’s left margin; the minimum distance in pixels between the left edges of the inner and outer plot area.
#' @param margin_right `<number | param()>` The mark’s right margin; the minimum distance in pixels between the right edges of the mark’s inner and outer plot area.
#' @param margin_top `<number | param()>` The mark’s top margin; the minimum distance in pixels between the top edges of the inner and outer plot area.
#' @param mix_blend_mode `<string | param()>` The mix-blend-mode; a constant string specifying how to blend content such as *multiply*.
#' @param normalize `<boolean | param()>` Flag to perform approximate arc length normalization of line segments to prevent artifacts due to overcounting steep lines.
#' @param offset `<"center" | "normalize" | "wiggle" | NULL | param()>` After stacking, an optional **offset** can be applied to translate and scale stacks, say to produce a streamgraph; defaults to null for a zero baseline (**y** = 0 for stackY, and **x** = 0 for stackX).
#' @param opacity `<column | literal | transform() | sql()/agg() | list(value=, ...)>` The opacity; a constant between 0 and 1, or a channel typically bound to the *opacity* scale.
#' @param order `<"value" | "x" | "y" | "z" | "sum" | "appearance" | "inside-out" | "-value" | "-x" | "-y" | ... | NULL | param()>` The order in which stacks are layered; one of: - null (default) for input order - a named stack order method such as *inside-out* or *sum* - a field name, for natural order of the corresponding values - a function of data, for natural order of the corresponding values - an array of explicit **z** values in the desired order If the *wiggle* **offset** is used, as for a streamgraph, the default changes to *inside-out*.
#' @param paint_order `<string | param()>` The paint-order; a constant string specifying the order in which the **fill**, **stroke**, and any markers are drawn; defaults to *normal*, which draws the fill, then stroke, then markers; defaults to *stroke* for the text mark to create a “halo” around text to improve legibility.
#' @param pointer_events `<string | param()>` The pointer-events property; a constant string such as *none*.
#' @param reverse `<boolean | param()>` Applies a transform to reverse the order of the mark’s index, say for reverse input order.
#' @param select `<"first" | "last" | "maxX" | "maxY" | "minX" | "minY" | "nearest" | "nearestX" | "nearestY">` Applies a filter transform after data is loaded to highlight selected values only.
#' @param shape_rendering `<string | param()>` The shape-rendering; a constant string such as *crispEdges*.
#' @param sort `<column | literal | transform() | sql()/agg() | list>` Either applies a transform to sort the mark’s index by the specified channel values, or imputes ordinal scale domains from this mark’s channels.
#' @param stack `<boolean | param()>` Flag indicating if densities should be stacked.
#' @param stroke `<column | literal | transform() | sql()/agg() | list(value=, ...) | param()>` The stroke; a constant CSS color string, or a channel typically bound to the *color* scale.
#' @param stroke_dasharray `<string | number | param()>` The stroke-dasharray; a constant number indicating the length in pixels of alternating dashes and gaps, or a constant string of numbers separated by spaces or commas (_e.g._, *10 2* for dashes of 10 pixels separated by gaps of 2 pixels), or *none* (the default) for no dashing : https://developer.mozilla.org/en-US/docs/Web/SVG/Attribute/stroke-dasharray
#' @param stroke_dashoffset `<string | number | param()>` The stroke-dashoffset; a constant indicating the offset in pixels of the first dash along the stroke; defaults to zero.
#' @param stroke_linecap `<string | param()>` The stroke-linecap; a constant specifying how to cap stroked paths, such as *butt*, *round*, or *square*.
#' @param stroke_linejoin `<string | param()>` The stroke-linejoin; a constant specifying how to join stroked paths, such as *bevel*, *miter*, *miter-clip*, or *round*.
#' @param stroke_miterlimit `<number | param()>` The stroke-miterlimit; a constant number specifying how to limit the length of *miter* joins on stroked paths.
#' @param stroke_opacity `<column | literal | transform() | sql()/agg() | list(value=, ...)>` The stroke-opacity; a constant between 0 and 1, or a channel typically bound to the *opacity* scale.
#' @param stroke_width `<column | literal | transform() | sql()/agg() | list(value=, ...)>` The stroke-width; a constant number in pixels, or a channel.
#' @param target `<string | param()>` The target; a constant string specifying the target window (_e.g._, *_blank*) for clickable links; used in conjunction with the **href** option.
#' @param tension `<number | param()>` The tension option only has an effect on bundle, cardinal and Catmull–Rom splines (*bundle*, *cardinal*, *cardinal-open*, *cardinal-closed*, *catmull-rom*, *catmull-rom-open*, and *catmull-rom-closed*).
#' @param tip `<boolean | "x" | "y" | "xy" | list | param()>` Whether to generate a tooltip for this mark, and any tip options.
#' @param title `<column | literal | transform() | sql()/agg()>` The title; a channel specifying accessible, short textual descriptions as strings (possibly with newlines).
#' @param type `<"dot" | "circle" | "hexagon" | "cell" | "text" | param()>` The basic mark type to use to render 2D density values.
#' @param x `<column | literal | transform() | sql()/agg() | list(value=, ...)>` The horizontal position (or length) channel, typically bound to the *x* scale.
#' @param z `<column | literal | transform() | sql()/agg()>` An optional ordinal channel for grouping data into (possibly stacked) series to be drawn as separate areas; defaults to **fill** if a channel, or **stroke** if a channel.
#' @param marker `<"arrow" | "arrow-reverse" | "dot" | "circle" | "circle-fill" | "circle-stroke" | "tick" | "tick-x" | "tick-y" | "none" | boolean | NULL | param()>` Shorthand to set the same default for markerStart, markerMid, and markerEnd; one of: - a marker name such as *arrow* or *circle* - *none* (default) - no marker * true - alias for *circle-fill* * false or null - alias for *none*
#' @param marker_end `<"arrow" | "arrow-reverse" | "dot" | "circle" | "circle-fill" | "circle-stroke" | "tick" | "tick-x" | "tick-y" | "none" | boolean | NULL | param()>` The marker for the ending point of a line segment; one of: - a marker name such as *arrow* or *circle* * *none* (default) - no marker * true - alias for *circle-fill* * false or null - alias for *none*
#' @param marker_mid `<"arrow" | "arrow-reverse" | "dot" | "circle" | "circle-fill" | "circle-stroke" | "tick" | "tick-x" | "tick-y" | "none" | boolean | NULL | param()>` The marker for any middle (interior) points of a line segment.
#' @param marker_start `<"arrow" | "arrow-reverse" | "dot" | "circle" | "circle-fill" | "circle-stroke" | "tick" | "tick-x" | "tick-y" | "none" | boolean | NULL | param()>` The marker for the starting point of a line segment; one of: - a marker name such as *arrow* or *circle* * *none* (default) - no marker * true - alias for *circle-fill* * false or null - alias for *none*
#' @param frame_anchor `<"middle" | "top-left" | "top" | "top-right" | "right" | "bottom-right" | "bottom" | "bottom-left" | "left" | param()>` The frame anchor specifies defaults for **x** and **y**, along with **textAnchor** and **lineAnchor**, based on the plot’s frame; it may be one of the four sides (*top*, *right*, *bottom*, *left*), one of the four corners (*top-left*, *top-right*, *bottom-right*, *bottom-left*), or the *middle* of the frame.
#' @param r `<column | literal | transform() | sql()/agg() | list(value=, ...) | number | param()>` The radius of dots; either a channel or constant.
#' @param rotate `<column | literal | transform() | sql()/agg() | param()>` The rotation angle in degrees clockwise; a constant or a channel; defaults to 0°.
#' @param symbol `<column | literal | transform() | sql()/agg() | list(value=, ...) | "asterisk" | "circle" | "cross" | "diamond" | "diamond2" | "hexagon" | "plus" | "square" | "square2" | "star" | ... | param()>` The categorical symbol; either a channel or a constant.
#' @param font_family `<string | param()>` The font-family; a constant; defaults to the plot’s font family, which is typically *system-ui*.
#' @param font_size `<column | literal | transform() | sql()/agg() | param()>` The font size in pixels; either a constant or a channel; defaults to the plot’s font size, which is typically 10.
#' @param font_style `<string | param()>` The font style; a constant; defaults to the plot’s font style, which is typically *normal*.
#' @param font_variant `<string | param()>` The font variant; a constant; if the **text** channel contains numbers or dates, defaults to *tabular-nums* to facilitate comparing numbers; otherwise defaults to the plot’s font style, which is typically *normal*.
#' @param font_weight `<string | number | param()>` The font weight; a constant; defaults to the plot’s font weight, which is typically *normal*.
#' @param line_anchor `<"top" | "middle" | "bottom" | param()>` The line anchor controls how text is aligned (typically vertically) relative to its anchor point; it is one of *top*, *bottom*, or *middle*.
#' @param line_height `<number | param()>` The line height in ems; defaults to 1.
#' @param line_width `<number | param()>` The line width in ems (e.g., 10 for about 20 characters); defaults to infinity, disabling wrapping and clipping.
#' @param monospace `<boolean | param()>` If true, changes the default **fontFamily** to *monospace*, and uses simplified monospaced text metrics calculations.
#' @param text `<column | literal | transform() | sql()/agg()>` The text contents channel, possibly with line breaks (\\n, \\r\\n, or \\r).
#' @param text_anchor `<"start" | "middle" | "end" | param()>` The text anchor controls how text is aligned (typically horizontally) relative to its anchor point; it is one of *start*, *end*, or *middle*.
#' @param text_overflow `<NULL | "clip" | "ellipsis" | "clip-start" | "clip-end" | "ellipsis-start" | "ellipsis-middle" | "ellipsis-end" | param()>` How truncate (or wrap) lines of text longer than the given **lineWidth**; one of: - null (default) - preserve overflowing characters (and wrap if needed) - *clip* or *clip-end* - remove characters from the end - *clip-start* - remove characters from the start - *ellipsis* or *ellipsis-end* - replace characters from the end with an ellipsis (…) - *ellipsis-start* - replace characters from the start with an ellipsis (…) - *ellipsis-middle* - replace characters from the middle with an ellipsis (…) If no **title** was specified, if text requires truncation, a title containing the non-truncated text will be implicitly added.
#' @param ... Additional options or plot-level attributes.
#' @param data_from The name of the data source this mark reads from (see [vg_data()]); a length-1 nonzero R integer (`1L`, `-1L`, ...; note the `L`) giving a 1-based index into the spec's registered data sources instead, negative counting from the end (e.g., `-1L` for the most recently registered one); or a literal vector of values to use as inline data directly (mosaic-spec's `"data": [...]` shorthand, e.g., for a single reference line -- a bare double like `0`/`c(0)`, or `0L` itself (never a valid index), means this, not an index). Left unset, defaults to the first registered data source (equivalent to `data_from = 1L`) for any mark type that takes data at all.
#' @param filter_by A Param/Selection (e.g., from [param()]) to filter this mark's data by.
#' @param data_optimize A flag (default `TRUE`) to enable mark-specific query optimizations for this mark's data; set `FALSE` to disable them (mosaic-spec's `data: {optimize: false}`).
#' @family mark functions
#' @export
vg_mark_density_y <- function(spec = NULL, formula = vg_unset, aria_description = vg_unset, aria_hidden = vg_unset, aria_label = vg_unset, bandwidth = vg_unset, bins = vg_unset, channels = vg_unset, clip = vg_unset, curve = vg_unset, dx = vg_unset, dy = vg_unset, facet = vg_unset, facet_anchor = vg_unset, fill = vg_unset, fill_opacity = vg_unset, filter = vg_unset, fx = vg_unset, fy = vg_unset, href = vg_unset, image_filter = vg_unset, margin = vg_unset, margin_bottom = vg_unset, margin_left = vg_unset, margin_right = vg_unset, margin_top = vg_unset, mix_blend_mode = vg_unset, normalize = vg_unset, offset = vg_unset, opacity = vg_unset, order = vg_unset, paint_order = vg_unset, pointer_events = vg_unset, reverse = vg_unset, select = vg_unset, shape_rendering = vg_unset, sort = vg_unset, stack = vg_unset, stroke = vg_unset, stroke_dasharray = vg_unset, stroke_dashoffset = vg_unset, stroke_linecap = vg_unset, stroke_linejoin = vg_unset, stroke_miterlimit = vg_unset, stroke_opacity = vg_unset, stroke_width = vg_unset, target = vg_unset, tension = vg_unset, tip = vg_unset, title = vg_unset, type = vg_unset, x = vg_unset, z = vg_unset, marker = vg_unset, marker_end = vg_unset, marker_mid = vg_unset, marker_start = vg_unset, frame_anchor = vg_unset, r = vg_unset, rotate = vg_unset, symbol = vg_unset, font_family = vg_unset, font_size = vg_unset, font_style = vg_unset, font_variant = vg_unset, font_weight = vg_unset, line_anchor = vg_unset, line_height = vg_unset, line_width = vg_unset, monospace = vg_unset, text = vg_unset, text_anchor = vg_unset, text_overflow = vg_unset, ..., data_from = vg_unset, filter_by = vg_unset, data_optimize = vg_unset) {
  vg_mark_(spec, "densityY", formula = formula, ariaDescription = aria_description, ariaHidden = aria_hidden, ariaLabel = aria_label, bandwidth = bandwidth, bins = bins, channels = channels, clip = clip, curve = curve, dx = dx, dy = dy, facet = facet, facetAnchor = facet_anchor, fill = fill, fillOpacity = fill_opacity, filter = filter, fx = fx, fy = fy, href = href, imageFilter = image_filter, margin = margin, marginBottom = margin_bottom, marginLeft = margin_left, marginRight = margin_right, marginTop = margin_top, mixBlendMode = mix_blend_mode, normalize = normalize, offset = offset, opacity = opacity, order = order, paintOrder = paint_order, pointerEvents = pointer_events, reverse = reverse, select = select, shapeRendering = shape_rendering, sort = sort, stack = stack, stroke = stroke, strokeDasharray = stroke_dasharray, strokeDashoffset = stroke_dashoffset, strokeLinecap = stroke_linecap, strokeLinejoin = stroke_linejoin, strokeMiterlimit = stroke_miterlimit, strokeOpacity = stroke_opacity, strokeWidth = stroke_width, target = target, tension = tension, tip = tip, title = title, type = type, x = x, z = z, marker = marker, markerEnd = marker_end, markerMid = marker_mid, markerStart = marker_start, frameAnchor = frame_anchor, r = r, rotate = rotate, symbol = symbol, fontFamily = font_family, fontSize = font_size, fontStyle = font_style, fontVariant = font_variant, fontWeight = font_weight, lineAnchor = line_anchor, lineHeight = line_height, lineWidth = line_width, monospace = monospace, text = text, textAnchor = text_anchor, textOverflow = text_overflow, ..., data_from = data_from, filter_by = filter_by, data_optimize = data_optimize)
}

#' The dot mark.
#'
#' See [vg_value_types] for what the `<...>` notation below (`column`, `param()`, ...) means.
#'
#' @param spec A plot fragment or `vgspec` to add this mark to, or `NULL` to start a new plot with just this mark.
#' @param formula A shorthand for this mark's position channels (`x`, `y`, `fx`, `fy`, and paired `x1`/`x2` or `y1`/`y2`), e.g., `Sepal.Length ~ Sepal.Width | ~ Species` for `y = ~Sepal.Length, x = ~Sepal.Width, fx = ~Species`. See [vg_mark()] for the full grammar.
#' @param aria_description `<string | param()>` The aria-description; a constant textual description.
#' @param aria_hidden `<string | param()>` The aria-hidden state; a constant indicating whether the element is exposed to an accessibility API.
#' @param aria_label `<column | literal | transform() | sql()/agg()>` The aria-label; a channel specifying short textual labels representing the value in the accessibility tree.
#' @param channels `<list>` Additional named channels, for example to include in a tooltip.
#' @param clip `<"frame" | "sphere" | boolean | NULL | param()>` How to clip the mark; one of: - *frame* or true - clip to the plot’s frame (inner area) - *sphere* - clip to the projected sphere (*e.g.*, front hemisphere) - null or false - do not clip The *sphere* clip option requires a geographic projection.
#' @param dx `<number | param()>` The horizontal offset in pixels; a constant option.
#' @param dy `<number | param()>` The vertical offset in pixels; a constant option.
#' @param facet `<"auto" | "include" | "exclude" | "super" | boolean | NULL | param()>` Whether to enable or disable faceting; one of: - *auto* (default) - automatically determine if this mark should be faceted - *include* (or true) - draw the subset of the mark’s data in the current facet - *exclude* - draw the subset of the mark’s data *not* in the current facet - *super* - draw this mark in a single frame that covers all facets - null (or false) - repeat this mark’s data across all facets (*i.e.*, no faceting) When a mark uses *super* faceting, it is not allowed to use position scales (*x*, *y*, *fx*, or *fy*); *super* faceting is intended for decorations, such as labels and legends.
#' @param facet_anchor `<"top" | "right" | "bottom" | "left" | "top-left" | "top-right" | "bottom-left" | "bottom-right" | "top-empty" | "right-empty" | "bottom-empty" | "left-empty" | "empty" | NULL | param()>` How to place the mark with respect to facets; one of: - null (default for most marks) - display the mark in each non-empty facet - *top*, *right*, *bottom*, or *left* - display the mark only in facets on the given side - *top-empty*, *right-empty*, *bottom-empty*, or *left-empty* (default for axis marks) - display the mark only in facets that have empty space on the given side: either the margin, or an empty facet - *empty* - display the mark in empty facets only
#' @param fill `<column | literal | transform() | sql()/agg() | list(value=, ...) | param()>` The fill; a constant CSS color string, or a channel typically bound to the *color* scale.
#' @param fill_opacity `<column | literal | transform() | sql()/agg() | list(value=, ...) | param()>` The fill-opacity; a constant number between 0 and 1, or a channel typically bound to the *opacity* scale.
#' @param filter `<column | literal | transform() | sql()/agg()>` Applies a transform to filter the mark’s index according to the given channel values; only truthy values are retained.
#' @param frame_anchor `<"middle" | "top-left" | "top" | "top-right" | "right" | "bottom-right" | "bottom" | "bottom-left" | "left" | param()>` The frame anchor specifies defaults for **x** and **y**, along with **textAnchor** and **lineAnchor**, based on the plot’s frame; it may be one of the four sides (*top*, *right*, *bottom*, *left*), one of the four corners (*top-left*, *top-right*, *bottom-right*, *bottom-left*), or the *middle* of the frame.
#' @param fx `<column | literal | transform() | sql()/agg()>` The horizontal facet position channel, for mark-level faceting, bound to the *fx* scale.
#' @param fy `<column | literal | transform() | sql()/agg()>` The vertical facet position channel, for mark-level faceting, bound to the *fy* scale.
#' @param href `<column | literal | transform() | sql()/agg()>` The href; a channel specifying URLs for clickable links.
#' @param image_filter `<string | param()>` A CSS filter; a constant string used to adjust the rendering of images, such as *blur(5px)*.
#' @param margin `<number | param()>` Shorthand to set the same default for all four mark margins: **marginTop**, **marginRight**, **marginBottom**, and **marginLeft**; typically defaults to 0, except for axis marks.
#' @param margin_bottom `<number | param()>` The mark’s bottom margin; the minimum distance in pixels between the bottom edges of the inner and outer plot area.
#' @param margin_left `<number | param()>` The mark’s left margin; the minimum distance in pixels between the left edges of the inner and outer plot area.
#' @param margin_right `<number | param()>` The mark’s right margin; the minimum distance in pixels between the right edges of the mark’s inner and outer plot area.
#' @param margin_top `<number | param()>` The mark’s top margin; the minimum distance in pixels between the top edges of the inner and outer plot area.
#' @param mix_blend_mode `<string | param()>` The mix-blend-mode; a constant string specifying how to blend content such as *multiply*.
#' @param opacity `<column | literal | transform() | sql()/agg() | list(value=, ...)>` The opacity; a constant between 0 and 1, or a channel typically bound to the *opacity* scale.
#' @param paint_order `<string | param()>` The paint-order; a constant string specifying the order in which the **fill**, **stroke**, and any markers are drawn; defaults to *normal*, which draws the fill, then stroke, then markers; defaults to *stroke* for the text mark to create a “halo” around text to improve legibility.
#' @param pointer_events `<string | param()>` The pointer-events property; a constant string such as *none*.
#' @param r `<column | literal | transform() | sql()/agg() | list(value=, ...) | number | param()>` The radius of dots; either a channel or constant.
#' @param reverse `<boolean | param()>` Applies a transform to reverse the order of the mark’s index, say for reverse input order.
#' @param rotate `<column | literal | transform() | sql()/agg() | param()>` The rotation angle in degrees clockwise; a constant or a channel; defaults to 0°.
#' @param select `<"first" | "last" | "maxX" | "maxY" | "minX" | "minY" | "nearest" | "nearestX" | "nearestY">` Applies a filter transform after data is loaded to highlight selected values only.
#' @param shape_rendering `<string | param()>` The shape-rendering; a constant string such as *crispEdges*.
#' @param sort `<column | literal | transform() | sql()/agg() | list>` Either applies a transform to sort the mark’s index by the specified channel values, or imputes ordinal scale domains from this mark’s channels.
#' @param stroke `<column | literal | transform() | sql()/agg() | list(value=, ...) | param()>` The stroke; a constant CSS color string, or a channel typically bound to the *color* scale.
#' @param stroke_dasharray `<string | number | param()>` The stroke-dasharray; a constant number indicating the length in pixels of alternating dashes and gaps, or a constant string of numbers separated by spaces or commas (_e.g._, *10 2* for dashes of 10 pixels separated by gaps of 2 pixels), or *none* (the default) for no dashing : https://developer.mozilla.org/en-US/docs/Web/SVG/Attribute/stroke-dasharray
#' @param stroke_dashoffset `<string | number | param()>` The stroke-dashoffset; a constant indicating the offset in pixels of the first dash along the stroke; defaults to zero.
#' @param stroke_linecap `<string | param()>` The stroke-linecap; a constant specifying how to cap stroked paths, such as *butt*, *round*, or *square*.
#' @param stroke_linejoin `<string | param()>` The stroke-linejoin; a constant specifying how to join stroked paths, such as *bevel*, *miter*, *miter-clip*, or *round*.
#' @param stroke_miterlimit `<number | param()>` The stroke-miterlimit; a constant number specifying how to limit the length of *miter* joins on stroked paths.
#' @param stroke_opacity `<column | literal | transform() | sql()/agg() | list(value=, ...)>` The stroke-opacity; a constant between 0 and 1, or a channel typically bound to the *opacity* scale.
#' @param stroke_width `<column | literal | transform() | sql()/agg() | list(value=, ...)>` The stroke-width; a constant number in pixels, or a channel.
#' @param symbol `<column | literal | transform() | sql()/agg() | list(value=, ...) | "asterisk" | "circle" | "cross" | "diamond" | "diamond2" | "hexagon" | "plus" | "square" | "square2" | "star" | ... | param()>` The categorical symbol; either a channel or a constant.
#' @param target `<string | param()>` The target; a constant string specifying the target window (_e.g._, *_blank*) for clickable links; used in conjunction with the **href** option.
#' @param tip `<boolean | "x" | "y" | "xy" | list | param()>` Whether to generate a tooltip for this mark, and any tip options.
#' @param title `<column | literal | transform() | sql()/agg()>` The title; a channel specifying accessible, short textual descriptions as strings (possibly with newlines).
#' @param x `<column | literal | transform() | sql()/agg() | list(value=, ...)>` The horizontal position (or length) channel, typically bound to the *x* scale.
#' @param y `<column | literal | transform() | sql()/agg() | list(value=, ...)>` The vertical position channel, typically bound to the *y* scale; defaults to the zero-based index of the data 0, 1, 2, ….
#' @param z `<column | literal | transform() | sql()/agg()>` An optional ordinal channel for grouping data into (possibly stacked) series to be drawn as separate areas; defaults to **fill** if a channel, or **stroke** if a channel.
#' @param ... Additional options or plot-level attributes.
#' @param data_from The name of the data source this mark reads from (see [vg_data()]); a length-1 nonzero R integer (`1L`, `-1L`, ...; note the `L`) giving a 1-based index into the spec's registered data sources instead, negative counting from the end (e.g., `-1L` for the most recently registered one); or a literal vector of values to use as inline data directly (mosaic-spec's `"data": [...]` shorthand, e.g., for a single reference line -- a bare double like `0`/`c(0)`, or `0L` itself (never a valid index), means this, not an index). Left unset, defaults to the first registered data source (equivalent to `data_from = 1L`) for any mark type that takes data at all.
#' @param filter_by A Param/Selection (e.g., from [param()]) to filter this mark's data by.
#' @param data_optimize A flag (default `TRUE`) to enable mark-specific query optimizations for this mark's data; set `FALSE` to disable them (mosaic-spec's `data: {optimize: false}`).
#' @family mark functions
#' @export
vg_mark_dot <- function(spec = NULL, formula = vg_unset, aria_description = vg_unset, aria_hidden = vg_unset, aria_label = vg_unset, channels = vg_unset, clip = vg_unset, dx = vg_unset, dy = vg_unset, facet = vg_unset, facet_anchor = vg_unset, fill = vg_unset, fill_opacity = vg_unset, filter = vg_unset, frame_anchor = vg_unset, fx = vg_unset, fy = vg_unset, href = vg_unset, image_filter = vg_unset, margin = vg_unset, margin_bottom = vg_unset, margin_left = vg_unset, margin_right = vg_unset, margin_top = vg_unset, mix_blend_mode = vg_unset, opacity = vg_unset, paint_order = vg_unset, pointer_events = vg_unset, r = vg_unset, reverse = vg_unset, rotate = vg_unset, select = vg_unset, shape_rendering = vg_unset, sort = vg_unset, stroke = vg_unset, stroke_dasharray = vg_unset, stroke_dashoffset = vg_unset, stroke_linecap = vg_unset, stroke_linejoin = vg_unset, stroke_miterlimit = vg_unset, stroke_opacity = vg_unset, stroke_width = vg_unset, symbol = vg_unset, target = vg_unset, tip = vg_unset, title = vg_unset, x = vg_unset, y = vg_unset, z = vg_unset, ..., data_from = vg_unset, filter_by = vg_unset, data_optimize = vg_unset) {
  vg_mark_(spec, "dot", formula = formula, ariaDescription = aria_description, ariaHidden = aria_hidden, ariaLabel = aria_label, channels = channels, clip = clip, dx = dx, dy = dy, facet = facet, facetAnchor = facet_anchor, fill = fill, fillOpacity = fill_opacity, filter = filter, frameAnchor = frame_anchor, fx = fx, fy = fy, href = href, imageFilter = image_filter, margin = margin, marginBottom = margin_bottom, marginLeft = margin_left, marginRight = margin_right, marginTop = margin_top, mixBlendMode = mix_blend_mode, opacity = opacity, paintOrder = paint_order, pointerEvents = pointer_events, r = r, reverse = reverse, rotate = rotate, select = select, shapeRendering = shape_rendering, sort = sort, stroke = stroke, strokeDasharray = stroke_dasharray, strokeDashoffset = stroke_dashoffset, strokeLinecap = stroke_linecap, strokeLinejoin = stroke_linejoin, strokeMiterlimit = stroke_miterlimit, strokeOpacity = stroke_opacity, strokeWidth = stroke_width, symbol = symbol, target = target, tip = tip, title = title, x = x, y = y, z = z, ..., data_from = data_from, filter_by = filter_by, data_optimize = data_optimize)
}

#' The dotX mark.
#'
#' See [vg_value_types] for what the `<...>` notation below (`column`, `param()`, ...) means.
#'
#' @param spec A plot fragment or `vgspec` to add this mark to, or `NULL` to start a new plot with just this mark.
#' @param formula A shorthand for this mark's position channels (`x`, `y`, `fx`, `fy`, and paired `x1`/`x2` or `y1`/`y2`), e.g., `Sepal.Length ~ Sepal.Width | ~ Species` for `y = ~Sepal.Length, x = ~Sepal.Width, fx = ~Species`. See [vg_mark()] for the full grammar.
#' @param aria_description `<string | param()>` The aria-description; a constant textual description.
#' @param aria_hidden `<string | param()>` The aria-hidden state; a constant indicating whether the element is exposed to an accessibility API.
#' @param aria_label `<column | literal | transform() | sql()/agg()>` The aria-label; a channel specifying short textual labels representing the value in the accessibility tree.
#' @param channels `<list>` Additional named channels, for example to include in a tooltip.
#' @param clip `<"frame" | "sphere" | boolean | NULL | param()>` How to clip the mark; one of: - *frame* or true - clip to the plot’s frame (inner area) - *sphere* - clip to the projected sphere (*e.g.*, front hemisphere) - null or false - do not clip The *sphere* clip option requires a geographic projection.
#' @param dx `<number | param()>` The horizontal offset in pixels; a constant option.
#' @param dy `<number | param()>` The vertical offset in pixels; a constant option.
#' @param facet `<"auto" | "include" | "exclude" | "super" | boolean | NULL | param()>` Whether to enable or disable faceting; one of: - *auto* (default) - automatically determine if this mark should be faceted - *include* (or true) - draw the subset of the mark’s data in the current facet - *exclude* - draw the subset of the mark’s data *not* in the current facet - *super* - draw this mark in a single frame that covers all facets - null (or false) - repeat this mark’s data across all facets (*i.e.*, no faceting) When a mark uses *super* faceting, it is not allowed to use position scales (*x*, *y*, *fx*, or *fy*); *super* faceting is intended for decorations, such as labels and legends.
#' @param facet_anchor `<"top" | "right" | "bottom" | "left" | "top-left" | "top-right" | "bottom-left" | "bottom-right" | "top-empty" | "right-empty" | "bottom-empty" | "left-empty" | "empty" | NULL | param()>` How to place the mark with respect to facets; one of: - null (default for most marks) - display the mark in each non-empty facet - *top*, *right*, *bottom*, or *left* - display the mark only in facets on the given side - *top-empty*, *right-empty*, *bottom-empty*, or *left-empty* (default for axis marks) - display the mark only in facets that have empty space on the given side: either the margin, or an empty facet - *empty* - display the mark in empty facets only
#' @param fill `<column | literal | transform() | sql()/agg() | list(value=, ...) | param()>` The fill; a constant CSS color string, or a channel typically bound to the *color* scale.
#' @param fill_opacity `<column | literal | transform() | sql()/agg() | list(value=, ...) | param()>` The fill-opacity; a constant number between 0 and 1, or a channel typically bound to the *opacity* scale.
#' @param filter `<column | literal | transform() | sql()/agg()>` Applies a transform to filter the mark’s index according to the given channel values; only truthy values are retained.
#' @param frame_anchor `<"middle" | "top-left" | "top" | "top-right" | "right" | "bottom-right" | "bottom" | "bottom-left" | "left" | param()>` The frame anchor specifies defaults for **x** and **y**, along with **textAnchor** and **lineAnchor**, based on the plot’s frame; it may be one of the four sides (*top*, *right*, *bottom*, *left*), one of the four corners (*top-left*, *top-right*, *bottom-right*, *bottom-left*), or the *middle* of the frame.
#' @param fx `<column | literal | transform() | sql()/agg()>` The horizontal facet position channel, for mark-level faceting, bound to the *fx* scale.
#' @param fy `<column | literal | transform() | sql()/agg()>` The vertical facet position channel, for mark-level faceting, bound to the *fy* scale.
#' @param href `<column | literal | transform() | sql()/agg()>` The href; a channel specifying URLs for clickable links.
#' @param image_filter `<string | param()>` A CSS filter; a constant string used to adjust the rendering of images, such as *blur(5px)*.
#' @param interval `<"day"/"week"/"month"/... | number | param()>` Enforces uniformity for data at regular intervals, such as integer values or daily samples.
#' @param margin `<number | param()>` Shorthand to set the same default for all four mark margins: **marginTop**, **marginRight**, **marginBottom**, and **marginLeft**; typically defaults to 0, except for axis marks.
#' @param margin_bottom `<number | param()>` The mark’s bottom margin; the minimum distance in pixels between the bottom edges of the inner and outer plot area.
#' @param margin_left `<number | param()>` The mark’s left margin; the minimum distance in pixels between the left edges of the inner and outer plot area.
#' @param margin_right `<number | param()>` The mark’s right margin; the minimum distance in pixels between the right edges of the mark’s inner and outer plot area.
#' @param margin_top `<number | param()>` The mark’s top margin; the minimum distance in pixels between the top edges of the inner and outer plot area.
#' @param mix_blend_mode `<string | param()>` The mix-blend-mode; a constant string specifying how to blend content such as *multiply*.
#' @param opacity `<column | literal | transform() | sql()/agg() | list(value=, ...)>` The opacity; a constant between 0 and 1, or a channel typically bound to the *opacity* scale.
#' @param paint_order `<string | param()>` The paint-order; a constant string specifying the order in which the **fill**, **stroke**, and any markers are drawn; defaults to *normal*, which draws the fill, then stroke, then markers; defaults to *stroke* for the text mark to create a “halo” around text to improve legibility.
#' @param pointer_events `<string | param()>` The pointer-events property; a constant string such as *none*.
#' @param r `<column | literal | transform() | sql()/agg() | list(value=, ...) | number | param()>` The radius of dots; either a channel or constant.
#' @param reverse `<boolean | param()>` Applies a transform to reverse the order of the mark’s index, say for reverse input order.
#' @param rotate `<column | literal | transform() | sql()/agg() | param()>` The rotation angle in degrees clockwise; a constant or a channel; defaults to 0°.
#' @param select `<"first" | "last" | "maxX" | "maxY" | "minX" | "minY" | "nearest" | "nearestX" | "nearestY">` Applies a filter transform after data is loaded to highlight selected values only.
#' @param shape_rendering `<string | param()>` The shape-rendering; a constant string such as *crispEdges*.
#' @param sort `<column | literal | transform() | sql()/agg() | list>` Either applies a transform to sort the mark’s index by the specified channel values, or imputes ordinal scale domains from this mark’s channels.
#' @param stroke `<column | literal | transform() | sql()/agg() | list(value=, ...) | param()>` The stroke; a constant CSS color string, or a channel typically bound to the *color* scale.
#' @param stroke_dasharray `<string | number | param()>` The stroke-dasharray; a constant number indicating the length in pixels of alternating dashes and gaps, or a constant string of numbers separated by spaces or commas (_e.g._, *10 2* for dashes of 10 pixels separated by gaps of 2 pixels), or *none* (the default) for no dashing : https://developer.mozilla.org/en-US/docs/Web/SVG/Attribute/stroke-dasharray
#' @param stroke_dashoffset `<string | number | param()>` The stroke-dashoffset; a constant indicating the offset in pixels of the first dash along the stroke; defaults to zero.
#' @param stroke_linecap `<string | param()>` The stroke-linecap; a constant specifying how to cap stroked paths, such as *butt*, *round*, or *square*.
#' @param stroke_linejoin `<string | param()>` The stroke-linejoin; a constant specifying how to join stroked paths, such as *bevel*, *miter*, *miter-clip*, or *round*.
#' @param stroke_miterlimit `<number | param()>` The stroke-miterlimit; a constant number specifying how to limit the length of *miter* joins on stroked paths.
#' @param stroke_opacity `<column | literal | transform() | sql()/agg() | list(value=, ...)>` The stroke-opacity; a constant between 0 and 1, or a channel typically bound to the *opacity* scale.
#' @param stroke_width `<column | literal | transform() | sql()/agg() | list(value=, ...)>` The stroke-width; a constant number in pixels, or a channel.
#' @param symbol `<column | literal | transform() | sql()/agg() | list(value=, ...) | "asterisk" | "circle" | "cross" | "diamond" | "diamond2" | "hexagon" | "plus" | "square" | "square2" | "star" | ... | param()>` The categorical symbol; either a channel or a constant.
#' @param target `<string | param()>` The target; a constant string specifying the target window (_e.g._, *_blank*) for clickable links; used in conjunction with the **href** option.
#' @param tip `<boolean | "x" | "y" | "xy" | list | param()>` Whether to generate a tooltip for this mark, and any tip options.
#' @param title `<column | literal | transform() | sql()/agg()>` The title; a channel specifying accessible, short textual descriptions as strings (possibly with newlines).
#' @param x `<column | literal | transform() | sql()/agg() | list(value=, ...)>` The horizontal position (or length) channel, typically bound to the *x* scale.
#' @param y `<column | literal | transform() | sql()/agg() | list(value=, ...)>` The vertical position channel, typically bound to the *y* scale; defaults to the zero-based index of the data 0, 1, 2, ….
#' @param z `<column | literal | transform() | sql()/agg()>` An optional ordinal channel for grouping data into (possibly stacked) series to be drawn as separate areas; defaults to **fill** if a channel, or **stroke** if a channel.
#' @param ... Additional options or plot-level attributes.
#' @param data_from The name of the data source this mark reads from (see [vg_data()]); a length-1 nonzero R integer (`1L`, `-1L`, ...; note the `L`) giving a 1-based index into the spec's registered data sources instead, negative counting from the end (e.g., `-1L` for the most recently registered one); or a literal vector of values to use as inline data directly (mosaic-spec's `"data": [...]` shorthand, e.g., for a single reference line -- a bare double like `0`/`c(0)`, or `0L` itself (never a valid index), means this, not an index). Left unset, defaults to the first registered data source (equivalent to `data_from = 1L`) for any mark type that takes data at all.
#' @param filter_by A Param/Selection (e.g., from [param()]) to filter this mark's data by.
#' @param data_optimize A flag (default `TRUE`) to enable mark-specific query optimizations for this mark's data; set `FALSE` to disable them (mosaic-spec's `data: {optimize: false}`).
#' @family mark functions
#' @export
vg_mark_dot_x <- function(spec = NULL, formula = vg_unset, aria_description = vg_unset, aria_hidden = vg_unset, aria_label = vg_unset, channels = vg_unset, clip = vg_unset, dx = vg_unset, dy = vg_unset, facet = vg_unset, facet_anchor = vg_unset, fill = vg_unset, fill_opacity = vg_unset, filter = vg_unset, frame_anchor = vg_unset, fx = vg_unset, fy = vg_unset, href = vg_unset, image_filter = vg_unset, interval = vg_unset, margin = vg_unset, margin_bottom = vg_unset, margin_left = vg_unset, margin_right = vg_unset, margin_top = vg_unset, mix_blend_mode = vg_unset, opacity = vg_unset, paint_order = vg_unset, pointer_events = vg_unset, r = vg_unset, reverse = vg_unset, rotate = vg_unset, select = vg_unset, shape_rendering = vg_unset, sort = vg_unset, stroke = vg_unset, stroke_dasharray = vg_unset, stroke_dashoffset = vg_unset, stroke_linecap = vg_unset, stroke_linejoin = vg_unset, stroke_miterlimit = vg_unset, stroke_opacity = vg_unset, stroke_width = vg_unset, symbol = vg_unset, target = vg_unset, tip = vg_unset, title = vg_unset, x = vg_unset, y = vg_unset, z = vg_unset, ..., data_from = vg_unset, filter_by = vg_unset, data_optimize = vg_unset) {
  vg_mark_(spec, "dotX", formula = formula, ariaDescription = aria_description, ariaHidden = aria_hidden, ariaLabel = aria_label, channels = channels, clip = clip, dx = dx, dy = dy, facet = facet, facetAnchor = facet_anchor, fill = fill, fillOpacity = fill_opacity, filter = filter, frameAnchor = frame_anchor, fx = fx, fy = fy, href = href, imageFilter = image_filter, interval = interval, margin = margin, marginBottom = margin_bottom, marginLeft = margin_left, marginRight = margin_right, marginTop = margin_top, mixBlendMode = mix_blend_mode, opacity = opacity, paintOrder = paint_order, pointerEvents = pointer_events, r = r, reverse = reverse, rotate = rotate, select = select, shapeRendering = shape_rendering, sort = sort, stroke = stroke, strokeDasharray = stroke_dasharray, strokeDashoffset = stroke_dashoffset, strokeLinecap = stroke_linecap, strokeLinejoin = stroke_linejoin, strokeMiterlimit = stroke_miterlimit, strokeOpacity = stroke_opacity, strokeWidth = stroke_width, symbol = symbol, target = target, tip = tip, title = title, x = x, y = y, z = z, ..., data_from = data_from, filter_by = filter_by, data_optimize = data_optimize)
}

#' The dotY mark.
#'
#' See [vg_value_types] for what the `<...>` notation below (`column`, `param()`, ...) means.
#'
#' @param spec A plot fragment or `vgspec` to add this mark to, or `NULL` to start a new plot with just this mark.
#' @param formula A shorthand for this mark's position channels (`x`, `y`, `fx`, `fy`, and paired `x1`/`x2` or `y1`/`y2`), e.g., `Sepal.Length ~ Sepal.Width | ~ Species` for `y = ~Sepal.Length, x = ~Sepal.Width, fx = ~Species`. See [vg_mark()] for the full grammar.
#' @param aria_description `<string | param()>` The aria-description; a constant textual description.
#' @param aria_hidden `<string | param()>` The aria-hidden state; a constant indicating whether the element is exposed to an accessibility API.
#' @param aria_label `<column | literal | transform() | sql()/agg()>` The aria-label; a channel specifying short textual labels representing the value in the accessibility tree.
#' @param channels `<list>` Additional named channels, for example to include in a tooltip.
#' @param clip `<"frame" | "sphere" | boolean | NULL | param()>` How to clip the mark; one of: - *frame* or true - clip to the plot’s frame (inner area) - *sphere* - clip to the projected sphere (*e.g.*, front hemisphere) - null or false - do not clip The *sphere* clip option requires a geographic projection.
#' @param dx `<number | param()>` The horizontal offset in pixels; a constant option.
#' @param dy `<number | param()>` The vertical offset in pixels; a constant option.
#' @param facet `<"auto" | "include" | "exclude" | "super" | boolean | NULL | param()>` Whether to enable or disable faceting; one of: - *auto* (default) - automatically determine if this mark should be faceted - *include* (or true) - draw the subset of the mark’s data in the current facet - *exclude* - draw the subset of the mark’s data *not* in the current facet - *super* - draw this mark in a single frame that covers all facets - null (or false) - repeat this mark’s data across all facets (*i.e.*, no faceting) When a mark uses *super* faceting, it is not allowed to use position scales (*x*, *y*, *fx*, or *fy*); *super* faceting is intended for decorations, such as labels and legends.
#' @param facet_anchor `<"top" | "right" | "bottom" | "left" | "top-left" | "top-right" | "bottom-left" | "bottom-right" | "top-empty" | "right-empty" | "bottom-empty" | "left-empty" | "empty" | NULL | param()>` How to place the mark with respect to facets; one of: - null (default for most marks) - display the mark in each non-empty facet - *top*, *right*, *bottom*, or *left* - display the mark only in facets on the given side - *top-empty*, *right-empty*, *bottom-empty*, or *left-empty* (default for axis marks) - display the mark only in facets that have empty space on the given side: either the margin, or an empty facet - *empty* - display the mark in empty facets only
#' @param fill `<column | literal | transform() | sql()/agg() | list(value=, ...) | param()>` The fill; a constant CSS color string, or a channel typically bound to the *color* scale.
#' @param fill_opacity `<column | literal | transform() | sql()/agg() | list(value=, ...) | param()>` The fill-opacity; a constant number between 0 and 1, or a channel typically bound to the *opacity* scale.
#' @param filter `<column | literal | transform() | sql()/agg()>` Applies a transform to filter the mark’s index according to the given channel values; only truthy values are retained.
#' @param frame_anchor `<"middle" | "top-left" | "top" | "top-right" | "right" | "bottom-right" | "bottom" | "bottom-left" | "left" | param()>` The frame anchor specifies defaults for **x** and **y**, along with **textAnchor** and **lineAnchor**, based on the plot’s frame; it may be one of the four sides (*top*, *right*, *bottom*, *left*), one of the four corners (*top-left*, *top-right*, *bottom-right*, *bottom-left*), or the *middle* of the frame.
#' @param fx `<column | literal | transform() | sql()/agg()>` The horizontal facet position channel, for mark-level faceting, bound to the *fx* scale.
#' @param fy `<column | literal | transform() | sql()/agg()>` The vertical facet position channel, for mark-level faceting, bound to the *fy* scale.
#' @param href `<column | literal | transform() | sql()/agg()>` The href; a channel specifying URLs for clickable links.
#' @param image_filter `<string | param()>` A CSS filter; a constant string used to adjust the rendering of images, such as *blur(5px)*.
#' @param interval `<"day"/"week"/"month"/... | number | param()>` Enforces uniformity for data at regular intervals, such as integer values or daily samples.
#' @param margin `<number | param()>` Shorthand to set the same default for all four mark margins: **marginTop**, **marginRight**, **marginBottom**, and **marginLeft**; typically defaults to 0, except for axis marks.
#' @param margin_bottom `<number | param()>` The mark’s bottom margin; the minimum distance in pixels between the bottom edges of the inner and outer plot area.
#' @param margin_left `<number | param()>` The mark’s left margin; the minimum distance in pixels between the left edges of the inner and outer plot area.
#' @param margin_right `<number | param()>` The mark’s right margin; the minimum distance in pixels between the right edges of the mark’s inner and outer plot area.
#' @param margin_top `<number | param()>` The mark’s top margin; the minimum distance in pixels between the top edges of the inner and outer plot area.
#' @param mix_blend_mode `<string | param()>` The mix-blend-mode; a constant string specifying how to blend content such as *multiply*.
#' @param opacity `<column | literal | transform() | sql()/agg() | list(value=, ...)>` The opacity; a constant between 0 and 1, or a channel typically bound to the *opacity* scale.
#' @param paint_order `<string | param()>` The paint-order; a constant string specifying the order in which the **fill**, **stroke**, and any markers are drawn; defaults to *normal*, which draws the fill, then stroke, then markers; defaults to *stroke* for the text mark to create a “halo” around text to improve legibility.
#' @param pointer_events `<string | param()>` The pointer-events property; a constant string such as *none*.
#' @param r `<column | literal | transform() | sql()/agg() | list(value=, ...) | number | param()>` The radius of dots; either a channel or constant.
#' @param reverse `<boolean | param()>` Applies a transform to reverse the order of the mark’s index, say for reverse input order.
#' @param rotate `<column | literal | transform() | sql()/agg() | param()>` The rotation angle in degrees clockwise; a constant or a channel; defaults to 0°.
#' @param select `<"first" | "last" | "maxX" | "maxY" | "minX" | "minY" | "nearest" | "nearestX" | "nearestY">` Applies a filter transform after data is loaded to highlight selected values only.
#' @param shape_rendering `<string | param()>` The shape-rendering; a constant string such as *crispEdges*.
#' @param sort `<column | literal | transform() | sql()/agg() | list>` Either applies a transform to sort the mark’s index by the specified channel values, or imputes ordinal scale domains from this mark’s channels.
#' @param stroke `<column | literal | transform() | sql()/agg() | list(value=, ...) | param()>` The stroke; a constant CSS color string, or a channel typically bound to the *color* scale.
#' @param stroke_dasharray `<string | number | param()>` The stroke-dasharray; a constant number indicating the length in pixels of alternating dashes and gaps, or a constant string of numbers separated by spaces or commas (_e.g._, *10 2* for dashes of 10 pixels separated by gaps of 2 pixels), or *none* (the default) for no dashing : https://developer.mozilla.org/en-US/docs/Web/SVG/Attribute/stroke-dasharray
#' @param stroke_dashoffset `<string | number | param()>` The stroke-dashoffset; a constant indicating the offset in pixels of the first dash along the stroke; defaults to zero.
#' @param stroke_linecap `<string | param()>` The stroke-linecap; a constant specifying how to cap stroked paths, such as *butt*, *round*, or *square*.
#' @param stroke_linejoin `<string | param()>` The stroke-linejoin; a constant specifying how to join stroked paths, such as *bevel*, *miter*, *miter-clip*, or *round*.
#' @param stroke_miterlimit `<number | param()>` The stroke-miterlimit; a constant number specifying how to limit the length of *miter* joins on stroked paths.
#' @param stroke_opacity `<column | literal | transform() | sql()/agg() | list(value=, ...)>` The stroke-opacity; a constant between 0 and 1, or a channel typically bound to the *opacity* scale.
#' @param stroke_width `<column | literal | transform() | sql()/agg() | list(value=, ...)>` The stroke-width; a constant number in pixels, or a channel.
#' @param symbol `<column | literal | transform() | sql()/agg() | list(value=, ...) | "asterisk" | "circle" | "cross" | "diamond" | "diamond2" | "hexagon" | "plus" | "square" | "square2" | "star" | ... | param()>` The categorical symbol; either a channel or a constant.
#' @param target `<string | param()>` The target; a constant string specifying the target window (_e.g._, *_blank*) for clickable links; used in conjunction with the **href** option.
#' @param tip `<boolean | "x" | "y" | "xy" | list | param()>` Whether to generate a tooltip for this mark, and any tip options.
#' @param title `<column | literal | transform() | sql()/agg()>` The title; a channel specifying accessible, short textual descriptions as strings (possibly with newlines).
#' @param x `<column | literal | transform() | sql()/agg() | list(value=, ...)>` The horizontal position (or length) channel, typically bound to the *x* scale.
#' @param y `<column | literal | transform() | sql()/agg() | list(value=, ...)>` The vertical position channel, typically bound to the *y* scale; defaults to the zero-based index of the data 0, 1, 2, ….
#' @param z `<column | literal | transform() | sql()/agg()>` An optional ordinal channel for grouping data into (possibly stacked) series to be drawn as separate areas; defaults to **fill** if a channel, or **stroke** if a channel.
#' @param ... Additional options or plot-level attributes.
#' @param data_from The name of the data source this mark reads from (see [vg_data()]); a length-1 nonzero R integer (`1L`, `-1L`, ...; note the `L`) giving a 1-based index into the spec's registered data sources instead, negative counting from the end (e.g., `-1L` for the most recently registered one); or a literal vector of values to use as inline data directly (mosaic-spec's `"data": [...]` shorthand, e.g., for a single reference line -- a bare double like `0`/`c(0)`, or `0L` itself (never a valid index), means this, not an index). Left unset, defaults to the first registered data source (equivalent to `data_from = 1L`) for any mark type that takes data at all.
#' @param filter_by A Param/Selection (e.g., from [param()]) to filter this mark's data by.
#' @param data_optimize A flag (default `TRUE`) to enable mark-specific query optimizations for this mark's data; set `FALSE` to disable them (mosaic-spec's `data: {optimize: false}`).
#' @family mark functions
#' @export
vg_mark_dot_y <- function(spec = NULL, formula = vg_unset, aria_description = vg_unset, aria_hidden = vg_unset, aria_label = vg_unset, channels = vg_unset, clip = vg_unset, dx = vg_unset, dy = vg_unset, facet = vg_unset, facet_anchor = vg_unset, fill = vg_unset, fill_opacity = vg_unset, filter = vg_unset, frame_anchor = vg_unset, fx = vg_unset, fy = vg_unset, href = vg_unset, image_filter = vg_unset, interval = vg_unset, margin = vg_unset, margin_bottom = vg_unset, margin_left = vg_unset, margin_right = vg_unset, margin_top = vg_unset, mix_blend_mode = vg_unset, opacity = vg_unset, paint_order = vg_unset, pointer_events = vg_unset, r = vg_unset, reverse = vg_unset, rotate = vg_unset, select = vg_unset, shape_rendering = vg_unset, sort = vg_unset, stroke = vg_unset, stroke_dasharray = vg_unset, stroke_dashoffset = vg_unset, stroke_linecap = vg_unset, stroke_linejoin = vg_unset, stroke_miterlimit = vg_unset, stroke_opacity = vg_unset, stroke_width = vg_unset, symbol = vg_unset, target = vg_unset, tip = vg_unset, title = vg_unset, x = vg_unset, y = vg_unset, z = vg_unset, ..., data_from = vg_unset, filter_by = vg_unset, data_optimize = vg_unset) {
  vg_mark_(spec, "dotY", formula = formula, ariaDescription = aria_description, ariaHidden = aria_hidden, ariaLabel = aria_label, channels = channels, clip = clip, dx = dx, dy = dy, facet = facet, facetAnchor = facet_anchor, fill = fill, fillOpacity = fill_opacity, filter = filter, frameAnchor = frame_anchor, fx = fx, fy = fy, href = href, imageFilter = image_filter, interval = interval, margin = margin, marginBottom = margin_bottom, marginLeft = margin_left, marginRight = margin_right, marginTop = margin_top, mixBlendMode = mix_blend_mode, opacity = opacity, paintOrder = paint_order, pointerEvents = pointer_events, r = r, reverse = reverse, rotate = rotate, select = select, shapeRendering = shape_rendering, sort = sort, stroke = stroke, strokeDasharray = stroke_dasharray, strokeDashoffset = stroke_dashoffset, strokeLinecap = stroke_linecap, strokeLinejoin = stroke_linejoin, strokeMiterlimit = stroke_miterlimit, strokeOpacity = stroke_opacity, strokeWidth = stroke_width, symbol = symbol, target = target, tip = tip, title = title, x = x, y = y, z = z, ..., data_from = data_from, filter_by = filter_by, data_optimize = data_optimize)
}

#' The errorbarX mark.
#'
#' See [vg_value_types] for what the `<...>` notation below (`column`, `param()`, ...) means.
#'
#' @param spec A plot fragment or `vgspec` to add this mark to, or `NULL` to start a new plot with just this mark.
#' @param formula A shorthand for this mark's position channels (`x`, `y`, `fx`, `fy`, and paired `x1`/`x2` or `y1`/`y2`), e.g., `Sepal.Length ~ Sepal.Width | ~ Species` for `y = ~Sepal.Length, x = ~Sepal.Width, fx = ~Species`. See [vg_mark()] for the full grammar.
#' @param x `<column | literal | transform() | sql()/agg() | list(value=, ...)>` The horizontal position (or length) channel, typically bound to the *x* scale.
#' @param aria_description `<string | param()>` The aria-description; a constant textual description.
#' @param aria_hidden `<string | param()>` The aria-hidden state; a constant indicating whether the element is exposed to an accessibility API.
#' @param aria_label `<column | literal | transform() | sql()/agg()>` The aria-label; a channel specifying short textual labels representing the value in the accessibility tree.
#' @param channels `<list>` Additional named channels, for example to include in a tooltip.
#' @param ci `<number | param()>` The confidence interval in (0, 1); defaults to 0.95.
#' @param clip `<"frame" | "sphere" | boolean | NULL | param()>` How to clip the mark; one of: - *frame* or true - clip to the plot’s frame (inner area) - *sphere* - clip to the projected sphere (*e.g.*, front hemisphere) - null or false - do not clip The *sphere* clip option requires a geographic projection.
#' @param dx `<number | param()>` The horizontal offset in pixels; a constant option.
#' @param dy `<number | param()>` The vertical offset in pixels; a constant option.
#' @param facet `<"auto" | "include" | "exclude" | "super" | boolean | NULL | param()>` Whether to enable or disable faceting; one of: - *auto* (default) - automatically determine if this mark should be faceted - *include* (or true) - draw the subset of the mark’s data in the current facet - *exclude* - draw the subset of the mark’s data *not* in the current facet - *super* - draw this mark in a single frame that covers all facets - null (or false) - repeat this mark’s data across all facets (*i.e.*, no faceting) When a mark uses *super* faceting, it is not allowed to use position scales (*x*, *y*, *fx*, or *fy*); *super* faceting is intended for decorations, such as labels and legends.
#' @param facet_anchor `<"top" | "right" | "bottom" | "left" | "top-left" | "top-right" | "bottom-left" | "bottom-right" | "top-empty" | "right-empty" | "bottom-empty" | "left-empty" | "empty" | NULL | param()>` How to place the mark with respect to facets; one of: - null (default for most marks) - display the mark in each non-empty facet - *top*, *right*, *bottom*, or *left* - display the mark only in facets on the given side - *top-empty*, *right-empty*, *bottom-empty*, or *left-empty* (default for axis marks) - display the mark only in facets that have empty space on the given side: either the margin, or an empty facet - *empty* - display the mark in empty facets only
#' @param fill `<column | literal | transform() | sql()/agg() | list(value=, ...) | param()>` The fill; a constant CSS color string, or a channel typically bound to the *color* scale.
#' @param fill_opacity `<column | literal | transform() | sql()/agg() | list(value=, ...) | param()>` The fill-opacity; a constant number between 0 and 1, or a channel typically bound to the *opacity* scale.
#' @param filter `<column | literal | transform() | sql()/agg()>` Applies a transform to filter the mark’s index according to the given channel values; only truthy values are retained.
#' @param fx `<column | literal | transform() | sql()/agg()>` The horizontal facet position channel, for mark-level faceting, bound to the *fx* scale.
#' @param fy `<column | literal | transform() | sql()/agg()>` The vertical facet position channel, for mark-level faceting, bound to the *fy* scale.
#' @param href `<column | literal | transform() | sql()/agg()>` The href; a channel specifying URLs for clickable links.
#' @param image_filter `<string | param()>` A CSS filter; a constant string used to adjust the rendering of images, such as *blur(5px)*.
#' @param margin `<number | param()>` Shorthand to set the same default for all four mark margins: **marginTop**, **marginRight**, **marginBottom**, and **marginLeft**; typically defaults to 0, except for axis marks.
#' @param margin_bottom `<number | param()>` The mark’s bottom margin; the minimum distance in pixels between the bottom edges of the inner and outer plot area.
#' @param margin_left `<number | param()>` The mark’s left margin; the minimum distance in pixels between the left edges of the inner and outer plot area.
#' @param margin_right `<number | param()>` The mark’s right margin; the minimum distance in pixels between the right edges of the mark’s inner and outer plot area.
#' @param margin_top `<number | param()>` The mark’s top margin; the minimum distance in pixels between the top edges of the inner and outer plot area.
#' @param marker `<"arrow" | "arrow-reverse" | "dot" | "circle" | "circle-fill" | "circle-stroke" | "tick" | "tick-x" | "tick-y" | "none" | boolean | NULL | param()>` Shorthand to set the same default for markerStart, markerMid, and markerEnd; one of: - a marker name such as *arrow* or *circle* - *none* (default) - no marker * true - alias for *circle-fill* * false or null - alias for *none*
#' @param marker_end `<"arrow" | "arrow-reverse" | "dot" | "circle" | "circle-fill" | "circle-stroke" | "tick" | "tick-x" | "tick-y" | "none" | boolean | NULL | param()>` The marker for the ending point of a line segment; one of: - a marker name such as *arrow* or *circle* * *none* (default) - no marker * true - alias for *circle-fill* * false or null - alias for *none*
#' @param marker_mid `<"arrow" | "arrow-reverse" | "dot" | "circle" | "circle-fill" | "circle-stroke" | "tick" | "tick-x" | "tick-y" | "none" | boolean | NULL | param()>` The marker for any middle (interior) points of a line segment.
#' @param marker_start `<"arrow" | "arrow-reverse" | "dot" | "circle" | "circle-fill" | "circle-stroke" | "tick" | "tick-x" | "tick-y" | "none" | boolean | NULL | param()>` The marker for the starting point of a line segment; one of: - a marker name such as *arrow* or *circle* * *none* (default) - no marker * true - alias for *circle-fill* * false or null - alias for *none*
#' @param mix_blend_mode `<string | param()>` The mix-blend-mode; a constant string specifying how to blend content such as *multiply*.
#' @param opacity `<column | literal | transform() | sql()/agg() | list(value=, ...)>` The opacity; a constant between 0 and 1, or a channel typically bound to the *opacity* scale.
#' @param paint_order `<string | param()>` The paint-order; a constant string specifying the order in which the **fill**, **stroke**, and any markers are drawn; defaults to *normal*, which draws the fill, then stroke, then markers; defaults to *stroke* for the text mark to create a “halo” around text to improve legibility.
#' @param pointer_events `<string | param()>` The pointer-events property; a constant string such as *none*.
#' @param reverse `<boolean | param()>` Applies a transform to reverse the order of the mark’s index, say for reverse input order.
#' @param select `<"first" | "last" | "maxX" | "maxY" | "minX" | "minY" | "nearest" | "nearestX" | "nearestY">` Applies a filter transform after data is loaded to highlight selected values only.
#' @param shape_rendering `<string | param()>` The shape-rendering; a constant string such as *crispEdges*.
#' @param sort `<column | literal | transform() | sql()/agg() | list>` Either applies a transform to sort the mark’s index by the specified channel values, or imputes ordinal scale domains from this mark’s channels.
#' @param stroke `<column | literal | transform() | sql()/agg() | list(value=, ...) | param()>` The stroke; a constant CSS color string, or a channel typically bound to the *color* scale.
#' @param stroke_dasharray `<string | number | param()>` The stroke-dasharray; a constant number indicating the length in pixels of alternating dashes and gaps, or a constant string of numbers separated by spaces or commas (_e.g._, *10 2* for dashes of 10 pixels separated by gaps of 2 pixels), or *none* (the default) for no dashing : https://developer.mozilla.org/en-US/docs/Web/SVG/Attribute/stroke-dasharray
#' @param stroke_dashoffset `<string | number | param()>` The stroke-dashoffset; a constant indicating the offset in pixels of the first dash along the stroke; defaults to zero.
#' @param stroke_linecap `<string | param()>` The stroke-linecap; a constant specifying how to cap stroked paths, such as *butt*, *round*, or *square*.
#' @param stroke_linejoin `<string | param()>` The stroke-linejoin; a constant specifying how to join stroked paths, such as *bevel*, *miter*, *miter-clip*, or *round*.
#' @param stroke_miterlimit `<number | param()>` The stroke-miterlimit; a constant number specifying how to limit the length of *miter* joins on stroked paths.
#' @param stroke_opacity `<column | literal | transform() | sql()/agg() | list(value=, ...)>` The stroke-opacity; a constant between 0 and 1, or a channel typically bound to the *opacity* scale.
#' @param stroke_width `<column | literal | transform() | sql()/agg() | list(value=, ...)>` The stroke-width; a constant number in pixels, or a channel.
#' @param target `<string | param()>` The target; a constant string specifying the target window (_e.g._, *_blank*) for clickable links; used in conjunction with the **href** option.
#' @param tip `<boolean | "x" | "y" | "xy" | list | param()>` Whether to generate a tooltip for this mark, and any tip options.
#' @param title `<column | literal | transform() | sql()/agg()>` The title; a channel specifying accessible, short textual descriptions as strings (possibly with newlines).
#' @param y `<column | literal | transform() | sql()/agg() | list(value=, ...)>` The vertical position channel, typically bound to the *y* scale; defaults to the zero-based index of the data 0, 1, 2, ….
#' @param z `<column | literal | transform() | sql()/agg()>` An optional ordinal channel for grouping data into (possibly stacked) series to be drawn as separate areas; defaults to **fill** if a channel, or **stroke** if a channel.
#' @param ... Additional options or plot-level attributes.
#' @param data_from The name of the data source this mark reads from (see [vg_data()]); a length-1 nonzero R integer (`1L`, `-1L`, ...; note the `L`) giving a 1-based index into the spec's registered data sources instead, negative counting from the end (e.g., `-1L` for the most recently registered one); or a literal vector of values to use as inline data directly (mosaic-spec's `"data": [...]` shorthand, e.g., for a single reference line -- a bare double like `0`/`c(0)`, or `0L` itself (never a valid index), means this, not an index). Left unset, defaults to the first registered data source (equivalent to `data_from = 1L`) for any mark type that takes data at all.
#' @param filter_by A Param/Selection (e.g., from [param()]) to filter this mark's data by.
#' @param data_optimize A flag (default `TRUE`) to enable mark-specific query optimizations for this mark's data; set `FALSE` to disable them (mosaic-spec's `data: {optimize: false}`).
#' @family mark functions
#' @export
vg_mark_errorbar_x <- function(spec = NULL, formula = vg_unset, x, aria_description = vg_unset, aria_hidden = vg_unset, aria_label = vg_unset, channels = vg_unset, ci = vg_unset, clip = vg_unset, dx = vg_unset, dy = vg_unset, facet = vg_unset, facet_anchor = vg_unset, fill = vg_unset, fill_opacity = vg_unset, filter = vg_unset, fx = vg_unset, fy = vg_unset, href = vg_unset, image_filter = vg_unset, margin = vg_unset, margin_bottom = vg_unset, margin_left = vg_unset, margin_right = vg_unset, margin_top = vg_unset, marker = vg_unset, marker_end = vg_unset, marker_mid = vg_unset, marker_start = vg_unset, mix_blend_mode = vg_unset, opacity = vg_unset, paint_order = vg_unset, pointer_events = vg_unset, reverse = vg_unset, select = vg_unset, shape_rendering = vg_unset, sort = vg_unset, stroke = vg_unset, stroke_dasharray = vg_unset, stroke_dashoffset = vg_unset, stroke_linecap = vg_unset, stroke_linejoin = vg_unset, stroke_miterlimit = vg_unset, stroke_opacity = vg_unset, stroke_width = vg_unset, target = vg_unset, tip = vg_unset, title = vg_unset, y = vg_unset, z = vg_unset, ..., data_from = vg_unset, filter_by = vg_unset, data_optimize = vg_unset) {
  vg_mark_(spec, "errorbarX", formula = formula, x = x, ariaDescription = aria_description, ariaHidden = aria_hidden, ariaLabel = aria_label, channels = channels, ci = ci, clip = clip, dx = dx, dy = dy, facet = facet, facetAnchor = facet_anchor, fill = fill, fillOpacity = fill_opacity, filter = filter, fx = fx, fy = fy, href = href, imageFilter = image_filter, margin = margin, marginBottom = margin_bottom, marginLeft = margin_left, marginRight = margin_right, marginTop = margin_top, marker = marker, markerEnd = marker_end, markerMid = marker_mid, markerStart = marker_start, mixBlendMode = mix_blend_mode, opacity = opacity, paintOrder = paint_order, pointerEvents = pointer_events, reverse = reverse, select = select, shapeRendering = shape_rendering, sort = sort, stroke = stroke, strokeDasharray = stroke_dasharray, strokeDashoffset = stroke_dashoffset, strokeLinecap = stroke_linecap, strokeLinejoin = stroke_linejoin, strokeMiterlimit = stroke_miterlimit, strokeOpacity = stroke_opacity, strokeWidth = stroke_width, target = target, tip = tip, title = title, y = y, z = z, ..., data_from = data_from, filter_by = filter_by, data_optimize = data_optimize)
}

#' The errorbarY mark.
#'
#' See [vg_value_types] for what the `<...>` notation below (`column`, `param()`, ...) means.
#'
#' @param spec A plot fragment or `vgspec` to add this mark to, or `NULL` to start a new plot with just this mark.
#' @param formula A shorthand for this mark's position channels (`x`, `y`, `fx`, `fy`, and paired `x1`/`x2` or `y1`/`y2`), e.g., `Sepal.Length ~ Sepal.Width | ~ Species` for `y = ~Sepal.Length, x = ~Sepal.Width, fx = ~Species`. See [vg_mark()] for the full grammar.
#' @param y `<column | literal | transform() | sql()/agg() | list(value=, ...)>` The vertical position channel, typically bound to the *y* scale; defaults to the zero-based index of the data 0, 1, 2, ….
#' @param aria_description `<string | param()>` The aria-description; a constant textual description.
#' @param aria_hidden `<string | param()>` The aria-hidden state; a constant indicating whether the element is exposed to an accessibility API.
#' @param aria_label `<column | literal | transform() | sql()/agg()>` The aria-label; a channel specifying short textual labels representing the value in the accessibility tree.
#' @param channels `<list>` Additional named channels, for example to include in a tooltip.
#' @param ci `<number | param()>` The confidence interval in (0, 1); defaults to 0.95.
#' @param clip `<"frame" | "sphere" | boolean | NULL | param()>` How to clip the mark; one of: - *frame* or true - clip to the plot’s frame (inner area) - *sphere* - clip to the projected sphere (*e.g.*, front hemisphere) - null or false - do not clip The *sphere* clip option requires a geographic projection.
#' @param dx `<number | param()>` The horizontal offset in pixels; a constant option.
#' @param dy `<number | param()>` The vertical offset in pixels; a constant option.
#' @param facet `<"auto" | "include" | "exclude" | "super" | boolean | NULL | param()>` Whether to enable or disable faceting; one of: - *auto* (default) - automatically determine if this mark should be faceted - *include* (or true) - draw the subset of the mark’s data in the current facet - *exclude* - draw the subset of the mark’s data *not* in the current facet - *super* - draw this mark in a single frame that covers all facets - null (or false) - repeat this mark’s data across all facets (*i.e.*, no faceting) When a mark uses *super* faceting, it is not allowed to use position scales (*x*, *y*, *fx*, or *fy*); *super* faceting is intended for decorations, such as labels and legends.
#' @param facet_anchor `<"top" | "right" | "bottom" | "left" | "top-left" | "top-right" | "bottom-left" | "bottom-right" | "top-empty" | "right-empty" | "bottom-empty" | "left-empty" | "empty" | NULL | param()>` How to place the mark with respect to facets; one of: - null (default for most marks) - display the mark in each non-empty facet - *top*, *right*, *bottom*, or *left* - display the mark only in facets on the given side - *top-empty*, *right-empty*, *bottom-empty*, or *left-empty* (default for axis marks) - display the mark only in facets that have empty space on the given side: either the margin, or an empty facet - *empty* - display the mark in empty facets only
#' @param fill `<column | literal | transform() | sql()/agg() | list(value=, ...) | param()>` The fill; a constant CSS color string, or a channel typically bound to the *color* scale.
#' @param fill_opacity `<column | literal | transform() | sql()/agg() | list(value=, ...) | param()>` The fill-opacity; a constant number between 0 and 1, or a channel typically bound to the *opacity* scale.
#' @param filter `<column | literal | transform() | sql()/agg()>` Applies a transform to filter the mark’s index according to the given channel values; only truthy values are retained.
#' @param fx `<column | literal | transform() | sql()/agg()>` The horizontal facet position channel, for mark-level faceting, bound to the *fx* scale.
#' @param fy `<column | literal | transform() | sql()/agg()>` The vertical facet position channel, for mark-level faceting, bound to the *fy* scale.
#' @param href `<column | literal | transform() | sql()/agg()>` The href; a channel specifying URLs for clickable links.
#' @param image_filter `<string | param()>` A CSS filter; a constant string used to adjust the rendering of images, such as *blur(5px)*.
#' @param margin `<number | param()>` Shorthand to set the same default for all four mark margins: **marginTop**, **marginRight**, **marginBottom**, and **marginLeft**; typically defaults to 0, except for axis marks.
#' @param margin_bottom `<number | param()>` The mark’s bottom margin; the minimum distance in pixels between the bottom edges of the inner and outer plot area.
#' @param margin_left `<number | param()>` The mark’s left margin; the minimum distance in pixels between the left edges of the inner and outer plot area.
#' @param margin_right `<number | param()>` The mark’s right margin; the minimum distance in pixels between the right edges of the mark’s inner and outer plot area.
#' @param margin_top `<number | param()>` The mark’s top margin; the minimum distance in pixels between the top edges of the inner and outer plot area.
#' @param marker `<"arrow" | "arrow-reverse" | "dot" | "circle" | "circle-fill" | "circle-stroke" | "tick" | "tick-x" | "tick-y" | "none" | boolean | NULL | param()>` Shorthand to set the same default for markerStart, markerMid, and markerEnd; one of: - a marker name such as *arrow* or *circle* - *none* (default) - no marker * true - alias for *circle-fill* * false or null - alias for *none*
#' @param marker_end `<"arrow" | "arrow-reverse" | "dot" | "circle" | "circle-fill" | "circle-stroke" | "tick" | "tick-x" | "tick-y" | "none" | boolean | NULL | param()>` The marker for the ending point of a line segment; one of: - a marker name such as *arrow* or *circle* * *none* (default) - no marker * true - alias for *circle-fill* * false or null - alias for *none*
#' @param marker_mid `<"arrow" | "arrow-reverse" | "dot" | "circle" | "circle-fill" | "circle-stroke" | "tick" | "tick-x" | "tick-y" | "none" | boolean | NULL | param()>` The marker for any middle (interior) points of a line segment.
#' @param marker_start `<"arrow" | "arrow-reverse" | "dot" | "circle" | "circle-fill" | "circle-stroke" | "tick" | "tick-x" | "tick-y" | "none" | boolean | NULL | param()>` The marker for the starting point of a line segment; one of: - a marker name such as *arrow* or *circle* * *none* (default) - no marker * true - alias for *circle-fill* * false or null - alias for *none*
#' @param mix_blend_mode `<string | param()>` The mix-blend-mode; a constant string specifying how to blend content such as *multiply*.
#' @param opacity `<column | literal | transform() | sql()/agg() | list(value=, ...)>` The opacity; a constant between 0 and 1, or a channel typically bound to the *opacity* scale.
#' @param paint_order `<string | param()>` The paint-order; a constant string specifying the order in which the **fill**, **stroke**, and any markers are drawn; defaults to *normal*, which draws the fill, then stroke, then markers; defaults to *stroke* for the text mark to create a “halo” around text to improve legibility.
#' @param pointer_events `<string | param()>` The pointer-events property; a constant string such as *none*.
#' @param reverse `<boolean | param()>` Applies a transform to reverse the order of the mark’s index, say for reverse input order.
#' @param select `<"first" | "last" | "maxX" | "maxY" | "minX" | "minY" | "nearest" | "nearestX" | "nearestY">` Applies a filter transform after data is loaded to highlight selected values only.
#' @param shape_rendering `<string | param()>` The shape-rendering; a constant string such as *crispEdges*.
#' @param sort `<column | literal | transform() | sql()/agg() | list>` Either applies a transform to sort the mark’s index by the specified channel values, or imputes ordinal scale domains from this mark’s channels.
#' @param stroke `<column | literal | transform() | sql()/agg() | list(value=, ...) | param()>` The stroke; a constant CSS color string, or a channel typically bound to the *color* scale.
#' @param stroke_dasharray `<string | number | param()>` The stroke-dasharray; a constant number indicating the length in pixels of alternating dashes and gaps, or a constant string of numbers separated by spaces or commas (_e.g._, *10 2* for dashes of 10 pixels separated by gaps of 2 pixels), or *none* (the default) for no dashing : https://developer.mozilla.org/en-US/docs/Web/SVG/Attribute/stroke-dasharray
#' @param stroke_dashoffset `<string | number | param()>` The stroke-dashoffset; a constant indicating the offset in pixels of the first dash along the stroke; defaults to zero.
#' @param stroke_linecap `<string | param()>` The stroke-linecap; a constant specifying how to cap stroked paths, such as *butt*, *round*, or *square*.
#' @param stroke_linejoin `<string | param()>` The stroke-linejoin; a constant specifying how to join stroked paths, such as *bevel*, *miter*, *miter-clip*, or *round*.
#' @param stroke_miterlimit `<number | param()>` The stroke-miterlimit; a constant number specifying how to limit the length of *miter* joins on stroked paths.
#' @param stroke_opacity `<column | literal | transform() | sql()/agg() | list(value=, ...)>` The stroke-opacity; a constant between 0 and 1, or a channel typically bound to the *opacity* scale.
#' @param stroke_width `<column | literal | transform() | sql()/agg() | list(value=, ...)>` The stroke-width; a constant number in pixels, or a channel.
#' @param target `<string | param()>` The target; a constant string specifying the target window (_e.g._, *_blank*) for clickable links; used in conjunction with the **href** option.
#' @param tip `<boolean | "x" | "y" | "xy" | list | param()>` Whether to generate a tooltip for this mark, and any tip options.
#' @param title `<column | literal | transform() | sql()/agg()>` The title; a channel specifying accessible, short textual descriptions as strings (possibly with newlines).
#' @param x `<column | literal | transform() | sql()/agg() | list(value=, ...)>` The horizontal position (or length) channel, typically bound to the *x* scale.
#' @param z `<column | literal | transform() | sql()/agg()>` An optional ordinal channel for grouping data into (possibly stacked) series to be drawn as separate areas; defaults to **fill** if a channel, or **stroke** if a channel.
#' @param ... Additional options or plot-level attributes.
#' @param data_from The name of the data source this mark reads from (see [vg_data()]); a length-1 nonzero R integer (`1L`, `-1L`, ...; note the `L`) giving a 1-based index into the spec's registered data sources instead, negative counting from the end (e.g., `-1L` for the most recently registered one); or a literal vector of values to use as inline data directly (mosaic-spec's `"data": [...]` shorthand, e.g., for a single reference line -- a bare double like `0`/`c(0)`, or `0L` itself (never a valid index), means this, not an index). Left unset, defaults to the first registered data source (equivalent to `data_from = 1L`) for any mark type that takes data at all.
#' @param filter_by A Param/Selection (e.g., from [param()]) to filter this mark's data by.
#' @param data_optimize A flag (default `TRUE`) to enable mark-specific query optimizations for this mark's data; set `FALSE` to disable them (mosaic-spec's `data: {optimize: false}`).
#' @family mark functions
#' @export
vg_mark_errorbar_y <- function(spec = NULL, formula = vg_unset, y, aria_description = vg_unset, aria_hidden = vg_unset, aria_label = vg_unset, channels = vg_unset, ci = vg_unset, clip = vg_unset, dx = vg_unset, dy = vg_unset, facet = vg_unset, facet_anchor = vg_unset, fill = vg_unset, fill_opacity = vg_unset, filter = vg_unset, fx = vg_unset, fy = vg_unset, href = vg_unset, image_filter = vg_unset, margin = vg_unset, margin_bottom = vg_unset, margin_left = vg_unset, margin_right = vg_unset, margin_top = vg_unset, marker = vg_unset, marker_end = vg_unset, marker_mid = vg_unset, marker_start = vg_unset, mix_blend_mode = vg_unset, opacity = vg_unset, paint_order = vg_unset, pointer_events = vg_unset, reverse = vg_unset, select = vg_unset, shape_rendering = vg_unset, sort = vg_unset, stroke = vg_unset, stroke_dasharray = vg_unset, stroke_dashoffset = vg_unset, stroke_linecap = vg_unset, stroke_linejoin = vg_unset, stroke_miterlimit = vg_unset, stroke_opacity = vg_unset, stroke_width = vg_unset, target = vg_unset, tip = vg_unset, title = vg_unset, x = vg_unset, z = vg_unset, ..., data_from = vg_unset, filter_by = vg_unset, data_optimize = vg_unset) {
  vg_mark_(spec, "errorbarY", formula = formula, y = y, ariaDescription = aria_description, ariaHidden = aria_hidden, ariaLabel = aria_label, channels = channels, ci = ci, clip = clip, dx = dx, dy = dy, facet = facet, facetAnchor = facet_anchor, fill = fill, fillOpacity = fill_opacity, filter = filter, fx = fx, fy = fy, href = href, imageFilter = image_filter, margin = margin, marginBottom = margin_bottom, marginLeft = margin_left, marginRight = margin_right, marginTop = margin_top, marker = marker, markerEnd = marker_end, markerMid = marker_mid, markerStart = marker_start, mixBlendMode = mix_blend_mode, opacity = opacity, paintOrder = paint_order, pointerEvents = pointer_events, reverse = reverse, select = select, shapeRendering = shape_rendering, sort = sort, stroke = stroke, strokeDasharray = stroke_dasharray, strokeDashoffset = stroke_dashoffset, strokeLinecap = stroke_linecap, strokeLinejoin = stroke_linejoin, strokeMiterlimit = stroke_miterlimit, strokeOpacity = stroke_opacity, strokeWidth = stroke_width, target = target, tip = tip, title = title, x = x, z = z, ..., data_from = data_from, filter_by = filter_by, data_optimize = data_optimize)
}

#' The frame mark.
#'
#' See [vg_value_types] for what the `<...>` notation below (`column`, `param()`, ...) means.
#'
#' @param spec A plot fragment or `vgspec` to add this mark to, or `NULL` to start a new plot with just this mark.
#' @param formula A shorthand for this mark's position channels (`x`, `y`, `fx`, `fy`, and paired `x1`/`x2` or `y1`/`y2`), e.g., `Sepal.Length ~ Sepal.Width | ~ Species` for `y = ~Sepal.Length, x = ~Sepal.Width, fx = ~Species`. See [vg_mark()] for the full grammar.
#' @param anchor `<"top" | "right" | "bottom" | "left" | param()>` The side of the frame on which to place the axis: *top* or *bottom* for horizontal axes (axisX and axisFx) and their associated vertical grids (gridX and gridFx), or *left* or *right* for vertical axes (axisY and axisFY) and their associated horizontal grids (gridY and gridFy).
#' @param aria_description `<string | param()>` The aria-description; a constant textual description.
#' @param aria_hidden `<string | param()>` The aria-hidden state; a constant indicating whether the element is exposed to an accessibility API.
#' @param aria_label `<column | literal | transform() | sql()/agg()>` The aria-label; a channel specifying short textual labels representing the value in the accessibility tree.
#' @param channels `<list>` Additional named channels, for example to include in a tooltip.
#' @param clip `<"frame" | "sphere" | boolean | NULL | param()>` How to clip the mark; one of: - *frame* or true - clip to the plot’s frame (inner area) - *sphere* - clip to the projected sphere (*e.g.*, front hemisphere) - null or false - do not clip The *sphere* clip option requires a geographic projection.
#' @param dx `<number | param()>` The horizontal offset in pixels; a constant option.
#' @param dy `<number | param()>` The vertical offset in pixels; a constant option.
#' @param facet `<"auto" | "include" | "exclude" | "super" | boolean | NULL | param()>` Whether to enable or disable faceting; one of: - *auto* (default) - automatically determine if this mark should be faceted - *include* (or true) - draw the subset of the mark’s data in the current facet - *exclude* - draw the subset of the mark’s data *not* in the current facet - *super* - draw this mark in a single frame that covers all facets - null (or false) - repeat this mark’s data across all facets (*i.e.*, no faceting) When a mark uses *super* faceting, it is not allowed to use position scales (*x*, *y*, *fx*, or *fy*); *super* faceting is intended for decorations, such as labels and legends.
#' @param facet_anchor `<"top" | "right" | "bottom" | "left" | "top-left" | "top-right" | "bottom-left" | "bottom-right" | "top-empty" | "right-empty" | "bottom-empty" | "left-empty" | "empty" | NULL | param()>` How to place the mark with respect to facets; one of: - null (default for most marks) - display the mark in each non-empty facet - *top*, *right*, *bottom*, or *left* - display the mark only in facets on the given side - *top-empty*, *right-empty*, *bottom-empty*, or *left-empty* (default for axis marks) - display the mark only in facets that have empty space on the given side: either the margin, or an empty facet - *empty* - display the mark in empty facets only
#' @param fill `<column | literal | transform() | sql()/agg() | list(value=, ...) | param()>` The fill; a constant CSS color string, or a channel typically bound to the *color* scale.
#' @param fill_opacity `<column | literal | transform() | sql()/agg() | list(value=, ...) | param()>` The fill-opacity; a constant number between 0 and 1, or a channel typically bound to the *opacity* scale.
#' @param filter `<column | literal | transform() | sql()/agg()>` Applies a transform to filter the mark’s index according to the given channel values; only truthy values are retained.
#' @param fx `<column | literal | transform() | sql()/agg()>` The horizontal facet position channel, for mark-level faceting, bound to the *fx* scale.
#' @param fy `<column | literal | transform() | sql()/agg()>` The vertical facet position channel, for mark-level faceting, bound to the *fy* scale.
#' @param href `<column | literal | transform() | sql()/agg()>` The href; a channel specifying URLs for clickable links.
#' @param image_filter `<string | param()>` A CSS filter; a constant string used to adjust the rendering of images, such as *blur(5px)*.
#' @param inset `<number | param()>` Shorthand to set the same default for **insetStart** and **insetEnd**.
#' @param inset_bottom `<number | param()>` Insets the bottom edge by the specified number of pixels.
#' @param inset_left `<number | param()>` Insets the left edge by the specified number of pixels.
#' @param inset_right `<number | param()>` Insets the right edge by the specified number of pixels.
#' @param inset_top `<number | param()>` Insets the top edge by the specified number of pixels.
#' @param margin `<number | param()>` Shorthand to set the same default for all four mark margins: **marginTop**, **marginRight**, **marginBottom**, and **marginLeft**; typically defaults to 0, except for axis marks.
#' @param margin_bottom `<number | param()>` The mark’s bottom margin; the minimum distance in pixels between the bottom edges of the inner and outer plot area.
#' @param margin_left `<number | param()>` The mark’s left margin; the minimum distance in pixels between the left edges of the inner and outer plot area.
#' @param margin_right `<number | param()>` The mark’s right margin; the minimum distance in pixels between the right edges of the mark’s inner and outer plot area.
#' @param margin_top `<number | param()>` The mark’s top margin; the minimum distance in pixels between the top edges of the inner and outer plot area.
#' @param mix_blend_mode `<string | param()>` The mix-blend-mode; a constant string specifying how to blend content such as *multiply*.
#' @param opacity `<column | literal | transform() | sql()/agg() | list(value=, ...)>` The opacity; a constant between 0 and 1, or a channel typically bound to the *opacity* scale.
#' @param paint_order `<string | param()>` The paint-order; a constant string specifying the order in which the **fill**, **stroke**, and any markers are drawn; defaults to *normal*, which draws the fill, then stroke, then markers; defaults to *stroke* for the text mark to create a “halo” around text to improve legibility.
#' @param pointer_events `<string | param()>` The pointer-events property; a constant string such as *none*.
#' @param reverse `<boolean | param()>` Applies a transform to reverse the order of the mark’s index, say for reverse input order.
#' @param rx `<number | string | param()>` The rounded corner *x*-radius, either in pixels or as a percentage of the rect width.
#' @param ry `<number | string | param()>` The rounded corner *y*-radius, either in pixels or as a percentage of the rect height.
#' @param select `<"first" | "last" | "maxX" | "maxY" | "minX" | "minY" | "nearest" | "nearestX" | "nearestY">` Applies a filter transform after data is loaded to highlight selected values only.
#' @param shape_rendering `<string | param()>` The shape-rendering; a constant string such as *crispEdges*.
#' @param sort `<column | literal | transform() | sql()/agg() | list>` Either applies a transform to sort the mark’s index by the specified channel values, or imputes ordinal scale domains from this mark’s channels.
#' @param stroke `<column | literal | transform() | sql()/agg() | list(value=, ...) | param()>` The stroke; a constant CSS color string, or a channel typically bound to the *color* scale.
#' @param stroke_dasharray `<string | number | param()>` The stroke-dasharray; a constant number indicating the length in pixels of alternating dashes and gaps, or a constant string of numbers separated by spaces or commas (_e.g._, *10 2* for dashes of 10 pixels separated by gaps of 2 pixels), or *none* (the default) for no dashing : https://developer.mozilla.org/en-US/docs/Web/SVG/Attribute/stroke-dasharray
#' @param stroke_dashoffset `<string | number | param()>` The stroke-dashoffset; a constant indicating the offset in pixels of the first dash along the stroke; defaults to zero.
#' @param stroke_linecap `<string | param()>` The stroke-linecap; a constant specifying how to cap stroked paths, such as *butt*, *round*, or *square*.
#' @param stroke_linejoin `<string | param()>` The stroke-linejoin; a constant specifying how to join stroked paths, such as *bevel*, *miter*, *miter-clip*, or *round*.
#' @param stroke_miterlimit `<number | param()>` The stroke-miterlimit; a constant number specifying how to limit the length of *miter* joins on stroked paths.
#' @param stroke_opacity `<column | literal | transform() | sql()/agg() | list(value=, ...)>` The stroke-opacity; a constant between 0 and 1, or a channel typically bound to the *opacity* scale.
#' @param stroke_width `<column | literal | transform() | sql()/agg() | list(value=, ...)>` The stroke-width; a constant number in pixels, or a channel.
#' @param target `<string | param()>` The target; a constant string specifying the target window (_e.g._, *_blank*) for clickable links; used in conjunction with the **href** option.
#' @param tip `<boolean | "x" | "y" | "xy" | list | param()>` Whether to generate a tooltip for this mark, and any tip options.
#' @param title `<column | literal | transform() | sql()/agg()>` The title; a channel specifying accessible, short textual descriptions as strings (possibly with newlines).
#' @param ... Additional options or plot-level attributes.
#' @param data_from The name of the data source this mark reads from (see [vg_data()]); a length-1 nonzero R integer (`1L`, `-1L`, ...; note the `L`) giving a 1-based index into the spec's registered data sources instead, negative counting from the end (e.g., `-1L` for the most recently registered one); or a literal vector of values to use as inline data directly (mosaic-spec's `"data": [...]` shorthand, e.g., for a single reference line -- a bare double like `0`/`c(0)`, or `0L` itself (never a valid index), means this, not an index). Left unset, defaults to the first registered data source (equivalent to `data_from = 1L`) for any mark type that takes data at all.
#' @param filter_by A Param/Selection (e.g., from [param()]) to filter this mark's data by.
#' @param data_optimize A flag (default `TRUE`) to enable mark-specific query optimizations for this mark's data; set `FALSE` to disable them (mosaic-spec's `data: {optimize: false}`).
#' @family mark functions
#' @export
vg_mark_frame <- function(spec = NULL, formula = vg_unset, anchor = vg_unset, aria_description = vg_unset, aria_hidden = vg_unset, aria_label = vg_unset, channels = vg_unset, clip = vg_unset, dx = vg_unset, dy = vg_unset, facet = vg_unset, facet_anchor = vg_unset, fill = vg_unset, fill_opacity = vg_unset, filter = vg_unset, fx = vg_unset, fy = vg_unset, href = vg_unset, image_filter = vg_unset, inset = vg_unset, inset_bottom = vg_unset, inset_left = vg_unset, inset_right = vg_unset, inset_top = vg_unset, margin = vg_unset, margin_bottom = vg_unset, margin_left = vg_unset, margin_right = vg_unset, margin_top = vg_unset, mix_blend_mode = vg_unset, opacity = vg_unset, paint_order = vg_unset, pointer_events = vg_unset, reverse = vg_unset, rx = vg_unset, ry = vg_unset, select = vg_unset, shape_rendering = vg_unset, sort = vg_unset, stroke = vg_unset, stroke_dasharray = vg_unset, stroke_dashoffset = vg_unset, stroke_linecap = vg_unset, stroke_linejoin = vg_unset, stroke_miterlimit = vg_unset, stroke_opacity = vg_unset, stroke_width = vg_unset, target = vg_unset, tip = vg_unset, title = vg_unset, ..., data_from = vg_unset, filter_by = vg_unset, data_optimize = vg_unset) {
  vg_mark_(spec, "frame", formula = formula, anchor = anchor, ariaDescription = aria_description, ariaHidden = aria_hidden, ariaLabel = aria_label, channels = channels, clip = clip, dx = dx, dy = dy, facet = facet, facetAnchor = facet_anchor, fill = fill, fillOpacity = fill_opacity, filter = filter, fx = fx, fy = fy, href = href, imageFilter = image_filter, inset = inset, insetBottom = inset_bottom, insetLeft = inset_left, insetRight = inset_right, insetTop = inset_top, margin = margin, marginBottom = margin_bottom, marginLeft = margin_left, marginRight = margin_right, marginTop = margin_top, mixBlendMode = mix_blend_mode, opacity = opacity, paintOrder = paint_order, pointerEvents = pointer_events, reverse = reverse, rx = rx, ry = ry, select = select, shapeRendering = shape_rendering, sort = sort, stroke = stroke, strokeDasharray = stroke_dasharray, strokeDashoffset = stroke_dashoffset, strokeLinecap = stroke_linecap, strokeLinejoin = stroke_linejoin, strokeMiterlimit = stroke_miterlimit, strokeOpacity = stroke_opacity, strokeWidth = stroke_width, target = target, tip = tip, title = title, ..., data_from = data_from, filter_by = filter_by, data_optimize = data_optimize)
}

#' The geo mark.
#'
#' See [vg_value_types] for what the `<...>` notation below (`column`, `param()`, ...) means.
#'
#' @param spec A plot fragment or `vgspec` to add this mark to, or `NULL` to start a new plot with just this mark.
#' @param formula A shorthand for this mark's position channels (`x`, `y`, `fx`, `fy`, and paired `x1`/`x2` or `y1`/`y2`), e.g., `Sepal.Length ~ Sepal.Width | ~ Species` for `y = ~Sepal.Length, x = ~Sepal.Width, fx = ~Species`. See [vg_mark()] for the full grammar.
#' @param aria_description `<string | param()>` The aria-description; a constant textual description.
#' @param aria_hidden `<string | param()>` The aria-hidden state; a constant indicating whether the element is exposed to an accessibility API.
#' @param aria_label `<column | literal | transform() | sql()/agg()>` The aria-label; a channel specifying short textual labels representing the value in the accessibility tree.
#' @param channels `<list>` Additional named channels, for example to include in a tooltip.
#' @param clip `<"frame" | "sphere" | boolean | NULL | param()>` How to clip the mark; one of: - *frame* or true - clip to the plot’s frame (inner area) - *sphere* - clip to the projected sphere (*e.g.*, front hemisphere) - null or false - do not clip The *sphere* clip option requires a geographic projection.
#' @param dx `<number | param()>` The horizontal offset in pixels; a constant option.
#' @param dy `<number | param()>` The vertical offset in pixels; a constant option.
#' @param facet `<"auto" | "include" | "exclude" | "super" | boolean | NULL | param()>` Whether to enable or disable faceting; one of: - *auto* (default) - automatically determine if this mark should be faceted - *include* (or true) - draw the subset of the mark’s data in the current facet - *exclude* - draw the subset of the mark’s data *not* in the current facet - *super* - draw this mark in a single frame that covers all facets - null (or false) - repeat this mark’s data across all facets (*i.e.*, no faceting) When a mark uses *super* faceting, it is not allowed to use position scales (*x*, *y*, *fx*, or *fy*); *super* faceting is intended for decorations, such as labels and legends.
#' @param facet_anchor `<"top" | "right" | "bottom" | "left" | "top-left" | "top-right" | "bottom-left" | "bottom-right" | "top-empty" | "right-empty" | "bottom-empty" | "left-empty" | "empty" | NULL | param()>` How to place the mark with respect to facets; one of: - null (default for most marks) - display the mark in each non-empty facet - *top*, *right*, *bottom*, or *left* - display the mark only in facets on the given side - *top-empty*, *right-empty*, *bottom-empty*, or *left-empty* (default for axis marks) - display the mark only in facets that have empty space on the given side: either the margin, or an empty facet - *empty* - display the mark in empty facets only
#' @param fill `<column | literal | transform() | sql()/agg() | list(value=, ...) | param()>` The fill; a constant CSS color string, or a channel typically bound to the *color* scale.
#' @param fill_opacity `<column | literal | transform() | sql()/agg() | list(value=, ...) | param()>` The fill-opacity; a constant number between 0 and 1, or a channel typically bound to the *opacity* scale.
#' @param filter `<column | literal | transform() | sql()/agg()>` Applies a transform to filter the mark’s index according to the given channel values; only truthy values are retained.
#' @param fx `<column | literal | transform() | sql()/agg()>` The horizontal facet position channel, for mark-level faceting, bound to the *fx* scale.
#' @param fy `<column | literal | transform() | sql()/agg()>` The vertical facet position channel, for mark-level faceting, bound to the *fy* scale.
#' @param geometry `<column | literal | transform() | sql()/agg()>` A required channel for the geometry to render; defaults to identity, assuming *data* is a GeoJSON object or an iterable of GeoJSON objects.
#' @param href `<column | literal | transform() | sql()/agg()>` The href; a channel specifying URLs for clickable links.
#' @param image_filter `<string | param()>` A CSS filter; a constant string used to adjust the rendering of images, such as *blur(5px)*.
#' @param margin `<number | param()>` Shorthand to set the same default for all four mark margins: **marginTop**, **marginRight**, **marginBottom**, and **marginLeft**; typically defaults to 0, except for axis marks.
#' @param margin_bottom `<number | param()>` The mark’s bottom margin; the minimum distance in pixels between the bottom edges of the inner and outer plot area.
#' @param margin_left `<number | param()>` The mark’s left margin; the minimum distance in pixels between the left edges of the inner and outer plot area.
#' @param margin_right `<number | param()>` The mark’s right margin; the minimum distance in pixels between the right edges of the mark’s inner and outer plot area.
#' @param margin_top `<number | param()>` The mark’s top margin; the minimum distance in pixels between the top edges of the inner and outer plot area.
#' @param mix_blend_mode `<string | param()>` The mix-blend-mode; a constant string specifying how to blend content such as *multiply*.
#' @param opacity `<column | literal | transform() | sql()/agg() | list(value=, ...)>` The opacity; a constant between 0 and 1, or a channel typically bound to the *opacity* scale.
#' @param paint_order `<string | param()>` The paint-order; a constant string specifying the order in which the **fill**, **stroke**, and any markers are drawn; defaults to *normal*, which draws the fill, then stroke, then markers; defaults to *stroke* for the text mark to create a “halo” around text to improve legibility.
#' @param pointer_events `<string | param()>` The pointer-events property; a constant string such as *none*.
#' @param r `<column | literal | transform() | sql()/agg() | list(value=, ...) | number | param()>` The radius of dots; either a channel or constant.
#' @param reverse `<boolean | param()>` Applies a transform to reverse the order of the mark’s index, say for reverse input order.
#' @param select `<"first" | "last" | "maxX" | "maxY" | "minX" | "minY" | "nearest" | "nearestX" | "nearestY">` Applies a filter transform after data is loaded to highlight selected values only.
#' @param shape_rendering `<string | param()>` The shape-rendering; a constant string such as *crispEdges*.
#' @param sort `<column | literal | transform() | sql()/agg() | list>` Either applies a transform to sort the mark’s index by the specified channel values, or imputes ordinal scale domains from this mark’s channels.
#' @param stroke `<column | literal | transform() | sql()/agg() | list(value=, ...) | param()>` The stroke; a constant CSS color string, or a channel typically bound to the *color* scale.
#' @param stroke_dasharray `<string | number | param()>` The stroke-dasharray; a constant number indicating the length in pixels of alternating dashes and gaps, or a constant string of numbers separated by spaces or commas (_e.g._, *10 2* for dashes of 10 pixels separated by gaps of 2 pixels), or *none* (the default) for no dashing : https://developer.mozilla.org/en-US/docs/Web/SVG/Attribute/stroke-dasharray
#' @param stroke_dashoffset `<string | number | param()>` The stroke-dashoffset; a constant indicating the offset in pixels of the first dash along the stroke; defaults to zero.
#' @param stroke_linecap `<string | param()>` The stroke-linecap; a constant specifying how to cap stroked paths, such as *butt*, *round*, or *square*.
#' @param stroke_linejoin `<string | param()>` The stroke-linejoin; a constant specifying how to join stroked paths, such as *bevel*, *miter*, *miter-clip*, or *round*.
#' @param stroke_miterlimit `<number | param()>` The stroke-miterlimit; a constant number specifying how to limit the length of *miter* joins on stroked paths.
#' @param stroke_opacity `<column | literal | transform() | sql()/agg() | list(value=, ...)>` The stroke-opacity; a constant between 0 and 1, or a channel typically bound to the *opacity* scale.
#' @param stroke_width `<column | literal | transform() | sql()/agg() | list(value=, ...)>` The stroke-width; a constant number in pixels, or a channel.
#' @param target `<string | param()>` The target; a constant string specifying the target window (_e.g._, *_blank*) for clickable links; used in conjunction with the **href** option.
#' @param tip `<boolean | "x" | "y" | "xy" | list | param()>` Whether to generate a tooltip for this mark, and any tip options.
#' @param title `<column | literal | transform() | sql()/agg()>` The title; a channel specifying accessible, short textual descriptions as strings (possibly with newlines).
#' @param ... Additional options or plot-level attributes.
#' @param data_from The name of the data source this mark reads from (see [vg_data()]); a length-1 nonzero R integer (`1L`, `-1L`, ...; note the `L`) giving a 1-based index into the spec's registered data sources instead, negative counting from the end (e.g., `-1L` for the most recently registered one); or a literal vector of values to use as inline data directly (mosaic-spec's `"data": [...]` shorthand, e.g., for a single reference line -- a bare double like `0`/`c(0)`, or `0L` itself (never a valid index), means this, not an index). Left unset, defaults to the first registered data source (equivalent to `data_from = 1L`) for any mark type that takes data at all.
#' @param filter_by A Param/Selection (e.g., from [param()]) to filter this mark's data by.
#' @param data_optimize A flag (default `TRUE`) to enable mark-specific query optimizations for this mark's data; set `FALSE` to disable them (mosaic-spec's `data: {optimize: false}`).
#' @family mark functions
#' @export
vg_mark_geo <- function(spec = NULL, formula = vg_unset, aria_description = vg_unset, aria_hidden = vg_unset, aria_label = vg_unset, channels = vg_unset, clip = vg_unset, dx = vg_unset, dy = vg_unset, facet = vg_unset, facet_anchor = vg_unset, fill = vg_unset, fill_opacity = vg_unset, filter = vg_unset, fx = vg_unset, fy = vg_unset, geometry = vg_unset, href = vg_unset, image_filter = vg_unset, margin = vg_unset, margin_bottom = vg_unset, margin_left = vg_unset, margin_right = vg_unset, margin_top = vg_unset, mix_blend_mode = vg_unset, opacity = vg_unset, paint_order = vg_unset, pointer_events = vg_unset, r = vg_unset, reverse = vg_unset, select = vg_unset, shape_rendering = vg_unset, sort = vg_unset, stroke = vg_unset, stroke_dasharray = vg_unset, stroke_dashoffset = vg_unset, stroke_linecap = vg_unset, stroke_linejoin = vg_unset, stroke_miterlimit = vg_unset, stroke_opacity = vg_unset, stroke_width = vg_unset, target = vg_unset, tip = vg_unset, title = vg_unset, ..., data_from = vg_unset, filter_by = vg_unset, data_optimize = vg_unset) {
  vg_mark_(spec, "geo", formula = formula, ariaDescription = aria_description, ariaHidden = aria_hidden, ariaLabel = aria_label, channels = channels, clip = clip, dx = dx, dy = dy, facet = facet, facetAnchor = facet_anchor, fill = fill, fillOpacity = fill_opacity, filter = filter, fx = fx, fy = fy, geometry = geometry, href = href, imageFilter = image_filter, margin = margin, marginBottom = margin_bottom, marginLeft = margin_left, marginRight = margin_right, marginTop = margin_top, mixBlendMode = mix_blend_mode, opacity = opacity, paintOrder = paint_order, pointerEvents = pointer_events, r = r, reverse = reverse, select = select, shapeRendering = shape_rendering, sort = sort, stroke = stroke, strokeDasharray = stroke_dasharray, strokeDashoffset = stroke_dashoffset, strokeLinecap = stroke_linecap, strokeLinejoin = stroke_linejoin, strokeMiterlimit = stroke_miterlimit, strokeOpacity = stroke_opacity, strokeWidth = stroke_width, target = target, tip = tip, title = title, ..., data_from = data_from, filter_by = filter_by, data_optimize = data_optimize)
}

#' The graticule mark.
#'
#' See [vg_value_types] for what the `<...>` notation below (`column`, `param()`, ...) means.
#'
#' @param spec A plot fragment or `vgspec` to add this mark to, or `NULL` to start a new plot with just this mark.
#' @param formula A shorthand for this mark's position channels (`x`, `y`, `fx`, `fy`, and paired `x1`/`x2` or `y1`/`y2`), e.g., `Sepal.Length ~ Sepal.Width | ~ Species` for `y = ~Sepal.Length, x = ~Sepal.Width, fx = ~Species`. See [vg_mark()] for the full grammar.
#' @param aria_description `<string | param()>` The aria-description; a constant textual description.
#' @param aria_hidden `<string | param()>` The aria-hidden state; a constant indicating whether the element is exposed to an accessibility API.
#' @param aria_label `<column | literal | transform() | sql()/agg()>` The aria-label; a channel specifying short textual labels representing the value in the accessibility tree.
#' @param channels `<list>` Additional named channels, for example to include in a tooltip.
#' @param clip `<"frame" | "sphere" | boolean | NULL | param()>` How to clip the mark; one of: - *frame* or true - clip to the plot’s frame (inner area) - *sphere* - clip to the projected sphere (*e.g.*, front hemisphere) - null or false - do not clip The *sphere* clip option requires a geographic projection.
#' @param dx `<number | param()>` The horizontal offset in pixels; a constant option.
#' @param dy `<number | param()>` The vertical offset in pixels; a constant option.
#' @param facet `<"auto" | "include" | "exclude" | "super" | boolean | NULL | param()>` Whether to enable or disable faceting; one of: - *auto* (default) - automatically determine if this mark should be faceted - *include* (or true) - draw the subset of the mark’s data in the current facet - *exclude* - draw the subset of the mark’s data *not* in the current facet - *super* - draw this mark in a single frame that covers all facets - null (or false) - repeat this mark’s data across all facets (*i.e.*, no faceting) When a mark uses *super* faceting, it is not allowed to use position scales (*x*, *y*, *fx*, or *fy*); *super* faceting is intended for decorations, such as labels and legends.
#' @param facet_anchor `<"top" | "right" | "bottom" | "left" | "top-left" | "top-right" | "bottom-left" | "bottom-right" | "top-empty" | "right-empty" | "bottom-empty" | "left-empty" | "empty" | NULL | param()>` How to place the mark with respect to facets; one of: - null (default for most marks) - display the mark in each non-empty facet - *top*, *right*, *bottom*, or *left* - display the mark only in facets on the given side - *top-empty*, *right-empty*, *bottom-empty*, or *left-empty* (default for axis marks) - display the mark only in facets that have empty space on the given side: either the margin, or an empty facet - *empty* - display the mark in empty facets only
#' @param fill `<column | literal | transform() | sql()/agg() | list(value=, ...) | param()>` The fill; a constant CSS color string, or a channel typically bound to the *color* scale.
#' @param fill_opacity `<column | literal | transform() | sql()/agg() | list(value=, ...) | param()>` The fill-opacity; a constant number between 0 and 1, or a channel typically bound to the *opacity* scale.
#' @param filter `<column | literal | transform() | sql()/agg()>` Applies a transform to filter the mark’s index according to the given channel values; only truthy values are retained.
#' @param fx `<column | literal | transform() | sql()/agg()>` The horizontal facet position channel, for mark-level faceting, bound to the *fx* scale.
#' @param fy `<column | literal | transform() | sql()/agg()>` The vertical facet position channel, for mark-level faceting, bound to the *fy* scale.
#' @param href `<column | literal | transform() | sql()/agg()>` The href; a channel specifying URLs for clickable links.
#' @param image_filter `<string | param()>` A CSS filter; a constant string used to adjust the rendering of images, such as *blur(5px)*.
#' @param margin `<number | param()>` Shorthand to set the same default for all four mark margins: **marginTop**, **marginRight**, **marginBottom**, and **marginLeft**; typically defaults to 0, except for axis marks.
#' @param margin_bottom `<number | param()>` The mark’s bottom margin; the minimum distance in pixels between the bottom edges of the inner and outer plot area.
#' @param margin_left `<number | param()>` The mark’s left margin; the minimum distance in pixels between the left edges of the inner and outer plot area.
#' @param margin_right `<number | param()>` The mark’s right margin; the minimum distance in pixels between the right edges of the mark’s inner and outer plot area.
#' @param margin_top `<number | param()>` The mark’s top margin; the minimum distance in pixels between the top edges of the inner and outer plot area.
#' @param mix_blend_mode `<string | param()>` The mix-blend-mode; a constant string specifying how to blend content such as *multiply*.
#' @param opacity `<column | literal | transform() | sql()/agg() | list(value=, ...)>` The opacity; a constant between 0 and 1, or a channel typically bound to the *opacity* scale.
#' @param paint_order `<string | param()>` The paint-order; a constant string specifying the order in which the **fill**, **stroke**, and any markers are drawn; defaults to *normal*, which draws the fill, then stroke, then markers; defaults to *stroke* for the text mark to create a “halo” around text to improve legibility.
#' @param pointer_events `<string | param()>` The pointer-events property; a constant string such as *none*.
#' @param reverse `<boolean | param()>` Applies a transform to reverse the order of the mark’s index, say for reverse input order.
#' @param select `<"first" | "last" | "maxX" | "maxY" | "minX" | "minY" | "nearest" | "nearestX" | "nearestY">` Applies a filter transform after data is loaded to highlight selected values only.
#' @param shape_rendering `<string | param()>` The shape-rendering; a constant string such as *crispEdges*.
#' @param sort `<column | literal | transform() | sql()/agg() | list>` Either applies a transform to sort the mark’s index by the specified channel values, or imputes ordinal scale domains from this mark’s channels.
#' @param stroke `<column | literal | transform() | sql()/agg() | list(value=, ...) | param()>` The stroke; a constant CSS color string, or a channel typically bound to the *color* scale.
#' @param stroke_dasharray `<string | number | param()>` The stroke-dasharray; a constant number indicating the length in pixels of alternating dashes and gaps, or a constant string of numbers separated by spaces or commas (_e.g._, *10 2* for dashes of 10 pixels separated by gaps of 2 pixels), or *none* (the default) for no dashing : https://developer.mozilla.org/en-US/docs/Web/SVG/Attribute/stroke-dasharray
#' @param stroke_dashoffset `<string | number | param()>` The stroke-dashoffset; a constant indicating the offset in pixels of the first dash along the stroke; defaults to zero.
#' @param stroke_linecap `<string | param()>` The stroke-linecap; a constant specifying how to cap stroked paths, such as *butt*, *round*, or *square*.
#' @param stroke_linejoin `<string | param()>` The stroke-linejoin; a constant specifying how to join stroked paths, such as *bevel*, *miter*, *miter-clip*, or *round*.
#' @param stroke_miterlimit `<number | param()>` The stroke-miterlimit; a constant number specifying how to limit the length of *miter* joins on stroked paths.
#' @param stroke_opacity `<column | literal | transform() | sql()/agg() | list(value=, ...)>` The stroke-opacity; a constant between 0 and 1, or a channel typically bound to the *opacity* scale.
#' @param stroke_width `<column | literal | transform() | sql()/agg() | list(value=, ...)>` The stroke-width; a constant number in pixels, or a channel.
#' @param target `<string | param()>` The target; a constant string specifying the target window (_e.g._, *_blank*) for clickable links; used in conjunction with the **href** option.
#' @param tip `<boolean | "x" | "y" | "xy" | list | param()>` Whether to generate a tooltip for this mark, and any tip options.
#' @param title `<column | literal | transform() | sql()/agg()>` The title; a channel specifying accessible, short textual descriptions as strings (possibly with newlines).
#' @param ... Additional options or plot-level attributes.
#' @param data_from The name of the data source this mark reads from (see [vg_data()]); a length-1 nonzero R integer (`1L`, `-1L`, ...; note the `L`) giving a 1-based index into the spec's registered data sources instead, negative counting from the end (e.g., `-1L` for the most recently registered one); or a literal vector of values to use as inline data directly (mosaic-spec's `"data": [...]` shorthand, e.g., for a single reference line -- a bare double like `0`/`c(0)`, or `0L` itself (never a valid index), means this, not an index). Left unset, defaults to the first registered data source (equivalent to `data_from = 1L`) for any mark type that takes data at all.
#' @param filter_by A Param/Selection (e.g., from [param()]) to filter this mark's data by.
#' @param data_optimize A flag (default `TRUE`) to enable mark-specific query optimizations for this mark's data; set `FALSE` to disable them (mosaic-spec's `data: {optimize: false}`).
#' @family mark functions
#' @export
vg_mark_graticule <- function(spec = NULL, formula = vg_unset, aria_description = vg_unset, aria_hidden = vg_unset, aria_label = vg_unset, channels = vg_unset, clip = vg_unset, dx = vg_unset, dy = vg_unset, facet = vg_unset, facet_anchor = vg_unset, fill = vg_unset, fill_opacity = vg_unset, filter = vg_unset, fx = vg_unset, fy = vg_unset, href = vg_unset, image_filter = vg_unset, margin = vg_unset, margin_bottom = vg_unset, margin_left = vg_unset, margin_right = vg_unset, margin_top = vg_unset, mix_blend_mode = vg_unset, opacity = vg_unset, paint_order = vg_unset, pointer_events = vg_unset, reverse = vg_unset, select = vg_unset, shape_rendering = vg_unset, sort = vg_unset, stroke = vg_unset, stroke_dasharray = vg_unset, stroke_dashoffset = vg_unset, stroke_linecap = vg_unset, stroke_linejoin = vg_unset, stroke_miterlimit = vg_unset, stroke_opacity = vg_unset, stroke_width = vg_unset, target = vg_unset, tip = vg_unset, title = vg_unset, ..., data_from = vg_unset, filter_by = vg_unset, data_optimize = vg_unset) {
  vg_mark_(spec, "graticule", formula = formula, ariaDescription = aria_description, ariaHidden = aria_hidden, ariaLabel = aria_label, channels = channels, clip = clip, dx = dx, dy = dy, facet = facet, facetAnchor = facet_anchor, fill = fill, fillOpacity = fill_opacity, filter = filter, fx = fx, fy = fy, href = href, imageFilter = image_filter, margin = margin, marginBottom = margin_bottom, marginLeft = margin_left, marginRight = margin_right, marginTop = margin_top, mixBlendMode = mix_blend_mode, opacity = opacity, paintOrder = paint_order, pointerEvents = pointer_events, reverse = reverse, select = select, shapeRendering = shape_rendering, sort = sort, stroke = stroke, strokeDasharray = stroke_dasharray, strokeDashoffset = stroke_dashoffset, strokeLinecap = stroke_linecap, strokeLinejoin = stroke_linejoin, strokeMiterlimit = stroke_miterlimit, strokeOpacity = stroke_opacity, strokeWidth = stroke_width, target = target, tip = tip, title = title, ..., data_from = data_from, filter_by = filter_by, data_optimize = data_optimize)
}

#' The gridFx mark.
#'
#' See [vg_value_types] for what the `<...>` notation below (`column`, `param()`, ...) means.
#'
#' @param spec A plot fragment or `vgspec` to add this mark to, or `NULL` to start a new plot with just this mark.
#' @param formula A shorthand for this mark's position channels (`x`, `y`, `fx`, `fy`, and paired `x1`/`x2` or `y1`/`y2`), e.g., `Sepal.Length ~ Sepal.Width | ~ Species` for `y = ~Sepal.Length, x = ~Sepal.Width, fx = ~Species`. See [vg_mark()] for the full grammar.
#' @param anchor `<"top" | "right" | "bottom" | "left" | param()>` The side of the frame on which to place the axis: *top* or *bottom* for horizontal axes (axisX and axisFx) and their associated vertical grids (gridX and gridFx), or *left* or *right* for vertical axes (axisY and axisFY) and their associated horizontal grids (gridY and gridFy).
#' @param aria_description `<string | param()>` The aria-description; a constant textual description.
#' @param aria_hidden `<string | param()>` The aria-hidden state; a constant indicating whether the element is exposed to an accessibility API.
#' @param aria_label `<column | literal | transform() | sql()/agg()>` The aria-label; a channel specifying short textual labels representing the value in the accessibility tree.
#' @param channels `<list>` Additional named channels, for example to include in a tooltip.
#' @param clip `<"frame" | "sphere" | boolean | NULL | param()>` How to clip the mark; one of: - *frame* or true - clip to the plot’s frame (inner area) - *sphere* - clip to the projected sphere (*e.g.*, front hemisphere) - null or false - do not clip The *sphere* clip option requires a geographic projection.
#' @param color `<column | literal | transform() | sql()/agg() | list(value=, ...) | param()>` A shorthand for setting both **fill** and **stroke**; affects the stroke of tick vectors and grid rules, and the fill of tick texts and axis label texts; defaults to *currentColor*.
#' @param dx `<number | param()>` The horizontal offset in pixels; a constant option.
#' @param dy `<number | param()>` The vertical offset in pixels; a constant option.
#' @param facet `<"auto" | "include" | "exclude" | "super" | boolean | NULL | param()>` Whether to enable or disable faceting; one of: - *auto* (default) - automatically determine if this mark should be faceted - *include* (or true) - draw the subset of the mark’s data in the current facet - *exclude* - draw the subset of the mark’s data *not* in the current facet - *super* - draw this mark in a single frame that covers all facets - null (or false) - repeat this mark’s data across all facets (*i.e.*, no faceting) When a mark uses *super* faceting, it is not allowed to use position scales (*x*, *y*, *fx*, or *fy*); *super* faceting is intended for decorations, such as labels and legends.
#' @param facet_anchor `<"top" | "right" | "bottom" | "left" | "top-left" | "top-right" | "bottom-left" | "bottom-right" | "top-empty" | "right-empty" | "bottom-empty" | "left-empty" | "empty" | NULL | param()>` How to place the mark with respect to facets; one of: - null (default for most marks) - display the mark in each non-empty facet - *top*, *right*, *bottom*, or *left* - display the mark only in facets on the given side - *top-empty*, *right-empty*, *bottom-empty*, or *left-empty* (default for axis marks) - display the mark only in facets that have empty space on the given side: either the margin, or an empty facet - *empty* - display the mark in empty facets only
#' @param fill `<column | literal | transform() | sql()/agg() | list(value=, ...) | param()>` The fill; a constant CSS color string, or a channel typically bound to the *color* scale.
#' @param fill_opacity `<column | literal | transform() | sql()/agg() | list(value=, ...) | param()>` The fill-opacity; a constant number between 0 and 1, or a channel typically bound to the *opacity* scale.
#' @param filter `<column | literal | transform() | sql()/agg()>` Applies a transform to filter the mark’s index according to the given channel values; only truthy values are retained.
#' @param fx `<column | literal | transform() | sql()/agg()>` The horizontal facet position channel, for mark-level faceting, bound to the *fx* scale.
#' @param fy `<column | literal | transform() | sql()/agg()>` The vertical facet position channel, for mark-level faceting, bound to the *fy* scale.
#' @param href `<column | literal | transform() | sql()/agg()>` The href; a channel specifying URLs for clickable links.
#' @param image_filter `<string | param()>` A CSS filter; a constant string used to adjust the rendering of images, such as *blur(5px)*.
#' @param inset `<number | param()>` Shorthand to set the same default for **insetStart** and **insetEnd**.
#' @param inset_bottom `<number | param()>` Insets the bottom edge by the specified number of pixels.
#' @param inset_top `<number | param()>` Insets the top edge by the specified number of pixels.
#' @param interval `<"day"/"week"/"month"/... | number | param()>` Enforces uniformity for data at regular intervals, such as integer values or daily samples.
#' @param margin `<number | param()>` Shorthand to set the same default for all four mark margins: **marginTop**, **marginRight**, **marginBottom**, and **marginLeft**; typically defaults to 0, except for axis marks.
#' @param margin_bottom `<number | param()>` The mark’s bottom margin; the minimum distance in pixels between the bottom edges of the inner and outer plot area.
#' @param margin_left `<number | param()>` The mark’s left margin; the minimum distance in pixels between the left edges of the inner and outer plot area.
#' @param margin_right `<number | param()>` The mark’s right margin; the minimum distance in pixels between the right edges of the mark’s inner and outer plot area.
#' @param margin_top `<number | param()>` The mark’s top margin; the minimum distance in pixels between the top edges of the inner and outer plot area.
#' @param marker `<"arrow" | "arrow-reverse" | "dot" | "circle" | "circle-fill" | "circle-stroke" | "tick" | "tick-x" | "tick-y" | "none" | boolean | NULL | param()>` Shorthand to set the same default for markerStart, markerMid, and markerEnd; one of: - a marker name such as *arrow* or *circle* - *none* (default) - no marker * true - alias for *circle-fill* * false or null - alias for *none*
#' @param marker_end `<"arrow" | "arrow-reverse" | "dot" | "circle" | "circle-fill" | "circle-stroke" | "tick" | "tick-x" | "tick-y" | "none" | boolean | NULL | param()>` The marker for the ending point of a line segment; one of: - a marker name such as *arrow* or *circle* * *none* (default) - no marker * true - alias for *circle-fill* * false or null - alias for *none*
#' @param marker_mid `<"arrow" | "arrow-reverse" | "dot" | "circle" | "circle-fill" | "circle-stroke" | "tick" | "tick-x" | "tick-y" | "none" | boolean | NULL | param()>` The marker for any middle (interior) points of a line segment.
#' @param marker_start `<"arrow" | "arrow-reverse" | "dot" | "circle" | "circle-fill" | "circle-stroke" | "tick" | "tick-x" | "tick-y" | "none" | boolean | NULL | param()>` The marker for the starting point of a line segment; one of: - a marker name such as *arrow* or *circle* * *none* (default) - no marker * true - alias for *circle-fill* * false or null - alias for *none*
#' @param mix_blend_mode `<string | param()>` The mix-blend-mode; a constant string specifying how to blend content such as *multiply*.
#' @param opacity `<column | literal | transform() | sql()/agg() | list(value=, ...)>` The opacity; a constant between 0 and 1, or a channel typically bound to the *opacity* scale.
#' @param paint_order `<string | param()>` The paint-order; a constant string specifying the order in which the **fill**, **stroke**, and any markers are drawn; defaults to *normal*, which draws the fill, then stroke, then markers; defaults to *stroke* for the text mark to create a “halo” around text to improve legibility.
#' @param pointer_events `<string | param()>` The pointer-events property; a constant string such as *none*.
#' @param reverse `<boolean | param()>` Applies a transform to reverse the order of the mark’s index, say for reverse input order.
#' @param select `<"first" | "last" | "maxX" | "maxY" | "minX" | "minY" | "nearest" | "nearestX" | "nearestY">` Applies a filter transform after data is loaded to highlight selected values only.
#' @param shape_rendering `<string | param()>` The shape-rendering; a constant string such as *crispEdges*.
#' @param sort `<column | literal | transform() | sql()/agg() | list>` Either applies a transform to sort the mark’s index by the specified channel values, or imputes ordinal scale domains from this mark’s channels.
#' @param stroke `<column | literal | transform() | sql()/agg() | list(value=, ...) | param()>` The stroke; a constant CSS color string, or a channel typically bound to the *color* scale.
#' @param stroke_dasharray `<string | number | param()>` The stroke-dasharray; a constant number indicating the length in pixels of alternating dashes and gaps, or a constant string of numbers separated by spaces or commas (_e.g._, *10 2* for dashes of 10 pixels separated by gaps of 2 pixels), or *none* (the default) for no dashing : https://developer.mozilla.org/en-US/docs/Web/SVG/Attribute/stroke-dasharray
#' @param stroke_dashoffset `<string | number | param()>` The stroke-dashoffset; a constant indicating the offset in pixels of the first dash along the stroke; defaults to zero.
#' @param stroke_linecap `<string | param()>` The stroke-linecap; a constant specifying how to cap stroked paths, such as *butt*, *round*, or *square*.
#' @param stroke_linejoin `<string | param()>` The stroke-linejoin; a constant specifying how to join stroked paths, such as *bevel*, *miter*, *miter-clip*, or *round*.
#' @param stroke_miterlimit `<number | param()>` The stroke-miterlimit; a constant number specifying how to limit the length of *miter* joins on stroked paths.
#' @param stroke_opacity `<column | literal | transform() | sql()/agg() | list(value=, ...)>` The stroke-opacity; a constant between 0 and 1, or a channel typically bound to the *opacity* scale.
#' @param stroke_width `<column | literal | transform() | sql()/agg() | list(value=, ...)>` The stroke-width; a constant number in pixels, or a channel.
#' @param target `<string | param()>` The target; a constant string specifying the target window (_e.g._, *_blank*) for clickable links; used in conjunction with the **href** option.
#' @param tick_spacing `<number | param()>` The desired approximate spacing between adjacent axis ticks, affecting the default **ticks**; defaults to 80 pixels for *x* and *fx*, and 35 pixels for *y* and *fy*.
#' @param ticks `<number | "day"/"week"/"month"/... | vector | param()>` The desired approximate number of axis ticks, or an explicit array of tick values, or an interval such as *day* or *month*.
#' @param tip `<boolean | "x" | "y" | "xy" | list | param()>` Whether to generate a tooltip for this mark, and any tip options.
#' @param title `<column | literal | transform() | sql()/agg()>` The title; a channel specifying accessible, short textual descriptions as strings (possibly with newlines).
#' @param x `<column | literal | transform() | sql()/agg() | list(value=, ...)>` The horizontal position (or length) channel, typically bound to the *x* scale.
#' @param y `<column | literal | transform() | sql()/agg() | list(value=, ...)>` The vertical position channel, typically bound to the *y* scale; defaults to the zero-based index of the data 0, 1, 2, ….
#' @param y1 `<column | literal | transform() | sql()/agg() | list(value=, ...)>` The required primary (starting, often bottom) vertical position channel, representing the area’s baseline, typically bound to the *y* scale.
#' @param y2 `<column | literal | transform() | sql()/agg() | list(value=, ...)>` The optional secondary (ending, often top) vertical position channel, representing the area’s topline, typically bound to the *y* scale; if not specified, **y1** is used.
#' @param ... Additional options or plot-level attributes.
#' @param data_from The name of the data source this mark reads from (see [vg_data()]); a length-1 nonzero R integer (`1L`, `-1L`, ...; note the `L`) giving a 1-based index into the spec's registered data sources instead, negative counting from the end (e.g., `-1L` for the most recently registered one); or a literal vector of values to use as inline data directly (mosaic-spec's `"data": [...]` shorthand, e.g., for a single reference line -- a bare double like `0`/`c(0)`, or `0L` itself (never a valid index), means this, not an index). Left unset, defaults to the first registered data source (equivalent to `data_from = 1L`) for any mark type that takes data at all.
#' @param filter_by A Param/Selection (e.g., from [param()]) to filter this mark's data by.
#' @param data_optimize A flag (default `TRUE`) to enable mark-specific query optimizations for this mark's data; set `FALSE` to disable them (mosaic-spec's `data: {optimize: false}`).
#' @family mark functions
#' @export
vg_mark_grid_fx <- function(spec = NULL, formula = vg_unset, anchor = vg_unset, aria_description = vg_unset, aria_hidden = vg_unset, aria_label = vg_unset, channels = vg_unset, clip = vg_unset, color = vg_unset, dx = vg_unset, dy = vg_unset, facet = vg_unset, facet_anchor = vg_unset, fill = vg_unset, fill_opacity = vg_unset, filter = vg_unset, fx = vg_unset, fy = vg_unset, href = vg_unset, image_filter = vg_unset, inset = vg_unset, inset_bottom = vg_unset, inset_top = vg_unset, interval = vg_unset, margin = vg_unset, margin_bottom = vg_unset, margin_left = vg_unset, margin_right = vg_unset, margin_top = vg_unset, marker = vg_unset, marker_end = vg_unset, marker_mid = vg_unset, marker_start = vg_unset, mix_blend_mode = vg_unset, opacity = vg_unset, paint_order = vg_unset, pointer_events = vg_unset, reverse = vg_unset, select = vg_unset, shape_rendering = vg_unset, sort = vg_unset, stroke = vg_unset, stroke_dasharray = vg_unset, stroke_dashoffset = vg_unset, stroke_linecap = vg_unset, stroke_linejoin = vg_unset, stroke_miterlimit = vg_unset, stroke_opacity = vg_unset, stroke_width = vg_unset, target = vg_unset, tick_spacing = vg_unset, ticks = vg_unset, tip = vg_unset, title = vg_unset, x = vg_unset, y = vg_unset, y1 = vg_unset, y2 = vg_unset, ..., data_from = vg_unset, filter_by = vg_unset, data_optimize = vg_unset) {
  vg_mark_(spec, "gridFx", formula = formula, anchor = anchor, ariaDescription = aria_description, ariaHidden = aria_hidden, ariaLabel = aria_label, channels = channels, clip = clip, color = color, dx = dx, dy = dy, facet = facet, facetAnchor = facet_anchor, fill = fill, fillOpacity = fill_opacity, filter = filter, fx = fx, fy = fy, href = href, imageFilter = image_filter, inset = inset, insetBottom = inset_bottom, insetTop = inset_top, interval = interval, margin = margin, marginBottom = margin_bottom, marginLeft = margin_left, marginRight = margin_right, marginTop = margin_top, marker = marker, markerEnd = marker_end, markerMid = marker_mid, markerStart = marker_start, mixBlendMode = mix_blend_mode, opacity = opacity, paintOrder = paint_order, pointerEvents = pointer_events, reverse = reverse, select = select, shapeRendering = shape_rendering, sort = sort, stroke = stroke, strokeDasharray = stroke_dasharray, strokeDashoffset = stroke_dashoffset, strokeLinecap = stroke_linecap, strokeLinejoin = stroke_linejoin, strokeMiterlimit = stroke_miterlimit, strokeOpacity = stroke_opacity, strokeWidth = stroke_width, target = target, tickSpacing = tick_spacing, ticks = ticks, tip = tip, title = title, x = x, y = y, y1 = y1, y2 = y2, ..., data_from = data_from, filter_by = filter_by, data_optimize = data_optimize)
}

#' The gridFy mark.
#'
#' See [vg_value_types] for what the `<...>` notation below (`column`, `param()`, ...) means.
#'
#' @param spec A plot fragment or `vgspec` to add this mark to, or `NULL` to start a new plot with just this mark.
#' @param formula A shorthand for this mark's position channels (`x`, `y`, `fx`, `fy`, and paired `x1`/`x2` or `y1`/`y2`), e.g., `Sepal.Length ~ Sepal.Width | ~ Species` for `y = ~Sepal.Length, x = ~Sepal.Width, fx = ~Species`. See [vg_mark()] for the full grammar.
#' @param anchor `<"top" | "right" | "bottom" | "left" | param()>` The side of the frame on which to place the axis: *top* or *bottom* for horizontal axes (axisX and axisFx) and their associated vertical grids (gridX and gridFx), or *left* or *right* for vertical axes (axisY and axisFY) and their associated horizontal grids (gridY and gridFy).
#' @param aria_description `<string | param()>` The aria-description; a constant textual description.
#' @param aria_hidden `<string | param()>` The aria-hidden state; a constant indicating whether the element is exposed to an accessibility API.
#' @param aria_label `<column | literal | transform() | sql()/agg()>` The aria-label; a channel specifying short textual labels representing the value in the accessibility tree.
#' @param channels `<list>` Additional named channels, for example to include in a tooltip.
#' @param clip `<"frame" | "sphere" | boolean | NULL | param()>` How to clip the mark; one of: - *frame* or true - clip to the plot’s frame (inner area) - *sphere* - clip to the projected sphere (*e.g.*, front hemisphere) - null or false - do not clip The *sphere* clip option requires a geographic projection.
#' @param color `<column | literal | transform() | sql()/agg() | list(value=, ...) | param()>` A shorthand for setting both **fill** and **stroke**; affects the stroke of tick vectors and grid rules, and the fill of tick texts and axis label texts; defaults to *currentColor*.
#' @param dx `<number | param()>` The horizontal offset in pixels; a constant option.
#' @param dy `<number | param()>` The vertical offset in pixels; a constant option.
#' @param facet `<"auto" | "include" | "exclude" | "super" | boolean | NULL | param()>` Whether to enable or disable faceting; one of: - *auto* (default) - automatically determine if this mark should be faceted - *include* (or true) - draw the subset of the mark’s data in the current facet - *exclude* - draw the subset of the mark’s data *not* in the current facet - *super* - draw this mark in a single frame that covers all facets - null (or false) - repeat this mark’s data across all facets (*i.e.*, no faceting) When a mark uses *super* faceting, it is not allowed to use position scales (*x*, *y*, *fx*, or *fy*); *super* faceting is intended for decorations, such as labels and legends.
#' @param facet_anchor `<"top" | "right" | "bottom" | "left" | "top-left" | "top-right" | "bottom-left" | "bottom-right" | "top-empty" | "right-empty" | "bottom-empty" | "left-empty" | "empty" | NULL | param()>` How to place the mark with respect to facets; one of: - null (default for most marks) - display the mark in each non-empty facet - *top*, *right*, *bottom*, or *left* - display the mark only in facets on the given side - *top-empty*, *right-empty*, *bottom-empty*, or *left-empty* (default for axis marks) - display the mark only in facets that have empty space on the given side: either the margin, or an empty facet - *empty* - display the mark in empty facets only
#' @param fill `<column | literal | transform() | sql()/agg() | list(value=, ...) | param()>` The fill; a constant CSS color string, or a channel typically bound to the *color* scale.
#' @param fill_opacity `<column | literal | transform() | sql()/agg() | list(value=, ...) | param()>` The fill-opacity; a constant number between 0 and 1, or a channel typically bound to the *opacity* scale.
#' @param filter `<column | literal | transform() | sql()/agg()>` Applies a transform to filter the mark’s index according to the given channel values; only truthy values are retained.
#' @param fx `<column | literal | transform() | sql()/agg()>` The horizontal facet position channel, for mark-level faceting, bound to the *fx* scale.
#' @param fy `<column | literal | transform() | sql()/agg()>` The vertical facet position channel, for mark-level faceting, bound to the *fy* scale.
#' @param href `<column | literal | transform() | sql()/agg()>` The href; a channel specifying URLs for clickable links.
#' @param image_filter `<string | param()>` A CSS filter; a constant string used to adjust the rendering of images, such as *blur(5px)*.
#' @param inset `<number | param()>` Shorthand to set the same default for **insetStart** and **insetEnd**.
#' @param inset_left `<number | param()>` Insets the left edge by the specified number of pixels.
#' @param inset_right `<number | param()>` Insets the right edge by the specified number of pixels.
#' @param interval `<"day"/"week"/"month"/... | number | param()>` Enforces uniformity for data at regular intervals, such as integer values or daily samples.
#' @param margin `<number | param()>` Shorthand to set the same default for all four mark margins: **marginTop**, **marginRight**, **marginBottom**, and **marginLeft**; typically defaults to 0, except for axis marks.
#' @param margin_bottom `<number | param()>` The mark’s bottom margin; the minimum distance in pixels between the bottom edges of the inner and outer plot area.
#' @param margin_left `<number | param()>` The mark’s left margin; the minimum distance in pixels between the left edges of the inner and outer plot area.
#' @param margin_right `<number | param()>` The mark’s right margin; the minimum distance in pixels between the right edges of the mark’s inner and outer plot area.
#' @param margin_top `<number | param()>` The mark’s top margin; the minimum distance in pixels between the top edges of the inner and outer plot area.
#' @param marker `<"arrow" | "arrow-reverse" | "dot" | "circle" | "circle-fill" | "circle-stroke" | "tick" | "tick-x" | "tick-y" | "none" | boolean | NULL | param()>` Shorthand to set the same default for markerStart, markerMid, and markerEnd; one of: - a marker name such as *arrow* or *circle* - *none* (default) - no marker * true - alias for *circle-fill* * false or null - alias for *none*
#' @param marker_end `<"arrow" | "arrow-reverse" | "dot" | "circle" | "circle-fill" | "circle-stroke" | "tick" | "tick-x" | "tick-y" | "none" | boolean | NULL | param()>` The marker for the ending point of a line segment; one of: - a marker name such as *arrow* or *circle* * *none* (default) - no marker * true - alias for *circle-fill* * false or null - alias for *none*
#' @param marker_mid `<"arrow" | "arrow-reverse" | "dot" | "circle" | "circle-fill" | "circle-stroke" | "tick" | "tick-x" | "tick-y" | "none" | boolean | NULL | param()>` The marker for any middle (interior) points of a line segment.
#' @param marker_start `<"arrow" | "arrow-reverse" | "dot" | "circle" | "circle-fill" | "circle-stroke" | "tick" | "tick-x" | "tick-y" | "none" | boolean | NULL | param()>` The marker for the starting point of a line segment; one of: - a marker name such as *arrow* or *circle* * *none* (default) - no marker * true - alias for *circle-fill* * false or null - alias for *none*
#' @param mix_blend_mode `<string | param()>` The mix-blend-mode; a constant string specifying how to blend content such as *multiply*.
#' @param opacity `<column | literal | transform() | sql()/agg() | list(value=, ...)>` The opacity; a constant between 0 and 1, or a channel typically bound to the *opacity* scale.
#' @param paint_order `<string | param()>` The paint-order; a constant string specifying the order in which the **fill**, **stroke**, and any markers are drawn; defaults to *normal*, which draws the fill, then stroke, then markers; defaults to *stroke* for the text mark to create a “halo” around text to improve legibility.
#' @param pointer_events `<string | param()>` The pointer-events property; a constant string such as *none*.
#' @param reverse `<boolean | param()>` Applies a transform to reverse the order of the mark’s index, say for reverse input order.
#' @param select `<"first" | "last" | "maxX" | "maxY" | "minX" | "minY" | "nearest" | "nearestX" | "nearestY">` Applies a filter transform after data is loaded to highlight selected values only.
#' @param shape_rendering `<string | param()>` The shape-rendering; a constant string such as *crispEdges*.
#' @param sort `<column | literal | transform() | sql()/agg() | list>` Either applies a transform to sort the mark’s index by the specified channel values, or imputes ordinal scale domains from this mark’s channels.
#' @param stroke `<column | literal | transform() | sql()/agg() | list(value=, ...) | param()>` The stroke; a constant CSS color string, or a channel typically bound to the *color* scale.
#' @param stroke_dasharray `<string | number | param()>` The stroke-dasharray; a constant number indicating the length in pixels of alternating dashes and gaps, or a constant string of numbers separated by spaces or commas (_e.g._, *10 2* for dashes of 10 pixels separated by gaps of 2 pixels), or *none* (the default) for no dashing : https://developer.mozilla.org/en-US/docs/Web/SVG/Attribute/stroke-dasharray
#' @param stroke_dashoffset `<string | number | param()>` The stroke-dashoffset; a constant indicating the offset in pixels of the first dash along the stroke; defaults to zero.
#' @param stroke_linecap `<string | param()>` The stroke-linecap; a constant specifying how to cap stroked paths, such as *butt*, *round*, or *square*.
#' @param stroke_linejoin `<string | param()>` The stroke-linejoin; a constant specifying how to join stroked paths, such as *bevel*, *miter*, *miter-clip*, or *round*.
#' @param stroke_miterlimit `<number | param()>` The stroke-miterlimit; a constant number specifying how to limit the length of *miter* joins on stroked paths.
#' @param stroke_opacity `<column | literal | transform() | sql()/agg() | list(value=, ...)>` The stroke-opacity; a constant between 0 and 1, or a channel typically bound to the *opacity* scale.
#' @param stroke_width `<column | literal | transform() | sql()/agg() | list(value=, ...)>` The stroke-width; a constant number in pixels, or a channel.
#' @param target `<string | param()>` The target; a constant string specifying the target window (_e.g._, *_blank*) for clickable links; used in conjunction with the **href** option.
#' @param tick_spacing `<number | param()>` The desired approximate spacing between adjacent axis ticks, affecting the default **ticks**; defaults to 80 pixels for *x* and *fx*, and 35 pixels for *y* and *fy*.
#' @param ticks `<number | "day"/"week"/"month"/... | vector | param()>` The desired approximate number of axis ticks, or an explicit array of tick values, or an interval such as *day* or *month*.
#' @param tip `<boolean | "x" | "y" | "xy" | list | param()>` Whether to generate a tooltip for this mark, and any tip options.
#' @param title `<column | literal | transform() | sql()/agg()>` The title; a channel specifying accessible, short textual descriptions as strings (possibly with newlines).
#' @param x `<column | literal | transform() | sql()/agg() | list(value=, ...)>` The horizontal position (or length) channel, typically bound to the *x* scale.
#' @param x1 `<column | literal | transform() | sql()/agg() | list(value=, ...)>` The required primary (starting, often left) horizontal position channel, representing the area’s baseline, typically bound to the *x* scale.
#' @param x2 `<column | literal | transform() | sql()/agg() | list(value=, ...)>` The optional secondary (ending, often right) horizontal position channel, representing the area’s topline, typically bound to the *x* scale; if not specified, **x1** is used.
#' @param y `<column | literal | transform() | sql()/agg() | list(value=, ...)>` The vertical position channel, typically bound to the *y* scale; defaults to the zero-based index of the data 0, 1, 2, ….
#' @param ... Additional options or plot-level attributes.
#' @param data_from The name of the data source this mark reads from (see [vg_data()]); a length-1 nonzero R integer (`1L`, `-1L`, ...; note the `L`) giving a 1-based index into the spec's registered data sources instead, negative counting from the end (e.g., `-1L` for the most recently registered one); or a literal vector of values to use as inline data directly (mosaic-spec's `"data": [...]` shorthand, e.g., for a single reference line -- a bare double like `0`/`c(0)`, or `0L` itself (never a valid index), means this, not an index). Left unset, defaults to the first registered data source (equivalent to `data_from = 1L`) for any mark type that takes data at all.
#' @param filter_by A Param/Selection (e.g., from [param()]) to filter this mark's data by.
#' @param data_optimize A flag (default `TRUE`) to enable mark-specific query optimizations for this mark's data; set `FALSE` to disable them (mosaic-spec's `data: {optimize: false}`).
#' @family mark functions
#' @export
vg_mark_grid_fy <- function(spec = NULL, formula = vg_unset, anchor = vg_unset, aria_description = vg_unset, aria_hidden = vg_unset, aria_label = vg_unset, channels = vg_unset, clip = vg_unset, color = vg_unset, dx = vg_unset, dy = vg_unset, facet = vg_unset, facet_anchor = vg_unset, fill = vg_unset, fill_opacity = vg_unset, filter = vg_unset, fx = vg_unset, fy = vg_unset, href = vg_unset, image_filter = vg_unset, inset = vg_unset, inset_left = vg_unset, inset_right = vg_unset, interval = vg_unset, margin = vg_unset, margin_bottom = vg_unset, margin_left = vg_unset, margin_right = vg_unset, margin_top = vg_unset, marker = vg_unset, marker_end = vg_unset, marker_mid = vg_unset, marker_start = vg_unset, mix_blend_mode = vg_unset, opacity = vg_unset, paint_order = vg_unset, pointer_events = vg_unset, reverse = vg_unset, select = vg_unset, shape_rendering = vg_unset, sort = vg_unset, stroke = vg_unset, stroke_dasharray = vg_unset, stroke_dashoffset = vg_unset, stroke_linecap = vg_unset, stroke_linejoin = vg_unset, stroke_miterlimit = vg_unset, stroke_opacity = vg_unset, stroke_width = vg_unset, target = vg_unset, tick_spacing = vg_unset, ticks = vg_unset, tip = vg_unset, title = vg_unset, x = vg_unset, x1 = vg_unset, x2 = vg_unset, y = vg_unset, ..., data_from = vg_unset, filter_by = vg_unset, data_optimize = vg_unset) {
  vg_mark_(spec, "gridFy", formula = formula, anchor = anchor, ariaDescription = aria_description, ariaHidden = aria_hidden, ariaLabel = aria_label, channels = channels, clip = clip, color = color, dx = dx, dy = dy, facet = facet, facetAnchor = facet_anchor, fill = fill, fillOpacity = fill_opacity, filter = filter, fx = fx, fy = fy, href = href, imageFilter = image_filter, inset = inset, insetLeft = inset_left, insetRight = inset_right, interval = interval, margin = margin, marginBottom = margin_bottom, marginLeft = margin_left, marginRight = margin_right, marginTop = margin_top, marker = marker, markerEnd = marker_end, markerMid = marker_mid, markerStart = marker_start, mixBlendMode = mix_blend_mode, opacity = opacity, paintOrder = paint_order, pointerEvents = pointer_events, reverse = reverse, select = select, shapeRendering = shape_rendering, sort = sort, stroke = stroke, strokeDasharray = stroke_dasharray, strokeDashoffset = stroke_dashoffset, strokeLinecap = stroke_linecap, strokeLinejoin = stroke_linejoin, strokeMiterlimit = stroke_miterlimit, strokeOpacity = stroke_opacity, strokeWidth = stroke_width, target = target, tickSpacing = tick_spacing, ticks = ticks, tip = tip, title = title, x = x, x1 = x1, x2 = x2, y = y, ..., data_from = data_from, filter_by = filter_by, data_optimize = data_optimize)
}

#' The gridX mark.
#'
#' See [vg_value_types] for what the `<...>` notation below (`column`, `param()`, ...) means.
#'
#' @param spec A plot fragment or `vgspec` to add this mark to, or `NULL` to start a new plot with just this mark.
#' @param formula A shorthand for this mark's position channels (`x`, `y`, `fx`, `fy`, and paired `x1`/`x2` or `y1`/`y2`), e.g., `Sepal.Length ~ Sepal.Width | ~ Species` for `y = ~Sepal.Length, x = ~Sepal.Width, fx = ~Species`. See [vg_mark()] for the full grammar.
#' @param anchor `<"top" | "right" | "bottom" | "left" | param()>` The side of the frame on which to place the axis: *top* or *bottom* for horizontal axes (axisX and axisFx) and their associated vertical grids (gridX and gridFx), or *left* or *right* for vertical axes (axisY and axisFY) and their associated horizontal grids (gridY and gridFy).
#' @param aria_description `<string | param()>` The aria-description; a constant textual description.
#' @param aria_hidden `<string | param()>` The aria-hidden state; a constant indicating whether the element is exposed to an accessibility API.
#' @param aria_label `<column | literal | transform() | sql()/agg()>` The aria-label; a channel specifying short textual labels representing the value in the accessibility tree.
#' @param channels `<list>` Additional named channels, for example to include in a tooltip.
#' @param clip `<"frame" | "sphere" | boolean | NULL | param()>` How to clip the mark; one of: - *frame* or true - clip to the plot’s frame (inner area) - *sphere* - clip to the projected sphere (*e.g.*, front hemisphere) - null or false - do not clip The *sphere* clip option requires a geographic projection.
#' @param color `<column | literal | transform() | sql()/agg() | list(value=, ...) | param()>` A shorthand for setting both **fill** and **stroke**; affects the stroke of tick vectors and grid rules, and the fill of tick texts and axis label texts; defaults to *currentColor*.
#' @param dx `<number | param()>` The horizontal offset in pixels; a constant option.
#' @param dy `<number | param()>` The vertical offset in pixels; a constant option.
#' @param facet `<"auto" | "include" | "exclude" | "super" | boolean | NULL | param()>` Whether to enable or disable faceting; one of: - *auto* (default) - automatically determine if this mark should be faceted - *include* (or true) - draw the subset of the mark’s data in the current facet - *exclude* - draw the subset of the mark’s data *not* in the current facet - *super* - draw this mark in a single frame that covers all facets - null (or false) - repeat this mark’s data across all facets (*i.e.*, no faceting) When a mark uses *super* faceting, it is not allowed to use position scales (*x*, *y*, *fx*, or *fy*); *super* faceting is intended for decorations, such as labels and legends.
#' @param facet_anchor `<"top" | "right" | "bottom" | "left" | "top-left" | "top-right" | "bottom-left" | "bottom-right" | "top-empty" | "right-empty" | "bottom-empty" | "left-empty" | "empty" | NULL | param()>` How to place the mark with respect to facets; one of: - null (default for most marks) - display the mark in each non-empty facet - *top*, *right*, *bottom*, or *left* - display the mark only in facets on the given side - *top-empty*, *right-empty*, *bottom-empty*, or *left-empty* (default for axis marks) - display the mark only in facets that have empty space on the given side: either the margin, or an empty facet - *empty* - display the mark in empty facets only
#' @param fill `<column | literal | transform() | sql()/agg() | list(value=, ...) | param()>` The fill; a constant CSS color string, or a channel typically bound to the *color* scale.
#' @param fill_opacity `<column | literal | transform() | sql()/agg() | list(value=, ...) | param()>` The fill-opacity; a constant number between 0 and 1, or a channel typically bound to the *opacity* scale.
#' @param filter `<column | literal | transform() | sql()/agg()>` Applies a transform to filter the mark’s index according to the given channel values; only truthy values are retained.
#' @param fx `<column | literal | transform() | sql()/agg()>` The horizontal facet position channel, for mark-level faceting, bound to the *fx* scale.
#' @param fy `<column | literal | transform() | sql()/agg()>` The vertical facet position channel, for mark-level faceting, bound to the *fy* scale.
#' @param href `<column | literal | transform() | sql()/agg()>` The href; a channel specifying URLs for clickable links.
#' @param image_filter `<string | param()>` A CSS filter; a constant string used to adjust the rendering of images, such as *blur(5px)*.
#' @param inset `<number | param()>` Shorthand to set the same default for **insetStart** and **insetEnd**.
#' @param inset_bottom `<number | param()>` Insets the bottom edge by the specified number of pixels.
#' @param inset_top `<number | param()>` Insets the top edge by the specified number of pixels.
#' @param interval `<"day"/"week"/"month"/... | number | param()>` Enforces uniformity for data at regular intervals, such as integer values or daily samples.
#' @param margin `<number | param()>` Shorthand to set the same default for all four mark margins: **marginTop**, **marginRight**, **marginBottom**, and **marginLeft**; typically defaults to 0, except for axis marks.
#' @param margin_bottom `<number | param()>` The mark’s bottom margin; the minimum distance in pixels between the bottom edges of the inner and outer plot area.
#' @param margin_left `<number | param()>` The mark’s left margin; the minimum distance in pixels between the left edges of the inner and outer plot area.
#' @param margin_right `<number | param()>` The mark’s right margin; the minimum distance in pixels between the right edges of the mark’s inner and outer plot area.
#' @param margin_top `<number | param()>` The mark’s top margin; the minimum distance in pixels between the top edges of the inner and outer plot area.
#' @param marker `<"arrow" | "arrow-reverse" | "dot" | "circle" | "circle-fill" | "circle-stroke" | "tick" | "tick-x" | "tick-y" | "none" | boolean | NULL | param()>` Shorthand to set the same default for markerStart, markerMid, and markerEnd; one of: - a marker name such as *arrow* or *circle* - *none* (default) - no marker * true - alias for *circle-fill* * false or null - alias for *none*
#' @param marker_end `<"arrow" | "arrow-reverse" | "dot" | "circle" | "circle-fill" | "circle-stroke" | "tick" | "tick-x" | "tick-y" | "none" | boolean | NULL | param()>` The marker for the ending point of a line segment; one of: - a marker name such as *arrow* or *circle* * *none* (default) - no marker * true - alias for *circle-fill* * false or null - alias for *none*
#' @param marker_mid `<"arrow" | "arrow-reverse" | "dot" | "circle" | "circle-fill" | "circle-stroke" | "tick" | "tick-x" | "tick-y" | "none" | boolean | NULL | param()>` The marker for any middle (interior) points of a line segment.
#' @param marker_start `<"arrow" | "arrow-reverse" | "dot" | "circle" | "circle-fill" | "circle-stroke" | "tick" | "tick-x" | "tick-y" | "none" | boolean | NULL | param()>` The marker for the starting point of a line segment; one of: - a marker name such as *arrow* or *circle* * *none* (default) - no marker * true - alias for *circle-fill* * false or null - alias for *none*
#' @param mix_blend_mode `<string | param()>` The mix-blend-mode; a constant string specifying how to blend content such as *multiply*.
#' @param opacity `<column | literal | transform() | sql()/agg() | list(value=, ...)>` The opacity; a constant between 0 and 1, or a channel typically bound to the *opacity* scale.
#' @param paint_order `<string | param()>` The paint-order; a constant string specifying the order in which the **fill**, **stroke**, and any markers are drawn; defaults to *normal*, which draws the fill, then stroke, then markers; defaults to *stroke* for the text mark to create a “halo” around text to improve legibility.
#' @param pointer_events `<string | param()>` The pointer-events property; a constant string such as *none*.
#' @param reverse `<boolean | param()>` Applies a transform to reverse the order of the mark’s index, say for reverse input order.
#' @param select `<"first" | "last" | "maxX" | "maxY" | "minX" | "minY" | "nearest" | "nearestX" | "nearestY">` Applies a filter transform after data is loaded to highlight selected values only.
#' @param shape_rendering `<string | param()>` The shape-rendering; a constant string such as *crispEdges*.
#' @param sort `<column | literal | transform() | sql()/agg() | list>` Either applies a transform to sort the mark’s index by the specified channel values, or imputes ordinal scale domains from this mark’s channels.
#' @param stroke `<column | literal | transform() | sql()/agg() | list(value=, ...) | param()>` The stroke; a constant CSS color string, or a channel typically bound to the *color* scale.
#' @param stroke_dasharray `<string | number | param()>` The stroke-dasharray; a constant number indicating the length in pixels of alternating dashes and gaps, or a constant string of numbers separated by spaces or commas (_e.g._, *10 2* for dashes of 10 pixels separated by gaps of 2 pixels), or *none* (the default) for no dashing : https://developer.mozilla.org/en-US/docs/Web/SVG/Attribute/stroke-dasharray
#' @param stroke_dashoffset `<string | number | param()>` The stroke-dashoffset; a constant indicating the offset in pixels of the first dash along the stroke; defaults to zero.
#' @param stroke_linecap `<string | param()>` The stroke-linecap; a constant specifying how to cap stroked paths, such as *butt*, *round*, or *square*.
#' @param stroke_linejoin `<string | param()>` The stroke-linejoin; a constant specifying how to join stroked paths, such as *bevel*, *miter*, *miter-clip*, or *round*.
#' @param stroke_miterlimit `<number | param()>` The stroke-miterlimit; a constant number specifying how to limit the length of *miter* joins on stroked paths.
#' @param stroke_opacity `<column | literal | transform() | sql()/agg() | list(value=, ...)>` The stroke-opacity; a constant between 0 and 1, or a channel typically bound to the *opacity* scale.
#' @param stroke_width `<column | literal | transform() | sql()/agg() | list(value=, ...)>` The stroke-width; a constant number in pixels, or a channel.
#' @param target `<string | param()>` The target; a constant string specifying the target window (_e.g._, *_blank*) for clickable links; used in conjunction with the **href** option.
#' @param tick_spacing `<number | param()>` The desired approximate spacing between adjacent axis ticks, affecting the default **ticks**; defaults to 80 pixels for *x* and *fx*, and 35 pixels for *y* and *fy*.
#' @param ticks `<number | "day"/"week"/"month"/... | vector | param()>` The desired approximate number of axis ticks, or an explicit array of tick values, or an interval such as *day* or *month*.
#' @param tip `<boolean | "x" | "y" | "xy" | list | param()>` Whether to generate a tooltip for this mark, and any tip options.
#' @param title `<column | literal | transform() | sql()/agg()>` The title; a channel specifying accessible, short textual descriptions as strings (possibly with newlines).
#' @param x `<column | literal | transform() | sql()/agg() | list(value=, ...)>` The horizontal position (or length) channel, typically bound to the *x* scale.
#' @param y `<column | literal | transform() | sql()/agg() | list(value=, ...)>` The vertical position channel, typically bound to the *y* scale; defaults to the zero-based index of the data 0, 1, 2, ….
#' @param y1 `<column | literal | transform() | sql()/agg() | list(value=, ...)>` The required primary (starting, often bottom) vertical position channel, representing the area’s baseline, typically bound to the *y* scale.
#' @param y2 `<column | literal | transform() | sql()/agg() | list(value=, ...)>` The optional secondary (ending, often top) vertical position channel, representing the area’s topline, typically bound to the *y* scale; if not specified, **y1** is used.
#' @param ... Additional options or plot-level attributes.
#' @param data_from The name of the data source this mark reads from (see [vg_data()]); a length-1 nonzero R integer (`1L`, `-1L`, ...; note the `L`) giving a 1-based index into the spec's registered data sources instead, negative counting from the end (e.g., `-1L` for the most recently registered one); or a literal vector of values to use as inline data directly (mosaic-spec's `"data": [...]` shorthand, e.g., for a single reference line -- a bare double like `0`/`c(0)`, or `0L` itself (never a valid index), means this, not an index). Left unset, defaults to the first registered data source (equivalent to `data_from = 1L`) for any mark type that takes data at all.
#' @param filter_by A Param/Selection (e.g., from [param()]) to filter this mark's data by.
#' @param data_optimize A flag (default `TRUE`) to enable mark-specific query optimizations for this mark's data; set `FALSE` to disable them (mosaic-spec's `data: {optimize: false}`).
#' @family mark functions
#' @export
vg_mark_grid_x <- function(spec = NULL, formula = vg_unset, anchor = vg_unset, aria_description = vg_unset, aria_hidden = vg_unset, aria_label = vg_unset, channels = vg_unset, clip = vg_unset, color = vg_unset, dx = vg_unset, dy = vg_unset, facet = vg_unset, facet_anchor = vg_unset, fill = vg_unset, fill_opacity = vg_unset, filter = vg_unset, fx = vg_unset, fy = vg_unset, href = vg_unset, image_filter = vg_unset, inset = vg_unset, inset_bottom = vg_unset, inset_top = vg_unset, interval = vg_unset, margin = vg_unset, margin_bottom = vg_unset, margin_left = vg_unset, margin_right = vg_unset, margin_top = vg_unset, marker = vg_unset, marker_end = vg_unset, marker_mid = vg_unset, marker_start = vg_unset, mix_blend_mode = vg_unset, opacity = vg_unset, paint_order = vg_unset, pointer_events = vg_unset, reverse = vg_unset, select = vg_unset, shape_rendering = vg_unset, sort = vg_unset, stroke = vg_unset, stroke_dasharray = vg_unset, stroke_dashoffset = vg_unset, stroke_linecap = vg_unset, stroke_linejoin = vg_unset, stroke_miterlimit = vg_unset, stroke_opacity = vg_unset, stroke_width = vg_unset, target = vg_unset, tick_spacing = vg_unset, ticks = vg_unset, tip = vg_unset, title = vg_unset, x = vg_unset, y = vg_unset, y1 = vg_unset, y2 = vg_unset, ..., data_from = vg_unset, filter_by = vg_unset, data_optimize = vg_unset) {
  vg_mark_(spec, "gridX", formula = formula, anchor = anchor, ariaDescription = aria_description, ariaHidden = aria_hidden, ariaLabel = aria_label, channels = channels, clip = clip, color = color, dx = dx, dy = dy, facet = facet, facetAnchor = facet_anchor, fill = fill, fillOpacity = fill_opacity, filter = filter, fx = fx, fy = fy, href = href, imageFilter = image_filter, inset = inset, insetBottom = inset_bottom, insetTop = inset_top, interval = interval, margin = margin, marginBottom = margin_bottom, marginLeft = margin_left, marginRight = margin_right, marginTop = margin_top, marker = marker, markerEnd = marker_end, markerMid = marker_mid, markerStart = marker_start, mixBlendMode = mix_blend_mode, opacity = opacity, paintOrder = paint_order, pointerEvents = pointer_events, reverse = reverse, select = select, shapeRendering = shape_rendering, sort = sort, stroke = stroke, strokeDasharray = stroke_dasharray, strokeDashoffset = stroke_dashoffset, strokeLinecap = stroke_linecap, strokeLinejoin = stroke_linejoin, strokeMiterlimit = stroke_miterlimit, strokeOpacity = stroke_opacity, strokeWidth = stroke_width, target = target, tickSpacing = tick_spacing, ticks = ticks, tip = tip, title = title, x = x, y = y, y1 = y1, y2 = y2, ..., data_from = data_from, filter_by = filter_by, data_optimize = data_optimize)
}

#' The gridY mark.
#'
#' See [vg_value_types] for what the `<...>` notation below (`column`, `param()`, ...) means.
#'
#' @param spec A plot fragment or `vgspec` to add this mark to, or `NULL` to start a new plot with just this mark.
#' @param formula A shorthand for this mark's position channels (`x`, `y`, `fx`, `fy`, and paired `x1`/`x2` or `y1`/`y2`), e.g., `Sepal.Length ~ Sepal.Width | ~ Species` for `y = ~Sepal.Length, x = ~Sepal.Width, fx = ~Species`. See [vg_mark()] for the full grammar.
#' @param anchor `<"top" | "right" | "bottom" | "left" | param()>` The side of the frame on which to place the axis: *top* or *bottom* for horizontal axes (axisX and axisFx) and their associated vertical grids (gridX and gridFx), or *left* or *right* for vertical axes (axisY and axisFY) and their associated horizontal grids (gridY and gridFy).
#' @param aria_description `<string | param()>` The aria-description; a constant textual description.
#' @param aria_hidden `<string | param()>` The aria-hidden state; a constant indicating whether the element is exposed to an accessibility API.
#' @param aria_label `<column | literal | transform() | sql()/agg()>` The aria-label; a channel specifying short textual labels representing the value in the accessibility tree.
#' @param channels `<list>` Additional named channels, for example to include in a tooltip.
#' @param clip `<"frame" | "sphere" | boolean | NULL | param()>` How to clip the mark; one of: - *frame* or true - clip to the plot’s frame (inner area) - *sphere* - clip to the projected sphere (*e.g.*, front hemisphere) - null or false - do not clip The *sphere* clip option requires a geographic projection.
#' @param color `<column | literal | transform() | sql()/agg() | list(value=, ...) | param()>` A shorthand for setting both **fill** and **stroke**; affects the stroke of tick vectors and grid rules, and the fill of tick texts and axis label texts; defaults to *currentColor*.
#' @param dx `<number | param()>` The horizontal offset in pixels; a constant option.
#' @param dy `<number | param()>` The vertical offset in pixels; a constant option.
#' @param facet `<"auto" | "include" | "exclude" | "super" | boolean | NULL | param()>` Whether to enable or disable faceting; one of: - *auto* (default) - automatically determine if this mark should be faceted - *include* (or true) - draw the subset of the mark’s data in the current facet - *exclude* - draw the subset of the mark’s data *not* in the current facet - *super* - draw this mark in a single frame that covers all facets - null (or false) - repeat this mark’s data across all facets (*i.e.*, no faceting) When a mark uses *super* faceting, it is not allowed to use position scales (*x*, *y*, *fx*, or *fy*); *super* faceting is intended for decorations, such as labels and legends.
#' @param facet_anchor `<"top" | "right" | "bottom" | "left" | "top-left" | "top-right" | "bottom-left" | "bottom-right" | "top-empty" | "right-empty" | "bottom-empty" | "left-empty" | "empty" | NULL | param()>` How to place the mark with respect to facets; one of: - null (default for most marks) - display the mark in each non-empty facet - *top*, *right*, *bottom*, or *left* - display the mark only in facets on the given side - *top-empty*, *right-empty*, *bottom-empty*, or *left-empty* (default for axis marks) - display the mark only in facets that have empty space on the given side: either the margin, or an empty facet - *empty* - display the mark in empty facets only
#' @param fill `<column | literal | transform() | sql()/agg() | list(value=, ...) | param()>` The fill; a constant CSS color string, or a channel typically bound to the *color* scale.
#' @param fill_opacity `<column | literal | transform() | sql()/agg() | list(value=, ...) | param()>` The fill-opacity; a constant number between 0 and 1, or a channel typically bound to the *opacity* scale.
#' @param filter `<column | literal | transform() | sql()/agg()>` Applies a transform to filter the mark’s index according to the given channel values; only truthy values are retained.
#' @param fx `<column | literal | transform() | sql()/agg()>` The horizontal facet position channel, for mark-level faceting, bound to the *fx* scale.
#' @param fy `<column | literal | transform() | sql()/agg()>` The vertical facet position channel, for mark-level faceting, bound to the *fy* scale.
#' @param href `<column | literal | transform() | sql()/agg()>` The href; a channel specifying URLs for clickable links.
#' @param image_filter `<string | param()>` A CSS filter; a constant string used to adjust the rendering of images, such as *blur(5px)*.
#' @param inset `<number | param()>` Shorthand to set the same default for **insetStart** and **insetEnd**.
#' @param inset_left `<number | param()>` Insets the left edge by the specified number of pixels.
#' @param inset_right `<number | param()>` Insets the right edge by the specified number of pixels.
#' @param interval `<"day"/"week"/"month"/... | number | param()>` Enforces uniformity for data at regular intervals, such as integer values or daily samples.
#' @param margin `<number | param()>` Shorthand to set the same default for all four mark margins: **marginTop**, **marginRight**, **marginBottom**, and **marginLeft**; typically defaults to 0, except for axis marks.
#' @param margin_bottom `<number | param()>` The mark’s bottom margin; the minimum distance in pixels between the bottom edges of the inner and outer plot area.
#' @param margin_left `<number | param()>` The mark’s left margin; the minimum distance in pixels between the left edges of the inner and outer plot area.
#' @param margin_right `<number | param()>` The mark’s right margin; the minimum distance in pixels between the right edges of the mark’s inner and outer plot area.
#' @param margin_top `<number | param()>` The mark’s top margin; the minimum distance in pixels between the top edges of the inner and outer plot area.
#' @param marker `<"arrow" | "arrow-reverse" | "dot" | "circle" | "circle-fill" | "circle-stroke" | "tick" | "tick-x" | "tick-y" | "none" | boolean | NULL | param()>` Shorthand to set the same default for markerStart, markerMid, and markerEnd; one of: - a marker name such as *arrow* or *circle* - *none* (default) - no marker * true - alias for *circle-fill* * false or null - alias for *none*
#' @param marker_end `<"arrow" | "arrow-reverse" | "dot" | "circle" | "circle-fill" | "circle-stroke" | "tick" | "tick-x" | "tick-y" | "none" | boolean | NULL | param()>` The marker for the ending point of a line segment; one of: - a marker name such as *arrow* or *circle* * *none* (default) - no marker * true - alias for *circle-fill* * false or null - alias for *none*
#' @param marker_mid `<"arrow" | "arrow-reverse" | "dot" | "circle" | "circle-fill" | "circle-stroke" | "tick" | "tick-x" | "tick-y" | "none" | boolean | NULL | param()>` The marker for any middle (interior) points of a line segment.
#' @param marker_start `<"arrow" | "arrow-reverse" | "dot" | "circle" | "circle-fill" | "circle-stroke" | "tick" | "tick-x" | "tick-y" | "none" | boolean | NULL | param()>` The marker for the starting point of a line segment; one of: - a marker name such as *arrow* or *circle* * *none* (default) - no marker * true - alias for *circle-fill* * false or null - alias for *none*
#' @param mix_blend_mode `<string | param()>` The mix-blend-mode; a constant string specifying how to blend content such as *multiply*.
#' @param opacity `<column | literal | transform() | sql()/agg() | list(value=, ...)>` The opacity; a constant between 0 and 1, or a channel typically bound to the *opacity* scale.
#' @param paint_order `<string | param()>` The paint-order; a constant string specifying the order in which the **fill**, **stroke**, and any markers are drawn; defaults to *normal*, which draws the fill, then stroke, then markers; defaults to *stroke* for the text mark to create a “halo” around text to improve legibility.
#' @param pointer_events `<string | param()>` The pointer-events property; a constant string such as *none*.
#' @param reverse `<boolean | param()>` Applies a transform to reverse the order of the mark’s index, say for reverse input order.
#' @param select `<"first" | "last" | "maxX" | "maxY" | "minX" | "minY" | "nearest" | "nearestX" | "nearestY">` Applies a filter transform after data is loaded to highlight selected values only.
#' @param shape_rendering `<string | param()>` The shape-rendering; a constant string such as *crispEdges*.
#' @param sort `<column | literal | transform() | sql()/agg() | list>` Either applies a transform to sort the mark’s index by the specified channel values, or imputes ordinal scale domains from this mark’s channels.
#' @param stroke `<column | literal | transform() | sql()/agg() | list(value=, ...) | param()>` The stroke; a constant CSS color string, or a channel typically bound to the *color* scale.
#' @param stroke_dasharray `<string | number | param()>` The stroke-dasharray; a constant number indicating the length in pixels of alternating dashes and gaps, or a constant string of numbers separated by spaces or commas (_e.g._, *10 2* for dashes of 10 pixels separated by gaps of 2 pixels), or *none* (the default) for no dashing : https://developer.mozilla.org/en-US/docs/Web/SVG/Attribute/stroke-dasharray
#' @param stroke_dashoffset `<string | number | param()>` The stroke-dashoffset; a constant indicating the offset in pixels of the first dash along the stroke; defaults to zero.
#' @param stroke_linecap `<string | param()>` The stroke-linecap; a constant specifying how to cap stroked paths, such as *butt*, *round*, or *square*.
#' @param stroke_linejoin `<string | param()>` The stroke-linejoin; a constant specifying how to join stroked paths, such as *bevel*, *miter*, *miter-clip*, or *round*.
#' @param stroke_miterlimit `<number | param()>` The stroke-miterlimit; a constant number specifying how to limit the length of *miter* joins on stroked paths.
#' @param stroke_opacity `<column | literal | transform() | sql()/agg() | list(value=, ...)>` The stroke-opacity; a constant between 0 and 1, or a channel typically bound to the *opacity* scale.
#' @param stroke_width `<column | literal | transform() | sql()/agg() | list(value=, ...)>` The stroke-width; a constant number in pixels, or a channel.
#' @param target `<string | param()>` The target; a constant string specifying the target window (_e.g._, *_blank*) for clickable links; used in conjunction with the **href** option.
#' @param tick_spacing `<number | param()>` The desired approximate spacing between adjacent axis ticks, affecting the default **ticks**; defaults to 80 pixels for *x* and *fx*, and 35 pixels for *y* and *fy*.
#' @param ticks `<number | "day"/"week"/"month"/... | vector | param()>` The desired approximate number of axis ticks, or an explicit array of tick values, or an interval such as *day* or *month*.
#' @param tip `<boolean | "x" | "y" | "xy" | list | param()>` Whether to generate a tooltip for this mark, and any tip options.
#' @param title `<column | literal | transform() | sql()/agg()>` The title; a channel specifying accessible, short textual descriptions as strings (possibly with newlines).
#' @param x `<column | literal | transform() | sql()/agg() | list(value=, ...)>` The horizontal position (or length) channel, typically bound to the *x* scale.
#' @param x1 `<column | literal | transform() | sql()/agg() | list(value=, ...)>` The required primary (starting, often left) horizontal position channel, representing the area’s baseline, typically bound to the *x* scale.
#' @param x2 `<column | literal | transform() | sql()/agg() | list(value=, ...)>` The optional secondary (ending, often right) horizontal position channel, representing the area’s topline, typically bound to the *x* scale; if not specified, **x1** is used.
#' @param y `<column | literal | transform() | sql()/agg() | list(value=, ...)>` The vertical position channel, typically bound to the *y* scale; defaults to the zero-based index of the data 0, 1, 2, ….
#' @param ... Additional options or plot-level attributes.
#' @param data_from The name of the data source this mark reads from (see [vg_data()]); a length-1 nonzero R integer (`1L`, `-1L`, ...; note the `L`) giving a 1-based index into the spec's registered data sources instead, negative counting from the end (e.g., `-1L` for the most recently registered one); or a literal vector of values to use as inline data directly (mosaic-spec's `"data": [...]` shorthand, e.g., for a single reference line -- a bare double like `0`/`c(0)`, or `0L` itself (never a valid index), means this, not an index). Left unset, defaults to the first registered data source (equivalent to `data_from = 1L`) for any mark type that takes data at all.
#' @param filter_by A Param/Selection (e.g., from [param()]) to filter this mark's data by.
#' @param data_optimize A flag (default `TRUE`) to enable mark-specific query optimizations for this mark's data; set `FALSE` to disable them (mosaic-spec's `data: {optimize: false}`).
#' @family mark functions
#' @export
vg_mark_grid_y <- function(spec = NULL, formula = vg_unset, anchor = vg_unset, aria_description = vg_unset, aria_hidden = vg_unset, aria_label = vg_unset, channels = vg_unset, clip = vg_unset, color = vg_unset, dx = vg_unset, dy = vg_unset, facet = vg_unset, facet_anchor = vg_unset, fill = vg_unset, fill_opacity = vg_unset, filter = vg_unset, fx = vg_unset, fy = vg_unset, href = vg_unset, image_filter = vg_unset, inset = vg_unset, inset_left = vg_unset, inset_right = vg_unset, interval = vg_unset, margin = vg_unset, margin_bottom = vg_unset, margin_left = vg_unset, margin_right = vg_unset, margin_top = vg_unset, marker = vg_unset, marker_end = vg_unset, marker_mid = vg_unset, marker_start = vg_unset, mix_blend_mode = vg_unset, opacity = vg_unset, paint_order = vg_unset, pointer_events = vg_unset, reverse = vg_unset, select = vg_unset, shape_rendering = vg_unset, sort = vg_unset, stroke = vg_unset, stroke_dasharray = vg_unset, stroke_dashoffset = vg_unset, stroke_linecap = vg_unset, stroke_linejoin = vg_unset, stroke_miterlimit = vg_unset, stroke_opacity = vg_unset, stroke_width = vg_unset, target = vg_unset, tick_spacing = vg_unset, ticks = vg_unset, tip = vg_unset, title = vg_unset, x = vg_unset, x1 = vg_unset, x2 = vg_unset, y = vg_unset, ..., data_from = vg_unset, filter_by = vg_unset, data_optimize = vg_unset) {
  vg_mark_(spec, "gridY", formula = formula, anchor = anchor, ariaDescription = aria_description, ariaHidden = aria_hidden, ariaLabel = aria_label, channels = channels, clip = clip, color = color, dx = dx, dy = dy, facet = facet, facetAnchor = facet_anchor, fill = fill, fillOpacity = fill_opacity, filter = filter, fx = fx, fy = fy, href = href, imageFilter = image_filter, inset = inset, insetLeft = inset_left, insetRight = inset_right, interval = interval, margin = margin, marginBottom = margin_bottom, marginLeft = margin_left, marginRight = margin_right, marginTop = margin_top, marker = marker, markerEnd = marker_end, markerMid = marker_mid, markerStart = marker_start, mixBlendMode = mix_blend_mode, opacity = opacity, paintOrder = paint_order, pointerEvents = pointer_events, reverse = reverse, select = select, shapeRendering = shape_rendering, sort = sort, stroke = stroke, strokeDasharray = stroke_dasharray, strokeDashoffset = stroke_dashoffset, strokeLinecap = stroke_linecap, strokeLinejoin = stroke_linejoin, strokeMiterlimit = stroke_miterlimit, strokeOpacity = stroke_opacity, strokeWidth = stroke_width, target = target, tickSpacing = tick_spacing, ticks = ticks, tip = tip, title = title, x = x, x1 = x1, x2 = x2, y = y, ..., data_from = data_from, filter_by = filter_by, data_optimize = data_optimize)
}

#' The heatmap mark.
#'
#' See [vg_value_types] for what the `<...>` notation below (`column`, `param()`, ...) means.
#'
#' @param spec A plot fragment or `vgspec` to add this mark to, or `NULL` to start a new plot with just this mark.
#' @param formula A shorthand for this mark's position channels (`x`, `y`, `fx`, `fy`, and paired `x1`/`x2` or `y1`/`y2`), e.g., `Sepal.Length ~ Sepal.Width | ~ Species` for `y = ~Sepal.Length, x = ~Sepal.Width, fx = ~Species`. See [vg_mark()] for the full grammar.
#' @param aria_description `<string | param()>` The aria-description; a constant textual description.
#' @param aria_hidden `<string | param()>` The aria-hidden state; a constant indicating whether the element is exposed to an accessibility API.
#' @param aria_label `<column | literal | transform() | sql()/agg()>` The aria-label; a channel specifying short textual labels representing the value in the accessibility tree.
#' @param bandwidth `<number | param()>` The kernel density bandwidth for smoothing, in pixels.
#' @param channels `<list>` Additional named channels, for example to include in a tooltip.
#' @param clip `<"frame" | "sphere" | boolean | NULL | param()>` How to clip the mark; one of: - *frame* or true - clip to the plot’s frame (inner area) - *sphere* - clip to the projected sphere (*e.g.*, front hemisphere) - null or false - do not clip The *sphere* clip option requires a geographic projection.
#' @param dx `<number | param()>` The horizontal offset in pixels; a constant option.
#' @param dy `<number | param()>` The vertical offset in pixels; a constant option.
#' @param facet `<"auto" | "include" | "exclude" | "super" | boolean | NULL | param()>` Whether to enable or disable faceting; one of: - *auto* (default) - automatically determine if this mark should be faceted - *include* (or true) - draw the subset of the mark’s data in the current facet - *exclude* - draw the subset of the mark’s data *not* in the current facet - *super* - draw this mark in a single frame that covers all facets - null (or false) - repeat this mark’s data across all facets (*i.e.*, no faceting) When a mark uses *super* faceting, it is not allowed to use position scales (*x*, *y*, *fx*, or *fy*); *super* faceting is intended for decorations, such as labels and legends.
#' @param facet_anchor `<"top" | "right" | "bottom" | "left" | "top-left" | "top-right" | "bottom-left" | "bottom-right" | "top-empty" | "right-empty" | "bottom-empty" | "left-empty" | "empty" | NULL | param()>` How to place the mark with respect to facets; one of: - null (default for most marks) - display the mark in each non-empty facet - *top*, *right*, *bottom*, or *left* - display the mark only in facets on the given side - *top-empty*, *right-empty*, *bottom-empty*, or *left-empty* (default for axis marks) - display the mark only in facets that have empty space on the given side: either the margin, or an empty facet - *empty* - display the mark in empty facets only
#' @param fill `<column | literal | transform() | sql()/agg() | list(value=, ...) | param()>` The fill; a constant CSS color string, or a channel typically bound to the *color* scale.
#' @param fill_opacity `<column | literal | transform() | sql()/agg() | list(value=, ...) | param()>` The fill-opacity; a constant number between 0 and 1, or a channel typically bound to the *opacity* scale.
#' @param filter `<column | literal | transform() | sql()/agg()>` Applies a transform to filter the mark’s index according to the given channel values; only truthy values are retained.
#' @param fx `<column | literal | transform() | sql()/agg()>` The horizontal facet position channel, for mark-level faceting, bound to the *fx* scale.
#' @param fy `<column | literal | transform() | sql()/agg()>` The vertical facet position channel, for mark-level faceting, bound to the *fy* scale.
#' @param height `<number | param()>` The height (number of rows) of the grid, in actual pixels.
#' @param href `<column | literal | transform() | sql()/agg()>` The href; a channel specifying URLs for clickable links.
#' @param image_filter `<string | param()>` A CSS filter; a constant string used to adjust the rendering of images, such as *blur(5px)*.
#' @param image_rendering `<string | param()>` The image-rendering attribute; defaults to *auto* (bilinear).
#' @param interpolate `<"none" | "linear" | "nearest" | "barycentric" | "random-walk" | NULL | param()>` The spatial interpolation method; one of: - *none* - do not perform interpolation (the default), maps samples to single bins - *linear* - apply proportional linear interpolation across adjacent bins - *nearest* - assign each pixel to the closest sample’s value (Voronoi diagram) - *barycentric* - apply barycentric interpolation over the Delaunay triangulation - *random-walk* - apply a random walk from each pixel, stopping when near a sample
#' @param margin `<number | param()>` Shorthand to set the same default for all four mark margins: **marginTop**, **marginRight**, **marginBottom**, and **marginLeft**; typically defaults to 0, except for axis marks.
#' @param margin_bottom `<number | param()>` The mark’s bottom margin; the minimum distance in pixels between the bottom edges of the inner and outer plot area.
#' @param margin_left `<number | param()>` The mark’s left margin; the minimum distance in pixels between the left edges of the inner and outer plot area.
#' @param margin_right `<number | param()>` The mark’s right margin; the minimum distance in pixels between the right edges of the mark’s inner and outer plot area.
#' @param margin_top `<number | param()>` The mark’s top margin; the minimum distance in pixels between the top edges of the inner and outer plot area.
#' @param mix_blend_mode `<string | param()>` The mix-blend-mode; a constant string specifying how to blend content such as *multiply*.
#' @param opacity `<column | literal | transform() | sql()/agg() | list(value=, ...)>` The opacity; a constant between 0 and 1, or a channel typically bound to the *opacity* scale.
#' @param pad `<number | param()>` The bin padding, one of 1 (default) to include extra padding for the final bin, or 0 to make the bins flush with the maximum domain value.
#' @param paint_order `<string | param()>` The paint-order; a constant string specifying the order in which the **fill**, **stroke**, and any markers are drawn; defaults to *normal*, which draws the fill, then stroke, then markers; defaults to *stroke* for the text mark to create a “halo” around text to improve legibility.
#' @param pixel_size `<number | param()>` The effective screen size of a raster pixel, used to determine the height and width of the raster from the frame’s dimensions; defaults to 1.
#' @param pointer_events `<string | param()>` The pointer-events property; a constant string such as *none*.
#' @param reverse `<boolean | param()>` Applies a transform to reverse the order of the mark’s index, say for reverse input order.
#' @param select `<"first" | "last" | "maxX" | "maxY" | "minX" | "minY" | "nearest" | "nearestX" | "nearestY">` Applies a filter transform after data is loaded to highlight selected values only.
#' @param shape_rendering `<string | param()>` The shape-rendering; a constant string such as *crispEdges*.
#' @param sort `<column | literal | transform() | sql()/agg() | list>` Either applies a transform to sort the mark’s index by the specified channel values, or imputes ordinal scale domains from this mark’s channels.
#' @param stroke `<column | literal | transform() | sql()/agg() | list(value=, ...) | param()>` The stroke; a constant CSS color string, or a channel typically bound to the *color* scale.
#' @param stroke_dasharray `<string | number | param()>` The stroke-dasharray; a constant number indicating the length in pixels of alternating dashes and gaps, or a constant string of numbers separated by spaces or commas (_e.g._, *10 2* for dashes of 10 pixels separated by gaps of 2 pixels), or *none* (the default) for no dashing : https://developer.mozilla.org/en-US/docs/Web/SVG/Attribute/stroke-dasharray
#' @param stroke_dashoffset `<string | number | param()>` The stroke-dashoffset; a constant indicating the offset in pixels of the first dash along the stroke; defaults to zero.
#' @param stroke_linecap `<string | param()>` The stroke-linecap; a constant specifying how to cap stroked paths, such as *butt*, *round*, or *square*.
#' @param stroke_linejoin `<string | param()>` The stroke-linejoin; a constant specifying how to join stroked paths, such as *bevel*, *miter*, *miter-clip*, or *round*.
#' @param stroke_miterlimit `<number | param()>` The stroke-miterlimit; a constant number specifying how to limit the length of *miter* joins on stroked paths.
#' @param stroke_opacity `<column | literal | transform() | sql()/agg() | list(value=, ...)>` The stroke-opacity; a constant between 0 and 1, or a channel typically bound to the *opacity* scale.
#' @param stroke_width `<column | literal | transform() | sql()/agg() | list(value=, ...)>` The stroke-width; a constant number in pixels, or a channel.
#' @param target `<string | param()>` The target; a constant string specifying the target window (_e.g._, *_blank*) for clickable links; used in conjunction with the **href** option.
#' @param tip `<boolean | "x" | "y" | "xy" | list | param()>` Whether to generate a tooltip for this mark, and any tip options.
#' @param title `<column | literal | transform() | sql()/agg()>` The title; a channel specifying accessible, short textual descriptions as strings (possibly with newlines).
#' @param width `<number | param()>` The width (number of columns) of the grid, in actual pixels.
#' @param x `<column | literal | transform() | sql()/agg() | list(value=, ...)>` The horizontal position (or length) channel, typically bound to the *x* scale.
#' @param y `<column | literal | transform() | sql()/agg() | list(value=, ...)>` The vertical position channel, typically bound to the *y* scale; defaults to the zero-based index of the data 0, 1, 2, ….
#' @param ... Additional options or plot-level attributes.
#' @param data_from The name of the data source this mark reads from (see [vg_data()]); a length-1 nonzero R integer (`1L`, `-1L`, ...; note the `L`) giving a 1-based index into the spec's registered data sources instead, negative counting from the end (e.g., `-1L` for the most recently registered one); or a literal vector of values to use as inline data directly (mosaic-spec's `"data": [...]` shorthand, e.g., for a single reference line -- a bare double like `0`/`c(0)`, or `0L` itself (never a valid index), means this, not an index). Left unset, defaults to the first registered data source (equivalent to `data_from = 1L`) for any mark type that takes data at all.
#' @param filter_by A Param/Selection (e.g., from [param()]) to filter this mark's data by.
#' @param data_optimize A flag (default `TRUE`) to enable mark-specific query optimizations for this mark's data; set `FALSE` to disable them (mosaic-spec's `data: {optimize: false}`).
#' @family mark functions
#' @export
vg_mark_heatmap <- function(spec = NULL, formula = vg_unset, aria_description = vg_unset, aria_hidden = vg_unset, aria_label = vg_unset, bandwidth = vg_unset, channels = vg_unset, clip = vg_unset, dx = vg_unset, dy = vg_unset, facet = vg_unset, facet_anchor = vg_unset, fill = vg_unset, fill_opacity = vg_unset, filter = vg_unset, fx = vg_unset, fy = vg_unset, height = vg_unset, href = vg_unset, image_filter = vg_unset, image_rendering = vg_unset, interpolate = vg_unset, margin = vg_unset, margin_bottom = vg_unset, margin_left = vg_unset, margin_right = vg_unset, margin_top = vg_unset, mix_blend_mode = vg_unset, opacity = vg_unset, pad = vg_unset, paint_order = vg_unset, pixel_size = vg_unset, pointer_events = vg_unset, reverse = vg_unset, select = vg_unset, shape_rendering = vg_unset, sort = vg_unset, stroke = vg_unset, stroke_dasharray = vg_unset, stroke_dashoffset = vg_unset, stroke_linecap = vg_unset, stroke_linejoin = vg_unset, stroke_miterlimit = vg_unset, stroke_opacity = vg_unset, stroke_width = vg_unset, target = vg_unset, tip = vg_unset, title = vg_unset, width = vg_unset, x = vg_unset, y = vg_unset, ..., data_from = vg_unset, filter_by = vg_unset, data_optimize = vg_unset) {
  vg_mark_(spec, "heatmap", formula = formula, ariaDescription = aria_description, ariaHidden = aria_hidden, ariaLabel = aria_label, bandwidth = bandwidth, channels = channels, clip = clip, dx = dx, dy = dy, facet = facet, facetAnchor = facet_anchor, fill = fill, fillOpacity = fill_opacity, filter = filter, fx = fx, fy = fy, height = height, href = href, imageFilter = image_filter, imageRendering = image_rendering, interpolate = interpolate, margin = margin, marginBottom = margin_bottom, marginLeft = margin_left, marginRight = margin_right, marginTop = margin_top, mixBlendMode = mix_blend_mode, opacity = opacity, pad = pad, paintOrder = paint_order, pixelSize = pixel_size, pointerEvents = pointer_events, reverse = reverse, select = select, shapeRendering = shape_rendering, sort = sort, stroke = stroke, strokeDasharray = stroke_dasharray, strokeDashoffset = stroke_dashoffset, strokeLinecap = stroke_linecap, strokeLinejoin = stroke_linejoin, strokeMiterlimit = stroke_miterlimit, strokeOpacity = stroke_opacity, strokeWidth = stroke_width, target = target, tip = tip, title = title, width = width, x = x, y = y, ..., data_from = data_from, filter_by = filter_by, data_optimize = data_optimize)
}

#' The hexagon mark.
#'
#' See [vg_value_types] for what the `<...>` notation below (`column`, `param()`, ...) means.
#'
#' @param spec A plot fragment or `vgspec` to add this mark to, or `NULL` to start a new plot with just this mark.
#' @param formula A shorthand for this mark's position channels (`x`, `y`, `fx`, `fy`, and paired `x1`/`x2` or `y1`/`y2`), e.g., `Sepal.Length ~ Sepal.Width | ~ Species` for `y = ~Sepal.Length, x = ~Sepal.Width, fx = ~Species`. See [vg_mark()] for the full grammar.
#' @param aria_description `<string | param()>` The aria-description; a constant textual description.
#' @param aria_hidden `<string | param()>` The aria-hidden state; a constant indicating whether the element is exposed to an accessibility API.
#' @param aria_label `<column | literal | transform() | sql()/agg()>` The aria-label; a channel specifying short textual labels representing the value in the accessibility tree.
#' @param channels `<list>` Additional named channels, for example to include in a tooltip.
#' @param clip `<"frame" | "sphere" | boolean | NULL | param()>` How to clip the mark; one of: - *frame* or true - clip to the plot’s frame (inner area) - *sphere* - clip to the projected sphere (*e.g.*, front hemisphere) - null or false - do not clip The *sphere* clip option requires a geographic projection.
#' @param dx `<number | param()>` The horizontal offset in pixels; a constant option.
#' @param dy `<number | param()>` The vertical offset in pixels; a constant option.
#' @param facet `<"auto" | "include" | "exclude" | "super" | boolean | NULL | param()>` Whether to enable or disable faceting; one of: - *auto* (default) - automatically determine if this mark should be faceted - *include* (or true) - draw the subset of the mark’s data in the current facet - *exclude* - draw the subset of the mark’s data *not* in the current facet - *super* - draw this mark in a single frame that covers all facets - null (or false) - repeat this mark’s data across all facets (*i.e.*, no faceting) When a mark uses *super* faceting, it is not allowed to use position scales (*x*, *y*, *fx*, or *fy*); *super* faceting is intended for decorations, such as labels and legends.
#' @param facet_anchor `<"top" | "right" | "bottom" | "left" | "top-left" | "top-right" | "bottom-left" | "bottom-right" | "top-empty" | "right-empty" | "bottom-empty" | "left-empty" | "empty" | NULL | param()>` How to place the mark with respect to facets; one of: - null (default for most marks) - display the mark in each non-empty facet - *top*, *right*, *bottom*, or *left* - display the mark only in facets on the given side - *top-empty*, *right-empty*, *bottom-empty*, or *left-empty* (default for axis marks) - display the mark only in facets that have empty space on the given side: either the margin, or an empty facet - *empty* - display the mark in empty facets only
#' @param fill `<column | literal | transform() | sql()/agg() | list(value=, ...) | param()>` The fill; a constant CSS color string, or a channel typically bound to the *color* scale.
#' @param fill_opacity `<column | literal | transform() | sql()/agg() | list(value=, ...) | param()>` The fill-opacity; a constant number between 0 and 1, or a channel typically bound to the *opacity* scale.
#' @param filter `<column | literal | transform() | sql()/agg()>` Applies a transform to filter the mark’s index according to the given channel values; only truthy values are retained.
#' @param frame_anchor `<"middle" | "top-left" | "top" | "top-right" | "right" | "bottom-right" | "bottom" | "bottom-left" | "left" | param()>` The frame anchor specifies defaults for **x** and **y**, along with **textAnchor** and **lineAnchor**, based on the plot’s frame; it may be one of the four sides (*top*, *right*, *bottom*, *left*), one of the four corners (*top-left*, *top-right*, *bottom-right*, *bottom-left*), or the *middle* of the frame.
#' @param fx `<column | literal | transform() | sql()/agg()>` The horizontal facet position channel, for mark-level faceting, bound to the *fx* scale.
#' @param fy `<column | literal | transform() | sql()/agg()>` The vertical facet position channel, for mark-level faceting, bound to the *fy* scale.
#' @param href `<column | literal | transform() | sql()/agg()>` The href; a channel specifying URLs for clickable links.
#' @param image_filter `<string | param()>` A CSS filter; a constant string used to adjust the rendering of images, such as *blur(5px)*.
#' @param margin `<number | param()>` Shorthand to set the same default for all four mark margins: **marginTop**, **marginRight**, **marginBottom**, and **marginLeft**; typically defaults to 0, except for axis marks.
#' @param margin_bottom `<number | param()>` The mark’s bottom margin; the minimum distance in pixels between the bottom edges of the inner and outer plot area.
#' @param margin_left `<number | param()>` The mark’s left margin; the minimum distance in pixels between the left edges of the inner and outer plot area.
#' @param margin_right `<number | param()>` The mark’s right margin; the minimum distance in pixels between the right edges of the mark’s inner and outer plot area.
#' @param margin_top `<number | param()>` The mark’s top margin; the minimum distance in pixels between the top edges of the inner and outer plot area.
#' @param mix_blend_mode `<string | param()>` The mix-blend-mode; a constant string specifying how to blend content such as *multiply*.
#' @param opacity `<column | literal | transform() | sql()/agg() | list(value=, ...)>` The opacity; a constant between 0 and 1, or a channel typically bound to the *opacity* scale.
#' @param paint_order `<string | param()>` The paint-order; a constant string specifying the order in which the **fill**, **stroke**, and any markers are drawn; defaults to *normal*, which draws the fill, then stroke, then markers; defaults to *stroke* for the text mark to create a “halo” around text to improve legibility.
#' @param pointer_events `<string | param()>` The pointer-events property; a constant string such as *none*.
#' @param r `<column | literal | transform() | sql()/agg() | list(value=, ...) | number | param()>` The radius of dots; either a channel or constant.
#' @param reverse `<boolean | param()>` Applies a transform to reverse the order of the mark’s index, say for reverse input order.
#' @param rotate `<column | literal | transform() | sql()/agg() | param()>` The rotation angle in degrees clockwise; a constant or a channel; defaults to 0°.
#' @param select `<"first" | "last" | "maxX" | "maxY" | "minX" | "minY" | "nearest" | "nearestX" | "nearestY">` Applies a filter transform after data is loaded to highlight selected values only.
#' @param shape_rendering `<string | param()>` The shape-rendering; a constant string such as *crispEdges*.
#' @param sort `<column | literal | transform() | sql()/agg() | list>` Either applies a transform to sort the mark’s index by the specified channel values, or imputes ordinal scale domains from this mark’s channels.
#' @param stroke `<column | literal | transform() | sql()/agg() | list(value=, ...) | param()>` The stroke; a constant CSS color string, or a channel typically bound to the *color* scale.
#' @param stroke_dasharray `<string | number | param()>` The stroke-dasharray; a constant number indicating the length in pixels of alternating dashes and gaps, or a constant string of numbers separated by spaces or commas (_e.g._, *10 2* for dashes of 10 pixels separated by gaps of 2 pixels), or *none* (the default) for no dashing : https://developer.mozilla.org/en-US/docs/Web/SVG/Attribute/stroke-dasharray
#' @param stroke_dashoffset `<string | number | param()>` The stroke-dashoffset; a constant indicating the offset in pixels of the first dash along the stroke; defaults to zero.
#' @param stroke_linecap `<string | param()>` The stroke-linecap; a constant specifying how to cap stroked paths, such as *butt*, *round*, or *square*.
#' @param stroke_linejoin `<string | param()>` The stroke-linejoin; a constant specifying how to join stroked paths, such as *bevel*, *miter*, *miter-clip*, or *round*.
#' @param stroke_miterlimit `<number | param()>` The stroke-miterlimit; a constant number specifying how to limit the length of *miter* joins on stroked paths.
#' @param stroke_opacity `<column | literal | transform() | sql()/agg() | list(value=, ...)>` The stroke-opacity; a constant between 0 and 1, or a channel typically bound to the *opacity* scale.
#' @param stroke_width `<column | literal | transform() | sql()/agg() | list(value=, ...)>` The stroke-width; a constant number in pixels, or a channel.
#' @param symbol `<column | literal | transform() | sql()/agg() | list(value=, ...) | "asterisk" | "circle" | "cross" | "diamond" | "diamond2" | "hexagon" | "plus" | "square" | "square2" | "star" | ... | param()>` The categorical symbol; either a channel or a constant.
#' @param target `<string | param()>` The target; a constant string specifying the target window (_e.g._, *_blank*) for clickable links; used in conjunction with the **href** option.
#' @param tip `<boolean | "x" | "y" | "xy" | list | param()>` Whether to generate a tooltip for this mark, and any tip options.
#' @param title `<column | literal | transform() | sql()/agg()>` The title; a channel specifying accessible, short textual descriptions as strings (possibly with newlines).
#' @param x `<column | literal | transform() | sql()/agg() | list(value=, ...)>` The horizontal position (or length) channel, typically bound to the *x* scale.
#' @param y `<column | literal | transform() | sql()/agg() | list(value=, ...)>` The vertical position channel, typically bound to the *y* scale; defaults to the zero-based index of the data 0, 1, 2, ….
#' @param z `<column | literal | transform() | sql()/agg()>` An optional ordinal channel for grouping data into (possibly stacked) series to be drawn as separate areas; defaults to **fill** if a channel, or **stroke** if a channel.
#' @param ... Additional options or plot-level attributes.
#' @param data_from The name of the data source this mark reads from (see [vg_data()]); a length-1 nonzero R integer (`1L`, `-1L`, ...; note the `L`) giving a 1-based index into the spec's registered data sources instead, negative counting from the end (e.g., `-1L` for the most recently registered one); or a literal vector of values to use as inline data directly (mosaic-spec's `"data": [...]` shorthand, e.g., for a single reference line -- a bare double like `0`/`c(0)`, or `0L` itself (never a valid index), means this, not an index). Left unset, defaults to the first registered data source (equivalent to `data_from = 1L`) for any mark type that takes data at all.
#' @param filter_by A Param/Selection (e.g., from [param()]) to filter this mark's data by.
#' @param data_optimize A flag (default `TRUE`) to enable mark-specific query optimizations for this mark's data; set `FALSE` to disable them (mosaic-spec's `data: {optimize: false}`).
#' @family mark functions
#' @export
vg_mark_hexagon <- function(spec = NULL, formula = vg_unset, aria_description = vg_unset, aria_hidden = vg_unset, aria_label = vg_unset, channels = vg_unset, clip = vg_unset, dx = vg_unset, dy = vg_unset, facet = vg_unset, facet_anchor = vg_unset, fill = vg_unset, fill_opacity = vg_unset, filter = vg_unset, frame_anchor = vg_unset, fx = vg_unset, fy = vg_unset, href = vg_unset, image_filter = vg_unset, margin = vg_unset, margin_bottom = vg_unset, margin_left = vg_unset, margin_right = vg_unset, margin_top = vg_unset, mix_blend_mode = vg_unset, opacity = vg_unset, paint_order = vg_unset, pointer_events = vg_unset, r = vg_unset, reverse = vg_unset, rotate = vg_unset, select = vg_unset, shape_rendering = vg_unset, sort = vg_unset, stroke = vg_unset, stroke_dasharray = vg_unset, stroke_dashoffset = vg_unset, stroke_linecap = vg_unset, stroke_linejoin = vg_unset, stroke_miterlimit = vg_unset, stroke_opacity = vg_unset, stroke_width = vg_unset, symbol = vg_unset, target = vg_unset, tip = vg_unset, title = vg_unset, x = vg_unset, y = vg_unset, z = vg_unset, ..., data_from = vg_unset, filter_by = vg_unset, data_optimize = vg_unset) {
  vg_mark_(spec, "hexagon", formula = formula, ariaDescription = aria_description, ariaHidden = aria_hidden, ariaLabel = aria_label, channels = channels, clip = clip, dx = dx, dy = dy, facet = facet, facetAnchor = facet_anchor, fill = fill, fillOpacity = fill_opacity, filter = filter, frameAnchor = frame_anchor, fx = fx, fy = fy, href = href, imageFilter = image_filter, margin = margin, marginBottom = margin_bottom, marginLeft = margin_left, marginRight = margin_right, marginTop = margin_top, mixBlendMode = mix_blend_mode, opacity = opacity, paintOrder = paint_order, pointerEvents = pointer_events, r = r, reverse = reverse, rotate = rotate, select = select, shapeRendering = shape_rendering, sort = sort, stroke = stroke, strokeDasharray = stroke_dasharray, strokeDashoffset = stroke_dashoffset, strokeLinecap = stroke_linecap, strokeLinejoin = stroke_linejoin, strokeMiterlimit = stroke_miterlimit, strokeOpacity = stroke_opacity, strokeWidth = stroke_width, symbol = symbol, target = target, tip = tip, title = title, x = x, y = y, z = z, ..., data_from = data_from, filter_by = filter_by, data_optimize = data_optimize)
}

#' The hexbin mark.
#'
#' See [vg_value_types] for what the `<...>` notation below (`column`, `param()`, ...) means.
#'
#' @param spec A plot fragment or `vgspec` to add this mark to, or `NULL` to start a new plot with just this mark.
#' @param formula A shorthand for this mark's position channels (`x`, `y`, `fx`, `fy`, and paired `x1`/`x2` or `y1`/`y2`), e.g., `Sepal.Length ~ Sepal.Width | ~ Species` for `y = ~Sepal.Length, x = ~Sepal.Width, fx = ~Species`. See [vg_mark()] for the full grammar.
#' @param aria_description `<string | param()>` The aria-description; a constant textual description.
#' @param aria_hidden `<string | param()>` The aria-hidden state; a constant indicating whether the element is exposed to an accessibility API.
#' @param aria_label `<column | literal | transform() | sql()/agg()>` The aria-label; a channel specifying short textual labels representing the value in the accessibility tree.
#' @param bin_width `<number | param()>` The distance between centers of neighboring hexagons, in pixels; defaults to 20.
#' @param channels `<list>` Additional named channels, for example to include in a tooltip.
#' @param clip `<"frame" | "sphere" | boolean | NULL | param()>` How to clip the mark; one of: - *frame* or true - clip to the plot’s frame (inner area) - *sphere* - clip to the projected sphere (*e.g.*, front hemisphere) - null or false - do not clip The *sphere* clip option requires a geographic projection.
#' @param dx `<number | param()>` The horizontal offset in pixels; a constant option.
#' @param dy `<number | param()>` The vertical offset in pixels; a constant option.
#' @param facet `<"auto" | "include" | "exclude" | "super" | boolean | NULL | param()>` Whether to enable or disable faceting; one of: - *auto* (default) - automatically determine if this mark should be faceted - *include* (or true) - draw the subset of the mark’s data in the current facet - *exclude* - draw the subset of the mark’s data *not* in the current facet - *super* - draw this mark in a single frame that covers all facets - null (or false) - repeat this mark’s data across all facets (*i.e.*, no faceting) When a mark uses *super* faceting, it is not allowed to use position scales (*x*, *y*, *fx*, or *fy*); *super* faceting is intended for decorations, such as labels and legends.
#' @param facet_anchor `<"top" | "right" | "bottom" | "left" | "top-left" | "top-right" | "bottom-left" | "bottom-right" | "top-empty" | "right-empty" | "bottom-empty" | "left-empty" | "empty" | NULL | param()>` How to place the mark with respect to facets; one of: - null (default for most marks) - display the mark in each non-empty facet - *top*, *right*, *bottom*, or *left* - display the mark only in facets on the given side - *top-empty*, *right-empty*, *bottom-empty*, or *left-empty* (default for axis marks) - display the mark only in facets that have empty space on the given side: either the margin, or an empty facet - *empty* - display the mark in empty facets only
#' @param fill `<column | literal | transform() | sql()/agg() | list(value=, ...) | param()>` The fill; a constant CSS color string, or a channel typically bound to the *color* scale.
#' @param fill_opacity `<column | literal | transform() | sql()/agg() | list(value=, ...) | param()>` The fill-opacity; a constant number between 0 and 1, or a channel typically bound to the *opacity* scale.
#' @param filter `<column | literal | transform() | sql()/agg()>` Applies a transform to filter the mark’s index according to the given channel values; only truthy values are retained.
#' @param font_family `<string | param()>` The font-family; a constant; defaults to the plot’s font family, which is typically *system-ui*.
#' @param font_size `<column | literal | transform() | sql()/agg() | param()>` The font size in pixels; either a constant or a channel; defaults to the plot’s font size, which is typically 10.
#' @param font_style `<string | param()>` The font style; a constant; defaults to the plot’s font style, which is typically *normal*.
#' @param font_variant `<string | param()>` The font variant; a constant; if the **text** channel contains numbers or dates, defaults to *tabular-nums* to facilitate comparing numbers; otherwise defaults to the plot’s font style, which is typically *normal*.
#' @param font_weight `<string | number | param()>` The font weight; a constant; defaults to the plot’s font weight, which is typically *normal*.
#' @param frame_anchor `<"middle" | "top-left" | "top" | "top-right" | "right" | "bottom-right" | "bottom" | "bottom-left" | "left" | param()>` The frame anchor specifies defaults for **x** and **y**, along with **textAnchor** and **lineAnchor**, based on the plot’s frame; it may be one of the four sides (*top*, *right*, *bottom*, *left*), one of the four corners (*top-left*, *top-right*, *bottom-right*, *bottom-left*), or the *middle* of the frame.
#' @param fx `<column | literal | transform() | sql()/agg()>` The horizontal facet position channel, for mark-level faceting, bound to the *fx* scale.
#' @param fy `<column | literal | transform() | sql()/agg()>` The vertical facet position channel, for mark-level faceting, bound to the *fy* scale.
#' @param href `<column | literal | transform() | sql()/agg()>` The href; a channel specifying URLs for clickable links.
#' @param image_filter `<string | param()>` A CSS filter; a constant string used to adjust the rendering of images, such as *blur(5px)*.
#' @param line_height `<number | param()>` The line height in ems; defaults to 1.
#' @param line_width `<number | param()>` The line width in ems (e.g., 10 for about 20 characters); defaults to infinity, disabling wrapping and clipping.
#' @param margin `<number | param()>` Shorthand to set the same default for all four mark margins: **marginTop**, **marginRight**, **marginBottom**, and **marginLeft**; typically defaults to 0, except for axis marks.
#' @param margin_bottom `<number | param()>` The mark’s bottom margin; the minimum distance in pixels between the bottom edges of the inner and outer plot area.
#' @param margin_left `<number | param()>` The mark’s left margin; the minimum distance in pixels between the left edges of the inner and outer plot area.
#' @param margin_right `<number | param()>` The mark’s right margin; the minimum distance in pixels between the right edges of the mark’s inner and outer plot area.
#' @param margin_top `<number | param()>` The mark’s top margin; the minimum distance in pixels between the top edges of the inner and outer plot area.
#' @param mix_blend_mode `<string | param()>` The mix-blend-mode; a constant string specifying how to blend content such as *multiply*.
#' @param monospace `<boolean | param()>` If true, changes the default **fontFamily** to *monospace*, and uses simplified monospaced text metrics calculations.
#' @param opacity `<column | literal | transform() | sql()/agg() | list(value=, ...)>` The opacity; a constant between 0 and 1, or a channel typically bound to the *opacity* scale.
#' @param paint_order `<string | param()>` The paint-order; a constant string specifying the order in which the **fill**, **stroke**, and any markers are drawn; defaults to *normal*, which draws the fill, then stroke, then markers; defaults to *stroke* for the text mark to create a “halo” around text to improve legibility.
#' @param pointer_events `<string | param()>` The pointer-events property; a constant string such as *none*.
#' @param r `<column | literal | transform() | sql()/agg() | list(value=, ...) | number | param()>` The radius of dots; either a channel or constant.
#' @param reverse `<boolean | param()>` Applies a transform to reverse the order of the mark’s index, say for reverse input order.
#' @param rotate `<column | literal | transform() | sql()/agg() | param()>` The rotation angle in degrees clockwise; a constant or a channel; defaults to 0°.
#' @param select `<"first" | "last" | "maxX" | "maxY" | "minX" | "minY" | "nearest" | "nearestX" | "nearestY">` Applies a filter transform after data is loaded to highlight selected values only.
#' @param shape_rendering `<string | param()>` The shape-rendering; a constant string such as *crispEdges*.
#' @param sort `<column | literal | transform() | sql()/agg() | list>` Either applies a transform to sort the mark’s index by the specified channel values, or imputes ordinal scale domains from this mark’s channels.
#' @param stroke `<column | literal | transform() | sql()/agg() | list(value=, ...) | param()>` The stroke; a constant CSS color string, or a channel typically bound to the *color* scale.
#' @param stroke_dasharray `<string | number | param()>` The stroke-dasharray; a constant number indicating the length in pixels of alternating dashes and gaps, or a constant string of numbers separated by spaces or commas (_e.g._, *10 2* for dashes of 10 pixels separated by gaps of 2 pixels), or *none* (the default) for no dashing : https://developer.mozilla.org/en-US/docs/Web/SVG/Attribute/stroke-dasharray
#' @param stroke_dashoffset `<string | number | param()>` The stroke-dashoffset; a constant indicating the offset in pixels of the first dash along the stroke; defaults to zero.
#' @param stroke_linecap `<string | param()>` The stroke-linecap; a constant specifying how to cap stroked paths, such as *butt*, *round*, or *square*.
#' @param stroke_linejoin `<string | param()>` The stroke-linejoin; a constant specifying how to join stroked paths, such as *bevel*, *miter*, *miter-clip*, or *round*.
#' @param stroke_miterlimit `<number | param()>` The stroke-miterlimit; a constant number specifying how to limit the length of *miter* joins on stroked paths.
#' @param stroke_opacity `<column | literal | transform() | sql()/agg() | list(value=, ...)>` The stroke-opacity; a constant between 0 and 1, or a channel typically bound to the *opacity* scale.
#' @param stroke_width `<column | literal | transform() | sql()/agg() | list(value=, ...)>` The stroke-width; a constant number in pixels, or a channel.
#' @param symbol `<column | literal | transform() | sql()/agg() | list(value=, ...) | "asterisk" | "circle" | "cross" | "diamond" | "diamond2" | "hexagon" | "plus" | "square" | "square2" | "star" | ... | param()>` The categorical symbol; either a channel or a constant.
#' @param target `<string | param()>` The target; a constant string specifying the target window (_e.g._, *_blank*) for clickable links; used in conjunction with the **href** option.
#' @param text_anchor `<"start" | "middle" | "end" | param()>` The text anchor controls how text is aligned (typically horizontally) relative to its anchor point; it is one of *start*, *end*, or *middle*.
#' @param text_overflow `<NULL | "clip" | "ellipsis" | "clip-start" | "clip-end" | "ellipsis-start" | "ellipsis-middle" | "ellipsis-end" | param()>` How truncate (or wrap) lines of text longer than the given **lineWidth**; one of: - null (default) - preserve overflowing characters (and wrap if needed) - *clip* or *clip-end* - remove characters from the end - *clip-start* - remove characters from the start - *ellipsis* or *ellipsis-end* - replace characters from the end with an ellipsis (…) - *ellipsis-start* - replace characters from the start with an ellipsis (…) - *ellipsis-middle* - replace characters from the middle with an ellipsis (…) If no **title** was specified, if text requires truncation, a title containing the non-truncated text will be implicitly added.
#' @param tip `<boolean | "x" | "y" | "xy" | list | param()>` Whether to generate a tooltip for this mark, and any tip options.
#' @param title `<column | literal | transform() | sql()/agg()>` The title; a channel specifying accessible, short textual descriptions as strings (possibly with newlines).
#' @param type `<"dot" | "circle" | "hexagon" | "cell" | "text" | param()>` The basic mark type to use to render 2D density values.
#' @param x `<column | literal | transform() | sql()/agg() | list(value=, ...)>` The horizontal position (or length) channel, typically bound to the *x* scale.
#' @param y `<column | literal | transform() | sql()/agg() | list(value=, ...)>` The vertical position channel, typically bound to the *y* scale; defaults to the zero-based index of the data 0, 1, 2, ….
#' @param z `<column | literal | transform() | sql()/agg()>` An optional ordinal channel for grouping data into (possibly stacked) series to be drawn as separate areas; defaults to **fill** if a channel, or **stroke** if a channel.
#' @param ... Additional options or plot-level attributes.
#' @param data_from The name of the data source this mark reads from (see [vg_data()]); a length-1 nonzero R integer (`1L`, `-1L`, ...; note the `L`) giving a 1-based index into the spec's registered data sources instead, negative counting from the end (e.g., `-1L` for the most recently registered one); or a literal vector of values to use as inline data directly (mosaic-spec's `"data": [...]` shorthand, e.g., for a single reference line -- a bare double like `0`/`c(0)`, or `0L` itself (never a valid index), means this, not an index). Left unset, defaults to the first registered data source (equivalent to `data_from = 1L`) for any mark type that takes data at all.
#' @param filter_by A Param/Selection (e.g., from [param()]) to filter this mark's data by.
#' @param data_optimize A flag (default `TRUE`) to enable mark-specific query optimizations for this mark's data; set `FALSE` to disable them (mosaic-spec's `data: {optimize: false}`).
#' @family mark functions
#' @export
vg_mark_hexbin <- function(spec = NULL, formula = vg_unset, aria_description = vg_unset, aria_hidden = vg_unset, aria_label = vg_unset, bin_width = vg_unset, channels = vg_unset, clip = vg_unset, dx = vg_unset, dy = vg_unset, facet = vg_unset, facet_anchor = vg_unset, fill = vg_unset, fill_opacity = vg_unset, filter = vg_unset, font_family = vg_unset, font_size = vg_unset, font_style = vg_unset, font_variant = vg_unset, font_weight = vg_unset, frame_anchor = vg_unset, fx = vg_unset, fy = vg_unset, href = vg_unset, image_filter = vg_unset, line_height = vg_unset, line_width = vg_unset, margin = vg_unset, margin_bottom = vg_unset, margin_left = vg_unset, margin_right = vg_unset, margin_top = vg_unset, mix_blend_mode = vg_unset, monospace = vg_unset, opacity = vg_unset, paint_order = vg_unset, pointer_events = vg_unset, r = vg_unset, reverse = vg_unset, rotate = vg_unset, select = vg_unset, shape_rendering = vg_unset, sort = vg_unset, stroke = vg_unset, stroke_dasharray = vg_unset, stroke_dashoffset = vg_unset, stroke_linecap = vg_unset, stroke_linejoin = vg_unset, stroke_miterlimit = vg_unset, stroke_opacity = vg_unset, stroke_width = vg_unset, symbol = vg_unset, target = vg_unset, text_anchor = vg_unset, text_overflow = vg_unset, tip = vg_unset, title = vg_unset, type = vg_unset, x = vg_unset, y = vg_unset, z = vg_unset, ..., data_from = vg_unset, filter_by = vg_unset, data_optimize = vg_unset) {
  vg_mark_(spec, "hexbin", formula = formula, ariaDescription = aria_description, ariaHidden = aria_hidden, ariaLabel = aria_label, binWidth = bin_width, channels = channels, clip = clip, dx = dx, dy = dy, facet = facet, facetAnchor = facet_anchor, fill = fill, fillOpacity = fill_opacity, filter = filter, fontFamily = font_family, fontSize = font_size, fontStyle = font_style, fontVariant = font_variant, fontWeight = font_weight, frameAnchor = frame_anchor, fx = fx, fy = fy, href = href, imageFilter = image_filter, lineHeight = line_height, lineWidth = line_width, margin = margin, marginBottom = margin_bottom, marginLeft = margin_left, marginRight = margin_right, marginTop = margin_top, mixBlendMode = mix_blend_mode, monospace = monospace, opacity = opacity, paintOrder = paint_order, pointerEvents = pointer_events, r = r, reverse = reverse, rotate = rotate, select = select, shapeRendering = shape_rendering, sort = sort, stroke = stroke, strokeDasharray = stroke_dasharray, strokeDashoffset = stroke_dashoffset, strokeLinecap = stroke_linecap, strokeLinejoin = stroke_linejoin, strokeMiterlimit = stroke_miterlimit, strokeOpacity = stroke_opacity, strokeWidth = stroke_width, symbol = symbol, target = target, textAnchor = text_anchor, textOverflow = text_overflow, tip = tip, title = title, type = type, x = x, y = y, z = z, ..., data_from = data_from, filter_by = filter_by, data_optimize = data_optimize)
}

#' The hexgrid mark.
#'
#' See [vg_value_types] for what the `<...>` notation below (`column`, `param()`, ...) means.
#'
#' @param spec A plot fragment or `vgspec` to add this mark to, or `NULL` to start a new plot with just this mark.
#' @param formula A shorthand for this mark's position channels (`x`, `y`, `fx`, `fy`, and paired `x1`/`x2` or `y1`/`y2`), e.g., `Sepal.Length ~ Sepal.Width | ~ Species` for `y = ~Sepal.Length, x = ~Sepal.Width, fx = ~Species`. See [vg_mark()] for the full grammar.
#' @param aria_description `<string | param()>` The aria-description; a constant textual description.
#' @param aria_hidden `<string | param()>` The aria-hidden state; a constant indicating whether the element is exposed to an accessibility API.
#' @param aria_label `<column | literal | transform() | sql()/agg()>` The aria-label; a channel specifying short textual labels representing the value in the accessibility tree.
#' @param bin_width `<number | param()>` The distance between centers of neighboring hexagons, in pixels; defaults to 20.
#' @param channels `<list>` Additional named channels, for example to include in a tooltip.
#' @param clip `<"frame" | "sphere" | boolean | NULL | param()>` How to clip the mark; one of: - *frame* or true - clip to the plot’s frame (inner area) - *sphere* - clip to the projected sphere (*e.g.*, front hemisphere) - null or false - do not clip The *sphere* clip option requires a geographic projection.
#' @param dx `<number | param()>` The horizontal offset in pixels; a constant option.
#' @param dy `<number | param()>` The vertical offset in pixels; a constant option.
#' @param facet `<"auto" | "include" | "exclude" | "super" | boolean | NULL | param()>` Whether to enable or disable faceting; one of: - *auto* (default) - automatically determine if this mark should be faceted - *include* (or true) - draw the subset of the mark’s data in the current facet - *exclude* - draw the subset of the mark’s data *not* in the current facet - *super* - draw this mark in a single frame that covers all facets - null (or false) - repeat this mark’s data across all facets (*i.e.*, no faceting) When a mark uses *super* faceting, it is not allowed to use position scales (*x*, *y*, *fx*, or *fy*); *super* faceting is intended for decorations, such as labels and legends.
#' @param facet_anchor `<"top" | "right" | "bottom" | "left" | "top-left" | "top-right" | "bottom-left" | "bottom-right" | "top-empty" | "right-empty" | "bottom-empty" | "left-empty" | "empty" | NULL | param()>` How to place the mark with respect to facets; one of: - null (default for most marks) - display the mark in each non-empty facet - *top*, *right*, *bottom*, or *left* - display the mark only in facets on the given side - *top-empty*, *right-empty*, *bottom-empty*, or *left-empty* (default for axis marks) - display the mark only in facets that have empty space on the given side: either the margin, or an empty facet - *empty* - display the mark in empty facets only
#' @param fill `<column | literal | transform() | sql()/agg() | list(value=, ...) | param()>` The fill; a constant CSS color string, or a channel typically bound to the *color* scale.
#' @param fill_opacity `<column | literal | transform() | sql()/agg() | list(value=, ...) | param()>` The fill-opacity; a constant number between 0 and 1, or a channel typically bound to the *opacity* scale.
#' @param filter `<column | literal | transform() | sql()/agg()>` Applies a transform to filter the mark’s index according to the given channel values; only truthy values are retained.
#' @param fx `<column | literal | transform() | sql()/agg()>` The horizontal facet position channel, for mark-level faceting, bound to the *fx* scale.
#' @param fy `<column | literal | transform() | sql()/agg()>` The vertical facet position channel, for mark-level faceting, bound to the *fy* scale.
#' @param href `<column | literal | transform() | sql()/agg()>` The href; a channel specifying URLs for clickable links.
#' @param image_filter `<string | param()>` A CSS filter; a constant string used to adjust the rendering of images, such as *blur(5px)*.
#' @param margin `<number | param()>` Shorthand to set the same default for all four mark margins: **marginTop**, **marginRight**, **marginBottom**, and **marginLeft**; typically defaults to 0, except for axis marks.
#' @param margin_bottom `<number | param()>` The mark’s bottom margin; the minimum distance in pixels between the bottom edges of the inner and outer plot area.
#' @param margin_left `<number | param()>` The mark’s left margin; the minimum distance in pixels between the left edges of the inner and outer plot area.
#' @param margin_right `<number | param()>` The mark’s right margin; the minimum distance in pixels between the right edges of the mark’s inner and outer plot area.
#' @param margin_top `<number | param()>` The mark’s top margin; the minimum distance in pixels between the top edges of the inner and outer plot area.
#' @param mix_blend_mode `<string | param()>` The mix-blend-mode; a constant string specifying how to blend content such as *multiply*.
#' @param opacity `<column | literal | transform() | sql()/agg() | list(value=, ...)>` The opacity; a constant between 0 and 1, or a channel typically bound to the *opacity* scale.
#' @param paint_order `<string | param()>` The paint-order; a constant string specifying the order in which the **fill**, **stroke**, and any markers are drawn; defaults to *normal*, which draws the fill, then stroke, then markers; defaults to *stroke* for the text mark to create a “halo” around text to improve legibility.
#' @param pointer_events `<string | param()>` The pointer-events property; a constant string such as *none*.
#' @param reverse `<boolean | param()>` Applies a transform to reverse the order of the mark’s index, say for reverse input order.
#' @param select `<"first" | "last" | "maxX" | "maxY" | "minX" | "minY" | "nearest" | "nearestX" | "nearestY">` Applies a filter transform after data is loaded to highlight selected values only.
#' @param shape_rendering `<string | param()>` The shape-rendering; a constant string such as *crispEdges*.
#' @param sort `<column | literal | transform() | sql()/agg() | list>` Either applies a transform to sort the mark’s index by the specified channel values, or imputes ordinal scale domains from this mark’s channels.
#' @param stroke `<column | literal | transform() | sql()/agg() | list(value=, ...) | param()>` The stroke; a constant CSS color string, or a channel typically bound to the *color* scale.
#' @param stroke_dasharray `<string | number | param()>` The stroke-dasharray; a constant number indicating the length in pixels of alternating dashes and gaps, or a constant string of numbers separated by spaces or commas (_e.g._, *10 2* for dashes of 10 pixels separated by gaps of 2 pixels), or *none* (the default) for no dashing : https://developer.mozilla.org/en-US/docs/Web/SVG/Attribute/stroke-dasharray
#' @param stroke_dashoffset `<string | number | param()>` The stroke-dashoffset; a constant indicating the offset in pixels of the first dash along the stroke; defaults to zero.
#' @param stroke_linecap `<string | param()>` The stroke-linecap; a constant specifying how to cap stroked paths, such as *butt*, *round*, or *square*.
#' @param stroke_linejoin `<string | param()>` The stroke-linejoin; a constant specifying how to join stroked paths, such as *bevel*, *miter*, *miter-clip*, or *round*.
#' @param stroke_miterlimit `<number | param()>` The stroke-miterlimit; a constant number specifying how to limit the length of *miter* joins on stroked paths.
#' @param stroke_opacity `<column | literal | transform() | sql()/agg() | list(value=, ...)>` The stroke-opacity; a constant between 0 and 1, or a channel typically bound to the *opacity* scale.
#' @param stroke_width `<column | literal | transform() | sql()/agg() | list(value=, ...)>` The stroke-width; a constant number in pixels, or a channel.
#' @param target `<string | param()>` The target; a constant string specifying the target window (_e.g._, *_blank*) for clickable links; used in conjunction with the **href** option.
#' @param tip `<boolean | "x" | "y" | "xy" | list | param()>` Whether to generate a tooltip for this mark, and any tip options.
#' @param title `<column | literal | transform() | sql()/agg()>` The title; a channel specifying accessible, short textual descriptions as strings (possibly with newlines).
#' @param ... Additional options or plot-level attributes.
#' @param data_from The name of the data source this mark reads from (see [vg_data()]); a length-1 nonzero R integer (`1L`, `-1L`, ...; note the `L`) giving a 1-based index into the spec's registered data sources instead, negative counting from the end (e.g., `-1L` for the most recently registered one); or a literal vector of values to use as inline data directly (mosaic-spec's `"data": [...]` shorthand, e.g., for a single reference line -- a bare double like `0`/`c(0)`, or `0L` itself (never a valid index), means this, not an index). Left unset, defaults to the first registered data source (equivalent to `data_from = 1L`) for any mark type that takes data at all.
#' @param filter_by A Param/Selection (e.g., from [param()]) to filter this mark's data by.
#' @param data_optimize A flag (default `TRUE`) to enable mark-specific query optimizations for this mark's data; set `FALSE` to disable them (mosaic-spec's `data: {optimize: false}`).
#' @family mark functions
#' @export
vg_mark_hexgrid <- function(spec = NULL, formula = vg_unset, aria_description = vg_unset, aria_hidden = vg_unset, aria_label = vg_unset, bin_width = vg_unset, channels = vg_unset, clip = vg_unset, dx = vg_unset, dy = vg_unset, facet = vg_unset, facet_anchor = vg_unset, fill = vg_unset, fill_opacity = vg_unset, filter = vg_unset, fx = vg_unset, fy = vg_unset, href = vg_unset, image_filter = vg_unset, margin = vg_unset, margin_bottom = vg_unset, margin_left = vg_unset, margin_right = vg_unset, margin_top = vg_unset, mix_blend_mode = vg_unset, opacity = vg_unset, paint_order = vg_unset, pointer_events = vg_unset, reverse = vg_unset, select = vg_unset, shape_rendering = vg_unset, sort = vg_unset, stroke = vg_unset, stroke_dasharray = vg_unset, stroke_dashoffset = vg_unset, stroke_linecap = vg_unset, stroke_linejoin = vg_unset, stroke_miterlimit = vg_unset, stroke_opacity = vg_unset, stroke_width = vg_unset, target = vg_unset, tip = vg_unset, title = vg_unset, ..., data_from = vg_unset, filter_by = vg_unset, data_optimize = vg_unset) {
  vg_mark_(spec, "hexgrid", formula = formula, ariaDescription = aria_description, ariaHidden = aria_hidden, ariaLabel = aria_label, binWidth = bin_width, channels = channels, clip = clip, dx = dx, dy = dy, facet = facet, facetAnchor = facet_anchor, fill = fill, fillOpacity = fill_opacity, filter = filter, fx = fx, fy = fy, href = href, imageFilter = image_filter, margin = margin, marginBottom = margin_bottom, marginLeft = margin_left, marginRight = margin_right, marginTop = margin_top, mixBlendMode = mix_blend_mode, opacity = opacity, paintOrder = paint_order, pointerEvents = pointer_events, reverse = reverse, select = select, shapeRendering = shape_rendering, sort = sort, stroke = stroke, strokeDasharray = stroke_dasharray, strokeDashoffset = stroke_dashoffset, strokeLinecap = stroke_linecap, strokeLinejoin = stroke_linejoin, strokeMiterlimit = stroke_miterlimit, strokeOpacity = stroke_opacity, strokeWidth = stroke_width, target = target, tip = tip, title = title, ..., data_from = data_from, filter_by = filter_by, data_optimize = data_optimize)
}

#' The hull mark.
#'
#' See [vg_value_types] for what the `<...>` notation below (`column`, `param()`, ...) means.
#'
#' @param spec A plot fragment or `vgspec` to add this mark to, or `NULL` to start a new plot with just this mark.
#' @param formula A shorthand for this mark's position channels (`x`, `y`, `fx`, `fy`, and paired `x1`/`x2` or `y1`/`y2`), e.g., `Sepal.Length ~ Sepal.Width | ~ Species` for `y = ~Sepal.Length, x = ~Sepal.Width, fx = ~Species`. See [vg_mark()] for the full grammar.
#' @param aria_description `<string | param()>` The aria-description; a constant textual description.
#' @param aria_hidden `<string | param()>` The aria-hidden state; a constant indicating whether the element is exposed to an accessibility API.
#' @param aria_label `<column | literal | transform() | sql()/agg()>` The aria-label; a channel specifying short textual labels representing the value in the accessibility tree.
#' @param channels `<list>` Additional named channels, for example to include in a tooltip.
#' @param clip `<"frame" | "sphere" | boolean | NULL | param()>` How to clip the mark; one of: - *frame* or true - clip to the plot’s frame (inner area) - *sphere* - clip to the projected sphere (*e.g.*, front hemisphere) - null or false - do not clip The *sphere* clip option requires a geographic projection.
#' @param curve `<"basis" | "basis-closed" | "basis-open" | "bundle" | "bump-x" | "bump-y" | "cardinal" | "cardinal-closed" | "cardinal-open" | "catmull-rom" | ... | param()>` The curve (interpolation) method for connecting adjacent points.
#' @param dx `<number | param()>` The horizontal offset in pixels; a constant option.
#' @param dy `<number | param()>` The vertical offset in pixels; a constant option.
#' @param facet `<"auto" | "include" | "exclude" | "super" | boolean | NULL | param()>` Whether to enable or disable faceting; one of: - *auto* (default) - automatically determine if this mark should be faceted - *include* (or true) - draw the subset of the mark’s data in the current facet - *exclude* - draw the subset of the mark’s data *not* in the current facet - *super* - draw this mark in a single frame that covers all facets - null (or false) - repeat this mark’s data across all facets (*i.e.*, no faceting) When a mark uses *super* faceting, it is not allowed to use position scales (*x*, *y*, *fx*, or *fy*); *super* faceting is intended for decorations, such as labels and legends.
#' @param facet_anchor `<"top" | "right" | "bottom" | "left" | "top-left" | "top-right" | "bottom-left" | "bottom-right" | "top-empty" | "right-empty" | "bottom-empty" | "left-empty" | "empty" | NULL | param()>` How to place the mark with respect to facets; one of: - null (default for most marks) - display the mark in each non-empty facet - *top*, *right*, *bottom*, or *left* - display the mark only in facets on the given side - *top-empty*, *right-empty*, *bottom-empty*, or *left-empty* (default for axis marks) - display the mark only in facets that have empty space on the given side: either the margin, or an empty facet - *empty* - display the mark in empty facets only
#' @param fill `<column | literal | transform() | sql()/agg() | list(value=, ...) | param()>` The fill; a constant CSS color string, or a channel typically bound to the *color* scale.
#' @param fill_opacity `<column | literal | transform() | sql()/agg() | list(value=, ...) | param()>` The fill-opacity; a constant number between 0 and 1, or a channel typically bound to the *opacity* scale.
#' @param filter `<column | literal | transform() | sql()/agg()>` Applies a transform to filter the mark’s index according to the given channel values; only truthy values are retained.
#' @param fx `<column | literal | transform() | sql()/agg()>` The horizontal facet position channel, for mark-level faceting, bound to the *fx* scale.
#' @param fy `<column | literal | transform() | sql()/agg()>` The vertical facet position channel, for mark-level faceting, bound to the *fy* scale.
#' @param href `<column | literal | transform() | sql()/agg()>` The href; a channel specifying URLs for clickable links.
#' @param image_filter `<string | param()>` A CSS filter; a constant string used to adjust the rendering of images, such as *blur(5px)*.
#' @param margin `<number | param()>` Shorthand to set the same default for all four mark margins: **marginTop**, **marginRight**, **marginBottom**, and **marginLeft**; typically defaults to 0, except for axis marks.
#' @param margin_bottom `<number | param()>` The mark’s bottom margin; the minimum distance in pixels between the bottom edges of the inner and outer plot area.
#' @param margin_left `<number | param()>` The mark’s left margin; the minimum distance in pixels between the left edges of the inner and outer plot area.
#' @param margin_right `<number | param()>` The mark’s right margin; the minimum distance in pixels between the right edges of the mark’s inner and outer plot area.
#' @param margin_top `<number | param()>` The mark’s top margin; the minimum distance in pixels between the top edges of the inner and outer plot area.
#' @param marker `<"arrow" | "arrow-reverse" | "dot" | "circle" | "circle-fill" | "circle-stroke" | "tick" | "tick-x" | "tick-y" | "none" | boolean | NULL | param()>` Shorthand to set the same default for markerStart, markerMid, and markerEnd; one of: - a marker name such as *arrow* or *circle* - *none* (default) - no marker * true - alias for *circle-fill* * false or null - alias for *none*
#' @param marker_end `<"arrow" | "arrow-reverse" | "dot" | "circle" | "circle-fill" | "circle-stroke" | "tick" | "tick-x" | "tick-y" | "none" | boolean | NULL | param()>` The marker for the ending point of a line segment; one of: - a marker name such as *arrow* or *circle* * *none* (default) - no marker * true - alias for *circle-fill* * false or null - alias for *none*
#' @param marker_mid `<"arrow" | "arrow-reverse" | "dot" | "circle" | "circle-fill" | "circle-stroke" | "tick" | "tick-x" | "tick-y" | "none" | boolean | NULL | param()>` The marker for any middle (interior) points of a line segment.
#' @param marker_start `<"arrow" | "arrow-reverse" | "dot" | "circle" | "circle-fill" | "circle-stroke" | "tick" | "tick-x" | "tick-y" | "none" | boolean | NULL | param()>` The marker for the starting point of a line segment; one of: - a marker name such as *arrow* or *circle* * *none* (default) - no marker * true - alias for *circle-fill* * false or null - alias for *none*
#' @param mix_blend_mode `<string | param()>` The mix-blend-mode; a constant string specifying how to blend content such as *multiply*.
#' @param opacity `<column | literal | transform() | sql()/agg() | list(value=, ...)>` The opacity; a constant between 0 and 1, or a channel typically bound to the *opacity* scale.
#' @param paint_order `<string | param()>` The paint-order; a constant string specifying the order in which the **fill**, **stroke**, and any markers are drawn; defaults to *normal*, which draws the fill, then stroke, then markers; defaults to *stroke* for the text mark to create a “halo” around text to improve legibility.
#' @param pointer_events `<string | param()>` The pointer-events property; a constant string such as *none*.
#' @param reverse `<boolean | param()>` Applies a transform to reverse the order of the mark’s index, say for reverse input order.
#' @param select `<"first" | "last" | "maxX" | "maxY" | "minX" | "minY" | "nearest" | "nearestX" | "nearestY">` Applies a filter transform after data is loaded to highlight selected values only.
#' @param shape_rendering `<string | param()>` The shape-rendering; a constant string such as *crispEdges*.
#' @param sort `<column | literal | transform() | sql()/agg() | list>` Either applies a transform to sort the mark’s index by the specified channel values, or imputes ordinal scale domains from this mark’s channels.
#' @param stroke `<column | literal | transform() | sql()/agg() | list(value=, ...) | param()>` The stroke; a constant CSS color string, or a channel typically bound to the *color* scale.
#' @param stroke_dasharray `<string | number | param()>` The stroke-dasharray; a constant number indicating the length in pixels of alternating dashes and gaps, or a constant string of numbers separated by spaces or commas (_e.g._, *10 2* for dashes of 10 pixels separated by gaps of 2 pixels), or *none* (the default) for no dashing : https://developer.mozilla.org/en-US/docs/Web/SVG/Attribute/stroke-dasharray
#' @param stroke_dashoffset `<string | number | param()>` The stroke-dashoffset; a constant indicating the offset in pixels of the first dash along the stroke; defaults to zero.
#' @param stroke_linecap `<string | param()>` The stroke-linecap; a constant specifying how to cap stroked paths, such as *butt*, *round*, or *square*.
#' @param stroke_linejoin `<string | param()>` The stroke-linejoin; a constant specifying how to join stroked paths, such as *bevel*, *miter*, *miter-clip*, or *round*.
#' @param stroke_miterlimit `<number | param()>` The stroke-miterlimit; a constant number specifying how to limit the length of *miter* joins on stroked paths.
#' @param stroke_opacity `<column | literal | transform() | sql()/agg() | list(value=, ...)>` The stroke-opacity; a constant between 0 and 1, or a channel typically bound to the *opacity* scale.
#' @param stroke_width `<column | literal | transform() | sql()/agg() | list(value=, ...)>` The stroke-width; a constant number in pixels, or a channel.
#' @param target `<string | param()>` The target; a constant string specifying the target window (_e.g._, *_blank*) for clickable links; used in conjunction with the **href** option.
#' @param tension `<number | param()>` The tension option only has an effect on bundle, cardinal and Catmull–Rom splines (*bundle*, *cardinal*, *cardinal-open*, *cardinal-closed*, *catmull-rom*, *catmull-rom-open*, and *catmull-rom-closed*).
#' @param tip `<boolean | "x" | "y" | "xy" | list | param()>` Whether to generate a tooltip for this mark, and any tip options.
#' @param title `<column | literal | transform() | sql()/agg()>` The title; a channel specifying accessible, short textual descriptions as strings (possibly with newlines).
#' @param x `<column | literal | transform() | sql()/agg() | list(value=, ...)>` The horizontal position (or length) channel, typically bound to the *x* scale.
#' @param y `<column | literal | transform() | sql()/agg() | list(value=, ...)>` The vertical position channel, typically bound to the *y* scale; defaults to the zero-based index of the data 0, 1, 2, ….
#' @param z `<column | literal | transform() | sql()/agg()>` An optional ordinal channel for grouping data into (possibly stacked) series to be drawn as separate areas; defaults to **fill** if a channel, or **stroke** if a channel.
#' @param ... Additional options or plot-level attributes.
#' @param data_from The name of the data source this mark reads from (see [vg_data()]); a length-1 nonzero R integer (`1L`, `-1L`, ...; note the `L`) giving a 1-based index into the spec's registered data sources instead, negative counting from the end (e.g., `-1L` for the most recently registered one); or a literal vector of values to use as inline data directly (mosaic-spec's `"data": [...]` shorthand, e.g., for a single reference line -- a bare double like `0`/`c(0)`, or `0L` itself (never a valid index), means this, not an index). Left unset, defaults to the first registered data source (equivalent to `data_from = 1L`) for any mark type that takes data at all.
#' @param filter_by A Param/Selection (e.g., from [param()]) to filter this mark's data by.
#' @param data_optimize A flag (default `TRUE`) to enable mark-specific query optimizations for this mark's data; set `FALSE` to disable them (mosaic-spec's `data: {optimize: false}`).
#' @family mark functions
#' @export
vg_mark_hull <- function(spec = NULL, formula = vg_unset, aria_description = vg_unset, aria_hidden = vg_unset, aria_label = vg_unset, channels = vg_unset, clip = vg_unset, curve = vg_unset, dx = vg_unset, dy = vg_unset, facet = vg_unset, facet_anchor = vg_unset, fill = vg_unset, fill_opacity = vg_unset, filter = vg_unset, fx = vg_unset, fy = vg_unset, href = vg_unset, image_filter = vg_unset, margin = vg_unset, margin_bottom = vg_unset, margin_left = vg_unset, margin_right = vg_unset, margin_top = vg_unset, marker = vg_unset, marker_end = vg_unset, marker_mid = vg_unset, marker_start = vg_unset, mix_blend_mode = vg_unset, opacity = vg_unset, paint_order = vg_unset, pointer_events = vg_unset, reverse = vg_unset, select = vg_unset, shape_rendering = vg_unset, sort = vg_unset, stroke = vg_unset, stroke_dasharray = vg_unset, stroke_dashoffset = vg_unset, stroke_linecap = vg_unset, stroke_linejoin = vg_unset, stroke_miterlimit = vg_unset, stroke_opacity = vg_unset, stroke_width = vg_unset, target = vg_unset, tension = vg_unset, tip = vg_unset, title = vg_unset, x = vg_unset, y = vg_unset, z = vg_unset, ..., data_from = vg_unset, filter_by = vg_unset, data_optimize = vg_unset) {
  vg_mark_(spec, "hull", formula = formula, ariaDescription = aria_description, ariaHidden = aria_hidden, ariaLabel = aria_label, channels = channels, clip = clip, curve = curve, dx = dx, dy = dy, facet = facet, facetAnchor = facet_anchor, fill = fill, fillOpacity = fill_opacity, filter = filter, fx = fx, fy = fy, href = href, imageFilter = image_filter, margin = margin, marginBottom = margin_bottom, marginLeft = margin_left, marginRight = margin_right, marginTop = margin_top, marker = marker, markerEnd = marker_end, markerMid = marker_mid, markerStart = marker_start, mixBlendMode = mix_blend_mode, opacity = opacity, paintOrder = paint_order, pointerEvents = pointer_events, reverse = reverse, select = select, shapeRendering = shape_rendering, sort = sort, stroke = stroke, strokeDasharray = stroke_dasharray, strokeDashoffset = stroke_dashoffset, strokeLinecap = stroke_linecap, strokeLinejoin = stroke_linejoin, strokeMiterlimit = stroke_miterlimit, strokeOpacity = stroke_opacity, strokeWidth = stroke_width, target = target, tension = tension, tip = tip, title = title, x = x, y = y, z = z, ..., data_from = data_from, filter_by = filter_by, data_optimize = data_optimize)
}

#' The `image` mark.
#'
#' See [vg_value_types] for what the `<...>` notation below (`column`, `param()`, ...) means.
#'
#' @param spec A plot fragment or `vgspec` to add this mark to, or `NULL` to start a new plot with just this mark.
#' @param formula A shorthand for this mark's position channels (`x`, `y`, `fx`, `fy`, and paired `x1`/`x2` or `y1`/`y2`), e.g., `Sepal.Length ~ Sepal.Width | ~ Species` for `y = ~Sepal.Length, x = ~Sepal.Width, fx = ~Species`. See [vg_mark()] for the full grammar.
#' @param aria_description `<string | param()>` The aria-description; a constant textual description.
#' @param aria_hidden `<string | param()>` The aria-hidden state; a constant indicating whether the element is exposed to an accessibility API.
#' @param aria_label `<column | literal | transform() | sql()/agg()>` The aria-label; a channel specifying short textual labels representing the value in the accessibility tree.
#' @param channels `<list>` Additional named channels, for example to include in a tooltip.
#' @param clip `<"frame" | "sphere" | boolean | NULL | param()>` How to clip the mark; one of: - *frame* or true - clip to the plot’s frame (inner area) - *sphere* - clip to the projected sphere (*e.g.*, front hemisphere) - null or false - do not clip The *sphere* clip option requires a geographic projection.
#' @param cross_origin `<string | param()>` The cross-origin behavior.
#' @param dx `<number | param()>` The horizontal offset in pixels; a constant option.
#' @param dy `<number | param()>` The vertical offset in pixels; a constant option.
#' @param facet `<"auto" | "include" | "exclude" | "super" | boolean | NULL | param()>` Whether to enable or disable faceting; one of: - *auto* (default) - automatically determine if this mark should be faceted - *include* (or true) - draw the subset of the mark’s data in the current facet - *exclude* - draw the subset of the mark’s data *not* in the current facet - *super* - draw this mark in a single frame that covers all facets - null (or false) - repeat this mark’s data across all facets (*i.e.*, no faceting) When a mark uses *super* faceting, it is not allowed to use position scales (*x*, *y*, *fx*, or *fy*); *super* faceting is intended for decorations, such as labels and legends.
#' @param facet_anchor `<"top" | "right" | "bottom" | "left" | "top-left" | "top-right" | "bottom-left" | "bottom-right" | "top-empty" | "right-empty" | "bottom-empty" | "left-empty" | "empty" | NULL | param()>` How to place the mark with respect to facets; one of: - null (default for most marks) - display the mark in each non-empty facet - *top*, *right*, *bottom*, or *left* - display the mark only in facets on the given side - *top-empty*, *right-empty*, *bottom-empty*, or *left-empty* (default for axis marks) - display the mark only in facets that have empty space on the given side: either the margin, or an empty facet - *empty* - display the mark in empty facets only
#' @param fill `<column | literal | transform() | sql()/agg() | list(value=, ...) | param()>` The fill; a constant CSS color string, or a channel typically bound to the *color* scale.
#' @param fill_opacity `<column | literal | transform() | sql()/agg() | list(value=, ...) | param()>` The fill-opacity; a constant number between 0 and 1, or a channel typically bound to the *opacity* scale.
#' @param filter `<column | literal | transform() | sql()/agg()>` Applies a transform to filter the mark’s index according to the given channel values; only truthy values are retained.
#' @param frame_anchor `<"middle" | "top-left" | "top" | "top-right" | "right" | "bottom-right" | "bottom" | "bottom-left" | "left" | param()>` The frame anchor specifies defaults for **x** and **y**, along with **textAnchor** and **lineAnchor**, based on the plot’s frame; it may be one of the four sides (*top*, *right*, *bottom*, *left*), one of the four corners (*top-left*, *top-right*, *bottom-right*, *bottom-left*), or the *middle* of the frame.
#' @param fx `<column | literal | transform() | sql()/agg()>` The horizontal facet position channel, for mark-level faceting, bound to the *fx* scale.
#' @param fy `<column | literal | transform() | sql()/agg()>` The vertical facet position channel, for mark-level faceting, bound to the *fy* scale.
#' @param height `<number | param()>` The height (number of rows) of the grid, in actual pixels.
#' @param href `<column | literal | transform() | sql()/agg()>` The href; a channel specifying URLs for clickable links.
#' @param image_filter `<string | param()>` A CSS filter; a constant string used to adjust the rendering of images, such as *blur(5px)*.
#' @param image_rendering `<string | param()>` The image-rendering attribute; defaults to *auto* (bilinear).
#' @param margin `<number | param()>` Shorthand to set the same default for all four mark margins: **marginTop**, **marginRight**, **marginBottom**, and **marginLeft**; typically defaults to 0, except for axis marks.
#' @param margin_bottom `<number | param()>` The mark’s bottom margin; the minimum distance in pixels between the bottom edges of the inner and outer plot area.
#' @param margin_left `<number | param()>` The mark’s left margin; the minimum distance in pixels between the left edges of the inner and outer plot area.
#' @param margin_right `<number | param()>` The mark’s right margin; the minimum distance in pixels between the right edges of the mark’s inner and outer plot area.
#' @param margin_top `<number | param()>` The mark’s top margin; the minimum distance in pixels between the top edges of the inner and outer plot area.
#' @param mix_blend_mode `<string | param()>` The mix-blend-mode; a constant string specifying how to blend content such as *multiply*.
#' @param opacity `<column | literal | transform() | sql()/agg() | list(value=, ...)>` The opacity; a constant between 0 and 1, or a channel typically bound to the *opacity* scale.
#' @param paint_order `<string | param()>` The paint-order; a constant string specifying the order in which the **fill**, **stroke**, and any markers are drawn; defaults to *normal*, which draws the fill, then stroke, then markers; defaults to *stroke* for the text mark to create a “halo” around text to improve legibility.
#' @param pointer_events `<string | param()>` The pointer-events property; a constant string such as *none*.
#' @param preserve_aspect_ratio `<string | param()>` The image aspect ratio; defaults to *xMidYMid meet*.
#' @param r `<column | literal | transform() | sql()/agg() | list(value=, ...) | number | param()>` The radius of dots; either a channel or constant.
#' @param reverse `<boolean | param()>` Applies a transform to reverse the order of the mark’s index, say for reverse input order.
#' @param rotate `<column | literal | transform() | sql()/agg() | param()>` The rotation angle in degrees clockwise; a constant or a channel; defaults to 0°.
#' @param select `<"first" | "last" | "maxX" | "maxY" | "minX" | "minY" | "nearest" | "nearestX" | "nearestY">` Applies a filter transform after data is loaded to highlight selected values only.
#' @param shape_rendering `<string | param()>` The shape-rendering; a constant string such as *crispEdges*.
#' @param sort `<column | literal | transform() | sql()/agg() | list>` Either applies a transform to sort the mark’s index by the specified channel values, or imputes ordinal scale domains from this mark’s channels.
#' @param src `<column | literal | transform() | sql()/agg() | param()>` The required image URL (or relative path).
#' @param stroke `<column | literal | transform() | sql()/agg() | list(value=, ...) | param()>` The stroke; a constant CSS color string, or a channel typically bound to the *color* scale.
#' @param stroke_dasharray `<string | number | param()>` The stroke-dasharray; a constant number indicating the length in pixels of alternating dashes and gaps, or a constant string of numbers separated by spaces or commas (_e.g._, *10 2* for dashes of 10 pixels separated by gaps of 2 pixels), or *none* (the default) for no dashing : https://developer.mozilla.org/en-US/docs/Web/SVG/Attribute/stroke-dasharray
#' @param stroke_dashoffset `<string | number | param()>` The stroke-dashoffset; a constant indicating the offset in pixels of the first dash along the stroke; defaults to zero.
#' @param stroke_linecap `<string | param()>` The stroke-linecap; a constant specifying how to cap stroked paths, such as *butt*, *round*, or *square*.
#' @param stroke_linejoin `<string | param()>` The stroke-linejoin; a constant specifying how to join stroked paths, such as *bevel*, *miter*, *miter-clip*, or *round*.
#' @param stroke_miterlimit `<number | param()>` The stroke-miterlimit; a constant number specifying how to limit the length of *miter* joins on stroked paths.
#' @param stroke_opacity `<column | literal | transform() | sql()/agg() | list(value=, ...)>` The stroke-opacity; a constant between 0 and 1, or a channel typically bound to the *opacity* scale.
#' @param stroke_width `<column | literal | transform() | sql()/agg() | list(value=, ...)>` The stroke-width; a constant number in pixels, or a channel.
#' @param target `<string | param()>` The target; a constant string specifying the target window (_e.g._, *_blank*) for clickable links; used in conjunction with the **href** option.
#' @param tip `<boolean | "x" | "y" | "xy" | list | param()>` Whether to generate a tooltip for this mark, and any tip options.
#' @param title `<column | literal | transform() | sql()/agg()>` The title; a channel specifying accessible, short textual descriptions as strings (possibly with newlines).
#' @param width `<number | param()>` The width (number of columns) of the grid, in actual pixels.
#' @param x `<column | literal | transform() | sql()/agg() | list(value=, ...)>` The horizontal position (or length) channel, typically bound to the *x* scale.
#' @param y `<column | literal | transform() | sql()/agg() | list(value=, ...)>` The vertical position channel, typically bound to the *y* scale; defaults to the zero-based index of the data 0, 1, 2, ….
#' @param ... Additional options or plot-level attributes.
#' @param data_from The name of the data source this mark reads from (see [vg_data()]); a length-1 nonzero R integer (`1L`, `-1L`, ...; note the `L`) giving a 1-based index into the spec's registered data sources instead, negative counting from the end (e.g., `-1L` for the most recently registered one); or a literal vector of values to use as inline data directly (mosaic-spec's `"data": [...]` shorthand, e.g., for a single reference line -- a bare double like `0`/`c(0)`, or `0L` itself (never a valid index), means this, not an index). Left unset, defaults to the first registered data source (equivalent to `data_from = 1L`) for any mark type that takes data at all.
#' @param filter_by A Param/Selection (e.g., from [param()]) to filter this mark's data by.
#' @param data_optimize A flag (default `TRUE`) to enable mark-specific query optimizations for this mark's data; set `FALSE` to disable them (mosaic-spec's `data: {optimize: false}`).
#' @family mark functions
#' @export
vg_mark_image <- function(spec = NULL, formula = vg_unset, aria_description = vg_unset, aria_hidden = vg_unset, aria_label = vg_unset, channels = vg_unset, clip = vg_unset, cross_origin = vg_unset, dx = vg_unset, dy = vg_unset, facet = vg_unset, facet_anchor = vg_unset, fill = vg_unset, fill_opacity = vg_unset, filter = vg_unset, frame_anchor = vg_unset, fx = vg_unset, fy = vg_unset, height = vg_unset, href = vg_unset, image_filter = vg_unset, image_rendering = vg_unset, margin = vg_unset, margin_bottom = vg_unset, margin_left = vg_unset, margin_right = vg_unset, margin_top = vg_unset, mix_blend_mode = vg_unset, opacity = vg_unset, paint_order = vg_unset, pointer_events = vg_unset, preserve_aspect_ratio = vg_unset, r = vg_unset, reverse = vg_unset, rotate = vg_unset, select = vg_unset, shape_rendering = vg_unset, sort = vg_unset, src = vg_unset, stroke = vg_unset, stroke_dasharray = vg_unset, stroke_dashoffset = vg_unset, stroke_linecap = vg_unset, stroke_linejoin = vg_unset, stroke_miterlimit = vg_unset, stroke_opacity = vg_unset, stroke_width = vg_unset, target = vg_unset, tip = vg_unset, title = vg_unset, width = vg_unset, x = vg_unset, y = vg_unset, ..., data_from = vg_unset, filter_by = vg_unset, data_optimize = vg_unset) {
  vg_mark_(spec, "image", formula = formula, ariaDescription = aria_description, ariaHidden = aria_hidden, ariaLabel = aria_label, channels = channels, clip = clip, crossOrigin = cross_origin, dx = dx, dy = dy, facet = facet, facetAnchor = facet_anchor, fill = fill, fillOpacity = fill_opacity, filter = filter, frameAnchor = frame_anchor, fx = fx, fy = fy, height = height, href = href, imageFilter = image_filter, imageRendering = image_rendering, margin = margin, marginBottom = margin_bottom, marginLeft = margin_left, marginRight = margin_right, marginTop = margin_top, mixBlendMode = mix_blend_mode, opacity = opacity, paintOrder = paint_order, pointerEvents = pointer_events, preserveAspectRatio = preserve_aspect_ratio, r = r, reverse = reverse, rotate = rotate, select = select, shapeRendering = shape_rendering, sort = sort, src = src, stroke = stroke, strokeDasharray = stroke_dasharray, strokeDashoffset = stroke_dashoffset, strokeLinecap = stroke_linecap, strokeLinejoin = stroke_linejoin, strokeMiterlimit = stroke_miterlimit, strokeOpacity = stroke_opacity, strokeWidth = stroke_width, target = target, tip = tip, title = title, width = width, x = x, y = y, ..., data_from = data_from, filter_by = filter_by, data_optimize = data_optimize)
}

#' The line mark.
#'
#' See [vg_value_types] for what the `<...>` notation below (`column`, `param()`, ...) means.
#'
#' @param spec A plot fragment or `vgspec` to add this mark to, or `NULL` to start a new plot with just this mark.
#' @param formula A shorthand for this mark's position channels (`x`, `y`, `fx`, `fy`, and paired `x1`/`x2` or `y1`/`y2`), e.g., `Sepal.Length ~ Sepal.Width | ~ Species` for `y = ~Sepal.Length, x = ~Sepal.Width, fx = ~Species`. See [vg_mark()] for the full grammar.
#' @param aria_description `<string | param()>` The aria-description; a constant textual description.
#' @param aria_hidden `<string | param()>` The aria-hidden state; a constant indicating whether the element is exposed to an accessibility API.
#' @param aria_label `<column | literal | transform() | sql()/agg()>` The aria-label; a channel specifying short textual labels representing the value in the accessibility tree.
#' @param channels `<list>` Additional named channels, for example to include in a tooltip.
#' @param clip `<"frame" | "sphere" | boolean | NULL | param()>` How to clip the mark; one of: - *frame* or true - clip to the plot’s frame (inner area) - *sphere* - clip to the projected sphere (*e.g.*, front hemisphere) - null or false - do not clip The *sphere* clip option requires a geographic projection.
#' @param curve `<"basis" | "basis-closed" | "basis-open" | "bundle" | "bump-x" | "bump-y" | "cardinal" | "cardinal-closed" | "cardinal-open" | "catmull-rom" | ... | param()>` The curve (interpolation) method for connecting adjacent points.
#' @param dx `<number | param()>` The horizontal offset in pixels; a constant option.
#' @param dy `<number | param()>` The vertical offset in pixels; a constant option.
#' @param facet `<"auto" | "include" | "exclude" | "super" | boolean | NULL | param()>` Whether to enable or disable faceting; one of: - *auto* (default) - automatically determine if this mark should be faceted - *include* (or true) - draw the subset of the mark’s data in the current facet - *exclude* - draw the subset of the mark’s data *not* in the current facet - *super* - draw this mark in a single frame that covers all facets - null (or false) - repeat this mark’s data across all facets (*i.e.*, no faceting) When a mark uses *super* faceting, it is not allowed to use position scales (*x*, *y*, *fx*, or *fy*); *super* faceting is intended for decorations, such as labels and legends.
#' @param facet_anchor `<"top" | "right" | "bottom" | "left" | "top-left" | "top-right" | "bottom-left" | "bottom-right" | "top-empty" | "right-empty" | "bottom-empty" | "left-empty" | "empty" | NULL | param()>` How to place the mark with respect to facets; one of: - null (default for most marks) - display the mark in each non-empty facet - *top*, *right*, *bottom*, or *left* - display the mark only in facets on the given side - *top-empty*, *right-empty*, *bottom-empty*, or *left-empty* (default for axis marks) - display the mark only in facets that have empty space on the given side: either the margin, or an empty facet - *empty* - display the mark in empty facets only
#' @param fill `<column | literal | transform() | sql()/agg() | list(value=, ...) | param()>` The fill; a constant CSS color string, or a channel typically bound to the *color* scale.
#' @param fill_opacity `<column | literal | transform() | sql()/agg() | list(value=, ...) | param()>` The fill-opacity; a constant number between 0 and 1, or a channel typically bound to the *opacity* scale.
#' @param filter `<column | literal | transform() | sql()/agg()>` Applies a transform to filter the mark’s index according to the given channel values; only truthy values are retained.
#' @param fx `<column | literal | transform() | sql()/agg()>` The horizontal facet position channel, for mark-level faceting, bound to the *fx* scale.
#' @param fy `<column | literal | transform() | sql()/agg()>` The vertical facet position channel, for mark-level faceting, bound to the *fy* scale.
#' @param href `<column | literal | transform() | sql()/agg()>` The href; a channel specifying URLs for clickable links.
#' @param image_filter `<string | param()>` A CSS filter; a constant string used to adjust the rendering of images, such as *blur(5px)*.
#' @param margin `<number | param()>` Shorthand to set the same default for all four mark margins: **marginTop**, **marginRight**, **marginBottom**, and **marginLeft**; typically defaults to 0, except for axis marks.
#' @param margin_bottom `<number | param()>` The mark’s bottom margin; the minimum distance in pixels between the bottom edges of the inner and outer plot area.
#' @param margin_left `<number | param()>` The mark’s left margin; the minimum distance in pixels between the left edges of the inner and outer plot area.
#' @param margin_right `<number | param()>` The mark’s right margin; the minimum distance in pixels between the right edges of the mark’s inner and outer plot area.
#' @param margin_top `<number | param()>` The mark’s top margin; the minimum distance in pixels between the top edges of the inner and outer plot area.
#' @param marker `<"arrow" | "arrow-reverse" | "dot" | "circle" | "circle-fill" | "circle-stroke" | "tick" | "tick-x" | "tick-y" | "none" | boolean | NULL | param()>` Shorthand to set the same default for markerStart, markerMid, and markerEnd; one of: - a marker name such as *arrow* or *circle* - *none* (default) - no marker * true - alias for *circle-fill* * false or null - alias for *none*
#' @param marker_end `<"arrow" | "arrow-reverse" | "dot" | "circle" | "circle-fill" | "circle-stroke" | "tick" | "tick-x" | "tick-y" | "none" | boolean | NULL | param()>` The marker for the ending point of a line segment; one of: - a marker name such as *arrow* or *circle* * *none* (default) - no marker * true - alias for *circle-fill* * false or null - alias for *none*
#' @param marker_mid `<"arrow" | "arrow-reverse" | "dot" | "circle" | "circle-fill" | "circle-stroke" | "tick" | "tick-x" | "tick-y" | "none" | boolean | NULL | param()>` The marker for any middle (interior) points of a line segment.
#' @param marker_start `<"arrow" | "arrow-reverse" | "dot" | "circle" | "circle-fill" | "circle-stroke" | "tick" | "tick-x" | "tick-y" | "none" | boolean | NULL | param()>` The marker for the starting point of a line segment; one of: - a marker name such as *arrow* or *circle* * *none* (default) - no marker * true - alias for *circle-fill* * false or null - alias for *none*
#' @param mix_blend_mode `<string | param()>` The mix-blend-mode; a constant string specifying how to blend content such as *multiply*.
#' @param opacity `<column | literal | transform() | sql()/agg() | list(value=, ...)>` The opacity; a constant between 0 and 1, or a channel typically bound to the *opacity* scale.
#' @param paint_order `<string | param()>` The paint-order; a constant string specifying the order in which the **fill**, **stroke**, and any markers are drawn; defaults to *normal*, which draws the fill, then stroke, then markers; defaults to *stroke* for the text mark to create a “halo” around text to improve legibility.
#' @param pointer_events `<string | param()>` The pointer-events property; a constant string such as *none*.
#' @param reverse `<boolean | param()>` Applies a transform to reverse the order of the mark’s index, say for reverse input order.
#' @param select `<"first" | "last" | "maxX" | "maxY" | "minX" | "minY" | "nearest" | "nearestX" | "nearestY">` Applies a filter transform after data is loaded to highlight selected values only.
#' @param shape_rendering `<string | param()>` The shape-rendering; a constant string such as *crispEdges*.
#' @param sort `<column | literal | transform() | sql()/agg() | list>` Either applies a transform to sort the mark’s index by the specified channel values, or imputes ordinal scale domains from this mark’s channels.
#' @param stroke `<column | literal | transform() | sql()/agg() | list(value=, ...) | param()>` The stroke; a constant CSS color string, or a channel typically bound to the *color* scale.
#' @param stroke_dasharray `<string | number | param()>` The stroke-dasharray; a constant number indicating the length in pixels of alternating dashes and gaps, or a constant string of numbers separated by spaces or commas (_e.g._, *10 2* for dashes of 10 pixels separated by gaps of 2 pixels), or *none* (the default) for no dashing : https://developer.mozilla.org/en-US/docs/Web/SVG/Attribute/stroke-dasharray
#' @param stroke_dashoffset `<string | number | param()>` The stroke-dashoffset; a constant indicating the offset in pixels of the first dash along the stroke; defaults to zero.
#' @param stroke_linecap `<string | param()>` The stroke-linecap; a constant specifying how to cap stroked paths, such as *butt*, *round*, or *square*.
#' @param stroke_linejoin `<string | param()>` The stroke-linejoin; a constant specifying how to join stroked paths, such as *bevel*, *miter*, *miter-clip*, or *round*.
#' @param stroke_miterlimit `<number | param()>` The stroke-miterlimit; a constant number specifying how to limit the length of *miter* joins on stroked paths.
#' @param stroke_opacity `<column | literal | transform() | sql()/agg() | list(value=, ...)>` The stroke-opacity; a constant between 0 and 1, or a channel typically bound to the *opacity* scale.
#' @param stroke_width `<column | literal | transform() | sql()/agg() | list(value=, ...)>` The stroke-width; a constant number in pixels, or a channel.
#' @param target `<string | param()>` The target; a constant string specifying the target window (_e.g._, *_blank*) for clickable links; used in conjunction with the **href** option.
#' @param tension `<number | param()>` The tension option only has an effect on bundle, cardinal and Catmull–Rom splines (*bundle*, *cardinal*, *cardinal-open*, *cardinal-closed*, *catmull-rom*, *catmull-rom-open*, and *catmull-rom-closed*).
#' @param tip `<boolean | "x" | "y" | "xy" | list | param()>` Whether to generate a tooltip for this mark, and any tip options.
#' @param title `<column | literal | transform() | sql()/agg()>` The title; a channel specifying accessible, short textual descriptions as strings (possibly with newlines).
#' @param x `<column | literal | transform() | sql()/agg() | list(value=, ...)>` The horizontal position (or length) channel, typically bound to the *x* scale.
#' @param y `<column | literal | transform() | sql()/agg() | list(value=, ...)>` The vertical position channel, typically bound to the *y* scale; defaults to the zero-based index of the data 0, 1, 2, ….
#' @param z `<column | literal | transform() | sql()/agg()>` An optional ordinal channel for grouping data into (possibly stacked) series to be drawn as separate areas; defaults to **fill** if a channel, or **stroke** if a channel.
#' @param ... Additional options or plot-level attributes.
#' @param data_from The name of the data source this mark reads from (see [vg_data()]); a length-1 nonzero R integer (`1L`, `-1L`, ...; note the `L`) giving a 1-based index into the spec's registered data sources instead, negative counting from the end (e.g., `-1L` for the most recently registered one); or a literal vector of values to use as inline data directly (mosaic-spec's `"data": [...]` shorthand, e.g., for a single reference line -- a bare double like `0`/`c(0)`, or `0L` itself (never a valid index), means this, not an index). Left unset, defaults to the first registered data source (equivalent to `data_from = 1L`) for any mark type that takes data at all.
#' @param filter_by A Param/Selection (e.g., from [param()]) to filter this mark's data by.
#' @param data_optimize A flag (default `TRUE`) to enable mark-specific query optimizations for this mark's data; set `FALSE` to disable them (mosaic-spec's `data: {optimize: false}`).
#' @family mark functions
#' @export
vg_mark_line <- function(spec = NULL, formula = vg_unset, aria_description = vg_unset, aria_hidden = vg_unset, aria_label = vg_unset, channels = vg_unset, clip = vg_unset, curve = vg_unset, dx = vg_unset, dy = vg_unset, facet = vg_unset, facet_anchor = vg_unset, fill = vg_unset, fill_opacity = vg_unset, filter = vg_unset, fx = vg_unset, fy = vg_unset, href = vg_unset, image_filter = vg_unset, margin = vg_unset, margin_bottom = vg_unset, margin_left = vg_unset, margin_right = vg_unset, margin_top = vg_unset, marker = vg_unset, marker_end = vg_unset, marker_mid = vg_unset, marker_start = vg_unset, mix_blend_mode = vg_unset, opacity = vg_unset, paint_order = vg_unset, pointer_events = vg_unset, reverse = vg_unset, select = vg_unset, shape_rendering = vg_unset, sort = vg_unset, stroke = vg_unset, stroke_dasharray = vg_unset, stroke_dashoffset = vg_unset, stroke_linecap = vg_unset, stroke_linejoin = vg_unset, stroke_miterlimit = vg_unset, stroke_opacity = vg_unset, stroke_width = vg_unset, target = vg_unset, tension = vg_unset, tip = vg_unset, title = vg_unset, x = vg_unset, y = vg_unset, z = vg_unset, ..., data_from = vg_unset, filter_by = vg_unset, data_optimize = vg_unset) {
  vg_mark_(spec, "line", formula = formula, ariaDescription = aria_description, ariaHidden = aria_hidden, ariaLabel = aria_label, channels = channels, clip = clip, curve = curve, dx = dx, dy = dy, facet = facet, facetAnchor = facet_anchor, fill = fill, fillOpacity = fill_opacity, filter = filter, fx = fx, fy = fy, href = href, imageFilter = image_filter, margin = margin, marginBottom = margin_bottom, marginLeft = margin_left, marginRight = margin_right, marginTop = margin_top, marker = marker, markerEnd = marker_end, markerMid = marker_mid, markerStart = marker_start, mixBlendMode = mix_blend_mode, opacity = opacity, paintOrder = paint_order, pointerEvents = pointer_events, reverse = reverse, select = select, shapeRendering = shape_rendering, sort = sort, stroke = stroke, strokeDasharray = stroke_dasharray, strokeDashoffset = stroke_dashoffset, strokeLinecap = stroke_linecap, strokeLinejoin = stroke_linejoin, strokeMiterlimit = stroke_miterlimit, strokeOpacity = stroke_opacity, strokeWidth = stroke_width, target = target, tension = tension, tip = tip, title = title, x = x, y = y, z = z, ..., data_from = data_from, filter_by = filter_by, data_optimize = data_optimize)
}

#' The lineX mark.
#'
#' See [vg_value_types] for what the `<...>` notation below (`column`, `param()`, ...) means.
#'
#' @param spec A plot fragment or `vgspec` to add this mark to, or `NULL` to start a new plot with just this mark.
#' @param formula A shorthand for this mark's position channels (`x`, `y`, `fx`, `fy`, and paired `x1`/`x2` or `y1`/`y2`), e.g., `Sepal.Length ~ Sepal.Width | ~ Species` for `y = ~Sepal.Length, x = ~Sepal.Width, fx = ~Species`. See [vg_mark()] for the full grammar.
#' @param aria_description `<string | param()>` The aria-description; a constant textual description.
#' @param aria_hidden `<string | param()>` The aria-hidden state; a constant indicating whether the element is exposed to an accessibility API.
#' @param aria_label `<column | literal | transform() | sql()/agg()>` The aria-label; a channel specifying short textual labels representing the value in the accessibility tree.
#' @param channels `<list>` Additional named channels, for example to include in a tooltip.
#' @param clip `<"frame" | "sphere" | boolean | NULL | param()>` How to clip the mark; one of: - *frame* or true - clip to the plot’s frame (inner area) - *sphere* - clip to the projected sphere (*e.g.*, front hemisphere) - null or false - do not clip The *sphere* clip option requires a geographic projection.
#' @param curve `<"basis" | "basis-closed" | "basis-open" | "bundle" | "bump-x" | "bump-y" | "cardinal" | "cardinal-closed" | "cardinal-open" | "catmull-rom" | ... | param()>` The curve (interpolation) method for connecting adjacent points.
#' @param dx `<number | param()>` The horizontal offset in pixels; a constant option.
#' @param dy `<number | param()>` The vertical offset in pixels; a constant option.
#' @param facet `<"auto" | "include" | "exclude" | "super" | boolean | NULL | param()>` Whether to enable or disable faceting; one of: - *auto* (default) - automatically determine if this mark should be faceted - *include* (or true) - draw the subset of the mark’s data in the current facet - *exclude* - draw the subset of the mark’s data *not* in the current facet - *super* - draw this mark in a single frame that covers all facets - null (or false) - repeat this mark’s data across all facets (*i.e.*, no faceting) When a mark uses *super* faceting, it is not allowed to use position scales (*x*, *y*, *fx*, or *fy*); *super* faceting is intended for decorations, such as labels and legends.
#' @param facet_anchor `<"top" | "right" | "bottom" | "left" | "top-left" | "top-right" | "bottom-left" | "bottom-right" | "top-empty" | "right-empty" | "bottom-empty" | "left-empty" | "empty" | NULL | param()>` How to place the mark with respect to facets; one of: - null (default for most marks) - display the mark in each non-empty facet - *top*, *right*, *bottom*, or *left* - display the mark only in facets on the given side - *top-empty*, *right-empty*, *bottom-empty*, or *left-empty* (default for axis marks) - display the mark only in facets that have empty space on the given side: either the margin, or an empty facet - *empty* - display the mark in empty facets only
#' @param fill `<column | literal | transform() | sql()/agg() | list(value=, ...) | param()>` The fill; a constant CSS color string, or a channel typically bound to the *color* scale.
#' @param fill_opacity `<column | literal | transform() | sql()/agg() | list(value=, ...) | param()>` The fill-opacity; a constant number between 0 and 1, or a channel typically bound to the *opacity* scale.
#' @param filter `<column | literal | transform() | sql()/agg()>` Applies a transform to filter the mark’s index according to the given channel values; only truthy values are retained.
#' @param fx `<column | literal | transform() | sql()/agg()>` The horizontal facet position channel, for mark-level faceting, bound to the *fx* scale.
#' @param fy `<column | literal | transform() | sql()/agg()>` The vertical facet position channel, for mark-level faceting, bound to the *fy* scale.
#' @param href `<column | literal | transform() | sql()/agg()>` The href; a channel specifying URLs for clickable links.
#' @param image_filter `<string | param()>` A CSS filter; a constant string used to adjust the rendering of images, such as *blur(5px)*.
#' @param margin `<number | param()>` Shorthand to set the same default for all four mark margins: **marginTop**, **marginRight**, **marginBottom**, and **marginLeft**; typically defaults to 0, except for axis marks.
#' @param margin_bottom `<number | param()>` The mark’s bottom margin; the minimum distance in pixels between the bottom edges of the inner and outer plot area.
#' @param margin_left `<number | param()>` The mark’s left margin; the minimum distance in pixels between the left edges of the inner and outer plot area.
#' @param margin_right `<number | param()>` The mark’s right margin; the minimum distance in pixels between the right edges of the mark’s inner and outer plot area.
#' @param margin_top `<number | param()>` The mark’s top margin; the minimum distance in pixels between the top edges of the inner and outer plot area.
#' @param marker `<"arrow" | "arrow-reverse" | "dot" | "circle" | "circle-fill" | "circle-stroke" | "tick" | "tick-x" | "tick-y" | "none" | boolean | NULL | param()>` Shorthand to set the same default for markerStart, markerMid, and markerEnd; one of: - a marker name such as *arrow* or *circle* - *none* (default) - no marker * true - alias for *circle-fill* * false or null - alias for *none*
#' @param marker_end `<"arrow" | "arrow-reverse" | "dot" | "circle" | "circle-fill" | "circle-stroke" | "tick" | "tick-x" | "tick-y" | "none" | boolean | NULL | param()>` The marker for the ending point of a line segment; one of: - a marker name such as *arrow* or *circle* * *none* (default) - no marker * true - alias for *circle-fill* * false or null - alias for *none*
#' @param marker_mid `<"arrow" | "arrow-reverse" | "dot" | "circle" | "circle-fill" | "circle-stroke" | "tick" | "tick-x" | "tick-y" | "none" | boolean | NULL | param()>` The marker for any middle (interior) points of a line segment.
#' @param marker_start `<"arrow" | "arrow-reverse" | "dot" | "circle" | "circle-fill" | "circle-stroke" | "tick" | "tick-x" | "tick-y" | "none" | boolean | NULL | param()>` The marker for the starting point of a line segment; one of: - a marker name such as *arrow* or *circle* * *none* (default) - no marker * true - alias for *circle-fill* * false or null - alias for *none*
#' @param mix_blend_mode `<string | param()>` The mix-blend-mode; a constant string specifying how to blend content such as *multiply*.
#' @param opacity `<column | literal | transform() | sql()/agg() | list(value=, ...)>` The opacity; a constant between 0 and 1, or a channel typically bound to the *opacity* scale.
#' @param paint_order `<string | param()>` The paint-order; a constant string specifying the order in which the **fill**, **stroke**, and any markers are drawn; defaults to *normal*, which draws the fill, then stroke, then markers; defaults to *stroke* for the text mark to create a “halo” around text to improve legibility.
#' @param pointer_events `<string | param()>` The pointer-events property; a constant string such as *none*.
#' @param reverse `<boolean | param()>` Applies a transform to reverse the order of the mark’s index, say for reverse input order.
#' @param select `<"first" | "last" | "maxX" | "maxY" | "minX" | "minY" | "nearest" | "nearestX" | "nearestY">` Applies a filter transform after data is loaded to highlight selected values only.
#' @param shape_rendering `<string | param()>` The shape-rendering; a constant string such as *crispEdges*.
#' @param sort `<column | literal | transform() | sql()/agg() | list>` Either applies a transform to sort the mark’s index by the specified channel values, or imputes ordinal scale domains from this mark’s channels.
#' @param stroke `<column | literal | transform() | sql()/agg() | list(value=, ...) | param()>` The stroke; a constant CSS color string, or a channel typically bound to the *color* scale.
#' @param stroke_dasharray `<string | number | param()>` The stroke-dasharray; a constant number indicating the length in pixels of alternating dashes and gaps, or a constant string of numbers separated by spaces or commas (_e.g._, *10 2* for dashes of 10 pixels separated by gaps of 2 pixels), or *none* (the default) for no dashing : https://developer.mozilla.org/en-US/docs/Web/SVG/Attribute/stroke-dasharray
#' @param stroke_dashoffset `<string | number | param()>` The stroke-dashoffset; a constant indicating the offset in pixels of the first dash along the stroke; defaults to zero.
#' @param stroke_linecap `<string | param()>` The stroke-linecap; a constant specifying how to cap stroked paths, such as *butt*, *round*, or *square*.
#' @param stroke_linejoin `<string | param()>` The stroke-linejoin; a constant specifying how to join stroked paths, such as *bevel*, *miter*, *miter-clip*, or *round*.
#' @param stroke_miterlimit `<number | param()>` The stroke-miterlimit; a constant number specifying how to limit the length of *miter* joins on stroked paths.
#' @param stroke_opacity `<column | literal | transform() | sql()/agg() | list(value=, ...)>` The stroke-opacity; a constant between 0 and 1, or a channel typically bound to the *opacity* scale.
#' @param stroke_width `<column | literal | transform() | sql()/agg() | list(value=, ...)>` The stroke-width; a constant number in pixels, or a channel.
#' @param target `<string | param()>` The target; a constant string specifying the target window (_e.g._, *_blank*) for clickable links; used in conjunction with the **href** option.
#' @param tension `<number | param()>` The tension option only has an effect on bundle, cardinal and Catmull–Rom splines (*bundle*, *cardinal*, *cardinal-open*, *cardinal-closed*, *catmull-rom*, *catmull-rom-open*, and *catmull-rom-closed*).
#' @param tip `<boolean | "x" | "y" | "xy" | list | param()>` Whether to generate a tooltip for this mark, and any tip options.
#' @param title `<column | literal | transform() | sql()/agg()>` The title; a channel specifying accessible, short textual descriptions as strings (possibly with newlines).
#' @param x `<column | literal | transform() | sql()/agg() | list(value=, ...)>` The horizontal position (or length) channel, typically bound to the *x* scale.
#' @param y `<column | literal | transform() | sql()/agg() | list(value=, ...)>` The vertical position channel, typically bound to the *y* scale; defaults to the zero-based index of the data 0, 1, 2, ….
#' @param z `<column | literal | transform() | sql()/agg()>` An optional ordinal channel for grouping data into (possibly stacked) series to be drawn as separate areas; defaults to **fill** if a channel, or **stroke** if a channel.
#' @param ... Additional options or plot-level attributes.
#' @param data_from The name of the data source this mark reads from (see [vg_data()]); a length-1 nonzero R integer (`1L`, `-1L`, ...; note the `L`) giving a 1-based index into the spec's registered data sources instead, negative counting from the end (e.g., `-1L` for the most recently registered one); or a literal vector of values to use as inline data directly (mosaic-spec's `"data": [...]` shorthand, e.g., for a single reference line -- a bare double like `0`/`c(0)`, or `0L` itself (never a valid index), means this, not an index). Left unset, defaults to the first registered data source (equivalent to `data_from = 1L`) for any mark type that takes data at all.
#' @param filter_by A Param/Selection (e.g., from [param()]) to filter this mark's data by.
#' @param data_optimize A flag (default `TRUE`) to enable mark-specific query optimizations for this mark's data; set `FALSE` to disable them (mosaic-spec's `data: {optimize: false}`).
#' @family mark functions
#' @export
vg_mark_line_x <- function(spec = NULL, formula = vg_unset, aria_description = vg_unset, aria_hidden = vg_unset, aria_label = vg_unset, channels = vg_unset, clip = vg_unset, curve = vg_unset, dx = vg_unset, dy = vg_unset, facet = vg_unset, facet_anchor = vg_unset, fill = vg_unset, fill_opacity = vg_unset, filter = vg_unset, fx = vg_unset, fy = vg_unset, href = vg_unset, image_filter = vg_unset, margin = vg_unset, margin_bottom = vg_unset, margin_left = vg_unset, margin_right = vg_unset, margin_top = vg_unset, marker = vg_unset, marker_end = vg_unset, marker_mid = vg_unset, marker_start = vg_unset, mix_blend_mode = vg_unset, opacity = vg_unset, paint_order = vg_unset, pointer_events = vg_unset, reverse = vg_unset, select = vg_unset, shape_rendering = vg_unset, sort = vg_unset, stroke = vg_unset, stroke_dasharray = vg_unset, stroke_dashoffset = vg_unset, stroke_linecap = vg_unset, stroke_linejoin = vg_unset, stroke_miterlimit = vg_unset, stroke_opacity = vg_unset, stroke_width = vg_unset, target = vg_unset, tension = vg_unset, tip = vg_unset, title = vg_unset, x = vg_unset, y = vg_unset, z = vg_unset, ..., data_from = vg_unset, filter_by = vg_unset, data_optimize = vg_unset) {
  vg_mark_(spec, "lineX", formula = formula, ariaDescription = aria_description, ariaHidden = aria_hidden, ariaLabel = aria_label, channels = channels, clip = clip, curve = curve, dx = dx, dy = dy, facet = facet, facetAnchor = facet_anchor, fill = fill, fillOpacity = fill_opacity, filter = filter, fx = fx, fy = fy, href = href, imageFilter = image_filter, margin = margin, marginBottom = margin_bottom, marginLeft = margin_left, marginRight = margin_right, marginTop = margin_top, marker = marker, markerEnd = marker_end, markerMid = marker_mid, markerStart = marker_start, mixBlendMode = mix_blend_mode, opacity = opacity, paintOrder = paint_order, pointerEvents = pointer_events, reverse = reverse, select = select, shapeRendering = shape_rendering, sort = sort, stroke = stroke, strokeDasharray = stroke_dasharray, strokeDashoffset = stroke_dashoffset, strokeLinecap = stroke_linecap, strokeLinejoin = stroke_linejoin, strokeMiterlimit = stroke_miterlimit, strokeOpacity = stroke_opacity, strokeWidth = stroke_width, target = target, tension = tension, tip = tip, title = title, x = x, y = y, z = z, ..., data_from = data_from, filter_by = filter_by, data_optimize = data_optimize)
}

#' The lineY mark.
#'
#' See [vg_value_types] for what the `<...>` notation below (`column`, `param()`, ...) means.
#'
#' @param spec A plot fragment or `vgspec` to add this mark to, or `NULL` to start a new plot with just this mark.
#' @param formula A shorthand for this mark's position channels (`x`, `y`, `fx`, `fy`, and paired `x1`/`x2` or `y1`/`y2`), e.g., `Sepal.Length ~ Sepal.Width | ~ Species` for `y = ~Sepal.Length, x = ~Sepal.Width, fx = ~Species`. See [vg_mark()] for the full grammar.
#' @param aria_description `<string | param()>` The aria-description; a constant textual description.
#' @param aria_hidden `<string | param()>` The aria-hidden state; a constant indicating whether the element is exposed to an accessibility API.
#' @param aria_label `<column | literal | transform() | sql()/agg()>` The aria-label; a channel specifying short textual labels representing the value in the accessibility tree.
#' @param channels `<list>` Additional named channels, for example to include in a tooltip.
#' @param clip `<"frame" | "sphere" | boolean | NULL | param()>` How to clip the mark; one of: - *frame* or true - clip to the plot’s frame (inner area) - *sphere* - clip to the projected sphere (*e.g.*, front hemisphere) - null or false - do not clip The *sphere* clip option requires a geographic projection.
#' @param curve `<"basis" | "basis-closed" | "basis-open" | "bundle" | "bump-x" | "bump-y" | "cardinal" | "cardinal-closed" | "cardinal-open" | "catmull-rom" | ... | param()>` The curve (interpolation) method for connecting adjacent points.
#' @param dx `<number | param()>` The horizontal offset in pixels; a constant option.
#' @param dy `<number | param()>` The vertical offset in pixels; a constant option.
#' @param facet `<"auto" | "include" | "exclude" | "super" | boolean | NULL | param()>` Whether to enable or disable faceting; one of: - *auto* (default) - automatically determine if this mark should be faceted - *include* (or true) - draw the subset of the mark’s data in the current facet - *exclude* - draw the subset of the mark’s data *not* in the current facet - *super* - draw this mark in a single frame that covers all facets - null (or false) - repeat this mark’s data across all facets (*i.e.*, no faceting) When a mark uses *super* faceting, it is not allowed to use position scales (*x*, *y*, *fx*, or *fy*); *super* faceting is intended for decorations, such as labels and legends.
#' @param facet_anchor `<"top" | "right" | "bottom" | "left" | "top-left" | "top-right" | "bottom-left" | "bottom-right" | "top-empty" | "right-empty" | "bottom-empty" | "left-empty" | "empty" | NULL | param()>` How to place the mark with respect to facets; one of: - null (default for most marks) - display the mark in each non-empty facet - *top*, *right*, *bottom*, or *left* - display the mark only in facets on the given side - *top-empty*, *right-empty*, *bottom-empty*, or *left-empty* (default for axis marks) - display the mark only in facets that have empty space on the given side: either the margin, or an empty facet - *empty* - display the mark in empty facets only
#' @param fill `<column | literal | transform() | sql()/agg() | list(value=, ...) | param()>` The fill; a constant CSS color string, or a channel typically bound to the *color* scale.
#' @param fill_opacity `<column | literal | transform() | sql()/agg() | list(value=, ...) | param()>` The fill-opacity; a constant number between 0 and 1, or a channel typically bound to the *opacity* scale.
#' @param filter `<column | literal | transform() | sql()/agg()>` Applies a transform to filter the mark’s index according to the given channel values; only truthy values are retained.
#' @param fx `<column | literal | transform() | sql()/agg()>` The horizontal facet position channel, for mark-level faceting, bound to the *fx* scale.
#' @param fy `<column | literal | transform() | sql()/agg()>` The vertical facet position channel, for mark-level faceting, bound to the *fy* scale.
#' @param href `<column | literal | transform() | sql()/agg()>` The href; a channel specifying URLs for clickable links.
#' @param image_filter `<string | param()>` A CSS filter; a constant string used to adjust the rendering of images, such as *blur(5px)*.
#' @param margin `<number | param()>` Shorthand to set the same default for all four mark margins: **marginTop**, **marginRight**, **marginBottom**, and **marginLeft**; typically defaults to 0, except for axis marks.
#' @param margin_bottom `<number | param()>` The mark’s bottom margin; the minimum distance in pixels between the bottom edges of the inner and outer plot area.
#' @param margin_left `<number | param()>` The mark’s left margin; the minimum distance in pixels between the left edges of the inner and outer plot area.
#' @param margin_right `<number | param()>` The mark’s right margin; the minimum distance in pixels between the right edges of the mark’s inner and outer plot area.
#' @param margin_top `<number | param()>` The mark’s top margin; the minimum distance in pixels between the top edges of the inner and outer plot area.
#' @param marker `<"arrow" | "arrow-reverse" | "dot" | "circle" | "circle-fill" | "circle-stroke" | "tick" | "tick-x" | "tick-y" | "none" | boolean | NULL | param()>` Shorthand to set the same default for markerStart, markerMid, and markerEnd; one of: - a marker name such as *arrow* or *circle* - *none* (default) - no marker * true - alias for *circle-fill* * false or null - alias for *none*
#' @param marker_end `<"arrow" | "arrow-reverse" | "dot" | "circle" | "circle-fill" | "circle-stroke" | "tick" | "tick-x" | "tick-y" | "none" | boolean | NULL | param()>` The marker for the ending point of a line segment; one of: - a marker name such as *arrow* or *circle* * *none* (default) - no marker * true - alias for *circle-fill* * false or null - alias for *none*
#' @param marker_mid `<"arrow" | "arrow-reverse" | "dot" | "circle" | "circle-fill" | "circle-stroke" | "tick" | "tick-x" | "tick-y" | "none" | boolean | NULL | param()>` The marker for any middle (interior) points of a line segment.
#' @param marker_start `<"arrow" | "arrow-reverse" | "dot" | "circle" | "circle-fill" | "circle-stroke" | "tick" | "tick-x" | "tick-y" | "none" | boolean | NULL | param()>` The marker for the starting point of a line segment; one of: - a marker name such as *arrow* or *circle* * *none* (default) - no marker * true - alias for *circle-fill* * false or null - alias for *none*
#' @param mix_blend_mode `<string | param()>` The mix-blend-mode; a constant string specifying how to blend content such as *multiply*.
#' @param opacity `<column | literal | transform() | sql()/agg() | list(value=, ...)>` The opacity; a constant between 0 and 1, or a channel typically bound to the *opacity* scale.
#' @param paint_order `<string | param()>` The paint-order; a constant string specifying the order in which the **fill**, **stroke**, and any markers are drawn; defaults to *normal*, which draws the fill, then stroke, then markers; defaults to *stroke* for the text mark to create a “halo” around text to improve legibility.
#' @param pointer_events `<string | param()>` The pointer-events property; a constant string such as *none*.
#' @param reverse `<boolean | param()>` Applies a transform to reverse the order of the mark’s index, say for reverse input order.
#' @param select `<"first" | "last" | "maxX" | "maxY" | "minX" | "minY" | "nearest" | "nearestX" | "nearestY">` Applies a filter transform after data is loaded to highlight selected values only.
#' @param shape_rendering `<string | param()>` The shape-rendering; a constant string such as *crispEdges*.
#' @param sort `<column | literal | transform() | sql()/agg() | list>` Either applies a transform to sort the mark’s index by the specified channel values, or imputes ordinal scale domains from this mark’s channels.
#' @param stroke `<column | literal | transform() | sql()/agg() | list(value=, ...) | param()>` The stroke; a constant CSS color string, or a channel typically bound to the *color* scale.
#' @param stroke_dasharray `<string | number | param()>` The stroke-dasharray; a constant number indicating the length in pixels of alternating dashes and gaps, or a constant string of numbers separated by spaces or commas (_e.g._, *10 2* for dashes of 10 pixels separated by gaps of 2 pixels), or *none* (the default) for no dashing : https://developer.mozilla.org/en-US/docs/Web/SVG/Attribute/stroke-dasharray
#' @param stroke_dashoffset `<string | number | param()>` The stroke-dashoffset; a constant indicating the offset in pixels of the first dash along the stroke; defaults to zero.
#' @param stroke_linecap `<string | param()>` The stroke-linecap; a constant specifying how to cap stroked paths, such as *butt*, *round*, or *square*.
#' @param stroke_linejoin `<string | param()>` The stroke-linejoin; a constant specifying how to join stroked paths, such as *bevel*, *miter*, *miter-clip*, or *round*.
#' @param stroke_miterlimit `<number | param()>` The stroke-miterlimit; a constant number specifying how to limit the length of *miter* joins on stroked paths.
#' @param stroke_opacity `<column | literal | transform() | sql()/agg() | list(value=, ...)>` The stroke-opacity; a constant between 0 and 1, or a channel typically bound to the *opacity* scale.
#' @param stroke_width `<column | literal | transform() | sql()/agg() | list(value=, ...)>` The stroke-width; a constant number in pixels, or a channel.
#' @param target `<string | param()>` The target; a constant string specifying the target window (_e.g._, *_blank*) for clickable links; used in conjunction with the **href** option.
#' @param tension `<number | param()>` The tension option only has an effect on bundle, cardinal and Catmull–Rom splines (*bundle*, *cardinal*, *cardinal-open*, *cardinal-closed*, *catmull-rom*, *catmull-rom-open*, and *catmull-rom-closed*).
#' @param tip `<boolean | "x" | "y" | "xy" | list | param()>` Whether to generate a tooltip for this mark, and any tip options.
#' @param title `<column | literal | transform() | sql()/agg()>` The title; a channel specifying accessible, short textual descriptions as strings (possibly with newlines).
#' @param x `<column | literal | transform() | sql()/agg() | list(value=, ...)>` The horizontal position (or length) channel, typically bound to the *x* scale.
#' @param y `<column | literal | transform() | sql()/agg() | list(value=, ...)>` The vertical position channel, typically bound to the *y* scale; defaults to the zero-based index of the data 0, 1, 2, ….
#' @param z `<column | literal | transform() | sql()/agg()>` An optional ordinal channel for grouping data into (possibly stacked) series to be drawn as separate areas; defaults to **fill** if a channel, or **stroke** if a channel.
#' @param ... Additional options or plot-level attributes.
#' @param data_from The name of the data source this mark reads from (see [vg_data()]); a length-1 nonzero R integer (`1L`, `-1L`, ...; note the `L`) giving a 1-based index into the spec's registered data sources instead, negative counting from the end (e.g., `-1L` for the most recently registered one); or a literal vector of values to use as inline data directly (mosaic-spec's `"data": [...]` shorthand, e.g., for a single reference line -- a bare double like `0`/`c(0)`, or `0L` itself (never a valid index), means this, not an index). Left unset, defaults to the first registered data source (equivalent to `data_from = 1L`) for any mark type that takes data at all.
#' @param filter_by A Param/Selection (e.g., from [param()]) to filter this mark's data by.
#' @param data_optimize A flag (default `TRUE`) to enable mark-specific query optimizations for this mark's data; set `FALSE` to disable them (mosaic-spec's `data: {optimize: false}`).
#' @family mark functions
#' @export
vg_mark_line_y <- function(spec = NULL, formula = vg_unset, aria_description = vg_unset, aria_hidden = vg_unset, aria_label = vg_unset, channels = vg_unset, clip = vg_unset, curve = vg_unset, dx = vg_unset, dy = vg_unset, facet = vg_unset, facet_anchor = vg_unset, fill = vg_unset, fill_opacity = vg_unset, filter = vg_unset, fx = vg_unset, fy = vg_unset, href = vg_unset, image_filter = vg_unset, margin = vg_unset, margin_bottom = vg_unset, margin_left = vg_unset, margin_right = vg_unset, margin_top = vg_unset, marker = vg_unset, marker_end = vg_unset, marker_mid = vg_unset, marker_start = vg_unset, mix_blend_mode = vg_unset, opacity = vg_unset, paint_order = vg_unset, pointer_events = vg_unset, reverse = vg_unset, select = vg_unset, shape_rendering = vg_unset, sort = vg_unset, stroke = vg_unset, stroke_dasharray = vg_unset, stroke_dashoffset = vg_unset, stroke_linecap = vg_unset, stroke_linejoin = vg_unset, stroke_miterlimit = vg_unset, stroke_opacity = vg_unset, stroke_width = vg_unset, target = vg_unset, tension = vg_unset, tip = vg_unset, title = vg_unset, x = vg_unset, y = vg_unset, z = vg_unset, ..., data_from = vg_unset, filter_by = vg_unset, data_optimize = vg_unset) {
  vg_mark_(spec, "lineY", formula = formula, ariaDescription = aria_description, ariaHidden = aria_hidden, ariaLabel = aria_label, channels = channels, clip = clip, curve = curve, dx = dx, dy = dy, facet = facet, facetAnchor = facet_anchor, fill = fill, fillOpacity = fill_opacity, filter = filter, fx = fx, fy = fy, href = href, imageFilter = image_filter, margin = margin, marginBottom = margin_bottom, marginLeft = margin_left, marginRight = margin_right, marginTop = margin_top, marker = marker, markerEnd = marker_end, markerMid = marker_mid, markerStart = marker_start, mixBlendMode = mix_blend_mode, opacity = opacity, paintOrder = paint_order, pointerEvents = pointer_events, reverse = reverse, select = select, shapeRendering = shape_rendering, sort = sort, stroke = stroke, strokeDasharray = stroke_dasharray, strokeDashoffset = stroke_dashoffset, strokeLinecap = stroke_linecap, strokeLinejoin = stroke_linejoin, strokeMiterlimit = stroke_miterlimit, strokeOpacity = stroke_opacity, strokeWidth = stroke_width, target = target, tension = tension, tip = tip, title = title, x = x, y = y, z = z, ..., data_from = data_from, filter_by = filter_by, data_optimize = data_optimize)
}

#' The link mark.
#'
#' See [vg_value_types] for what the `<...>` notation below (`column`, `param()`, ...) means.
#'
#' @param spec A plot fragment or `vgspec` to add this mark to, or `NULL` to start a new plot with just this mark.
#' @param formula A shorthand for this mark's position channels (`x`, `y`, `fx`, `fy`, and paired `x1`/`x2` or `y1`/`y2`), e.g., `Sepal.Length ~ Sepal.Width | ~ Species` for `y = ~Sepal.Length, x = ~Sepal.Width, fx = ~Species`. See [vg_mark()] for the full grammar.
#' @param aria_description `<string | param()>` The aria-description; a constant textual description.
#' @param aria_hidden `<string | param()>` The aria-hidden state; a constant indicating whether the element is exposed to an accessibility API.
#' @param aria_label `<column | literal | transform() | sql()/agg()>` The aria-label; a channel specifying short textual labels representing the value in the accessibility tree.
#' @param channels `<list>` Additional named channels, for example to include in a tooltip.
#' @param clip `<"frame" | "sphere" | boolean | NULL | param()>` How to clip the mark; one of: - *frame* or true - clip to the plot’s frame (inner area) - *sphere* - clip to the projected sphere (*e.g.*, front hemisphere) - null or false - do not clip The *sphere* clip option requires a geographic projection.
#' @param curve `<"basis" | "basis-closed" | "basis-open" | "bundle" | "bump-x" | "bump-y" | "cardinal" | "cardinal-closed" | "cardinal-open" | "catmull-rom" | ... | param()>` The curve (interpolation) method for connecting adjacent points.
#' @param dx `<number | param()>` The horizontal offset in pixels; a constant option.
#' @param dy `<number | param()>` The vertical offset in pixels; a constant option.
#' @param facet `<"auto" | "include" | "exclude" | "super" | boolean | NULL | param()>` Whether to enable or disable faceting; one of: - *auto* (default) - automatically determine if this mark should be faceted - *include* (or true) - draw the subset of the mark’s data in the current facet - *exclude* - draw the subset of the mark’s data *not* in the current facet - *super* - draw this mark in a single frame that covers all facets - null (or false) - repeat this mark’s data across all facets (*i.e.*, no faceting) When a mark uses *super* faceting, it is not allowed to use position scales (*x*, *y*, *fx*, or *fy*); *super* faceting is intended for decorations, such as labels and legends.
#' @param facet_anchor `<"top" | "right" | "bottom" | "left" | "top-left" | "top-right" | "bottom-left" | "bottom-right" | "top-empty" | "right-empty" | "bottom-empty" | "left-empty" | "empty" | NULL | param()>` How to place the mark with respect to facets; one of: - null (default for most marks) - display the mark in each non-empty facet - *top*, *right*, *bottom*, or *left* - display the mark only in facets on the given side - *top-empty*, *right-empty*, *bottom-empty*, or *left-empty* (default for axis marks) - display the mark only in facets that have empty space on the given side: either the margin, or an empty facet - *empty* - display the mark in empty facets only
#' @param fill `<column | literal | transform() | sql()/agg() | list(value=, ...) | param()>` The fill; a constant CSS color string, or a channel typically bound to the *color* scale.
#' @param fill_opacity `<column | literal | transform() | sql()/agg() | list(value=, ...) | param()>` The fill-opacity; a constant number between 0 and 1, or a channel typically bound to the *opacity* scale.
#' @param filter `<column | literal | transform() | sql()/agg()>` Applies a transform to filter the mark’s index according to the given channel values; only truthy values are retained.
#' @param fx `<column | literal | transform() | sql()/agg()>` The horizontal facet position channel, for mark-level faceting, bound to the *fx* scale.
#' @param fy `<column | literal | transform() | sql()/agg()>` The vertical facet position channel, for mark-level faceting, bound to the *fy* scale.
#' @param href `<column | literal | transform() | sql()/agg()>` The href; a channel specifying URLs for clickable links.
#' @param image_filter `<string | param()>` A CSS filter; a constant string used to adjust the rendering of images, such as *blur(5px)*.
#' @param margin `<number | param()>` Shorthand to set the same default for all four mark margins: **marginTop**, **marginRight**, **marginBottom**, and **marginLeft**; typically defaults to 0, except for axis marks.
#' @param margin_bottom `<number | param()>` The mark’s bottom margin; the minimum distance in pixels between the bottom edges of the inner and outer plot area.
#' @param margin_left `<number | param()>` The mark’s left margin; the minimum distance in pixels between the left edges of the inner and outer plot area.
#' @param margin_right `<number | param()>` The mark’s right margin; the minimum distance in pixels between the right edges of the mark’s inner and outer plot area.
#' @param margin_top `<number | param()>` The mark’s top margin; the minimum distance in pixels between the top edges of the inner and outer plot area.
#' @param marker `<"arrow" | "arrow-reverse" | "dot" | "circle" | "circle-fill" | "circle-stroke" | "tick" | "tick-x" | "tick-y" | "none" | boolean | NULL | param()>` Shorthand to set the same default for markerStart, markerMid, and markerEnd; one of: - a marker name such as *arrow* or *circle* - *none* (default) - no marker * true - alias for *circle-fill* * false or null - alias for *none*
#' @param marker_end `<"arrow" | "arrow-reverse" | "dot" | "circle" | "circle-fill" | "circle-stroke" | "tick" | "tick-x" | "tick-y" | "none" | boolean | NULL | param()>` The marker for the ending point of a line segment; one of: - a marker name such as *arrow* or *circle* * *none* (default) - no marker * true - alias for *circle-fill* * false or null - alias for *none*
#' @param marker_mid `<"arrow" | "arrow-reverse" | "dot" | "circle" | "circle-fill" | "circle-stroke" | "tick" | "tick-x" | "tick-y" | "none" | boolean | NULL | param()>` The marker for any middle (interior) points of a line segment.
#' @param marker_start `<"arrow" | "arrow-reverse" | "dot" | "circle" | "circle-fill" | "circle-stroke" | "tick" | "tick-x" | "tick-y" | "none" | boolean | NULL | param()>` The marker for the starting point of a line segment; one of: - a marker name such as *arrow* or *circle* * *none* (default) - no marker * true - alias for *circle-fill* * false or null - alias for *none*
#' @param mix_blend_mode `<string | param()>` The mix-blend-mode; a constant string specifying how to blend content such as *multiply*.
#' @param opacity `<column | literal | transform() | sql()/agg() | list(value=, ...)>` The opacity; a constant between 0 and 1, or a channel typically bound to the *opacity* scale.
#' @param paint_order `<string | param()>` The paint-order; a constant string specifying the order in which the **fill**, **stroke**, and any markers are drawn; defaults to *normal*, which draws the fill, then stroke, then markers; defaults to *stroke* for the text mark to create a “halo” around text to improve legibility.
#' @param pointer_events `<string | param()>` The pointer-events property; a constant string such as *none*.
#' @param reverse `<boolean | param()>` Applies a transform to reverse the order of the mark’s index, say for reverse input order.
#' @param select `<"first" | "last" | "maxX" | "maxY" | "minX" | "minY" | "nearest" | "nearestX" | "nearestY">` Applies a filter transform after data is loaded to highlight selected values only.
#' @param shape_rendering `<string | param()>` The shape-rendering; a constant string such as *crispEdges*.
#' @param sort `<column | literal | transform() | sql()/agg() | list>` Either applies a transform to sort the mark’s index by the specified channel values, or imputes ordinal scale domains from this mark’s channels.
#' @param stroke `<column | literal | transform() | sql()/agg() | list(value=, ...) | param()>` The stroke; a constant CSS color string, or a channel typically bound to the *color* scale.
#' @param stroke_dasharray `<string | number | param()>` The stroke-dasharray; a constant number indicating the length in pixels of alternating dashes and gaps, or a constant string of numbers separated by spaces or commas (_e.g._, *10 2* for dashes of 10 pixels separated by gaps of 2 pixels), or *none* (the default) for no dashing : https://developer.mozilla.org/en-US/docs/Web/SVG/Attribute/stroke-dasharray
#' @param stroke_dashoffset `<string | number | param()>` The stroke-dashoffset; a constant indicating the offset in pixels of the first dash along the stroke; defaults to zero.
#' @param stroke_linecap `<string | param()>` The stroke-linecap; a constant specifying how to cap stroked paths, such as *butt*, *round*, or *square*.
#' @param stroke_linejoin `<string | param()>` The stroke-linejoin; a constant specifying how to join stroked paths, such as *bevel*, *miter*, *miter-clip*, or *round*.
#' @param stroke_miterlimit `<number | param()>` The stroke-miterlimit; a constant number specifying how to limit the length of *miter* joins on stroked paths.
#' @param stroke_opacity `<column | literal | transform() | sql()/agg() | list(value=, ...)>` The stroke-opacity; a constant between 0 and 1, or a channel typically bound to the *opacity* scale.
#' @param stroke_width `<column | literal | transform() | sql()/agg() | list(value=, ...)>` The stroke-width; a constant number in pixels, or a channel.
#' @param target `<string | param()>` The target; a constant string specifying the target window (_e.g._, *_blank*) for clickable links; used in conjunction with the **href** option.
#' @param tension `<number | param()>` The tension option only has an effect on bundle, cardinal and Catmull–Rom splines (*bundle*, *cardinal*, *cardinal-open*, *cardinal-closed*, *catmull-rom*, *catmull-rom-open*, and *catmull-rom-closed*).
#' @param tip `<boolean | "x" | "y" | "xy" | list | param()>` Whether to generate a tooltip for this mark, and any tip options.
#' @param title `<column | literal | transform() | sql()/agg()>` The title; a channel specifying accessible, short textual descriptions as strings (possibly with newlines).
#' @param x `<column | literal | transform() | sql()/agg() | list(value=, ...)>` The horizontal position (or length) channel, typically bound to the *x* scale.
#' @param x1 `<column | literal | transform() | sql()/agg() | list(value=, ...)>` The required primary (starting, often left) horizontal position channel, representing the area’s baseline, typically bound to the *x* scale.
#' @param x2 `<column | literal | transform() | sql()/agg() | list(value=, ...)>` The optional secondary (ending, often right) horizontal position channel, representing the area’s topline, typically bound to the *x* scale; if not specified, **x1** is used.
#' @param y `<column | literal | transform() | sql()/agg() | list(value=, ...)>` The vertical position channel, typically bound to the *y* scale; defaults to the zero-based index of the data 0, 1, 2, ….
#' @param y1 `<column | literal | transform() | sql()/agg() | list(value=, ...)>` The required primary (starting, often bottom) vertical position channel, representing the area’s baseline, typically bound to the *y* scale.
#' @param y2 `<column | literal | transform() | sql()/agg() | list(value=, ...)>` The optional secondary (ending, often top) vertical position channel, representing the area’s topline, typically bound to the *y* scale; if not specified, **y1** is used.
#' @param ... Additional options or plot-level attributes.
#' @param data_from The name of the data source this mark reads from (see [vg_data()]); a length-1 nonzero R integer (`1L`, `-1L`, ...; note the `L`) giving a 1-based index into the spec's registered data sources instead, negative counting from the end (e.g., `-1L` for the most recently registered one); or a literal vector of values to use as inline data directly (mosaic-spec's `"data": [...]` shorthand, e.g., for a single reference line -- a bare double like `0`/`c(0)`, or `0L` itself (never a valid index), means this, not an index). Left unset, defaults to the first registered data source (equivalent to `data_from = 1L`) for any mark type that takes data at all.
#' @param filter_by A Param/Selection (e.g., from [param()]) to filter this mark's data by.
#' @param data_optimize A flag (default `TRUE`) to enable mark-specific query optimizations for this mark's data; set `FALSE` to disable them (mosaic-spec's `data: {optimize: false}`).
#' @family mark functions
#' @export
vg_mark_link <- function(spec = NULL, formula = vg_unset, aria_description = vg_unset, aria_hidden = vg_unset, aria_label = vg_unset, channels = vg_unset, clip = vg_unset, curve = vg_unset, dx = vg_unset, dy = vg_unset, facet = vg_unset, facet_anchor = vg_unset, fill = vg_unset, fill_opacity = vg_unset, filter = vg_unset, fx = vg_unset, fy = vg_unset, href = vg_unset, image_filter = vg_unset, margin = vg_unset, margin_bottom = vg_unset, margin_left = vg_unset, margin_right = vg_unset, margin_top = vg_unset, marker = vg_unset, marker_end = vg_unset, marker_mid = vg_unset, marker_start = vg_unset, mix_blend_mode = vg_unset, opacity = vg_unset, paint_order = vg_unset, pointer_events = vg_unset, reverse = vg_unset, select = vg_unset, shape_rendering = vg_unset, sort = vg_unset, stroke = vg_unset, stroke_dasharray = vg_unset, stroke_dashoffset = vg_unset, stroke_linecap = vg_unset, stroke_linejoin = vg_unset, stroke_miterlimit = vg_unset, stroke_opacity = vg_unset, stroke_width = vg_unset, target = vg_unset, tension = vg_unset, tip = vg_unset, title = vg_unset, x = vg_unset, x1 = vg_unset, x2 = vg_unset, y = vg_unset, y1 = vg_unset, y2 = vg_unset, ..., data_from = vg_unset, filter_by = vg_unset, data_optimize = vg_unset) {
  vg_mark_(spec, "link", formula = formula, ariaDescription = aria_description, ariaHidden = aria_hidden, ariaLabel = aria_label, channels = channels, clip = clip, curve = curve, dx = dx, dy = dy, facet = facet, facetAnchor = facet_anchor, fill = fill, fillOpacity = fill_opacity, filter = filter, fx = fx, fy = fy, href = href, imageFilter = image_filter, margin = margin, marginBottom = margin_bottom, marginLeft = margin_left, marginRight = margin_right, marginTop = margin_top, marker = marker, markerEnd = marker_end, markerMid = marker_mid, markerStart = marker_start, mixBlendMode = mix_blend_mode, opacity = opacity, paintOrder = paint_order, pointerEvents = pointer_events, reverse = reverse, select = select, shapeRendering = shape_rendering, sort = sort, stroke = stroke, strokeDasharray = stroke_dasharray, strokeDashoffset = stroke_dashoffset, strokeLinecap = stroke_linecap, strokeLinejoin = stroke_linejoin, strokeMiterlimit = stroke_miterlimit, strokeOpacity = stroke_opacity, strokeWidth = stroke_width, target = target, tension = tension, tip = tip, title = title, x = x, x1 = x1, x2 = x2, y = y, y1 = y1, y2 = y2, ..., data_from = data_from, filter_by = filter_by, data_optimize = data_optimize)
}

#' The raster mark.
#'
#' See [vg_value_types] for what the `<...>` notation below (`column`, `param()`, ...) means.
#'
#' @param spec A plot fragment or `vgspec` to add this mark to, or `NULL` to start a new plot with just this mark.
#' @param formula A shorthand for this mark's position channels (`x`, `y`, `fx`, `fy`, and paired `x1`/`x2` or `y1`/`y2`), e.g., `Sepal.Length ~ Sepal.Width | ~ Species` for `y = ~Sepal.Length, x = ~Sepal.Width, fx = ~Species`. See [vg_mark()] for the full grammar.
#' @param aria_description `<string | param()>` The aria-description; a constant textual description.
#' @param aria_hidden `<string | param()>` The aria-hidden state; a constant indicating whether the element is exposed to an accessibility API.
#' @param aria_label `<column | literal | transform() | sql()/agg()>` The aria-label; a channel specifying short textual labels representing the value in the accessibility tree.
#' @param bandwidth `<number | param()>` The kernel density bandwidth for smoothing, in pixels.
#' @param channels `<list>` Additional named channels, for example to include in a tooltip.
#' @param clip `<"frame" | "sphere" | boolean | NULL | param()>` How to clip the mark; one of: - *frame* or true - clip to the plot’s frame (inner area) - *sphere* - clip to the projected sphere (*e.g.*, front hemisphere) - null or false - do not clip The *sphere* clip option requires a geographic projection.
#' @param dx `<number | param()>` The horizontal offset in pixels; a constant option.
#' @param dy `<number | param()>` The vertical offset in pixels; a constant option.
#' @param facet `<"auto" | "include" | "exclude" | "super" | boolean | NULL | param()>` Whether to enable or disable faceting; one of: - *auto* (default) - automatically determine if this mark should be faceted - *include* (or true) - draw the subset of the mark’s data in the current facet - *exclude* - draw the subset of the mark’s data *not* in the current facet - *super* - draw this mark in a single frame that covers all facets - null (or false) - repeat this mark’s data across all facets (*i.e.*, no faceting) When a mark uses *super* faceting, it is not allowed to use position scales (*x*, *y*, *fx*, or *fy*); *super* faceting is intended for decorations, such as labels and legends.
#' @param facet_anchor `<"top" | "right" | "bottom" | "left" | "top-left" | "top-right" | "bottom-left" | "bottom-right" | "top-empty" | "right-empty" | "bottom-empty" | "left-empty" | "empty" | NULL | param()>` How to place the mark with respect to facets; one of: - null (default for most marks) - display the mark in each non-empty facet - *top*, *right*, *bottom*, or *left* - display the mark only in facets on the given side - *top-empty*, *right-empty*, *bottom-empty*, or *left-empty* (default for axis marks) - display the mark only in facets that have empty space on the given side: either the margin, or an empty facet - *empty* - display the mark in empty facets only
#' @param fill `<column | literal | transform() | sql()/agg() | list(value=, ...) | param()>` The fill; a constant CSS color string, or a channel typically bound to the *color* scale.
#' @param fill_opacity `<column | literal | transform() | sql()/agg() | list(value=, ...) | param()>` The fill-opacity; a constant number between 0 and 1, or a channel typically bound to the *opacity* scale.
#' @param filter `<column | literal | transform() | sql()/agg()>` Applies a transform to filter the mark’s index according to the given channel values; only truthy values are retained.
#' @param fx `<column | literal | transform() | sql()/agg()>` The horizontal facet position channel, for mark-level faceting, bound to the *fx* scale.
#' @param fy `<column | literal | transform() | sql()/agg()>` The vertical facet position channel, for mark-level faceting, bound to the *fy* scale.
#' @param height `<number | param()>` The height (number of rows) of the grid, in actual pixels.
#' @param href `<column | literal | transform() | sql()/agg()>` The href; a channel specifying URLs for clickable links.
#' @param image_filter `<string | param()>` A CSS filter; a constant string used to adjust the rendering of images, such as *blur(5px)*.
#' @param image_rendering `<string | param()>` The image-rendering attribute; defaults to *auto* (bilinear).
#' @param interpolate `<"none" | "linear" | "nearest" | "barycentric" | "random-walk" | NULL | param()>` The spatial interpolation method; one of: - *none* - do not perform interpolation (the default), maps samples to single bins - *linear* - apply proportional linear interpolation across adjacent bins - *nearest* - assign each pixel to the closest sample’s value (Voronoi diagram) - *barycentric* - apply barycentric interpolation over the Delaunay triangulation - *random-walk* - apply a random walk from each pixel, stopping when near a sample
#' @param margin `<number | param()>` Shorthand to set the same default for all four mark margins: **marginTop**, **marginRight**, **marginBottom**, and **marginLeft**; typically defaults to 0, except for axis marks.
#' @param margin_bottom `<number | param()>` The mark’s bottom margin; the minimum distance in pixels between the bottom edges of the inner and outer plot area.
#' @param margin_left `<number | param()>` The mark’s left margin; the minimum distance in pixels between the left edges of the inner and outer plot area.
#' @param margin_right `<number | param()>` The mark’s right margin; the minimum distance in pixels between the right edges of the mark’s inner and outer plot area.
#' @param margin_top `<number | param()>` The mark’s top margin; the minimum distance in pixels between the top edges of the inner and outer plot area.
#' @param mix_blend_mode `<string | param()>` The mix-blend-mode; a constant string specifying how to blend content such as *multiply*.
#' @param opacity `<column | literal | transform() | sql()/agg() | list(value=, ...)>` The opacity; a constant between 0 and 1, or a channel typically bound to the *opacity* scale.
#' @param pad `<number | param()>` The bin padding, one of 1 (default) to include extra padding for the final bin, or 0 to make the bins flush with the maximum domain value.
#' @param paint_order `<string | param()>` The paint-order; a constant string specifying the order in which the **fill**, **stroke**, and any markers are drawn; defaults to *normal*, which draws the fill, then stroke, then markers; defaults to *stroke* for the text mark to create a “halo” around text to improve legibility.
#' @param pixel_size `<number | param()>` The effective screen size of a raster pixel, used to determine the height and width of the raster from the frame’s dimensions; defaults to 1.
#' @param pointer_events `<string | param()>` The pointer-events property; a constant string such as *none*.
#' @param reverse `<boolean | param()>` Applies a transform to reverse the order of the mark’s index, say for reverse input order.
#' @param select `<"first" | "last" | "maxX" | "maxY" | "minX" | "minY" | "nearest" | "nearestX" | "nearestY">` Applies a filter transform after data is loaded to highlight selected values only.
#' @param shape_rendering `<string | param()>` The shape-rendering; a constant string such as *crispEdges*.
#' @param sort `<column | literal | transform() | sql()/agg() | list>` Either applies a transform to sort the mark’s index by the specified channel values, or imputes ordinal scale domains from this mark’s channels.
#' @param stroke `<column | literal | transform() | sql()/agg() | list(value=, ...) | param()>` The stroke; a constant CSS color string, or a channel typically bound to the *color* scale.
#' @param stroke_dasharray `<string | number | param()>` The stroke-dasharray; a constant number indicating the length in pixels of alternating dashes and gaps, or a constant string of numbers separated by spaces or commas (_e.g._, *10 2* for dashes of 10 pixels separated by gaps of 2 pixels), or *none* (the default) for no dashing : https://developer.mozilla.org/en-US/docs/Web/SVG/Attribute/stroke-dasharray
#' @param stroke_dashoffset `<string | number | param()>` The stroke-dashoffset; a constant indicating the offset in pixels of the first dash along the stroke; defaults to zero.
#' @param stroke_linecap `<string | param()>` The stroke-linecap; a constant specifying how to cap stroked paths, such as *butt*, *round*, or *square*.
#' @param stroke_linejoin `<string | param()>` The stroke-linejoin; a constant specifying how to join stroked paths, such as *bevel*, *miter*, *miter-clip*, or *round*.
#' @param stroke_miterlimit `<number | param()>` The stroke-miterlimit; a constant number specifying how to limit the length of *miter* joins on stroked paths.
#' @param stroke_opacity `<column | literal | transform() | sql()/agg() | list(value=, ...)>` The stroke-opacity; a constant between 0 and 1, or a channel typically bound to the *opacity* scale.
#' @param stroke_width `<column | literal | transform() | sql()/agg() | list(value=, ...)>` The stroke-width; a constant number in pixels, or a channel.
#' @param target `<string | param()>` The target; a constant string specifying the target window (_e.g._, *_blank*) for clickable links; used in conjunction with the **href** option.
#' @param tip `<boolean | "x" | "y" | "xy" | list | param()>` Whether to generate a tooltip for this mark, and any tip options.
#' @param title `<column | literal | transform() | sql()/agg()>` The title; a channel specifying accessible, short textual descriptions as strings (possibly with newlines).
#' @param width `<number | param()>` The width (number of columns) of the grid, in actual pixels.
#' @param x `<column | literal | transform() | sql()/agg() | list(value=, ...)>` The horizontal position (or length) channel, typically bound to the *x* scale.
#' @param y `<column | literal | transform() | sql()/agg() | list(value=, ...)>` The vertical position channel, typically bound to the *y* scale; defaults to the zero-based index of the data 0, 1, 2, ….
#' @param ... Additional options or plot-level attributes.
#' @param data_from The name of the data source this mark reads from (see [vg_data()]); a length-1 nonzero R integer (`1L`, `-1L`, ...; note the `L`) giving a 1-based index into the spec's registered data sources instead, negative counting from the end (e.g., `-1L` for the most recently registered one); or a literal vector of values to use as inline data directly (mosaic-spec's `"data": [...]` shorthand, e.g., for a single reference line -- a bare double like `0`/`c(0)`, or `0L` itself (never a valid index), means this, not an index). Left unset, defaults to the first registered data source (equivalent to `data_from = 1L`) for any mark type that takes data at all.
#' @param filter_by A Param/Selection (e.g., from [param()]) to filter this mark's data by.
#' @param data_optimize A flag (default `TRUE`) to enable mark-specific query optimizations for this mark's data; set `FALSE` to disable them (mosaic-spec's `data: {optimize: false}`).
#' @family mark functions
#' @export
vg_mark_raster <- function(spec = NULL, formula = vg_unset, aria_description = vg_unset, aria_hidden = vg_unset, aria_label = vg_unset, bandwidth = vg_unset, channels = vg_unset, clip = vg_unset, dx = vg_unset, dy = vg_unset, facet = vg_unset, facet_anchor = vg_unset, fill = vg_unset, fill_opacity = vg_unset, filter = vg_unset, fx = vg_unset, fy = vg_unset, height = vg_unset, href = vg_unset, image_filter = vg_unset, image_rendering = vg_unset, interpolate = vg_unset, margin = vg_unset, margin_bottom = vg_unset, margin_left = vg_unset, margin_right = vg_unset, margin_top = vg_unset, mix_blend_mode = vg_unset, opacity = vg_unset, pad = vg_unset, paint_order = vg_unset, pixel_size = vg_unset, pointer_events = vg_unset, reverse = vg_unset, select = vg_unset, shape_rendering = vg_unset, sort = vg_unset, stroke = vg_unset, stroke_dasharray = vg_unset, stroke_dashoffset = vg_unset, stroke_linecap = vg_unset, stroke_linejoin = vg_unset, stroke_miterlimit = vg_unset, stroke_opacity = vg_unset, stroke_width = vg_unset, target = vg_unset, tip = vg_unset, title = vg_unset, width = vg_unset, x = vg_unset, y = vg_unset, ..., data_from = vg_unset, filter_by = vg_unset, data_optimize = vg_unset) {
  vg_mark_(spec, "raster", formula = formula, ariaDescription = aria_description, ariaHidden = aria_hidden, ariaLabel = aria_label, bandwidth = bandwidth, channels = channels, clip = clip, dx = dx, dy = dy, facet = facet, facetAnchor = facet_anchor, fill = fill, fillOpacity = fill_opacity, filter = filter, fx = fx, fy = fy, height = height, href = href, imageFilter = image_filter, imageRendering = image_rendering, interpolate = interpolate, margin = margin, marginBottom = margin_bottom, marginLeft = margin_left, marginRight = margin_right, marginTop = margin_top, mixBlendMode = mix_blend_mode, opacity = opacity, pad = pad, paintOrder = paint_order, pixelSize = pixel_size, pointerEvents = pointer_events, reverse = reverse, select = select, shapeRendering = shape_rendering, sort = sort, stroke = stroke, strokeDasharray = stroke_dasharray, strokeDashoffset = stroke_dashoffset, strokeLinecap = stroke_linecap, strokeLinejoin = stroke_linejoin, strokeMiterlimit = stroke_miterlimit, strokeOpacity = stroke_opacity, strokeWidth = stroke_width, target = target, tip = tip, title = title, width = width, x = x, y = y, ..., data_from = data_from, filter_by = filter_by, data_optimize = data_optimize)
}

#' The rasterTile mark.
#'
#' See [vg_value_types] for what the `<...>` notation below (`column`, `param()`, ...) means.
#'
#' @param spec A plot fragment or `vgspec` to add this mark to, or `NULL` to start a new plot with just this mark.
#' @param formula A shorthand for this mark's position channels (`x`, `y`, `fx`, `fy`, and paired `x1`/`x2` or `y1`/`y2`), e.g., `Sepal.Length ~ Sepal.Width | ~ Species` for `y = ~Sepal.Length, x = ~Sepal.Width, fx = ~Species`. See [vg_mark()] for the full grammar.
#' @param aria_description `<string | param()>` The aria-description; a constant textual description.
#' @param aria_hidden `<string | param()>` The aria-hidden state; a constant indicating whether the element is exposed to an accessibility API.
#' @param aria_label `<column | literal | transform() | sql()/agg()>` The aria-label; a channel specifying short textual labels representing the value in the accessibility tree.
#' @param bandwidth `<number | param()>` The kernel density bandwidth for smoothing, in pixels.
#' @param channels `<list>` Additional named channels, for example to include in a tooltip.
#' @param clip `<"frame" | "sphere" | boolean | NULL | param()>` How to clip the mark; one of: - *frame* or true - clip to the plot’s frame (inner area) - *sphere* - clip to the projected sphere (*e.g.*, front hemisphere) - null or false - do not clip The *sphere* clip option requires a geographic projection.
#' @param dx `<number | param()>` The horizontal offset in pixels; a constant option.
#' @param dy `<number | param()>` The vertical offset in pixels; a constant option.
#' @param facet `<"auto" | "include" | "exclude" | "super" | boolean | NULL | param()>` Whether to enable or disable faceting; one of: - *auto* (default) - automatically determine if this mark should be faceted - *include* (or true) - draw the subset of the mark’s data in the current facet - *exclude* - draw the subset of the mark’s data *not* in the current facet - *super* - draw this mark in a single frame that covers all facets - null (or false) - repeat this mark’s data across all facets (*i.e.*, no faceting) When a mark uses *super* faceting, it is not allowed to use position scales (*x*, *y*, *fx*, or *fy*); *super* faceting is intended for decorations, such as labels and legends.
#' @param facet_anchor `<"top" | "right" | "bottom" | "left" | "top-left" | "top-right" | "bottom-left" | "bottom-right" | "top-empty" | "right-empty" | "bottom-empty" | "left-empty" | "empty" | NULL | param()>` How to place the mark with respect to facets; one of: - null (default for most marks) - display the mark in each non-empty facet - *top*, *right*, *bottom*, or *left* - display the mark only in facets on the given side - *top-empty*, *right-empty*, *bottom-empty*, or *left-empty* (default for axis marks) - display the mark only in facets that have empty space on the given side: either the margin, or an empty facet - *empty* - display the mark in empty facets only
#' @param fill `<column | literal | transform() | sql()/agg() | list(value=, ...) | param()>` The fill; a constant CSS color string, or a channel typically bound to the *color* scale.
#' @param fill_opacity `<column | literal | transform() | sql()/agg() | list(value=, ...) | param()>` The fill-opacity; a constant number between 0 and 1, or a channel typically bound to the *opacity* scale.
#' @param filter `<column | literal | transform() | sql()/agg()>` Applies a transform to filter the mark’s index according to the given channel values; only truthy values are retained.
#' @param fx `<column | literal | transform() | sql()/agg()>` The horizontal facet position channel, for mark-level faceting, bound to the *fx* scale.
#' @param fy `<column | literal | transform() | sql()/agg()>` The vertical facet position channel, for mark-level faceting, bound to the *fy* scale.
#' @param height `<number | param()>` The height (number of rows) of the grid, in actual pixels.
#' @param href `<column | literal | transform() | sql()/agg()>` The href; a channel specifying URLs for clickable links.
#' @param image_filter `<string | param()>` A CSS filter; a constant string used to adjust the rendering of images, such as *blur(5px)*.
#' @param image_rendering `<string | param()>` The image-rendering attribute; defaults to *auto* (bilinear).
#' @param interpolate `<"none" | "linear" | "nearest" | "barycentric" | "random-walk" | NULL | param()>` The spatial interpolation method; one of: - *none* - do not perform interpolation (the default), maps samples to single bins - *linear* - apply proportional linear interpolation across adjacent bins - *nearest* - assign each pixel to the closest sample’s value (Voronoi diagram) - *barycentric* - apply barycentric interpolation over the Delaunay triangulation - *random-walk* - apply a random walk from each pixel, stopping when near a sample
#' @param margin `<number | param()>` Shorthand to set the same default for all four mark margins: **marginTop**, **marginRight**, **marginBottom**, and **marginLeft**; typically defaults to 0, except for axis marks.
#' @param margin_bottom `<number | param()>` The mark’s bottom margin; the minimum distance in pixels between the bottom edges of the inner and outer plot area.
#' @param margin_left `<number | param()>` The mark’s left margin; the minimum distance in pixels between the left edges of the inner and outer plot area.
#' @param margin_right `<number | param()>` The mark’s right margin; the minimum distance in pixels between the right edges of the mark’s inner and outer plot area.
#' @param margin_top `<number | param()>` The mark’s top margin; the minimum distance in pixels between the top edges of the inner and outer plot area.
#' @param mix_blend_mode `<string | param()>` The mix-blend-mode; a constant string specifying how to blend content such as *multiply*.
#' @param opacity `<column | literal | transform() | sql()/agg() | list(value=, ...)>` The opacity; a constant between 0 and 1, or a channel typically bound to the *opacity* scale.
#' @param origin `<numeric vector | param()>` The coordinates of the tile origin in the **x** and **y** data domains.
#' @param pad `<number | param()>` The bin padding, one of 1 (default) to include extra padding for the final bin, or 0 to make the bins flush with the maximum domain value.
#' @param paint_order `<string | param()>` The paint-order; a constant string specifying the order in which the **fill**, **stroke**, and any markers are drawn; defaults to *normal*, which draws the fill, then stroke, then markers; defaults to *stroke* for the text mark to create a “halo” around text to improve legibility.
#' @param pixel_size `<number | param()>` The effective screen size of a raster pixel, used to determine the height and width of the raster from the frame’s dimensions; defaults to 1.
#' @param pointer_events `<string | param()>` The pointer-events property; a constant string such as *none*.
#' @param reverse `<boolean | param()>` Applies a transform to reverse the order of the mark’s index, say for reverse input order.
#' @param select `<"first" | "last" | "maxX" | "maxY" | "minX" | "minY" | "nearest" | "nearestX" | "nearestY">` Applies a filter transform after data is loaded to highlight selected values only.
#' @param shape_rendering `<string | param()>` The shape-rendering; a constant string such as *crispEdges*.
#' @param sort `<column | literal | transform() | sql()/agg() | list>` Either applies a transform to sort the mark’s index by the specified channel values, or imputes ordinal scale domains from this mark’s channels.
#' @param stroke `<column | literal | transform() | sql()/agg() | list(value=, ...) | param()>` The stroke; a constant CSS color string, or a channel typically bound to the *color* scale.
#' @param stroke_dasharray `<string | number | param()>` The stroke-dasharray; a constant number indicating the length in pixels of alternating dashes and gaps, or a constant string of numbers separated by spaces or commas (_e.g._, *10 2* for dashes of 10 pixels separated by gaps of 2 pixels), or *none* (the default) for no dashing : https://developer.mozilla.org/en-US/docs/Web/SVG/Attribute/stroke-dasharray
#' @param stroke_dashoffset `<string | number | param()>` The stroke-dashoffset; a constant indicating the offset in pixels of the first dash along the stroke; defaults to zero.
#' @param stroke_linecap `<string | param()>` The stroke-linecap; a constant specifying how to cap stroked paths, such as *butt*, *round*, or *square*.
#' @param stroke_linejoin `<string | param()>` The stroke-linejoin; a constant specifying how to join stroked paths, such as *bevel*, *miter*, *miter-clip*, or *round*.
#' @param stroke_miterlimit `<number | param()>` The stroke-miterlimit; a constant number specifying how to limit the length of *miter* joins on stroked paths.
#' @param stroke_opacity `<column | literal | transform() | sql()/agg() | list(value=, ...)>` The stroke-opacity; a constant between 0 and 1, or a channel typically bound to the *opacity* scale.
#' @param stroke_width `<column | literal | transform() | sql()/agg() | list(value=, ...)>` The stroke-width; a constant number in pixels, or a channel.
#' @param target `<string | param()>` The target; a constant string specifying the target window (_e.g._, *_blank*) for clickable links; used in conjunction with the **href** option.
#' @param tip `<boolean | "x" | "y" | "xy" | list | param()>` Whether to generate a tooltip for this mark, and any tip options.
#' @param title `<column | literal | transform() | sql()/agg()>` The title; a channel specifying accessible, short textual descriptions as strings (possibly with newlines).
#' @param width `<number | param()>` The width (number of columns) of the grid, in actual pixels.
#' @param x `<column | literal | transform() | sql()/agg() | list(value=, ...)>` The horizontal position (or length) channel, typically bound to the *x* scale.
#' @param y `<column | literal | transform() | sql()/agg() | list(value=, ...)>` The vertical position channel, typically bound to the *y* scale; defaults to the zero-based index of the data 0, 1, 2, ….
#' @param ... Additional options or plot-level attributes.
#' @param data_from The name of the data source this mark reads from (see [vg_data()]); a length-1 nonzero R integer (`1L`, `-1L`, ...; note the `L`) giving a 1-based index into the spec's registered data sources instead, negative counting from the end (e.g., `-1L` for the most recently registered one); or a literal vector of values to use as inline data directly (mosaic-spec's `"data": [...]` shorthand, e.g., for a single reference line -- a bare double like `0`/`c(0)`, or `0L` itself (never a valid index), means this, not an index). Left unset, defaults to the first registered data source (equivalent to `data_from = 1L`) for any mark type that takes data at all.
#' @param filter_by A Param/Selection (e.g., from [param()]) to filter this mark's data by.
#' @param data_optimize A flag (default `TRUE`) to enable mark-specific query optimizations for this mark's data; set `FALSE` to disable them (mosaic-spec's `data: {optimize: false}`).
#' @family mark functions
#' @export
vg_mark_raster_tile <- function(spec = NULL, formula = vg_unset, aria_description = vg_unset, aria_hidden = vg_unset, aria_label = vg_unset, bandwidth = vg_unset, channels = vg_unset, clip = vg_unset, dx = vg_unset, dy = vg_unset, facet = vg_unset, facet_anchor = vg_unset, fill = vg_unset, fill_opacity = vg_unset, filter = vg_unset, fx = vg_unset, fy = vg_unset, height = vg_unset, href = vg_unset, image_filter = vg_unset, image_rendering = vg_unset, interpolate = vg_unset, margin = vg_unset, margin_bottom = vg_unset, margin_left = vg_unset, margin_right = vg_unset, margin_top = vg_unset, mix_blend_mode = vg_unset, opacity = vg_unset, origin = vg_unset, pad = vg_unset, paint_order = vg_unset, pixel_size = vg_unset, pointer_events = vg_unset, reverse = vg_unset, select = vg_unset, shape_rendering = vg_unset, sort = vg_unset, stroke = vg_unset, stroke_dasharray = vg_unset, stroke_dashoffset = vg_unset, stroke_linecap = vg_unset, stroke_linejoin = vg_unset, stroke_miterlimit = vg_unset, stroke_opacity = vg_unset, stroke_width = vg_unset, target = vg_unset, tip = vg_unset, title = vg_unset, width = vg_unset, x = vg_unset, y = vg_unset, ..., data_from = vg_unset, filter_by = vg_unset, data_optimize = vg_unset) {
  vg_mark_(spec, "rasterTile", formula = formula, ariaDescription = aria_description, ariaHidden = aria_hidden, ariaLabel = aria_label, bandwidth = bandwidth, channels = channels, clip = clip, dx = dx, dy = dy, facet = facet, facetAnchor = facet_anchor, fill = fill, fillOpacity = fill_opacity, filter = filter, fx = fx, fy = fy, height = height, href = href, imageFilter = image_filter, imageRendering = image_rendering, interpolate = interpolate, margin = margin, marginBottom = margin_bottom, marginLeft = margin_left, marginRight = margin_right, marginTop = margin_top, mixBlendMode = mix_blend_mode, opacity = opacity, origin = origin, pad = pad, paintOrder = paint_order, pixelSize = pixel_size, pointerEvents = pointer_events, reverse = reverse, select = select, shapeRendering = shape_rendering, sort = sort, stroke = stroke, strokeDasharray = stroke_dasharray, strokeDashoffset = stroke_dashoffset, strokeLinecap = stroke_linecap, strokeLinejoin = stroke_linejoin, strokeMiterlimit = stroke_miterlimit, strokeOpacity = stroke_opacity, strokeWidth = stroke_width, target = target, tip = tip, title = title, width = width, x = x, y = y, ..., data_from = data_from, filter_by = filter_by, data_optimize = data_optimize)
}

#' The rect mark.
#'
#' See [vg_value_types] for what the `<...>` notation below (`column`, `param()`, ...) means.
#'
#' @param spec A plot fragment or `vgspec` to add this mark to, or `NULL` to start a new plot with just this mark.
#' @param formula A shorthand for this mark's position channels (`x`, `y`, `fx`, `fy`, and paired `x1`/`x2` or `y1`/`y2`), e.g., `Sepal.Length ~ Sepal.Width | ~ Species` for `y = ~Sepal.Length, x = ~Sepal.Width, fx = ~Species`. See [vg_mark()] for the full grammar.
#' @param aria_description `<string | param()>` The aria-description; a constant textual description.
#' @param aria_hidden `<string | param()>` The aria-hidden state; a constant indicating whether the element is exposed to an accessibility API.
#' @param aria_label `<column | literal | transform() | sql()/agg()>` The aria-label; a channel specifying short textual labels representing the value in the accessibility tree.
#' @param channels `<list>` Additional named channels, for example to include in a tooltip.
#' @param clip `<"frame" | "sphere" | boolean | NULL | param()>` How to clip the mark; one of: - *frame* or true - clip to the plot’s frame (inner area) - *sphere* - clip to the projected sphere (*e.g.*, front hemisphere) - null or false - do not clip The *sphere* clip option requires a geographic projection.
#' @param dx `<number | param()>` The horizontal offset in pixels; a constant option.
#' @param dy `<number | param()>` The vertical offset in pixels; a constant option.
#' @param facet `<"auto" | "include" | "exclude" | "super" | boolean | NULL | param()>` Whether to enable or disable faceting; one of: - *auto* (default) - automatically determine if this mark should be faceted - *include* (or true) - draw the subset of the mark’s data in the current facet - *exclude* - draw the subset of the mark’s data *not* in the current facet - *super* - draw this mark in a single frame that covers all facets - null (or false) - repeat this mark’s data across all facets (*i.e.*, no faceting) When a mark uses *super* faceting, it is not allowed to use position scales (*x*, *y*, *fx*, or *fy*); *super* faceting is intended for decorations, such as labels and legends.
#' @param facet_anchor `<"top" | "right" | "bottom" | "left" | "top-left" | "top-right" | "bottom-left" | "bottom-right" | "top-empty" | "right-empty" | "bottom-empty" | "left-empty" | "empty" | NULL | param()>` How to place the mark with respect to facets; one of: - null (default for most marks) - display the mark in each non-empty facet - *top*, *right*, *bottom*, or *left* - display the mark only in facets on the given side - *top-empty*, *right-empty*, *bottom-empty*, or *left-empty* (default for axis marks) - display the mark only in facets that have empty space on the given side: either the margin, or an empty facet - *empty* - display the mark in empty facets only
#' @param fill `<column | literal | transform() | sql()/agg() | list(value=, ...) | param()>` The fill; a constant CSS color string, or a channel typically bound to the *color* scale.
#' @param fill_opacity `<column | literal | transform() | sql()/agg() | list(value=, ...) | param()>` The fill-opacity; a constant number between 0 and 1, or a channel typically bound to the *opacity* scale.
#' @param filter `<column | literal | transform() | sql()/agg()>` Applies a transform to filter the mark’s index according to the given channel values; only truthy values are retained.
#' @param fx `<column | literal | transform() | sql()/agg()>` The horizontal facet position channel, for mark-level faceting, bound to the *fx* scale.
#' @param fy `<column | literal | transform() | sql()/agg()>` The vertical facet position channel, for mark-level faceting, bound to the *fy* scale.
#' @param href `<column | literal | transform() | sql()/agg()>` The href; a channel specifying URLs for clickable links.
#' @param image_filter `<string | param()>` A CSS filter; a constant string used to adjust the rendering of images, such as *blur(5px)*.
#' @param inset `<number | param()>` Shorthand to set the same default for **insetStart** and **insetEnd**.
#' @param inset_bottom `<number | param()>` Insets the bottom edge by the specified number of pixels.
#' @param inset_left `<number | param()>` Insets the left edge by the specified number of pixels.
#' @param inset_right `<number | param()>` Insets the right edge by the specified number of pixels.
#' @param inset_top `<number | param()>` Insets the top edge by the specified number of pixels.
#' @param interval `<"day"/"week"/"month"/... | number | param()>` Enforces uniformity for data at regular intervals, such as integer values or daily samples.
#' @param margin `<number | param()>` Shorthand to set the same default for all four mark margins: **marginTop**, **marginRight**, **marginBottom**, and **marginLeft**; typically defaults to 0, except for axis marks.
#' @param margin_bottom `<number | param()>` The mark’s bottom margin; the minimum distance in pixels between the bottom edges of the inner and outer plot area.
#' @param margin_left `<number | param()>` The mark’s left margin; the minimum distance in pixels between the left edges of the inner and outer plot area.
#' @param margin_right `<number | param()>` The mark’s right margin; the minimum distance in pixels between the right edges of the mark’s inner and outer plot area.
#' @param margin_top `<number | param()>` The mark’s top margin; the minimum distance in pixels between the top edges of the inner and outer plot area.
#' @param mix_blend_mode `<string | param()>` The mix-blend-mode; a constant string specifying how to blend content such as *multiply*.
#' @param offset `<"center" | "normalize" | "wiggle" | NULL | param()>` After stacking, an optional **offset** can be applied to translate and scale stacks, say to produce a streamgraph; defaults to null for a zero baseline (**y** = 0 for stackY, and **x** = 0 for stackX).
#' @param opacity `<column | literal | transform() | sql()/agg() | list(value=, ...)>` The opacity; a constant between 0 and 1, or a channel typically bound to the *opacity* scale.
#' @param order `<"value" | "x" | "y" | "z" | "sum" | "appearance" | "inside-out" | "-value" | "-x" | "-y" | ... | NULL | param()>` The order in which stacks are layered; one of: - null (default) for input order - a named stack order method such as *inside-out* or *sum* - a field name, for natural order of the corresponding values - a function of data, for natural order of the corresponding values - an array of explicit **z** values in the desired order If the *wiggle* **offset** is used, as for a streamgraph, the default changes to *inside-out*.
#' @param paint_order `<string | param()>` The paint-order; a constant string specifying the order in which the **fill**, **stroke**, and any markers are drawn; defaults to *normal*, which draws the fill, then stroke, then markers; defaults to *stroke* for the text mark to create a “halo” around text to improve legibility.
#' @param pointer_events `<string | param()>` The pointer-events property; a constant string such as *none*.
#' @param reverse `<boolean | param()>` Applies a transform to reverse the order of the mark’s index, say for reverse input order.
#' @param rx `<number | string | param()>` The rounded corner *x*-radius, either in pixels or as a percentage of the rect width.
#' @param ry `<number | string | param()>` The rounded corner *y*-radius, either in pixels or as a percentage of the rect height.
#' @param select `<"first" | "last" | "maxX" | "maxY" | "minX" | "minY" | "nearest" | "nearestX" | "nearestY">` Applies a filter transform after data is loaded to highlight selected values only.
#' @param shape_rendering `<string | param()>` The shape-rendering; a constant string such as *crispEdges*.
#' @param sort `<column | literal | transform() | sql()/agg() | list>` Either applies a transform to sort the mark’s index by the specified channel values, or imputes ordinal scale domains from this mark’s channels.
#' @param stroke `<column | literal | transform() | sql()/agg() | list(value=, ...) | param()>` The stroke; a constant CSS color string, or a channel typically bound to the *color* scale.
#' @param stroke_dasharray `<string | number | param()>` The stroke-dasharray; a constant number indicating the length in pixels of alternating dashes and gaps, or a constant string of numbers separated by spaces or commas (_e.g._, *10 2* for dashes of 10 pixels separated by gaps of 2 pixels), or *none* (the default) for no dashing : https://developer.mozilla.org/en-US/docs/Web/SVG/Attribute/stroke-dasharray
#' @param stroke_dashoffset `<string | number | param()>` The stroke-dashoffset; a constant indicating the offset in pixels of the first dash along the stroke; defaults to zero.
#' @param stroke_linecap `<string | param()>` The stroke-linecap; a constant specifying how to cap stroked paths, such as *butt*, *round*, or *square*.
#' @param stroke_linejoin `<string | param()>` The stroke-linejoin; a constant specifying how to join stroked paths, such as *bevel*, *miter*, *miter-clip*, or *round*.
#' @param stroke_miterlimit `<number | param()>` The stroke-miterlimit; a constant number specifying how to limit the length of *miter* joins on stroked paths.
#' @param stroke_opacity `<column | literal | transform() | sql()/agg() | list(value=, ...)>` The stroke-opacity; a constant between 0 and 1, or a channel typically bound to the *opacity* scale.
#' @param stroke_width `<column | literal | transform() | sql()/agg() | list(value=, ...)>` The stroke-width; a constant number in pixels, or a channel.
#' @param target `<string | param()>` The target; a constant string specifying the target window (_e.g._, *_blank*) for clickable links; used in conjunction with the **href** option.
#' @param tip `<boolean | "x" | "y" | "xy" | list | param()>` Whether to generate a tooltip for this mark, and any tip options.
#' @param title `<column | literal | transform() | sql()/agg()>` The title; a channel specifying accessible, short textual descriptions as strings (possibly with newlines).
#' @param x `<column | literal | transform() | sql()/agg() | list(value=, ...)>` The horizontal position (or length) channel, typically bound to the *x* scale.
#' @param x1 `<column | literal | transform() | sql()/agg() | list(value=, ...)>` The required primary (starting, often left) horizontal position channel, representing the area’s baseline, typically bound to the *x* scale.
#' @param x2 `<column | literal | transform() | sql()/agg() | list(value=, ...)>` The optional secondary (ending, often right) horizontal position channel, representing the area’s topline, typically bound to the *x* scale; if not specified, **x1** is used.
#' @param y `<column | literal | transform() | sql()/agg() | list(value=, ...)>` The vertical position channel, typically bound to the *y* scale; defaults to the zero-based index of the data 0, 1, 2, ….
#' @param y1 `<column | literal | transform() | sql()/agg() | list(value=, ...)>` The required primary (starting, often bottom) vertical position channel, representing the area’s baseline, typically bound to the *y* scale.
#' @param y2 `<column | literal | transform() | sql()/agg() | list(value=, ...)>` The optional secondary (ending, often top) vertical position channel, representing the area’s topline, typically bound to the *y* scale; if not specified, **y1** is used.
#' @param z `<column | literal | transform() | sql()/agg()>` An optional ordinal channel for grouping data into (possibly stacked) series to be drawn as separate areas; defaults to **fill** if a channel, or **stroke** if a channel.
#' @param ... Additional options or plot-level attributes.
#' @param data_from The name of the data source this mark reads from (see [vg_data()]); a length-1 nonzero R integer (`1L`, `-1L`, ...; note the `L`) giving a 1-based index into the spec's registered data sources instead, negative counting from the end (e.g., `-1L` for the most recently registered one); or a literal vector of values to use as inline data directly (mosaic-spec's `"data": [...]` shorthand, e.g., for a single reference line -- a bare double like `0`/`c(0)`, or `0L` itself (never a valid index), means this, not an index). Left unset, defaults to the first registered data source (equivalent to `data_from = 1L`) for any mark type that takes data at all.
#' @param filter_by A Param/Selection (e.g., from [param()]) to filter this mark's data by.
#' @param data_optimize A flag (default `TRUE`) to enable mark-specific query optimizations for this mark's data; set `FALSE` to disable them (mosaic-spec's `data: {optimize: false}`).
#' @family mark functions
#' @export
vg_mark_rect <- function(spec = NULL, formula = vg_unset, aria_description = vg_unset, aria_hidden = vg_unset, aria_label = vg_unset, channels = vg_unset, clip = vg_unset, dx = vg_unset, dy = vg_unset, facet = vg_unset, facet_anchor = vg_unset, fill = vg_unset, fill_opacity = vg_unset, filter = vg_unset, fx = vg_unset, fy = vg_unset, href = vg_unset, image_filter = vg_unset, inset = vg_unset, inset_bottom = vg_unset, inset_left = vg_unset, inset_right = vg_unset, inset_top = vg_unset, interval = vg_unset, margin = vg_unset, margin_bottom = vg_unset, margin_left = vg_unset, margin_right = vg_unset, margin_top = vg_unset, mix_blend_mode = vg_unset, offset = vg_unset, opacity = vg_unset, order = vg_unset, paint_order = vg_unset, pointer_events = vg_unset, reverse = vg_unset, rx = vg_unset, ry = vg_unset, select = vg_unset, shape_rendering = vg_unset, sort = vg_unset, stroke = vg_unset, stroke_dasharray = vg_unset, stroke_dashoffset = vg_unset, stroke_linecap = vg_unset, stroke_linejoin = vg_unset, stroke_miterlimit = vg_unset, stroke_opacity = vg_unset, stroke_width = vg_unset, target = vg_unset, tip = vg_unset, title = vg_unset, x = vg_unset, x1 = vg_unset, x2 = vg_unset, y = vg_unset, y1 = vg_unset, y2 = vg_unset, z = vg_unset, ..., data_from = vg_unset, filter_by = vg_unset, data_optimize = vg_unset) {
  vg_mark_(spec, "rect", formula = formula, ariaDescription = aria_description, ariaHidden = aria_hidden, ariaLabel = aria_label, channels = channels, clip = clip, dx = dx, dy = dy, facet = facet, facetAnchor = facet_anchor, fill = fill, fillOpacity = fill_opacity, filter = filter, fx = fx, fy = fy, href = href, imageFilter = image_filter, inset = inset, insetBottom = inset_bottom, insetLeft = inset_left, insetRight = inset_right, insetTop = inset_top, interval = interval, margin = margin, marginBottom = margin_bottom, marginLeft = margin_left, marginRight = margin_right, marginTop = margin_top, mixBlendMode = mix_blend_mode, offset = offset, opacity = opacity, order = order, paintOrder = paint_order, pointerEvents = pointer_events, reverse = reverse, rx = rx, ry = ry, select = select, shapeRendering = shape_rendering, sort = sort, stroke = stroke, strokeDasharray = stroke_dasharray, strokeDashoffset = stroke_dashoffset, strokeLinecap = stroke_linecap, strokeLinejoin = stroke_linejoin, strokeMiterlimit = stroke_miterlimit, strokeOpacity = stroke_opacity, strokeWidth = stroke_width, target = target, tip = tip, title = title, x = x, x1 = x1, x2 = x2, y = y, y1 = y1, y2 = y2, z = z, ..., data_from = data_from, filter_by = filter_by, data_optimize = data_optimize)
}

#' The rectX mark.
#'
#' See [vg_value_types] for what the `<...>` notation below (`column`, `param()`, ...) means.
#'
#' @param spec A plot fragment or `vgspec` to add this mark to, or `NULL` to start a new plot with just this mark.
#' @param formula A shorthand for this mark's position channels (`x`, `y`, `fx`, `fy`, and paired `x1`/`x2` or `y1`/`y2`), e.g., `Sepal.Length ~ Sepal.Width | ~ Species` for `y = ~Sepal.Length, x = ~Sepal.Width, fx = ~Species`. See [vg_mark()] for the full grammar.
#' @param aria_description `<string | param()>` The aria-description; a constant textual description.
#' @param aria_hidden `<string | param()>` The aria-hidden state; a constant indicating whether the element is exposed to an accessibility API.
#' @param aria_label `<column | literal | transform() | sql()/agg()>` The aria-label; a channel specifying short textual labels representing the value in the accessibility tree.
#' @param channels `<list>` Additional named channels, for example to include in a tooltip.
#' @param clip `<"frame" | "sphere" | boolean | NULL | param()>` How to clip the mark; one of: - *frame* or true - clip to the plot’s frame (inner area) - *sphere* - clip to the projected sphere (*e.g.*, front hemisphere) - null or false - do not clip The *sphere* clip option requires a geographic projection.
#' @param dx `<number | param()>` The horizontal offset in pixels; a constant option.
#' @param dy `<number | param()>` The vertical offset in pixels; a constant option.
#' @param facet `<"auto" | "include" | "exclude" | "super" | boolean | NULL | param()>` Whether to enable or disable faceting; one of: - *auto* (default) - automatically determine if this mark should be faceted - *include* (or true) - draw the subset of the mark’s data in the current facet - *exclude* - draw the subset of the mark’s data *not* in the current facet - *super* - draw this mark in a single frame that covers all facets - null (or false) - repeat this mark’s data across all facets (*i.e.*, no faceting) When a mark uses *super* faceting, it is not allowed to use position scales (*x*, *y*, *fx*, or *fy*); *super* faceting is intended for decorations, such as labels and legends.
#' @param facet_anchor `<"top" | "right" | "bottom" | "left" | "top-left" | "top-right" | "bottom-left" | "bottom-right" | "top-empty" | "right-empty" | "bottom-empty" | "left-empty" | "empty" | NULL | param()>` How to place the mark with respect to facets; one of: - null (default for most marks) - display the mark in each non-empty facet - *top*, *right*, *bottom*, or *left* - display the mark only in facets on the given side - *top-empty*, *right-empty*, *bottom-empty*, or *left-empty* (default for axis marks) - display the mark only in facets that have empty space on the given side: either the margin, or an empty facet - *empty* - display the mark in empty facets only
#' @param fill `<column | literal | transform() | sql()/agg() | list(value=, ...) | param()>` The fill; a constant CSS color string, or a channel typically bound to the *color* scale.
#' @param fill_opacity `<column | literal | transform() | sql()/agg() | list(value=, ...) | param()>` The fill-opacity; a constant number between 0 and 1, or a channel typically bound to the *opacity* scale.
#' @param filter `<column | literal | transform() | sql()/agg()>` Applies a transform to filter the mark’s index according to the given channel values; only truthy values are retained.
#' @param fx `<column | literal | transform() | sql()/agg()>` The horizontal facet position channel, for mark-level faceting, bound to the *fx* scale.
#' @param fy `<column | literal | transform() | sql()/agg()>` The vertical facet position channel, for mark-level faceting, bound to the *fy* scale.
#' @param href `<column | literal | transform() | sql()/agg()>` The href; a channel specifying URLs for clickable links.
#' @param image_filter `<string | param()>` A CSS filter; a constant string used to adjust the rendering of images, such as *blur(5px)*.
#' @param inset `<number | param()>` Shorthand to set the same default for **insetStart** and **insetEnd**.
#' @param inset_bottom `<number | param()>` Insets the bottom edge by the specified number of pixels.
#' @param inset_left `<number | param()>` Insets the left edge by the specified number of pixels.
#' @param inset_right `<number | param()>` Insets the right edge by the specified number of pixels.
#' @param inset_top `<number | param()>` Insets the top edge by the specified number of pixels.
#' @param interval `<"day"/"week"/"month"/... | number | param()>` Enforces uniformity for data at regular intervals, such as integer values or daily samples.
#' @param margin `<number | param()>` Shorthand to set the same default for all four mark margins: **marginTop**, **marginRight**, **marginBottom**, and **marginLeft**; typically defaults to 0, except for axis marks.
#' @param margin_bottom `<number | param()>` The mark’s bottom margin; the minimum distance in pixels between the bottom edges of the inner and outer plot area.
#' @param margin_left `<number | param()>` The mark’s left margin; the minimum distance in pixels between the left edges of the inner and outer plot area.
#' @param margin_right `<number | param()>` The mark’s right margin; the minimum distance in pixels between the right edges of the mark’s inner and outer plot area.
#' @param margin_top `<number | param()>` The mark’s top margin; the minimum distance in pixels between the top edges of the inner and outer plot area.
#' @param mix_blend_mode `<string | param()>` The mix-blend-mode; a constant string specifying how to blend content such as *multiply*.
#' @param offset `<"center" | "normalize" | "wiggle" | NULL | param()>` After stacking, an optional **offset** can be applied to translate and scale stacks, say to produce a streamgraph; defaults to null for a zero baseline (**y** = 0 for stackY, and **x** = 0 for stackX).
#' @param opacity `<column | literal | transform() | sql()/agg() | list(value=, ...)>` The opacity; a constant between 0 and 1, or a channel typically bound to the *opacity* scale.
#' @param order `<"value" | "x" | "y" | "z" | "sum" | "appearance" | "inside-out" | "-value" | "-x" | "-y" | ... | NULL | param()>` The order in which stacks are layered; one of: - null (default) for input order - a named stack order method such as *inside-out* or *sum* - a field name, for natural order of the corresponding values - a function of data, for natural order of the corresponding values - an array of explicit **z** values in the desired order If the *wiggle* **offset** is used, as for a streamgraph, the default changes to *inside-out*.
#' @param paint_order `<string | param()>` The paint-order; a constant string specifying the order in which the **fill**, **stroke**, and any markers are drawn; defaults to *normal*, which draws the fill, then stroke, then markers; defaults to *stroke* for the text mark to create a “halo” around text to improve legibility.
#' @param pointer_events `<string | param()>` The pointer-events property; a constant string such as *none*.
#' @param reverse `<boolean | param()>` Applies a transform to reverse the order of the mark’s index, say for reverse input order.
#' @param rx `<number | string | param()>` The rounded corner *x*-radius, either in pixels or as a percentage of the rect width.
#' @param ry `<number | string | param()>` The rounded corner *y*-radius, either in pixels or as a percentage of the rect height.
#' @param select `<"first" | "last" | "maxX" | "maxY" | "minX" | "minY" | "nearest" | "nearestX" | "nearestY">` Applies a filter transform after data is loaded to highlight selected values only.
#' @param shape_rendering `<string | param()>` The shape-rendering; a constant string such as *crispEdges*.
#' @param sort `<column | literal | transform() | sql()/agg() | list>` Either applies a transform to sort the mark’s index by the specified channel values, or imputes ordinal scale domains from this mark’s channels.
#' @param stroke `<column | literal | transform() | sql()/agg() | list(value=, ...) | param()>` The stroke; a constant CSS color string, or a channel typically bound to the *color* scale.
#' @param stroke_dasharray `<string | number | param()>` The stroke-dasharray; a constant number indicating the length in pixels of alternating dashes and gaps, or a constant string of numbers separated by spaces or commas (_e.g._, *10 2* for dashes of 10 pixels separated by gaps of 2 pixels), or *none* (the default) for no dashing : https://developer.mozilla.org/en-US/docs/Web/SVG/Attribute/stroke-dasharray
#' @param stroke_dashoffset `<string | number | param()>` The stroke-dashoffset; a constant indicating the offset in pixels of the first dash along the stroke; defaults to zero.
#' @param stroke_linecap `<string | param()>` The stroke-linecap; a constant specifying how to cap stroked paths, such as *butt*, *round*, or *square*.
#' @param stroke_linejoin `<string | param()>` The stroke-linejoin; a constant specifying how to join stroked paths, such as *bevel*, *miter*, *miter-clip*, or *round*.
#' @param stroke_miterlimit `<number | param()>` The stroke-miterlimit; a constant number specifying how to limit the length of *miter* joins on stroked paths.
#' @param stroke_opacity `<column | literal | transform() | sql()/agg() | list(value=, ...)>` The stroke-opacity; a constant between 0 and 1, or a channel typically bound to the *opacity* scale.
#' @param stroke_width `<column | literal | transform() | sql()/agg() | list(value=, ...)>` The stroke-width; a constant number in pixels, or a channel.
#' @param target `<string | param()>` The target; a constant string specifying the target window (_e.g._, *_blank*) for clickable links; used in conjunction with the **href** option.
#' @param tip `<boolean | "x" | "y" | "xy" | list | param()>` Whether to generate a tooltip for this mark, and any tip options.
#' @param title `<column | literal | transform() | sql()/agg()>` The title; a channel specifying accessible, short textual descriptions as strings (possibly with newlines).
#' @param x `<column | literal | transform() | sql()/agg() | list(value=, ...)>` The horizontal position (or length) channel, typically bound to the *x* scale.
#' @param x1 `<column | literal | transform() | sql()/agg() | list(value=, ...)>` The required primary (starting, often left) horizontal position channel, representing the area’s baseline, typically bound to the *x* scale.
#' @param x2 `<column | literal | transform() | sql()/agg() | list(value=, ...)>` The optional secondary (ending, often right) horizontal position channel, representing the area’s topline, typically bound to the *x* scale; if not specified, **x1** is used.
#' @param y `<column | literal | transform() | sql()/agg() | list(value=, ...)>` The vertical position channel, typically bound to the *y* scale; defaults to the zero-based index of the data 0, 1, 2, ….
#' @param y1 `<column | literal | transform() | sql()/agg() | list(value=, ...)>` The required primary (starting, often bottom) vertical position channel, representing the area’s baseline, typically bound to the *y* scale.
#' @param y2 `<column | literal | transform() | sql()/agg() | list(value=, ...)>` The optional secondary (ending, often top) vertical position channel, representing the area’s topline, typically bound to the *y* scale; if not specified, **y1** is used.
#' @param z `<column | literal | transform() | sql()/agg()>` An optional ordinal channel for grouping data into (possibly stacked) series to be drawn as separate areas; defaults to **fill** if a channel, or **stroke** if a channel.
#' @param ... Additional options or plot-level attributes.
#' @param data_from The name of the data source this mark reads from (see [vg_data()]); a length-1 nonzero R integer (`1L`, `-1L`, ...; note the `L`) giving a 1-based index into the spec's registered data sources instead, negative counting from the end (e.g., `-1L` for the most recently registered one); or a literal vector of values to use as inline data directly (mosaic-spec's `"data": [...]` shorthand, e.g., for a single reference line -- a bare double like `0`/`c(0)`, or `0L` itself (never a valid index), means this, not an index). Left unset, defaults to the first registered data source (equivalent to `data_from = 1L`) for any mark type that takes data at all.
#' @param filter_by A Param/Selection (e.g., from [param()]) to filter this mark's data by.
#' @param data_optimize A flag (default `TRUE`) to enable mark-specific query optimizations for this mark's data; set `FALSE` to disable them (mosaic-spec's `data: {optimize: false}`).
#' @family mark functions
#' @export
vg_mark_rect_x <- function(spec = NULL, formula = vg_unset, aria_description = vg_unset, aria_hidden = vg_unset, aria_label = vg_unset, channels = vg_unset, clip = vg_unset, dx = vg_unset, dy = vg_unset, facet = vg_unset, facet_anchor = vg_unset, fill = vg_unset, fill_opacity = vg_unset, filter = vg_unset, fx = vg_unset, fy = vg_unset, href = vg_unset, image_filter = vg_unset, inset = vg_unset, inset_bottom = vg_unset, inset_left = vg_unset, inset_right = vg_unset, inset_top = vg_unset, interval = vg_unset, margin = vg_unset, margin_bottom = vg_unset, margin_left = vg_unset, margin_right = vg_unset, margin_top = vg_unset, mix_blend_mode = vg_unset, offset = vg_unset, opacity = vg_unset, order = vg_unset, paint_order = vg_unset, pointer_events = vg_unset, reverse = vg_unset, rx = vg_unset, ry = vg_unset, select = vg_unset, shape_rendering = vg_unset, sort = vg_unset, stroke = vg_unset, stroke_dasharray = vg_unset, stroke_dashoffset = vg_unset, stroke_linecap = vg_unset, stroke_linejoin = vg_unset, stroke_miterlimit = vg_unset, stroke_opacity = vg_unset, stroke_width = vg_unset, target = vg_unset, tip = vg_unset, title = vg_unset, x = vg_unset, x1 = vg_unset, x2 = vg_unset, y = vg_unset, y1 = vg_unset, y2 = vg_unset, z = vg_unset, ..., data_from = vg_unset, filter_by = vg_unset, data_optimize = vg_unset) {
  vg_mark_(spec, "rectX", formula = formula, ariaDescription = aria_description, ariaHidden = aria_hidden, ariaLabel = aria_label, channels = channels, clip = clip, dx = dx, dy = dy, facet = facet, facetAnchor = facet_anchor, fill = fill, fillOpacity = fill_opacity, filter = filter, fx = fx, fy = fy, href = href, imageFilter = image_filter, inset = inset, insetBottom = inset_bottom, insetLeft = inset_left, insetRight = inset_right, insetTop = inset_top, interval = interval, margin = margin, marginBottom = margin_bottom, marginLeft = margin_left, marginRight = margin_right, marginTop = margin_top, mixBlendMode = mix_blend_mode, offset = offset, opacity = opacity, order = order, paintOrder = paint_order, pointerEvents = pointer_events, reverse = reverse, rx = rx, ry = ry, select = select, shapeRendering = shape_rendering, sort = sort, stroke = stroke, strokeDasharray = stroke_dasharray, strokeDashoffset = stroke_dashoffset, strokeLinecap = stroke_linecap, strokeLinejoin = stroke_linejoin, strokeMiterlimit = stroke_miterlimit, strokeOpacity = stroke_opacity, strokeWidth = stroke_width, target = target, tip = tip, title = title, x = x, x1 = x1, x2 = x2, y = y, y1 = y1, y2 = y2, z = z, ..., data_from = data_from, filter_by = filter_by, data_optimize = data_optimize)
}

#' The rectY mark.
#'
#' See [vg_value_types] for what the `<...>` notation below (`column`, `param()`, ...) means.
#'
#' @param spec A plot fragment or `vgspec` to add this mark to, or `NULL` to start a new plot with just this mark.
#' @param formula A shorthand for this mark's position channels (`x`, `y`, `fx`, `fy`, and paired `x1`/`x2` or `y1`/`y2`), e.g., `Sepal.Length ~ Sepal.Width | ~ Species` for `y = ~Sepal.Length, x = ~Sepal.Width, fx = ~Species`. See [vg_mark()] for the full grammar.
#' @param aria_description `<string | param()>` The aria-description; a constant textual description.
#' @param aria_hidden `<string | param()>` The aria-hidden state; a constant indicating whether the element is exposed to an accessibility API.
#' @param aria_label `<column | literal | transform() | sql()/agg()>` The aria-label; a channel specifying short textual labels representing the value in the accessibility tree.
#' @param channels `<list>` Additional named channels, for example to include in a tooltip.
#' @param clip `<"frame" | "sphere" | boolean | NULL | param()>` How to clip the mark; one of: - *frame* or true - clip to the plot’s frame (inner area) - *sphere* - clip to the projected sphere (*e.g.*, front hemisphere) - null or false - do not clip The *sphere* clip option requires a geographic projection.
#' @param dx `<number | param()>` The horizontal offset in pixels; a constant option.
#' @param dy `<number | param()>` The vertical offset in pixels; a constant option.
#' @param facet `<"auto" | "include" | "exclude" | "super" | boolean | NULL | param()>` Whether to enable or disable faceting; one of: - *auto* (default) - automatically determine if this mark should be faceted - *include* (or true) - draw the subset of the mark’s data in the current facet - *exclude* - draw the subset of the mark’s data *not* in the current facet - *super* - draw this mark in a single frame that covers all facets - null (or false) - repeat this mark’s data across all facets (*i.e.*, no faceting) When a mark uses *super* faceting, it is not allowed to use position scales (*x*, *y*, *fx*, or *fy*); *super* faceting is intended for decorations, such as labels and legends.
#' @param facet_anchor `<"top" | "right" | "bottom" | "left" | "top-left" | "top-right" | "bottom-left" | "bottom-right" | "top-empty" | "right-empty" | "bottom-empty" | "left-empty" | "empty" | NULL | param()>` How to place the mark with respect to facets; one of: - null (default for most marks) - display the mark in each non-empty facet - *top*, *right*, *bottom*, or *left* - display the mark only in facets on the given side - *top-empty*, *right-empty*, *bottom-empty*, or *left-empty* (default for axis marks) - display the mark only in facets that have empty space on the given side: either the margin, or an empty facet - *empty* - display the mark in empty facets only
#' @param fill `<column | literal | transform() | sql()/agg() | list(value=, ...) | param()>` The fill; a constant CSS color string, or a channel typically bound to the *color* scale.
#' @param fill_opacity `<column | literal | transform() | sql()/agg() | list(value=, ...) | param()>` The fill-opacity; a constant number between 0 and 1, or a channel typically bound to the *opacity* scale.
#' @param filter `<column | literal | transform() | sql()/agg()>` Applies a transform to filter the mark’s index according to the given channel values; only truthy values are retained.
#' @param fx `<column | literal | transform() | sql()/agg()>` The horizontal facet position channel, for mark-level faceting, bound to the *fx* scale.
#' @param fy `<column | literal | transform() | sql()/agg()>` The vertical facet position channel, for mark-level faceting, bound to the *fy* scale.
#' @param href `<column | literal | transform() | sql()/agg()>` The href; a channel specifying URLs for clickable links.
#' @param image_filter `<string | param()>` A CSS filter; a constant string used to adjust the rendering of images, such as *blur(5px)*.
#' @param inset `<number | param()>` Shorthand to set the same default for **insetStart** and **insetEnd**.
#' @param inset_bottom `<number | param()>` Insets the bottom edge by the specified number of pixels.
#' @param inset_left `<number | param()>` Insets the left edge by the specified number of pixels.
#' @param inset_right `<number | param()>` Insets the right edge by the specified number of pixels.
#' @param inset_top `<number | param()>` Insets the top edge by the specified number of pixels.
#' @param interval `<"day"/"week"/"month"/... | number | param()>` Enforces uniformity for data at regular intervals, such as integer values or daily samples.
#' @param margin `<number | param()>` Shorthand to set the same default for all four mark margins: **marginTop**, **marginRight**, **marginBottom**, and **marginLeft**; typically defaults to 0, except for axis marks.
#' @param margin_bottom `<number | param()>` The mark’s bottom margin; the minimum distance in pixels between the bottom edges of the inner and outer plot area.
#' @param margin_left `<number | param()>` The mark’s left margin; the minimum distance in pixels between the left edges of the inner and outer plot area.
#' @param margin_right `<number | param()>` The mark’s right margin; the minimum distance in pixels between the right edges of the mark’s inner and outer plot area.
#' @param margin_top `<number | param()>` The mark’s top margin; the minimum distance in pixels between the top edges of the inner and outer plot area.
#' @param mix_blend_mode `<string | param()>` The mix-blend-mode; a constant string specifying how to blend content such as *multiply*.
#' @param offset `<"center" | "normalize" | "wiggle" | NULL | param()>` After stacking, an optional **offset** can be applied to translate and scale stacks, say to produce a streamgraph; defaults to null for a zero baseline (**y** = 0 for stackY, and **x** = 0 for stackX).
#' @param opacity `<column | literal | transform() | sql()/agg() | list(value=, ...)>` The opacity; a constant between 0 and 1, or a channel typically bound to the *opacity* scale.
#' @param order `<"value" | "x" | "y" | "z" | "sum" | "appearance" | "inside-out" | "-value" | "-x" | "-y" | ... | NULL | param()>` The order in which stacks are layered; one of: - null (default) for input order - a named stack order method such as *inside-out* or *sum* - a field name, for natural order of the corresponding values - a function of data, for natural order of the corresponding values - an array of explicit **z** values in the desired order If the *wiggle* **offset** is used, as for a streamgraph, the default changes to *inside-out*.
#' @param paint_order `<string | param()>` The paint-order; a constant string specifying the order in which the **fill**, **stroke**, and any markers are drawn; defaults to *normal*, which draws the fill, then stroke, then markers; defaults to *stroke* for the text mark to create a “halo” around text to improve legibility.
#' @param pointer_events `<string | param()>` The pointer-events property; a constant string such as *none*.
#' @param reverse `<boolean | param()>` Applies a transform to reverse the order of the mark’s index, say for reverse input order.
#' @param rx `<number | string | param()>` The rounded corner *x*-radius, either in pixels or as a percentage of the rect width.
#' @param ry `<number | string | param()>` The rounded corner *y*-radius, either in pixels or as a percentage of the rect height.
#' @param select `<"first" | "last" | "maxX" | "maxY" | "minX" | "minY" | "nearest" | "nearestX" | "nearestY">` Applies a filter transform after data is loaded to highlight selected values only.
#' @param shape_rendering `<string | param()>` The shape-rendering; a constant string such as *crispEdges*.
#' @param sort `<column | literal | transform() | sql()/agg() | list>` Either applies a transform to sort the mark’s index by the specified channel values, or imputes ordinal scale domains from this mark’s channels.
#' @param stroke `<column | literal | transform() | sql()/agg() | list(value=, ...) | param()>` The stroke; a constant CSS color string, or a channel typically bound to the *color* scale.
#' @param stroke_dasharray `<string | number | param()>` The stroke-dasharray; a constant number indicating the length in pixels of alternating dashes and gaps, or a constant string of numbers separated by spaces or commas (_e.g._, *10 2* for dashes of 10 pixels separated by gaps of 2 pixels), or *none* (the default) for no dashing : https://developer.mozilla.org/en-US/docs/Web/SVG/Attribute/stroke-dasharray
#' @param stroke_dashoffset `<string | number | param()>` The stroke-dashoffset; a constant indicating the offset in pixels of the first dash along the stroke; defaults to zero.
#' @param stroke_linecap `<string | param()>` The stroke-linecap; a constant specifying how to cap stroked paths, such as *butt*, *round*, or *square*.
#' @param stroke_linejoin `<string | param()>` The stroke-linejoin; a constant specifying how to join stroked paths, such as *bevel*, *miter*, *miter-clip*, or *round*.
#' @param stroke_miterlimit `<number | param()>` The stroke-miterlimit; a constant number specifying how to limit the length of *miter* joins on stroked paths.
#' @param stroke_opacity `<column | literal | transform() | sql()/agg() | list(value=, ...)>` The stroke-opacity; a constant between 0 and 1, or a channel typically bound to the *opacity* scale.
#' @param stroke_width `<column | literal | transform() | sql()/agg() | list(value=, ...)>` The stroke-width; a constant number in pixels, or a channel.
#' @param target `<string | param()>` The target; a constant string specifying the target window (_e.g._, *_blank*) for clickable links; used in conjunction with the **href** option.
#' @param tip `<boolean | "x" | "y" | "xy" | list | param()>` Whether to generate a tooltip for this mark, and any tip options.
#' @param title `<column | literal | transform() | sql()/agg()>` The title; a channel specifying accessible, short textual descriptions as strings (possibly with newlines).
#' @param x `<column | literal | transform() | sql()/agg() | list(value=, ...)>` The horizontal position (or length) channel, typically bound to the *x* scale.
#' @param x1 `<column | literal | transform() | sql()/agg() | list(value=, ...)>` The required primary (starting, often left) horizontal position channel, representing the area’s baseline, typically bound to the *x* scale.
#' @param x2 `<column | literal | transform() | sql()/agg() | list(value=, ...)>` The optional secondary (ending, often right) horizontal position channel, representing the area’s topline, typically bound to the *x* scale; if not specified, **x1** is used.
#' @param y `<column | literal | transform() | sql()/agg() | list(value=, ...)>` The vertical position channel, typically bound to the *y* scale; defaults to the zero-based index of the data 0, 1, 2, ….
#' @param y1 `<column | literal | transform() | sql()/agg() | list(value=, ...)>` The required primary (starting, often bottom) vertical position channel, representing the area’s baseline, typically bound to the *y* scale.
#' @param y2 `<column | literal | transform() | sql()/agg() | list(value=, ...)>` The optional secondary (ending, often top) vertical position channel, representing the area’s topline, typically bound to the *y* scale; if not specified, **y1** is used.
#' @param z `<column | literal | transform() | sql()/agg()>` An optional ordinal channel for grouping data into (possibly stacked) series to be drawn as separate areas; defaults to **fill** if a channel, or **stroke** if a channel.
#' @param ... Additional options or plot-level attributes.
#' @param data_from The name of the data source this mark reads from (see [vg_data()]); a length-1 nonzero R integer (`1L`, `-1L`, ...; note the `L`) giving a 1-based index into the spec's registered data sources instead, negative counting from the end (e.g., `-1L` for the most recently registered one); or a literal vector of values to use as inline data directly (mosaic-spec's `"data": [...]` shorthand, e.g., for a single reference line -- a bare double like `0`/`c(0)`, or `0L` itself (never a valid index), means this, not an index). Left unset, defaults to the first registered data source (equivalent to `data_from = 1L`) for any mark type that takes data at all.
#' @param filter_by A Param/Selection (e.g., from [param()]) to filter this mark's data by.
#' @param data_optimize A flag (default `TRUE`) to enable mark-specific query optimizations for this mark's data; set `FALSE` to disable them (mosaic-spec's `data: {optimize: false}`).
#' @family mark functions
#' @export
vg_mark_rect_y <- function(spec = NULL, formula = vg_unset, aria_description = vg_unset, aria_hidden = vg_unset, aria_label = vg_unset, channels = vg_unset, clip = vg_unset, dx = vg_unset, dy = vg_unset, facet = vg_unset, facet_anchor = vg_unset, fill = vg_unset, fill_opacity = vg_unset, filter = vg_unset, fx = vg_unset, fy = vg_unset, href = vg_unset, image_filter = vg_unset, inset = vg_unset, inset_bottom = vg_unset, inset_left = vg_unset, inset_right = vg_unset, inset_top = vg_unset, interval = vg_unset, margin = vg_unset, margin_bottom = vg_unset, margin_left = vg_unset, margin_right = vg_unset, margin_top = vg_unset, mix_blend_mode = vg_unset, offset = vg_unset, opacity = vg_unset, order = vg_unset, paint_order = vg_unset, pointer_events = vg_unset, reverse = vg_unset, rx = vg_unset, ry = vg_unset, select = vg_unset, shape_rendering = vg_unset, sort = vg_unset, stroke = vg_unset, stroke_dasharray = vg_unset, stroke_dashoffset = vg_unset, stroke_linecap = vg_unset, stroke_linejoin = vg_unset, stroke_miterlimit = vg_unset, stroke_opacity = vg_unset, stroke_width = vg_unset, target = vg_unset, tip = vg_unset, title = vg_unset, x = vg_unset, x1 = vg_unset, x2 = vg_unset, y = vg_unset, y1 = vg_unset, y2 = vg_unset, z = vg_unset, ..., data_from = vg_unset, filter_by = vg_unset, data_optimize = vg_unset) {
  vg_mark_(spec, "rectY", formula = formula, ariaDescription = aria_description, ariaHidden = aria_hidden, ariaLabel = aria_label, channels = channels, clip = clip, dx = dx, dy = dy, facet = facet, facetAnchor = facet_anchor, fill = fill, fillOpacity = fill_opacity, filter = filter, fx = fx, fy = fy, href = href, imageFilter = image_filter, inset = inset, insetBottom = inset_bottom, insetLeft = inset_left, insetRight = inset_right, insetTop = inset_top, interval = interval, margin = margin, marginBottom = margin_bottom, marginLeft = margin_left, marginRight = margin_right, marginTop = margin_top, mixBlendMode = mix_blend_mode, offset = offset, opacity = opacity, order = order, paintOrder = paint_order, pointerEvents = pointer_events, reverse = reverse, rx = rx, ry = ry, select = select, shapeRendering = shape_rendering, sort = sort, stroke = stroke, strokeDasharray = stroke_dasharray, strokeDashoffset = stroke_dashoffset, strokeLinecap = stroke_linecap, strokeLinejoin = stroke_linejoin, strokeMiterlimit = stroke_miterlimit, strokeOpacity = stroke_opacity, strokeWidth = stroke_width, target = target, tip = tip, title = title, x = x, x1 = x1, x2 = x2, y = y, y1 = y1, y2 = y2, z = z, ..., data_from = data_from, filter_by = filter_by, data_optimize = data_optimize)
}

#' The regressionY mark.
#'
#' See [vg_value_types] for what the `<...>` notation below (`column`, `param()`, ...) means.
#'
#' @param spec A plot fragment or `vgspec` to add this mark to, or `NULL` to start a new plot with just this mark.
#' @param formula A shorthand for this mark's position channels (`x`, `y`, `fx`, `fy`, and paired `x1`/`x2` or `y1`/`y2`), e.g., `Sepal.Length ~ Sepal.Width | ~ Species` for `y = ~Sepal.Length, x = ~Sepal.Width, fx = ~Species`. See [vg_mark()] for the full grammar.
#' @param aria_description `<string | param()>` The aria-description; a constant textual description.
#' @param aria_hidden `<string | param()>` The aria-hidden state; a constant indicating whether the element is exposed to an accessibility API.
#' @param aria_label `<column | literal | transform() | sql()/agg()>` The aria-label; a channel specifying short textual labels representing the value in the accessibility tree.
#' @param channels `<list>` Additional named channels, for example to include in a tooltip.
#' @param ci `<number | param()>` The confidence interval in (0, 1); defaults to 0.95.
#' @param clip `<"frame" | "sphere" | boolean | NULL | param()>` How to clip the mark; one of: - *frame* or true - clip to the plot’s frame (inner area) - *sphere* - clip to the projected sphere (*e.g.*, front hemisphere) - null or false - do not clip The *sphere* clip option requires a geographic projection.
#' @param dx `<number | param()>` The horizontal offset in pixels; a constant option.
#' @param dy `<number | param()>` The vertical offset in pixels; a constant option.
#' @param facet `<"auto" | "include" | "exclude" | "super" | boolean | NULL | param()>` Whether to enable or disable faceting; one of: - *auto* (default) - automatically determine if this mark should be faceted - *include* (or true) - draw the subset of the mark’s data in the current facet - *exclude* - draw the subset of the mark’s data *not* in the current facet - *super* - draw this mark in a single frame that covers all facets - null (or false) - repeat this mark’s data across all facets (*i.e.*, no faceting) When a mark uses *super* faceting, it is not allowed to use position scales (*x*, *y*, *fx*, or *fy*); *super* faceting is intended for decorations, such as labels and legends.
#' @param facet_anchor `<"top" | "right" | "bottom" | "left" | "top-left" | "top-right" | "bottom-left" | "bottom-right" | "top-empty" | "right-empty" | "bottom-empty" | "left-empty" | "empty" | NULL | param()>` How to place the mark with respect to facets; one of: - null (default for most marks) - display the mark in each non-empty facet - *top*, *right*, *bottom*, or *left* - display the mark only in facets on the given side - *top-empty*, *right-empty*, *bottom-empty*, or *left-empty* (default for axis marks) - display the mark only in facets that have empty space on the given side: either the margin, or an empty facet - *empty* - display the mark in empty facets only
#' @param fill `<column | literal | transform() | sql()/agg() | list(value=, ...) | param()>` The fill; a constant CSS color string, or a channel typically bound to the *color* scale.
#' @param fill_opacity `<column | literal | transform() | sql()/agg() | list(value=, ...) | param()>` The fill-opacity; a constant number between 0 and 1, or a channel typically bound to the *opacity* scale.
#' @param filter `<column | literal | transform() | sql()/agg()>` Applies a transform to filter the mark’s index according to the given channel values; only truthy values are retained.
#' @param fx `<column | literal | transform() | sql()/agg()>` The horizontal facet position channel, for mark-level faceting, bound to the *fx* scale.
#' @param fy `<column | literal | transform() | sql()/agg()>` The vertical facet position channel, for mark-level faceting, bound to the *fy* scale.
#' @param href `<column | literal | transform() | sql()/agg()>` The href; a channel specifying URLs for clickable links.
#' @param image_filter `<string | param()>` A CSS filter; a constant string used to adjust the rendering of images, such as *blur(5px)*.
#' @param margin `<number | param()>` Shorthand to set the same default for all four mark margins: **marginTop**, **marginRight**, **marginBottom**, and **marginLeft**; typically defaults to 0, except for axis marks.
#' @param margin_bottom `<number | param()>` The mark’s bottom margin; the minimum distance in pixels between the bottom edges of the inner and outer plot area.
#' @param margin_left `<number | param()>` The mark’s left margin; the minimum distance in pixels between the left edges of the inner and outer plot area.
#' @param margin_right `<number | param()>` The mark’s right margin; the minimum distance in pixels between the right edges of the mark’s inner and outer plot area.
#' @param margin_top `<number | param()>` The mark’s top margin; the minimum distance in pixels between the top edges of the inner and outer plot area.
#' @param mix_blend_mode `<string | param()>` The mix-blend-mode; a constant string specifying how to blend content such as *multiply*.
#' @param opacity `<column | literal | transform() | sql()/agg() | list(value=, ...)>` The opacity; a constant between 0 and 1, or a channel typically bound to the *opacity* scale.
#' @param paint_order `<string | param()>` The paint-order; a constant string specifying the order in which the **fill**, **stroke**, and any markers are drawn; defaults to *normal*, which draws the fill, then stroke, then markers; defaults to *stroke* for the text mark to create a “halo” around text to improve legibility.
#' @param pointer_events `<string | param()>` The pointer-events property; a constant string such as *none*.
#' @param precision `<number | param()>` The distance in pixels between samples of the confidence band; defaults to 4.
#' @param reverse `<boolean | param()>` Applies a transform to reverse the order of the mark’s index, say for reverse input order.
#' @param select `<"first" | "last" | "maxX" | "maxY" | "minX" | "minY" | "nearest" | "nearestX" | "nearestY">` Applies a filter transform after data is loaded to highlight selected values only.
#' @param shape_rendering `<string | param()>` The shape-rendering; a constant string such as *crispEdges*.
#' @param sort `<column | literal | transform() | sql()/agg() | list>` Either applies a transform to sort the mark’s index by the specified channel values, or imputes ordinal scale domains from this mark’s channels.
#' @param stroke `<column | literal | transform() | sql()/agg() | list(value=, ...) | param()>` The stroke; a constant CSS color string, or a channel typically bound to the *color* scale.
#' @param stroke_dasharray `<string | number | param()>` The stroke-dasharray; a constant number indicating the length in pixels of alternating dashes and gaps, or a constant string of numbers separated by spaces or commas (_e.g._, *10 2* for dashes of 10 pixels separated by gaps of 2 pixels), or *none* (the default) for no dashing : https://developer.mozilla.org/en-US/docs/Web/SVG/Attribute/stroke-dasharray
#' @param stroke_dashoffset `<string | number | param()>` The stroke-dashoffset; a constant indicating the offset in pixels of the first dash along the stroke; defaults to zero.
#' @param stroke_linecap `<string | param()>` The stroke-linecap; a constant specifying how to cap stroked paths, such as *butt*, *round*, or *square*.
#' @param stroke_linejoin `<string | param()>` The stroke-linejoin; a constant specifying how to join stroked paths, such as *bevel*, *miter*, *miter-clip*, or *round*.
#' @param stroke_miterlimit `<number | param()>` The stroke-miterlimit; a constant number specifying how to limit the length of *miter* joins on stroked paths.
#' @param stroke_opacity `<column | literal | transform() | sql()/agg() | list(value=, ...)>` The stroke-opacity; a constant between 0 and 1, or a channel typically bound to the *opacity* scale.
#' @param stroke_width `<column | literal | transform() | sql()/agg() | list(value=, ...)>` The stroke-width; a constant number in pixels, or a channel.
#' @param target `<string | param()>` The target; a constant string specifying the target window (_e.g._, *_blank*) for clickable links; used in conjunction with the **href** option.
#' @param tip `<boolean | "x" | "y" | "xy" | list | param()>` Whether to generate a tooltip for this mark, and any tip options.
#' @param title `<column | literal | transform() | sql()/agg()>` The title; a channel specifying accessible, short textual descriptions as strings (possibly with newlines).
#' @param x `<column | literal | transform() | sql()/agg() | list(value=, ...)>` The horizontal position (or length) channel, typically bound to the *x* scale.
#' @param y `<column | literal | transform() | sql()/agg() | list(value=, ...)>` The vertical position channel, typically bound to the *y* scale; defaults to the zero-based index of the data 0, 1, 2, ….
#' @param z `<column | literal | transform() | sql()/agg()>` An optional ordinal channel for grouping data into (possibly stacked) series to be drawn as separate areas; defaults to **fill** if a channel, or **stroke** if a channel.
#' @param ... Additional options or plot-level attributes.
#' @param data_from The name of the data source this mark reads from (see [vg_data()]); a length-1 nonzero R integer (`1L`, `-1L`, ...; note the `L`) giving a 1-based index into the spec's registered data sources instead, negative counting from the end (e.g., `-1L` for the most recently registered one); or a literal vector of values to use as inline data directly (mosaic-spec's `"data": [...]` shorthand, e.g., for a single reference line -- a bare double like `0`/`c(0)`, or `0L` itself (never a valid index), means this, not an index). Left unset, defaults to the first registered data source (equivalent to `data_from = 1L`) for any mark type that takes data at all.
#' @param filter_by A Param/Selection (e.g., from [param()]) to filter this mark's data by.
#' @param data_optimize A flag (default `TRUE`) to enable mark-specific query optimizations for this mark's data; set `FALSE` to disable them (mosaic-spec's `data: {optimize: false}`).
#' @family mark functions
#' @export
vg_mark_regression_y <- function(spec = NULL, formula = vg_unset, aria_description = vg_unset, aria_hidden = vg_unset, aria_label = vg_unset, channels = vg_unset, ci = vg_unset, clip = vg_unset, dx = vg_unset, dy = vg_unset, facet = vg_unset, facet_anchor = vg_unset, fill = vg_unset, fill_opacity = vg_unset, filter = vg_unset, fx = vg_unset, fy = vg_unset, href = vg_unset, image_filter = vg_unset, margin = vg_unset, margin_bottom = vg_unset, margin_left = vg_unset, margin_right = vg_unset, margin_top = vg_unset, mix_blend_mode = vg_unset, opacity = vg_unset, paint_order = vg_unset, pointer_events = vg_unset, precision = vg_unset, reverse = vg_unset, select = vg_unset, shape_rendering = vg_unset, sort = vg_unset, stroke = vg_unset, stroke_dasharray = vg_unset, stroke_dashoffset = vg_unset, stroke_linecap = vg_unset, stroke_linejoin = vg_unset, stroke_miterlimit = vg_unset, stroke_opacity = vg_unset, stroke_width = vg_unset, target = vg_unset, tip = vg_unset, title = vg_unset, x = vg_unset, y = vg_unset, z = vg_unset, ..., data_from = vg_unset, filter_by = vg_unset, data_optimize = vg_unset) {
  vg_mark_(spec, "regressionY", formula = formula, ariaDescription = aria_description, ariaHidden = aria_hidden, ariaLabel = aria_label, channels = channels, ci = ci, clip = clip, dx = dx, dy = dy, facet = facet, facetAnchor = facet_anchor, fill = fill, fillOpacity = fill_opacity, filter = filter, fx = fx, fy = fy, href = href, imageFilter = image_filter, margin = margin, marginBottom = margin_bottom, marginLeft = margin_left, marginRight = margin_right, marginTop = margin_top, mixBlendMode = mix_blend_mode, opacity = opacity, paintOrder = paint_order, pointerEvents = pointer_events, precision = precision, reverse = reverse, select = select, shapeRendering = shape_rendering, sort = sort, stroke = stroke, strokeDasharray = stroke_dasharray, strokeDashoffset = stroke_dashoffset, strokeLinecap = stroke_linecap, strokeLinejoin = stroke_linejoin, strokeMiterlimit = stroke_miterlimit, strokeOpacity = stroke_opacity, strokeWidth = stroke_width, target = target, tip = tip, title = title, x = x, y = y, z = z, ..., data_from = data_from, filter_by = filter_by, data_optimize = data_optimize)
}

#' The ruleX mark.
#'
#' See [vg_value_types] for what the `<...>` notation below (`column`, `param()`, ...) means.
#'
#' @param spec A plot fragment or `vgspec` to add this mark to, or `NULL` to start a new plot with just this mark.
#' @param formula A shorthand for this mark's position channels (`x`, `y`, `fx`, `fy`, and paired `x1`/`x2` or `y1`/`y2`), e.g., `Sepal.Length ~ Sepal.Width | ~ Species` for `y = ~Sepal.Length, x = ~Sepal.Width, fx = ~Species`. See [vg_mark()] for the full grammar.
#' @param aria_description `<string | param()>` The aria-description; a constant textual description.
#' @param aria_hidden `<string | param()>` The aria-hidden state; a constant indicating whether the element is exposed to an accessibility API.
#' @param aria_label `<column | literal | transform() | sql()/agg()>` The aria-label; a channel specifying short textual labels representing the value in the accessibility tree.
#' @param channels `<list>` Additional named channels, for example to include in a tooltip.
#' @param clip `<"frame" | "sphere" | boolean | NULL | param()>` How to clip the mark; one of: - *frame* or true - clip to the plot’s frame (inner area) - *sphere* - clip to the projected sphere (*e.g.*, front hemisphere) - null or false - do not clip The *sphere* clip option requires a geographic projection.
#' @param dx `<number | param()>` The horizontal offset in pixels; a constant option.
#' @param dy `<number | param()>` The vertical offset in pixels; a constant option.
#' @param facet `<"auto" | "include" | "exclude" | "super" | boolean | NULL | param()>` Whether to enable or disable faceting; one of: - *auto* (default) - automatically determine if this mark should be faceted - *include* (or true) - draw the subset of the mark’s data in the current facet - *exclude* - draw the subset of the mark’s data *not* in the current facet - *super* - draw this mark in a single frame that covers all facets - null (or false) - repeat this mark’s data across all facets (*i.e.*, no faceting) When a mark uses *super* faceting, it is not allowed to use position scales (*x*, *y*, *fx*, or *fy*); *super* faceting is intended for decorations, such as labels and legends.
#' @param facet_anchor `<"top" | "right" | "bottom" | "left" | "top-left" | "top-right" | "bottom-left" | "bottom-right" | "top-empty" | "right-empty" | "bottom-empty" | "left-empty" | "empty" | NULL | param()>` How to place the mark with respect to facets; one of: - null (default for most marks) - display the mark in each non-empty facet - *top*, *right*, *bottom*, or *left* - display the mark only in facets on the given side - *top-empty*, *right-empty*, *bottom-empty*, or *left-empty* (default for axis marks) - display the mark only in facets that have empty space on the given side: either the margin, or an empty facet - *empty* - display the mark in empty facets only
#' @param fill `<column | literal | transform() | sql()/agg() | list(value=, ...) | param()>` The fill; a constant CSS color string, or a channel typically bound to the *color* scale.
#' @param fill_opacity `<column | literal | transform() | sql()/agg() | list(value=, ...) | param()>` The fill-opacity; a constant number between 0 and 1, or a channel typically bound to the *opacity* scale.
#' @param filter `<column | literal | transform() | sql()/agg()>` Applies a transform to filter the mark’s index according to the given channel values; only truthy values are retained.
#' @param fx `<column | literal | transform() | sql()/agg()>` The horizontal facet position channel, for mark-level faceting, bound to the *fx* scale.
#' @param fy `<column | literal | transform() | sql()/agg()>` The vertical facet position channel, for mark-level faceting, bound to the *fy* scale.
#' @param href `<column | literal | transform() | sql()/agg()>` The href; a channel specifying URLs for clickable links.
#' @param image_filter `<string | param()>` A CSS filter; a constant string used to adjust the rendering of images, such as *blur(5px)*.
#' @param inset `<number | param()>` Shorthand to set the same default for **insetStart** and **insetEnd**.
#' @param inset_bottom `<number | param()>` Insets the bottom edge by the specified number of pixels.
#' @param inset_top `<number | param()>` Insets the top edge by the specified number of pixels.
#' @param interval `<"day"/"week"/"month"/... | number | param()>` Enforces uniformity for data at regular intervals, such as integer values or daily samples.
#' @param margin `<number | param()>` Shorthand to set the same default for all four mark margins: **marginTop**, **marginRight**, **marginBottom**, and **marginLeft**; typically defaults to 0, except for axis marks.
#' @param margin_bottom `<number | param()>` The mark’s bottom margin; the minimum distance in pixels between the bottom edges of the inner and outer plot area.
#' @param margin_left `<number | param()>` The mark’s left margin; the minimum distance in pixels between the left edges of the inner and outer plot area.
#' @param margin_right `<number | param()>` The mark’s right margin; the minimum distance in pixels between the right edges of the mark’s inner and outer plot area.
#' @param margin_top `<number | param()>` The mark’s top margin; the minimum distance in pixels between the top edges of the inner and outer plot area.
#' @param marker `<"arrow" | "arrow-reverse" | "dot" | "circle" | "circle-fill" | "circle-stroke" | "tick" | "tick-x" | "tick-y" | "none" | boolean | NULL | param()>` Shorthand to set the same default for markerStart, markerMid, and markerEnd; one of: - a marker name such as *arrow* or *circle* - *none* (default) - no marker * true - alias for *circle-fill* * false or null - alias for *none*
#' @param marker_end `<"arrow" | "arrow-reverse" | "dot" | "circle" | "circle-fill" | "circle-stroke" | "tick" | "tick-x" | "tick-y" | "none" | boolean | NULL | param()>` The marker for the ending point of a line segment; one of: - a marker name such as *arrow* or *circle* * *none* (default) - no marker * true - alias for *circle-fill* * false or null - alias for *none*
#' @param marker_mid `<"arrow" | "arrow-reverse" | "dot" | "circle" | "circle-fill" | "circle-stroke" | "tick" | "tick-x" | "tick-y" | "none" | boolean | NULL | param()>` The marker for any middle (interior) points of a line segment.
#' @param marker_start `<"arrow" | "arrow-reverse" | "dot" | "circle" | "circle-fill" | "circle-stroke" | "tick" | "tick-x" | "tick-y" | "none" | boolean | NULL | param()>` The marker for the starting point of a line segment; one of: - a marker name such as *arrow* or *circle* * *none* (default) - no marker * true - alias for *circle-fill* * false or null - alias for *none*
#' @param mix_blend_mode `<string | param()>` The mix-blend-mode; a constant string specifying how to blend content such as *multiply*.
#' @param opacity `<column | literal | transform() | sql()/agg() | list(value=, ...)>` The opacity; a constant between 0 and 1, or a channel typically bound to the *opacity* scale.
#' @param paint_order `<string | param()>` The paint-order; a constant string specifying the order in which the **fill**, **stroke**, and any markers are drawn; defaults to *normal*, which draws the fill, then stroke, then markers; defaults to *stroke* for the text mark to create a “halo” around text to improve legibility.
#' @param pointer_events `<string | param()>` The pointer-events property; a constant string such as *none*.
#' @param reverse `<boolean | param()>` Applies a transform to reverse the order of the mark’s index, say for reverse input order.
#' @param select `<"first" | "last" | "maxX" | "maxY" | "minX" | "minY" | "nearest" | "nearestX" | "nearestY">` Applies a filter transform after data is loaded to highlight selected values only.
#' @param shape_rendering `<string | param()>` The shape-rendering; a constant string such as *crispEdges*.
#' @param sort `<column | literal | transform() | sql()/agg() | list>` Either applies a transform to sort the mark’s index by the specified channel values, or imputes ordinal scale domains from this mark’s channels.
#' @param stroke `<column | literal | transform() | sql()/agg() | list(value=, ...) | param()>` The stroke; a constant CSS color string, or a channel typically bound to the *color* scale.
#' @param stroke_dasharray `<string | number | param()>` The stroke-dasharray; a constant number indicating the length in pixels of alternating dashes and gaps, or a constant string of numbers separated by spaces or commas (_e.g._, *10 2* for dashes of 10 pixels separated by gaps of 2 pixels), or *none* (the default) for no dashing : https://developer.mozilla.org/en-US/docs/Web/SVG/Attribute/stroke-dasharray
#' @param stroke_dashoffset `<string | number | param()>` The stroke-dashoffset; a constant indicating the offset in pixels of the first dash along the stroke; defaults to zero.
#' @param stroke_linecap `<string | param()>` The stroke-linecap; a constant specifying how to cap stroked paths, such as *butt*, *round*, or *square*.
#' @param stroke_linejoin `<string | param()>` The stroke-linejoin; a constant specifying how to join stroked paths, such as *bevel*, *miter*, *miter-clip*, or *round*.
#' @param stroke_miterlimit `<number | param()>` The stroke-miterlimit; a constant number specifying how to limit the length of *miter* joins on stroked paths.
#' @param stroke_opacity `<column | literal | transform() | sql()/agg() | list(value=, ...)>` The stroke-opacity; a constant between 0 and 1, or a channel typically bound to the *opacity* scale.
#' @param stroke_width `<column | literal | transform() | sql()/agg() | list(value=, ...)>` The stroke-width; a constant number in pixels, or a channel.
#' @param target `<string | param()>` The target; a constant string specifying the target window (_e.g._, *_blank*) for clickable links; used in conjunction with the **href** option.
#' @param tip `<boolean | "x" | "y" | "xy" | list | param()>` Whether to generate a tooltip for this mark, and any tip options.
#' @param title `<column | literal | transform() | sql()/agg()>` The title; a channel specifying accessible, short textual descriptions as strings (possibly with newlines).
#' @param x `<column | literal | transform() | sql()/agg() | list(value=, ...)>` The horizontal position (or length) channel, typically bound to the *x* scale.
#' @param y `<column | literal | transform() | sql()/agg() | list(value=, ...)>` The vertical position channel, typically bound to the *y* scale; defaults to the zero-based index of the data 0, 1, 2, ….
#' @param y1 `<column | literal | transform() | sql()/agg() | list(value=, ...)>` The required primary (starting, often bottom) vertical position channel, representing the area’s baseline, typically bound to the *y* scale.
#' @param y2 `<column | literal | transform() | sql()/agg() | list(value=, ...)>` The optional secondary (ending, often top) vertical position channel, representing the area’s topline, typically bound to the *y* scale; if not specified, **y1** is used.
#' @param ... Additional options or plot-level attributes.
#' @param data_from The name of the data source this mark reads from (see [vg_data()]); a length-1 nonzero R integer (`1L`, `-1L`, ...; note the `L`) giving a 1-based index into the spec's registered data sources instead, negative counting from the end (e.g., `-1L` for the most recently registered one); or a literal vector of values to use as inline data directly (mosaic-spec's `"data": [...]` shorthand, e.g., for a single reference line -- a bare double like `0`/`c(0)`, or `0L` itself (never a valid index), means this, not an index). Left unset, defaults to the first registered data source (equivalent to `data_from = 1L`) for any mark type that takes data at all.
#' @param filter_by A Param/Selection (e.g., from [param()]) to filter this mark's data by.
#' @param data_optimize A flag (default `TRUE`) to enable mark-specific query optimizations for this mark's data; set `FALSE` to disable them (mosaic-spec's `data: {optimize: false}`).
#' @family mark functions
#' @export
vg_mark_rule_x <- function(spec = NULL, formula = vg_unset, aria_description = vg_unset, aria_hidden = vg_unset, aria_label = vg_unset, channels = vg_unset, clip = vg_unset, dx = vg_unset, dy = vg_unset, facet = vg_unset, facet_anchor = vg_unset, fill = vg_unset, fill_opacity = vg_unset, filter = vg_unset, fx = vg_unset, fy = vg_unset, href = vg_unset, image_filter = vg_unset, inset = vg_unset, inset_bottom = vg_unset, inset_top = vg_unset, interval = vg_unset, margin = vg_unset, margin_bottom = vg_unset, margin_left = vg_unset, margin_right = vg_unset, margin_top = vg_unset, marker = vg_unset, marker_end = vg_unset, marker_mid = vg_unset, marker_start = vg_unset, mix_blend_mode = vg_unset, opacity = vg_unset, paint_order = vg_unset, pointer_events = vg_unset, reverse = vg_unset, select = vg_unset, shape_rendering = vg_unset, sort = vg_unset, stroke = vg_unset, stroke_dasharray = vg_unset, stroke_dashoffset = vg_unset, stroke_linecap = vg_unset, stroke_linejoin = vg_unset, stroke_miterlimit = vg_unset, stroke_opacity = vg_unset, stroke_width = vg_unset, target = vg_unset, tip = vg_unset, title = vg_unset, x = vg_unset, y = vg_unset, y1 = vg_unset, y2 = vg_unset, ..., data_from = vg_unset, filter_by = vg_unset, data_optimize = vg_unset) {
  vg_mark_(spec, "ruleX", formula = formula, ariaDescription = aria_description, ariaHidden = aria_hidden, ariaLabel = aria_label, channels = channels, clip = clip, dx = dx, dy = dy, facet = facet, facetAnchor = facet_anchor, fill = fill, fillOpacity = fill_opacity, filter = filter, fx = fx, fy = fy, href = href, imageFilter = image_filter, inset = inset, insetBottom = inset_bottom, insetTop = inset_top, interval = interval, margin = margin, marginBottom = margin_bottom, marginLeft = margin_left, marginRight = margin_right, marginTop = margin_top, marker = marker, markerEnd = marker_end, markerMid = marker_mid, markerStart = marker_start, mixBlendMode = mix_blend_mode, opacity = opacity, paintOrder = paint_order, pointerEvents = pointer_events, reverse = reverse, select = select, shapeRendering = shape_rendering, sort = sort, stroke = stroke, strokeDasharray = stroke_dasharray, strokeDashoffset = stroke_dashoffset, strokeLinecap = stroke_linecap, strokeLinejoin = stroke_linejoin, strokeMiterlimit = stroke_miterlimit, strokeOpacity = stroke_opacity, strokeWidth = stroke_width, target = target, tip = tip, title = title, x = x, y = y, y1 = y1, y2 = y2, ..., data_from = data_from, filter_by = filter_by, data_optimize = data_optimize)
}

#' The ruleY mark.
#'
#' See [vg_value_types] for what the `<...>` notation below (`column`, `param()`, ...) means.
#'
#' @param spec A plot fragment or `vgspec` to add this mark to, or `NULL` to start a new plot with just this mark.
#' @param formula A shorthand for this mark's position channels (`x`, `y`, `fx`, `fy`, and paired `x1`/`x2` or `y1`/`y2`), e.g., `Sepal.Length ~ Sepal.Width | ~ Species` for `y = ~Sepal.Length, x = ~Sepal.Width, fx = ~Species`. See [vg_mark()] for the full grammar.
#' @param aria_description `<string | param()>` The aria-description; a constant textual description.
#' @param aria_hidden `<string | param()>` The aria-hidden state; a constant indicating whether the element is exposed to an accessibility API.
#' @param aria_label `<column | literal | transform() | sql()/agg()>` The aria-label; a channel specifying short textual labels representing the value in the accessibility tree.
#' @param channels `<list>` Additional named channels, for example to include in a tooltip.
#' @param clip `<"frame" | "sphere" | boolean | NULL | param()>` How to clip the mark; one of: - *frame* or true - clip to the plot’s frame (inner area) - *sphere* - clip to the projected sphere (*e.g.*, front hemisphere) - null or false - do not clip The *sphere* clip option requires a geographic projection.
#' @param dx `<number | param()>` The horizontal offset in pixels; a constant option.
#' @param dy `<number | param()>` The vertical offset in pixels; a constant option.
#' @param facet `<"auto" | "include" | "exclude" | "super" | boolean | NULL | param()>` Whether to enable or disable faceting; one of: - *auto* (default) - automatically determine if this mark should be faceted - *include* (or true) - draw the subset of the mark’s data in the current facet - *exclude* - draw the subset of the mark’s data *not* in the current facet - *super* - draw this mark in a single frame that covers all facets - null (or false) - repeat this mark’s data across all facets (*i.e.*, no faceting) When a mark uses *super* faceting, it is not allowed to use position scales (*x*, *y*, *fx*, or *fy*); *super* faceting is intended for decorations, such as labels and legends.
#' @param facet_anchor `<"top" | "right" | "bottom" | "left" | "top-left" | "top-right" | "bottom-left" | "bottom-right" | "top-empty" | "right-empty" | "bottom-empty" | "left-empty" | "empty" | NULL | param()>` How to place the mark with respect to facets; one of: - null (default for most marks) - display the mark in each non-empty facet - *top*, *right*, *bottom*, or *left* - display the mark only in facets on the given side - *top-empty*, *right-empty*, *bottom-empty*, or *left-empty* (default for axis marks) - display the mark only in facets that have empty space on the given side: either the margin, or an empty facet - *empty* - display the mark in empty facets only
#' @param fill `<column | literal | transform() | sql()/agg() | list(value=, ...) | param()>` The fill; a constant CSS color string, or a channel typically bound to the *color* scale.
#' @param fill_opacity `<column | literal | transform() | sql()/agg() | list(value=, ...) | param()>` The fill-opacity; a constant number between 0 and 1, or a channel typically bound to the *opacity* scale.
#' @param filter `<column | literal | transform() | sql()/agg()>` Applies a transform to filter the mark’s index according to the given channel values; only truthy values are retained.
#' @param fx `<column | literal | transform() | sql()/agg()>` The horizontal facet position channel, for mark-level faceting, bound to the *fx* scale.
#' @param fy `<column | literal | transform() | sql()/agg()>` The vertical facet position channel, for mark-level faceting, bound to the *fy* scale.
#' @param href `<column | literal | transform() | sql()/agg()>` The href; a channel specifying URLs for clickable links.
#' @param image_filter `<string | param()>` A CSS filter; a constant string used to adjust the rendering of images, such as *blur(5px)*.
#' @param inset `<number | param()>` Shorthand to set the same default for **insetStart** and **insetEnd**.
#' @param inset_bottom `<number | param()>` Insets the bottom edge by the specified number of pixels.
#' @param inset_top `<number | param()>` Insets the top edge by the specified number of pixels.
#' @param interval `<"day"/"week"/"month"/... | number | param()>` Enforces uniformity for data at regular intervals, such as integer values or daily samples.
#' @param margin `<number | param()>` Shorthand to set the same default for all four mark margins: **marginTop**, **marginRight**, **marginBottom**, and **marginLeft**; typically defaults to 0, except for axis marks.
#' @param margin_bottom `<number | param()>` The mark’s bottom margin; the minimum distance in pixels between the bottom edges of the inner and outer plot area.
#' @param margin_left `<number | param()>` The mark’s left margin; the minimum distance in pixels between the left edges of the inner and outer plot area.
#' @param margin_right `<number | param()>` The mark’s right margin; the minimum distance in pixels between the right edges of the mark’s inner and outer plot area.
#' @param margin_top `<number | param()>` The mark’s top margin; the minimum distance in pixels between the top edges of the inner and outer plot area.
#' @param marker `<"arrow" | "arrow-reverse" | "dot" | "circle" | "circle-fill" | "circle-stroke" | "tick" | "tick-x" | "tick-y" | "none" | boolean | NULL | param()>` Shorthand to set the same default for markerStart, markerMid, and markerEnd; one of: - a marker name such as *arrow* or *circle* - *none* (default) - no marker * true - alias for *circle-fill* * false or null - alias for *none*
#' @param marker_end `<"arrow" | "arrow-reverse" | "dot" | "circle" | "circle-fill" | "circle-stroke" | "tick" | "tick-x" | "tick-y" | "none" | boolean | NULL | param()>` The marker for the ending point of a line segment; one of: - a marker name such as *arrow* or *circle* * *none* (default) - no marker * true - alias for *circle-fill* * false or null - alias for *none*
#' @param marker_mid `<"arrow" | "arrow-reverse" | "dot" | "circle" | "circle-fill" | "circle-stroke" | "tick" | "tick-x" | "tick-y" | "none" | boolean | NULL | param()>` The marker for any middle (interior) points of a line segment.
#' @param marker_start `<"arrow" | "arrow-reverse" | "dot" | "circle" | "circle-fill" | "circle-stroke" | "tick" | "tick-x" | "tick-y" | "none" | boolean | NULL | param()>` The marker for the starting point of a line segment; one of: - a marker name such as *arrow* or *circle* * *none* (default) - no marker * true - alias for *circle-fill* * false or null - alias for *none*
#' @param mix_blend_mode `<string | param()>` The mix-blend-mode; a constant string specifying how to blend content such as *multiply*.
#' @param opacity `<column | literal | transform() | sql()/agg() | list(value=, ...)>` The opacity; a constant between 0 and 1, or a channel typically bound to the *opacity* scale.
#' @param paint_order `<string | param()>` The paint-order; a constant string specifying the order in which the **fill**, **stroke**, and any markers are drawn; defaults to *normal*, which draws the fill, then stroke, then markers; defaults to *stroke* for the text mark to create a “halo” around text to improve legibility.
#' @param pointer_events `<string | param()>` The pointer-events property; a constant string such as *none*.
#' @param reverse `<boolean | param()>` Applies a transform to reverse the order of the mark’s index, say for reverse input order.
#' @param select `<"first" | "last" | "maxX" | "maxY" | "minX" | "minY" | "nearest" | "nearestX" | "nearestY">` Applies a filter transform after data is loaded to highlight selected values only.
#' @param shape_rendering `<string | param()>` The shape-rendering; a constant string such as *crispEdges*.
#' @param sort `<column | literal | transform() | sql()/agg() | list>` Either applies a transform to sort the mark’s index by the specified channel values, or imputes ordinal scale domains from this mark’s channels.
#' @param stroke `<column | literal | transform() | sql()/agg() | list(value=, ...) | param()>` The stroke; a constant CSS color string, or a channel typically bound to the *color* scale.
#' @param stroke_dasharray `<string | number | param()>` The stroke-dasharray; a constant number indicating the length in pixels of alternating dashes and gaps, or a constant string of numbers separated by spaces or commas (_e.g._, *10 2* for dashes of 10 pixels separated by gaps of 2 pixels), or *none* (the default) for no dashing : https://developer.mozilla.org/en-US/docs/Web/SVG/Attribute/stroke-dasharray
#' @param stroke_dashoffset `<string | number | param()>` The stroke-dashoffset; a constant indicating the offset in pixels of the first dash along the stroke; defaults to zero.
#' @param stroke_linecap `<string | param()>` The stroke-linecap; a constant specifying how to cap stroked paths, such as *butt*, *round*, or *square*.
#' @param stroke_linejoin `<string | param()>` The stroke-linejoin; a constant specifying how to join stroked paths, such as *bevel*, *miter*, *miter-clip*, or *round*.
#' @param stroke_miterlimit `<number | param()>` The stroke-miterlimit; a constant number specifying how to limit the length of *miter* joins on stroked paths.
#' @param stroke_opacity `<column | literal | transform() | sql()/agg() | list(value=, ...)>` The stroke-opacity; a constant between 0 and 1, or a channel typically bound to the *opacity* scale.
#' @param stroke_width `<column | literal | transform() | sql()/agg() | list(value=, ...)>` The stroke-width; a constant number in pixels, or a channel.
#' @param target `<string | param()>` The target; a constant string specifying the target window (_e.g._, *_blank*) for clickable links; used in conjunction with the **href** option.
#' @param tip `<boolean | "x" | "y" | "xy" | list | param()>` Whether to generate a tooltip for this mark, and any tip options.
#' @param title `<column | literal | transform() | sql()/agg()>` The title; a channel specifying accessible, short textual descriptions as strings (possibly with newlines).
#' @param x `<column | literal | transform() | sql()/agg() | list(value=, ...)>` The horizontal position (or length) channel, typically bound to the *x* scale.
#' @param y `<column | literal | transform() | sql()/agg() | list(value=, ...)>` The vertical position channel, typically bound to the *y* scale; defaults to the zero-based index of the data 0, 1, 2, ….
#' @param y1 `<column | literal | transform() | sql()/agg() | list(value=, ...)>` The required primary (starting, often bottom) vertical position channel, representing the area’s baseline, typically bound to the *y* scale.
#' @param y2 `<column | literal | transform() | sql()/agg() | list(value=, ...)>` The optional secondary (ending, often top) vertical position channel, representing the area’s topline, typically bound to the *y* scale; if not specified, **y1** is used.
#' @param ... Additional options or plot-level attributes.
#' @param data_from The name of the data source this mark reads from (see [vg_data()]); a length-1 nonzero R integer (`1L`, `-1L`, ...; note the `L`) giving a 1-based index into the spec's registered data sources instead, negative counting from the end (e.g., `-1L` for the most recently registered one); or a literal vector of values to use as inline data directly (mosaic-spec's `"data": [...]` shorthand, e.g., for a single reference line -- a bare double like `0`/`c(0)`, or `0L` itself (never a valid index), means this, not an index). Left unset, defaults to the first registered data source (equivalent to `data_from = 1L`) for any mark type that takes data at all.
#' @param filter_by A Param/Selection (e.g., from [param()]) to filter this mark's data by.
#' @param data_optimize A flag (default `TRUE`) to enable mark-specific query optimizations for this mark's data; set `FALSE` to disable them (mosaic-spec's `data: {optimize: false}`).
#' @family mark functions
#' @export
vg_mark_rule_y <- function(spec = NULL, formula = vg_unset, aria_description = vg_unset, aria_hidden = vg_unset, aria_label = vg_unset, channels = vg_unset, clip = vg_unset, dx = vg_unset, dy = vg_unset, facet = vg_unset, facet_anchor = vg_unset, fill = vg_unset, fill_opacity = vg_unset, filter = vg_unset, fx = vg_unset, fy = vg_unset, href = vg_unset, image_filter = vg_unset, inset = vg_unset, inset_bottom = vg_unset, inset_top = vg_unset, interval = vg_unset, margin = vg_unset, margin_bottom = vg_unset, margin_left = vg_unset, margin_right = vg_unset, margin_top = vg_unset, marker = vg_unset, marker_end = vg_unset, marker_mid = vg_unset, marker_start = vg_unset, mix_blend_mode = vg_unset, opacity = vg_unset, paint_order = vg_unset, pointer_events = vg_unset, reverse = vg_unset, select = vg_unset, shape_rendering = vg_unset, sort = vg_unset, stroke = vg_unset, stroke_dasharray = vg_unset, stroke_dashoffset = vg_unset, stroke_linecap = vg_unset, stroke_linejoin = vg_unset, stroke_miterlimit = vg_unset, stroke_opacity = vg_unset, stroke_width = vg_unset, target = vg_unset, tip = vg_unset, title = vg_unset, x = vg_unset, y = vg_unset, y1 = vg_unset, y2 = vg_unset, ..., data_from = vg_unset, filter_by = vg_unset, data_optimize = vg_unset) {
  vg_mark_(spec, "ruleY", formula = formula, ariaDescription = aria_description, ariaHidden = aria_hidden, ariaLabel = aria_label, channels = channels, clip = clip, dx = dx, dy = dy, facet = facet, facetAnchor = facet_anchor, fill = fill, fillOpacity = fill_opacity, filter = filter, fx = fx, fy = fy, href = href, imageFilter = image_filter, inset = inset, insetBottom = inset_bottom, insetTop = inset_top, interval = interval, margin = margin, marginBottom = margin_bottom, marginLeft = margin_left, marginRight = margin_right, marginTop = margin_top, marker = marker, markerEnd = marker_end, markerMid = marker_mid, markerStart = marker_start, mixBlendMode = mix_blend_mode, opacity = opacity, paintOrder = paint_order, pointerEvents = pointer_events, reverse = reverse, select = select, shapeRendering = shape_rendering, sort = sort, stroke = stroke, strokeDasharray = stroke_dasharray, strokeDashoffset = stroke_dashoffset, strokeLinecap = stroke_linecap, strokeLinejoin = stroke_linejoin, strokeMiterlimit = stroke_miterlimit, strokeOpacity = stroke_opacity, strokeWidth = stroke_width, target = target, tip = tip, title = title, x = x, y = y, y1 = y1, y2 = y2, ..., data_from = data_from, filter_by = filter_by, data_optimize = data_optimize)
}

#' The sphere mark.
#'
#' See [vg_value_types] for what the `<...>` notation below (`column`, `param()`, ...) means.
#'
#' @param spec A plot fragment or `vgspec` to add this mark to, or `NULL` to start a new plot with just this mark.
#' @param formula A shorthand for this mark's position channels (`x`, `y`, `fx`, `fy`, and paired `x1`/`x2` or `y1`/`y2`), e.g., `Sepal.Length ~ Sepal.Width | ~ Species` for `y = ~Sepal.Length, x = ~Sepal.Width, fx = ~Species`. See [vg_mark()] for the full grammar.
#' @param aria_description `<string | param()>` The aria-description; a constant textual description.
#' @param aria_hidden `<string | param()>` The aria-hidden state; a constant indicating whether the element is exposed to an accessibility API.
#' @param aria_label `<column | literal | transform() | sql()/agg()>` The aria-label; a channel specifying short textual labels representing the value in the accessibility tree.
#' @param channels `<list>` Additional named channels, for example to include in a tooltip.
#' @param clip `<"frame" | "sphere" | boolean | NULL | param()>` How to clip the mark; one of: - *frame* or true - clip to the plot’s frame (inner area) - *sphere* - clip to the projected sphere (*e.g.*, front hemisphere) - null or false - do not clip The *sphere* clip option requires a geographic projection.
#' @param dx `<number | param()>` The horizontal offset in pixels; a constant option.
#' @param dy `<number | param()>` The vertical offset in pixels; a constant option.
#' @param facet `<"auto" | "include" | "exclude" | "super" | boolean | NULL | param()>` Whether to enable or disable faceting; one of: - *auto* (default) - automatically determine if this mark should be faceted - *include* (or true) - draw the subset of the mark’s data in the current facet - *exclude* - draw the subset of the mark’s data *not* in the current facet - *super* - draw this mark in a single frame that covers all facets - null (or false) - repeat this mark’s data across all facets (*i.e.*, no faceting) When a mark uses *super* faceting, it is not allowed to use position scales (*x*, *y*, *fx*, or *fy*); *super* faceting is intended for decorations, such as labels and legends.
#' @param facet_anchor `<"top" | "right" | "bottom" | "left" | "top-left" | "top-right" | "bottom-left" | "bottom-right" | "top-empty" | "right-empty" | "bottom-empty" | "left-empty" | "empty" | NULL | param()>` How to place the mark with respect to facets; one of: - null (default for most marks) - display the mark in each non-empty facet - *top*, *right*, *bottom*, or *left* - display the mark only in facets on the given side - *top-empty*, *right-empty*, *bottom-empty*, or *left-empty* (default for axis marks) - display the mark only in facets that have empty space on the given side: either the margin, or an empty facet - *empty* - display the mark in empty facets only
#' @param fill `<column | literal | transform() | sql()/agg() | list(value=, ...) | param()>` The fill; a constant CSS color string, or a channel typically bound to the *color* scale.
#' @param fill_opacity `<column | literal | transform() | sql()/agg() | list(value=, ...) | param()>` The fill-opacity; a constant number between 0 and 1, or a channel typically bound to the *opacity* scale.
#' @param filter `<column | literal | transform() | sql()/agg()>` Applies a transform to filter the mark’s index according to the given channel values; only truthy values are retained.
#' @param fx `<column | literal | transform() | sql()/agg()>` The horizontal facet position channel, for mark-level faceting, bound to the *fx* scale.
#' @param fy `<column | literal | transform() | sql()/agg()>` The vertical facet position channel, for mark-level faceting, bound to the *fy* scale.
#' @param href `<column | literal | transform() | sql()/agg()>` The href; a channel specifying URLs for clickable links.
#' @param image_filter `<string | param()>` A CSS filter; a constant string used to adjust the rendering of images, such as *blur(5px)*.
#' @param margin `<number | param()>` Shorthand to set the same default for all four mark margins: **marginTop**, **marginRight**, **marginBottom**, and **marginLeft**; typically defaults to 0, except for axis marks.
#' @param margin_bottom `<number | param()>` The mark’s bottom margin; the minimum distance in pixels between the bottom edges of the inner and outer plot area.
#' @param margin_left `<number | param()>` The mark’s left margin; the minimum distance in pixels between the left edges of the inner and outer plot area.
#' @param margin_right `<number | param()>` The mark’s right margin; the minimum distance in pixels between the right edges of the mark’s inner and outer plot area.
#' @param margin_top `<number | param()>` The mark’s top margin; the minimum distance in pixels between the top edges of the inner and outer plot area.
#' @param mix_blend_mode `<string | param()>` The mix-blend-mode; a constant string specifying how to blend content such as *multiply*.
#' @param opacity `<column | literal | transform() | sql()/agg() | list(value=, ...)>` The opacity; a constant between 0 and 1, or a channel typically bound to the *opacity* scale.
#' @param paint_order `<string | param()>` The paint-order; a constant string specifying the order in which the **fill**, **stroke**, and any markers are drawn; defaults to *normal*, which draws the fill, then stroke, then markers; defaults to *stroke* for the text mark to create a “halo” around text to improve legibility.
#' @param pointer_events `<string | param()>` The pointer-events property; a constant string such as *none*.
#' @param reverse `<boolean | param()>` Applies a transform to reverse the order of the mark’s index, say for reverse input order.
#' @param select `<"first" | "last" | "maxX" | "maxY" | "minX" | "minY" | "nearest" | "nearestX" | "nearestY">` Applies a filter transform after data is loaded to highlight selected values only.
#' @param shape_rendering `<string | param()>` The shape-rendering; a constant string such as *crispEdges*.
#' @param sort `<column | literal | transform() | sql()/agg() | list>` Either applies a transform to sort the mark’s index by the specified channel values, or imputes ordinal scale domains from this mark’s channels.
#' @param stroke `<column | literal | transform() | sql()/agg() | list(value=, ...) | param()>` The stroke; a constant CSS color string, or a channel typically bound to the *color* scale.
#' @param stroke_dasharray `<string | number | param()>` The stroke-dasharray; a constant number indicating the length in pixels of alternating dashes and gaps, or a constant string of numbers separated by spaces or commas (_e.g._, *10 2* for dashes of 10 pixels separated by gaps of 2 pixels), or *none* (the default) for no dashing : https://developer.mozilla.org/en-US/docs/Web/SVG/Attribute/stroke-dasharray
#' @param stroke_dashoffset `<string | number | param()>` The stroke-dashoffset; a constant indicating the offset in pixels of the first dash along the stroke; defaults to zero.
#' @param stroke_linecap `<string | param()>` The stroke-linecap; a constant specifying how to cap stroked paths, such as *butt*, *round*, or *square*.
#' @param stroke_linejoin `<string | param()>` The stroke-linejoin; a constant specifying how to join stroked paths, such as *bevel*, *miter*, *miter-clip*, or *round*.
#' @param stroke_miterlimit `<number | param()>` The stroke-miterlimit; a constant number specifying how to limit the length of *miter* joins on stroked paths.
#' @param stroke_opacity `<column | literal | transform() | sql()/agg() | list(value=, ...)>` The stroke-opacity; a constant between 0 and 1, or a channel typically bound to the *opacity* scale.
#' @param stroke_width `<column | literal | transform() | sql()/agg() | list(value=, ...)>` The stroke-width; a constant number in pixels, or a channel.
#' @param target `<string | param()>` The target; a constant string specifying the target window (_e.g._, *_blank*) for clickable links; used in conjunction with the **href** option.
#' @param tip `<boolean | "x" | "y" | "xy" | list | param()>` Whether to generate a tooltip for this mark, and any tip options.
#' @param title `<column | literal | transform() | sql()/agg()>` The title; a channel specifying accessible, short textual descriptions as strings (possibly with newlines).
#' @param ... Additional options or plot-level attributes.
#' @param data_from The name of the data source this mark reads from (see [vg_data()]); a length-1 nonzero R integer (`1L`, `-1L`, ...; note the `L`) giving a 1-based index into the spec's registered data sources instead, negative counting from the end (e.g., `-1L` for the most recently registered one); or a literal vector of values to use as inline data directly (mosaic-spec's `"data": [...]` shorthand, e.g., for a single reference line -- a bare double like `0`/`c(0)`, or `0L` itself (never a valid index), means this, not an index). Left unset, defaults to the first registered data source (equivalent to `data_from = 1L`) for any mark type that takes data at all.
#' @param filter_by A Param/Selection (e.g., from [param()]) to filter this mark's data by.
#' @param data_optimize A flag (default `TRUE`) to enable mark-specific query optimizations for this mark's data; set `FALSE` to disable them (mosaic-spec's `data: {optimize: false}`).
#' @family mark functions
#' @export
vg_mark_sphere <- function(spec = NULL, formula = vg_unset, aria_description = vg_unset, aria_hidden = vg_unset, aria_label = vg_unset, channels = vg_unset, clip = vg_unset, dx = vg_unset, dy = vg_unset, facet = vg_unset, facet_anchor = vg_unset, fill = vg_unset, fill_opacity = vg_unset, filter = vg_unset, fx = vg_unset, fy = vg_unset, href = vg_unset, image_filter = vg_unset, margin = vg_unset, margin_bottom = vg_unset, margin_left = vg_unset, margin_right = vg_unset, margin_top = vg_unset, mix_blend_mode = vg_unset, opacity = vg_unset, paint_order = vg_unset, pointer_events = vg_unset, reverse = vg_unset, select = vg_unset, shape_rendering = vg_unset, sort = vg_unset, stroke = vg_unset, stroke_dasharray = vg_unset, stroke_dashoffset = vg_unset, stroke_linecap = vg_unset, stroke_linejoin = vg_unset, stroke_miterlimit = vg_unset, stroke_opacity = vg_unset, stroke_width = vg_unset, target = vg_unset, tip = vg_unset, title = vg_unset, ..., data_from = vg_unset, filter_by = vg_unset, data_optimize = vg_unset) {
  vg_mark_(spec, "sphere", formula = formula, ariaDescription = aria_description, ariaHidden = aria_hidden, ariaLabel = aria_label, channels = channels, clip = clip, dx = dx, dy = dy, facet = facet, facetAnchor = facet_anchor, fill = fill, fillOpacity = fill_opacity, filter = filter, fx = fx, fy = fy, href = href, imageFilter = image_filter, margin = margin, marginBottom = margin_bottom, marginLeft = margin_left, marginRight = margin_right, marginTop = margin_top, mixBlendMode = mix_blend_mode, opacity = opacity, paintOrder = paint_order, pointerEvents = pointer_events, reverse = reverse, select = select, shapeRendering = shape_rendering, sort = sort, stroke = stroke, strokeDasharray = stroke_dasharray, strokeDashoffset = stroke_dashoffset, strokeLinecap = stroke_linecap, strokeLinejoin = stroke_linejoin, strokeMiterlimit = stroke_miterlimit, strokeOpacity = stroke_opacity, strokeWidth = stroke_width, target = target, tip = tip, title = title, ..., data_from = data_from, filter_by = filter_by, data_optimize = data_optimize)
}

#' The spike mark.
#'
#' See [vg_value_types] for what the `<...>` notation below (`column`, `param()`, ...) means.
#'
#' @param spec A plot fragment or `vgspec` to add this mark to, or `NULL` to start a new plot with just this mark.
#' @param formula A shorthand for this mark's position channels (`x`, `y`, `fx`, `fy`, and paired `x1`/`x2` or `y1`/`y2`), e.g., `Sepal.Length ~ Sepal.Width | ~ Species` for `y = ~Sepal.Length, x = ~Sepal.Width, fx = ~Species`. See [vg_mark()] for the full grammar.
#' @param anchor `<"top" | "right" | "bottom" | "left" | param()>` The side of the frame on which to place the axis: *top* or *bottom* for horizontal axes (axisX and axisFx) and their associated vertical grids (gridX and gridFx), or *left* or *right* for vertical axes (axisY and axisFY) and their associated horizontal grids (gridY and gridFy).
#' @param aria_description `<string | param()>` The aria-description; a constant textual description.
#' @param aria_hidden `<string | param()>` The aria-hidden state; a constant indicating whether the element is exposed to an accessibility API.
#' @param aria_label `<column | literal | transform() | sql()/agg()>` The aria-label; a channel specifying short textual labels representing the value in the accessibility tree.
#' @param channels `<list>` Additional named channels, for example to include in a tooltip.
#' @param clip `<"frame" | "sphere" | boolean | NULL | param()>` How to clip the mark; one of: - *frame* or true - clip to the plot’s frame (inner area) - *sphere* - clip to the projected sphere (*e.g.*, front hemisphere) - null or false - do not clip The *sphere* clip option requires a geographic projection.
#' @param dx `<number | param()>` The horizontal offset in pixels; a constant option.
#' @param dy `<number | param()>` The vertical offset in pixels; a constant option.
#' @param facet `<"auto" | "include" | "exclude" | "super" | boolean | NULL | param()>` Whether to enable or disable faceting; one of: - *auto* (default) - automatically determine if this mark should be faceted - *include* (or true) - draw the subset of the mark’s data in the current facet - *exclude* - draw the subset of the mark’s data *not* in the current facet - *super* - draw this mark in a single frame that covers all facets - null (or false) - repeat this mark’s data across all facets (*i.e.*, no faceting) When a mark uses *super* faceting, it is not allowed to use position scales (*x*, *y*, *fx*, or *fy*); *super* faceting is intended for decorations, such as labels and legends.
#' @param facet_anchor `<"top" | "right" | "bottom" | "left" | "top-left" | "top-right" | "bottom-left" | "bottom-right" | "top-empty" | "right-empty" | "bottom-empty" | "left-empty" | "empty" | NULL | param()>` How to place the mark with respect to facets; one of: - null (default for most marks) - display the mark in each non-empty facet - *top*, *right*, *bottom*, or *left* - display the mark only in facets on the given side - *top-empty*, *right-empty*, *bottom-empty*, or *left-empty* (default for axis marks) - display the mark only in facets that have empty space on the given side: either the margin, or an empty facet - *empty* - display the mark in empty facets only
#' @param fill `<column | literal | transform() | sql()/agg() | list(value=, ...) | param()>` The fill; a constant CSS color string, or a channel typically bound to the *color* scale.
#' @param fill_opacity `<column | literal | transform() | sql()/agg() | list(value=, ...) | param()>` The fill-opacity; a constant number between 0 and 1, or a channel typically bound to the *opacity* scale.
#' @param filter `<column | literal | transform() | sql()/agg()>` Applies a transform to filter the mark’s index according to the given channel values; only truthy values are retained.
#' @param frame_anchor `<"middle" | "top-left" | "top" | "top-right" | "right" | "bottom-right" | "bottom" | "bottom-left" | "left" | param()>` The frame anchor specifies defaults for **x** and **y**, along with **textAnchor** and **lineAnchor**, based on the plot’s frame; it may be one of the four sides (*top*, *right*, *bottom*, *left*), one of the four corners (*top-left*, *top-right*, *bottom-right*, *bottom-left*), or the *middle* of the frame.
#' @param fx `<column | literal | transform() | sql()/agg()>` The horizontal facet position channel, for mark-level faceting, bound to the *fx* scale.
#' @param fy `<column | literal | transform() | sql()/agg()>` The vertical facet position channel, for mark-level faceting, bound to the *fy* scale.
#' @param href `<column | literal | transform() | sql()/agg()>` The href; a channel specifying URLs for clickable links.
#' @param image_filter `<string | param()>` A CSS filter; a constant string used to adjust the rendering of images, such as *blur(5px)*.
#' @param length `<column | literal | transform() | sql()/agg() | list(value=, ...)>` The vector’s length; either an optional channel bound to the *length* scale or a constant number in pixels.
#' @param margin `<number | param()>` Shorthand to set the same default for all four mark margins: **marginTop**, **marginRight**, **marginBottom**, and **marginLeft**; typically defaults to 0, except for axis marks.
#' @param margin_bottom `<number | param()>` The mark’s bottom margin; the minimum distance in pixels between the bottom edges of the inner and outer plot area.
#' @param margin_left `<number | param()>` The mark’s left margin; the minimum distance in pixels between the left edges of the inner and outer plot area.
#' @param margin_right `<number | param()>` The mark’s right margin; the minimum distance in pixels between the right edges of the mark’s inner and outer plot area.
#' @param margin_top `<number | param()>` The mark’s top margin; the minimum distance in pixels between the top edges of the inner and outer plot area.
#' @param mix_blend_mode `<string | param()>` The mix-blend-mode; a constant string specifying how to blend content such as *multiply*.
#' @param opacity `<column | literal | transform() | sql()/agg() | list(value=, ...)>` The opacity; a constant between 0 and 1, or a channel typically bound to the *opacity* scale.
#' @param paint_order `<string | param()>` The paint-order; a constant string specifying the order in which the **fill**, **stroke**, and any markers are drawn; defaults to *normal*, which draws the fill, then stroke, then markers; defaults to *stroke* for the text mark to create a “halo” around text to improve legibility.
#' @param pointer_events `<string | param()>` The pointer-events property; a constant string such as *none*.
#' @param r `<column | literal | transform() | sql()/agg() | list(value=, ...) | number | param()>` The radius of dots; either a channel or constant.
#' @param reverse `<boolean | param()>` Applies a transform to reverse the order of the mark’s index, say for reverse input order.
#' @param rotate `<column | literal | transform() | sql()/agg() | param()>` The rotation angle in degrees clockwise; a constant or a channel; defaults to 0°.
#' @param select `<"first" | "last" | "maxX" | "maxY" | "minX" | "minY" | "nearest" | "nearestX" | "nearestY">` Applies a filter transform after data is loaded to highlight selected values only.
#' @param shape `<"arrow" | "spike" | param()>` The shape of the vector; a constant.
#' @param shape_rendering `<string | param()>` The shape-rendering; a constant string such as *crispEdges*.
#' @param sort `<column | literal | transform() | sql()/agg() | list>` Either applies a transform to sort the mark’s index by the specified channel values, or imputes ordinal scale domains from this mark’s channels.
#' @param stroke `<column | literal | transform() | sql()/agg() | list(value=, ...) | param()>` The stroke; a constant CSS color string, or a channel typically bound to the *color* scale.
#' @param stroke_dasharray `<string | number | param()>` The stroke-dasharray; a constant number indicating the length in pixels of alternating dashes and gaps, or a constant string of numbers separated by spaces or commas (_e.g._, *10 2* for dashes of 10 pixels separated by gaps of 2 pixels), or *none* (the default) for no dashing : https://developer.mozilla.org/en-US/docs/Web/SVG/Attribute/stroke-dasharray
#' @param stroke_dashoffset `<string | number | param()>` The stroke-dashoffset; a constant indicating the offset in pixels of the first dash along the stroke; defaults to zero.
#' @param stroke_linecap `<string | param()>` The stroke-linecap; a constant specifying how to cap stroked paths, such as *butt*, *round*, or *square*.
#' @param stroke_linejoin `<string | param()>` The stroke-linejoin; a constant specifying how to join stroked paths, such as *bevel*, *miter*, *miter-clip*, or *round*.
#' @param stroke_miterlimit `<number | param()>` The stroke-miterlimit; a constant number specifying how to limit the length of *miter* joins on stroked paths.
#' @param stroke_opacity `<column | literal | transform() | sql()/agg() | list(value=, ...)>` The stroke-opacity; a constant between 0 and 1, or a channel typically bound to the *opacity* scale.
#' @param stroke_width `<column | literal | transform() | sql()/agg() | list(value=, ...)>` The stroke-width; a constant number in pixels, or a channel.
#' @param target `<string | param()>` The target; a constant string specifying the target window (_e.g._, *_blank*) for clickable links; used in conjunction with the **href** option.
#' @param tip `<boolean | "x" | "y" | "xy" | list | param()>` Whether to generate a tooltip for this mark, and any tip options.
#' @param title `<column | literal | transform() | sql()/agg()>` The title; a channel specifying accessible, short textual descriptions as strings (possibly with newlines).
#' @param x `<column | literal | transform() | sql()/agg() | list(value=, ...)>` The horizontal position (or length) channel, typically bound to the *x* scale.
#' @param y `<column | literal | transform() | sql()/agg() | list(value=, ...)>` The vertical position channel, typically bound to the *y* scale; defaults to the zero-based index of the data 0, 1, 2, ….
#' @param ... Additional options or plot-level attributes.
#' @param data_from The name of the data source this mark reads from (see [vg_data()]); a length-1 nonzero R integer (`1L`, `-1L`, ...; note the `L`) giving a 1-based index into the spec's registered data sources instead, negative counting from the end (e.g., `-1L` for the most recently registered one); or a literal vector of values to use as inline data directly (mosaic-spec's `"data": [...]` shorthand, e.g., for a single reference line -- a bare double like `0`/`c(0)`, or `0L` itself (never a valid index), means this, not an index). Left unset, defaults to the first registered data source (equivalent to `data_from = 1L`) for any mark type that takes data at all.
#' @param filter_by A Param/Selection (e.g., from [param()]) to filter this mark's data by.
#' @param data_optimize A flag (default `TRUE`) to enable mark-specific query optimizations for this mark's data; set `FALSE` to disable them (mosaic-spec's `data: {optimize: false}`).
#' @family mark functions
#' @export
vg_mark_spike <- function(spec = NULL, formula = vg_unset, anchor = vg_unset, aria_description = vg_unset, aria_hidden = vg_unset, aria_label = vg_unset, channels = vg_unset, clip = vg_unset, dx = vg_unset, dy = vg_unset, facet = vg_unset, facet_anchor = vg_unset, fill = vg_unset, fill_opacity = vg_unset, filter = vg_unset, frame_anchor = vg_unset, fx = vg_unset, fy = vg_unset, href = vg_unset, image_filter = vg_unset, length = vg_unset, margin = vg_unset, margin_bottom = vg_unset, margin_left = vg_unset, margin_right = vg_unset, margin_top = vg_unset, mix_blend_mode = vg_unset, opacity = vg_unset, paint_order = vg_unset, pointer_events = vg_unset, r = vg_unset, reverse = vg_unset, rotate = vg_unset, select = vg_unset, shape = vg_unset, shape_rendering = vg_unset, sort = vg_unset, stroke = vg_unset, stroke_dasharray = vg_unset, stroke_dashoffset = vg_unset, stroke_linecap = vg_unset, stroke_linejoin = vg_unset, stroke_miterlimit = vg_unset, stroke_opacity = vg_unset, stroke_width = vg_unset, target = vg_unset, tip = vg_unset, title = vg_unset, x = vg_unset, y = vg_unset, ..., data_from = vg_unset, filter_by = vg_unset, data_optimize = vg_unset) {
  vg_mark_(spec, "spike", formula = formula, anchor = anchor, ariaDescription = aria_description, ariaHidden = aria_hidden, ariaLabel = aria_label, channels = channels, clip = clip, dx = dx, dy = dy, facet = facet, facetAnchor = facet_anchor, fill = fill, fillOpacity = fill_opacity, filter = filter, frameAnchor = frame_anchor, fx = fx, fy = fy, href = href, imageFilter = image_filter, length = length, margin = margin, marginBottom = margin_bottom, marginLeft = margin_left, marginRight = margin_right, marginTop = margin_top, mixBlendMode = mix_blend_mode, opacity = opacity, paintOrder = paint_order, pointerEvents = pointer_events, r = r, reverse = reverse, rotate = rotate, select = select, shape = shape, shapeRendering = shape_rendering, sort = sort, stroke = stroke, strokeDasharray = stroke_dasharray, strokeDashoffset = stroke_dashoffset, strokeLinecap = stroke_linecap, strokeLinejoin = stroke_linejoin, strokeMiterlimit = stroke_miterlimit, strokeOpacity = stroke_opacity, strokeWidth = stroke_width, target = target, tip = tip, title = title, x = x, y = y, ..., data_from = data_from, filter_by = filter_by, data_optimize = data_optimize)
}

#' The text mark.
#'
#' See [vg_value_types] for what the `<...>` notation below (`column`, `param()`, ...) means.
#'
#' @param spec A plot fragment or `vgspec` to add this mark to, or `NULL` to start a new plot with just this mark.
#' @param formula A shorthand for this mark's position channels (`x`, `y`, `fx`, `fy`, and paired `x1`/`x2` or `y1`/`y2`), e.g., `Sepal.Length ~ Sepal.Width | ~ Species` for `y = ~Sepal.Length, x = ~Sepal.Width, fx = ~Species`. See [vg_mark()] for the full grammar.
#' @param aria_description `<string | param()>` The aria-description; a constant textual description.
#' @param aria_hidden `<string | param()>` The aria-hidden state; a constant indicating whether the element is exposed to an accessibility API.
#' @param aria_label `<column | literal | transform() | sql()/agg()>` The aria-label; a channel specifying short textual labels representing the value in the accessibility tree.
#' @param channels `<list>` Additional named channels, for example to include in a tooltip.
#' @param clip `<"frame" | "sphere" | boolean | NULL | param()>` How to clip the mark; one of: - *frame* or true - clip to the plot’s frame (inner area) - *sphere* - clip to the projected sphere (*e.g.*, front hemisphere) - null or false - do not clip The *sphere* clip option requires a geographic projection.
#' @param dx `<number | param()>` The horizontal offset in pixels; a constant option.
#' @param dy `<number | param()>` The vertical offset in pixels; a constant option.
#' @param facet `<"auto" | "include" | "exclude" | "super" | boolean | NULL | param()>` Whether to enable or disable faceting; one of: - *auto* (default) - automatically determine if this mark should be faceted - *include* (or true) - draw the subset of the mark’s data in the current facet - *exclude* - draw the subset of the mark’s data *not* in the current facet - *super* - draw this mark in a single frame that covers all facets - null (or false) - repeat this mark’s data across all facets (*i.e.*, no faceting) When a mark uses *super* faceting, it is not allowed to use position scales (*x*, *y*, *fx*, or *fy*); *super* faceting is intended for decorations, such as labels and legends.
#' @param facet_anchor `<"top" | "right" | "bottom" | "left" | "top-left" | "top-right" | "bottom-left" | "bottom-right" | "top-empty" | "right-empty" | "bottom-empty" | "left-empty" | "empty" | NULL | param()>` How to place the mark with respect to facets; one of: - null (default for most marks) - display the mark in each non-empty facet - *top*, *right*, *bottom*, or *left* - display the mark only in facets on the given side - *top-empty*, *right-empty*, *bottom-empty*, or *left-empty* (default for axis marks) - display the mark only in facets that have empty space on the given side: either the margin, or an empty facet - *empty* - display the mark in empty facets only
#' @param fill `<column | literal | transform() | sql()/agg() | list(value=, ...) | param()>` The fill; a constant CSS color string, or a channel typically bound to the *color* scale.
#' @param fill_opacity `<column | literal | transform() | sql()/agg() | list(value=, ...) | param()>` The fill-opacity; a constant number between 0 and 1, or a channel typically bound to the *opacity* scale.
#' @param filter `<column | literal | transform() | sql()/agg()>` Applies a transform to filter the mark’s index according to the given channel values; only truthy values are retained.
#' @param font_family `<string | param()>` The font-family; a constant; defaults to the plot’s font family, which is typically *system-ui*.
#' @param font_size `<column | literal | transform() | sql()/agg() | param()>` The font size in pixels; either a constant or a channel; defaults to the plot’s font size, which is typically 10.
#' @param font_style `<string | param()>` The font style; a constant; defaults to the plot’s font style, which is typically *normal*.
#' @param font_variant `<string | param()>` The font variant; a constant; if the **text** channel contains numbers or dates, defaults to *tabular-nums* to facilitate comparing numbers; otherwise defaults to the plot’s font style, which is typically *normal*.
#' @param font_weight `<string | number | param()>` The font weight; a constant; defaults to the plot’s font weight, which is typically *normal*.
#' @param frame_anchor `<"middle" | "top-left" | "top" | "top-right" | "right" | "bottom-right" | "bottom" | "bottom-left" | "left" | param()>` The frame anchor specifies defaults for **x** and **y**, along with **textAnchor** and **lineAnchor**, based on the plot’s frame; it may be one of the four sides (*top*, *right*, *bottom*, *left*), one of the four corners (*top-left*, *top-right*, *bottom-right*, *bottom-left*), or the *middle* of the frame.
#' @param fx `<column | literal | transform() | sql()/agg()>` The horizontal facet position channel, for mark-level faceting, bound to the *fx* scale.
#' @param fy `<column | literal | transform() | sql()/agg()>` The vertical facet position channel, for mark-level faceting, bound to the *fy* scale.
#' @param href `<column | literal | transform() | sql()/agg()>` The href; a channel specifying URLs for clickable links.
#' @param image_filter `<string | param()>` A CSS filter; a constant string used to adjust the rendering of images, such as *blur(5px)*.
#' @param line_anchor `<"top" | "middle" | "bottom" | param()>` The line anchor controls how text is aligned (typically vertically) relative to its anchor point; it is one of *top*, *bottom*, or *middle*.
#' @param line_height `<number | param()>` The line height in ems; defaults to 1.
#' @param line_width `<number | param()>` The line width in ems (e.g., 10 for about 20 characters); defaults to infinity, disabling wrapping and clipping.
#' @param margin `<number | param()>` Shorthand to set the same default for all four mark margins: **marginTop**, **marginRight**, **marginBottom**, and **marginLeft**; typically defaults to 0, except for axis marks.
#' @param margin_bottom `<number | param()>` The mark’s bottom margin; the minimum distance in pixels between the bottom edges of the inner and outer plot area.
#' @param margin_left `<number | param()>` The mark’s left margin; the minimum distance in pixels between the left edges of the inner and outer plot area.
#' @param margin_right `<number | param()>` The mark’s right margin; the minimum distance in pixels between the right edges of the mark’s inner and outer plot area.
#' @param margin_top `<number | param()>` The mark’s top margin; the minimum distance in pixels between the top edges of the inner and outer plot area.
#' @param mix_blend_mode `<string | param()>` The mix-blend-mode; a constant string specifying how to blend content such as *multiply*.
#' @param monospace `<boolean | param()>` If true, changes the default **fontFamily** to *monospace*, and uses simplified monospaced text metrics calculations.
#' @param opacity `<column | literal | transform() | sql()/agg() | list(value=, ...)>` The opacity; a constant between 0 and 1, or a channel typically bound to the *opacity* scale.
#' @param paint_order `<string | param()>` The paint-order; a constant string specifying the order in which the **fill**, **stroke**, and any markers are drawn; defaults to *normal*, which draws the fill, then stroke, then markers; defaults to *stroke* for the text mark to create a “halo” around text to improve legibility.
#' @param pointer_events `<string | param()>` The pointer-events property; a constant string such as *none*.
#' @param reverse `<boolean | param()>` Applies a transform to reverse the order of the mark’s index, say for reverse input order.
#' @param rotate `<column | literal | transform() | sql()/agg() | param()>` The rotation angle in degrees clockwise; a constant or a channel; defaults to 0°.
#' @param select `<"first" | "last" | "maxX" | "maxY" | "minX" | "minY" | "nearest" | "nearestX" | "nearestY">` Applies a filter transform after data is loaded to highlight selected values only.
#' @param shape_rendering `<string | param()>` The shape-rendering; a constant string such as *crispEdges*.
#' @param sort `<column | literal | transform() | sql()/agg() | list>` Either applies a transform to sort the mark’s index by the specified channel values, or imputes ordinal scale domains from this mark’s channels.
#' @param stroke `<column | literal | transform() | sql()/agg() | list(value=, ...) | param()>` The stroke; a constant CSS color string, or a channel typically bound to the *color* scale.
#' @param stroke_dasharray `<string | number | param()>` The stroke-dasharray; a constant number indicating the length in pixels of alternating dashes and gaps, or a constant string of numbers separated by spaces or commas (_e.g._, *10 2* for dashes of 10 pixels separated by gaps of 2 pixels), or *none* (the default) for no dashing : https://developer.mozilla.org/en-US/docs/Web/SVG/Attribute/stroke-dasharray
#' @param stroke_dashoffset `<string | number | param()>` The stroke-dashoffset; a constant indicating the offset in pixels of the first dash along the stroke; defaults to zero.
#' @param stroke_linecap `<string | param()>` The stroke-linecap; a constant specifying how to cap stroked paths, such as *butt*, *round*, or *square*.
#' @param stroke_linejoin `<string | param()>` The stroke-linejoin; a constant specifying how to join stroked paths, such as *bevel*, *miter*, *miter-clip*, or *round*.
#' @param stroke_miterlimit `<number | param()>` The stroke-miterlimit; a constant number specifying how to limit the length of *miter* joins on stroked paths.
#' @param stroke_opacity `<column | literal | transform() | sql()/agg() | list(value=, ...)>` The stroke-opacity; a constant between 0 and 1, or a channel typically bound to the *opacity* scale.
#' @param stroke_width `<column | literal | transform() | sql()/agg() | list(value=, ...)>` The stroke-width; a constant number in pixels, or a channel.
#' @param target `<string | param()>` The target; a constant string specifying the target window (_e.g._, *_blank*) for clickable links; used in conjunction with the **href** option.
#' @param text `<column | literal | transform() | sql()/agg()>` The text contents channel, possibly with line breaks (\\n, \\r\\n, or \\r).
#' @param text_anchor `<"start" | "middle" | "end" | param()>` The text anchor controls how text is aligned (typically horizontally) relative to its anchor point; it is one of *start*, *end*, or *middle*.
#' @param text_overflow `<NULL | "clip" | "ellipsis" | "clip-start" | "clip-end" | "ellipsis-start" | "ellipsis-middle" | "ellipsis-end" | param()>` How truncate (or wrap) lines of text longer than the given **lineWidth**; one of: - null (default) - preserve overflowing characters (and wrap if needed) - *clip* or *clip-end* - remove characters from the end - *clip-start* - remove characters from the start - *ellipsis* or *ellipsis-end* - replace characters from the end with an ellipsis (…) - *ellipsis-start* - replace characters from the start with an ellipsis (…) - *ellipsis-middle* - replace characters from the middle with an ellipsis (…) If no **title** was specified, if text requires truncation, a title containing the non-truncated text will be implicitly added.
#' @param tip `<boolean | "x" | "y" | "xy" | list | param()>` Whether to generate a tooltip for this mark, and any tip options.
#' @param title `<column | literal | transform() | sql()/agg()>` The title; a channel specifying accessible, short textual descriptions as strings (possibly with newlines).
#' @param x `<column | literal | transform() | sql()/agg() | list(value=, ...)>` The horizontal position (or length) channel, typically bound to the *x* scale.
#' @param y `<column | literal | transform() | sql()/agg() | list(value=, ...)>` The vertical position channel, typically bound to the *y* scale; defaults to the zero-based index of the data 0, 1, 2, ….
#' @param z `<column | literal | transform() | sql()/agg()>` An optional ordinal channel for grouping data into (possibly stacked) series to be drawn as separate areas; defaults to **fill** if a channel, or **stroke** if a channel.
#' @param ... Additional options or plot-level attributes.
#' @param data_from The name of the data source this mark reads from (see [vg_data()]); a length-1 nonzero R integer (`1L`, `-1L`, ...; note the `L`) giving a 1-based index into the spec's registered data sources instead, negative counting from the end (e.g., `-1L` for the most recently registered one); or a literal vector of values to use as inline data directly (mosaic-spec's `"data": [...]` shorthand, e.g., for a single reference line -- a bare double like `0`/`c(0)`, or `0L` itself (never a valid index), means this, not an index). Left unset, defaults to the first registered data source (equivalent to `data_from = 1L`) for any mark type that takes data at all.
#' @param filter_by A Param/Selection (e.g., from [param()]) to filter this mark's data by.
#' @param data_optimize A flag (default `TRUE`) to enable mark-specific query optimizations for this mark's data; set `FALSE` to disable them (mosaic-spec's `data: {optimize: false}`).
#' @family mark functions
#' @export
vg_mark_text <- function(spec = NULL, formula = vg_unset, aria_description = vg_unset, aria_hidden = vg_unset, aria_label = vg_unset, channels = vg_unset, clip = vg_unset, dx = vg_unset, dy = vg_unset, facet = vg_unset, facet_anchor = vg_unset, fill = vg_unset, fill_opacity = vg_unset, filter = vg_unset, font_family = vg_unset, font_size = vg_unset, font_style = vg_unset, font_variant = vg_unset, font_weight = vg_unset, frame_anchor = vg_unset, fx = vg_unset, fy = vg_unset, href = vg_unset, image_filter = vg_unset, line_anchor = vg_unset, line_height = vg_unset, line_width = vg_unset, margin = vg_unset, margin_bottom = vg_unset, margin_left = vg_unset, margin_right = vg_unset, margin_top = vg_unset, mix_blend_mode = vg_unset, monospace = vg_unset, opacity = vg_unset, paint_order = vg_unset, pointer_events = vg_unset, reverse = vg_unset, rotate = vg_unset, select = vg_unset, shape_rendering = vg_unset, sort = vg_unset, stroke = vg_unset, stroke_dasharray = vg_unset, stroke_dashoffset = vg_unset, stroke_linecap = vg_unset, stroke_linejoin = vg_unset, stroke_miterlimit = vg_unset, stroke_opacity = vg_unset, stroke_width = vg_unset, target = vg_unset, text = vg_unset, text_anchor = vg_unset, text_overflow = vg_unset, tip = vg_unset, title = vg_unset, x = vg_unset, y = vg_unset, z = vg_unset, ..., data_from = vg_unset, filter_by = vg_unset, data_optimize = vg_unset) {
  vg_mark_(spec, "text", formula = formula, ariaDescription = aria_description, ariaHidden = aria_hidden, ariaLabel = aria_label, channels = channels, clip = clip, dx = dx, dy = dy, facet = facet, facetAnchor = facet_anchor, fill = fill, fillOpacity = fill_opacity, filter = filter, fontFamily = font_family, fontSize = font_size, fontStyle = font_style, fontVariant = font_variant, fontWeight = font_weight, frameAnchor = frame_anchor, fx = fx, fy = fy, href = href, imageFilter = image_filter, lineAnchor = line_anchor, lineHeight = line_height, lineWidth = line_width, margin = margin, marginBottom = margin_bottom, marginLeft = margin_left, marginRight = margin_right, marginTop = margin_top, mixBlendMode = mix_blend_mode, monospace = monospace, opacity = opacity, paintOrder = paint_order, pointerEvents = pointer_events, reverse = reverse, rotate = rotate, select = select, shapeRendering = shape_rendering, sort = sort, stroke = stroke, strokeDasharray = stroke_dasharray, strokeDashoffset = stroke_dashoffset, strokeLinecap = stroke_linecap, strokeLinejoin = stroke_linejoin, strokeMiterlimit = stroke_miterlimit, strokeOpacity = stroke_opacity, strokeWidth = stroke_width, target = target, text = text, textAnchor = text_anchor, textOverflow = text_overflow, tip = tip, title = title, x = x, y = y, z = z, ..., data_from = data_from, filter_by = filter_by, data_optimize = data_optimize)
}

#' The textX mark.
#'
#' See [vg_value_types] for what the `<...>` notation below (`column`, `param()`, ...) means.
#'
#' @param spec A plot fragment or `vgspec` to add this mark to, or `NULL` to start a new plot with just this mark.
#' @param formula A shorthand for this mark's position channels (`x`, `y`, `fx`, `fy`, and paired `x1`/`x2` or `y1`/`y2`), e.g., `Sepal.Length ~ Sepal.Width | ~ Species` for `y = ~Sepal.Length, x = ~Sepal.Width, fx = ~Species`. See [vg_mark()] for the full grammar.
#' @param aria_description `<string | param()>` The aria-description; a constant textual description.
#' @param aria_hidden `<string | param()>` The aria-hidden state; a constant indicating whether the element is exposed to an accessibility API.
#' @param aria_label `<column | literal | transform() | sql()/agg()>` The aria-label; a channel specifying short textual labels representing the value in the accessibility tree.
#' @param channels `<list>` Additional named channels, for example to include in a tooltip.
#' @param clip `<"frame" | "sphere" | boolean | NULL | param()>` How to clip the mark; one of: - *frame* or true - clip to the plot’s frame (inner area) - *sphere* - clip to the projected sphere (*e.g.*, front hemisphere) - null or false - do not clip The *sphere* clip option requires a geographic projection.
#' @param dx `<number | param()>` The horizontal offset in pixels; a constant option.
#' @param dy `<number | param()>` The vertical offset in pixels; a constant option.
#' @param facet `<"auto" | "include" | "exclude" | "super" | boolean | NULL | param()>` Whether to enable or disable faceting; one of: - *auto* (default) - automatically determine if this mark should be faceted - *include* (or true) - draw the subset of the mark’s data in the current facet - *exclude* - draw the subset of the mark’s data *not* in the current facet - *super* - draw this mark in a single frame that covers all facets - null (or false) - repeat this mark’s data across all facets (*i.e.*, no faceting) When a mark uses *super* faceting, it is not allowed to use position scales (*x*, *y*, *fx*, or *fy*); *super* faceting is intended for decorations, such as labels and legends.
#' @param facet_anchor `<"top" | "right" | "bottom" | "left" | "top-left" | "top-right" | "bottom-left" | "bottom-right" | "top-empty" | "right-empty" | "bottom-empty" | "left-empty" | "empty" | NULL | param()>` How to place the mark with respect to facets; one of: - null (default for most marks) - display the mark in each non-empty facet - *top*, *right*, *bottom*, or *left* - display the mark only in facets on the given side - *top-empty*, *right-empty*, *bottom-empty*, or *left-empty* (default for axis marks) - display the mark only in facets that have empty space on the given side: either the margin, or an empty facet - *empty* - display the mark in empty facets only
#' @param fill `<column | literal | transform() | sql()/agg() | list(value=, ...) | param()>` The fill; a constant CSS color string, or a channel typically bound to the *color* scale.
#' @param fill_opacity `<column | literal | transform() | sql()/agg() | list(value=, ...) | param()>` The fill-opacity; a constant number between 0 and 1, or a channel typically bound to the *opacity* scale.
#' @param filter `<column | literal | transform() | sql()/agg()>` Applies a transform to filter the mark’s index according to the given channel values; only truthy values are retained.
#' @param font_family `<string | param()>` The font-family; a constant; defaults to the plot’s font family, which is typically *system-ui*.
#' @param font_size `<column | literal | transform() | sql()/agg() | param()>` The font size in pixels; either a constant or a channel; defaults to the plot’s font size, which is typically 10.
#' @param font_style `<string | param()>` The font style; a constant; defaults to the plot’s font style, which is typically *normal*.
#' @param font_variant `<string | param()>` The font variant; a constant; if the **text** channel contains numbers or dates, defaults to *tabular-nums* to facilitate comparing numbers; otherwise defaults to the plot’s font style, which is typically *normal*.
#' @param font_weight `<string | number | param()>` The font weight; a constant; defaults to the plot’s font weight, which is typically *normal*.
#' @param frame_anchor `<"middle" | "top-left" | "top" | "top-right" | "right" | "bottom-right" | "bottom" | "bottom-left" | "left" | param()>` The frame anchor specifies defaults for **x** and **y**, along with **textAnchor** and **lineAnchor**, based on the plot’s frame; it may be one of the four sides (*top*, *right*, *bottom*, *left*), one of the four corners (*top-left*, *top-right*, *bottom-right*, *bottom-left*), or the *middle* of the frame.
#' @param fx `<column | literal | transform() | sql()/agg()>` The horizontal facet position channel, for mark-level faceting, bound to the *fx* scale.
#' @param fy `<column | literal | transform() | sql()/agg()>` The vertical facet position channel, for mark-level faceting, bound to the *fy* scale.
#' @param href `<column | literal | transform() | sql()/agg()>` The href; a channel specifying URLs for clickable links.
#' @param image_filter `<string | param()>` A CSS filter; a constant string used to adjust the rendering of images, such as *blur(5px)*.
#' @param interval `<"day"/"week"/"month"/... | number | param()>` Enforces uniformity for data at regular intervals, such as integer values or daily samples.
#' @param line_anchor `<"top" | "middle" | "bottom" | param()>` The line anchor controls how text is aligned (typically vertically) relative to its anchor point; it is one of *top*, *bottom*, or *middle*.
#' @param line_height `<number | param()>` The line height in ems; defaults to 1.
#' @param line_width `<number | param()>` The line width in ems (e.g., 10 for about 20 characters); defaults to infinity, disabling wrapping and clipping.
#' @param margin `<number | param()>` Shorthand to set the same default for all four mark margins: **marginTop**, **marginRight**, **marginBottom**, and **marginLeft**; typically defaults to 0, except for axis marks.
#' @param margin_bottom `<number | param()>` The mark’s bottom margin; the minimum distance in pixels between the bottom edges of the inner and outer plot area.
#' @param margin_left `<number | param()>` The mark’s left margin; the minimum distance in pixels between the left edges of the inner and outer plot area.
#' @param margin_right `<number | param()>` The mark’s right margin; the minimum distance in pixels between the right edges of the mark’s inner and outer plot area.
#' @param margin_top `<number | param()>` The mark’s top margin; the minimum distance in pixels between the top edges of the inner and outer plot area.
#' @param mix_blend_mode `<string | param()>` The mix-blend-mode; a constant string specifying how to blend content such as *multiply*.
#' @param monospace `<boolean | param()>` If true, changes the default **fontFamily** to *monospace*, and uses simplified monospaced text metrics calculations.
#' @param opacity `<column | literal | transform() | sql()/agg() | list(value=, ...)>` The opacity; a constant between 0 and 1, or a channel typically bound to the *opacity* scale.
#' @param paint_order `<string | param()>` The paint-order; a constant string specifying the order in which the **fill**, **stroke**, and any markers are drawn; defaults to *normal*, which draws the fill, then stroke, then markers; defaults to *stroke* for the text mark to create a “halo” around text to improve legibility.
#' @param pointer_events `<string | param()>` The pointer-events property; a constant string such as *none*.
#' @param reverse `<boolean | param()>` Applies a transform to reverse the order of the mark’s index, say for reverse input order.
#' @param rotate `<column | literal | transform() | sql()/agg() | param()>` The rotation angle in degrees clockwise; a constant or a channel; defaults to 0°.
#' @param select `<"first" | "last" | "maxX" | "maxY" | "minX" | "minY" | "nearest" | "nearestX" | "nearestY">` Applies a filter transform after data is loaded to highlight selected values only.
#' @param shape_rendering `<string | param()>` The shape-rendering; a constant string such as *crispEdges*.
#' @param sort `<column | literal | transform() | sql()/agg() | list>` Either applies a transform to sort the mark’s index by the specified channel values, or imputes ordinal scale domains from this mark’s channels.
#' @param stroke `<column | literal | transform() | sql()/agg() | list(value=, ...) | param()>` The stroke; a constant CSS color string, or a channel typically bound to the *color* scale.
#' @param stroke_dasharray `<string | number | param()>` The stroke-dasharray; a constant number indicating the length in pixels of alternating dashes and gaps, or a constant string of numbers separated by spaces or commas (_e.g._, *10 2* for dashes of 10 pixels separated by gaps of 2 pixels), or *none* (the default) for no dashing : https://developer.mozilla.org/en-US/docs/Web/SVG/Attribute/stroke-dasharray
#' @param stroke_dashoffset `<string | number | param()>` The stroke-dashoffset; a constant indicating the offset in pixels of the first dash along the stroke; defaults to zero.
#' @param stroke_linecap `<string | param()>` The stroke-linecap; a constant specifying how to cap stroked paths, such as *butt*, *round*, or *square*.
#' @param stroke_linejoin `<string | param()>` The stroke-linejoin; a constant specifying how to join stroked paths, such as *bevel*, *miter*, *miter-clip*, or *round*.
#' @param stroke_miterlimit `<number | param()>` The stroke-miterlimit; a constant number specifying how to limit the length of *miter* joins on stroked paths.
#' @param stroke_opacity `<column | literal | transform() | sql()/agg() | list(value=, ...)>` The stroke-opacity; a constant between 0 and 1, or a channel typically bound to the *opacity* scale.
#' @param stroke_width `<column | literal | transform() | sql()/agg() | list(value=, ...)>` The stroke-width; a constant number in pixels, or a channel.
#' @param target `<string | param()>` The target; a constant string specifying the target window (_e.g._, *_blank*) for clickable links; used in conjunction with the **href** option.
#' @param text `<column | literal | transform() | sql()/agg()>` The text contents channel, possibly with line breaks (\\n, \\r\\n, or \\r).
#' @param text_anchor `<"start" | "middle" | "end" | param()>` The text anchor controls how text is aligned (typically horizontally) relative to its anchor point; it is one of *start*, *end*, or *middle*.
#' @param text_overflow `<NULL | "clip" | "ellipsis" | "clip-start" | "clip-end" | "ellipsis-start" | "ellipsis-middle" | "ellipsis-end" | param()>` How truncate (or wrap) lines of text longer than the given **lineWidth**; one of: - null (default) - preserve overflowing characters (and wrap if needed) - *clip* or *clip-end* - remove characters from the end - *clip-start* - remove characters from the start - *ellipsis* or *ellipsis-end* - replace characters from the end with an ellipsis (…) - *ellipsis-start* - replace characters from the start with an ellipsis (…) - *ellipsis-middle* - replace characters from the middle with an ellipsis (…) If no **title** was specified, if text requires truncation, a title containing the non-truncated text will be implicitly added.
#' @param tip `<boolean | "x" | "y" | "xy" | list | param()>` Whether to generate a tooltip for this mark, and any tip options.
#' @param title `<column | literal | transform() | sql()/agg()>` The title; a channel specifying accessible, short textual descriptions as strings (possibly with newlines).
#' @param x `<column | literal | transform() | sql()/agg() | list(value=, ...)>` The horizontal position (or length) channel, typically bound to the *x* scale.
#' @param y `<column | literal | transform() | sql()/agg() | list(value=, ...)>` The vertical position channel, typically bound to the *y* scale; defaults to the zero-based index of the data 0, 1, 2, ….
#' @param z `<column | literal | transform() | sql()/agg()>` An optional ordinal channel for grouping data into (possibly stacked) series to be drawn as separate areas; defaults to **fill** if a channel, or **stroke** if a channel.
#' @param ... Additional options or plot-level attributes.
#' @param data_from The name of the data source this mark reads from (see [vg_data()]); a length-1 nonzero R integer (`1L`, `-1L`, ...; note the `L`) giving a 1-based index into the spec's registered data sources instead, negative counting from the end (e.g., `-1L` for the most recently registered one); or a literal vector of values to use as inline data directly (mosaic-spec's `"data": [...]` shorthand, e.g., for a single reference line -- a bare double like `0`/`c(0)`, or `0L` itself (never a valid index), means this, not an index). Left unset, defaults to the first registered data source (equivalent to `data_from = 1L`) for any mark type that takes data at all.
#' @param filter_by A Param/Selection (e.g., from [param()]) to filter this mark's data by.
#' @param data_optimize A flag (default `TRUE`) to enable mark-specific query optimizations for this mark's data; set `FALSE` to disable them (mosaic-spec's `data: {optimize: false}`).
#' @family mark functions
#' @export
vg_mark_text_x <- function(spec = NULL, formula = vg_unset, aria_description = vg_unset, aria_hidden = vg_unset, aria_label = vg_unset, channels = vg_unset, clip = vg_unset, dx = vg_unset, dy = vg_unset, facet = vg_unset, facet_anchor = vg_unset, fill = vg_unset, fill_opacity = vg_unset, filter = vg_unset, font_family = vg_unset, font_size = vg_unset, font_style = vg_unset, font_variant = vg_unset, font_weight = vg_unset, frame_anchor = vg_unset, fx = vg_unset, fy = vg_unset, href = vg_unset, image_filter = vg_unset, interval = vg_unset, line_anchor = vg_unset, line_height = vg_unset, line_width = vg_unset, margin = vg_unset, margin_bottom = vg_unset, margin_left = vg_unset, margin_right = vg_unset, margin_top = vg_unset, mix_blend_mode = vg_unset, monospace = vg_unset, opacity = vg_unset, paint_order = vg_unset, pointer_events = vg_unset, reverse = vg_unset, rotate = vg_unset, select = vg_unset, shape_rendering = vg_unset, sort = vg_unset, stroke = vg_unset, stroke_dasharray = vg_unset, stroke_dashoffset = vg_unset, stroke_linecap = vg_unset, stroke_linejoin = vg_unset, stroke_miterlimit = vg_unset, stroke_opacity = vg_unset, stroke_width = vg_unset, target = vg_unset, text = vg_unset, text_anchor = vg_unset, text_overflow = vg_unset, tip = vg_unset, title = vg_unset, x = vg_unset, y = vg_unset, z = vg_unset, ..., data_from = vg_unset, filter_by = vg_unset, data_optimize = vg_unset) {
  vg_mark_(spec, "textX", formula = formula, ariaDescription = aria_description, ariaHidden = aria_hidden, ariaLabel = aria_label, channels = channels, clip = clip, dx = dx, dy = dy, facet = facet, facetAnchor = facet_anchor, fill = fill, fillOpacity = fill_opacity, filter = filter, fontFamily = font_family, fontSize = font_size, fontStyle = font_style, fontVariant = font_variant, fontWeight = font_weight, frameAnchor = frame_anchor, fx = fx, fy = fy, href = href, imageFilter = image_filter, interval = interval, lineAnchor = line_anchor, lineHeight = line_height, lineWidth = line_width, margin = margin, marginBottom = margin_bottom, marginLeft = margin_left, marginRight = margin_right, marginTop = margin_top, mixBlendMode = mix_blend_mode, monospace = monospace, opacity = opacity, paintOrder = paint_order, pointerEvents = pointer_events, reverse = reverse, rotate = rotate, select = select, shapeRendering = shape_rendering, sort = sort, stroke = stroke, strokeDasharray = stroke_dasharray, strokeDashoffset = stroke_dashoffset, strokeLinecap = stroke_linecap, strokeLinejoin = stroke_linejoin, strokeMiterlimit = stroke_miterlimit, strokeOpacity = stroke_opacity, strokeWidth = stroke_width, target = target, text = text, textAnchor = text_anchor, textOverflow = text_overflow, tip = tip, title = title, x = x, y = y, z = z, ..., data_from = data_from, filter_by = filter_by, data_optimize = data_optimize)
}

#' The textY mark.
#'
#' See [vg_value_types] for what the `<...>` notation below (`column`, `param()`, ...) means.
#'
#' @param spec A plot fragment or `vgspec` to add this mark to, or `NULL` to start a new plot with just this mark.
#' @param formula A shorthand for this mark's position channels (`x`, `y`, `fx`, `fy`, and paired `x1`/`x2` or `y1`/`y2`), e.g., `Sepal.Length ~ Sepal.Width | ~ Species` for `y = ~Sepal.Length, x = ~Sepal.Width, fx = ~Species`. See [vg_mark()] for the full grammar.
#' @param aria_description `<string | param()>` The aria-description; a constant textual description.
#' @param aria_hidden `<string | param()>` The aria-hidden state; a constant indicating whether the element is exposed to an accessibility API.
#' @param aria_label `<column | literal | transform() | sql()/agg()>` The aria-label; a channel specifying short textual labels representing the value in the accessibility tree.
#' @param channels `<list>` Additional named channels, for example to include in a tooltip.
#' @param clip `<"frame" | "sphere" | boolean | NULL | param()>` How to clip the mark; one of: - *frame* or true - clip to the plot’s frame (inner area) - *sphere* - clip to the projected sphere (*e.g.*, front hemisphere) - null or false - do not clip The *sphere* clip option requires a geographic projection.
#' @param dx `<number | param()>` The horizontal offset in pixels; a constant option.
#' @param dy `<number | param()>` The vertical offset in pixels; a constant option.
#' @param facet `<"auto" | "include" | "exclude" | "super" | boolean | NULL | param()>` Whether to enable or disable faceting; one of: - *auto* (default) - automatically determine if this mark should be faceted - *include* (or true) - draw the subset of the mark’s data in the current facet - *exclude* - draw the subset of the mark’s data *not* in the current facet - *super* - draw this mark in a single frame that covers all facets - null (or false) - repeat this mark’s data across all facets (*i.e.*, no faceting) When a mark uses *super* faceting, it is not allowed to use position scales (*x*, *y*, *fx*, or *fy*); *super* faceting is intended for decorations, such as labels and legends.
#' @param facet_anchor `<"top" | "right" | "bottom" | "left" | "top-left" | "top-right" | "bottom-left" | "bottom-right" | "top-empty" | "right-empty" | "bottom-empty" | "left-empty" | "empty" | NULL | param()>` How to place the mark with respect to facets; one of: - null (default for most marks) - display the mark in each non-empty facet - *top*, *right*, *bottom*, or *left* - display the mark only in facets on the given side - *top-empty*, *right-empty*, *bottom-empty*, or *left-empty* (default for axis marks) - display the mark only in facets that have empty space on the given side: either the margin, or an empty facet - *empty* - display the mark in empty facets only
#' @param fill `<column | literal | transform() | sql()/agg() | list(value=, ...) | param()>` The fill; a constant CSS color string, or a channel typically bound to the *color* scale.
#' @param fill_opacity `<column | literal | transform() | sql()/agg() | list(value=, ...) | param()>` The fill-opacity; a constant number between 0 and 1, or a channel typically bound to the *opacity* scale.
#' @param filter `<column | literal | transform() | sql()/agg()>` Applies a transform to filter the mark’s index according to the given channel values; only truthy values are retained.
#' @param font_family `<string | param()>` The font-family; a constant; defaults to the plot’s font family, which is typically *system-ui*.
#' @param font_size `<column | literal | transform() | sql()/agg() | param()>` The font size in pixels; either a constant or a channel; defaults to the plot’s font size, which is typically 10.
#' @param font_style `<string | param()>` The font style; a constant; defaults to the plot’s font style, which is typically *normal*.
#' @param font_variant `<string | param()>` The font variant; a constant; if the **text** channel contains numbers or dates, defaults to *tabular-nums* to facilitate comparing numbers; otherwise defaults to the plot’s font style, which is typically *normal*.
#' @param font_weight `<string | number | param()>` The font weight; a constant; defaults to the plot’s font weight, which is typically *normal*.
#' @param frame_anchor `<"middle" | "top-left" | "top" | "top-right" | "right" | "bottom-right" | "bottom" | "bottom-left" | "left" | param()>` The frame anchor specifies defaults for **x** and **y**, along with **textAnchor** and **lineAnchor**, based on the plot’s frame; it may be one of the four sides (*top*, *right*, *bottom*, *left*), one of the four corners (*top-left*, *top-right*, *bottom-right*, *bottom-left*), or the *middle* of the frame.
#' @param fx `<column | literal | transform() | sql()/agg()>` The horizontal facet position channel, for mark-level faceting, bound to the *fx* scale.
#' @param fy `<column | literal | transform() | sql()/agg()>` The vertical facet position channel, for mark-level faceting, bound to the *fy* scale.
#' @param href `<column | literal | transform() | sql()/agg()>` The href; a channel specifying URLs for clickable links.
#' @param image_filter `<string | param()>` A CSS filter; a constant string used to adjust the rendering of images, such as *blur(5px)*.
#' @param interval `<"day"/"week"/"month"/... | number | param()>` Enforces uniformity for data at regular intervals, such as integer values or daily samples.
#' @param line_anchor `<"top" | "middle" | "bottom" | param()>` The line anchor controls how text is aligned (typically vertically) relative to its anchor point; it is one of *top*, *bottom*, or *middle*.
#' @param line_height `<number | param()>` The line height in ems; defaults to 1.
#' @param line_width `<number | param()>` The line width in ems (e.g., 10 for about 20 characters); defaults to infinity, disabling wrapping and clipping.
#' @param margin `<number | param()>` Shorthand to set the same default for all four mark margins: **marginTop**, **marginRight**, **marginBottom**, and **marginLeft**; typically defaults to 0, except for axis marks.
#' @param margin_bottom `<number | param()>` The mark’s bottom margin; the minimum distance in pixels between the bottom edges of the inner and outer plot area.
#' @param margin_left `<number | param()>` The mark’s left margin; the minimum distance in pixels between the left edges of the inner and outer plot area.
#' @param margin_right `<number | param()>` The mark’s right margin; the minimum distance in pixels between the right edges of the mark’s inner and outer plot area.
#' @param margin_top `<number | param()>` The mark’s top margin; the minimum distance in pixels between the top edges of the inner and outer plot area.
#' @param mix_blend_mode `<string | param()>` The mix-blend-mode; a constant string specifying how to blend content such as *multiply*.
#' @param monospace `<boolean | param()>` If true, changes the default **fontFamily** to *monospace*, and uses simplified monospaced text metrics calculations.
#' @param opacity `<column | literal | transform() | sql()/agg() | list(value=, ...)>` The opacity; a constant between 0 and 1, or a channel typically bound to the *opacity* scale.
#' @param paint_order `<string | param()>` The paint-order; a constant string specifying the order in which the **fill**, **stroke**, and any markers are drawn; defaults to *normal*, which draws the fill, then stroke, then markers; defaults to *stroke* for the text mark to create a “halo” around text to improve legibility.
#' @param pointer_events `<string | param()>` The pointer-events property; a constant string such as *none*.
#' @param reverse `<boolean | param()>` Applies a transform to reverse the order of the mark’s index, say for reverse input order.
#' @param rotate `<column | literal | transform() | sql()/agg() | param()>` The rotation angle in degrees clockwise; a constant or a channel; defaults to 0°.
#' @param select `<"first" | "last" | "maxX" | "maxY" | "minX" | "minY" | "nearest" | "nearestX" | "nearestY">` Applies a filter transform after data is loaded to highlight selected values only.
#' @param shape_rendering `<string | param()>` The shape-rendering; a constant string such as *crispEdges*.
#' @param sort `<column | literal | transform() | sql()/agg() | list>` Either applies a transform to sort the mark’s index by the specified channel values, or imputes ordinal scale domains from this mark’s channels.
#' @param stroke `<column | literal | transform() | sql()/agg() | list(value=, ...) | param()>` The stroke; a constant CSS color string, or a channel typically bound to the *color* scale.
#' @param stroke_dasharray `<string | number | param()>` The stroke-dasharray; a constant number indicating the length in pixels of alternating dashes and gaps, or a constant string of numbers separated by spaces or commas (_e.g._, *10 2* for dashes of 10 pixels separated by gaps of 2 pixels), or *none* (the default) for no dashing : https://developer.mozilla.org/en-US/docs/Web/SVG/Attribute/stroke-dasharray
#' @param stroke_dashoffset `<string | number | param()>` The stroke-dashoffset; a constant indicating the offset in pixels of the first dash along the stroke; defaults to zero.
#' @param stroke_linecap `<string | param()>` The stroke-linecap; a constant specifying how to cap stroked paths, such as *butt*, *round*, or *square*.
#' @param stroke_linejoin `<string | param()>` The stroke-linejoin; a constant specifying how to join stroked paths, such as *bevel*, *miter*, *miter-clip*, or *round*.
#' @param stroke_miterlimit `<number | param()>` The stroke-miterlimit; a constant number specifying how to limit the length of *miter* joins on stroked paths.
#' @param stroke_opacity `<column | literal | transform() | sql()/agg() | list(value=, ...)>` The stroke-opacity; a constant between 0 and 1, or a channel typically bound to the *opacity* scale.
#' @param stroke_width `<column | literal | transform() | sql()/agg() | list(value=, ...)>` The stroke-width; a constant number in pixels, or a channel.
#' @param target `<string | param()>` The target; a constant string specifying the target window (_e.g._, *_blank*) for clickable links; used in conjunction with the **href** option.
#' @param text `<column | literal | transform() | sql()/agg()>` The text contents channel, possibly with line breaks (\\n, \\r\\n, or \\r).
#' @param text_anchor `<"start" | "middle" | "end" | param()>` The text anchor controls how text is aligned (typically horizontally) relative to its anchor point; it is one of *start*, *end*, or *middle*.
#' @param text_overflow `<NULL | "clip" | "ellipsis" | "clip-start" | "clip-end" | "ellipsis-start" | "ellipsis-middle" | "ellipsis-end" | param()>` How truncate (or wrap) lines of text longer than the given **lineWidth**; one of: - null (default) - preserve overflowing characters (and wrap if needed) - *clip* or *clip-end* - remove characters from the end - *clip-start* - remove characters from the start - *ellipsis* or *ellipsis-end* - replace characters from the end with an ellipsis (…) - *ellipsis-start* - replace characters from the start with an ellipsis (…) - *ellipsis-middle* - replace characters from the middle with an ellipsis (…) If no **title** was specified, if text requires truncation, a title containing the non-truncated text will be implicitly added.
#' @param tip `<boolean | "x" | "y" | "xy" | list | param()>` Whether to generate a tooltip for this mark, and any tip options.
#' @param title `<column | literal | transform() | sql()/agg()>` The title; a channel specifying accessible, short textual descriptions as strings (possibly with newlines).
#' @param x `<column | literal | transform() | sql()/agg() | list(value=, ...)>` The horizontal position (or length) channel, typically bound to the *x* scale.
#' @param y `<column | literal | transform() | sql()/agg() | list(value=, ...)>` The vertical position channel, typically bound to the *y* scale; defaults to the zero-based index of the data 0, 1, 2, ….
#' @param z `<column | literal | transform() | sql()/agg()>` An optional ordinal channel for grouping data into (possibly stacked) series to be drawn as separate areas; defaults to **fill** if a channel, or **stroke** if a channel.
#' @param ... Additional options or plot-level attributes.
#' @param data_from The name of the data source this mark reads from (see [vg_data()]); a length-1 nonzero R integer (`1L`, `-1L`, ...; note the `L`) giving a 1-based index into the spec's registered data sources instead, negative counting from the end (e.g., `-1L` for the most recently registered one); or a literal vector of values to use as inline data directly (mosaic-spec's `"data": [...]` shorthand, e.g., for a single reference line -- a bare double like `0`/`c(0)`, or `0L` itself (never a valid index), means this, not an index). Left unset, defaults to the first registered data source (equivalent to `data_from = 1L`) for any mark type that takes data at all.
#' @param filter_by A Param/Selection (e.g., from [param()]) to filter this mark's data by.
#' @param data_optimize A flag (default `TRUE`) to enable mark-specific query optimizations for this mark's data; set `FALSE` to disable them (mosaic-spec's `data: {optimize: false}`).
#' @family mark functions
#' @export
vg_mark_text_y <- function(spec = NULL, formula = vg_unset, aria_description = vg_unset, aria_hidden = vg_unset, aria_label = vg_unset, channels = vg_unset, clip = vg_unset, dx = vg_unset, dy = vg_unset, facet = vg_unset, facet_anchor = vg_unset, fill = vg_unset, fill_opacity = vg_unset, filter = vg_unset, font_family = vg_unset, font_size = vg_unset, font_style = vg_unset, font_variant = vg_unset, font_weight = vg_unset, frame_anchor = vg_unset, fx = vg_unset, fy = vg_unset, href = vg_unset, image_filter = vg_unset, interval = vg_unset, line_anchor = vg_unset, line_height = vg_unset, line_width = vg_unset, margin = vg_unset, margin_bottom = vg_unset, margin_left = vg_unset, margin_right = vg_unset, margin_top = vg_unset, mix_blend_mode = vg_unset, monospace = vg_unset, opacity = vg_unset, paint_order = vg_unset, pointer_events = vg_unset, reverse = vg_unset, rotate = vg_unset, select = vg_unset, shape_rendering = vg_unset, sort = vg_unset, stroke = vg_unset, stroke_dasharray = vg_unset, stroke_dashoffset = vg_unset, stroke_linecap = vg_unset, stroke_linejoin = vg_unset, stroke_miterlimit = vg_unset, stroke_opacity = vg_unset, stroke_width = vg_unset, target = vg_unset, text = vg_unset, text_anchor = vg_unset, text_overflow = vg_unset, tip = vg_unset, title = vg_unset, x = vg_unset, y = vg_unset, z = vg_unset, ..., data_from = vg_unset, filter_by = vg_unset, data_optimize = vg_unset) {
  vg_mark_(spec, "textY", formula = formula, ariaDescription = aria_description, ariaHidden = aria_hidden, ariaLabel = aria_label, channels = channels, clip = clip, dx = dx, dy = dy, facet = facet, facetAnchor = facet_anchor, fill = fill, fillOpacity = fill_opacity, filter = filter, fontFamily = font_family, fontSize = font_size, fontStyle = font_style, fontVariant = font_variant, fontWeight = font_weight, frameAnchor = frame_anchor, fx = fx, fy = fy, href = href, imageFilter = image_filter, interval = interval, lineAnchor = line_anchor, lineHeight = line_height, lineWidth = line_width, margin = margin, marginBottom = margin_bottom, marginLeft = margin_left, marginRight = margin_right, marginTop = margin_top, mixBlendMode = mix_blend_mode, monospace = monospace, opacity = opacity, paintOrder = paint_order, pointerEvents = pointer_events, reverse = reverse, rotate = rotate, select = select, shapeRendering = shape_rendering, sort = sort, stroke = stroke, strokeDasharray = stroke_dasharray, strokeDashoffset = stroke_dashoffset, strokeLinecap = stroke_linecap, strokeLinejoin = stroke_linejoin, strokeMiterlimit = stroke_miterlimit, strokeOpacity = stroke_opacity, strokeWidth = stroke_width, target = target, text = text, textAnchor = text_anchor, textOverflow = text_overflow, tip = tip, title = title, x = x, y = y, z = z, ..., data_from = data_from, filter_by = filter_by, data_optimize = data_optimize)
}

#' The tickX mark.
#'
#' See [vg_value_types] for what the `<...>` notation below (`column`, `param()`, ...) means.
#'
#' @param spec A plot fragment or `vgspec` to add this mark to, or `NULL` to start a new plot with just this mark.
#' @param formula A shorthand for this mark's position channels (`x`, `y`, `fx`, `fy`, and paired `x1`/`x2` or `y1`/`y2`), e.g., `Sepal.Length ~ Sepal.Width | ~ Species` for `y = ~Sepal.Length, x = ~Sepal.Width, fx = ~Species`. See [vg_mark()] for the full grammar.
#' @param aria_description `<string | param()>` The aria-description; a constant textual description.
#' @param aria_hidden `<string | param()>` The aria-hidden state; a constant indicating whether the element is exposed to an accessibility API.
#' @param aria_label `<column | literal | transform() | sql()/agg()>` The aria-label; a channel specifying short textual labels representing the value in the accessibility tree.
#' @param channels `<list>` Additional named channels, for example to include in a tooltip.
#' @param clip `<"frame" | "sphere" | boolean | NULL | param()>` How to clip the mark; one of: - *frame* or true - clip to the plot’s frame (inner area) - *sphere* - clip to the projected sphere (*e.g.*, front hemisphere) - null or false - do not clip The *sphere* clip option requires a geographic projection.
#' @param dx `<number | param()>` The horizontal offset in pixels; a constant option.
#' @param dy `<number | param()>` The vertical offset in pixels; a constant option.
#' @param facet `<"auto" | "include" | "exclude" | "super" | boolean | NULL | param()>` Whether to enable or disable faceting; one of: - *auto* (default) - automatically determine if this mark should be faceted - *include* (or true) - draw the subset of the mark’s data in the current facet - *exclude* - draw the subset of the mark’s data *not* in the current facet - *super* - draw this mark in a single frame that covers all facets - null (or false) - repeat this mark’s data across all facets (*i.e.*, no faceting) When a mark uses *super* faceting, it is not allowed to use position scales (*x*, *y*, *fx*, or *fy*); *super* faceting is intended for decorations, such as labels and legends.
#' @param facet_anchor `<"top" | "right" | "bottom" | "left" | "top-left" | "top-right" | "bottom-left" | "bottom-right" | "top-empty" | "right-empty" | "bottom-empty" | "left-empty" | "empty" | NULL | param()>` How to place the mark with respect to facets; one of: - null (default for most marks) - display the mark in each non-empty facet - *top*, *right*, *bottom*, or *left* - display the mark only in facets on the given side - *top-empty*, *right-empty*, *bottom-empty*, or *left-empty* (default for axis marks) - display the mark only in facets that have empty space on the given side: either the margin, or an empty facet - *empty* - display the mark in empty facets only
#' @param fill `<column | literal | transform() | sql()/agg() | list(value=, ...) | param()>` The fill; a constant CSS color string, or a channel typically bound to the *color* scale.
#' @param fill_opacity `<column | literal | transform() | sql()/agg() | list(value=, ...) | param()>` The fill-opacity; a constant number between 0 and 1, or a channel typically bound to the *opacity* scale.
#' @param filter `<column | literal | transform() | sql()/agg()>` Applies a transform to filter the mark’s index according to the given channel values; only truthy values are retained.
#' @param fx `<column | literal | transform() | sql()/agg()>` The horizontal facet position channel, for mark-level faceting, bound to the *fx* scale.
#' @param fy `<column | literal | transform() | sql()/agg()>` The vertical facet position channel, for mark-level faceting, bound to the *fy* scale.
#' @param href `<column | literal | transform() | sql()/agg()>` The href; a channel specifying URLs for clickable links.
#' @param image_filter `<string | param()>` A CSS filter; a constant string used to adjust the rendering of images, such as *blur(5px)*.
#' @param inset `<number | param()>` Shorthand to set the same default for **insetStart** and **insetEnd**.
#' @param inset_bottom `<number | param()>` Insets the bottom edge by the specified number of pixels.
#' @param inset_top `<number | param()>` Insets the top edge by the specified number of pixels.
#' @param margin `<number | param()>` Shorthand to set the same default for all four mark margins: **marginTop**, **marginRight**, **marginBottom**, and **marginLeft**; typically defaults to 0, except for axis marks.
#' @param margin_bottom `<number | param()>` The mark’s bottom margin; the minimum distance in pixels between the bottom edges of the inner and outer plot area.
#' @param margin_left `<number | param()>` The mark’s left margin; the minimum distance in pixels between the left edges of the inner and outer plot area.
#' @param margin_right `<number | param()>` The mark’s right margin; the minimum distance in pixels between the right edges of the mark’s inner and outer plot area.
#' @param margin_top `<number | param()>` The mark’s top margin; the minimum distance in pixels between the top edges of the inner and outer plot area.
#' @param marker `<"arrow" | "arrow-reverse" | "dot" | "circle" | "circle-fill" | "circle-stroke" | "tick" | "tick-x" | "tick-y" | "none" | boolean | NULL | param()>` Shorthand to set the same default for markerStart, markerMid, and markerEnd; one of: - a marker name such as *arrow* or *circle* - *none* (default) - no marker * true - alias for *circle-fill* * false or null - alias for *none*
#' @param marker_end `<"arrow" | "arrow-reverse" | "dot" | "circle" | "circle-fill" | "circle-stroke" | "tick" | "tick-x" | "tick-y" | "none" | boolean | NULL | param()>` The marker for the ending point of a line segment; one of: - a marker name such as *arrow* or *circle* * *none* (default) - no marker * true - alias for *circle-fill* * false or null - alias for *none*
#' @param marker_mid `<"arrow" | "arrow-reverse" | "dot" | "circle" | "circle-fill" | "circle-stroke" | "tick" | "tick-x" | "tick-y" | "none" | boolean | NULL | param()>` The marker for any middle (interior) points of a line segment.
#' @param marker_start `<"arrow" | "arrow-reverse" | "dot" | "circle" | "circle-fill" | "circle-stroke" | "tick" | "tick-x" | "tick-y" | "none" | boolean | NULL | param()>` The marker for the starting point of a line segment; one of: - a marker name such as *arrow* or *circle* * *none* (default) - no marker * true - alias for *circle-fill* * false or null - alias for *none*
#' @param mix_blend_mode `<string | param()>` The mix-blend-mode; a constant string specifying how to blend content such as *multiply*.
#' @param opacity `<column | literal | transform() | sql()/agg() | list(value=, ...)>` The opacity; a constant between 0 and 1, or a channel typically bound to the *opacity* scale.
#' @param paint_order `<string | param()>` The paint-order; a constant string specifying the order in which the **fill**, **stroke**, and any markers are drawn; defaults to *normal*, which draws the fill, then stroke, then markers; defaults to *stroke* for the text mark to create a “halo” around text to improve legibility.
#' @param pointer_events `<string | param()>` The pointer-events property; a constant string such as *none*.
#' @param reverse `<boolean | param()>` Applies a transform to reverse the order of the mark’s index, say for reverse input order.
#' @param select `<"first" | "last" | "maxX" | "maxY" | "minX" | "minY" | "nearest" | "nearestX" | "nearestY">` Applies a filter transform after data is loaded to highlight selected values only.
#' @param shape_rendering `<string | param()>` The shape-rendering; a constant string such as *crispEdges*.
#' @param sort `<column | literal | transform() | sql()/agg() | list>` Either applies a transform to sort the mark’s index by the specified channel values, or imputes ordinal scale domains from this mark’s channels.
#' @param stroke `<column | literal | transform() | sql()/agg() | list(value=, ...) | param()>` The stroke; a constant CSS color string, or a channel typically bound to the *color* scale.
#' @param stroke_dasharray `<string | number | param()>` The stroke-dasharray; a constant number indicating the length in pixels of alternating dashes and gaps, or a constant string of numbers separated by spaces or commas (_e.g._, *10 2* for dashes of 10 pixels separated by gaps of 2 pixels), or *none* (the default) for no dashing : https://developer.mozilla.org/en-US/docs/Web/SVG/Attribute/stroke-dasharray
#' @param stroke_dashoffset `<string | number | param()>` The stroke-dashoffset; a constant indicating the offset in pixels of the first dash along the stroke; defaults to zero.
#' @param stroke_linecap `<string | param()>` The stroke-linecap; a constant specifying how to cap stroked paths, such as *butt*, *round*, or *square*.
#' @param stroke_linejoin `<string | param()>` The stroke-linejoin; a constant specifying how to join stroked paths, such as *bevel*, *miter*, *miter-clip*, or *round*.
#' @param stroke_miterlimit `<number | param()>` The stroke-miterlimit; a constant number specifying how to limit the length of *miter* joins on stroked paths.
#' @param stroke_opacity `<column | literal | transform() | sql()/agg() | list(value=, ...)>` The stroke-opacity; a constant between 0 and 1, or a channel typically bound to the *opacity* scale.
#' @param stroke_width `<column | literal | transform() | sql()/agg() | list(value=, ...)>` The stroke-width; a constant number in pixels, or a channel.
#' @param target `<string | param()>` The target; a constant string specifying the target window (_e.g._, *_blank*) for clickable links; used in conjunction with the **href** option.
#' @param tip `<boolean | "x" | "y" | "xy" | list | param()>` Whether to generate a tooltip for this mark, and any tip options.
#' @param title `<column | literal | transform() | sql()/agg()>` The title; a channel specifying accessible, short textual descriptions as strings (possibly with newlines).
#' @param x `<column | literal | transform() | sql()/agg() | list(value=, ...)>` The horizontal position (or length) channel, typically bound to the *x* scale.
#' @param y `<column | literal | transform() | sql()/agg() | list(value=, ...)>` The vertical position channel, typically bound to the *y* scale; defaults to the zero-based index of the data 0, 1, 2, ….
#' @param ... Additional options or plot-level attributes.
#' @param data_from The name of the data source this mark reads from (see [vg_data()]); a length-1 nonzero R integer (`1L`, `-1L`, ...; note the `L`) giving a 1-based index into the spec's registered data sources instead, negative counting from the end (e.g., `-1L` for the most recently registered one); or a literal vector of values to use as inline data directly (mosaic-spec's `"data": [...]` shorthand, e.g., for a single reference line -- a bare double like `0`/`c(0)`, or `0L` itself (never a valid index), means this, not an index). Left unset, defaults to the first registered data source (equivalent to `data_from = 1L`) for any mark type that takes data at all.
#' @param filter_by A Param/Selection (e.g., from [param()]) to filter this mark's data by.
#' @param data_optimize A flag (default `TRUE`) to enable mark-specific query optimizations for this mark's data; set `FALSE` to disable them (mosaic-spec's `data: {optimize: false}`).
#' @family mark functions
#' @export
vg_mark_tick_x <- function(spec = NULL, formula = vg_unset, aria_description = vg_unset, aria_hidden = vg_unset, aria_label = vg_unset, channels = vg_unset, clip = vg_unset, dx = vg_unset, dy = vg_unset, facet = vg_unset, facet_anchor = vg_unset, fill = vg_unset, fill_opacity = vg_unset, filter = vg_unset, fx = vg_unset, fy = vg_unset, href = vg_unset, image_filter = vg_unset, inset = vg_unset, inset_bottom = vg_unset, inset_top = vg_unset, margin = vg_unset, margin_bottom = vg_unset, margin_left = vg_unset, margin_right = vg_unset, margin_top = vg_unset, marker = vg_unset, marker_end = vg_unset, marker_mid = vg_unset, marker_start = vg_unset, mix_blend_mode = vg_unset, opacity = vg_unset, paint_order = vg_unset, pointer_events = vg_unset, reverse = vg_unset, select = vg_unset, shape_rendering = vg_unset, sort = vg_unset, stroke = vg_unset, stroke_dasharray = vg_unset, stroke_dashoffset = vg_unset, stroke_linecap = vg_unset, stroke_linejoin = vg_unset, stroke_miterlimit = vg_unset, stroke_opacity = vg_unset, stroke_width = vg_unset, target = vg_unset, tip = vg_unset, title = vg_unset, x = vg_unset, y = vg_unset, ..., data_from = vg_unset, filter_by = vg_unset, data_optimize = vg_unset) {
  vg_mark_(spec, "tickX", formula = formula, ariaDescription = aria_description, ariaHidden = aria_hidden, ariaLabel = aria_label, channels = channels, clip = clip, dx = dx, dy = dy, facet = facet, facetAnchor = facet_anchor, fill = fill, fillOpacity = fill_opacity, filter = filter, fx = fx, fy = fy, href = href, imageFilter = image_filter, inset = inset, insetBottom = inset_bottom, insetTop = inset_top, margin = margin, marginBottom = margin_bottom, marginLeft = margin_left, marginRight = margin_right, marginTop = margin_top, marker = marker, markerEnd = marker_end, markerMid = marker_mid, markerStart = marker_start, mixBlendMode = mix_blend_mode, opacity = opacity, paintOrder = paint_order, pointerEvents = pointer_events, reverse = reverse, select = select, shapeRendering = shape_rendering, sort = sort, stroke = stroke, strokeDasharray = stroke_dasharray, strokeDashoffset = stroke_dashoffset, strokeLinecap = stroke_linecap, strokeLinejoin = stroke_linejoin, strokeMiterlimit = stroke_miterlimit, strokeOpacity = stroke_opacity, strokeWidth = stroke_width, target = target, tip = tip, title = title, x = x, y = y, ..., data_from = data_from, filter_by = filter_by, data_optimize = data_optimize)
}

#' The tickY mark.
#'
#' See [vg_value_types] for what the `<...>` notation below (`column`, `param()`, ...) means.
#'
#' @param spec A plot fragment or `vgspec` to add this mark to, or `NULL` to start a new plot with just this mark.
#' @param formula A shorthand for this mark's position channels (`x`, `y`, `fx`, `fy`, and paired `x1`/`x2` or `y1`/`y2`), e.g., `Sepal.Length ~ Sepal.Width | ~ Species` for `y = ~Sepal.Length, x = ~Sepal.Width, fx = ~Species`. See [vg_mark()] for the full grammar.
#' @param aria_description `<string | param()>` The aria-description; a constant textual description.
#' @param aria_hidden `<string | param()>` The aria-hidden state; a constant indicating whether the element is exposed to an accessibility API.
#' @param aria_label `<column | literal | transform() | sql()/agg()>` The aria-label; a channel specifying short textual labels representing the value in the accessibility tree.
#' @param channels `<list>` Additional named channels, for example to include in a tooltip.
#' @param clip `<"frame" | "sphere" | boolean | NULL | param()>` How to clip the mark; one of: - *frame* or true - clip to the plot’s frame (inner area) - *sphere* - clip to the projected sphere (*e.g.*, front hemisphere) - null or false - do not clip The *sphere* clip option requires a geographic projection.
#' @param dx `<number | param()>` The horizontal offset in pixels; a constant option.
#' @param dy `<number | param()>` The vertical offset in pixels; a constant option.
#' @param facet `<"auto" | "include" | "exclude" | "super" | boolean | NULL | param()>` Whether to enable or disable faceting; one of: - *auto* (default) - automatically determine if this mark should be faceted - *include* (or true) - draw the subset of the mark’s data in the current facet - *exclude* - draw the subset of the mark’s data *not* in the current facet - *super* - draw this mark in a single frame that covers all facets - null (or false) - repeat this mark’s data across all facets (*i.e.*, no faceting) When a mark uses *super* faceting, it is not allowed to use position scales (*x*, *y*, *fx*, or *fy*); *super* faceting is intended for decorations, such as labels and legends.
#' @param facet_anchor `<"top" | "right" | "bottom" | "left" | "top-left" | "top-right" | "bottom-left" | "bottom-right" | "top-empty" | "right-empty" | "bottom-empty" | "left-empty" | "empty" | NULL | param()>` How to place the mark with respect to facets; one of: - null (default for most marks) - display the mark in each non-empty facet - *top*, *right*, *bottom*, or *left* - display the mark only in facets on the given side - *top-empty*, *right-empty*, *bottom-empty*, or *left-empty* (default for axis marks) - display the mark only in facets that have empty space on the given side: either the margin, or an empty facet - *empty* - display the mark in empty facets only
#' @param fill `<column | literal | transform() | sql()/agg() | list(value=, ...) | param()>` The fill; a constant CSS color string, or a channel typically bound to the *color* scale.
#' @param fill_opacity `<column | literal | transform() | sql()/agg() | list(value=, ...) | param()>` The fill-opacity; a constant number between 0 and 1, or a channel typically bound to the *opacity* scale.
#' @param filter `<column | literal | transform() | sql()/agg()>` Applies a transform to filter the mark’s index according to the given channel values; only truthy values are retained.
#' @param fx `<column | literal | transform() | sql()/agg()>` The horizontal facet position channel, for mark-level faceting, bound to the *fx* scale.
#' @param fy `<column | literal | transform() | sql()/agg()>` The vertical facet position channel, for mark-level faceting, bound to the *fy* scale.
#' @param href `<column | literal | transform() | sql()/agg()>` The href; a channel specifying URLs for clickable links.
#' @param image_filter `<string | param()>` A CSS filter; a constant string used to adjust the rendering of images, such as *blur(5px)*.
#' @param inset `<number | param()>` Shorthand to set the same default for **insetStart** and **insetEnd**.
#' @param inset_left `<number | param()>` Insets the left edge by the specified number of pixels.
#' @param inset_right `<number | param()>` Insets the right edge by the specified number of pixels.
#' @param margin `<number | param()>` Shorthand to set the same default for all four mark margins: **marginTop**, **marginRight**, **marginBottom**, and **marginLeft**; typically defaults to 0, except for axis marks.
#' @param margin_bottom `<number | param()>` The mark’s bottom margin; the minimum distance in pixels between the bottom edges of the inner and outer plot area.
#' @param margin_left `<number | param()>` The mark’s left margin; the minimum distance in pixels between the left edges of the inner and outer plot area.
#' @param margin_right `<number | param()>` The mark’s right margin; the minimum distance in pixels between the right edges of the mark’s inner and outer plot area.
#' @param margin_top `<number | param()>` The mark’s top margin; the minimum distance in pixels between the top edges of the inner and outer plot area.
#' @param marker `<"arrow" | "arrow-reverse" | "dot" | "circle" | "circle-fill" | "circle-stroke" | "tick" | "tick-x" | "tick-y" | "none" | boolean | NULL | param()>` Shorthand to set the same default for markerStart, markerMid, and markerEnd; one of: - a marker name such as *arrow* or *circle* - *none* (default) - no marker * true - alias for *circle-fill* * false or null - alias for *none*
#' @param marker_end `<"arrow" | "arrow-reverse" | "dot" | "circle" | "circle-fill" | "circle-stroke" | "tick" | "tick-x" | "tick-y" | "none" | boolean | NULL | param()>` The marker for the ending point of a line segment; one of: - a marker name such as *arrow* or *circle* * *none* (default) - no marker * true - alias for *circle-fill* * false or null - alias for *none*
#' @param marker_mid `<"arrow" | "arrow-reverse" | "dot" | "circle" | "circle-fill" | "circle-stroke" | "tick" | "tick-x" | "tick-y" | "none" | boolean | NULL | param()>` The marker for any middle (interior) points of a line segment.
#' @param marker_start `<"arrow" | "arrow-reverse" | "dot" | "circle" | "circle-fill" | "circle-stroke" | "tick" | "tick-x" | "tick-y" | "none" | boolean | NULL | param()>` The marker for the starting point of a line segment; one of: - a marker name such as *arrow* or *circle* * *none* (default) - no marker * true - alias for *circle-fill* * false or null - alias for *none*
#' @param mix_blend_mode `<string | param()>` The mix-blend-mode; a constant string specifying how to blend content such as *multiply*.
#' @param opacity `<column | literal | transform() | sql()/agg() | list(value=, ...)>` The opacity; a constant between 0 and 1, or a channel typically bound to the *opacity* scale.
#' @param paint_order `<string | param()>` The paint-order; a constant string specifying the order in which the **fill**, **stroke**, and any markers are drawn; defaults to *normal*, which draws the fill, then stroke, then markers; defaults to *stroke* for the text mark to create a “halo” around text to improve legibility.
#' @param pointer_events `<string | param()>` The pointer-events property; a constant string such as *none*.
#' @param reverse `<boolean | param()>` Applies a transform to reverse the order of the mark’s index, say for reverse input order.
#' @param select `<"first" | "last" | "maxX" | "maxY" | "minX" | "minY" | "nearest" | "nearestX" | "nearestY">` Applies a filter transform after data is loaded to highlight selected values only.
#' @param shape_rendering `<string | param()>` The shape-rendering; a constant string such as *crispEdges*.
#' @param sort `<column | literal | transform() | sql()/agg() | list>` Either applies a transform to sort the mark’s index by the specified channel values, or imputes ordinal scale domains from this mark’s channels.
#' @param stroke `<column | literal | transform() | sql()/agg() | list(value=, ...) | param()>` The stroke; a constant CSS color string, or a channel typically bound to the *color* scale.
#' @param stroke_dasharray `<string | number | param()>` The stroke-dasharray; a constant number indicating the length in pixels of alternating dashes and gaps, or a constant string of numbers separated by spaces or commas (_e.g._, *10 2* for dashes of 10 pixels separated by gaps of 2 pixels), or *none* (the default) for no dashing : https://developer.mozilla.org/en-US/docs/Web/SVG/Attribute/stroke-dasharray
#' @param stroke_dashoffset `<string | number | param()>` The stroke-dashoffset; a constant indicating the offset in pixels of the first dash along the stroke; defaults to zero.
#' @param stroke_linecap `<string | param()>` The stroke-linecap; a constant specifying how to cap stroked paths, such as *butt*, *round*, or *square*.
#' @param stroke_linejoin `<string | param()>` The stroke-linejoin; a constant specifying how to join stroked paths, such as *bevel*, *miter*, *miter-clip*, or *round*.
#' @param stroke_miterlimit `<number | param()>` The stroke-miterlimit; a constant number specifying how to limit the length of *miter* joins on stroked paths.
#' @param stroke_opacity `<column | literal | transform() | sql()/agg() | list(value=, ...)>` The stroke-opacity; a constant between 0 and 1, or a channel typically bound to the *opacity* scale.
#' @param stroke_width `<column | literal | transform() | sql()/agg() | list(value=, ...)>` The stroke-width; a constant number in pixels, or a channel.
#' @param target `<string | param()>` The target; a constant string specifying the target window (_e.g._, *_blank*) for clickable links; used in conjunction with the **href** option.
#' @param tip `<boolean | "x" | "y" | "xy" | list | param()>` Whether to generate a tooltip for this mark, and any tip options.
#' @param title `<column | literal | transform() | sql()/agg()>` The title; a channel specifying accessible, short textual descriptions as strings (possibly with newlines).
#' @param x `<column | literal | transform() | sql()/agg() | list(value=, ...)>` The horizontal position (or length) channel, typically bound to the *x* scale.
#' @param y `<column | literal | transform() | sql()/agg() | list(value=, ...)>` The vertical position channel, typically bound to the *y* scale; defaults to the zero-based index of the data 0, 1, 2, ….
#' @param ... Additional options or plot-level attributes.
#' @param data_from The name of the data source this mark reads from (see [vg_data()]); a length-1 nonzero R integer (`1L`, `-1L`, ...; note the `L`) giving a 1-based index into the spec's registered data sources instead, negative counting from the end (e.g., `-1L` for the most recently registered one); or a literal vector of values to use as inline data directly (mosaic-spec's `"data": [...]` shorthand, e.g., for a single reference line -- a bare double like `0`/`c(0)`, or `0L` itself (never a valid index), means this, not an index). Left unset, defaults to the first registered data source (equivalent to `data_from = 1L`) for any mark type that takes data at all.
#' @param filter_by A Param/Selection (e.g., from [param()]) to filter this mark's data by.
#' @param data_optimize A flag (default `TRUE`) to enable mark-specific query optimizations for this mark's data; set `FALSE` to disable them (mosaic-spec's `data: {optimize: false}`).
#' @family mark functions
#' @export
vg_mark_tick_y <- function(spec = NULL, formula = vg_unset, aria_description = vg_unset, aria_hidden = vg_unset, aria_label = vg_unset, channels = vg_unset, clip = vg_unset, dx = vg_unset, dy = vg_unset, facet = vg_unset, facet_anchor = vg_unset, fill = vg_unset, fill_opacity = vg_unset, filter = vg_unset, fx = vg_unset, fy = vg_unset, href = vg_unset, image_filter = vg_unset, inset = vg_unset, inset_left = vg_unset, inset_right = vg_unset, margin = vg_unset, margin_bottom = vg_unset, margin_left = vg_unset, margin_right = vg_unset, margin_top = vg_unset, marker = vg_unset, marker_end = vg_unset, marker_mid = vg_unset, marker_start = vg_unset, mix_blend_mode = vg_unset, opacity = vg_unset, paint_order = vg_unset, pointer_events = vg_unset, reverse = vg_unset, select = vg_unset, shape_rendering = vg_unset, sort = vg_unset, stroke = vg_unset, stroke_dasharray = vg_unset, stroke_dashoffset = vg_unset, stroke_linecap = vg_unset, stroke_linejoin = vg_unset, stroke_miterlimit = vg_unset, stroke_opacity = vg_unset, stroke_width = vg_unset, target = vg_unset, tip = vg_unset, title = vg_unset, x = vg_unset, y = vg_unset, ..., data_from = vg_unset, filter_by = vg_unset, data_optimize = vg_unset) {
  vg_mark_(spec, "tickY", formula = formula, ariaDescription = aria_description, ariaHidden = aria_hidden, ariaLabel = aria_label, channels = channels, clip = clip, dx = dx, dy = dy, facet = facet, facetAnchor = facet_anchor, fill = fill, fillOpacity = fill_opacity, filter = filter, fx = fx, fy = fy, href = href, imageFilter = image_filter, inset = inset, insetLeft = inset_left, insetRight = inset_right, margin = margin, marginBottom = margin_bottom, marginLeft = margin_left, marginRight = margin_right, marginTop = margin_top, marker = marker, markerEnd = marker_end, markerMid = marker_mid, markerStart = marker_start, mixBlendMode = mix_blend_mode, opacity = opacity, paintOrder = paint_order, pointerEvents = pointer_events, reverse = reverse, select = select, shapeRendering = shape_rendering, sort = sort, stroke = stroke, strokeDasharray = stroke_dasharray, strokeDashoffset = stroke_dashoffset, strokeLinecap = stroke_linecap, strokeLinejoin = stroke_linejoin, strokeMiterlimit = stroke_miterlimit, strokeOpacity = stroke_opacity, strokeWidth = stroke_width, target = target, tip = tip, title = title, x = x, y = y, ..., data_from = data_from, filter_by = filter_by, data_optimize = data_optimize)
}

#' The vector mark.
#'
#' See [vg_value_types] for what the `<...>` notation below (`column`, `param()`, ...) means.
#'
#' @param spec A plot fragment or `vgspec` to add this mark to, or `NULL` to start a new plot with just this mark.
#' @param formula A shorthand for this mark's position channels (`x`, `y`, `fx`, `fy`, and paired `x1`/`x2` or `y1`/`y2`), e.g., `Sepal.Length ~ Sepal.Width | ~ Species` for `y = ~Sepal.Length, x = ~Sepal.Width, fx = ~Species`. See [vg_mark()] for the full grammar.
#' @param anchor `<"top" | "right" | "bottom" | "left" | param()>` The side of the frame on which to place the axis: *top* or *bottom* for horizontal axes (axisX and axisFx) and their associated vertical grids (gridX and gridFx), or *left* or *right* for vertical axes (axisY and axisFY) and their associated horizontal grids (gridY and gridFy).
#' @param aria_description `<string | param()>` The aria-description; a constant textual description.
#' @param aria_hidden `<string | param()>` The aria-hidden state; a constant indicating whether the element is exposed to an accessibility API.
#' @param aria_label `<column | literal | transform() | sql()/agg()>` The aria-label; a channel specifying short textual labels representing the value in the accessibility tree.
#' @param channels `<list>` Additional named channels, for example to include in a tooltip.
#' @param clip `<"frame" | "sphere" | boolean | NULL | param()>` How to clip the mark; one of: - *frame* or true - clip to the plot’s frame (inner area) - *sphere* - clip to the projected sphere (*e.g.*, front hemisphere) - null or false - do not clip The *sphere* clip option requires a geographic projection.
#' @param dx `<number | param()>` The horizontal offset in pixels; a constant option.
#' @param dy `<number | param()>` The vertical offset in pixels; a constant option.
#' @param facet `<"auto" | "include" | "exclude" | "super" | boolean | NULL | param()>` Whether to enable or disable faceting; one of: - *auto* (default) - automatically determine if this mark should be faceted - *include* (or true) - draw the subset of the mark’s data in the current facet - *exclude* - draw the subset of the mark’s data *not* in the current facet - *super* - draw this mark in a single frame that covers all facets - null (or false) - repeat this mark’s data across all facets (*i.e.*, no faceting) When a mark uses *super* faceting, it is not allowed to use position scales (*x*, *y*, *fx*, or *fy*); *super* faceting is intended for decorations, such as labels and legends.
#' @param facet_anchor `<"top" | "right" | "bottom" | "left" | "top-left" | "top-right" | "bottom-left" | "bottom-right" | "top-empty" | "right-empty" | "bottom-empty" | "left-empty" | "empty" | NULL | param()>` How to place the mark with respect to facets; one of: - null (default for most marks) - display the mark in each non-empty facet - *top*, *right*, *bottom*, or *left* - display the mark only in facets on the given side - *top-empty*, *right-empty*, *bottom-empty*, or *left-empty* (default for axis marks) - display the mark only in facets that have empty space on the given side: either the margin, or an empty facet - *empty* - display the mark in empty facets only
#' @param fill `<column | literal | transform() | sql()/agg() | list(value=, ...) | param()>` The fill; a constant CSS color string, or a channel typically bound to the *color* scale.
#' @param fill_opacity `<column | literal | transform() | sql()/agg() | list(value=, ...) | param()>` The fill-opacity; a constant number between 0 and 1, or a channel typically bound to the *opacity* scale.
#' @param filter `<column | literal | transform() | sql()/agg()>` Applies a transform to filter the mark’s index according to the given channel values; only truthy values are retained.
#' @param frame_anchor `<"middle" | "top-left" | "top" | "top-right" | "right" | "bottom-right" | "bottom" | "bottom-left" | "left" | param()>` The frame anchor specifies defaults for **x** and **y**, along with **textAnchor** and **lineAnchor**, based on the plot’s frame; it may be one of the four sides (*top*, *right*, *bottom*, *left*), one of the four corners (*top-left*, *top-right*, *bottom-right*, *bottom-left*), or the *middle* of the frame.
#' @param fx `<column | literal | transform() | sql()/agg()>` The horizontal facet position channel, for mark-level faceting, bound to the *fx* scale.
#' @param fy `<column | literal | transform() | sql()/agg()>` The vertical facet position channel, for mark-level faceting, bound to the *fy* scale.
#' @param href `<column | literal | transform() | sql()/agg()>` The href; a channel specifying URLs for clickable links.
#' @param image_filter `<string | param()>` A CSS filter; a constant string used to adjust the rendering of images, such as *blur(5px)*.
#' @param length `<column | literal | transform() | sql()/agg() | list(value=, ...)>` The vector’s length; either an optional channel bound to the *length* scale or a constant number in pixels.
#' @param margin `<number | param()>` Shorthand to set the same default for all four mark margins: **marginTop**, **marginRight**, **marginBottom**, and **marginLeft**; typically defaults to 0, except for axis marks.
#' @param margin_bottom `<number | param()>` The mark’s bottom margin; the minimum distance in pixels between the bottom edges of the inner and outer plot area.
#' @param margin_left `<number | param()>` The mark’s left margin; the minimum distance in pixels between the left edges of the inner and outer plot area.
#' @param margin_right `<number | param()>` The mark’s right margin; the minimum distance in pixels between the right edges of the mark’s inner and outer plot area.
#' @param margin_top `<number | param()>` The mark’s top margin; the minimum distance in pixels between the top edges of the inner and outer plot area.
#' @param mix_blend_mode `<string | param()>` The mix-blend-mode; a constant string specifying how to blend content such as *multiply*.
#' @param opacity `<column | literal | transform() | sql()/agg() | list(value=, ...)>` The opacity; a constant between 0 and 1, or a channel typically bound to the *opacity* scale.
#' @param paint_order `<string | param()>` The paint-order; a constant string specifying the order in which the **fill**, **stroke**, and any markers are drawn; defaults to *normal*, which draws the fill, then stroke, then markers; defaults to *stroke* for the text mark to create a “halo” around text to improve legibility.
#' @param pointer_events `<string | param()>` The pointer-events property; a constant string such as *none*.
#' @param r `<column | literal | transform() | sql()/agg() | list(value=, ...) | number | param()>` The radius of dots; either a channel or constant.
#' @param reverse `<boolean | param()>` Applies a transform to reverse the order of the mark’s index, say for reverse input order.
#' @param rotate `<column | literal | transform() | sql()/agg() | param()>` The rotation angle in degrees clockwise; a constant or a channel; defaults to 0°.
#' @param select `<"first" | "last" | "maxX" | "maxY" | "minX" | "minY" | "nearest" | "nearestX" | "nearestY">` Applies a filter transform after data is loaded to highlight selected values only.
#' @param shape `<"arrow" | "spike" | param()>` The shape of the vector; a constant.
#' @param shape_rendering `<string | param()>` The shape-rendering; a constant string such as *crispEdges*.
#' @param sort `<column | literal | transform() | sql()/agg() | list>` Either applies a transform to sort the mark’s index by the specified channel values, or imputes ordinal scale domains from this mark’s channels.
#' @param stroke `<column | literal | transform() | sql()/agg() | list(value=, ...) | param()>` The stroke; a constant CSS color string, or a channel typically bound to the *color* scale.
#' @param stroke_dasharray `<string | number | param()>` The stroke-dasharray; a constant number indicating the length in pixels of alternating dashes and gaps, or a constant string of numbers separated by spaces or commas (_e.g._, *10 2* for dashes of 10 pixels separated by gaps of 2 pixels), or *none* (the default) for no dashing : https://developer.mozilla.org/en-US/docs/Web/SVG/Attribute/stroke-dasharray
#' @param stroke_dashoffset `<string | number | param()>` The stroke-dashoffset; a constant indicating the offset in pixels of the first dash along the stroke; defaults to zero.
#' @param stroke_linecap `<string | param()>` The stroke-linecap; a constant specifying how to cap stroked paths, such as *butt*, *round*, or *square*.
#' @param stroke_linejoin `<string | param()>` The stroke-linejoin; a constant specifying how to join stroked paths, such as *bevel*, *miter*, *miter-clip*, or *round*.
#' @param stroke_miterlimit `<number | param()>` The stroke-miterlimit; a constant number specifying how to limit the length of *miter* joins on stroked paths.
#' @param stroke_opacity `<column | literal | transform() | sql()/agg() | list(value=, ...)>` The stroke-opacity; a constant between 0 and 1, or a channel typically bound to the *opacity* scale.
#' @param stroke_width `<column | literal | transform() | sql()/agg() | list(value=, ...)>` The stroke-width; a constant number in pixels, or a channel.
#' @param target `<string | param()>` The target; a constant string specifying the target window (_e.g._, *_blank*) for clickable links; used in conjunction with the **href** option.
#' @param tip `<boolean | "x" | "y" | "xy" | list | param()>` Whether to generate a tooltip for this mark, and any tip options.
#' @param title `<column | literal | transform() | sql()/agg()>` The title; a channel specifying accessible, short textual descriptions as strings (possibly with newlines).
#' @param x `<column | literal | transform() | sql()/agg() | list(value=, ...)>` The horizontal position (or length) channel, typically bound to the *x* scale.
#' @param y `<column | literal | transform() | sql()/agg() | list(value=, ...)>` The vertical position channel, typically bound to the *y* scale; defaults to the zero-based index of the data 0, 1, 2, ….
#' @param ... Additional options or plot-level attributes.
#' @param data_from The name of the data source this mark reads from (see [vg_data()]); a length-1 nonzero R integer (`1L`, `-1L`, ...; note the `L`) giving a 1-based index into the spec's registered data sources instead, negative counting from the end (e.g., `-1L` for the most recently registered one); or a literal vector of values to use as inline data directly (mosaic-spec's `"data": [...]` shorthand, e.g., for a single reference line -- a bare double like `0`/`c(0)`, or `0L` itself (never a valid index), means this, not an index). Left unset, defaults to the first registered data source (equivalent to `data_from = 1L`) for any mark type that takes data at all.
#' @param filter_by A Param/Selection (e.g., from [param()]) to filter this mark's data by.
#' @param data_optimize A flag (default `TRUE`) to enable mark-specific query optimizations for this mark's data; set `FALSE` to disable them (mosaic-spec's `data: {optimize: false}`).
#' @family mark functions
#' @export
vg_mark_vector <- function(spec = NULL, formula = vg_unset, anchor = vg_unset, aria_description = vg_unset, aria_hidden = vg_unset, aria_label = vg_unset, channels = vg_unset, clip = vg_unset, dx = vg_unset, dy = vg_unset, facet = vg_unset, facet_anchor = vg_unset, fill = vg_unset, fill_opacity = vg_unset, filter = vg_unset, frame_anchor = vg_unset, fx = vg_unset, fy = vg_unset, href = vg_unset, image_filter = vg_unset, length = vg_unset, margin = vg_unset, margin_bottom = vg_unset, margin_left = vg_unset, margin_right = vg_unset, margin_top = vg_unset, mix_blend_mode = vg_unset, opacity = vg_unset, paint_order = vg_unset, pointer_events = vg_unset, r = vg_unset, reverse = vg_unset, rotate = vg_unset, select = vg_unset, shape = vg_unset, shape_rendering = vg_unset, sort = vg_unset, stroke = vg_unset, stroke_dasharray = vg_unset, stroke_dashoffset = vg_unset, stroke_linecap = vg_unset, stroke_linejoin = vg_unset, stroke_miterlimit = vg_unset, stroke_opacity = vg_unset, stroke_width = vg_unset, target = vg_unset, tip = vg_unset, title = vg_unset, x = vg_unset, y = vg_unset, ..., data_from = vg_unset, filter_by = vg_unset, data_optimize = vg_unset) {
  vg_mark_(spec, "vector", formula = formula, anchor = anchor, ariaDescription = aria_description, ariaHidden = aria_hidden, ariaLabel = aria_label, channels = channels, clip = clip, dx = dx, dy = dy, facet = facet, facetAnchor = facet_anchor, fill = fill, fillOpacity = fill_opacity, filter = filter, frameAnchor = frame_anchor, fx = fx, fy = fy, href = href, imageFilter = image_filter, length = length, margin = margin, marginBottom = margin_bottom, marginLeft = margin_left, marginRight = margin_right, marginTop = margin_top, mixBlendMode = mix_blend_mode, opacity = opacity, paintOrder = paint_order, pointerEvents = pointer_events, r = r, reverse = reverse, rotate = rotate, select = select, shape = shape, shapeRendering = shape_rendering, sort = sort, stroke = stroke, strokeDasharray = stroke_dasharray, strokeDashoffset = stroke_dashoffset, strokeLinecap = stroke_linecap, strokeLinejoin = stroke_linejoin, strokeMiterlimit = stroke_miterlimit, strokeOpacity = stroke_opacity, strokeWidth = stroke_width, target = target, tip = tip, title = title, x = x, y = y, ..., data_from = data_from, filter_by = filter_by, data_optimize = data_optimize)
}

#' The vectorX mark.
#'
#' See [vg_value_types] for what the `<...>` notation below (`column`, `param()`, ...) means.
#'
#' @param spec A plot fragment or `vgspec` to add this mark to, or `NULL` to start a new plot with just this mark.
#' @param formula A shorthand for this mark's position channels (`x`, `y`, `fx`, `fy`, and paired `x1`/`x2` or `y1`/`y2`), e.g., `Sepal.Length ~ Sepal.Width | ~ Species` for `y = ~Sepal.Length, x = ~Sepal.Width, fx = ~Species`. See [vg_mark()] for the full grammar.
#' @param anchor `<"top" | "right" | "bottom" | "left" | param()>` The side of the frame on which to place the axis: *top* or *bottom* for horizontal axes (axisX and axisFx) and their associated vertical grids (gridX and gridFx), or *left* or *right* for vertical axes (axisY and axisFY) and their associated horizontal grids (gridY and gridFy).
#' @param aria_description `<string | param()>` The aria-description; a constant textual description.
#' @param aria_hidden `<string | param()>` The aria-hidden state; a constant indicating whether the element is exposed to an accessibility API.
#' @param aria_label `<column | literal | transform() | sql()/agg()>` The aria-label; a channel specifying short textual labels representing the value in the accessibility tree.
#' @param channels `<list>` Additional named channels, for example to include in a tooltip.
#' @param clip `<"frame" | "sphere" | boolean | NULL | param()>` How to clip the mark; one of: - *frame* or true - clip to the plot’s frame (inner area) - *sphere* - clip to the projected sphere (*e.g.*, front hemisphere) - null or false - do not clip The *sphere* clip option requires a geographic projection.
#' @param dx `<number | param()>` The horizontal offset in pixels; a constant option.
#' @param dy `<number | param()>` The vertical offset in pixels; a constant option.
#' @param facet `<"auto" | "include" | "exclude" | "super" | boolean | NULL | param()>` Whether to enable or disable faceting; one of: - *auto* (default) - automatically determine if this mark should be faceted - *include* (or true) - draw the subset of the mark’s data in the current facet - *exclude* - draw the subset of the mark’s data *not* in the current facet - *super* - draw this mark in a single frame that covers all facets - null (or false) - repeat this mark’s data across all facets (*i.e.*, no faceting) When a mark uses *super* faceting, it is not allowed to use position scales (*x*, *y*, *fx*, or *fy*); *super* faceting is intended for decorations, such as labels and legends.
#' @param facet_anchor `<"top" | "right" | "bottom" | "left" | "top-left" | "top-right" | "bottom-left" | "bottom-right" | "top-empty" | "right-empty" | "bottom-empty" | "left-empty" | "empty" | NULL | param()>` How to place the mark with respect to facets; one of: - null (default for most marks) - display the mark in each non-empty facet - *top*, *right*, *bottom*, or *left* - display the mark only in facets on the given side - *top-empty*, *right-empty*, *bottom-empty*, or *left-empty* (default for axis marks) - display the mark only in facets that have empty space on the given side: either the margin, or an empty facet - *empty* - display the mark in empty facets only
#' @param fill `<column | literal | transform() | sql()/agg() | list(value=, ...) | param()>` The fill; a constant CSS color string, or a channel typically bound to the *color* scale.
#' @param fill_opacity `<column | literal | transform() | sql()/agg() | list(value=, ...) | param()>` The fill-opacity; a constant number between 0 and 1, or a channel typically bound to the *opacity* scale.
#' @param filter `<column | literal | transform() | sql()/agg()>` Applies a transform to filter the mark’s index according to the given channel values; only truthy values are retained.
#' @param frame_anchor `<"middle" | "top-left" | "top" | "top-right" | "right" | "bottom-right" | "bottom" | "bottom-left" | "left" | param()>` The frame anchor specifies defaults for **x** and **y**, along with **textAnchor** and **lineAnchor**, based on the plot’s frame; it may be one of the four sides (*top*, *right*, *bottom*, *left*), one of the four corners (*top-left*, *top-right*, *bottom-right*, *bottom-left*), or the *middle* of the frame.
#' @param fx `<column | literal | transform() | sql()/agg()>` The horizontal facet position channel, for mark-level faceting, bound to the *fx* scale.
#' @param fy `<column | literal | transform() | sql()/agg()>` The vertical facet position channel, for mark-level faceting, bound to the *fy* scale.
#' @param href `<column | literal | transform() | sql()/agg()>` The href; a channel specifying URLs for clickable links.
#' @param image_filter `<string | param()>` A CSS filter; a constant string used to adjust the rendering of images, such as *blur(5px)*.
#' @param length `<column | literal | transform() | sql()/agg() | list(value=, ...)>` The vector’s length; either an optional channel bound to the *length* scale or a constant number in pixels.
#' @param margin `<number | param()>` Shorthand to set the same default for all four mark margins: **marginTop**, **marginRight**, **marginBottom**, and **marginLeft**; typically defaults to 0, except for axis marks.
#' @param margin_bottom `<number | param()>` The mark’s bottom margin; the minimum distance in pixels between the bottom edges of the inner and outer plot area.
#' @param margin_left `<number | param()>` The mark’s left margin; the minimum distance in pixels between the left edges of the inner and outer plot area.
#' @param margin_right `<number | param()>` The mark’s right margin; the minimum distance in pixels between the right edges of the mark’s inner and outer plot area.
#' @param margin_top `<number | param()>` The mark’s top margin; the minimum distance in pixels between the top edges of the inner and outer plot area.
#' @param mix_blend_mode `<string | param()>` The mix-blend-mode; a constant string specifying how to blend content such as *multiply*.
#' @param opacity `<column | literal | transform() | sql()/agg() | list(value=, ...)>` The opacity; a constant between 0 and 1, or a channel typically bound to the *opacity* scale.
#' @param paint_order `<string | param()>` The paint-order; a constant string specifying the order in which the **fill**, **stroke**, and any markers are drawn; defaults to *normal*, which draws the fill, then stroke, then markers; defaults to *stroke* for the text mark to create a “halo” around text to improve legibility.
#' @param pointer_events `<string | param()>` The pointer-events property; a constant string such as *none*.
#' @param r `<column | literal | transform() | sql()/agg() | list(value=, ...) | number | param()>` The radius of dots; either a channel or constant.
#' @param reverse `<boolean | param()>` Applies a transform to reverse the order of the mark’s index, say for reverse input order.
#' @param rotate `<column | literal | transform() | sql()/agg() | param()>` The rotation angle in degrees clockwise; a constant or a channel; defaults to 0°.
#' @param select `<"first" | "last" | "maxX" | "maxY" | "minX" | "minY" | "nearest" | "nearestX" | "nearestY">` Applies a filter transform after data is loaded to highlight selected values only.
#' @param shape `<"arrow" | "spike" | param()>` The shape of the vector; a constant.
#' @param shape_rendering `<string | param()>` The shape-rendering; a constant string such as *crispEdges*.
#' @param sort `<column | literal | transform() | sql()/agg() | list>` Either applies a transform to sort the mark’s index by the specified channel values, or imputes ordinal scale domains from this mark’s channels.
#' @param stroke `<column | literal | transform() | sql()/agg() | list(value=, ...) | param()>` The stroke; a constant CSS color string, or a channel typically bound to the *color* scale.
#' @param stroke_dasharray `<string | number | param()>` The stroke-dasharray; a constant number indicating the length in pixels of alternating dashes and gaps, or a constant string of numbers separated by spaces or commas (_e.g._, *10 2* for dashes of 10 pixels separated by gaps of 2 pixels), or *none* (the default) for no dashing : https://developer.mozilla.org/en-US/docs/Web/SVG/Attribute/stroke-dasharray
#' @param stroke_dashoffset `<string | number | param()>` The stroke-dashoffset; a constant indicating the offset in pixels of the first dash along the stroke; defaults to zero.
#' @param stroke_linecap `<string | param()>` The stroke-linecap; a constant specifying how to cap stroked paths, such as *butt*, *round*, or *square*.
#' @param stroke_linejoin `<string | param()>` The stroke-linejoin; a constant specifying how to join stroked paths, such as *bevel*, *miter*, *miter-clip*, or *round*.
#' @param stroke_miterlimit `<number | param()>` The stroke-miterlimit; a constant number specifying how to limit the length of *miter* joins on stroked paths.
#' @param stroke_opacity `<column | literal | transform() | sql()/agg() | list(value=, ...)>` The stroke-opacity; a constant between 0 and 1, or a channel typically bound to the *opacity* scale.
#' @param stroke_width `<column | literal | transform() | sql()/agg() | list(value=, ...)>` The stroke-width; a constant number in pixels, or a channel.
#' @param target `<string | param()>` The target; a constant string specifying the target window (_e.g._, *_blank*) for clickable links; used in conjunction with the **href** option.
#' @param tip `<boolean | "x" | "y" | "xy" | list | param()>` Whether to generate a tooltip for this mark, and any tip options.
#' @param title `<column | literal | transform() | sql()/agg()>` The title; a channel specifying accessible, short textual descriptions as strings (possibly with newlines).
#' @param x `<column | literal | transform() | sql()/agg() | list(value=, ...)>` The horizontal position (or length) channel, typically bound to the *x* scale.
#' @param y `<column | literal | transform() | sql()/agg() | list(value=, ...)>` The vertical position channel, typically bound to the *y* scale; defaults to the zero-based index of the data 0, 1, 2, ….
#' @param ... Additional options or plot-level attributes.
#' @param data_from The name of the data source this mark reads from (see [vg_data()]); a length-1 nonzero R integer (`1L`, `-1L`, ...; note the `L`) giving a 1-based index into the spec's registered data sources instead, negative counting from the end (e.g., `-1L` for the most recently registered one); or a literal vector of values to use as inline data directly (mosaic-spec's `"data": [...]` shorthand, e.g., for a single reference line -- a bare double like `0`/`c(0)`, or `0L` itself (never a valid index), means this, not an index). Left unset, defaults to the first registered data source (equivalent to `data_from = 1L`) for any mark type that takes data at all.
#' @param filter_by A Param/Selection (e.g., from [param()]) to filter this mark's data by.
#' @param data_optimize A flag (default `TRUE`) to enable mark-specific query optimizations for this mark's data; set `FALSE` to disable them (mosaic-spec's `data: {optimize: false}`).
#' @family mark functions
#' @export
vg_mark_vector_x <- function(spec = NULL, formula = vg_unset, anchor = vg_unset, aria_description = vg_unset, aria_hidden = vg_unset, aria_label = vg_unset, channels = vg_unset, clip = vg_unset, dx = vg_unset, dy = vg_unset, facet = vg_unset, facet_anchor = vg_unset, fill = vg_unset, fill_opacity = vg_unset, filter = vg_unset, frame_anchor = vg_unset, fx = vg_unset, fy = vg_unset, href = vg_unset, image_filter = vg_unset, length = vg_unset, margin = vg_unset, margin_bottom = vg_unset, margin_left = vg_unset, margin_right = vg_unset, margin_top = vg_unset, mix_blend_mode = vg_unset, opacity = vg_unset, paint_order = vg_unset, pointer_events = vg_unset, r = vg_unset, reverse = vg_unset, rotate = vg_unset, select = vg_unset, shape = vg_unset, shape_rendering = vg_unset, sort = vg_unset, stroke = vg_unset, stroke_dasharray = vg_unset, stroke_dashoffset = vg_unset, stroke_linecap = vg_unset, stroke_linejoin = vg_unset, stroke_miterlimit = vg_unset, stroke_opacity = vg_unset, stroke_width = vg_unset, target = vg_unset, tip = vg_unset, title = vg_unset, x = vg_unset, y = vg_unset, ..., data_from = vg_unset, filter_by = vg_unset, data_optimize = vg_unset) {
  vg_mark_(spec, "vectorX", formula = formula, anchor = anchor, ariaDescription = aria_description, ariaHidden = aria_hidden, ariaLabel = aria_label, channels = channels, clip = clip, dx = dx, dy = dy, facet = facet, facetAnchor = facet_anchor, fill = fill, fillOpacity = fill_opacity, filter = filter, frameAnchor = frame_anchor, fx = fx, fy = fy, href = href, imageFilter = image_filter, length = length, margin = margin, marginBottom = margin_bottom, marginLeft = margin_left, marginRight = margin_right, marginTop = margin_top, mixBlendMode = mix_blend_mode, opacity = opacity, paintOrder = paint_order, pointerEvents = pointer_events, r = r, reverse = reverse, rotate = rotate, select = select, shape = shape, shapeRendering = shape_rendering, sort = sort, stroke = stroke, strokeDasharray = stroke_dasharray, strokeDashoffset = stroke_dashoffset, strokeLinecap = stroke_linecap, strokeLinejoin = stroke_linejoin, strokeMiterlimit = stroke_miterlimit, strokeOpacity = stroke_opacity, strokeWidth = stroke_width, target = target, tip = tip, title = title, x = x, y = y, ..., data_from = data_from, filter_by = filter_by, data_optimize = data_optimize)
}

#' The vectorY mark.
#'
#' See [vg_value_types] for what the `<...>` notation below (`column`, `param()`, ...) means.
#'
#' @param spec A plot fragment or `vgspec` to add this mark to, or `NULL` to start a new plot with just this mark.
#' @param formula A shorthand for this mark's position channels (`x`, `y`, `fx`, `fy`, and paired `x1`/`x2` or `y1`/`y2`), e.g., `Sepal.Length ~ Sepal.Width | ~ Species` for `y = ~Sepal.Length, x = ~Sepal.Width, fx = ~Species`. See [vg_mark()] for the full grammar.
#' @param anchor `<"top" | "right" | "bottom" | "left" | param()>` The side of the frame on which to place the axis: *top* or *bottom* for horizontal axes (axisX and axisFx) and their associated vertical grids (gridX and gridFx), or *left* or *right* for vertical axes (axisY and axisFY) and their associated horizontal grids (gridY and gridFy).
#' @param aria_description `<string | param()>` The aria-description; a constant textual description.
#' @param aria_hidden `<string | param()>` The aria-hidden state; a constant indicating whether the element is exposed to an accessibility API.
#' @param aria_label `<column | literal | transform() | sql()/agg()>` The aria-label; a channel specifying short textual labels representing the value in the accessibility tree.
#' @param channels `<list>` Additional named channels, for example to include in a tooltip.
#' @param clip `<"frame" | "sphere" | boolean | NULL | param()>` How to clip the mark; one of: - *frame* or true - clip to the plot’s frame (inner area) - *sphere* - clip to the projected sphere (*e.g.*, front hemisphere) - null or false - do not clip The *sphere* clip option requires a geographic projection.
#' @param dx `<number | param()>` The horizontal offset in pixels; a constant option.
#' @param dy `<number | param()>` The vertical offset in pixels; a constant option.
#' @param facet `<"auto" | "include" | "exclude" | "super" | boolean | NULL | param()>` Whether to enable or disable faceting; one of: - *auto* (default) - automatically determine if this mark should be faceted - *include* (or true) - draw the subset of the mark’s data in the current facet - *exclude* - draw the subset of the mark’s data *not* in the current facet - *super* - draw this mark in a single frame that covers all facets - null (or false) - repeat this mark’s data across all facets (*i.e.*, no faceting) When a mark uses *super* faceting, it is not allowed to use position scales (*x*, *y*, *fx*, or *fy*); *super* faceting is intended for decorations, such as labels and legends.
#' @param facet_anchor `<"top" | "right" | "bottom" | "left" | "top-left" | "top-right" | "bottom-left" | "bottom-right" | "top-empty" | "right-empty" | "bottom-empty" | "left-empty" | "empty" | NULL | param()>` How to place the mark with respect to facets; one of: - null (default for most marks) - display the mark in each non-empty facet - *top*, *right*, *bottom*, or *left* - display the mark only in facets on the given side - *top-empty*, *right-empty*, *bottom-empty*, or *left-empty* (default for axis marks) - display the mark only in facets that have empty space on the given side: either the margin, or an empty facet - *empty* - display the mark in empty facets only
#' @param fill `<column | literal | transform() | sql()/agg() | list(value=, ...) | param()>` The fill; a constant CSS color string, or a channel typically bound to the *color* scale.
#' @param fill_opacity `<column | literal | transform() | sql()/agg() | list(value=, ...) | param()>` The fill-opacity; a constant number between 0 and 1, or a channel typically bound to the *opacity* scale.
#' @param filter `<column | literal | transform() | sql()/agg()>` Applies a transform to filter the mark’s index according to the given channel values; only truthy values are retained.
#' @param frame_anchor `<"middle" | "top-left" | "top" | "top-right" | "right" | "bottom-right" | "bottom" | "bottom-left" | "left" | param()>` The frame anchor specifies defaults for **x** and **y**, along with **textAnchor** and **lineAnchor**, based on the plot’s frame; it may be one of the four sides (*top*, *right*, *bottom*, *left*), one of the four corners (*top-left*, *top-right*, *bottom-right*, *bottom-left*), or the *middle* of the frame.
#' @param fx `<column | literal | transform() | sql()/agg()>` The horizontal facet position channel, for mark-level faceting, bound to the *fx* scale.
#' @param fy `<column | literal | transform() | sql()/agg()>` The vertical facet position channel, for mark-level faceting, bound to the *fy* scale.
#' @param href `<column | literal | transform() | sql()/agg()>` The href; a channel specifying URLs for clickable links.
#' @param image_filter `<string | param()>` A CSS filter; a constant string used to adjust the rendering of images, such as *blur(5px)*.
#' @param length `<column | literal | transform() | sql()/agg() | list(value=, ...)>` The vector’s length; either an optional channel bound to the *length* scale or a constant number in pixels.
#' @param margin `<number | param()>` Shorthand to set the same default for all four mark margins: **marginTop**, **marginRight**, **marginBottom**, and **marginLeft**; typically defaults to 0, except for axis marks.
#' @param margin_bottom `<number | param()>` The mark’s bottom margin; the minimum distance in pixels between the bottom edges of the inner and outer plot area.
#' @param margin_left `<number | param()>` The mark’s left margin; the minimum distance in pixels between the left edges of the inner and outer plot area.
#' @param margin_right `<number | param()>` The mark’s right margin; the minimum distance in pixels between the right edges of the mark’s inner and outer plot area.
#' @param margin_top `<number | param()>` The mark’s top margin; the minimum distance in pixels between the top edges of the inner and outer plot area.
#' @param mix_blend_mode `<string | param()>` The mix-blend-mode; a constant string specifying how to blend content such as *multiply*.
#' @param opacity `<column | literal | transform() | sql()/agg() | list(value=, ...)>` The opacity; a constant between 0 and 1, or a channel typically bound to the *opacity* scale.
#' @param paint_order `<string | param()>` The paint-order; a constant string specifying the order in which the **fill**, **stroke**, and any markers are drawn; defaults to *normal*, which draws the fill, then stroke, then markers; defaults to *stroke* for the text mark to create a “halo” around text to improve legibility.
#' @param pointer_events `<string | param()>` The pointer-events property; a constant string such as *none*.
#' @param r `<column | literal | transform() | sql()/agg() | list(value=, ...) | number | param()>` The radius of dots; either a channel or constant.
#' @param reverse `<boolean | param()>` Applies a transform to reverse the order of the mark’s index, say for reverse input order.
#' @param rotate `<column | literal | transform() | sql()/agg() | param()>` The rotation angle in degrees clockwise; a constant or a channel; defaults to 0°.
#' @param select `<"first" | "last" | "maxX" | "maxY" | "minX" | "minY" | "nearest" | "nearestX" | "nearestY">` Applies a filter transform after data is loaded to highlight selected values only.
#' @param shape `<"arrow" | "spike" | param()>` The shape of the vector; a constant.
#' @param shape_rendering `<string | param()>` The shape-rendering; a constant string such as *crispEdges*.
#' @param sort `<column | literal | transform() | sql()/agg() | list>` Either applies a transform to sort the mark’s index by the specified channel values, or imputes ordinal scale domains from this mark’s channels.
#' @param stroke `<column | literal | transform() | sql()/agg() | list(value=, ...) | param()>` The stroke; a constant CSS color string, or a channel typically bound to the *color* scale.
#' @param stroke_dasharray `<string | number | param()>` The stroke-dasharray; a constant number indicating the length in pixels of alternating dashes and gaps, or a constant string of numbers separated by spaces or commas (_e.g._, *10 2* for dashes of 10 pixels separated by gaps of 2 pixels), or *none* (the default) for no dashing : https://developer.mozilla.org/en-US/docs/Web/SVG/Attribute/stroke-dasharray
#' @param stroke_dashoffset `<string | number | param()>` The stroke-dashoffset; a constant indicating the offset in pixels of the first dash along the stroke; defaults to zero.
#' @param stroke_linecap `<string | param()>` The stroke-linecap; a constant specifying how to cap stroked paths, such as *butt*, *round*, or *square*.
#' @param stroke_linejoin `<string | param()>` The stroke-linejoin; a constant specifying how to join stroked paths, such as *bevel*, *miter*, *miter-clip*, or *round*.
#' @param stroke_miterlimit `<number | param()>` The stroke-miterlimit; a constant number specifying how to limit the length of *miter* joins on stroked paths.
#' @param stroke_opacity `<column | literal | transform() | sql()/agg() | list(value=, ...)>` The stroke-opacity; a constant between 0 and 1, or a channel typically bound to the *opacity* scale.
#' @param stroke_width `<column | literal | transform() | sql()/agg() | list(value=, ...)>` The stroke-width; a constant number in pixels, or a channel.
#' @param target `<string | param()>` The target; a constant string specifying the target window (_e.g._, *_blank*) for clickable links; used in conjunction with the **href** option.
#' @param tip `<boolean | "x" | "y" | "xy" | list | param()>` Whether to generate a tooltip for this mark, and any tip options.
#' @param title `<column | literal | transform() | sql()/agg()>` The title; a channel specifying accessible, short textual descriptions as strings (possibly with newlines).
#' @param x `<column | literal | transform() | sql()/agg() | list(value=, ...)>` The horizontal position (or length) channel, typically bound to the *x* scale.
#' @param y `<column | literal | transform() | sql()/agg() | list(value=, ...)>` The vertical position channel, typically bound to the *y* scale; defaults to the zero-based index of the data 0, 1, 2, ….
#' @param ... Additional options or plot-level attributes.
#' @param data_from The name of the data source this mark reads from (see [vg_data()]); a length-1 nonzero R integer (`1L`, `-1L`, ...; note the `L`) giving a 1-based index into the spec's registered data sources instead, negative counting from the end (e.g., `-1L` for the most recently registered one); or a literal vector of values to use as inline data directly (mosaic-spec's `"data": [...]` shorthand, e.g., for a single reference line -- a bare double like `0`/`c(0)`, or `0L` itself (never a valid index), means this, not an index). Left unset, defaults to the first registered data source (equivalent to `data_from = 1L`) for any mark type that takes data at all.
#' @param filter_by A Param/Selection (e.g., from [param()]) to filter this mark's data by.
#' @param data_optimize A flag (default `TRUE`) to enable mark-specific query optimizations for this mark's data; set `FALSE` to disable them (mosaic-spec's `data: {optimize: false}`).
#' @family mark functions
#' @export
vg_mark_vector_y <- function(spec = NULL, formula = vg_unset, anchor = vg_unset, aria_description = vg_unset, aria_hidden = vg_unset, aria_label = vg_unset, channels = vg_unset, clip = vg_unset, dx = vg_unset, dy = vg_unset, facet = vg_unset, facet_anchor = vg_unset, fill = vg_unset, fill_opacity = vg_unset, filter = vg_unset, frame_anchor = vg_unset, fx = vg_unset, fy = vg_unset, href = vg_unset, image_filter = vg_unset, length = vg_unset, margin = vg_unset, margin_bottom = vg_unset, margin_left = vg_unset, margin_right = vg_unset, margin_top = vg_unset, mix_blend_mode = vg_unset, opacity = vg_unset, paint_order = vg_unset, pointer_events = vg_unset, r = vg_unset, reverse = vg_unset, rotate = vg_unset, select = vg_unset, shape = vg_unset, shape_rendering = vg_unset, sort = vg_unset, stroke = vg_unset, stroke_dasharray = vg_unset, stroke_dashoffset = vg_unset, stroke_linecap = vg_unset, stroke_linejoin = vg_unset, stroke_miterlimit = vg_unset, stroke_opacity = vg_unset, stroke_width = vg_unset, target = vg_unset, tip = vg_unset, title = vg_unset, x = vg_unset, y = vg_unset, ..., data_from = vg_unset, filter_by = vg_unset, data_optimize = vg_unset) {
  vg_mark_(spec, "vectorY", formula = formula, anchor = anchor, ariaDescription = aria_description, ariaHidden = aria_hidden, ariaLabel = aria_label, channels = channels, clip = clip, dx = dx, dy = dy, facet = facet, facetAnchor = facet_anchor, fill = fill, fillOpacity = fill_opacity, filter = filter, frameAnchor = frame_anchor, fx = fx, fy = fy, href = href, imageFilter = image_filter, length = length, margin = margin, marginBottom = margin_bottom, marginLeft = margin_left, marginRight = margin_right, marginTop = margin_top, mixBlendMode = mix_blend_mode, opacity = opacity, paintOrder = paint_order, pointerEvents = pointer_events, r = r, reverse = reverse, rotate = rotate, select = select, shape = shape, shapeRendering = shape_rendering, sort = sort, stroke = stroke, strokeDasharray = stroke_dasharray, strokeDashoffset = stroke_dashoffset, strokeLinecap = stroke_linecap, strokeLinejoin = stroke_linejoin, strokeMiterlimit = stroke_miterlimit, strokeOpacity = stroke_opacity, strokeWidth = stroke_width, target = target, tip = tip, title = title, x = x, y = y, ..., data_from = data_from, filter_by = filter_by, data_optimize = data_optimize)
}

#' The voronoi mark.
#'
#' See [vg_value_types] for what the `<...>` notation below (`column`, `param()`, ...) means.
#'
#' @param spec A plot fragment or `vgspec` to add this mark to, or `NULL` to start a new plot with just this mark.
#' @param formula A shorthand for this mark's position channels (`x`, `y`, `fx`, `fy`, and paired `x1`/`x2` or `y1`/`y2`), e.g., `Sepal.Length ~ Sepal.Width | ~ Species` for `y = ~Sepal.Length, x = ~Sepal.Width, fx = ~Species`. See [vg_mark()] for the full grammar.
#' @param aria_description `<string | param()>` The aria-description; a constant textual description.
#' @param aria_hidden `<string | param()>` The aria-hidden state; a constant indicating whether the element is exposed to an accessibility API.
#' @param aria_label `<column | literal | transform() | sql()/agg()>` The aria-label; a channel specifying short textual labels representing the value in the accessibility tree.
#' @param channels `<list>` Additional named channels, for example to include in a tooltip.
#' @param clip `<"frame" | "sphere" | boolean | NULL | param()>` How to clip the mark; one of: - *frame* or true - clip to the plot’s frame (inner area) - *sphere* - clip to the projected sphere (*e.g.*, front hemisphere) - null or false - do not clip The *sphere* clip option requires a geographic projection.
#' @param curve `<"basis" | "basis-closed" | "basis-open" | "bundle" | "bump-x" | "bump-y" | "cardinal" | "cardinal-closed" | "cardinal-open" | "catmull-rom" | ... | param()>` The curve (interpolation) method for connecting adjacent points.
#' @param dx `<number | param()>` The horizontal offset in pixels; a constant option.
#' @param dy `<number | param()>` The vertical offset in pixels; a constant option.
#' @param facet `<"auto" | "include" | "exclude" | "super" | boolean | NULL | param()>` Whether to enable or disable faceting; one of: - *auto* (default) - automatically determine if this mark should be faceted - *include* (or true) - draw the subset of the mark’s data in the current facet - *exclude* - draw the subset of the mark’s data *not* in the current facet - *super* - draw this mark in a single frame that covers all facets - null (or false) - repeat this mark’s data across all facets (*i.e.*, no faceting) When a mark uses *super* faceting, it is not allowed to use position scales (*x*, *y*, *fx*, or *fy*); *super* faceting is intended for decorations, such as labels and legends.
#' @param facet_anchor `<"top" | "right" | "bottom" | "left" | "top-left" | "top-right" | "bottom-left" | "bottom-right" | "top-empty" | "right-empty" | "bottom-empty" | "left-empty" | "empty" | NULL | param()>` How to place the mark with respect to facets; one of: - null (default for most marks) - display the mark in each non-empty facet - *top*, *right*, *bottom*, or *left* - display the mark only in facets on the given side - *top-empty*, *right-empty*, *bottom-empty*, or *left-empty* (default for axis marks) - display the mark only in facets that have empty space on the given side: either the margin, or an empty facet - *empty* - display the mark in empty facets only
#' @param fill `<column | literal | transform() | sql()/agg() | list(value=, ...) | param()>` The fill; a constant CSS color string, or a channel typically bound to the *color* scale.
#' @param fill_opacity `<column | literal | transform() | sql()/agg() | list(value=, ...) | param()>` The fill-opacity; a constant number between 0 and 1, or a channel typically bound to the *opacity* scale.
#' @param filter `<column | literal | transform() | sql()/agg()>` Applies a transform to filter the mark’s index according to the given channel values; only truthy values are retained.
#' @param fx `<column | literal | transform() | sql()/agg()>` The horizontal facet position channel, for mark-level faceting, bound to the *fx* scale.
#' @param fy `<column | literal | transform() | sql()/agg()>` The vertical facet position channel, for mark-level faceting, bound to the *fy* scale.
#' @param href `<column | literal | transform() | sql()/agg()>` The href; a channel specifying URLs for clickable links.
#' @param image_filter `<string | param()>` A CSS filter; a constant string used to adjust the rendering of images, such as *blur(5px)*.
#' @param margin `<number | param()>` Shorthand to set the same default for all four mark margins: **marginTop**, **marginRight**, **marginBottom**, and **marginLeft**; typically defaults to 0, except for axis marks.
#' @param margin_bottom `<number | param()>` The mark’s bottom margin; the minimum distance in pixels between the bottom edges of the inner and outer plot area.
#' @param margin_left `<number | param()>` The mark’s left margin; the minimum distance in pixels between the left edges of the inner and outer plot area.
#' @param margin_right `<number | param()>` The mark’s right margin; the minimum distance in pixels between the right edges of the mark’s inner and outer plot area.
#' @param margin_top `<number | param()>` The mark’s top margin; the minimum distance in pixels between the top edges of the inner and outer plot area.
#' @param marker `<"arrow" | "arrow-reverse" | "dot" | "circle" | "circle-fill" | "circle-stroke" | "tick" | "tick-x" | "tick-y" | "none" | boolean | NULL | param()>` Shorthand to set the same default for markerStart, markerMid, and markerEnd; one of: - a marker name such as *arrow* or *circle* - *none* (default) - no marker * true - alias for *circle-fill* * false or null - alias for *none*
#' @param marker_end `<"arrow" | "arrow-reverse" | "dot" | "circle" | "circle-fill" | "circle-stroke" | "tick" | "tick-x" | "tick-y" | "none" | boolean | NULL | param()>` The marker for the ending point of a line segment; one of: - a marker name such as *arrow* or *circle* * *none* (default) - no marker * true - alias for *circle-fill* * false or null - alias for *none*
#' @param marker_mid `<"arrow" | "arrow-reverse" | "dot" | "circle" | "circle-fill" | "circle-stroke" | "tick" | "tick-x" | "tick-y" | "none" | boolean | NULL | param()>` The marker for any middle (interior) points of a line segment.
#' @param marker_start `<"arrow" | "arrow-reverse" | "dot" | "circle" | "circle-fill" | "circle-stroke" | "tick" | "tick-x" | "tick-y" | "none" | boolean | NULL | param()>` The marker for the starting point of a line segment; one of: - a marker name such as *arrow* or *circle* * *none* (default) - no marker * true - alias for *circle-fill* * false or null - alias for *none*
#' @param mix_blend_mode `<string | param()>` The mix-blend-mode; a constant string specifying how to blend content such as *multiply*.
#' @param opacity `<column | literal | transform() | sql()/agg() | list(value=, ...)>` The opacity; a constant between 0 and 1, or a channel typically bound to the *opacity* scale.
#' @param paint_order `<string | param()>` The paint-order; a constant string specifying the order in which the **fill**, **stroke**, and any markers are drawn; defaults to *normal*, which draws the fill, then stroke, then markers; defaults to *stroke* for the text mark to create a “halo” around text to improve legibility.
#' @param pointer_events `<string | param()>` The pointer-events property; a constant string such as *none*.
#' @param reverse `<boolean | param()>` Applies a transform to reverse the order of the mark’s index, say for reverse input order.
#' @param select `<"first" | "last" | "maxX" | "maxY" | "minX" | "minY" | "nearest" | "nearestX" | "nearestY">` Applies a filter transform after data is loaded to highlight selected values only.
#' @param shape_rendering `<string | param()>` The shape-rendering; a constant string such as *crispEdges*.
#' @param sort `<column | literal | transform() | sql()/agg() | list>` Either applies a transform to sort the mark’s index by the specified channel values, or imputes ordinal scale domains from this mark’s channels.
#' @param stroke `<column | literal | transform() | sql()/agg() | list(value=, ...) | param()>` The stroke; a constant CSS color string, or a channel typically bound to the *color* scale.
#' @param stroke_dasharray `<string | number | param()>` The stroke-dasharray; a constant number indicating the length in pixels of alternating dashes and gaps, or a constant string of numbers separated by spaces or commas (_e.g._, *10 2* for dashes of 10 pixels separated by gaps of 2 pixels), or *none* (the default) for no dashing : https://developer.mozilla.org/en-US/docs/Web/SVG/Attribute/stroke-dasharray
#' @param stroke_dashoffset `<string | number | param()>` The stroke-dashoffset; a constant indicating the offset in pixels of the first dash along the stroke; defaults to zero.
#' @param stroke_linecap `<string | param()>` The stroke-linecap; a constant specifying how to cap stroked paths, such as *butt*, *round*, or *square*.
#' @param stroke_linejoin `<string | param()>` The stroke-linejoin; a constant specifying how to join stroked paths, such as *bevel*, *miter*, *miter-clip*, or *round*.
#' @param stroke_miterlimit `<number | param()>` The stroke-miterlimit; a constant number specifying how to limit the length of *miter* joins on stroked paths.
#' @param stroke_opacity `<column | literal | transform() | sql()/agg() | list(value=, ...)>` The stroke-opacity; a constant between 0 and 1, or a channel typically bound to the *opacity* scale.
#' @param stroke_width `<column | literal | transform() | sql()/agg() | list(value=, ...)>` The stroke-width; a constant number in pixels, or a channel.
#' @param target `<string | param()>` The target; a constant string specifying the target window (_e.g._, *_blank*) for clickable links; used in conjunction with the **href** option.
#' @param tension `<number | param()>` The tension option only has an effect on bundle, cardinal and Catmull–Rom splines (*bundle*, *cardinal*, *cardinal-open*, *cardinal-closed*, *catmull-rom*, *catmull-rom-open*, and *catmull-rom-closed*).
#' @param tip `<boolean | "x" | "y" | "xy" | list | param()>` Whether to generate a tooltip for this mark, and any tip options.
#' @param title `<column | literal | transform() | sql()/agg()>` The title; a channel specifying accessible, short textual descriptions as strings (possibly with newlines).
#' @param x `<column | literal | transform() | sql()/agg() | list(value=, ...)>` The horizontal position (or length) channel, typically bound to the *x* scale.
#' @param y `<column | literal | transform() | sql()/agg() | list(value=, ...)>` The vertical position channel, typically bound to the *y* scale; defaults to the zero-based index of the data 0, 1, 2, ….
#' @param z `<column | literal | transform() | sql()/agg()>` An optional ordinal channel for grouping data into (possibly stacked) series to be drawn as separate areas; defaults to **fill** if a channel, or **stroke** if a channel.
#' @param ... Additional options or plot-level attributes.
#' @param data_from The name of the data source this mark reads from (see [vg_data()]); a length-1 nonzero R integer (`1L`, `-1L`, ...; note the `L`) giving a 1-based index into the spec's registered data sources instead, negative counting from the end (e.g., `-1L` for the most recently registered one); or a literal vector of values to use as inline data directly (mosaic-spec's `"data": [...]` shorthand, e.g., for a single reference line -- a bare double like `0`/`c(0)`, or `0L` itself (never a valid index), means this, not an index). Left unset, defaults to the first registered data source (equivalent to `data_from = 1L`) for any mark type that takes data at all.
#' @param filter_by A Param/Selection (e.g., from [param()]) to filter this mark's data by.
#' @param data_optimize A flag (default `TRUE`) to enable mark-specific query optimizations for this mark's data; set `FALSE` to disable them (mosaic-spec's `data: {optimize: false}`).
#' @family mark functions
#' @export
vg_mark_voronoi <- function(spec = NULL, formula = vg_unset, aria_description = vg_unset, aria_hidden = vg_unset, aria_label = vg_unset, channels = vg_unset, clip = vg_unset, curve = vg_unset, dx = vg_unset, dy = vg_unset, facet = vg_unset, facet_anchor = vg_unset, fill = vg_unset, fill_opacity = vg_unset, filter = vg_unset, fx = vg_unset, fy = vg_unset, href = vg_unset, image_filter = vg_unset, margin = vg_unset, margin_bottom = vg_unset, margin_left = vg_unset, margin_right = vg_unset, margin_top = vg_unset, marker = vg_unset, marker_end = vg_unset, marker_mid = vg_unset, marker_start = vg_unset, mix_blend_mode = vg_unset, opacity = vg_unset, paint_order = vg_unset, pointer_events = vg_unset, reverse = vg_unset, select = vg_unset, shape_rendering = vg_unset, sort = vg_unset, stroke = vg_unset, stroke_dasharray = vg_unset, stroke_dashoffset = vg_unset, stroke_linecap = vg_unset, stroke_linejoin = vg_unset, stroke_miterlimit = vg_unset, stroke_opacity = vg_unset, stroke_width = vg_unset, target = vg_unset, tension = vg_unset, tip = vg_unset, title = vg_unset, x = vg_unset, y = vg_unset, z = vg_unset, ..., data_from = vg_unset, filter_by = vg_unset, data_optimize = vg_unset) {
  vg_mark_(spec, "voronoi", formula = formula, ariaDescription = aria_description, ariaHidden = aria_hidden, ariaLabel = aria_label, channels = channels, clip = clip, curve = curve, dx = dx, dy = dy, facet = facet, facetAnchor = facet_anchor, fill = fill, fillOpacity = fill_opacity, filter = filter, fx = fx, fy = fy, href = href, imageFilter = image_filter, margin = margin, marginBottom = margin_bottom, marginLeft = margin_left, marginRight = margin_right, marginTop = margin_top, marker = marker, markerEnd = marker_end, markerMid = marker_mid, markerStart = marker_start, mixBlendMode = mix_blend_mode, opacity = opacity, paintOrder = paint_order, pointerEvents = pointer_events, reverse = reverse, select = select, shapeRendering = shape_rendering, sort = sort, stroke = stroke, strokeDasharray = stroke_dasharray, strokeDashoffset = stroke_dashoffset, strokeLinecap = stroke_linecap, strokeLinejoin = stroke_linejoin, strokeMiterlimit = stroke_miterlimit, strokeOpacity = stroke_opacity, strokeWidth = stroke_width, target = target, tension = tension, tip = tip, title = title, x = x, y = y, z = z, ..., data_from = data_from, filter_by = filter_by, data_optimize = data_optimize)
}

#' The voronoiMesh mark.
#'
#' See [vg_value_types] for what the `<...>` notation below (`column`, `param()`, ...) means.
#'
#' @param spec A plot fragment or `vgspec` to add this mark to, or `NULL` to start a new plot with just this mark.
#' @param formula A shorthand for this mark's position channels (`x`, `y`, `fx`, `fy`, and paired `x1`/`x2` or `y1`/`y2`), e.g., `Sepal.Length ~ Sepal.Width | ~ Species` for `y = ~Sepal.Length, x = ~Sepal.Width, fx = ~Species`. See [vg_mark()] for the full grammar.
#' @param aria_description `<string | param()>` The aria-description; a constant textual description.
#' @param aria_hidden `<string | param()>` The aria-hidden state; a constant indicating whether the element is exposed to an accessibility API.
#' @param aria_label `<column | literal | transform() | sql()/agg()>` The aria-label; a channel specifying short textual labels representing the value in the accessibility tree.
#' @param channels `<list>` Additional named channels, for example to include in a tooltip.
#' @param clip `<"frame" | "sphere" | boolean | NULL | param()>` How to clip the mark; one of: - *frame* or true - clip to the plot’s frame (inner area) - *sphere* - clip to the projected sphere (*e.g.*, front hemisphere) - null or false - do not clip The *sphere* clip option requires a geographic projection.
#' @param curve `<"basis" | "basis-closed" | "basis-open" | "bundle" | "bump-x" | "bump-y" | "cardinal" | "cardinal-closed" | "cardinal-open" | "catmull-rom" | ... | param()>` The curve (interpolation) method for connecting adjacent points.
#' @param dx `<number | param()>` The horizontal offset in pixels; a constant option.
#' @param dy `<number | param()>` The vertical offset in pixels; a constant option.
#' @param facet `<"auto" | "include" | "exclude" | "super" | boolean | NULL | param()>` Whether to enable or disable faceting; one of: - *auto* (default) - automatically determine if this mark should be faceted - *include* (or true) - draw the subset of the mark’s data in the current facet - *exclude* - draw the subset of the mark’s data *not* in the current facet - *super* - draw this mark in a single frame that covers all facets - null (or false) - repeat this mark’s data across all facets (*i.e.*, no faceting) When a mark uses *super* faceting, it is not allowed to use position scales (*x*, *y*, *fx*, or *fy*); *super* faceting is intended for decorations, such as labels and legends.
#' @param facet_anchor `<"top" | "right" | "bottom" | "left" | "top-left" | "top-right" | "bottom-left" | "bottom-right" | "top-empty" | "right-empty" | "bottom-empty" | "left-empty" | "empty" | NULL | param()>` How to place the mark with respect to facets; one of: - null (default for most marks) - display the mark in each non-empty facet - *top*, *right*, *bottom*, or *left* - display the mark only in facets on the given side - *top-empty*, *right-empty*, *bottom-empty*, or *left-empty* (default for axis marks) - display the mark only in facets that have empty space on the given side: either the margin, or an empty facet - *empty* - display the mark in empty facets only
#' @param fill `<column | literal | transform() | sql()/agg() | list(value=, ...) | param()>` The fill; a constant CSS color string, or a channel typically bound to the *color* scale.
#' @param fill_opacity `<column | literal | transform() | sql()/agg() | list(value=, ...) | param()>` The fill-opacity; a constant number between 0 and 1, or a channel typically bound to the *opacity* scale.
#' @param filter `<column | literal | transform() | sql()/agg()>` Applies a transform to filter the mark’s index according to the given channel values; only truthy values are retained.
#' @param fx `<column | literal | transform() | sql()/agg()>` The horizontal facet position channel, for mark-level faceting, bound to the *fx* scale.
#' @param fy `<column | literal | transform() | sql()/agg()>` The vertical facet position channel, for mark-level faceting, bound to the *fy* scale.
#' @param href `<column | literal | transform() | sql()/agg()>` The href; a channel specifying URLs for clickable links.
#' @param image_filter `<string | param()>` A CSS filter; a constant string used to adjust the rendering of images, such as *blur(5px)*.
#' @param margin `<number | param()>` Shorthand to set the same default for all four mark margins: **marginTop**, **marginRight**, **marginBottom**, and **marginLeft**; typically defaults to 0, except for axis marks.
#' @param margin_bottom `<number | param()>` The mark’s bottom margin; the minimum distance in pixels between the bottom edges of the inner and outer plot area.
#' @param margin_left `<number | param()>` The mark’s left margin; the minimum distance in pixels between the left edges of the inner and outer plot area.
#' @param margin_right `<number | param()>` The mark’s right margin; the minimum distance in pixels between the right edges of the mark’s inner and outer plot area.
#' @param margin_top `<number | param()>` The mark’s top margin; the minimum distance in pixels between the top edges of the inner and outer plot area.
#' @param marker `<"arrow" | "arrow-reverse" | "dot" | "circle" | "circle-fill" | "circle-stroke" | "tick" | "tick-x" | "tick-y" | "none" | boolean | NULL | param()>` Shorthand to set the same default for markerStart, markerMid, and markerEnd; one of: - a marker name such as *arrow* or *circle* - *none* (default) - no marker * true - alias for *circle-fill* * false or null - alias for *none*
#' @param marker_end `<"arrow" | "arrow-reverse" | "dot" | "circle" | "circle-fill" | "circle-stroke" | "tick" | "tick-x" | "tick-y" | "none" | boolean | NULL | param()>` The marker for the ending point of a line segment; one of: - a marker name such as *arrow* or *circle* * *none* (default) - no marker * true - alias for *circle-fill* * false or null - alias for *none*
#' @param marker_mid `<"arrow" | "arrow-reverse" | "dot" | "circle" | "circle-fill" | "circle-stroke" | "tick" | "tick-x" | "tick-y" | "none" | boolean | NULL | param()>` The marker for any middle (interior) points of a line segment.
#' @param marker_start `<"arrow" | "arrow-reverse" | "dot" | "circle" | "circle-fill" | "circle-stroke" | "tick" | "tick-x" | "tick-y" | "none" | boolean | NULL | param()>` The marker for the starting point of a line segment; one of: - a marker name such as *arrow* or *circle* * *none* (default) - no marker * true - alias for *circle-fill* * false or null - alias for *none*
#' @param mix_blend_mode `<string | param()>` The mix-blend-mode; a constant string specifying how to blend content such as *multiply*.
#' @param opacity `<column | literal | transform() | sql()/agg() | list(value=, ...)>` The opacity; a constant between 0 and 1, or a channel typically bound to the *opacity* scale.
#' @param paint_order `<string | param()>` The paint-order; a constant string specifying the order in which the **fill**, **stroke**, and any markers are drawn; defaults to *normal*, which draws the fill, then stroke, then markers; defaults to *stroke* for the text mark to create a “halo” around text to improve legibility.
#' @param pointer_events `<string | param()>` The pointer-events property; a constant string such as *none*.
#' @param reverse `<boolean | param()>` Applies a transform to reverse the order of the mark’s index, say for reverse input order.
#' @param select `<"first" | "last" | "maxX" | "maxY" | "minX" | "minY" | "nearest" | "nearestX" | "nearestY">` Applies a filter transform after data is loaded to highlight selected values only.
#' @param shape_rendering `<string | param()>` The shape-rendering; a constant string such as *crispEdges*.
#' @param sort `<column | literal | transform() | sql()/agg() | list>` Either applies a transform to sort the mark’s index by the specified channel values, or imputes ordinal scale domains from this mark’s channels.
#' @param stroke `<column | literal | transform() | sql()/agg() | list(value=, ...) | param()>` The stroke; a constant CSS color string, or a channel typically bound to the *color* scale.
#' @param stroke_dasharray `<string | number | param()>` The stroke-dasharray; a constant number indicating the length in pixels of alternating dashes and gaps, or a constant string of numbers separated by spaces or commas (_e.g._, *10 2* for dashes of 10 pixels separated by gaps of 2 pixels), or *none* (the default) for no dashing : https://developer.mozilla.org/en-US/docs/Web/SVG/Attribute/stroke-dasharray
#' @param stroke_dashoffset `<string | number | param()>` The stroke-dashoffset; a constant indicating the offset in pixels of the first dash along the stroke; defaults to zero.
#' @param stroke_linecap `<string | param()>` The stroke-linecap; a constant specifying how to cap stroked paths, such as *butt*, *round*, or *square*.
#' @param stroke_linejoin `<string | param()>` The stroke-linejoin; a constant specifying how to join stroked paths, such as *bevel*, *miter*, *miter-clip*, or *round*.
#' @param stroke_miterlimit `<number | param()>` The stroke-miterlimit; a constant number specifying how to limit the length of *miter* joins on stroked paths.
#' @param stroke_opacity `<column | literal | transform() | sql()/agg() | list(value=, ...)>` The stroke-opacity; a constant between 0 and 1, or a channel typically bound to the *opacity* scale.
#' @param stroke_width `<column | literal | transform() | sql()/agg() | list(value=, ...)>` The stroke-width; a constant number in pixels, or a channel.
#' @param target `<string | param()>` The target; a constant string specifying the target window (_e.g._, *_blank*) for clickable links; used in conjunction with the **href** option.
#' @param tension `<number | param()>` The tension option only has an effect on bundle, cardinal and Catmull–Rom splines (*bundle*, *cardinal*, *cardinal-open*, *cardinal-closed*, *catmull-rom*, *catmull-rom-open*, and *catmull-rom-closed*).
#' @param tip `<boolean | "x" | "y" | "xy" | list | param()>` Whether to generate a tooltip for this mark, and any tip options.
#' @param title `<column | literal | transform() | sql()/agg()>` The title; a channel specifying accessible, short textual descriptions as strings (possibly with newlines).
#' @param x `<column | literal | transform() | sql()/agg() | list(value=, ...)>` The horizontal position (or length) channel, typically bound to the *x* scale.
#' @param y `<column | literal | transform() | sql()/agg() | list(value=, ...)>` The vertical position channel, typically bound to the *y* scale; defaults to the zero-based index of the data 0, 1, 2, ….
#' @param z `<column | literal | transform() | sql()/agg()>` An optional ordinal channel for grouping data into (possibly stacked) series to be drawn as separate areas; defaults to **fill** if a channel, or **stroke** if a channel.
#' @param ... Additional options or plot-level attributes.
#' @param data_from The name of the data source this mark reads from (see [vg_data()]); a length-1 nonzero R integer (`1L`, `-1L`, ...; note the `L`) giving a 1-based index into the spec's registered data sources instead, negative counting from the end (e.g., `-1L` for the most recently registered one); or a literal vector of values to use as inline data directly (mosaic-spec's `"data": [...]` shorthand, e.g., for a single reference line -- a bare double like `0`/`c(0)`, or `0L` itself (never a valid index), means this, not an index). Left unset, defaults to the first registered data source (equivalent to `data_from = 1L`) for any mark type that takes data at all.
#' @param filter_by A Param/Selection (e.g., from [param()]) to filter this mark's data by.
#' @param data_optimize A flag (default `TRUE`) to enable mark-specific query optimizations for this mark's data; set `FALSE` to disable them (mosaic-spec's `data: {optimize: false}`).
#' @family mark functions
#' @export
vg_mark_voronoi_mesh <- function(spec = NULL, formula = vg_unset, aria_description = vg_unset, aria_hidden = vg_unset, aria_label = vg_unset, channels = vg_unset, clip = vg_unset, curve = vg_unset, dx = vg_unset, dy = vg_unset, facet = vg_unset, facet_anchor = vg_unset, fill = vg_unset, fill_opacity = vg_unset, filter = vg_unset, fx = vg_unset, fy = vg_unset, href = vg_unset, image_filter = vg_unset, margin = vg_unset, margin_bottom = vg_unset, margin_left = vg_unset, margin_right = vg_unset, margin_top = vg_unset, marker = vg_unset, marker_end = vg_unset, marker_mid = vg_unset, marker_start = vg_unset, mix_blend_mode = vg_unset, opacity = vg_unset, paint_order = vg_unset, pointer_events = vg_unset, reverse = vg_unset, select = vg_unset, shape_rendering = vg_unset, sort = vg_unset, stroke = vg_unset, stroke_dasharray = vg_unset, stroke_dashoffset = vg_unset, stroke_linecap = vg_unset, stroke_linejoin = vg_unset, stroke_miterlimit = vg_unset, stroke_opacity = vg_unset, stroke_width = vg_unset, target = vg_unset, tension = vg_unset, tip = vg_unset, title = vg_unset, x = vg_unset, y = vg_unset, z = vg_unset, ..., data_from = vg_unset, filter_by = vg_unset, data_optimize = vg_unset) {
  vg_mark_(spec, "voronoiMesh", formula = formula, ariaDescription = aria_description, ariaHidden = aria_hidden, ariaLabel = aria_label, channels = channels, clip = clip, curve = curve, dx = dx, dy = dy, facet = facet, facetAnchor = facet_anchor, fill = fill, fillOpacity = fill_opacity, filter = filter, fx = fx, fy = fy, href = href, imageFilter = image_filter, margin = margin, marginBottom = margin_bottom, marginLeft = margin_left, marginRight = margin_right, marginTop = margin_top, marker = marker, markerEnd = marker_end, markerMid = marker_mid, markerStart = marker_start, mixBlendMode = mix_blend_mode, opacity = opacity, paintOrder = paint_order, pointerEvents = pointer_events, reverse = reverse, select = select, shapeRendering = shape_rendering, sort = sort, stroke = stroke, strokeDasharray = stroke_dasharray, strokeDashoffset = stroke_dashoffset, strokeLinecap = stroke_linecap, strokeLinejoin = stroke_linejoin, strokeMiterlimit = stroke_miterlimit, strokeOpacity = stroke_opacity, strokeWidth = stroke_width, target = target, tension = tension, tip = tip, title = title, x = x, y = y, z = z, ..., data_from = data_from, filter_by = filter_by, data_optimize = data_optimize)
}

#' The waffleX mark.
#'
#' See [vg_value_types] for what the `<...>` notation below (`column`, `param()`, ...) means.
#'
#' @param spec A plot fragment or `vgspec` to add this mark to, or `NULL` to start a new plot with just this mark.
#' @param formula A shorthand for this mark's position channels (`x`, `y`, `fx`, `fy`, and paired `x1`/`x2` or `y1`/`y2`), e.g., `Sepal.Length ~ Sepal.Width | ~ Species` for `y = ~Sepal.Length, x = ~Sepal.Width, fx = ~Species`. See [vg_mark()] for the full grammar.
#' @param aria_description `<string | param()>` The aria-description; a constant textual description.
#' @param aria_hidden `<string | param()>` The aria-hidden state; a constant indicating whether the element is exposed to an accessibility API.
#' @param aria_label `<column | literal | transform() | sql()/agg()>` The aria-label; a channel specifying short textual labels representing the value in the accessibility tree.
#' @param channels `<list>` Additional named channels, for example to include in a tooltip.
#' @param clip `<"frame" | "sphere" | boolean | NULL | param()>` How to clip the mark; one of: - *frame* or true - clip to the plot’s frame (inner area) - *sphere* - clip to the projected sphere (*e.g.*, front hemisphere) - null or false - do not clip The *sphere* clip option requires a geographic projection.
#' @param dx `<number | param()>` The horizontal offset in pixels; a constant option.
#' @param dy `<number | param()>` The vertical offset in pixels; a constant option.
#' @param facet `<"auto" | "include" | "exclude" | "super" | boolean | NULL | param()>` Whether to enable or disable faceting; one of: - *auto* (default) - automatically determine if this mark should be faceted - *include* (or true) - draw the subset of the mark’s data in the current facet - *exclude* - draw the subset of the mark’s data *not* in the current facet - *super* - draw this mark in a single frame that covers all facets - null (or false) - repeat this mark’s data across all facets (*i.e.*, no faceting) When a mark uses *super* faceting, it is not allowed to use position scales (*x*, *y*, *fx*, or *fy*); *super* faceting is intended for decorations, such as labels and legends.
#' @param facet_anchor `<"top" | "right" | "bottom" | "left" | "top-left" | "top-right" | "bottom-left" | "bottom-right" | "top-empty" | "right-empty" | "bottom-empty" | "left-empty" | "empty" | NULL | param()>` How to place the mark with respect to facets; one of: - null (default for most marks) - display the mark in each non-empty facet - *top*, *right*, *bottom*, or *left* - display the mark only in facets on the given side - *top-empty*, *right-empty*, *bottom-empty*, or *left-empty* (default for axis marks) - display the mark only in facets that have empty space on the given side: either the margin, or an empty facet - *empty* - display the mark in empty facets only
#' @param fill `<column | literal | transform() | sql()/agg() | list(value=, ...) | param()>` The fill; a constant CSS color string, or a channel typically bound to the *color* scale.
#' @param fill_opacity `<column | literal | transform() | sql()/agg() | list(value=, ...) | param()>` The fill-opacity; a constant number between 0 and 1, or a channel typically bound to the *opacity* scale.
#' @param filter `<column | literal | transform() | sql()/agg()>` Applies a transform to filter the mark’s index according to the given channel values; only truthy values are retained.
#' @param fx `<column | literal | transform() | sql()/agg()>` The horizontal facet position channel, for mark-level faceting, bound to the *fx* scale.
#' @param fy `<column | literal | transform() | sql()/agg()>` The vertical facet position channel, for mark-level faceting, bound to the *fy* scale.
#' @param gap `<number | param()>` The gap in pixels between cells; defaults to 1.
#' @param href `<column | literal | transform() | sql()/agg()>` The href; a channel specifying URLs for clickable links.
#' @param image_filter `<string | param()>` A CSS filter; a constant string used to adjust the rendering of images, such as *blur(5px)*.
#' @param inset `<number | param()>` Shorthand to set the same default for **insetStart** and **insetEnd**.
#' @param inset_bottom `<number | param()>` Insets the bottom edge by the specified number of pixels.
#' @param inset_left `<number | param()>` Insets the left edge by the specified number of pixels.
#' @param inset_right `<number | param()>` Insets the right edge by the specified number of pixels.
#' @param inset_top `<number | param()>` Insets the top edge by the specified number of pixels.
#' @param interval `<"day"/"week"/"month"/... | number | param()>` Enforces uniformity for data at regular intervals, such as integer values or daily samples.
#' @param margin `<number | param()>` Shorthand to set the same default for all four mark margins: **marginTop**, **marginRight**, **marginBottom**, and **marginLeft**; typically defaults to 0, except for axis marks.
#' @param margin_bottom `<number | param()>` The mark’s bottom margin; the minimum distance in pixels between the bottom edges of the inner and outer plot area.
#' @param margin_left `<number | param()>` The mark’s left margin; the minimum distance in pixels between the left edges of the inner and outer plot area.
#' @param margin_right `<number | param()>` The mark’s right margin; the minimum distance in pixels between the right edges of the mark’s inner and outer plot area.
#' @param margin_top `<number | param()>` The mark’s top margin; the minimum distance in pixels between the top edges of the inner and outer plot area.
#' @param mix_blend_mode `<string | param()>` The mix-blend-mode; a constant string specifying how to blend content such as *multiply*.
#' @param multiple `<number | param()>` The number of cells per row or column; defaults to undefined for automatic.
#' @param offset `<"center" | "normalize" | "wiggle" | NULL | param()>` After stacking, an optional **offset** can be applied to translate and scale stacks, say to produce a streamgraph; defaults to null for a zero baseline (**y** = 0 for stackY, and **x** = 0 for stackX).
#' @param opacity `<column | literal | transform() | sql()/agg() | list(value=, ...)>` The opacity; a constant between 0 and 1, or a channel typically bound to the *opacity* scale.
#' @param order `<"value" | "x" | "y" | "z" | "sum" | "appearance" | "inside-out" | "-value" | "-x" | "-y" | ... | NULL | param()>` The order in which stacks are layered; one of: - null (default) for input order - a named stack order method such as *inside-out* or *sum* - a field name, for natural order of the corresponding values - a function of data, for natural order of the corresponding values - an array of explicit **z** values in the desired order If the *wiggle* **offset** is used, as for a streamgraph, the default changes to *inside-out*.
#' @param paint_order `<string | param()>` The paint-order; a constant string specifying the order in which the **fill**, **stroke**, and any markers are drawn; defaults to *normal*, which draws the fill, then stroke, then markers; defaults to *stroke* for the text mark to create a “halo” around text to improve legibility.
#' @param pointer_events `<string | param()>` The pointer-events property; a constant string such as *none*.
#' @param reverse `<boolean | param()>` Applies a transform to reverse the order of the mark’s index, say for reverse input order.
#' @param round `<boolean | param()>` If true, round to integers to avoid partial cells.
#' @param rx `<number | string | param()>` The rounded corner *x*-radius, either in pixels or as a percentage of the rect width.
#' @param ry `<number | string | param()>` The rounded corner *y*-radius, either in pixels or as a percentage of the rect height.
#' @param select `<"first" | "last" | "maxX" | "maxY" | "minX" | "minY" | "nearest" | "nearestX" | "nearestY">` Applies a filter transform after data is loaded to highlight selected values only.
#' @param shape_rendering `<string | param()>` The shape-rendering; a constant string such as *crispEdges*.
#' @param sort `<column | literal | transform() | sql()/agg() | list>` Either applies a transform to sort the mark’s index by the specified channel values, or imputes ordinal scale domains from this mark’s channels.
#' @param stroke `<column | literal | transform() | sql()/agg() | list(value=, ...) | param()>` The stroke; a constant CSS color string, or a channel typically bound to the *color* scale.
#' @param stroke_dasharray `<string | number | param()>` The stroke-dasharray; a constant number indicating the length in pixels of alternating dashes and gaps, or a constant string of numbers separated by spaces or commas (_e.g._, *10 2* for dashes of 10 pixels separated by gaps of 2 pixels), or *none* (the default) for no dashing : https://developer.mozilla.org/en-US/docs/Web/SVG/Attribute/stroke-dasharray
#' @param stroke_dashoffset `<string | number | param()>` The stroke-dashoffset; a constant indicating the offset in pixels of the first dash along the stroke; defaults to zero.
#' @param stroke_linecap `<string | param()>` The stroke-linecap; a constant specifying how to cap stroked paths, such as *butt*, *round*, or *square*.
#' @param stroke_linejoin `<string | param()>` The stroke-linejoin; a constant specifying how to join stroked paths, such as *bevel*, *miter*, *miter-clip*, or *round*.
#' @param stroke_miterlimit `<number | param()>` The stroke-miterlimit; a constant number specifying how to limit the length of *miter* joins on stroked paths.
#' @param stroke_opacity `<column | literal | transform() | sql()/agg() | list(value=, ...)>` The stroke-opacity; a constant between 0 and 1, or a channel typically bound to the *opacity* scale.
#' @param stroke_width `<column | literal | transform() | sql()/agg() | list(value=, ...)>` The stroke-width; a constant number in pixels, or a channel.
#' @param target `<string | param()>` The target; a constant string specifying the target window (_e.g._, *_blank*) for clickable links; used in conjunction with the **href** option.
#' @param tip `<boolean | "x" | "y" | "xy" | list | param()>` Whether to generate a tooltip for this mark, and any tip options.
#' @param title `<column | literal | transform() | sql()/agg()>` The title; a channel specifying accessible, short textual descriptions as strings (possibly with newlines).
#' @param unit `<number | param()>` The quantity each cell represents; defaults to 1.
#' @param x `<column | literal | transform() | sql()/agg() | list(value=, ...)>` The horizontal position (or length) channel, typically bound to the *x* scale.
#' @param x1 `<column | literal | transform() | sql()/agg() | list(value=, ...)>` The required primary (starting, often left) horizontal position channel, representing the area’s baseline, typically bound to the *x* scale.
#' @param x2 `<column | literal | transform() | sql()/agg() | list(value=, ...)>` The optional secondary (ending, often right) horizontal position channel, representing the area’s topline, typically bound to the *x* scale; if not specified, **x1** is used.
#' @param y `<column | literal | transform() | sql()/agg() | list(value=, ...)>` The vertical position channel, typically bound to the *y* scale; defaults to the zero-based index of the data 0, 1, 2, ….
#' @param z `<column | literal | transform() | sql()/agg()>` An optional ordinal channel for grouping data into (possibly stacked) series to be drawn as separate areas; defaults to **fill** if a channel, or **stroke** if a channel.
#' @param ... Additional options or plot-level attributes.
#' @param data_from The name of the data source this mark reads from (see [vg_data()]); a length-1 nonzero R integer (`1L`, `-1L`, ...; note the `L`) giving a 1-based index into the spec's registered data sources instead, negative counting from the end (e.g., `-1L` for the most recently registered one); or a literal vector of values to use as inline data directly (mosaic-spec's `"data": [...]` shorthand, e.g., for a single reference line -- a bare double like `0`/`c(0)`, or `0L` itself (never a valid index), means this, not an index). Left unset, defaults to the first registered data source (equivalent to `data_from = 1L`) for any mark type that takes data at all.
#' @param filter_by A Param/Selection (e.g., from [param()]) to filter this mark's data by.
#' @param data_optimize A flag (default `TRUE`) to enable mark-specific query optimizations for this mark's data; set `FALSE` to disable them (mosaic-spec's `data: {optimize: false}`).
#' @family mark functions
#' @export
vg_mark_waffle_x <- function(spec = NULL, formula = vg_unset, aria_description = vg_unset, aria_hidden = vg_unset, aria_label = vg_unset, channels = vg_unset, clip = vg_unset, dx = vg_unset, dy = vg_unset, facet = vg_unset, facet_anchor = vg_unset, fill = vg_unset, fill_opacity = vg_unset, filter = vg_unset, fx = vg_unset, fy = vg_unset, gap = vg_unset, href = vg_unset, image_filter = vg_unset, inset = vg_unset, inset_bottom = vg_unset, inset_left = vg_unset, inset_right = vg_unset, inset_top = vg_unset, interval = vg_unset, margin = vg_unset, margin_bottom = vg_unset, margin_left = vg_unset, margin_right = vg_unset, margin_top = vg_unset, mix_blend_mode = vg_unset, multiple = vg_unset, offset = vg_unset, opacity = vg_unset, order = vg_unset, paint_order = vg_unset, pointer_events = vg_unset, reverse = vg_unset, round = vg_unset, rx = vg_unset, ry = vg_unset, select = vg_unset, shape_rendering = vg_unset, sort = vg_unset, stroke = vg_unset, stroke_dasharray = vg_unset, stroke_dashoffset = vg_unset, stroke_linecap = vg_unset, stroke_linejoin = vg_unset, stroke_miterlimit = vg_unset, stroke_opacity = vg_unset, stroke_width = vg_unset, target = vg_unset, tip = vg_unset, title = vg_unset, unit = vg_unset, x = vg_unset, x1 = vg_unset, x2 = vg_unset, y = vg_unset, z = vg_unset, ..., data_from = vg_unset, filter_by = vg_unset, data_optimize = vg_unset) {
  vg_mark_(spec, "waffleX", formula = formula, ariaDescription = aria_description, ariaHidden = aria_hidden, ariaLabel = aria_label, channels = channels, clip = clip, dx = dx, dy = dy, facet = facet, facetAnchor = facet_anchor, fill = fill, fillOpacity = fill_opacity, filter = filter, fx = fx, fy = fy, gap = gap, href = href, imageFilter = image_filter, inset = inset, insetBottom = inset_bottom, insetLeft = inset_left, insetRight = inset_right, insetTop = inset_top, interval = interval, margin = margin, marginBottom = margin_bottom, marginLeft = margin_left, marginRight = margin_right, marginTop = margin_top, mixBlendMode = mix_blend_mode, multiple = multiple, offset = offset, opacity = opacity, order = order, paintOrder = paint_order, pointerEvents = pointer_events, reverse = reverse, round = round, rx = rx, ry = ry, select = select, shapeRendering = shape_rendering, sort = sort, stroke = stroke, strokeDasharray = stroke_dasharray, strokeDashoffset = stroke_dashoffset, strokeLinecap = stroke_linecap, strokeLinejoin = stroke_linejoin, strokeMiterlimit = stroke_miterlimit, strokeOpacity = stroke_opacity, strokeWidth = stroke_width, target = target, tip = tip, title = title, unit = unit, x = x, x1 = x1, x2 = x2, y = y, z = z, ..., data_from = data_from, filter_by = filter_by, data_optimize = data_optimize)
}

#' The waffleY mark.
#'
#' See [vg_value_types] for what the `<...>` notation below (`column`, `param()`, ...) means.
#'
#' @param spec A plot fragment or `vgspec` to add this mark to, or `NULL` to start a new plot with just this mark.
#' @param formula A shorthand for this mark's position channels (`x`, `y`, `fx`, `fy`, and paired `x1`/`x2` or `y1`/`y2`), e.g., `Sepal.Length ~ Sepal.Width | ~ Species` for `y = ~Sepal.Length, x = ~Sepal.Width, fx = ~Species`. See [vg_mark()] for the full grammar.
#' @param aria_description `<string | param()>` The aria-description; a constant textual description.
#' @param aria_hidden `<string | param()>` The aria-hidden state; a constant indicating whether the element is exposed to an accessibility API.
#' @param aria_label `<column | literal | transform() | sql()/agg()>` The aria-label; a channel specifying short textual labels representing the value in the accessibility tree.
#' @param channels `<list>` Additional named channels, for example to include in a tooltip.
#' @param clip `<"frame" | "sphere" | boolean | NULL | param()>` How to clip the mark; one of: - *frame* or true - clip to the plot’s frame (inner area) - *sphere* - clip to the projected sphere (*e.g.*, front hemisphere) - null or false - do not clip The *sphere* clip option requires a geographic projection.
#' @param dx `<number | param()>` The horizontal offset in pixels; a constant option.
#' @param dy `<number | param()>` The vertical offset in pixels; a constant option.
#' @param facet `<"auto" | "include" | "exclude" | "super" | boolean | NULL | param()>` Whether to enable or disable faceting; one of: - *auto* (default) - automatically determine if this mark should be faceted - *include* (or true) - draw the subset of the mark’s data in the current facet - *exclude* - draw the subset of the mark’s data *not* in the current facet - *super* - draw this mark in a single frame that covers all facets - null (or false) - repeat this mark’s data across all facets (*i.e.*, no faceting) When a mark uses *super* faceting, it is not allowed to use position scales (*x*, *y*, *fx*, or *fy*); *super* faceting is intended for decorations, such as labels and legends.
#' @param facet_anchor `<"top" | "right" | "bottom" | "left" | "top-left" | "top-right" | "bottom-left" | "bottom-right" | "top-empty" | "right-empty" | "bottom-empty" | "left-empty" | "empty" | NULL | param()>` How to place the mark with respect to facets; one of: - null (default for most marks) - display the mark in each non-empty facet - *top*, *right*, *bottom*, or *left* - display the mark only in facets on the given side - *top-empty*, *right-empty*, *bottom-empty*, or *left-empty* (default for axis marks) - display the mark only in facets that have empty space on the given side: either the margin, or an empty facet - *empty* - display the mark in empty facets only
#' @param fill `<column | literal | transform() | sql()/agg() | list(value=, ...) | param()>` The fill; a constant CSS color string, or a channel typically bound to the *color* scale.
#' @param fill_opacity `<column | literal | transform() | sql()/agg() | list(value=, ...) | param()>` The fill-opacity; a constant number between 0 and 1, or a channel typically bound to the *opacity* scale.
#' @param filter `<column | literal | transform() | sql()/agg()>` Applies a transform to filter the mark’s index according to the given channel values; only truthy values are retained.
#' @param fx `<column | literal | transform() | sql()/agg()>` The horizontal facet position channel, for mark-level faceting, bound to the *fx* scale.
#' @param fy `<column | literal | transform() | sql()/agg()>` The vertical facet position channel, for mark-level faceting, bound to the *fy* scale.
#' @param gap `<number | param()>` The gap in pixels between cells; defaults to 1.
#' @param href `<column | literal | transform() | sql()/agg()>` The href; a channel specifying URLs for clickable links.
#' @param image_filter `<string | param()>` A CSS filter; a constant string used to adjust the rendering of images, such as *blur(5px)*.
#' @param inset `<number | param()>` Shorthand to set the same default for **insetStart** and **insetEnd**.
#' @param inset_bottom `<number | param()>` Insets the bottom edge by the specified number of pixels.
#' @param inset_left `<number | param()>` Insets the left edge by the specified number of pixels.
#' @param inset_right `<number | param()>` Insets the right edge by the specified number of pixels.
#' @param inset_top `<number | param()>` Insets the top edge by the specified number of pixels.
#' @param interval `<"day"/"week"/"month"/... | number | param()>` Enforces uniformity for data at regular intervals, such as integer values or daily samples.
#' @param margin `<number | param()>` Shorthand to set the same default for all four mark margins: **marginTop**, **marginRight**, **marginBottom**, and **marginLeft**; typically defaults to 0, except for axis marks.
#' @param margin_bottom `<number | param()>` The mark’s bottom margin; the minimum distance in pixels between the bottom edges of the inner and outer plot area.
#' @param margin_left `<number | param()>` The mark’s left margin; the minimum distance in pixels between the left edges of the inner and outer plot area.
#' @param margin_right `<number | param()>` The mark’s right margin; the minimum distance in pixels between the right edges of the mark’s inner and outer plot area.
#' @param margin_top `<number | param()>` The mark’s top margin; the minimum distance in pixels between the top edges of the inner and outer plot area.
#' @param mix_blend_mode `<string | param()>` The mix-blend-mode; a constant string specifying how to blend content such as *multiply*.
#' @param multiple `<number | param()>` The number of cells per row or column; defaults to undefined for automatic.
#' @param offset `<"center" | "normalize" | "wiggle" | NULL | param()>` After stacking, an optional **offset** can be applied to translate and scale stacks, say to produce a streamgraph; defaults to null for a zero baseline (**y** = 0 for stackY, and **x** = 0 for stackX).
#' @param opacity `<column | literal | transform() | sql()/agg() | list(value=, ...)>` The opacity; a constant between 0 and 1, or a channel typically bound to the *opacity* scale.
#' @param order `<"value" | "x" | "y" | "z" | "sum" | "appearance" | "inside-out" | "-value" | "-x" | "-y" | ... | NULL | param()>` The order in which stacks are layered; one of: - null (default) for input order - a named stack order method such as *inside-out* or *sum* - a field name, for natural order of the corresponding values - a function of data, for natural order of the corresponding values - an array of explicit **z** values in the desired order If the *wiggle* **offset** is used, as for a streamgraph, the default changes to *inside-out*.
#' @param paint_order `<string | param()>` The paint-order; a constant string specifying the order in which the **fill**, **stroke**, and any markers are drawn; defaults to *normal*, which draws the fill, then stroke, then markers; defaults to *stroke* for the text mark to create a “halo” around text to improve legibility.
#' @param pointer_events `<string | param()>` The pointer-events property; a constant string such as *none*.
#' @param reverse `<boolean | param()>` Applies a transform to reverse the order of the mark’s index, say for reverse input order.
#' @param round `<boolean | param()>` If true, round to integers to avoid partial cells.
#' @param rx `<number | string | param()>` The rounded corner *x*-radius, either in pixels or as a percentage of the rect width.
#' @param ry `<number | string | param()>` The rounded corner *y*-radius, either in pixels or as a percentage of the rect height.
#' @param select `<"first" | "last" | "maxX" | "maxY" | "minX" | "minY" | "nearest" | "nearestX" | "nearestY">` Applies a filter transform after data is loaded to highlight selected values only.
#' @param shape_rendering `<string | param()>` The shape-rendering; a constant string such as *crispEdges*.
#' @param sort `<column | literal | transform() | sql()/agg() | list>` Either applies a transform to sort the mark’s index by the specified channel values, or imputes ordinal scale domains from this mark’s channels.
#' @param stroke `<column | literal | transform() | sql()/agg() | list(value=, ...) | param()>` The stroke; a constant CSS color string, or a channel typically bound to the *color* scale.
#' @param stroke_dasharray `<string | number | param()>` The stroke-dasharray; a constant number indicating the length in pixels of alternating dashes and gaps, or a constant string of numbers separated by spaces or commas (_e.g._, *10 2* for dashes of 10 pixels separated by gaps of 2 pixels), or *none* (the default) for no dashing : https://developer.mozilla.org/en-US/docs/Web/SVG/Attribute/stroke-dasharray
#' @param stroke_dashoffset `<string | number | param()>` The stroke-dashoffset; a constant indicating the offset in pixels of the first dash along the stroke; defaults to zero.
#' @param stroke_linecap `<string | param()>` The stroke-linecap; a constant specifying how to cap stroked paths, such as *butt*, *round*, or *square*.
#' @param stroke_linejoin `<string | param()>` The stroke-linejoin; a constant specifying how to join stroked paths, such as *bevel*, *miter*, *miter-clip*, or *round*.
#' @param stroke_miterlimit `<number | param()>` The stroke-miterlimit; a constant number specifying how to limit the length of *miter* joins on stroked paths.
#' @param stroke_opacity `<column | literal | transform() | sql()/agg() | list(value=, ...)>` The stroke-opacity; a constant between 0 and 1, or a channel typically bound to the *opacity* scale.
#' @param stroke_width `<column | literal | transform() | sql()/agg() | list(value=, ...)>` The stroke-width; a constant number in pixels, or a channel.
#' @param target `<string | param()>` The target; a constant string specifying the target window (_e.g._, *_blank*) for clickable links; used in conjunction with the **href** option.
#' @param tip `<boolean | "x" | "y" | "xy" | list | param()>` Whether to generate a tooltip for this mark, and any tip options.
#' @param title `<column | literal | transform() | sql()/agg()>` The title; a channel specifying accessible, short textual descriptions as strings (possibly with newlines).
#' @param unit `<number | param()>` The quantity each cell represents; defaults to 1.
#' @param x `<column | literal | transform() | sql()/agg() | list(value=, ...)>` The horizontal position (or length) channel, typically bound to the *x* scale.
#' @param y `<column | literal | transform() | sql()/agg() | list(value=, ...)>` The vertical position channel, typically bound to the *y* scale; defaults to the zero-based index of the data 0, 1, 2, ….
#' @param y1 `<column | literal | transform() | sql()/agg() | list(value=, ...)>` The required primary (starting, often bottom) vertical position channel, representing the area’s baseline, typically bound to the *y* scale.
#' @param y2 `<column | literal | transform() | sql()/agg() | list(value=, ...)>` The optional secondary (ending, often top) vertical position channel, representing the area’s topline, typically bound to the *y* scale; if not specified, **y1** is used.
#' @param z `<column | literal | transform() | sql()/agg()>` An optional ordinal channel for grouping data into (possibly stacked) series to be drawn as separate areas; defaults to **fill** if a channel, or **stroke** if a channel.
#' @param ... Additional options or plot-level attributes.
#' @param data_from The name of the data source this mark reads from (see [vg_data()]); a length-1 nonzero R integer (`1L`, `-1L`, ...; note the `L`) giving a 1-based index into the spec's registered data sources instead, negative counting from the end (e.g., `-1L` for the most recently registered one); or a literal vector of values to use as inline data directly (mosaic-spec's `"data": [...]` shorthand, e.g., for a single reference line -- a bare double like `0`/`c(0)`, or `0L` itself (never a valid index), means this, not an index). Left unset, defaults to the first registered data source (equivalent to `data_from = 1L`) for any mark type that takes data at all.
#' @param filter_by A Param/Selection (e.g., from [param()]) to filter this mark's data by.
#' @param data_optimize A flag (default `TRUE`) to enable mark-specific query optimizations for this mark's data; set `FALSE` to disable them (mosaic-spec's `data: {optimize: false}`).
#' @family mark functions
#' @export
vg_mark_waffle_y <- function(spec = NULL, formula = vg_unset, aria_description = vg_unset, aria_hidden = vg_unset, aria_label = vg_unset, channels = vg_unset, clip = vg_unset, dx = vg_unset, dy = vg_unset, facet = vg_unset, facet_anchor = vg_unset, fill = vg_unset, fill_opacity = vg_unset, filter = vg_unset, fx = vg_unset, fy = vg_unset, gap = vg_unset, href = vg_unset, image_filter = vg_unset, inset = vg_unset, inset_bottom = vg_unset, inset_left = vg_unset, inset_right = vg_unset, inset_top = vg_unset, interval = vg_unset, margin = vg_unset, margin_bottom = vg_unset, margin_left = vg_unset, margin_right = vg_unset, margin_top = vg_unset, mix_blend_mode = vg_unset, multiple = vg_unset, offset = vg_unset, opacity = vg_unset, order = vg_unset, paint_order = vg_unset, pointer_events = vg_unset, reverse = vg_unset, round = vg_unset, rx = vg_unset, ry = vg_unset, select = vg_unset, shape_rendering = vg_unset, sort = vg_unset, stroke = vg_unset, stroke_dasharray = vg_unset, stroke_dashoffset = vg_unset, stroke_linecap = vg_unset, stroke_linejoin = vg_unset, stroke_miterlimit = vg_unset, stroke_opacity = vg_unset, stroke_width = vg_unset, target = vg_unset, tip = vg_unset, title = vg_unset, unit = vg_unset, x = vg_unset, y = vg_unset, y1 = vg_unset, y2 = vg_unset, z = vg_unset, ..., data_from = vg_unset, filter_by = vg_unset, data_optimize = vg_unset) {
  vg_mark_(spec, "waffleY", formula = formula, ariaDescription = aria_description, ariaHidden = aria_hidden, ariaLabel = aria_label, channels = channels, clip = clip, dx = dx, dy = dy, facet = facet, facetAnchor = facet_anchor, fill = fill, fillOpacity = fill_opacity, filter = filter, fx = fx, fy = fy, gap = gap, href = href, imageFilter = image_filter, inset = inset, insetBottom = inset_bottom, insetLeft = inset_left, insetRight = inset_right, insetTop = inset_top, interval = interval, margin = margin, marginBottom = margin_bottom, marginLeft = margin_left, marginRight = margin_right, marginTop = margin_top, mixBlendMode = mix_blend_mode, multiple = multiple, offset = offset, opacity = opacity, order = order, paintOrder = paint_order, pointerEvents = pointer_events, reverse = reverse, round = round, rx = rx, ry = ry, select = select, shapeRendering = shape_rendering, sort = sort, stroke = stroke, strokeDasharray = stroke_dasharray, strokeDashoffset = stroke_dashoffset, strokeLinecap = stroke_linecap, strokeLinejoin = stroke_linejoin, strokeMiterlimit = stroke_miterlimit, strokeOpacity = stroke_opacity, strokeWidth = stroke_width, target = target, tip = tip, title = title, unit = unit, x = x, y = y, y1 = y1, y2 = y2, z = z, ..., data_from = data_from, filter_by = filter_by, data_optimize = data_optimize)
}

