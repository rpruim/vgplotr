# Generated by data-raw/update-schema.R from mosaic's JSON schema.
# DO NOT EDIT BY HAND -- rerun that script instead.
#
# One vg_<mark>() wrapper per mark type in the mosaic-spec schema, each
# with a real named argument per property that mark accepts (so tab
# completion and ?vg_dot show the actual options) -- e.g. vg_dot(spec, x
# = ~a, y = ~b) is vg_mark(spec, "dot", x = ~a, y = ~b) with x/y (and
# every other dot property) as real, documented arguments instead of an
# opaque `...`.

#' The area mark.
#'
#' @param spec A plot fragment or `vgspec` to add this mark to, or `NULL` to start a new plot with just this mark.
#' @param ariaDescription The aria-description; a constant textual description.
#' @param ariaHidden The aria-hidden state; a constant indicating whether the element is exposed to an accessibility API.
#' @param ariaLabel The aria-label; a channel specifying short textual labels representing the value in the accessibility tree.
#' @param channels Additional named channels, for example to include in a tooltip.
#' @param clip How to clip the mark; one of: - *frame* or true - clip to the plot’s frame (inner area) - *sphere* - clip to the projected sphere (*e.g.*, front hemisphere) - null or false - do not clip The *sphere* clip option requires a geographic projection.
#' @param curve The curve (interpolation) method for connecting adjacent points.
#' @param dx The horizontal offset in pixels; a constant option.
#' @param dy The vertical offset in pixels; a constant option.
#' @param facet Whether to enable or disable faceting; one of: - *auto* (default) - automatically determine if this mark should be faceted - *include* (or true) - draw the subset of the mark’s data in the current facet - *exclude* - draw the subset of the mark’s data *not* in the current facet - *super* - draw this mark in a single frame that covers all facets - null (or false) - repeat this mark’s data across all facets (*i.e.*, no faceting) When a mark uses *super* faceting, it is not allowed to use position scales (*x*, *y*, *fx*, or *fy*); *super* faceting is intended for decorations, such as labels and legends.
#' @param facetAnchor How to place the mark with respect to facets; one of: - null (default for most marks) - display the mark in each non-empty facet - *top*, *right*, *bottom*, or *left* - display the mark only in facets on the given side - *top-empty*, *right-empty*, *bottom-empty*, or *left-empty* (default for axis marks) - display the mark only in facets that have empty space on the given side: either the margin, or an empty facet - *empty* - display the mark in empty facets only
#' @param fill The fill; a constant CSS color string, or a channel typically bound to the *color* scale.
#' @param fillOpacity The fill-opacity; a constant number between 0 and 1, or a channel typically bound to the *opacity* scale.
#' @param filter Applies a transform to filter the mark’s index according to the given channel values; only truthy values are retained.
#' @param fx The horizontal facet position channel, for mark-level faceting, bound to the *fx* scale.
#' @param fy The vertical facet position channel, for mark-level faceting, bound to the *fy* scale.
#' @param href The href; a channel specifying URLs for clickable links.
#' @param imageFilter A CSS filter; a constant string used to adjust the rendering of images, such as *blur(5px)*.
#' @param margin Shorthand to set the same default for all four mark margins: **marginTop**, **marginRight**, **marginBottom**, and **marginLeft**; typically defaults to 0, except for axis marks.
#' @param marginBottom The mark’s bottom margin; the minimum distance in pixels between the bottom edges of the inner and outer plot area.
#' @param marginLeft The mark’s left margin; the minimum distance in pixels between the left edges of the inner and outer plot area.
#' @param marginRight The mark’s right margin; the minimum distance in pixels between the right edges of the mark’s inner and outer plot area.
#' @param marginTop The mark’s top margin; the minimum distance in pixels between the top edges of the inner and outer plot area.
#' @param mixBlendMode The mix-blend-mode; a constant string specifying how to blend content such as *multiply*.
#' @param offset After stacking, an optional **offset** can be applied to translate and scale stacks, say to produce a streamgraph; defaults to null for a zero baseline (**y** = 0 for stackY, and **x** = 0 for stackX).
#' @param opacity The opacity; a constant between 0 and 1, or a channel typically bound to the *opacity* scale.
#' @param order The order in which stacks are layered; one of: - null (default) for input order - a named stack order method such as *inside-out* or *sum* - a field name, for natural order of the corresponding values - a function of data, for natural order of the corresponding values - an array of explicit **z** values in the desired order If the *wiggle* **offset** is used, as for a streamgraph, the default changes to *inside-out*.
#' @param paintOrder The paint-order; a constant string specifying the order in which the **fill**, **stroke**, and any markers are drawn; defaults to *normal*, which draws the fill, then stroke, then markers; defaults to *stroke* for the text mark to create a “halo” around text to improve legibility.
#' @param pointerEvents The pointer-events property; a constant string such as *none*.
#' @param reverse Applies a transform to reverse the order of the mark’s index, say for reverse input order.
#' @param select Applies a filter transform after data is loaded to highlight selected values only.
#' @param shapeRendering The shape-rendering; a constant string such as *crispEdges*.
#' @param sort Either applies a transform to sort the mark’s index by the specified channel values, or imputes ordinal scale domains from this mark’s channels.
#' @param stroke The stroke; a constant CSS color string, or a channel typically bound to the *color* scale.
#' @param strokeDasharray The stroke-dasharray; a constant number indicating the length in pixels of alternating dashes and gaps, or a constant string of numbers separated by spaces or commas (_e.g._, *10 2* for dashes of 10 pixels separated by gaps of 2 pixels), or *none* (the default) for no dashing : https://developer.mozilla.org/en-US/docs/Web/SVG/Attribute/stroke-dasharray
#' @param strokeDashoffset The stroke-dashoffset; a constant indicating the offset in pixels of the first dash along the stroke; defaults to zero.
#' @param strokeLinecap The stroke-linecap; a constant specifying how to cap stroked paths, such as *butt*, *round*, or *square*.
#' @param strokeLinejoin The stroke-linejoin; a constant specifying how to join stroked paths, such as *bevel*, *miter*, *miter-clip*, or *round*.
#' @param strokeMiterlimit The stroke-miterlimit; a constant number specifying how to limit the length of *miter* joins on stroked paths.
#' @param strokeOpacity The stroke-opacity; a constant between 0 and 1, or a channel typically bound to the *opacity* scale.
#' @param strokeWidth The stroke-width; a constant number in pixels, or a channel.
#' @param target The target; a constant string specifying the target window (_e.g._, *_blank*) for clickable links; used in conjunction with the **href** option.
#' @param tension The tension option only has an effect on bundle, cardinal and Catmull–Rom splines (*bundle*, *cardinal*, *cardinal-open*, *cardinal-closed*, *catmull-rom*, *catmull-rom-open*, and *catmull-rom-closed*).
#' @param tip Whether to generate a tooltip for this mark, and any tip options.
#' @param title The title; a channel specifying accessible, short textual descriptions as strings (possibly with newlines).
#' @param x1 The required primary (starting, often left) horizontal position channel, representing the area’s baseline, typically bound to the *x* scale.
#' @param x2 The optional secondary (ending, often right) horizontal position channel, representing the area’s topline, typically bound to the *x* scale; if not specified, **x1** is used.
#' @param y1 The required primary (starting, often bottom) vertical position channel, representing the area’s baseline, typically bound to the *y* scale.
#' @param y2 The optional secondary (ending, often top) vertical position channel, representing the area’s topline, typically bound to the *y* scale; if not specified, **y1** is used.
#' @param z An optional ordinal channel for grouping data into (possibly stacked) series to be drawn as separate areas; defaults to **fill** if a channel, or **stroke** if a channel.
#' @param ... Additional options or plot-level attributes.
#' @param data_from The name of the data source this mark reads from (see [vg_data()]).
#' @param filter_by A Param/Selection (e.g. from [param()]) to filter this mark's data by.
#' @family mark functions
#' @export
vg_area <- function(spec = NULL, ariaDescription = vg_unset, ariaHidden = vg_unset, ariaLabel = vg_unset, channels = vg_unset, clip = vg_unset, curve = vg_unset, dx = vg_unset, dy = vg_unset, facet = vg_unset, facetAnchor = vg_unset, fill = vg_unset, fillOpacity = vg_unset, filter = vg_unset, fx = vg_unset, fy = vg_unset, href = vg_unset, imageFilter = vg_unset, margin = vg_unset, marginBottom = vg_unset, marginLeft = vg_unset, marginRight = vg_unset, marginTop = vg_unset, mixBlendMode = vg_unset, offset = vg_unset, opacity = vg_unset, order = vg_unset, paintOrder = vg_unset, pointerEvents = vg_unset, reverse = vg_unset, select = vg_unset, shapeRendering = vg_unset, sort = vg_unset, stroke = vg_unset, strokeDasharray = vg_unset, strokeDashoffset = vg_unset, strokeLinecap = vg_unset, strokeLinejoin = vg_unset, strokeMiterlimit = vg_unset, strokeOpacity = vg_unset, strokeWidth = vg_unset, target = vg_unset, tension = vg_unset, tip = vg_unset, title = vg_unset, x1 = vg_unset, x2 = vg_unset, y1 = vg_unset, y2 = vg_unset, z = vg_unset, ..., data_from = vg_unset, filter_by = vg_unset) {
  vg_mark_(spec, "area", ariaDescription = ariaDescription, ariaHidden = ariaHidden, ariaLabel = ariaLabel, channels = channels, clip = clip, curve = curve, dx = dx, dy = dy, facet = facet, facetAnchor = facetAnchor, fill = fill, fillOpacity = fillOpacity, filter = filter, fx = fx, fy = fy, href = href, imageFilter = imageFilter, margin = margin, marginBottom = marginBottom, marginLeft = marginLeft, marginRight = marginRight, marginTop = marginTop, mixBlendMode = mixBlendMode, offset = offset, opacity = opacity, order = order, paintOrder = paintOrder, pointerEvents = pointerEvents, reverse = reverse, select = select, shapeRendering = shapeRendering, sort = sort, stroke = stroke, strokeDasharray = strokeDasharray, strokeDashoffset = strokeDashoffset, strokeLinecap = strokeLinecap, strokeLinejoin = strokeLinejoin, strokeMiterlimit = strokeMiterlimit, strokeOpacity = strokeOpacity, strokeWidth = strokeWidth, target = target, tension = tension, tip = tip, title = title, x1 = x1, x2 = x2, y1 = y1, y2 = y2, z = z, ..., data_from = data_from, filter_by = filter_by)
}

#' The areaX mark.
#'
#' @param spec A plot fragment or `vgspec` to add this mark to, or `NULL` to start a new plot with just this mark.
#' @param ariaDescription The aria-description; a constant textual description.
#' @param ariaHidden The aria-hidden state; a constant indicating whether the element is exposed to an accessibility API.
#' @param ariaLabel The aria-label; a channel specifying short textual labels representing the value in the accessibility tree.
#' @param channels Additional named channels, for example to include in a tooltip.
#' @param clip How to clip the mark; one of: - *frame* or true - clip to the plot’s frame (inner area) - *sphere* - clip to the projected sphere (*e.g.*, front hemisphere) - null or false - do not clip The *sphere* clip option requires a geographic projection.
#' @param curve The curve (interpolation) method for connecting adjacent points.
#' @param dx The horizontal offset in pixels; a constant option.
#' @param dy The vertical offset in pixels; a constant option.
#' @param facet Whether to enable or disable faceting; one of: - *auto* (default) - automatically determine if this mark should be faceted - *include* (or true) - draw the subset of the mark’s data in the current facet - *exclude* - draw the subset of the mark’s data *not* in the current facet - *super* - draw this mark in a single frame that covers all facets - null (or false) - repeat this mark’s data across all facets (*i.e.*, no faceting) When a mark uses *super* faceting, it is not allowed to use position scales (*x*, *y*, *fx*, or *fy*); *super* faceting is intended for decorations, such as labels and legends.
#' @param facetAnchor How to place the mark with respect to facets; one of: - null (default for most marks) - display the mark in each non-empty facet - *top*, *right*, *bottom*, or *left* - display the mark only in facets on the given side - *top-empty*, *right-empty*, *bottom-empty*, or *left-empty* (default for axis marks) - display the mark only in facets that have empty space on the given side: either the margin, or an empty facet - *empty* - display the mark in empty facets only
#' @param fill The fill; a constant CSS color string, or a channel typically bound to the *color* scale.
#' @param fillOpacity The fill-opacity; a constant number between 0 and 1, or a channel typically bound to the *opacity* scale.
#' @param filter Applies a transform to filter the mark’s index according to the given channel values; only truthy values are retained.
#' @param fx The horizontal facet position channel, for mark-level faceting, bound to the *fx* scale.
#' @param fy The vertical facet position channel, for mark-level faceting, bound to the *fy* scale.
#' @param href The href; a channel specifying URLs for clickable links.
#' @param imageFilter A CSS filter; a constant string used to adjust the rendering of images, such as *blur(5px)*.
#' @param margin Shorthand to set the same default for all four mark margins: **marginTop**, **marginRight**, **marginBottom**, and **marginLeft**; typically defaults to 0, except for axis marks.
#' @param marginBottom The mark’s bottom margin; the minimum distance in pixels between the bottom edges of the inner and outer plot area.
#' @param marginLeft The mark’s left margin; the minimum distance in pixels between the left edges of the inner and outer plot area.
#' @param marginRight The mark’s right margin; the minimum distance in pixels between the right edges of the mark’s inner and outer plot area.
#' @param marginTop The mark’s top margin; the minimum distance in pixels between the top edges of the inner and outer plot area.
#' @param mixBlendMode The mix-blend-mode; a constant string specifying how to blend content such as *multiply*.
#' @param offset After stacking, an optional **offset** can be applied to translate and scale stacks, say to produce a streamgraph; defaults to null for a zero baseline (**y** = 0 for stackY, and **x** = 0 for stackX).
#' @param opacity The opacity; a constant between 0 and 1, or a channel typically bound to the *opacity* scale.
#' @param order The order in which stacks are layered; one of: - null (default) for input order - a named stack order method such as *inside-out* or *sum* - a field name, for natural order of the corresponding values - a function of data, for natural order of the corresponding values - an array of explicit **z** values in the desired order If the *wiggle* **offset** is used, as for a streamgraph, the default changes to *inside-out*.
#' @param paintOrder The paint-order; a constant string specifying the order in which the **fill**, **stroke**, and any markers are drawn; defaults to *normal*, which draws the fill, then stroke, then markers; defaults to *stroke* for the text mark to create a “halo” around text to improve legibility.
#' @param pointerEvents The pointer-events property; a constant string such as *none*.
#' @param reverse Applies a transform to reverse the order of the mark’s index, say for reverse input order.
#' @param select Applies a filter transform after data is loaded to highlight selected values only.
#' @param shapeRendering The shape-rendering; a constant string such as *crispEdges*.
#' @param sort Either applies a transform to sort the mark’s index by the specified channel values, or imputes ordinal scale domains from this mark’s channels.
#' @param stroke The stroke; a constant CSS color string, or a channel typically bound to the *color* scale.
#' @param strokeDasharray The stroke-dasharray; a constant number indicating the length in pixels of alternating dashes and gaps, or a constant string of numbers separated by spaces or commas (_e.g._, *10 2* for dashes of 10 pixels separated by gaps of 2 pixels), or *none* (the default) for no dashing : https://developer.mozilla.org/en-US/docs/Web/SVG/Attribute/stroke-dasharray
#' @param strokeDashoffset The stroke-dashoffset; a constant indicating the offset in pixels of the first dash along the stroke; defaults to zero.
#' @param strokeLinecap The stroke-linecap; a constant specifying how to cap stroked paths, such as *butt*, *round*, or *square*.
#' @param strokeLinejoin The stroke-linejoin; a constant specifying how to join stroked paths, such as *bevel*, *miter*, *miter-clip*, or *round*.
#' @param strokeMiterlimit The stroke-miterlimit; a constant number specifying how to limit the length of *miter* joins on stroked paths.
#' @param strokeOpacity The stroke-opacity; a constant between 0 and 1, or a channel typically bound to the *opacity* scale.
#' @param strokeWidth The stroke-width; a constant number in pixels, or a channel.
#' @param target The target; a constant string specifying the target window (_e.g._, *_blank*) for clickable links; used in conjunction with the **href** option.
#' @param tension The tension option only has an effect on bundle, cardinal and Catmull–Rom splines (*bundle*, *cardinal*, *cardinal-open*, *cardinal-closed*, *catmull-rom*, *catmull-rom-open*, and *catmull-rom-closed*).
#' @param tip Whether to generate a tooltip for this mark, and any tip options.
#' @param title The title; a channel specifying accessible, short textual descriptions as strings (possibly with newlines).
#' @param x The horizontal position (or length) channel, typically bound to the *x* scale.
#' @param x1 The required primary (starting, often left) horizontal position channel, representing the area’s baseline, typically bound to the *x* scale.
#' @param x2 The optional secondary (ending, often right) horizontal position channel, representing the area’s topline, typically bound to the *x* scale; if not specified, **x1** is used.
#' @param y The vertical position channel, typically bound to the *y* scale; defaults to the zero-based index of the data 0, 1, 2, ….
#' @param z An optional ordinal channel for grouping data into (possibly stacked) series to be drawn as separate areas; defaults to **fill** if a channel, or **stroke** if a channel.
#' @param ... Additional options or plot-level attributes.
#' @param data_from The name of the data source this mark reads from (see [vg_data()]).
#' @param filter_by A Param/Selection (e.g. from [param()]) to filter this mark's data by.
#' @family mark functions
#' @export
vg_area_x <- function(spec = NULL, ariaDescription = vg_unset, ariaHidden = vg_unset, ariaLabel = vg_unset, channels = vg_unset, clip = vg_unset, curve = vg_unset, dx = vg_unset, dy = vg_unset, facet = vg_unset, facetAnchor = vg_unset, fill = vg_unset, fillOpacity = vg_unset, filter = vg_unset, fx = vg_unset, fy = vg_unset, href = vg_unset, imageFilter = vg_unset, margin = vg_unset, marginBottom = vg_unset, marginLeft = vg_unset, marginRight = vg_unset, marginTop = vg_unset, mixBlendMode = vg_unset, offset = vg_unset, opacity = vg_unset, order = vg_unset, paintOrder = vg_unset, pointerEvents = vg_unset, reverse = vg_unset, select = vg_unset, shapeRendering = vg_unset, sort = vg_unset, stroke = vg_unset, strokeDasharray = vg_unset, strokeDashoffset = vg_unset, strokeLinecap = vg_unset, strokeLinejoin = vg_unset, strokeMiterlimit = vg_unset, strokeOpacity = vg_unset, strokeWidth = vg_unset, target = vg_unset, tension = vg_unset, tip = vg_unset, title = vg_unset, x = vg_unset, x1 = vg_unset, x2 = vg_unset, y = vg_unset, z = vg_unset, ..., data_from = vg_unset, filter_by = vg_unset) {
  vg_mark_(spec, "areaX", ariaDescription = ariaDescription, ariaHidden = ariaHidden, ariaLabel = ariaLabel, channels = channels, clip = clip, curve = curve, dx = dx, dy = dy, facet = facet, facetAnchor = facetAnchor, fill = fill, fillOpacity = fillOpacity, filter = filter, fx = fx, fy = fy, href = href, imageFilter = imageFilter, margin = margin, marginBottom = marginBottom, marginLeft = marginLeft, marginRight = marginRight, marginTop = marginTop, mixBlendMode = mixBlendMode, offset = offset, opacity = opacity, order = order, paintOrder = paintOrder, pointerEvents = pointerEvents, reverse = reverse, select = select, shapeRendering = shapeRendering, sort = sort, stroke = stroke, strokeDasharray = strokeDasharray, strokeDashoffset = strokeDashoffset, strokeLinecap = strokeLinecap, strokeLinejoin = strokeLinejoin, strokeMiterlimit = strokeMiterlimit, strokeOpacity = strokeOpacity, strokeWidth = strokeWidth, target = target, tension = tension, tip = tip, title = title, x = x, x1 = x1, x2 = x2, y = y, z = z, ..., data_from = data_from, filter_by = filter_by)
}

#' The areaY mark.
#'
#' @param spec A plot fragment or `vgspec` to add this mark to, or `NULL` to start a new plot with just this mark.
#' @param ariaDescription The aria-description; a constant textual description.
#' @param ariaHidden The aria-hidden state; a constant indicating whether the element is exposed to an accessibility API.
#' @param ariaLabel The aria-label; a channel specifying short textual labels representing the value in the accessibility tree.
#' @param channels Additional named channels, for example to include in a tooltip.
#' @param clip How to clip the mark; one of: - *frame* or true - clip to the plot’s frame (inner area) - *sphere* - clip to the projected sphere (*e.g.*, front hemisphere) - null or false - do not clip The *sphere* clip option requires a geographic projection.
#' @param curve The curve (interpolation) method for connecting adjacent points.
#' @param dx The horizontal offset in pixels; a constant option.
#' @param dy The vertical offset in pixels; a constant option.
#' @param facet Whether to enable or disable faceting; one of: - *auto* (default) - automatically determine if this mark should be faceted - *include* (or true) - draw the subset of the mark’s data in the current facet - *exclude* - draw the subset of the mark’s data *not* in the current facet - *super* - draw this mark in a single frame that covers all facets - null (or false) - repeat this mark’s data across all facets (*i.e.*, no faceting) When a mark uses *super* faceting, it is not allowed to use position scales (*x*, *y*, *fx*, or *fy*); *super* faceting is intended for decorations, such as labels and legends.
#' @param facetAnchor How to place the mark with respect to facets; one of: - null (default for most marks) - display the mark in each non-empty facet - *top*, *right*, *bottom*, or *left* - display the mark only in facets on the given side - *top-empty*, *right-empty*, *bottom-empty*, or *left-empty* (default for axis marks) - display the mark only in facets that have empty space on the given side: either the margin, or an empty facet - *empty* - display the mark in empty facets only
#' @param fill The fill; a constant CSS color string, or a channel typically bound to the *color* scale.
#' @param fillOpacity The fill-opacity; a constant number between 0 and 1, or a channel typically bound to the *opacity* scale.
#' @param filter Applies a transform to filter the mark’s index according to the given channel values; only truthy values are retained.
#' @param fx The horizontal facet position channel, for mark-level faceting, bound to the *fx* scale.
#' @param fy The vertical facet position channel, for mark-level faceting, bound to the *fy* scale.
#' @param href The href; a channel specifying URLs for clickable links.
#' @param imageFilter A CSS filter; a constant string used to adjust the rendering of images, such as *blur(5px)*.
#' @param margin Shorthand to set the same default for all four mark margins: **marginTop**, **marginRight**, **marginBottom**, and **marginLeft**; typically defaults to 0, except for axis marks.
#' @param marginBottom The mark’s bottom margin; the minimum distance in pixels between the bottom edges of the inner and outer plot area.
#' @param marginLeft The mark’s left margin; the minimum distance in pixels between the left edges of the inner and outer plot area.
#' @param marginRight The mark’s right margin; the minimum distance in pixels between the right edges of the mark’s inner and outer plot area.
#' @param marginTop The mark’s top margin; the minimum distance in pixels between the top edges of the inner and outer plot area.
#' @param mixBlendMode The mix-blend-mode; a constant string specifying how to blend content such as *multiply*.
#' @param offset After stacking, an optional **offset** can be applied to translate and scale stacks, say to produce a streamgraph; defaults to null for a zero baseline (**y** = 0 for stackY, and **x** = 0 for stackX).
#' @param opacity The opacity; a constant between 0 and 1, or a channel typically bound to the *opacity* scale.
#' @param order The order in which stacks are layered; one of: - null (default) for input order - a named stack order method such as *inside-out* or *sum* - a field name, for natural order of the corresponding values - a function of data, for natural order of the corresponding values - an array of explicit **z** values in the desired order If the *wiggle* **offset** is used, as for a streamgraph, the default changes to *inside-out*.
#' @param paintOrder The paint-order; a constant string specifying the order in which the **fill**, **stroke**, and any markers are drawn; defaults to *normal*, which draws the fill, then stroke, then markers; defaults to *stroke* for the text mark to create a “halo” around text to improve legibility.
#' @param pointerEvents The pointer-events property; a constant string such as *none*.
#' @param reverse Applies a transform to reverse the order of the mark’s index, say for reverse input order.
#' @param select Applies a filter transform after data is loaded to highlight selected values only.
#' @param shapeRendering The shape-rendering; a constant string such as *crispEdges*.
#' @param sort Either applies a transform to sort the mark’s index by the specified channel values, or imputes ordinal scale domains from this mark’s channels.
#' @param stroke The stroke; a constant CSS color string, or a channel typically bound to the *color* scale.
#' @param strokeDasharray The stroke-dasharray; a constant number indicating the length in pixels of alternating dashes and gaps, or a constant string of numbers separated by spaces or commas (_e.g._, *10 2* for dashes of 10 pixels separated by gaps of 2 pixels), or *none* (the default) for no dashing : https://developer.mozilla.org/en-US/docs/Web/SVG/Attribute/stroke-dasharray
#' @param strokeDashoffset The stroke-dashoffset; a constant indicating the offset in pixels of the first dash along the stroke; defaults to zero.
#' @param strokeLinecap The stroke-linecap; a constant specifying how to cap stroked paths, such as *butt*, *round*, or *square*.
#' @param strokeLinejoin The stroke-linejoin; a constant specifying how to join stroked paths, such as *bevel*, *miter*, *miter-clip*, or *round*.
#' @param strokeMiterlimit The stroke-miterlimit; a constant number specifying how to limit the length of *miter* joins on stroked paths.
#' @param strokeOpacity The stroke-opacity; a constant between 0 and 1, or a channel typically bound to the *opacity* scale.
#' @param strokeWidth The stroke-width; a constant number in pixels, or a channel.
#' @param target The target; a constant string specifying the target window (_e.g._, *_blank*) for clickable links; used in conjunction with the **href** option.
#' @param tension The tension option only has an effect on bundle, cardinal and Catmull–Rom splines (*bundle*, *cardinal*, *cardinal-open*, *cardinal-closed*, *catmull-rom*, *catmull-rom-open*, and *catmull-rom-closed*).
#' @param tip Whether to generate a tooltip for this mark, and any tip options.
#' @param title The title; a channel specifying accessible, short textual descriptions as strings (possibly with newlines).
#' @param x The horizontal position (or length) channel, typically bound to the *x* scale.
#' @param y The vertical position channel, typically bound to the *y* scale; defaults to the zero-based index of the data 0, 1, 2, ….
#' @param y1 The required primary (starting, often bottom) vertical position channel, representing the area’s baseline, typically bound to the *y* scale.
#' @param y2 The optional secondary (ending, often top) vertical position channel, representing the area’s topline, typically bound to the *y* scale; if not specified, **y1** is used.
#' @param z An optional ordinal channel for grouping data into (possibly stacked) series to be drawn as separate areas; defaults to **fill** if a channel, or **stroke** if a channel.
#' @param ... Additional options or plot-level attributes.
#' @param data_from The name of the data source this mark reads from (see [vg_data()]).
#' @param filter_by A Param/Selection (e.g. from [param()]) to filter this mark's data by.
#' @family mark functions
#' @export
vg_area_y <- function(spec = NULL, ariaDescription = vg_unset, ariaHidden = vg_unset, ariaLabel = vg_unset, channels = vg_unset, clip = vg_unset, curve = vg_unset, dx = vg_unset, dy = vg_unset, facet = vg_unset, facetAnchor = vg_unset, fill = vg_unset, fillOpacity = vg_unset, filter = vg_unset, fx = vg_unset, fy = vg_unset, href = vg_unset, imageFilter = vg_unset, margin = vg_unset, marginBottom = vg_unset, marginLeft = vg_unset, marginRight = vg_unset, marginTop = vg_unset, mixBlendMode = vg_unset, offset = vg_unset, opacity = vg_unset, order = vg_unset, paintOrder = vg_unset, pointerEvents = vg_unset, reverse = vg_unset, select = vg_unset, shapeRendering = vg_unset, sort = vg_unset, stroke = vg_unset, strokeDasharray = vg_unset, strokeDashoffset = vg_unset, strokeLinecap = vg_unset, strokeLinejoin = vg_unset, strokeMiterlimit = vg_unset, strokeOpacity = vg_unset, strokeWidth = vg_unset, target = vg_unset, tension = vg_unset, tip = vg_unset, title = vg_unset, x = vg_unset, y = vg_unset, y1 = vg_unset, y2 = vg_unset, z = vg_unset, ..., data_from = vg_unset, filter_by = vg_unset) {
  vg_mark_(spec, "areaY", ariaDescription = ariaDescription, ariaHidden = ariaHidden, ariaLabel = ariaLabel, channels = channels, clip = clip, curve = curve, dx = dx, dy = dy, facet = facet, facetAnchor = facetAnchor, fill = fill, fillOpacity = fillOpacity, filter = filter, fx = fx, fy = fy, href = href, imageFilter = imageFilter, margin = margin, marginBottom = marginBottom, marginLeft = marginLeft, marginRight = marginRight, marginTop = marginTop, mixBlendMode = mixBlendMode, offset = offset, opacity = opacity, order = order, paintOrder = paintOrder, pointerEvents = pointerEvents, reverse = reverse, select = select, shapeRendering = shapeRendering, sort = sort, stroke = stroke, strokeDasharray = strokeDasharray, strokeDashoffset = strokeDashoffset, strokeLinecap = strokeLinecap, strokeLinejoin = strokeLinejoin, strokeMiterlimit = strokeMiterlimit, strokeOpacity = strokeOpacity, strokeWidth = strokeWidth, target = target, tension = tension, tip = tip, title = title, x = x, y = y, y1 = y1, y2 = y2, z = z, ..., data_from = data_from, filter_by = filter_by)
}

#' The arrow mark.
#'
#' @param spec A plot fragment or `vgspec` to add this mark to, or `NULL` to start a new plot with just this mark.
#' @param ariaDescription The aria-description; a constant textual description.
#' @param ariaHidden The aria-hidden state; a constant indicating whether the element is exposed to an accessibility API.
#' @param ariaLabel The aria-label; a channel specifying short textual labels representing the value in the accessibility tree.
#' @param bend The angle, a constant in degrees, between the straight line intersecting the arrow’s two control points and the outgoing tangent direction of the arrow from the start point.
#' @param channels Additional named channels, for example to include in a tooltip.
#' @param clip How to clip the mark; one of: - *frame* or true - clip to the plot’s frame (inner area) - *sphere* - clip to the projected sphere (*e.g.*, front hemisphere) - null or false - do not clip The *sphere* clip option requires a geographic projection.
#' @param dx The horizontal offset in pixels; a constant option.
#' @param dy The vertical offset in pixels; a constant option.
#' @param facet Whether to enable or disable faceting; one of: - *auto* (default) - automatically determine if this mark should be faceted - *include* (or true) - draw the subset of the mark’s data in the current facet - *exclude* - draw the subset of the mark’s data *not* in the current facet - *super* - draw this mark in a single frame that covers all facets - null (or false) - repeat this mark’s data across all facets (*i.e.*, no faceting) When a mark uses *super* faceting, it is not allowed to use position scales (*x*, *y*, *fx*, or *fy*); *super* faceting is intended for decorations, such as labels and legends.
#' @param facetAnchor How to place the mark with respect to facets; one of: - null (default for most marks) - display the mark in each non-empty facet - *top*, *right*, *bottom*, or *left* - display the mark only in facets on the given side - *top-empty*, *right-empty*, *bottom-empty*, or *left-empty* (default for axis marks) - display the mark only in facets that have empty space on the given side: either the margin, or an empty facet - *empty* - display the mark in empty facets only
#' @param fill The fill; a constant CSS color string, or a channel typically bound to the *color* scale.
#' @param fillOpacity The fill-opacity; a constant number between 0 and 1, or a channel typically bound to the *opacity* scale.
#' @param filter Applies a transform to filter the mark’s index according to the given channel values; only truthy values are retained.
#' @param fx The horizontal facet position channel, for mark-level faceting, bound to the *fx* scale.
#' @param fy The vertical facet position channel, for mark-level faceting, bound to the *fy* scale.
#' @param headAngle How pointy the arrowhead is, in degrees; a constant typically between 0° and 180°, and defaults to 60°.
#' @param headLength The size of the arrowhead relative to the **strokeWidth**; a constant.
#' @param href The href; a channel specifying URLs for clickable links.
#' @param imageFilter A CSS filter; a constant string used to adjust the rendering of images, such as *blur(5px)*.
#' @param inset Shorthand to set the same default for **insetStart** and **insetEnd**.
#' @param insetEnd The ending inset, a constant in pixels; defaults to 0.
#' @param insetStart The starting inset, a constant in pixels; defaults to 0.
#' @param margin Shorthand to set the same default for all four mark margins: **marginTop**, **marginRight**, **marginBottom**, and **marginLeft**; typically defaults to 0, except for axis marks.
#' @param marginBottom The mark’s bottom margin; the minimum distance in pixels between the bottom edges of the inner and outer plot area.
#' @param marginLeft The mark’s left margin; the minimum distance in pixels between the left edges of the inner and outer plot area.
#' @param marginRight The mark’s right margin; the minimum distance in pixels between the right edges of the mark’s inner and outer plot area.
#' @param marginTop The mark’s top margin; the minimum distance in pixels between the top edges of the inner and outer plot area.
#' @param mixBlendMode The mix-blend-mode; a constant string specifying how to blend content such as *multiply*.
#' @param opacity The opacity; a constant between 0 and 1, or a channel typically bound to the *opacity* scale.
#' @param paintOrder The paint-order; a constant string specifying the order in which the **fill**, **stroke**, and any markers are drawn; defaults to *normal*, which draws the fill, then stroke, then markers; defaults to *stroke* for the text mark to create a “halo” around text to improve legibility.
#' @param pointerEvents The pointer-events property; a constant string such as *none*.
#' @param reverse Applies a transform to reverse the order of the mark’s index, say for reverse input order.
#' @param select Applies a filter transform after data is loaded to highlight selected values only.
#' @param shapeRendering The shape-rendering; a constant string such as *crispEdges*.
#' @param sort Either applies a transform to sort the mark’s index by the specified channel values, or imputes ordinal scale domains from this mark’s channels.
#' @param stroke The stroke; a constant CSS color string, or a channel typically bound to the *color* scale.
#' @param strokeDasharray The stroke-dasharray; a constant number indicating the length in pixels of alternating dashes and gaps, or a constant string of numbers separated by spaces or commas (_e.g._, *10 2* for dashes of 10 pixels separated by gaps of 2 pixels), or *none* (the default) for no dashing : https://developer.mozilla.org/en-US/docs/Web/SVG/Attribute/stroke-dasharray
#' @param strokeDashoffset The stroke-dashoffset; a constant indicating the offset in pixels of the first dash along the stroke; defaults to zero.
#' @param strokeLinecap The stroke-linecap; a constant specifying how to cap stroked paths, such as *butt*, *round*, or *square*.
#' @param strokeLinejoin The stroke-linejoin; a constant specifying how to join stroked paths, such as *bevel*, *miter*, *miter-clip*, or *round*.
#' @param strokeMiterlimit The stroke-miterlimit; a constant number specifying how to limit the length of *miter* joins on stroked paths.
#' @param strokeOpacity The stroke-opacity; a constant between 0 and 1, or a channel typically bound to the *opacity* scale.
#' @param strokeWidth The stroke-width; a constant number in pixels, or a channel.
#' @param sweep The sweep order; defaults to 1 indicating a positive (clockwise) bend angle; -1 indicates a negative (anticlockwise) bend angle; 0 effectively clears the bend angle.
#' @param target The target; a constant string specifying the target window (_e.g._, *_blank*) for clickable links; used in conjunction with the **href** option.
#' @param tip Whether to generate a tooltip for this mark, and any tip options.
#' @param title The title; a channel specifying accessible, short textual descriptions as strings (possibly with newlines).
#' @param x The horizontal position (or length) channel, typically bound to the *x* scale.
#' @param x1 The required primary (starting, often left) horizontal position channel, representing the area’s baseline, typically bound to the *x* scale.
#' @param x2 The optional secondary (ending, often right) horizontal position channel, representing the area’s topline, typically bound to the *x* scale; if not specified, **x1** is used.
#' @param y The vertical position channel, typically bound to the *y* scale; defaults to the zero-based index of the data 0, 1, 2, ….
#' @param y1 The required primary (starting, often bottom) vertical position channel, representing the area’s baseline, typically bound to the *y* scale.
#' @param y2 The optional secondary (ending, often top) vertical position channel, representing the area’s topline, typically bound to the *y* scale; if not specified, **y1** is used.
#' @param ... Additional options or plot-level attributes.
#' @param data_from The name of the data source this mark reads from (see [vg_data()]).
#' @param filter_by A Param/Selection (e.g. from [param()]) to filter this mark's data by.
#' @family mark functions
#' @export
vg_arrow <- function(spec = NULL, ariaDescription = vg_unset, ariaHidden = vg_unset, ariaLabel = vg_unset, bend = vg_unset, channels = vg_unset, clip = vg_unset, dx = vg_unset, dy = vg_unset, facet = vg_unset, facetAnchor = vg_unset, fill = vg_unset, fillOpacity = vg_unset, filter = vg_unset, fx = vg_unset, fy = vg_unset, headAngle = vg_unset, headLength = vg_unset, href = vg_unset, imageFilter = vg_unset, inset = vg_unset, insetEnd = vg_unset, insetStart = vg_unset, margin = vg_unset, marginBottom = vg_unset, marginLeft = vg_unset, marginRight = vg_unset, marginTop = vg_unset, mixBlendMode = vg_unset, opacity = vg_unset, paintOrder = vg_unset, pointerEvents = vg_unset, reverse = vg_unset, select = vg_unset, shapeRendering = vg_unset, sort = vg_unset, stroke = vg_unset, strokeDasharray = vg_unset, strokeDashoffset = vg_unset, strokeLinecap = vg_unset, strokeLinejoin = vg_unset, strokeMiterlimit = vg_unset, strokeOpacity = vg_unset, strokeWidth = vg_unset, sweep = vg_unset, target = vg_unset, tip = vg_unset, title = vg_unset, x = vg_unset, x1 = vg_unset, x2 = vg_unset, y = vg_unset, y1 = vg_unset, y2 = vg_unset, ..., data_from = vg_unset, filter_by = vg_unset) {
  vg_mark_(spec, "arrow", ariaDescription = ariaDescription, ariaHidden = ariaHidden, ariaLabel = ariaLabel, bend = bend, channels = channels, clip = clip, dx = dx, dy = dy, facet = facet, facetAnchor = facetAnchor, fill = fill, fillOpacity = fillOpacity, filter = filter, fx = fx, fy = fy, headAngle = headAngle, headLength = headLength, href = href, imageFilter = imageFilter, inset = inset, insetEnd = insetEnd, insetStart = insetStart, margin = margin, marginBottom = marginBottom, marginLeft = marginLeft, marginRight = marginRight, marginTop = marginTop, mixBlendMode = mixBlendMode, opacity = opacity, paintOrder = paintOrder, pointerEvents = pointerEvents, reverse = reverse, select = select, shapeRendering = shapeRendering, sort = sort, stroke = stroke, strokeDasharray = strokeDasharray, strokeDashoffset = strokeDashoffset, strokeLinecap = strokeLinecap, strokeLinejoin = strokeLinejoin, strokeMiterlimit = strokeMiterlimit, strokeOpacity = strokeOpacity, strokeWidth = strokeWidth, sweep = sweep, target = target, tip = tip, title = title, x = x, x1 = x1, x2 = x2, y = y, y1 = y1, y2 = y2, ..., data_from = data_from, filter_by = filter_by)
}

#' The axisFx mark.
#'
#' @param spec A plot fragment or `vgspec` to add this mark to, or `NULL` to start a new plot with just this mark.
#' @param anchor The side of the frame on which to place the axis: *top* or *bottom* for horizontal axes (axisX and axisFx) and their associated vertical grids (gridX and gridFx), or *left* or *right* for vertical axes (axisY and axisFY) and their associated horizontal grids (gridY and gridFy).
#' @param ariaDescription The aria-description; a constant textual description.
#' @param ariaHidden The aria-hidden state; a constant indicating whether the element is exposed to an accessibility API.
#' @param ariaLabel The aria-label; a channel specifying short textual labels representing the value in the accessibility tree.
#' @param channels Additional named channels, for example to include in a tooltip.
#' @param clip How to clip the mark; one of: - *frame* or true - clip to the plot’s frame (inner area) - *sphere* - clip to the projected sphere (*e.g.*, front hemisphere) - null or false - do not clip The *sphere* clip option requires a geographic projection.
#' @param color A shorthand for setting both **fill** and **stroke**; affects the stroke of tick vectors and grid rules, and the fill of tick texts and axis label texts; defaults to *currentColor*.
#' @param dx The horizontal offset in pixels; a constant option.
#' @param dy The vertical offset in pixels; a constant option.
#' @param facet Whether to enable or disable faceting; one of: - *auto* (default) - automatically determine if this mark should be faceted - *include* (or true) - draw the subset of the mark’s data in the current facet - *exclude* - draw the subset of the mark’s data *not* in the current facet - *super* - draw this mark in a single frame that covers all facets - null (or false) - repeat this mark’s data across all facets (*i.e.*, no faceting) When a mark uses *super* faceting, it is not allowed to use position scales (*x*, *y*, *fx*, or *fy*); *super* faceting is intended for decorations, such as labels and legends.
#' @param facetAnchor How to place the mark with respect to facets; one of: - null (default for most marks) - display the mark in each non-empty facet - *top*, *right*, *bottom*, or *left* - display the mark only in facets on the given side - *top-empty*, *right-empty*, *bottom-empty*, or *left-empty* (default for axis marks) - display the mark only in facets that have empty space on the given side: either the margin, or an empty facet - *empty* - display the mark in empty facets only
#' @param fill The fill; a constant CSS color string, or a channel typically bound to the *color* scale.
#' @param fillOpacity The fill-opacity; a constant number between 0 and 1, or a channel typically bound to the *opacity* scale.
#' @param filter Applies a transform to filter the mark’s index according to the given channel values; only truthy values are retained.
#' @param fontFamily The font-family; a constant; defaults to the plot’s font family, which is typically *system-ui*.
#' @param fontSize The font size in pixels; either a constant or a channel; defaults to the plot’s font size, which is typically 10.
#' @param fontStyle The font style; a constant; defaults to the plot’s font style, which is typically *normal*.
#' @param fontVariant The font variant; a constant; if the **text** channel contains numbers or dates, defaults to *tabular-nums* to facilitate comparing numbers; otherwise defaults to the plot’s font style, which is typically *normal*.
#' @param fontWeight The font weight; a constant; defaults to the plot’s font weight, which is typically *normal*.
#' @param frameAnchor The frame anchor specifies defaults for **x** and **y**, along with **textAnchor** and **lineAnchor**, based on the plot’s frame; it may be one of the four sides (*top*, *right*, *bottom*, *left*), one of the four corners (*top-left*, *top-right*, *bottom-right*, *bottom-left*), or the *middle* of the frame.
#' @param fx The horizontal facet position channel, for mark-level faceting, bound to the *fx* scale.
#' @param fy The vertical facet position channel, for mark-level faceting, bound to the *fy* scale.
#' @param href The href; a channel specifying URLs for clickable links.
#' @param imageFilter A CSS filter; a constant string used to adjust the rendering of images, such as *blur(5px)*.
#' @param inset Shorthand to set the same default for **insetStart** and **insetEnd**.
#' @param insetBottom Insets the bottom edge by the specified number of pixels.
#' @param insetTop Insets the top edge by the specified number of pixels.
#' @param interval Enforces uniformity for data at regular intervals, such as integer values or daily samples.
#' @param label A textual label to show on the axis or legend; if null, show no label.
#' @param labelAnchor Where to place the axis **label** relative to the plot’s frame.
#' @param labelArrow Whether to apply a directional arrow such as → or ↑ to the scale label.
#' @param labelOffset The axis **label** position offset (in pixels); default depends on margins and orientation.
#' @param lineAnchor The line anchor controls how text is aligned (typically vertically) relative to its anchor point; it is one of *top*, *bottom*, or *middle*.
#' @param lineHeight The line height in ems; defaults to 1.
#' @param lineWidth The line width in ems (e.g., 10 for about 20 characters); defaults to infinity, disabling wrapping and clipping.
#' @param margin Shorthand to set the same default for all four mark margins: **marginTop**, **marginRight**, **marginBottom**, and **marginLeft**; typically defaults to 0, except for axis marks.
#' @param marginBottom The mark’s bottom margin; the minimum distance in pixels between the bottom edges of the inner and outer plot area.
#' @param marginLeft The mark’s left margin; the minimum distance in pixels between the left edges of the inner and outer plot area.
#' @param marginRight The mark’s right margin; the minimum distance in pixels between the right edges of the mark’s inner and outer plot area.
#' @param marginTop The mark’s top margin; the minimum distance in pixels between the top edges of the inner and outer plot area.
#' @param marker Shorthand to set the same default for markerStart, markerMid, and markerEnd; one of: - a marker name such as *arrow* or *circle* - *none* (default) - no marker * true - alias for *circle-fill* * false or null - alias for *none*
#' @param markerEnd The marker for the ending point of a line segment; one of: - a marker name such as *arrow* or *circle* * *none* (default) - no marker * true - alias for *circle-fill* * false or null - alias for *none*
#' @param markerMid The marker for any middle (interior) points of a line segment.
#' @param markerStart The marker for the starting point of a line segment; one of: - a marker name such as *arrow* or *circle* * *none* (default) - no marker * true - alias for *circle-fill* * false or null - alias for *none*
#' @param mixBlendMode The mix-blend-mode; a constant string specifying how to blend content such as *multiply*.
#' @param monospace If true, changes the default **fontFamily** to *monospace*, and uses simplified monospaced text metrics calculations.
#' @param opacity The opacity; a constant between 0 and 1, or a channel typically bound to the *opacity* scale.
#' @param paintOrder The paint-order; a constant string specifying the order in which the **fill**, **stroke**, and any markers are drawn; defaults to *normal*, which draws the fill, then stroke, then markers; defaults to *stroke* for the text mark to create a “halo” around text to improve legibility.
#' @param pointerEvents The pointer-events property; a constant string such as *none*.
#' @param reverse Applies a transform to reverse the order of the mark’s index, say for reverse input order.
#' @param rotate The rotation angle in degrees clockwise; a constant or a channel; defaults to 0°.
#' @param select Applies a filter transform after data is loaded to highlight selected values only.
#' @param shapeRendering The shape-rendering; a constant string such as *crispEdges*.
#' @param sort Either applies a transform to sort the mark’s index by the specified channel values, or imputes ordinal scale domains from this mark’s channels.
#' @param stroke The stroke; a constant CSS color string, or a channel typically bound to the *color* scale.
#' @param strokeDasharray The stroke-dasharray; a constant number indicating the length in pixels of alternating dashes and gaps, or a constant string of numbers separated by spaces or commas (_e.g._, *10 2* for dashes of 10 pixels separated by gaps of 2 pixels), or *none* (the default) for no dashing : https://developer.mozilla.org/en-US/docs/Web/SVG/Attribute/stroke-dasharray
#' @param strokeDashoffset The stroke-dashoffset; a constant indicating the offset in pixels of the first dash along the stroke; defaults to zero.
#' @param strokeLinecap The stroke-linecap; a constant specifying how to cap stroked paths, such as *butt*, *round*, or *square*.
#' @param strokeLinejoin The stroke-linejoin; a constant specifying how to join stroked paths, such as *bevel*, *miter*, *miter-clip*, or *round*.
#' @param strokeMiterlimit The stroke-miterlimit; a constant number specifying how to limit the length of *miter* joins on stroked paths.
#' @param strokeOpacity The stroke-opacity; a constant between 0 and 1, or a channel typically bound to the *opacity* scale.
#' @param strokeWidth The stroke-width; a constant number in pixels, or a channel.
#' @param target The target; a constant string specifying the target window (_e.g._, *_blank*) for clickable links; used in conjunction with the **href** option.
#' @param text The text contents channel, possibly with line breaks (\\n, \\r\\n, or \\r).
#' @param textAnchor The text anchor controls how text is aligned (typically horizontally) relative to its anchor point; it is one of *start*, *end*, or *middle*.
#' @param textOverflow How truncate (or wrap) lines of text longer than the given **lineWidth**; one of: - null (default) - preserve overflowing characters (and wrap if needed) - *clip* or *clip-end* - remove characters from the end - *clip-start* - remove characters from the start - *ellipsis* or *ellipsis-end* - replace characters from the end with an ellipsis (…) - *ellipsis-start* - replace characters from the start with an ellipsis (…) - *ellipsis-middle* - replace characters from the middle with an ellipsis (…) If no **title** was specified, if text requires truncation, a title containing the non-truncated text will be implicitly added.
#' @param textStroke The tick text **stroke**, say for a *white* outline to improve legibility; defaults to null.
#' @param textStrokeOpacity The tick text **strokeOpacity**; defaults to 1; has no effect unless **textStroke** is set.
#' @param textStrokeWidth The tick text **strokeWidth**; defaults to 4; has no effect unless **textStroke** is set.
#' @param tickFormat How to format inputs (abstract values) for axis tick labels; one of: - a d3-format string for numeric scales - a d3-time-format string for temporal scales : https://d3js.org/d3-time : https://d3js.org/d3-time-format
#' @param tickPadding The distance between an axis tick mark and its associated text label (in pixels); often defaults to 3, but may be affected by **xTickSize** and **xTickRotate**.
#' @param tickRotate The rotation angle of axis tick labels in degrees clocksize; defaults to 0.
#' @param tickSize The length of axis tick marks in pixels; negative values extend in the opposite direction.
#' @param tickSpacing The desired approximate spacing between adjacent axis ticks, affecting the default **ticks**; defaults to 80 pixels for *x* and *fx*, and 35 pixels for *y* and *fy*.
#' @param ticks The desired approximate number of axis ticks, or an explicit array of tick values, or an interval such as *day* or *month*.
#' @param tip Whether to generate a tooltip for this mark, and any tip options.
#' @param title The title; a channel specifying accessible, short textual descriptions as strings (possibly with newlines).
#' @param x The horizontal position (or length) channel, typically bound to the *x* scale.
#' @param y The vertical position channel, typically bound to the *y* scale; defaults to the zero-based index of the data 0, 1, 2, ….
#' @param z An optional ordinal channel for grouping data into (possibly stacked) series to be drawn as separate areas; defaults to **fill** if a channel, or **stroke** if a channel.
#' @param ... Additional options or plot-level attributes.
#' @param data_from The name of the data source this mark reads from (see [vg_data()]).
#' @param filter_by A Param/Selection (e.g. from [param()]) to filter this mark's data by.
#' @family mark functions
#' @export
vg_axis_fx <- function(spec = NULL, anchor = vg_unset, ariaDescription = vg_unset, ariaHidden = vg_unset, ariaLabel = vg_unset, channels = vg_unset, clip = vg_unset, color = vg_unset, dx = vg_unset, dy = vg_unset, facet = vg_unset, facetAnchor = vg_unset, fill = vg_unset, fillOpacity = vg_unset, filter = vg_unset, fontFamily = vg_unset, fontSize = vg_unset, fontStyle = vg_unset, fontVariant = vg_unset, fontWeight = vg_unset, frameAnchor = vg_unset, fx = vg_unset, fy = vg_unset, href = vg_unset, imageFilter = vg_unset, inset = vg_unset, insetBottom = vg_unset, insetTop = vg_unset, interval = vg_unset, label = vg_unset, labelAnchor = vg_unset, labelArrow = vg_unset, labelOffset = vg_unset, lineAnchor = vg_unset, lineHeight = vg_unset, lineWidth = vg_unset, margin = vg_unset, marginBottom = vg_unset, marginLeft = vg_unset, marginRight = vg_unset, marginTop = vg_unset, marker = vg_unset, markerEnd = vg_unset, markerMid = vg_unset, markerStart = vg_unset, mixBlendMode = vg_unset, monospace = vg_unset, opacity = vg_unset, paintOrder = vg_unset, pointerEvents = vg_unset, reverse = vg_unset, rotate = vg_unset, select = vg_unset, shapeRendering = vg_unset, sort = vg_unset, stroke = vg_unset, strokeDasharray = vg_unset, strokeDashoffset = vg_unset, strokeLinecap = vg_unset, strokeLinejoin = vg_unset, strokeMiterlimit = vg_unset, strokeOpacity = vg_unset, strokeWidth = vg_unset, target = vg_unset, text = vg_unset, textAnchor = vg_unset, textOverflow = vg_unset, textStroke = vg_unset, textStrokeOpacity = vg_unset, textStrokeWidth = vg_unset, tickFormat = vg_unset, tickPadding = vg_unset, tickRotate = vg_unset, tickSize = vg_unset, tickSpacing = vg_unset, ticks = vg_unset, tip = vg_unset, title = vg_unset, x = vg_unset, y = vg_unset, z = vg_unset, ..., data_from = vg_unset, filter_by = vg_unset) {
  vg_mark_(spec, "axisFx", anchor = anchor, ariaDescription = ariaDescription, ariaHidden = ariaHidden, ariaLabel = ariaLabel, channels = channels, clip = clip, color = color, dx = dx, dy = dy, facet = facet, facetAnchor = facetAnchor, fill = fill, fillOpacity = fillOpacity, filter = filter, fontFamily = fontFamily, fontSize = fontSize, fontStyle = fontStyle, fontVariant = fontVariant, fontWeight = fontWeight, frameAnchor = frameAnchor, fx = fx, fy = fy, href = href, imageFilter = imageFilter, inset = inset, insetBottom = insetBottom, insetTop = insetTop, interval = interval, label = label, labelAnchor = labelAnchor, labelArrow = labelArrow, labelOffset = labelOffset, lineAnchor = lineAnchor, lineHeight = lineHeight, lineWidth = lineWidth, margin = margin, marginBottom = marginBottom, marginLeft = marginLeft, marginRight = marginRight, marginTop = marginTop, marker = marker, markerEnd = markerEnd, markerMid = markerMid, markerStart = markerStart, mixBlendMode = mixBlendMode, monospace = monospace, opacity = opacity, paintOrder = paintOrder, pointerEvents = pointerEvents, reverse = reverse, rotate = rotate, select = select, shapeRendering = shapeRendering, sort = sort, stroke = stroke, strokeDasharray = strokeDasharray, strokeDashoffset = strokeDashoffset, strokeLinecap = strokeLinecap, strokeLinejoin = strokeLinejoin, strokeMiterlimit = strokeMiterlimit, strokeOpacity = strokeOpacity, strokeWidth = strokeWidth, target = target, text = text, textAnchor = textAnchor, textOverflow = textOverflow, textStroke = textStroke, textStrokeOpacity = textStrokeOpacity, textStrokeWidth = textStrokeWidth, tickFormat = tickFormat, tickPadding = tickPadding, tickRotate = tickRotate, tickSize = tickSize, tickSpacing = tickSpacing, ticks = ticks, tip = tip, title = title, x = x, y = y, z = z, ..., data_from = data_from, filter_by = filter_by)
}

#' The axisFy mark.
#'
#' @param spec A plot fragment or `vgspec` to add this mark to, or `NULL` to start a new plot with just this mark.
#' @param anchor The side of the frame on which to place the axis: *top* or *bottom* for horizontal axes (axisX and axisFx) and their associated vertical grids (gridX and gridFx), or *left* or *right* for vertical axes (axisY and axisFY) and their associated horizontal grids (gridY and gridFy).
#' @param ariaDescription The aria-description; a constant textual description.
#' @param ariaHidden The aria-hidden state; a constant indicating whether the element is exposed to an accessibility API.
#' @param ariaLabel The aria-label; a channel specifying short textual labels representing the value in the accessibility tree.
#' @param channels Additional named channels, for example to include in a tooltip.
#' @param clip How to clip the mark; one of: - *frame* or true - clip to the plot’s frame (inner area) - *sphere* - clip to the projected sphere (*e.g.*, front hemisphere) - null or false - do not clip The *sphere* clip option requires a geographic projection.
#' @param color A shorthand for setting both **fill** and **stroke**; affects the stroke of tick vectors and grid rules, and the fill of tick texts and axis label texts; defaults to *currentColor*.
#' @param dx The horizontal offset in pixels; a constant option.
#' @param dy The vertical offset in pixels; a constant option.
#' @param facet Whether to enable or disable faceting; one of: - *auto* (default) - automatically determine if this mark should be faceted - *include* (or true) - draw the subset of the mark’s data in the current facet - *exclude* - draw the subset of the mark’s data *not* in the current facet - *super* - draw this mark in a single frame that covers all facets - null (or false) - repeat this mark’s data across all facets (*i.e.*, no faceting) When a mark uses *super* faceting, it is not allowed to use position scales (*x*, *y*, *fx*, or *fy*); *super* faceting is intended for decorations, such as labels and legends.
#' @param facetAnchor How to place the mark with respect to facets; one of: - null (default for most marks) - display the mark in each non-empty facet - *top*, *right*, *bottom*, or *left* - display the mark only in facets on the given side - *top-empty*, *right-empty*, *bottom-empty*, or *left-empty* (default for axis marks) - display the mark only in facets that have empty space on the given side: either the margin, or an empty facet - *empty* - display the mark in empty facets only
#' @param fill The fill; a constant CSS color string, or a channel typically bound to the *color* scale.
#' @param fillOpacity The fill-opacity; a constant number between 0 and 1, or a channel typically bound to the *opacity* scale.
#' @param filter Applies a transform to filter the mark’s index according to the given channel values; only truthy values are retained.
#' @param fontFamily The font-family; a constant; defaults to the plot’s font family, which is typically *system-ui*.
#' @param fontSize The font size in pixels; either a constant or a channel; defaults to the plot’s font size, which is typically 10.
#' @param fontStyle The font style; a constant; defaults to the plot’s font style, which is typically *normal*.
#' @param fontVariant The font variant; a constant; if the **text** channel contains numbers or dates, defaults to *tabular-nums* to facilitate comparing numbers; otherwise defaults to the plot’s font style, which is typically *normal*.
#' @param fontWeight The font weight; a constant; defaults to the plot’s font weight, which is typically *normal*.
#' @param frameAnchor The frame anchor specifies defaults for **x** and **y**, along with **textAnchor** and **lineAnchor**, based on the plot’s frame; it may be one of the four sides (*top*, *right*, *bottom*, *left*), one of the four corners (*top-left*, *top-right*, *bottom-right*, *bottom-left*), or the *middle* of the frame.
#' @param fx The horizontal facet position channel, for mark-level faceting, bound to the *fx* scale.
#' @param fy The vertical facet position channel, for mark-level faceting, bound to the *fy* scale.
#' @param href The href; a channel specifying URLs for clickable links.
#' @param imageFilter A CSS filter; a constant string used to adjust the rendering of images, such as *blur(5px)*.
#' @param inset Shorthand to set the same default for **insetStart** and **insetEnd**.
#' @param insetLeft Insets the left edge by the specified number of pixels.
#' @param insetRight Insets the right edge by the specified number of pixels.
#' @param interval Enforces uniformity for data at regular intervals, such as integer values or daily samples.
#' @param label A textual label to show on the axis or legend; if null, show no label.
#' @param labelAnchor Where to place the axis **label** relative to the plot’s frame.
#' @param labelArrow Whether to apply a directional arrow such as → or ↑ to the scale label.
#' @param labelOffset The axis **label** position offset (in pixels); default depends on margins and orientation.
#' @param lineAnchor The line anchor controls how text is aligned (typically vertically) relative to its anchor point; it is one of *top*, *bottom*, or *middle*.
#' @param lineHeight The line height in ems; defaults to 1.
#' @param lineWidth The line width in ems (e.g., 10 for about 20 characters); defaults to infinity, disabling wrapping and clipping.
#' @param margin Shorthand to set the same default for all four mark margins: **marginTop**, **marginRight**, **marginBottom**, and **marginLeft**; typically defaults to 0, except for axis marks.
#' @param marginBottom The mark’s bottom margin; the minimum distance in pixels between the bottom edges of the inner and outer plot area.
#' @param marginLeft The mark’s left margin; the minimum distance in pixels between the left edges of the inner and outer plot area.
#' @param marginRight The mark’s right margin; the minimum distance in pixels between the right edges of the mark’s inner and outer plot area.
#' @param marginTop The mark’s top margin; the minimum distance in pixels between the top edges of the inner and outer plot area.
#' @param marker Shorthand to set the same default for markerStart, markerMid, and markerEnd; one of: - a marker name such as *arrow* or *circle* - *none* (default) - no marker * true - alias for *circle-fill* * false or null - alias for *none*
#' @param markerEnd The marker for the ending point of a line segment; one of: - a marker name such as *arrow* or *circle* * *none* (default) - no marker * true - alias for *circle-fill* * false or null - alias for *none*
#' @param markerMid The marker for any middle (interior) points of a line segment.
#' @param markerStart The marker for the starting point of a line segment; one of: - a marker name such as *arrow* or *circle* * *none* (default) - no marker * true - alias for *circle-fill* * false or null - alias for *none*
#' @param mixBlendMode The mix-blend-mode; a constant string specifying how to blend content such as *multiply*.
#' @param monospace If true, changes the default **fontFamily** to *monospace*, and uses simplified monospaced text metrics calculations.
#' @param opacity The opacity; a constant between 0 and 1, or a channel typically bound to the *opacity* scale.
#' @param paintOrder The paint-order; a constant string specifying the order in which the **fill**, **stroke**, and any markers are drawn; defaults to *normal*, which draws the fill, then stroke, then markers; defaults to *stroke* for the text mark to create a “halo” around text to improve legibility.
#' @param pointerEvents The pointer-events property; a constant string such as *none*.
#' @param reverse Applies a transform to reverse the order of the mark’s index, say for reverse input order.
#' @param rotate The rotation angle in degrees clockwise; a constant or a channel; defaults to 0°.
#' @param select Applies a filter transform after data is loaded to highlight selected values only.
#' @param shapeRendering The shape-rendering; a constant string such as *crispEdges*.
#' @param sort Either applies a transform to sort the mark’s index by the specified channel values, or imputes ordinal scale domains from this mark’s channels.
#' @param stroke The stroke; a constant CSS color string, or a channel typically bound to the *color* scale.
#' @param strokeDasharray The stroke-dasharray; a constant number indicating the length in pixels of alternating dashes and gaps, or a constant string of numbers separated by spaces or commas (_e.g._, *10 2* for dashes of 10 pixels separated by gaps of 2 pixels), or *none* (the default) for no dashing : https://developer.mozilla.org/en-US/docs/Web/SVG/Attribute/stroke-dasharray
#' @param strokeDashoffset The stroke-dashoffset; a constant indicating the offset in pixels of the first dash along the stroke; defaults to zero.
#' @param strokeLinecap The stroke-linecap; a constant specifying how to cap stroked paths, such as *butt*, *round*, or *square*.
#' @param strokeLinejoin The stroke-linejoin; a constant specifying how to join stroked paths, such as *bevel*, *miter*, *miter-clip*, or *round*.
#' @param strokeMiterlimit The stroke-miterlimit; a constant number specifying how to limit the length of *miter* joins on stroked paths.
#' @param strokeOpacity The stroke-opacity; a constant between 0 and 1, or a channel typically bound to the *opacity* scale.
#' @param strokeWidth The stroke-width; a constant number in pixels, or a channel.
#' @param target The target; a constant string specifying the target window (_e.g._, *_blank*) for clickable links; used in conjunction with the **href** option.
#' @param text The text contents channel, possibly with line breaks (\\n, \\r\\n, or \\r).
#' @param textAnchor The text anchor controls how text is aligned (typically horizontally) relative to its anchor point; it is one of *start*, *end*, or *middle*.
#' @param textOverflow How truncate (or wrap) lines of text longer than the given **lineWidth**; one of: - null (default) - preserve overflowing characters (and wrap if needed) - *clip* or *clip-end* - remove characters from the end - *clip-start* - remove characters from the start - *ellipsis* or *ellipsis-end* - replace characters from the end with an ellipsis (…) - *ellipsis-start* - replace characters from the start with an ellipsis (…) - *ellipsis-middle* - replace characters from the middle with an ellipsis (…) If no **title** was specified, if text requires truncation, a title containing the non-truncated text will be implicitly added.
#' @param textStroke The tick text **stroke**, say for a *white* outline to improve legibility; defaults to null.
#' @param textStrokeOpacity The tick text **strokeOpacity**; defaults to 1; has no effect unless **textStroke** is set.
#' @param textStrokeWidth The tick text **strokeWidth**; defaults to 4; has no effect unless **textStroke** is set.
#' @param tickFormat How to format inputs (abstract values) for axis tick labels; one of: - a d3-format string for numeric scales - a d3-time-format string for temporal scales : https://d3js.org/d3-time : https://d3js.org/d3-time-format
#' @param tickPadding The distance between an axis tick mark and its associated text label (in pixels); often defaults to 3, but may be affected by **xTickSize** and **xTickRotate**.
#' @param tickRotate The rotation angle of axis tick labels in degrees clocksize; defaults to 0.
#' @param tickSize The length of axis tick marks in pixels; negative values extend in the opposite direction.
#' @param tickSpacing The desired approximate spacing between adjacent axis ticks, affecting the default **ticks**; defaults to 80 pixels for *x* and *fx*, and 35 pixels for *y* and *fy*.
#' @param ticks The desired approximate number of axis ticks, or an explicit array of tick values, or an interval such as *day* or *month*.
#' @param tip Whether to generate a tooltip for this mark, and any tip options.
#' @param title The title; a channel specifying accessible, short textual descriptions as strings (possibly with newlines).
#' @param x The horizontal position (or length) channel, typically bound to the *x* scale.
#' @param y The vertical position channel, typically bound to the *y* scale; defaults to the zero-based index of the data 0, 1, 2, ….
#' @param z An optional ordinal channel for grouping data into (possibly stacked) series to be drawn as separate areas; defaults to **fill** if a channel, or **stroke** if a channel.
#' @param ... Additional options or plot-level attributes.
#' @param data_from The name of the data source this mark reads from (see [vg_data()]).
#' @param filter_by A Param/Selection (e.g. from [param()]) to filter this mark's data by.
#' @family mark functions
#' @export
vg_axis_fy <- function(spec = NULL, anchor = vg_unset, ariaDescription = vg_unset, ariaHidden = vg_unset, ariaLabel = vg_unset, channels = vg_unset, clip = vg_unset, color = vg_unset, dx = vg_unset, dy = vg_unset, facet = vg_unset, facetAnchor = vg_unset, fill = vg_unset, fillOpacity = vg_unset, filter = vg_unset, fontFamily = vg_unset, fontSize = vg_unset, fontStyle = vg_unset, fontVariant = vg_unset, fontWeight = vg_unset, frameAnchor = vg_unset, fx = vg_unset, fy = vg_unset, href = vg_unset, imageFilter = vg_unset, inset = vg_unset, insetLeft = vg_unset, insetRight = vg_unset, interval = vg_unset, label = vg_unset, labelAnchor = vg_unset, labelArrow = vg_unset, labelOffset = vg_unset, lineAnchor = vg_unset, lineHeight = vg_unset, lineWidth = vg_unset, margin = vg_unset, marginBottom = vg_unset, marginLeft = vg_unset, marginRight = vg_unset, marginTop = vg_unset, marker = vg_unset, markerEnd = vg_unset, markerMid = vg_unset, markerStart = vg_unset, mixBlendMode = vg_unset, monospace = vg_unset, opacity = vg_unset, paintOrder = vg_unset, pointerEvents = vg_unset, reverse = vg_unset, rotate = vg_unset, select = vg_unset, shapeRendering = vg_unset, sort = vg_unset, stroke = vg_unset, strokeDasharray = vg_unset, strokeDashoffset = vg_unset, strokeLinecap = vg_unset, strokeLinejoin = vg_unset, strokeMiterlimit = vg_unset, strokeOpacity = vg_unset, strokeWidth = vg_unset, target = vg_unset, text = vg_unset, textAnchor = vg_unset, textOverflow = vg_unset, textStroke = vg_unset, textStrokeOpacity = vg_unset, textStrokeWidth = vg_unset, tickFormat = vg_unset, tickPadding = vg_unset, tickRotate = vg_unset, tickSize = vg_unset, tickSpacing = vg_unset, ticks = vg_unset, tip = vg_unset, title = vg_unset, x = vg_unset, y = vg_unset, z = vg_unset, ..., data_from = vg_unset, filter_by = vg_unset) {
  vg_mark_(spec, "axisFy", anchor = anchor, ariaDescription = ariaDescription, ariaHidden = ariaHidden, ariaLabel = ariaLabel, channels = channels, clip = clip, color = color, dx = dx, dy = dy, facet = facet, facetAnchor = facetAnchor, fill = fill, fillOpacity = fillOpacity, filter = filter, fontFamily = fontFamily, fontSize = fontSize, fontStyle = fontStyle, fontVariant = fontVariant, fontWeight = fontWeight, frameAnchor = frameAnchor, fx = fx, fy = fy, href = href, imageFilter = imageFilter, inset = inset, insetLeft = insetLeft, insetRight = insetRight, interval = interval, label = label, labelAnchor = labelAnchor, labelArrow = labelArrow, labelOffset = labelOffset, lineAnchor = lineAnchor, lineHeight = lineHeight, lineWidth = lineWidth, margin = margin, marginBottom = marginBottom, marginLeft = marginLeft, marginRight = marginRight, marginTop = marginTop, marker = marker, markerEnd = markerEnd, markerMid = markerMid, markerStart = markerStart, mixBlendMode = mixBlendMode, monospace = monospace, opacity = opacity, paintOrder = paintOrder, pointerEvents = pointerEvents, reverse = reverse, rotate = rotate, select = select, shapeRendering = shapeRendering, sort = sort, stroke = stroke, strokeDasharray = strokeDasharray, strokeDashoffset = strokeDashoffset, strokeLinecap = strokeLinecap, strokeLinejoin = strokeLinejoin, strokeMiterlimit = strokeMiterlimit, strokeOpacity = strokeOpacity, strokeWidth = strokeWidth, target = target, text = text, textAnchor = textAnchor, textOverflow = textOverflow, textStroke = textStroke, textStrokeOpacity = textStrokeOpacity, textStrokeWidth = textStrokeWidth, tickFormat = tickFormat, tickPadding = tickPadding, tickRotate = tickRotate, tickSize = tickSize, tickSpacing = tickSpacing, ticks = ticks, tip = tip, title = title, x = x, y = y, z = z, ..., data_from = data_from, filter_by = filter_by)
}

#' The axisX mark.
#'
#' @param spec A plot fragment or `vgspec` to add this mark to, or `NULL` to start a new plot with just this mark.
#' @param anchor The side of the frame on which to place the axis: *top* or *bottom* for horizontal axes (axisX and axisFx) and their associated vertical grids (gridX and gridFx), or *left* or *right* for vertical axes (axisY and axisFY) and their associated horizontal grids (gridY and gridFy).
#' @param ariaDescription The aria-description; a constant textual description.
#' @param ariaHidden The aria-hidden state; a constant indicating whether the element is exposed to an accessibility API.
#' @param ariaLabel The aria-label; a channel specifying short textual labels representing the value in the accessibility tree.
#' @param channels Additional named channels, for example to include in a tooltip.
#' @param clip How to clip the mark; one of: - *frame* or true - clip to the plot’s frame (inner area) - *sphere* - clip to the projected sphere (*e.g.*, front hemisphere) - null or false - do not clip The *sphere* clip option requires a geographic projection.
#' @param color A shorthand for setting both **fill** and **stroke**; affects the stroke of tick vectors and grid rules, and the fill of tick texts and axis label texts; defaults to *currentColor*.
#' @param dx The horizontal offset in pixels; a constant option.
#' @param dy The vertical offset in pixels; a constant option.
#' @param facet Whether to enable or disable faceting; one of: - *auto* (default) - automatically determine if this mark should be faceted - *include* (or true) - draw the subset of the mark’s data in the current facet - *exclude* - draw the subset of the mark’s data *not* in the current facet - *super* - draw this mark in a single frame that covers all facets - null (or false) - repeat this mark’s data across all facets (*i.e.*, no faceting) When a mark uses *super* faceting, it is not allowed to use position scales (*x*, *y*, *fx*, or *fy*); *super* faceting is intended for decorations, such as labels and legends.
#' @param facetAnchor How to place the mark with respect to facets; one of: - null (default for most marks) - display the mark in each non-empty facet - *top*, *right*, *bottom*, or *left* - display the mark only in facets on the given side - *top-empty*, *right-empty*, *bottom-empty*, or *left-empty* (default for axis marks) - display the mark only in facets that have empty space on the given side: either the margin, or an empty facet - *empty* - display the mark in empty facets only
#' @param fill The fill; a constant CSS color string, or a channel typically bound to the *color* scale.
#' @param fillOpacity The fill-opacity; a constant number between 0 and 1, or a channel typically bound to the *opacity* scale.
#' @param filter Applies a transform to filter the mark’s index according to the given channel values; only truthy values are retained.
#' @param fontFamily The font-family; a constant; defaults to the plot’s font family, which is typically *system-ui*.
#' @param fontSize The font size in pixels; either a constant or a channel; defaults to the plot’s font size, which is typically 10.
#' @param fontStyle The font style; a constant; defaults to the plot’s font style, which is typically *normal*.
#' @param fontVariant The font variant; a constant; if the **text** channel contains numbers or dates, defaults to *tabular-nums* to facilitate comparing numbers; otherwise defaults to the plot’s font style, which is typically *normal*.
#' @param fontWeight The font weight; a constant; defaults to the plot’s font weight, which is typically *normal*.
#' @param frameAnchor The frame anchor specifies defaults for **x** and **y**, along with **textAnchor** and **lineAnchor**, based on the plot’s frame; it may be one of the four sides (*top*, *right*, *bottom*, *left*), one of the four corners (*top-left*, *top-right*, *bottom-right*, *bottom-left*), or the *middle* of the frame.
#' @param fx The horizontal facet position channel, for mark-level faceting, bound to the *fx* scale.
#' @param fy The vertical facet position channel, for mark-level faceting, bound to the *fy* scale.
#' @param href The href; a channel specifying URLs for clickable links.
#' @param imageFilter A CSS filter; a constant string used to adjust the rendering of images, such as *blur(5px)*.
#' @param inset Shorthand to set the same default for **insetStart** and **insetEnd**.
#' @param insetBottom Insets the bottom edge by the specified number of pixels.
#' @param insetTop Insets the top edge by the specified number of pixels.
#' @param interval Enforces uniformity for data at regular intervals, such as integer values or daily samples.
#' @param label A textual label to show on the axis or legend; if null, show no label.
#' @param labelAnchor Where to place the axis **label** relative to the plot’s frame.
#' @param labelArrow Whether to apply a directional arrow such as → or ↑ to the scale label.
#' @param labelOffset The axis **label** position offset (in pixels); default depends on margins and orientation.
#' @param lineAnchor The line anchor controls how text is aligned (typically vertically) relative to its anchor point; it is one of *top*, *bottom*, or *middle*.
#' @param lineHeight The line height in ems; defaults to 1.
#' @param lineWidth The line width in ems (e.g., 10 for about 20 characters); defaults to infinity, disabling wrapping and clipping.
#' @param margin Shorthand to set the same default for all four mark margins: **marginTop**, **marginRight**, **marginBottom**, and **marginLeft**; typically defaults to 0, except for axis marks.
#' @param marginBottom The mark’s bottom margin; the minimum distance in pixels between the bottom edges of the inner and outer plot area.
#' @param marginLeft The mark’s left margin; the minimum distance in pixels between the left edges of the inner and outer plot area.
#' @param marginRight The mark’s right margin; the minimum distance in pixels between the right edges of the mark’s inner and outer plot area.
#' @param marginTop The mark’s top margin; the minimum distance in pixels between the top edges of the inner and outer plot area.
#' @param marker Shorthand to set the same default for markerStart, markerMid, and markerEnd; one of: - a marker name such as *arrow* or *circle* - *none* (default) - no marker * true - alias for *circle-fill* * false or null - alias for *none*
#' @param markerEnd The marker for the ending point of a line segment; one of: - a marker name such as *arrow* or *circle* * *none* (default) - no marker * true - alias for *circle-fill* * false or null - alias for *none*
#' @param markerMid The marker for any middle (interior) points of a line segment.
#' @param markerStart The marker for the starting point of a line segment; one of: - a marker name such as *arrow* or *circle* * *none* (default) - no marker * true - alias for *circle-fill* * false or null - alias for *none*
#' @param mixBlendMode The mix-blend-mode; a constant string specifying how to blend content such as *multiply*.
#' @param monospace If true, changes the default **fontFamily** to *monospace*, and uses simplified monospaced text metrics calculations.
#' @param opacity The opacity; a constant between 0 and 1, or a channel typically bound to the *opacity* scale.
#' @param paintOrder The paint-order; a constant string specifying the order in which the **fill**, **stroke**, and any markers are drawn; defaults to *normal*, which draws the fill, then stroke, then markers; defaults to *stroke* for the text mark to create a “halo” around text to improve legibility.
#' @param pointerEvents The pointer-events property; a constant string such as *none*.
#' @param reverse Applies a transform to reverse the order of the mark’s index, say for reverse input order.
#' @param rotate The rotation angle in degrees clockwise; a constant or a channel; defaults to 0°.
#' @param select Applies a filter transform after data is loaded to highlight selected values only.
#' @param shapeRendering The shape-rendering; a constant string such as *crispEdges*.
#' @param sort Either applies a transform to sort the mark’s index by the specified channel values, or imputes ordinal scale domains from this mark’s channels.
#' @param stroke The stroke; a constant CSS color string, or a channel typically bound to the *color* scale.
#' @param strokeDasharray The stroke-dasharray; a constant number indicating the length in pixels of alternating dashes and gaps, or a constant string of numbers separated by spaces or commas (_e.g._, *10 2* for dashes of 10 pixels separated by gaps of 2 pixels), or *none* (the default) for no dashing : https://developer.mozilla.org/en-US/docs/Web/SVG/Attribute/stroke-dasharray
#' @param strokeDashoffset The stroke-dashoffset; a constant indicating the offset in pixels of the first dash along the stroke; defaults to zero.
#' @param strokeLinecap The stroke-linecap; a constant specifying how to cap stroked paths, such as *butt*, *round*, or *square*.
#' @param strokeLinejoin The stroke-linejoin; a constant specifying how to join stroked paths, such as *bevel*, *miter*, *miter-clip*, or *round*.
#' @param strokeMiterlimit The stroke-miterlimit; a constant number specifying how to limit the length of *miter* joins on stroked paths.
#' @param strokeOpacity The stroke-opacity; a constant between 0 and 1, or a channel typically bound to the *opacity* scale.
#' @param strokeWidth The stroke-width; a constant number in pixels, or a channel.
#' @param target The target; a constant string specifying the target window (_e.g._, *_blank*) for clickable links; used in conjunction with the **href** option.
#' @param text The text contents channel, possibly with line breaks (\\n, \\r\\n, or \\r).
#' @param textAnchor The text anchor controls how text is aligned (typically horizontally) relative to its anchor point; it is one of *start*, *end*, or *middle*.
#' @param textOverflow How truncate (or wrap) lines of text longer than the given **lineWidth**; one of: - null (default) - preserve overflowing characters (and wrap if needed) - *clip* or *clip-end* - remove characters from the end - *clip-start* - remove characters from the start - *ellipsis* or *ellipsis-end* - replace characters from the end with an ellipsis (…) - *ellipsis-start* - replace characters from the start with an ellipsis (…) - *ellipsis-middle* - replace characters from the middle with an ellipsis (…) If no **title** was specified, if text requires truncation, a title containing the non-truncated text will be implicitly added.
#' @param textStroke The tick text **stroke**, say for a *white* outline to improve legibility; defaults to null.
#' @param textStrokeOpacity The tick text **strokeOpacity**; defaults to 1; has no effect unless **textStroke** is set.
#' @param textStrokeWidth The tick text **strokeWidth**; defaults to 4; has no effect unless **textStroke** is set.
#' @param tickFormat How to format inputs (abstract values) for axis tick labels; one of: - a d3-format string for numeric scales - a d3-time-format string for temporal scales : https://d3js.org/d3-time : https://d3js.org/d3-time-format
#' @param tickPadding The distance between an axis tick mark and its associated text label (in pixels); often defaults to 3, but may be affected by **xTickSize** and **xTickRotate**.
#' @param tickRotate The rotation angle of axis tick labels in degrees clocksize; defaults to 0.
#' @param tickSize The length of axis tick marks in pixels; negative values extend in the opposite direction.
#' @param tickSpacing The desired approximate spacing between adjacent axis ticks, affecting the default **ticks**; defaults to 80 pixels for *x* and *fx*, and 35 pixels for *y* and *fy*.
#' @param ticks The desired approximate number of axis ticks, or an explicit array of tick values, or an interval such as *day* or *month*.
#' @param tip Whether to generate a tooltip for this mark, and any tip options.
#' @param title The title; a channel specifying accessible, short textual descriptions as strings (possibly with newlines).
#' @param x The horizontal position (or length) channel, typically bound to the *x* scale.
#' @param y The vertical position channel, typically bound to the *y* scale; defaults to the zero-based index of the data 0, 1, 2, ….
#' @param z An optional ordinal channel for grouping data into (possibly stacked) series to be drawn as separate areas; defaults to **fill** if a channel, or **stroke** if a channel.
#' @param ... Additional options or plot-level attributes.
#' @param data_from The name of the data source this mark reads from (see [vg_data()]).
#' @param filter_by A Param/Selection (e.g. from [param()]) to filter this mark's data by.
#' @family mark functions
#' @export
vg_axis_x <- function(spec = NULL, anchor = vg_unset, ariaDescription = vg_unset, ariaHidden = vg_unset, ariaLabel = vg_unset, channels = vg_unset, clip = vg_unset, color = vg_unset, dx = vg_unset, dy = vg_unset, facet = vg_unset, facetAnchor = vg_unset, fill = vg_unset, fillOpacity = vg_unset, filter = vg_unset, fontFamily = vg_unset, fontSize = vg_unset, fontStyle = vg_unset, fontVariant = vg_unset, fontWeight = vg_unset, frameAnchor = vg_unset, fx = vg_unset, fy = vg_unset, href = vg_unset, imageFilter = vg_unset, inset = vg_unset, insetBottom = vg_unset, insetTop = vg_unset, interval = vg_unset, label = vg_unset, labelAnchor = vg_unset, labelArrow = vg_unset, labelOffset = vg_unset, lineAnchor = vg_unset, lineHeight = vg_unset, lineWidth = vg_unset, margin = vg_unset, marginBottom = vg_unset, marginLeft = vg_unset, marginRight = vg_unset, marginTop = vg_unset, marker = vg_unset, markerEnd = vg_unset, markerMid = vg_unset, markerStart = vg_unset, mixBlendMode = vg_unset, monospace = vg_unset, opacity = vg_unset, paintOrder = vg_unset, pointerEvents = vg_unset, reverse = vg_unset, rotate = vg_unset, select = vg_unset, shapeRendering = vg_unset, sort = vg_unset, stroke = vg_unset, strokeDasharray = vg_unset, strokeDashoffset = vg_unset, strokeLinecap = vg_unset, strokeLinejoin = vg_unset, strokeMiterlimit = vg_unset, strokeOpacity = vg_unset, strokeWidth = vg_unset, target = vg_unset, text = vg_unset, textAnchor = vg_unset, textOverflow = vg_unset, textStroke = vg_unset, textStrokeOpacity = vg_unset, textStrokeWidth = vg_unset, tickFormat = vg_unset, tickPadding = vg_unset, tickRotate = vg_unset, tickSize = vg_unset, tickSpacing = vg_unset, ticks = vg_unset, tip = vg_unset, title = vg_unset, x = vg_unset, y = vg_unset, z = vg_unset, ..., data_from = vg_unset, filter_by = vg_unset) {
  vg_mark_(spec, "axisX", anchor = anchor, ariaDescription = ariaDescription, ariaHidden = ariaHidden, ariaLabel = ariaLabel, channels = channels, clip = clip, color = color, dx = dx, dy = dy, facet = facet, facetAnchor = facetAnchor, fill = fill, fillOpacity = fillOpacity, filter = filter, fontFamily = fontFamily, fontSize = fontSize, fontStyle = fontStyle, fontVariant = fontVariant, fontWeight = fontWeight, frameAnchor = frameAnchor, fx = fx, fy = fy, href = href, imageFilter = imageFilter, inset = inset, insetBottom = insetBottom, insetTop = insetTop, interval = interval, label = label, labelAnchor = labelAnchor, labelArrow = labelArrow, labelOffset = labelOffset, lineAnchor = lineAnchor, lineHeight = lineHeight, lineWidth = lineWidth, margin = margin, marginBottom = marginBottom, marginLeft = marginLeft, marginRight = marginRight, marginTop = marginTop, marker = marker, markerEnd = markerEnd, markerMid = markerMid, markerStart = markerStart, mixBlendMode = mixBlendMode, monospace = monospace, opacity = opacity, paintOrder = paintOrder, pointerEvents = pointerEvents, reverse = reverse, rotate = rotate, select = select, shapeRendering = shapeRendering, sort = sort, stroke = stroke, strokeDasharray = strokeDasharray, strokeDashoffset = strokeDashoffset, strokeLinecap = strokeLinecap, strokeLinejoin = strokeLinejoin, strokeMiterlimit = strokeMiterlimit, strokeOpacity = strokeOpacity, strokeWidth = strokeWidth, target = target, text = text, textAnchor = textAnchor, textOverflow = textOverflow, textStroke = textStroke, textStrokeOpacity = textStrokeOpacity, textStrokeWidth = textStrokeWidth, tickFormat = tickFormat, tickPadding = tickPadding, tickRotate = tickRotate, tickSize = tickSize, tickSpacing = tickSpacing, ticks = ticks, tip = tip, title = title, x = x, y = y, z = z, ..., data_from = data_from, filter_by = filter_by)
}

#' The axisY mark.
#'
#' @param spec A plot fragment or `vgspec` to add this mark to, or `NULL` to start a new plot with just this mark.
#' @param anchor The side of the frame on which to place the axis: *top* or *bottom* for horizontal axes (axisX and axisFx) and their associated vertical grids (gridX and gridFx), or *left* or *right* for vertical axes (axisY and axisFY) and their associated horizontal grids (gridY and gridFy).
#' @param ariaDescription The aria-description; a constant textual description.
#' @param ariaHidden The aria-hidden state; a constant indicating whether the element is exposed to an accessibility API.
#' @param ariaLabel The aria-label; a channel specifying short textual labels representing the value in the accessibility tree.
#' @param channels Additional named channels, for example to include in a tooltip.
#' @param clip How to clip the mark; one of: - *frame* or true - clip to the plot’s frame (inner area) - *sphere* - clip to the projected sphere (*e.g.*, front hemisphere) - null or false - do not clip The *sphere* clip option requires a geographic projection.
#' @param color A shorthand for setting both **fill** and **stroke**; affects the stroke of tick vectors and grid rules, and the fill of tick texts and axis label texts; defaults to *currentColor*.
#' @param dx The horizontal offset in pixels; a constant option.
#' @param dy The vertical offset in pixels; a constant option.
#' @param facet Whether to enable or disable faceting; one of: - *auto* (default) - automatically determine if this mark should be faceted - *include* (or true) - draw the subset of the mark’s data in the current facet - *exclude* - draw the subset of the mark’s data *not* in the current facet - *super* - draw this mark in a single frame that covers all facets - null (or false) - repeat this mark’s data across all facets (*i.e.*, no faceting) When a mark uses *super* faceting, it is not allowed to use position scales (*x*, *y*, *fx*, or *fy*); *super* faceting is intended for decorations, such as labels and legends.
#' @param facetAnchor How to place the mark with respect to facets; one of: - null (default for most marks) - display the mark in each non-empty facet - *top*, *right*, *bottom*, or *left* - display the mark only in facets on the given side - *top-empty*, *right-empty*, *bottom-empty*, or *left-empty* (default for axis marks) - display the mark only in facets that have empty space on the given side: either the margin, or an empty facet - *empty* - display the mark in empty facets only
#' @param fill The fill; a constant CSS color string, or a channel typically bound to the *color* scale.
#' @param fillOpacity The fill-opacity; a constant number between 0 and 1, or a channel typically bound to the *opacity* scale.
#' @param filter Applies a transform to filter the mark’s index according to the given channel values; only truthy values are retained.
#' @param fontFamily The font-family; a constant; defaults to the plot’s font family, which is typically *system-ui*.
#' @param fontSize The font size in pixels; either a constant or a channel; defaults to the plot’s font size, which is typically 10.
#' @param fontStyle The font style; a constant; defaults to the plot’s font style, which is typically *normal*.
#' @param fontVariant The font variant; a constant; if the **text** channel contains numbers or dates, defaults to *tabular-nums* to facilitate comparing numbers; otherwise defaults to the plot’s font style, which is typically *normal*.
#' @param fontWeight The font weight; a constant; defaults to the plot’s font weight, which is typically *normal*.
#' @param frameAnchor The frame anchor specifies defaults for **x** and **y**, along with **textAnchor** and **lineAnchor**, based on the plot’s frame; it may be one of the four sides (*top*, *right*, *bottom*, *left*), one of the four corners (*top-left*, *top-right*, *bottom-right*, *bottom-left*), or the *middle* of the frame.
#' @param fx The horizontal facet position channel, for mark-level faceting, bound to the *fx* scale.
#' @param fy The vertical facet position channel, for mark-level faceting, bound to the *fy* scale.
#' @param href The href; a channel specifying URLs for clickable links.
#' @param imageFilter A CSS filter; a constant string used to adjust the rendering of images, such as *blur(5px)*.
#' @param inset Shorthand to set the same default for **insetStart** and **insetEnd**.
#' @param insetLeft Insets the left edge by the specified number of pixels.
#' @param insetRight Insets the right edge by the specified number of pixels.
#' @param interval Enforces uniformity for data at regular intervals, such as integer values or daily samples.
#' @param label A textual label to show on the axis or legend; if null, show no label.
#' @param labelAnchor Where to place the axis **label** relative to the plot’s frame.
#' @param labelArrow Whether to apply a directional arrow such as → or ↑ to the scale label.
#' @param labelOffset The axis **label** position offset (in pixels); default depends on margins and orientation.
#' @param lineAnchor The line anchor controls how text is aligned (typically vertically) relative to its anchor point; it is one of *top*, *bottom*, or *middle*.
#' @param lineHeight The line height in ems; defaults to 1.
#' @param lineWidth The line width in ems (e.g., 10 for about 20 characters); defaults to infinity, disabling wrapping and clipping.
#' @param margin Shorthand to set the same default for all four mark margins: **marginTop**, **marginRight**, **marginBottom**, and **marginLeft**; typically defaults to 0, except for axis marks.
#' @param marginBottom The mark’s bottom margin; the minimum distance in pixels between the bottom edges of the inner and outer plot area.
#' @param marginLeft The mark’s left margin; the minimum distance in pixels between the left edges of the inner and outer plot area.
#' @param marginRight The mark’s right margin; the minimum distance in pixels between the right edges of the mark’s inner and outer plot area.
#' @param marginTop The mark’s top margin; the minimum distance in pixels between the top edges of the inner and outer plot area.
#' @param marker Shorthand to set the same default for markerStart, markerMid, and markerEnd; one of: - a marker name such as *arrow* or *circle* - *none* (default) - no marker * true - alias for *circle-fill* * false or null - alias for *none*
#' @param markerEnd The marker for the ending point of a line segment; one of: - a marker name such as *arrow* or *circle* * *none* (default) - no marker * true - alias for *circle-fill* * false or null - alias for *none*
#' @param markerMid The marker for any middle (interior) points of a line segment.
#' @param markerStart The marker for the starting point of a line segment; one of: - a marker name such as *arrow* or *circle* * *none* (default) - no marker * true - alias for *circle-fill* * false or null - alias for *none*
#' @param mixBlendMode The mix-blend-mode; a constant string specifying how to blend content such as *multiply*.
#' @param monospace If true, changes the default **fontFamily** to *monospace*, and uses simplified monospaced text metrics calculations.
#' @param opacity The opacity; a constant between 0 and 1, or a channel typically bound to the *opacity* scale.
#' @param paintOrder The paint-order; a constant string specifying the order in which the **fill**, **stroke**, and any markers are drawn; defaults to *normal*, which draws the fill, then stroke, then markers; defaults to *stroke* for the text mark to create a “halo” around text to improve legibility.
#' @param pointerEvents The pointer-events property; a constant string such as *none*.
#' @param reverse Applies a transform to reverse the order of the mark’s index, say for reverse input order.
#' @param rotate The rotation angle in degrees clockwise; a constant or a channel; defaults to 0°.
#' @param select Applies a filter transform after data is loaded to highlight selected values only.
#' @param shapeRendering The shape-rendering; a constant string such as *crispEdges*.
#' @param sort Either applies a transform to sort the mark’s index by the specified channel values, or imputes ordinal scale domains from this mark’s channels.
#' @param stroke The stroke; a constant CSS color string, or a channel typically bound to the *color* scale.
#' @param strokeDasharray The stroke-dasharray; a constant number indicating the length in pixels of alternating dashes and gaps, or a constant string of numbers separated by spaces or commas (_e.g._, *10 2* for dashes of 10 pixels separated by gaps of 2 pixels), or *none* (the default) for no dashing : https://developer.mozilla.org/en-US/docs/Web/SVG/Attribute/stroke-dasharray
#' @param strokeDashoffset The stroke-dashoffset; a constant indicating the offset in pixels of the first dash along the stroke; defaults to zero.
#' @param strokeLinecap The stroke-linecap; a constant specifying how to cap stroked paths, such as *butt*, *round*, or *square*.
#' @param strokeLinejoin The stroke-linejoin; a constant specifying how to join stroked paths, such as *bevel*, *miter*, *miter-clip*, or *round*.
#' @param strokeMiterlimit The stroke-miterlimit; a constant number specifying how to limit the length of *miter* joins on stroked paths.
#' @param strokeOpacity The stroke-opacity; a constant between 0 and 1, or a channel typically bound to the *opacity* scale.
#' @param strokeWidth The stroke-width; a constant number in pixels, or a channel.
#' @param target The target; a constant string specifying the target window (_e.g._, *_blank*) for clickable links; used in conjunction with the **href** option.
#' @param text The text contents channel, possibly with line breaks (\\n, \\r\\n, or \\r).
#' @param textAnchor The text anchor controls how text is aligned (typically horizontally) relative to its anchor point; it is one of *start*, *end*, or *middle*.
#' @param textOverflow How truncate (or wrap) lines of text longer than the given **lineWidth**; one of: - null (default) - preserve overflowing characters (and wrap if needed) - *clip* or *clip-end* - remove characters from the end - *clip-start* - remove characters from the start - *ellipsis* or *ellipsis-end* - replace characters from the end with an ellipsis (…) - *ellipsis-start* - replace characters from the start with an ellipsis (…) - *ellipsis-middle* - replace characters from the middle with an ellipsis (…) If no **title** was specified, if text requires truncation, a title containing the non-truncated text will be implicitly added.
#' @param textStroke The tick text **stroke**, say for a *white* outline to improve legibility; defaults to null.
#' @param textStrokeOpacity The tick text **strokeOpacity**; defaults to 1; has no effect unless **textStroke** is set.
#' @param textStrokeWidth The tick text **strokeWidth**; defaults to 4; has no effect unless **textStroke** is set.
#' @param tickFormat How to format inputs (abstract values) for axis tick labels; one of: - a d3-format string for numeric scales - a d3-time-format string for temporal scales : https://d3js.org/d3-time : https://d3js.org/d3-time-format
#' @param tickPadding The distance between an axis tick mark and its associated text label (in pixels); often defaults to 3, but may be affected by **xTickSize** and **xTickRotate**.
#' @param tickRotate The rotation angle of axis tick labels in degrees clocksize; defaults to 0.
#' @param tickSize The length of axis tick marks in pixels; negative values extend in the opposite direction.
#' @param tickSpacing The desired approximate spacing between adjacent axis ticks, affecting the default **ticks**; defaults to 80 pixels for *x* and *fx*, and 35 pixels for *y* and *fy*.
#' @param ticks The desired approximate number of axis ticks, or an explicit array of tick values, or an interval such as *day* or *month*.
#' @param tip Whether to generate a tooltip for this mark, and any tip options.
#' @param title The title; a channel specifying accessible, short textual descriptions as strings (possibly with newlines).
#' @param x The horizontal position (or length) channel, typically bound to the *x* scale.
#' @param y The vertical position channel, typically bound to the *y* scale; defaults to the zero-based index of the data 0, 1, 2, ….
#' @param z An optional ordinal channel for grouping data into (possibly stacked) series to be drawn as separate areas; defaults to **fill** if a channel, or **stroke** if a channel.
#' @param ... Additional options or plot-level attributes.
#' @param data_from The name of the data source this mark reads from (see [vg_data()]).
#' @param filter_by A Param/Selection (e.g. from [param()]) to filter this mark's data by.
#' @family mark functions
#' @export
vg_axis_y <- function(spec = NULL, anchor = vg_unset, ariaDescription = vg_unset, ariaHidden = vg_unset, ariaLabel = vg_unset, channels = vg_unset, clip = vg_unset, color = vg_unset, dx = vg_unset, dy = vg_unset, facet = vg_unset, facetAnchor = vg_unset, fill = vg_unset, fillOpacity = vg_unset, filter = vg_unset, fontFamily = vg_unset, fontSize = vg_unset, fontStyle = vg_unset, fontVariant = vg_unset, fontWeight = vg_unset, frameAnchor = vg_unset, fx = vg_unset, fy = vg_unset, href = vg_unset, imageFilter = vg_unset, inset = vg_unset, insetLeft = vg_unset, insetRight = vg_unset, interval = vg_unset, label = vg_unset, labelAnchor = vg_unset, labelArrow = vg_unset, labelOffset = vg_unset, lineAnchor = vg_unset, lineHeight = vg_unset, lineWidth = vg_unset, margin = vg_unset, marginBottom = vg_unset, marginLeft = vg_unset, marginRight = vg_unset, marginTop = vg_unset, marker = vg_unset, markerEnd = vg_unset, markerMid = vg_unset, markerStart = vg_unset, mixBlendMode = vg_unset, monospace = vg_unset, opacity = vg_unset, paintOrder = vg_unset, pointerEvents = vg_unset, reverse = vg_unset, rotate = vg_unset, select = vg_unset, shapeRendering = vg_unset, sort = vg_unset, stroke = vg_unset, strokeDasharray = vg_unset, strokeDashoffset = vg_unset, strokeLinecap = vg_unset, strokeLinejoin = vg_unset, strokeMiterlimit = vg_unset, strokeOpacity = vg_unset, strokeWidth = vg_unset, target = vg_unset, text = vg_unset, textAnchor = vg_unset, textOverflow = vg_unset, textStroke = vg_unset, textStrokeOpacity = vg_unset, textStrokeWidth = vg_unset, tickFormat = vg_unset, tickPadding = vg_unset, tickRotate = vg_unset, tickSize = vg_unset, tickSpacing = vg_unset, ticks = vg_unset, tip = vg_unset, title = vg_unset, x = vg_unset, y = vg_unset, z = vg_unset, ..., data_from = vg_unset, filter_by = vg_unset) {
  vg_mark_(spec, "axisY", anchor = anchor, ariaDescription = ariaDescription, ariaHidden = ariaHidden, ariaLabel = ariaLabel, channels = channels, clip = clip, color = color, dx = dx, dy = dy, facet = facet, facetAnchor = facetAnchor, fill = fill, fillOpacity = fillOpacity, filter = filter, fontFamily = fontFamily, fontSize = fontSize, fontStyle = fontStyle, fontVariant = fontVariant, fontWeight = fontWeight, frameAnchor = frameAnchor, fx = fx, fy = fy, href = href, imageFilter = imageFilter, inset = inset, insetLeft = insetLeft, insetRight = insetRight, interval = interval, label = label, labelAnchor = labelAnchor, labelArrow = labelArrow, labelOffset = labelOffset, lineAnchor = lineAnchor, lineHeight = lineHeight, lineWidth = lineWidth, margin = margin, marginBottom = marginBottom, marginLeft = marginLeft, marginRight = marginRight, marginTop = marginTop, marker = marker, markerEnd = markerEnd, markerMid = markerMid, markerStart = markerStart, mixBlendMode = mixBlendMode, monospace = monospace, opacity = opacity, paintOrder = paintOrder, pointerEvents = pointerEvents, reverse = reverse, rotate = rotate, select = select, shapeRendering = shapeRendering, sort = sort, stroke = stroke, strokeDasharray = strokeDasharray, strokeDashoffset = strokeDashoffset, strokeLinecap = strokeLinecap, strokeLinejoin = strokeLinejoin, strokeMiterlimit = strokeMiterlimit, strokeOpacity = strokeOpacity, strokeWidth = strokeWidth, target = target, text = text, textAnchor = textAnchor, textOverflow = textOverflow, textStroke = textStroke, textStrokeOpacity = textStrokeOpacity, textStrokeWidth = textStrokeWidth, tickFormat = tickFormat, tickPadding = tickPadding, tickRotate = tickRotate, tickSize = tickSize, tickSpacing = tickSpacing, ticks = ticks, tip = tip, title = title, x = x, y = y, z = z, ..., data_from = data_from, filter_by = filter_by)
}

#' The barX mark.
#'
#' @param spec A plot fragment or `vgspec` to add this mark to, or `NULL` to start a new plot with just this mark.
#' @param ariaDescription The aria-description; a constant textual description.
#' @param ariaHidden The aria-hidden state; a constant indicating whether the element is exposed to an accessibility API.
#' @param ariaLabel The aria-label; a channel specifying short textual labels representing the value in the accessibility tree.
#' @param channels Additional named channels, for example to include in a tooltip.
#' @param clip How to clip the mark; one of: - *frame* or true - clip to the plot’s frame (inner area) - *sphere* - clip to the projected sphere (*e.g.*, front hemisphere) - null or false - do not clip The *sphere* clip option requires a geographic projection.
#' @param dx The horizontal offset in pixels; a constant option.
#' @param dy The vertical offset in pixels; a constant option.
#' @param facet Whether to enable or disable faceting; one of: - *auto* (default) - automatically determine if this mark should be faceted - *include* (or true) - draw the subset of the mark’s data in the current facet - *exclude* - draw the subset of the mark’s data *not* in the current facet - *super* - draw this mark in a single frame that covers all facets - null (or false) - repeat this mark’s data across all facets (*i.e.*, no faceting) When a mark uses *super* faceting, it is not allowed to use position scales (*x*, *y*, *fx*, or *fy*); *super* faceting is intended for decorations, such as labels and legends.
#' @param facetAnchor How to place the mark with respect to facets; one of: - null (default for most marks) - display the mark in each non-empty facet - *top*, *right*, *bottom*, or *left* - display the mark only in facets on the given side - *top-empty*, *right-empty*, *bottom-empty*, or *left-empty* (default for axis marks) - display the mark only in facets that have empty space on the given side: either the margin, or an empty facet - *empty* - display the mark in empty facets only
#' @param fill The fill; a constant CSS color string, or a channel typically bound to the *color* scale.
#' @param fillOpacity The fill-opacity; a constant number between 0 and 1, or a channel typically bound to the *opacity* scale.
#' @param filter Applies a transform to filter the mark’s index according to the given channel values; only truthy values are retained.
#' @param fx The horizontal facet position channel, for mark-level faceting, bound to the *fx* scale.
#' @param fy The vertical facet position channel, for mark-level faceting, bound to the *fy* scale.
#' @param href The href; a channel specifying URLs for clickable links.
#' @param imageFilter A CSS filter; a constant string used to adjust the rendering of images, such as *blur(5px)*.
#' @param inset Shorthand to set the same default for **insetStart** and **insetEnd**.
#' @param insetBottom Insets the bottom edge by the specified number of pixels.
#' @param insetLeft Insets the left edge by the specified number of pixels.
#' @param insetRight Insets the right edge by the specified number of pixels.
#' @param insetTop Insets the top edge by the specified number of pixels.
#' @param interval Enforces uniformity for data at regular intervals, such as integer values or daily samples.
#' @param margin Shorthand to set the same default for all four mark margins: **marginTop**, **marginRight**, **marginBottom**, and **marginLeft**; typically defaults to 0, except for axis marks.
#' @param marginBottom The mark’s bottom margin; the minimum distance in pixels between the bottom edges of the inner and outer plot area.
#' @param marginLeft The mark’s left margin; the minimum distance in pixels between the left edges of the inner and outer plot area.
#' @param marginRight The mark’s right margin; the minimum distance in pixels between the right edges of the mark’s inner and outer plot area.
#' @param marginTop The mark’s top margin; the minimum distance in pixels between the top edges of the inner and outer plot area.
#' @param mixBlendMode The mix-blend-mode; a constant string specifying how to blend content such as *multiply*.
#' @param offset After stacking, an optional **offset** can be applied to translate and scale stacks, say to produce a streamgraph; defaults to null for a zero baseline (**y** = 0 for stackY, and **x** = 0 for stackX).
#' @param opacity The opacity; a constant between 0 and 1, or a channel typically bound to the *opacity* scale.
#' @param order The order in which stacks are layered; one of: - null (default) for input order - a named stack order method such as *inside-out* or *sum* - a field name, for natural order of the corresponding values - a function of data, for natural order of the corresponding values - an array of explicit **z** values in the desired order If the *wiggle* **offset** is used, as for a streamgraph, the default changes to *inside-out*.
#' @param paintOrder The paint-order; a constant string specifying the order in which the **fill**, **stroke**, and any markers are drawn; defaults to *normal*, which draws the fill, then stroke, then markers; defaults to *stroke* for the text mark to create a “halo” around text to improve legibility.
#' @param pointerEvents The pointer-events property; a constant string such as *none*.
#' @param reverse Applies a transform to reverse the order of the mark’s index, say for reverse input order.
#' @param rx The rounded corner *x*-radius, either in pixels or as a percentage of the rect width.
#' @param ry The rounded corner *y*-radius, either in pixels or as a percentage of the rect height.
#' @param select Applies a filter transform after data is loaded to highlight selected values only.
#' @param shapeRendering The shape-rendering; a constant string such as *crispEdges*.
#' @param sort Either applies a transform to sort the mark’s index by the specified channel values, or imputes ordinal scale domains from this mark’s channels.
#' @param stroke The stroke; a constant CSS color string, or a channel typically bound to the *color* scale.
#' @param strokeDasharray The stroke-dasharray; a constant number indicating the length in pixels of alternating dashes and gaps, or a constant string of numbers separated by spaces or commas (_e.g._, *10 2* for dashes of 10 pixels separated by gaps of 2 pixels), or *none* (the default) for no dashing : https://developer.mozilla.org/en-US/docs/Web/SVG/Attribute/stroke-dasharray
#' @param strokeDashoffset The stroke-dashoffset; a constant indicating the offset in pixels of the first dash along the stroke; defaults to zero.
#' @param strokeLinecap The stroke-linecap; a constant specifying how to cap stroked paths, such as *butt*, *round*, or *square*.
#' @param strokeLinejoin The stroke-linejoin; a constant specifying how to join stroked paths, such as *bevel*, *miter*, *miter-clip*, or *round*.
#' @param strokeMiterlimit The stroke-miterlimit; a constant number specifying how to limit the length of *miter* joins on stroked paths.
#' @param strokeOpacity The stroke-opacity; a constant between 0 and 1, or a channel typically bound to the *opacity* scale.
#' @param strokeWidth The stroke-width; a constant number in pixels, or a channel.
#' @param target The target; a constant string specifying the target window (_e.g._, *_blank*) for clickable links; used in conjunction with the **href** option.
#' @param tip Whether to generate a tooltip for this mark, and any tip options.
#' @param title The title; a channel specifying accessible, short textual descriptions as strings (possibly with newlines).
#' @param x The horizontal position (or length) channel, typically bound to the *x* scale.
#' @param x1 The required primary (starting, often left) horizontal position channel, representing the area’s baseline, typically bound to the *x* scale.
#' @param x2 The optional secondary (ending, often right) horizontal position channel, representing the area’s topline, typically bound to the *x* scale; if not specified, **x1** is used.
#' @param y The vertical position channel, typically bound to the *y* scale; defaults to the zero-based index of the data 0, 1, 2, ….
#' @param z An optional ordinal channel for grouping data into (possibly stacked) series to be drawn as separate areas; defaults to **fill** if a channel, or **stroke** if a channel.
#' @param ... Additional options or plot-level attributes.
#' @param data_from The name of the data source this mark reads from (see [vg_data()]).
#' @param filter_by A Param/Selection (e.g. from [param()]) to filter this mark's data by.
#' @family mark functions
#' @export
vg_bar_x <- function(spec = NULL, ariaDescription = vg_unset, ariaHidden = vg_unset, ariaLabel = vg_unset, channels = vg_unset, clip = vg_unset, dx = vg_unset, dy = vg_unset, facet = vg_unset, facetAnchor = vg_unset, fill = vg_unset, fillOpacity = vg_unset, filter = vg_unset, fx = vg_unset, fy = vg_unset, href = vg_unset, imageFilter = vg_unset, inset = vg_unset, insetBottom = vg_unset, insetLeft = vg_unset, insetRight = vg_unset, insetTop = vg_unset, interval = vg_unset, margin = vg_unset, marginBottom = vg_unset, marginLeft = vg_unset, marginRight = vg_unset, marginTop = vg_unset, mixBlendMode = vg_unset, offset = vg_unset, opacity = vg_unset, order = vg_unset, paintOrder = vg_unset, pointerEvents = vg_unset, reverse = vg_unset, rx = vg_unset, ry = vg_unset, select = vg_unset, shapeRendering = vg_unset, sort = vg_unset, stroke = vg_unset, strokeDasharray = vg_unset, strokeDashoffset = vg_unset, strokeLinecap = vg_unset, strokeLinejoin = vg_unset, strokeMiterlimit = vg_unset, strokeOpacity = vg_unset, strokeWidth = vg_unset, target = vg_unset, tip = vg_unset, title = vg_unset, x = vg_unset, x1 = vg_unset, x2 = vg_unset, y = vg_unset, z = vg_unset, ..., data_from = vg_unset, filter_by = vg_unset) {
  vg_mark_(spec, "barX", ariaDescription = ariaDescription, ariaHidden = ariaHidden, ariaLabel = ariaLabel, channels = channels, clip = clip, dx = dx, dy = dy, facet = facet, facetAnchor = facetAnchor, fill = fill, fillOpacity = fillOpacity, filter = filter, fx = fx, fy = fy, href = href, imageFilter = imageFilter, inset = inset, insetBottom = insetBottom, insetLeft = insetLeft, insetRight = insetRight, insetTop = insetTop, interval = interval, margin = margin, marginBottom = marginBottom, marginLeft = marginLeft, marginRight = marginRight, marginTop = marginTop, mixBlendMode = mixBlendMode, offset = offset, opacity = opacity, order = order, paintOrder = paintOrder, pointerEvents = pointerEvents, reverse = reverse, rx = rx, ry = ry, select = select, shapeRendering = shapeRendering, sort = sort, stroke = stroke, strokeDasharray = strokeDasharray, strokeDashoffset = strokeDashoffset, strokeLinecap = strokeLinecap, strokeLinejoin = strokeLinejoin, strokeMiterlimit = strokeMiterlimit, strokeOpacity = strokeOpacity, strokeWidth = strokeWidth, target = target, tip = tip, title = title, x = x, x1 = x1, x2 = x2, y = y, z = z, ..., data_from = data_from, filter_by = filter_by)
}

#' The barY mark.
#'
#' @param spec A plot fragment or `vgspec` to add this mark to, or `NULL` to start a new plot with just this mark.
#' @param ariaDescription The aria-description; a constant textual description.
#' @param ariaHidden The aria-hidden state; a constant indicating whether the element is exposed to an accessibility API.
#' @param ariaLabel The aria-label; a channel specifying short textual labels representing the value in the accessibility tree.
#' @param channels Additional named channels, for example to include in a tooltip.
#' @param clip How to clip the mark; one of: - *frame* or true - clip to the plot’s frame (inner area) - *sphere* - clip to the projected sphere (*e.g.*, front hemisphere) - null or false - do not clip The *sphere* clip option requires a geographic projection.
#' @param dx The horizontal offset in pixels; a constant option.
#' @param dy The vertical offset in pixels; a constant option.
#' @param facet Whether to enable or disable faceting; one of: - *auto* (default) - automatically determine if this mark should be faceted - *include* (or true) - draw the subset of the mark’s data in the current facet - *exclude* - draw the subset of the mark’s data *not* in the current facet - *super* - draw this mark in a single frame that covers all facets - null (or false) - repeat this mark’s data across all facets (*i.e.*, no faceting) When a mark uses *super* faceting, it is not allowed to use position scales (*x*, *y*, *fx*, or *fy*); *super* faceting is intended for decorations, such as labels and legends.
#' @param facetAnchor How to place the mark with respect to facets; one of: - null (default for most marks) - display the mark in each non-empty facet - *top*, *right*, *bottom*, or *left* - display the mark only in facets on the given side - *top-empty*, *right-empty*, *bottom-empty*, or *left-empty* (default for axis marks) - display the mark only in facets that have empty space on the given side: either the margin, or an empty facet - *empty* - display the mark in empty facets only
#' @param fill The fill; a constant CSS color string, or a channel typically bound to the *color* scale.
#' @param fillOpacity The fill-opacity; a constant number between 0 and 1, or a channel typically bound to the *opacity* scale.
#' @param filter Applies a transform to filter the mark’s index according to the given channel values; only truthy values are retained.
#' @param fx The horizontal facet position channel, for mark-level faceting, bound to the *fx* scale.
#' @param fy The vertical facet position channel, for mark-level faceting, bound to the *fy* scale.
#' @param href The href; a channel specifying URLs for clickable links.
#' @param imageFilter A CSS filter; a constant string used to adjust the rendering of images, such as *blur(5px)*.
#' @param inset Shorthand to set the same default for **insetStart** and **insetEnd**.
#' @param insetBottom Insets the bottom edge by the specified number of pixels.
#' @param insetLeft Insets the left edge by the specified number of pixels.
#' @param insetRight Insets the right edge by the specified number of pixels.
#' @param insetTop Insets the top edge by the specified number of pixels.
#' @param interval Enforces uniformity for data at regular intervals, such as integer values or daily samples.
#' @param margin Shorthand to set the same default for all four mark margins: **marginTop**, **marginRight**, **marginBottom**, and **marginLeft**; typically defaults to 0, except for axis marks.
#' @param marginBottom The mark’s bottom margin; the minimum distance in pixels between the bottom edges of the inner and outer plot area.
#' @param marginLeft The mark’s left margin; the minimum distance in pixels between the left edges of the inner and outer plot area.
#' @param marginRight The mark’s right margin; the minimum distance in pixels between the right edges of the mark’s inner and outer plot area.
#' @param marginTop The mark’s top margin; the minimum distance in pixels between the top edges of the inner and outer plot area.
#' @param mixBlendMode The mix-blend-mode; a constant string specifying how to blend content such as *multiply*.
#' @param offset After stacking, an optional **offset** can be applied to translate and scale stacks, say to produce a streamgraph; defaults to null for a zero baseline (**y** = 0 for stackY, and **x** = 0 for stackX).
#' @param opacity The opacity; a constant between 0 and 1, or a channel typically bound to the *opacity* scale.
#' @param order The order in which stacks are layered; one of: - null (default) for input order - a named stack order method such as *inside-out* or *sum* - a field name, for natural order of the corresponding values - a function of data, for natural order of the corresponding values - an array of explicit **z** values in the desired order If the *wiggle* **offset** is used, as for a streamgraph, the default changes to *inside-out*.
#' @param paintOrder The paint-order; a constant string specifying the order in which the **fill**, **stroke**, and any markers are drawn; defaults to *normal*, which draws the fill, then stroke, then markers; defaults to *stroke* for the text mark to create a “halo” around text to improve legibility.
#' @param pointerEvents The pointer-events property; a constant string such as *none*.
#' @param reverse Applies a transform to reverse the order of the mark’s index, say for reverse input order.
#' @param rx The rounded corner *x*-radius, either in pixels or as a percentage of the rect width.
#' @param ry The rounded corner *y*-radius, either in pixels or as a percentage of the rect height.
#' @param select Applies a filter transform after data is loaded to highlight selected values only.
#' @param shapeRendering The shape-rendering; a constant string such as *crispEdges*.
#' @param sort Either applies a transform to sort the mark’s index by the specified channel values, or imputes ordinal scale domains from this mark’s channels.
#' @param stroke The stroke; a constant CSS color string, or a channel typically bound to the *color* scale.
#' @param strokeDasharray The stroke-dasharray; a constant number indicating the length in pixels of alternating dashes and gaps, or a constant string of numbers separated by spaces or commas (_e.g._, *10 2* for dashes of 10 pixels separated by gaps of 2 pixels), or *none* (the default) for no dashing : https://developer.mozilla.org/en-US/docs/Web/SVG/Attribute/stroke-dasharray
#' @param strokeDashoffset The stroke-dashoffset; a constant indicating the offset in pixels of the first dash along the stroke; defaults to zero.
#' @param strokeLinecap The stroke-linecap; a constant specifying how to cap stroked paths, such as *butt*, *round*, or *square*.
#' @param strokeLinejoin The stroke-linejoin; a constant specifying how to join stroked paths, such as *bevel*, *miter*, *miter-clip*, or *round*.
#' @param strokeMiterlimit The stroke-miterlimit; a constant number specifying how to limit the length of *miter* joins on stroked paths.
#' @param strokeOpacity The stroke-opacity; a constant between 0 and 1, or a channel typically bound to the *opacity* scale.
#' @param strokeWidth The stroke-width; a constant number in pixels, or a channel.
#' @param target The target; a constant string specifying the target window (_e.g._, *_blank*) for clickable links; used in conjunction with the **href** option.
#' @param tip Whether to generate a tooltip for this mark, and any tip options.
#' @param title The title; a channel specifying accessible, short textual descriptions as strings (possibly with newlines).
#' @param x The horizontal position (or length) channel, typically bound to the *x* scale.
#' @param y The vertical position channel, typically bound to the *y* scale; defaults to the zero-based index of the data 0, 1, 2, ….
#' @param y1 The required primary (starting, often bottom) vertical position channel, representing the area’s baseline, typically bound to the *y* scale.
#' @param y2 The optional secondary (ending, often top) vertical position channel, representing the area’s topline, typically bound to the *y* scale; if not specified, **y1** is used.
#' @param z An optional ordinal channel for grouping data into (possibly stacked) series to be drawn as separate areas; defaults to **fill** if a channel, or **stroke** if a channel.
#' @param ... Additional options or plot-level attributes.
#' @param data_from The name of the data source this mark reads from (see [vg_data()]).
#' @param filter_by A Param/Selection (e.g. from [param()]) to filter this mark's data by.
#' @family mark functions
#' @export
vg_bar_y <- function(spec = NULL, ariaDescription = vg_unset, ariaHidden = vg_unset, ariaLabel = vg_unset, channels = vg_unset, clip = vg_unset, dx = vg_unset, dy = vg_unset, facet = vg_unset, facetAnchor = vg_unset, fill = vg_unset, fillOpacity = vg_unset, filter = vg_unset, fx = vg_unset, fy = vg_unset, href = vg_unset, imageFilter = vg_unset, inset = vg_unset, insetBottom = vg_unset, insetLeft = vg_unset, insetRight = vg_unset, insetTop = vg_unset, interval = vg_unset, margin = vg_unset, marginBottom = vg_unset, marginLeft = vg_unset, marginRight = vg_unset, marginTop = vg_unset, mixBlendMode = vg_unset, offset = vg_unset, opacity = vg_unset, order = vg_unset, paintOrder = vg_unset, pointerEvents = vg_unset, reverse = vg_unset, rx = vg_unset, ry = vg_unset, select = vg_unset, shapeRendering = vg_unset, sort = vg_unset, stroke = vg_unset, strokeDasharray = vg_unset, strokeDashoffset = vg_unset, strokeLinecap = vg_unset, strokeLinejoin = vg_unset, strokeMiterlimit = vg_unset, strokeOpacity = vg_unset, strokeWidth = vg_unset, target = vg_unset, tip = vg_unset, title = vg_unset, x = vg_unset, y = vg_unset, y1 = vg_unset, y2 = vg_unset, z = vg_unset, ..., data_from = vg_unset, filter_by = vg_unset) {
  vg_mark_(spec, "barY", ariaDescription = ariaDescription, ariaHidden = ariaHidden, ariaLabel = ariaLabel, channels = channels, clip = clip, dx = dx, dy = dy, facet = facet, facetAnchor = facetAnchor, fill = fill, fillOpacity = fillOpacity, filter = filter, fx = fx, fy = fy, href = href, imageFilter = imageFilter, inset = inset, insetBottom = insetBottom, insetLeft = insetLeft, insetRight = insetRight, insetTop = insetTop, interval = interval, margin = margin, marginBottom = marginBottom, marginLeft = marginLeft, marginRight = marginRight, marginTop = marginTop, mixBlendMode = mixBlendMode, offset = offset, opacity = opacity, order = order, paintOrder = paintOrder, pointerEvents = pointerEvents, reverse = reverse, rx = rx, ry = ry, select = select, shapeRendering = shapeRendering, sort = sort, stroke = stroke, strokeDasharray = strokeDasharray, strokeDashoffset = strokeDashoffset, strokeLinecap = strokeLinecap, strokeLinejoin = strokeLinejoin, strokeMiterlimit = strokeMiterlimit, strokeOpacity = strokeOpacity, strokeWidth = strokeWidth, target = target, tip = tip, title = title, x = x, y = y, y1 = y1, y2 = y2, z = z, ..., data_from = data_from, filter_by = filter_by)
}

#' The cell mark.
#'
#' @param spec A plot fragment or `vgspec` to add this mark to, or `NULL` to start a new plot with just this mark.
#' @param ariaDescription The aria-description; a constant textual description.
#' @param ariaHidden The aria-hidden state; a constant indicating whether the element is exposed to an accessibility API.
#' @param ariaLabel The aria-label; a channel specifying short textual labels representing the value in the accessibility tree.
#' @param channels Additional named channels, for example to include in a tooltip.
#' @param clip How to clip the mark; one of: - *frame* or true - clip to the plot’s frame (inner area) - *sphere* - clip to the projected sphere (*e.g.*, front hemisphere) - null or false - do not clip The *sphere* clip option requires a geographic projection.
#' @param dx The horizontal offset in pixels; a constant option.
#' @param dy The vertical offset in pixels; a constant option.
#' @param facet Whether to enable or disable faceting; one of: - *auto* (default) - automatically determine if this mark should be faceted - *include* (or true) - draw the subset of the mark’s data in the current facet - *exclude* - draw the subset of the mark’s data *not* in the current facet - *super* - draw this mark in a single frame that covers all facets - null (or false) - repeat this mark’s data across all facets (*i.e.*, no faceting) When a mark uses *super* faceting, it is not allowed to use position scales (*x*, *y*, *fx*, or *fy*); *super* faceting is intended for decorations, such as labels and legends.
#' @param facetAnchor How to place the mark with respect to facets; one of: - null (default for most marks) - display the mark in each non-empty facet - *top*, *right*, *bottom*, or *left* - display the mark only in facets on the given side - *top-empty*, *right-empty*, *bottom-empty*, or *left-empty* (default for axis marks) - display the mark only in facets that have empty space on the given side: either the margin, or an empty facet - *empty* - display the mark in empty facets only
#' @param fill The fill; a constant CSS color string, or a channel typically bound to the *color* scale.
#' @param fillOpacity The fill-opacity; a constant number between 0 and 1, or a channel typically bound to the *opacity* scale.
#' @param filter Applies a transform to filter the mark’s index according to the given channel values; only truthy values are retained.
#' @param fx The horizontal facet position channel, for mark-level faceting, bound to the *fx* scale.
#' @param fy The vertical facet position channel, for mark-level faceting, bound to the *fy* scale.
#' @param href The href; a channel specifying URLs for clickable links.
#' @param imageFilter A CSS filter; a constant string used to adjust the rendering of images, such as *blur(5px)*.
#' @param inset Shorthand to set the same default for **insetStart** and **insetEnd**.
#' @param insetBottom Insets the bottom edge by the specified number of pixels.
#' @param insetLeft Insets the left edge by the specified number of pixels.
#' @param insetRight Insets the right edge by the specified number of pixels.
#' @param insetTop Insets the top edge by the specified number of pixels.
#' @param margin Shorthand to set the same default for all four mark margins: **marginTop**, **marginRight**, **marginBottom**, and **marginLeft**; typically defaults to 0, except for axis marks.
#' @param marginBottom The mark’s bottom margin; the minimum distance in pixels between the bottom edges of the inner and outer plot area.
#' @param marginLeft The mark’s left margin; the minimum distance in pixels between the left edges of the inner and outer plot area.
#' @param marginRight The mark’s right margin; the minimum distance in pixels between the right edges of the mark’s inner and outer plot area.
#' @param marginTop The mark’s top margin; the minimum distance in pixels between the top edges of the inner and outer plot area.
#' @param mixBlendMode The mix-blend-mode; a constant string specifying how to blend content such as *multiply*.
#' @param opacity The opacity; a constant between 0 and 1, or a channel typically bound to the *opacity* scale.
#' @param paintOrder The paint-order; a constant string specifying the order in which the **fill**, **stroke**, and any markers are drawn; defaults to *normal*, which draws the fill, then stroke, then markers; defaults to *stroke* for the text mark to create a “halo” around text to improve legibility.
#' @param pointerEvents The pointer-events property; a constant string such as *none*.
#' @param reverse Applies a transform to reverse the order of the mark’s index, say for reverse input order.
#' @param rx The rounded corner *x*-radius, either in pixels or as a percentage of the rect width.
#' @param ry The rounded corner *y*-radius, either in pixels or as a percentage of the rect height.
#' @param select Applies a filter transform after data is loaded to highlight selected values only.
#' @param shapeRendering The shape-rendering; a constant string such as *crispEdges*.
#' @param sort Either applies a transform to sort the mark’s index by the specified channel values, or imputes ordinal scale domains from this mark’s channels.
#' @param stroke The stroke; a constant CSS color string, or a channel typically bound to the *color* scale.
#' @param strokeDasharray The stroke-dasharray; a constant number indicating the length in pixels of alternating dashes and gaps, or a constant string of numbers separated by spaces or commas (_e.g._, *10 2* for dashes of 10 pixels separated by gaps of 2 pixels), or *none* (the default) for no dashing : https://developer.mozilla.org/en-US/docs/Web/SVG/Attribute/stroke-dasharray
#' @param strokeDashoffset The stroke-dashoffset; a constant indicating the offset in pixels of the first dash along the stroke; defaults to zero.
#' @param strokeLinecap The stroke-linecap; a constant specifying how to cap stroked paths, such as *butt*, *round*, or *square*.
#' @param strokeLinejoin The stroke-linejoin; a constant specifying how to join stroked paths, such as *bevel*, *miter*, *miter-clip*, or *round*.
#' @param strokeMiterlimit The stroke-miterlimit; a constant number specifying how to limit the length of *miter* joins on stroked paths.
#' @param strokeOpacity The stroke-opacity; a constant between 0 and 1, or a channel typically bound to the *opacity* scale.
#' @param strokeWidth The stroke-width; a constant number in pixels, or a channel.
#' @param target The target; a constant string specifying the target window (_e.g._, *_blank*) for clickable links; used in conjunction with the **href** option.
#' @param tip Whether to generate a tooltip for this mark, and any tip options.
#' @param title The title; a channel specifying accessible, short textual descriptions as strings (possibly with newlines).
#' @param x The horizontal position (or length) channel, typically bound to the *x* scale.
#' @param y The vertical position channel, typically bound to the *y* scale; defaults to the zero-based index of the data 0, 1, 2, ….
#' @param ... Additional options or plot-level attributes.
#' @param data_from The name of the data source this mark reads from (see [vg_data()]).
#' @param filter_by A Param/Selection (e.g. from [param()]) to filter this mark's data by.
#' @family mark functions
#' @export
vg_cell <- function(spec = NULL, ariaDescription = vg_unset, ariaHidden = vg_unset, ariaLabel = vg_unset, channels = vg_unset, clip = vg_unset, dx = vg_unset, dy = vg_unset, facet = vg_unset, facetAnchor = vg_unset, fill = vg_unset, fillOpacity = vg_unset, filter = vg_unset, fx = vg_unset, fy = vg_unset, href = vg_unset, imageFilter = vg_unset, inset = vg_unset, insetBottom = vg_unset, insetLeft = vg_unset, insetRight = vg_unset, insetTop = vg_unset, margin = vg_unset, marginBottom = vg_unset, marginLeft = vg_unset, marginRight = vg_unset, marginTop = vg_unset, mixBlendMode = vg_unset, opacity = vg_unset, paintOrder = vg_unset, pointerEvents = vg_unset, reverse = vg_unset, rx = vg_unset, ry = vg_unset, select = vg_unset, shapeRendering = vg_unset, sort = vg_unset, stroke = vg_unset, strokeDasharray = vg_unset, strokeDashoffset = vg_unset, strokeLinecap = vg_unset, strokeLinejoin = vg_unset, strokeMiterlimit = vg_unset, strokeOpacity = vg_unset, strokeWidth = vg_unset, target = vg_unset, tip = vg_unset, title = vg_unset, x = vg_unset, y = vg_unset, ..., data_from = vg_unset, filter_by = vg_unset) {
  vg_mark_(spec, "cell", ariaDescription = ariaDescription, ariaHidden = ariaHidden, ariaLabel = ariaLabel, channels = channels, clip = clip, dx = dx, dy = dy, facet = facet, facetAnchor = facetAnchor, fill = fill, fillOpacity = fillOpacity, filter = filter, fx = fx, fy = fy, href = href, imageFilter = imageFilter, inset = inset, insetBottom = insetBottom, insetLeft = insetLeft, insetRight = insetRight, insetTop = insetTop, margin = margin, marginBottom = marginBottom, marginLeft = marginLeft, marginRight = marginRight, marginTop = marginTop, mixBlendMode = mixBlendMode, opacity = opacity, paintOrder = paintOrder, pointerEvents = pointerEvents, reverse = reverse, rx = rx, ry = ry, select = select, shapeRendering = shapeRendering, sort = sort, stroke = stroke, strokeDasharray = strokeDasharray, strokeDashoffset = strokeDashoffset, strokeLinecap = strokeLinecap, strokeLinejoin = strokeLinejoin, strokeMiterlimit = strokeMiterlimit, strokeOpacity = strokeOpacity, strokeWidth = strokeWidth, target = target, tip = tip, title = title, x = x, y = y, ..., data_from = data_from, filter_by = filter_by)
}

#' The cellX mark.
#'
#' @param spec A plot fragment or `vgspec` to add this mark to, or `NULL` to start a new plot with just this mark.
#' @param ariaDescription The aria-description; a constant textual description.
#' @param ariaHidden The aria-hidden state; a constant indicating whether the element is exposed to an accessibility API.
#' @param ariaLabel The aria-label; a channel specifying short textual labels representing the value in the accessibility tree.
#' @param channels Additional named channels, for example to include in a tooltip.
#' @param clip How to clip the mark; one of: - *frame* or true - clip to the plot’s frame (inner area) - *sphere* - clip to the projected sphere (*e.g.*, front hemisphere) - null or false - do not clip The *sphere* clip option requires a geographic projection.
#' @param dx The horizontal offset in pixels; a constant option.
#' @param dy The vertical offset in pixels; a constant option.
#' @param facet Whether to enable or disable faceting; one of: - *auto* (default) - automatically determine if this mark should be faceted - *include* (or true) - draw the subset of the mark’s data in the current facet - *exclude* - draw the subset of the mark’s data *not* in the current facet - *super* - draw this mark in a single frame that covers all facets - null (or false) - repeat this mark’s data across all facets (*i.e.*, no faceting) When a mark uses *super* faceting, it is not allowed to use position scales (*x*, *y*, *fx*, or *fy*); *super* faceting is intended for decorations, such as labels and legends.
#' @param facetAnchor How to place the mark with respect to facets; one of: - null (default for most marks) - display the mark in each non-empty facet - *top*, *right*, *bottom*, or *left* - display the mark only in facets on the given side - *top-empty*, *right-empty*, *bottom-empty*, or *left-empty* (default for axis marks) - display the mark only in facets that have empty space on the given side: either the margin, or an empty facet - *empty* - display the mark in empty facets only
#' @param fill The fill; a constant CSS color string, or a channel typically bound to the *color* scale.
#' @param fillOpacity The fill-opacity; a constant number between 0 and 1, or a channel typically bound to the *opacity* scale.
#' @param filter Applies a transform to filter the mark’s index according to the given channel values; only truthy values are retained.
#' @param fx The horizontal facet position channel, for mark-level faceting, bound to the *fx* scale.
#' @param fy The vertical facet position channel, for mark-level faceting, bound to the *fy* scale.
#' @param href The href; a channel specifying URLs for clickable links.
#' @param imageFilter A CSS filter; a constant string used to adjust the rendering of images, such as *blur(5px)*.
#' @param inset Shorthand to set the same default for **insetStart** and **insetEnd**.
#' @param insetBottom Insets the bottom edge by the specified number of pixels.
#' @param insetLeft Insets the left edge by the specified number of pixels.
#' @param insetRight Insets the right edge by the specified number of pixels.
#' @param insetTop Insets the top edge by the specified number of pixels.
#' @param margin Shorthand to set the same default for all four mark margins: **marginTop**, **marginRight**, **marginBottom**, and **marginLeft**; typically defaults to 0, except for axis marks.
#' @param marginBottom The mark’s bottom margin; the minimum distance in pixels between the bottom edges of the inner and outer plot area.
#' @param marginLeft The mark’s left margin; the minimum distance in pixels between the left edges of the inner and outer plot area.
#' @param marginRight The mark’s right margin; the minimum distance in pixels between the right edges of the mark’s inner and outer plot area.
#' @param marginTop The mark’s top margin; the minimum distance in pixels between the top edges of the inner and outer plot area.
#' @param mixBlendMode The mix-blend-mode; a constant string specifying how to blend content such as *multiply*.
#' @param opacity The opacity; a constant between 0 and 1, or a channel typically bound to the *opacity* scale.
#' @param paintOrder The paint-order; a constant string specifying the order in which the **fill**, **stroke**, and any markers are drawn; defaults to *normal*, which draws the fill, then stroke, then markers; defaults to *stroke* for the text mark to create a “halo” around text to improve legibility.
#' @param pointerEvents The pointer-events property; a constant string such as *none*.
#' @param reverse Applies a transform to reverse the order of the mark’s index, say for reverse input order.
#' @param rx The rounded corner *x*-radius, either in pixels or as a percentage of the rect width.
#' @param ry The rounded corner *y*-radius, either in pixels or as a percentage of the rect height.
#' @param select Applies a filter transform after data is loaded to highlight selected values only.
#' @param shapeRendering The shape-rendering; a constant string such as *crispEdges*.
#' @param sort Either applies a transform to sort the mark’s index by the specified channel values, or imputes ordinal scale domains from this mark’s channels.
#' @param stroke The stroke; a constant CSS color string, or a channel typically bound to the *color* scale.
#' @param strokeDasharray The stroke-dasharray; a constant number indicating the length in pixels of alternating dashes and gaps, or a constant string of numbers separated by spaces or commas (_e.g._, *10 2* for dashes of 10 pixels separated by gaps of 2 pixels), or *none* (the default) for no dashing : https://developer.mozilla.org/en-US/docs/Web/SVG/Attribute/stroke-dasharray
#' @param strokeDashoffset The stroke-dashoffset; a constant indicating the offset in pixels of the first dash along the stroke; defaults to zero.
#' @param strokeLinecap The stroke-linecap; a constant specifying how to cap stroked paths, such as *butt*, *round*, or *square*.
#' @param strokeLinejoin The stroke-linejoin; a constant specifying how to join stroked paths, such as *bevel*, *miter*, *miter-clip*, or *round*.
#' @param strokeMiterlimit The stroke-miterlimit; a constant number specifying how to limit the length of *miter* joins on stroked paths.
#' @param strokeOpacity The stroke-opacity; a constant between 0 and 1, or a channel typically bound to the *opacity* scale.
#' @param strokeWidth The stroke-width; a constant number in pixels, or a channel.
#' @param target The target; a constant string specifying the target window (_e.g._, *_blank*) for clickable links; used in conjunction with the **href** option.
#' @param tip Whether to generate a tooltip for this mark, and any tip options.
#' @param title The title; a channel specifying accessible, short textual descriptions as strings (possibly with newlines).
#' @param x The horizontal position (or length) channel, typically bound to the *x* scale.
#' @param y The vertical position channel, typically bound to the *y* scale; defaults to the zero-based index of the data 0, 1, 2, ….
#' @param ... Additional options or plot-level attributes.
#' @param data_from The name of the data source this mark reads from (see [vg_data()]).
#' @param filter_by A Param/Selection (e.g. from [param()]) to filter this mark's data by.
#' @family mark functions
#' @export
vg_cell_x <- function(spec = NULL, ariaDescription = vg_unset, ariaHidden = vg_unset, ariaLabel = vg_unset, channels = vg_unset, clip = vg_unset, dx = vg_unset, dy = vg_unset, facet = vg_unset, facetAnchor = vg_unset, fill = vg_unset, fillOpacity = vg_unset, filter = vg_unset, fx = vg_unset, fy = vg_unset, href = vg_unset, imageFilter = vg_unset, inset = vg_unset, insetBottom = vg_unset, insetLeft = vg_unset, insetRight = vg_unset, insetTop = vg_unset, margin = vg_unset, marginBottom = vg_unset, marginLeft = vg_unset, marginRight = vg_unset, marginTop = vg_unset, mixBlendMode = vg_unset, opacity = vg_unset, paintOrder = vg_unset, pointerEvents = vg_unset, reverse = vg_unset, rx = vg_unset, ry = vg_unset, select = vg_unset, shapeRendering = vg_unset, sort = vg_unset, stroke = vg_unset, strokeDasharray = vg_unset, strokeDashoffset = vg_unset, strokeLinecap = vg_unset, strokeLinejoin = vg_unset, strokeMiterlimit = vg_unset, strokeOpacity = vg_unset, strokeWidth = vg_unset, target = vg_unset, tip = vg_unset, title = vg_unset, x = vg_unset, y = vg_unset, ..., data_from = vg_unset, filter_by = vg_unset) {
  vg_mark_(spec, "cellX", ariaDescription = ariaDescription, ariaHidden = ariaHidden, ariaLabel = ariaLabel, channels = channels, clip = clip, dx = dx, dy = dy, facet = facet, facetAnchor = facetAnchor, fill = fill, fillOpacity = fillOpacity, filter = filter, fx = fx, fy = fy, href = href, imageFilter = imageFilter, inset = inset, insetBottom = insetBottom, insetLeft = insetLeft, insetRight = insetRight, insetTop = insetTop, margin = margin, marginBottom = marginBottom, marginLeft = marginLeft, marginRight = marginRight, marginTop = marginTop, mixBlendMode = mixBlendMode, opacity = opacity, paintOrder = paintOrder, pointerEvents = pointerEvents, reverse = reverse, rx = rx, ry = ry, select = select, shapeRendering = shapeRendering, sort = sort, stroke = stroke, strokeDasharray = strokeDasharray, strokeDashoffset = strokeDashoffset, strokeLinecap = strokeLinecap, strokeLinejoin = strokeLinejoin, strokeMiterlimit = strokeMiterlimit, strokeOpacity = strokeOpacity, strokeWidth = strokeWidth, target = target, tip = tip, title = title, x = x, y = y, ..., data_from = data_from, filter_by = filter_by)
}

#' The cellY mark.
#'
#' @param spec A plot fragment or `vgspec` to add this mark to, or `NULL` to start a new plot with just this mark.
#' @param ariaDescription The aria-description; a constant textual description.
#' @param ariaHidden The aria-hidden state; a constant indicating whether the element is exposed to an accessibility API.
#' @param ariaLabel The aria-label; a channel specifying short textual labels representing the value in the accessibility tree.
#' @param channels Additional named channels, for example to include in a tooltip.
#' @param clip How to clip the mark; one of: - *frame* or true - clip to the plot’s frame (inner area) - *sphere* - clip to the projected sphere (*e.g.*, front hemisphere) - null or false - do not clip The *sphere* clip option requires a geographic projection.
#' @param dx The horizontal offset in pixels; a constant option.
#' @param dy The vertical offset in pixels; a constant option.
#' @param facet Whether to enable or disable faceting; one of: - *auto* (default) - automatically determine if this mark should be faceted - *include* (or true) - draw the subset of the mark’s data in the current facet - *exclude* - draw the subset of the mark’s data *not* in the current facet - *super* - draw this mark in a single frame that covers all facets - null (or false) - repeat this mark’s data across all facets (*i.e.*, no faceting) When a mark uses *super* faceting, it is not allowed to use position scales (*x*, *y*, *fx*, or *fy*); *super* faceting is intended for decorations, such as labels and legends.
#' @param facetAnchor How to place the mark with respect to facets; one of: - null (default for most marks) - display the mark in each non-empty facet - *top*, *right*, *bottom*, or *left* - display the mark only in facets on the given side - *top-empty*, *right-empty*, *bottom-empty*, or *left-empty* (default for axis marks) - display the mark only in facets that have empty space on the given side: either the margin, or an empty facet - *empty* - display the mark in empty facets only
#' @param fill The fill; a constant CSS color string, or a channel typically bound to the *color* scale.
#' @param fillOpacity The fill-opacity; a constant number between 0 and 1, or a channel typically bound to the *opacity* scale.
#' @param filter Applies a transform to filter the mark’s index according to the given channel values; only truthy values are retained.
#' @param fx The horizontal facet position channel, for mark-level faceting, bound to the *fx* scale.
#' @param fy The vertical facet position channel, for mark-level faceting, bound to the *fy* scale.
#' @param href The href; a channel specifying URLs for clickable links.
#' @param imageFilter A CSS filter; a constant string used to adjust the rendering of images, such as *blur(5px)*.
#' @param inset Shorthand to set the same default for **insetStart** and **insetEnd**.
#' @param insetBottom Insets the bottom edge by the specified number of pixels.
#' @param insetLeft Insets the left edge by the specified number of pixels.
#' @param insetRight Insets the right edge by the specified number of pixels.
#' @param insetTop Insets the top edge by the specified number of pixels.
#' @param margin Shorthand to set the same default for all four mark margins: **marginTop**, **marginRight**, **marginBottom**, and **marginLeft**; typically defaults to 0, except for axis marks.
#' @param marginBottom The mark’s bottom margin; the minimum distance in pixels between the bottom edges of the inner and outer plot area.
#' @param marginLeft The mark’s left margin; the minimum distance in pixels between the left edges of the inner and outer plot area.
#' @param marginRight The mark’s right margin; the minimum distance in pixels between the right edges of the mark’s inner and outer plot area.
#' @param marginTop The mark’s top margin; the minimum distance in pixels between the top edges of the inner and outer plot area.
#' @param mixBlendMode The mix-blend-mode; a constant string specifying how to blend content such as *multiply*.
#' @param opacity The opacity; a constant between 0 and 1, or a channel typically bound to the *opacity* scale.
#' @param paintOrder The paint-order; a constant string specifying the order in which the **fill**, **stroke**, and any markers are drawn; defaults to *normal*, which draws the fill, then stroke, then markers; defaults to *stroke* for the text mark to create a “halo” around text to improve legibility.
#' @param pointerEvents The pointer-events property; a constant string such as *none*.
#' @param reverse Applies a transform to reverse the order of the mark’s index, say for reverse input order.
#' @param rx The rounded corner *x*-radius, either in pixels or as a percentage of the rect width.
#' @param ry The rounded corner *y*-radius, either in pixels or as a percentage of the rect height.
#' @param select Applies a filter transform after data is loaded to highlight selected values only.
#' @param shapeRendering The shape-rendering; a constant string such as *crispEdges*.
#' @param sort Either applies a transform to sort the mark’s index by the specified channel values, or imputes ordinal scale domains from this mark’s channels.
#' @param stroke The stroke; a constant CSS color string, or a channel typically bound to the *color* scale.
#' @param strokeDasharray The stroke-dasharray; a constant number indicating the length in pixels of alternating dashes and gaps, or a constant string of numbers separated by spaces or commas (_e.g._, *10 2* for dashes of 10 pixels separated by gaps of 2 pixels), or *none* (the default) for no dashing : https://developer.mozilla.org/en-US/docs/Web/SVG/Attribute/stroke-dasharray
#' @param strokeDashoffset The stroke-dashoffset; a constant indicating the offset in pixels of the first dash along the stroke; defaults to zero.
#' @param strokeLinecap The stroke-linecap; a constant specifying how to cap stroked paths, such as *butt*, *round*, or *square*.
#' @param strokeLinejoin The stroke-linejoin; a constant specifying how to join stroked paths, such as *bevel*, *miter*, *miter-clip*, or *round*.
#' @param strokeMiterlimit The stroke-miterlimit; a constant number specifying how to limit the length of *miter* joins on stroked paths.
#' @param strokeOpacity The stroke-opacity; a constant between 0 and 1, or a channel typically bound to the *opacity* scale.
#' @param strokeWidth The stroke-width; a constant number in pixels, or a channel.
#' @param target The target; a constant string specifying the target window (_e.g._, *_blank*) for clickable links; used in conjunction with the **href** option.
#' @param tip Whether to generate a tooltip for this mark, and any tip options.
#' @param title The title; a channel specifying accessible, short textual descriptions as strings (possibly with newlines).
#' @param x The horizontal position (or length) channel, typically bound to the *x* scale.
#' @param y The vertical position channel, typically bound to the *y* scale; defaults to the zero-based index of the data 0, 1, 2, ….
#' @param ... Additional options or plot-level attributes.
#' @param data_from The name of the data source this mark reads from (see [vg_data()]).
#' @param filter_by A Param/Selection (e.g. from [param()]) to filter this mark's data by.
#' @family mark functions
#' @export
vg_cell_y <- function(spec = NULL, ariaDescription = vg_unset, ariaHidden = vg_unset, ariaLabel = vg_unset, channels = vg_unset, clip = vg_unset, dx = vg_unset, dy = vg_unset, facet = vg_unset, facetAnchor = vg_unset, fill = vg_unset, fillOpacity = vg_unset, filter = vg_unset, fx = vg_unset, fy = vg_unset, href = vg_unset, imageFilter = vg_unset, inset = vg_unset, insetBottom = vg_unset, insetLeft = vg_unset, insetRight = vg_unset, insetTop = vg_unset, margin = vg_unset, marginBottom = vg_unset, marginLeft = vg_unset, marginRight = vg_unset, marginTop = vg_unset, mixBlendMode = vg_unset, opacity = vg_unset, paintOrder = vg_unset, pointerEvents = vg_unset, reverse = vg_unset, rx = vg_unset, ry = vg_unset, select = vg_unset, shapeRendering = vg_unset, sort = vg_unset, stroke = vg_unset, strokeDasharray = vg_unset, strokeDashoffset = vg_unset, strokeLinecap = vg_unset, strokeLinejoin = vg_unset, strokeMiterlimit = vg_unset, strokeOpacity = vg_unset, strokeWidth = vg_unset, target = vg_unset, tip = vg_unset, title = vg_unset, x = vg_unset, y = vg_unset, ..., data_from = vg_unset, filter_by = vg_unset) {
  vg_mark_(spec, "cellY", ariaDescription = ariaDescription, ariaHidden = ariaHidden, ariaLabel = ariaLabel, channels = channels, clip = clip, dx = dx, dy = dy, facet = facet, facetAnchor = facetAnchor, fill = fill, fillOpacity = fillOpacity, filter = filter, fx = fx, fy = fy, href = href, imageFilter = imageFilter, inset = inset, insetBottom = insetBottom, insetLeft = insetLeft, insetRight = insetRight, insetTop = insetTop, margin = margin, marginBottom = marginBottom, marginLeft = marginLeft, marginRight = marginRight, marginTop = marginTop, mixBlendMode = mixBlendMode, opacity = opacity, paintOrder = paintOrder, pointerEvents = pointerEvents, reverse = reverse, rx = rx, ry = ry, select = select, shapeRendering = shapeRendering, sort = sort, stroke = stroke, strokeDasharray = strokeDasharray, strokeDashoffset = strokeDashoffset, strokeLinecap = strokeLinecap, strokeLinejoin = strokeLinejoin, strokeMiterlimit = strokeMiterlimit, strokeOpacity = strokeOpacity, strokeWidth = strokeWidth, target = target, tip = tip, title = title, x = x, y = y, ..., data_from = data_from, filter_by = filter_by)
}

#' The circle mark.
#'
#' @param spec A plot fragment or `vgspec` to add this mark to, or `NULL` to start a new plot with just this mark.
#' @param ariaDescription The aria-description; a constant textual description.
#' @param ariaHidden The aria-hidden state; a constant indicating whether the element is exposed to an accessibility API.
#' @param ariaLabel The aria-label; a channel specifying short textual labels representing the value in the accessibility tree.
#' @param channels Additional named channels, for example to include in a tooltip.
#' @param clip How to clip the mark; one of: - *frame* or true - clip to the plot’s frame (inner area) - *sphere* - clip to the projected sphere (*e.g.*, front hemisphere) - null or false - do not clip The *sphere* clip option requires a geographic projection.
#' @param dx The horizontal offset in pixels; a constant option.
#' @param dy The vertical offset in pixels; a constant option.
#' @param facet Whether to enable or disable faceting; one of: - *auto* (default) - automatically determine if this mark should be faceted - *include* (or true) - draw the subset of the mark’s data in the current facet - *exclude* - draw the subset of the mark’s data *not* in the current facet - *super* - draw this mark in a single frame that covers all facets - null (or false) - repeat this mark’s data across all facets (*i.e.*, no faceting) When a mark uses *super* faceting, it is not allowed to use position scales (*x*, *y*, *fx*, or *fy*); *super* faceting is intended for decorations, such as labels and legends.
#' @param facetAnchor How to place the mark with respect to facets; one of: - null (default for most marks) - display the mark in each non-empty facet - *top*, *right*, *bottom*, or *left* - display the mark only in facets on the given side - *top-empty*, *right-empty*, *bottom-empty*, or *left-empty* (default for axis marks) - display the mark only in facets that have empty space on the given side: either the margin, or an empty facet - *empty* - display the mark in empty facets only
#' @param fill The fill; a constant CSS color string, or a channel typically bound to the *color* scale.
#' @param fillOpacity The fill-opacity; a constant number between 0 and 1, or a channel typically bound to the *opacity* scale.
#' @param filter Applies a transform to filter the mark’s index according to the given channel values; only truthy values are retained.
#' @param frameAnchor The frame anchor specifies defaults for **x** and **y**, along with **textAnchor** and **lineAnchor**, based on the plot’s frame; it may be one of the four sides (*top*, *right*, *bottom*, *left*), one of the four corners (*top-left*, *top-right*, *bottom-right*, *bottom-left*), or the *middle* of the frame.
#' @param fx The horizontal facet position channel, for mark-level faceting, bound to the *fx* scale.
#' @param fy The vertical facet position channel, for mark-level faceting, bound to the *fy* scale.
#' @param href The href; a channel specifying URLs for clickable links.
#' @param imageFilter A CSS filter; a constant string used to adjust the rendering of images, such as *blur(5px)*.
#' @param margin Shorthand to set the same default for all four mark margins: **marginTop**, **marginRight**, **marginBottom**, and **marginLeft**; typically defaults to 0, except for axis marks.
#' @param marginBottom The mark’s bottom margin; the minimum distance in pixels between the bottom edges of the inner and outer plot area.
#' @param marginLeft The mark’s left margin; the minimum distance in pixels between the left edges of the inner and outer plot area.
#' @param marginRight The mark’s right margin; the minimum distance in pixels between the right edges of the mark’s inner and outer plot area.
#' @param marginTop The mark’s top margin; the minimum distance in pixels between the top edges of the inner and outer plot area.
#' @param mixBlendMode The mix-blend-mode; a constant string specifying how to blend content such as *multiply*.
#' @param opacity The opacity; a constant between 0 and 1, or a channel typically bound to the *opacity* scale.
#' @param paintOrder The paint-order; a constant string specifying the order in which the **fill**, **stroke**, and any markers are drawn; defaults to *normal*, which draws the fill, then stroke, then markers; defaults to *stroke* for the text mark to create a “halo” around text to improve legibility.
#' @param pointerEvents The pointer-events property; a constant string such as *none*.
#' @param r The radius of dots; either a channel or constant.
#' @param reverse Applies a transform to reverse the order of the mark’s index, say for reverse input order.
#' @param rotate The rotation angle in degrees clockwise; a constant or a channel; defaults to 0°.
#' @param select Applies a filter transform after data is loaded to highlight selected values only.
#' @param shapeRendering The shape-rendering; a constant string such as *crispEdges*.
#' @param sort Either applies a transform to sort the mark’s index by the specified channel values, or imputes ordinal scale domains from this mark’s channels.
#' @param stroke The stroke; a constant CSS color string, or a channel typically bound to the *color* scale.
#' @param strokeDasharray The stroke-dasharray; a constant number indicating the length in pixels of alternating dashes and gaps, or a constant string of numbers separated by spaces or commas (_e.g._, *10 2* for dashes of 10 pixels separated by gaps of 2 pixels), or *none* (the default) for no dashing : https://developer.mozilla.org/en-US/docs/Web/SVG/Attribute/stroke-dasharray
#' @param strokeDashoffset The stroke-dashoffset; a constant indicating the offset in pixels of the first dash along the stroke; defaults to zero.
#' @param strokeLinecap The stroke-linecap; a constant specifying how to cap stroked paths, such as *butt*, *round*, or *square*.
#' @param strokeLinejoin The stroke-linejoin; a constant specifying how to join stroked paths, such as *bevel*, *miter*, *miter-clip*, or *round*.
#' @param strokeMiterlimit The stroke-miterlimit; a constant number specifying how to limit the length of *miter* joins on stroked paths.
#' @param strokeOpacity The stroke-opacity; a constant between 0 and 1, or a channel typically bound to the *opacity* scale.
#' @param strokeWidth The stroke-width; a constant number in pixels, or a channel.
#' @param symbol The categorical symbol; either a channel or a constant.
#' @param target The target; a constant string specifying the target window (_e.g._, *_blank*) for clickable links; used in conjunction with the **href** option.
#' @param tip Whether to generate a tooltip for this mark, and any tip options.
#' @param title The title; a channel specifying accessible, short textual descriptions as strings (possibly with newlines).
#' @param x The horizontal position (or length) channel, typically bound to the *x* scale.
#' @param y The vertical position channel, typically bound to the *y* scale; defaults to the zero-based index of the data 0, 1, 2, ….
#' @param z An optional ordinal channel for grouping data into (possibly stacked) series to be drawn as separate areas; defaults to **fill** if a channel, or **stroke** if a channel.
#' @param ... Additional options or plot-level attributes.
#' @param data_from The name of the data source this mark reads from (see [vg_data()]).
#' @param filter_by A Param/Selection (e.g. from [param()]) to filter this mark's data by.
#' @family mark functions
#' @export
vg_circle <- function(spec = NULL, ariaDescription = vg_unset, ariaHidden = vg_unset, ariaLabel = vg_unset, channels = vg_unset, clip = vg_unset, dx = vg_unset, dy = vg_unset, facet = vg_unset, facetAnchor = vg_unset, fill = vg_unset, fillOpacity = vg_unset, filter = vg_unset, frameAnchor = vg_unset, fx = vg_unset, fy = vg_unset, href = vg_unset, imageFilter = vg_unset, margin = vg_unset, marginBottom = vg_unset, marginLeft = vg_unset, marginRight = vg_unset, marginTop = vg_unset, mixBlendMode = vg_unset, opacity = vg_unset, paintOrder = vg_unset, pointerEvents = vg_unset, r = vg_unset, reverse = vg_unset, rotate = vg_unset, select = vg_unset, shapeRendering = vg_unset, sort = vg_unset, stroke = vg_unset, strokeDasharray = vg_unset, strokeDashoffset = vg_unset, strokeLinecap = vg_unset, strokeLinejoin = vg_unset, strokeMiterlimit = vg_unset, strokeOpacity = vg_unset, strokeWidth = vg_unset, symbol = vg_unset, target = vg_unset, tip = vg_unset, title = vg_unset, x = vg_unset, y = vg_unset, z = vg_unset, ..., data_from = vg_unset, filter_by = vg_unset) {
  vg_mark_(spec, "circle", ariaDescription = ariaDescription, ariaHidden = ariaHidden, ariaLabel = ariaLabel, channels = channels, clip = clip, dx = dx, dy = dy, facet = facet, facetAnchor = facetAnchor, fill = fill, fillOpacity = fillOpacity, filter = filter, frameAnchor = frameAnchor, fx = fx, fy = fy, href = href, imageFilter = imageFilter, margin = margin, marginBottom = marginBottom, marginLeft = marginLeft, marginRight = marginRight, marginTop = marginTop, mixBlendMode = mixBlendMode, opacity = opacity, paintOrder = paintOrder, pointerEvents = pointerEvents, r = r, reverse = reverse, rotate = rotate, select = select, shapeRendering = shapeRendering, sort = sort, stroke = stroke, strokeDasharray = strokeDasharray, strokeDashoffset = strokeDashoffset, strokeLinecap = strokeLinecap, strokeLinejoin = strokeLinejoin, strokeMiterlimit = strokeMiterlimit, strokeOpacity = strokeOpacity, strokeWidth = strokeWidth, symbol = symbol, target = target, tip = tip, title = title, x = x, y = y, z = z, ..., data_from = data_from, filter_by = filter_by)
}

#' The contour mark.
#'
#' @param spec A plot fragment or `vgspec` to add this mark to, or `NULL` to start a new plot with just this mark.
#' @param ariaDescription The aria-description; a constant textual description.
#' @param ariaHidden The aria-hidden state; a constant indicating whether the element is exposed to an accessibility API.
#' @param ariaLabel The aria-label; a channel specifying short textual labels representing the value in the accessibility tree.
#' @param bandwidth The kernel density bandwidth for smoothing, in pixels.
#' @param channels Additional named channels, for example to include in a tooltip.
#' @param clip How to clip the mark; one of: - *frame* or true - clip to the plot’s frame (inner area) - *sphere* - clip to the projected sphere (*e.g.*, front hemisphere) - null or false - do not clip The *sphere* clip option requires a geographic projection.
#' @param dx The horizontal offset in pixels; a constant option.
#' @param dy The vertical offset in pixels; a constant option.
#' @param facet Whether to enable or disable faceting; one of: - *auto* (default) - automatically determine if this mark should be faceted - *include* (or true) - draw the subset of the mark’s data in the current facet - *exclude* - draw the subset of the mark’s data *not* in the current facet - *super* - draw this mark in a single frame that covers all facets - null (or false) - repeat this mark’s data across all facets (*i.e.*, no faceting) When a mark uses *super* faceting, it is not allowed to use position scales (*x*, *y*, *fx*, or *fy*); *super* faceting is intended for decorations, such as labels and legends.
#' @param facetAnchor How to place the mark with respect to facets; one of: - null (default for most marks) - display the mark in each non-empty facet - *top*, *right*, *bottom*, or *left* - display the mark only in facets on the given side - *top-empty*, *right-empty*, *bottom-empty*, or *left-empty* (default for axis marks) - display the mark only in facets that have empty space on the given side: either the margin, or an empty facet - *empty* - display the mark in empty facets only
#' @param fill The fill; a constant CSS color string, or a channel typically bound to the *color* scale.
#' @param fillOpacity The fill-opacity; a constant number between 0 and 1, or a channel typically bound to the *opacity* scale.
#' @param filter Applies a transform to filter the mark’s index according to the given channel values; only truthy values are retained.
#' @param fx The horizontal facet position channel, for mark-level faceting, bound to the *fx* scale.
#' @param fy The vertical facet position channel, for mark-level faceting, bound to the *fy* scale.
#' @param height The height (number of rows) of the grid, in actual pixels.
#' @param href The href; a channel specifying URLs for clickable links.
#' @param imageFilter A CSS filter; a constant string used to adjust the rendering of images, such as *blur(5px)*.
#' @param interpolate The spatial interpolation method; one of: - *none* - do not perform interpolation (the default), maps samples to single bins - *linear* - apply proportional linear interpolation across adjacent bins - *nearest* - assign each pixel to the closest sample’s value (Voronoi diagram) - *barycentric* - apply barycentric interpolation over the Delaunay triangulation - *random-walk* - apply a random walk from each pixel, stopping when near a sample
#' @param margin Shorthand to set the same default for all four mark margins: **marginTop**, **marginRight**, **marginBottom**, and **marginLeft**; typically defaults to 0, except for axis marks.
#' @param marginBottom The mark’s bottom margin; the minimum distance in pixels between the bottom edges of the inner and outer plot area.
#' @param marginLeft The mark’s left margin; the minimum distance in pixels between the left edges of the inner and outer plot area.
#' @param marginRight The mark’s right margin; the minimum distance in pixels between the right edges of the mark’s inner and outer plot area.
#' @param marginTop The mark’s top margin; the minimum distance in pixels between the top edges of the inner and outer plot area.
#' @param mixBlendMode The mix-blend-mode; a constant string specifying how to blend content such as *multiply*.
#' @param opacity The opacity; a constant between 0 and 1, or a channel typically bound to the *opacity* scale.
#' @param pad The bin padding, one of 1 (default) to include extra padding for the final bin, or 0 to make the bins flush with the maximum domain value.
#' @param paintOrder The paint-order; a constant string specifying the order in which the **fill**, **stroke**, and any markers are drawn; defaults to *normal*, which draws the fill, then stroke, then markers; defaults to *stroke* for the text mark to create a “halo” around text to improve legibility.
#' @param pixelSize The effective screen size of a raster pixel, used to determine the height and width of the raster from the frame’s dimensions; defaults to 1.
#' @param pointerEvents The pointer-events property; a constant string such as *none*.
#' @param reverse Applies a transform to reverse the order of the mark’s index, say for reverse input order.
#' @param select Applies a filter transform after data is loaded to highlight selected values only.
#' @param shapeRendering The shape-rendering; a constant string such as *crispEdges*.
#' @param sort Either applies a transform to sort the mark’s index by the specified channel values, or imputes ordinal scale domains from this mark’s channels.
#' @param stroke The stroke; a constant CSS color string, or a channel typically bound to the *color* scale.
#' @param strokeDasharray The stroke-dasharray; a constant number indicating the length in pixels of alternating dashes and gaps, or a constant string of numbers separated by spaces or commas (_e.g._, *10 2* for dashes of 10 pixels separated by gaps of 2 pixels), or *none* (the default) for no dashing : https://developer.mozilla.org/en-US/docs/Web/SVG/Attribute/stroke-dasharray
#' @param strokeDashoffset The stroke-dashoffset; a constant indicating the offset in pixels of the first dash along the stroke; defaults to zero.
#' @param strokeLinecap The stroke-linecap; a constant specifying how to cap stroked paths, such as *butt*, *round*, or *square*.
#' @param strokeLinejoin The stroke-linejoin; a constant specifying how to join stroked paths, such as *bevel*, *miter*, *miter-clip*, or *round*.
#' @param strokeMiterlimit The stroke-miterlimit; a constant number specifying how to limit the length of *miter* joins on stroked paths.
#' @param strokeOpacity The stroke-opacity; a constant between 0 and 1, or a channel typically bound to the *opacity* scale.
#' @param strokeWidth The stroke-width; a constant number in pixels, or a channel.
#' @param target The target; a constant string specifying the target window (_e.g._, *_blank*) for clickable links; used in conjunction with the **href** option.
#' @param thresholds The number of contour thresholds to subdivide the domain into discrete level sets; defaults to 10.
#' @param tip Whether to generate a tooltip for this mark, and any tip options.
#' @param title The title; a channel specifying accessible, short textual descriptions as strings (possibly with newlines).
#' @param width The width (number of columns) of the grid, in actual pixels.
#' @param x The horizontal position (or length) channel, typically bound to the *x* scale.
#' @param y The vertical position channel, typically bound to the *y* scale; defaults to the zero-based index of the data 0, 1, 2, ….
#' @param ... Additional options or plot-level attributes.
#' @param data_from The name of the data source this mark reads from (see [vg_data()]).
#' @param filter_by A Param/Selection (e.g. from [param()]) to filter this mark's data by.
#' @family mark functions
#' @export
vg_contour <- function(spec = NULL, ariaDescription = vg_unset, ariaHidden = vg_unset, ariaLabel = vg_unset, bandwidth = vg_unset, channels = vg_unset, clip = vg_unset, dx = vg_unset, dy = vg_unset, facet = vg_unset, facetAnchor = vg_unset, fill = vg_unset, fillOpacity = vg_unset, filter = vg_unset, fx = vg_unset, fy = vg_unset, height = vg_unset, href = vg_unset, imageFilter = vg_unset, interpolate = vg_unset, margin = vg_unset, marginBottom = vg_unset, marginLeft = vg_unset, marginRight = vg_unset, marginTop = vg_unset, mixBlendMode = vg_unset, opacity = vg_unset, pad = vg_unset, paintOrder = vg_unset, pixelSize = vg_unset, pointerEvents = vg_unset, reverse = vg_unset, select = vg_unset, shapeRendering = vg_unset, sort = vg_unset, stroke = vg_unset, strokeDasharray = vg_unset, strokeDashoffset = vg_unset, strokeLinecap = vg_unset, strokeLinejoin = vg_unset, strokeMiterlimit = vg_unset, strokeOpacity = vg_unset, strokeWidth = vg_unset, target = vg_unset, thresholds = vg_unset, tip = vg_unset, title = vg_unset, width = vg_unset, x = vg_unset, y = vg_unset, ..., data_from = vg_unset, filter_by = vg_unset) {
  vg_mark_(spec, "contour", ariaDescription = ariaDescription, ariaHidden = ariaHidden, ariaLabel = ariaLabel, bandwidth = bandwidth, channels = channels, clip = clip, dx = dx, dy = dy, facet = facet, facetAnchor = facetAnchor, fill = fill, fillOpacity = fillOpacity, filter = filter, fx = fx, fy = fy, height = height, href = href, imageFilter = imageFilter, interpolate = interpolate, margin = margin, marginBottom = marginBottom, marginLeft = marginLeft, marginRight = marginRight, marginTop = marginTop, mixBlendMode = mixBlendMode, opacity = opacity, pad = pad, paintOrder = paintOrder, pixelSize = pixelSize, pointerEvents = pointerEvents, reverse = reverse, select = select, shapeRendering = shapeRendering, sort = sort, stroke = stroke, strokeDasharray = strokeDasharray, strokeDashoffset = strokeDashoffset, strokeLinecap = strokeLinecap, strokeLinejoin = strokeLinejoin, strokeMiterlimit = strokeMiterlimit, strokeOpacity = strokeOpacity, strokeWidth = strokeWidth, target = target, thresholds = thresholds, tip = tip, title = title, width = width, x = x, y = y, ..., data_from = data_from, filter_by = filter_by)
}

#' The delaunayLink mark.
#'
#' @param spec A plot fragment or `vgspec` to add this mark to, or `NULL` to start a new plot with just this mark.
#' @param ariaDescription The aria-description; a constant textual description.
#' @param ariaHidden The aria-hidden state; a constant indicating whether the element is exposed to an accessibility API.
#' @param ariaLabel The aria-label; a channel specifying short textual labels representing the value in the accessibility tree.
#' @param channels Additional named channels, for example to include in a tooltip.
#' @param clip How to clip the mark; one of: - *frame* or true - clip to the plot’s frame (inner area) - *sphere* - clip to the projected sphere (*e.g.*, front hemisphere) - null or false - do not clip The *sphere* clip option requires a geographic projection.
#' @param curve The curve (interpolation) method for connecting adjacent points.
#' @param dx The horizontal offset in pixels; a constant option.
#' @param dy The vertical offset in pixels; a constant option.
#' @param facet Whether to enable or disable faceting; one of: - *auto* (default) - automatically determine if this mark should be faceted - *include* (or true) - draw the subset of the mark’s data in the current facet - *exclude* - draw the subset of the mark’s data *not* in the current facet - *super* - draw this mark in a single frame that covers all facets - null (or false) - repeat this mark’s data across all facets (*i.e.*, no faceting) When a mark uses *super* faceting, it is not allowed to use position scales (*x*, *y*, *fx*, or *fy*); *super* faceting is intended for decorations, such as labels and legends.
#' @param facetAnchor How to place the mark with respect to facets; one of: - null (default for most marks) - display the mark in each non-empty facet - *top*, *right*, *bottom*, or *left* - display the mark only in facets on the given side - *top-empty*, *right-empty*, *bottom-empty*, or *left-empty* (default for axis marks) - display the mark only in facets that have empty space on the given side: either the margin, or an empty facet - *empty* - display the mark in empty facets only
#' @param fill The fill; a constant CSS color string, or a channel typically bound to the *color* scale.
#' @param fillOpacity The fill-opacity; a constant number between 0 and 1, or a channel typically bound to the *opacity* scale.
#' @param filter Applies a transform to filter the mark’s index according to the given channel values; only truthy values are retained.
#' @param fx The horizontal facet position channel, for mark-level faceting, bound to the *fx* scale.
#' @param fy The vertical facet position channel, for mark-level faceting, bound to the *fy* scale.
#' @param href The href; a channel specifying URLs for clickable links.
#' @param imageFilter A CSS filter; a constant string used to adjust the rendering of images, such as *blur(5px)*.
#' @param margin Shorthand to set the same default for all four mark margins: **marginTop**, **marginRight**, **marginBottom**, and **marginLeft**; typically defaults to 0, except for axis marks.
#' @param marginBottom The mark’s bottom margin; the minimum distance in pixels between the bottom edges of the inner and outer plot area.
#' @param marginLeft The mark’s left margin; the minimum distance in pixels between the left edges of the inner and outer plot area.
#' @param marginRight The mark’s right margin; the minimum distance in pixels between the right edges of the mark’s inner and outer plot area.
#' @param marginTop The mark’s top margin; the minimum distance in pixels between the top edges of the inner and outer plot area.
#' @param marker Shorthand to set the same default for markerStart, markerMid, and markerEnd; one of: - a marker name such as *arrow* or *circle* - *none* (default) - no marker * true - alias for *circle-fill* * false or null - alias for *none*
#' @param markerEnd The marker for the ending point of a line segment; one of: - a marker name such as *arrow* or *circle* * *none* (default) - no marker * true - alias for *circle-fill* * false or null - alias for *none*
#' @param markerMid The marker for any middle (interior) points of a line segment.
#' @param markerStart The marker for the starting point of a line segment; one of: - a marker name such as *arrow* or *circle* * *none* (default) - no marker * true - alias for *circle-fill* * false or null - alias for *none*
#' @param mixBlendMode The mix-blend-mode; a constant string specifying how to blend content such as *multiply*.
#' @param opacity The opacity; a constant between 0 and 1, or a channel typically bound to the *opacity* scale.
#' @param paintOrder The paint-order; a constant string specifying the order in which the **fill**, **stroke**, and any markers are drawn; defaults to *normal*, which draws the fill, then stroke, then markers; defaults to *stroke* for the text mark to create a “halo” around text to improve legibility.
#' @param pointerEvents The pointer-events property; a constant string such as *none*.
#' @param reverse Applies a transform to reverse the order of the mark’s index, say for reverse input order.
#' @param select Applies a filter transform after data is loaded to highlight selected values only.
#' @param shapeRendering The shape-rendering; a constant string such as *crispEdges*.
#' @param sort Either applies a transform to sort the mark’s index by the specified channel values, or imputes ordinal scale domains from this mark’s channels.
#' @param stroke The stroke; a constant CSS color string, or a channel typically bound to the *color* scale.
#' @param strokeDasharray The stroke-dasharray; a constant number indicating the length in pixels of alternating dashes and gaps, or a constant string of numbers separated by spaces or commas (_e.g._, *10 2* for dashes of 10 pixels separated by gaps of 2 pixels), or *none* (the default) for no dashing : https://developer.mozilla.org/en-US/docs/Web/SVG/Attribute/stroke-dasharray
#' @param strokeDashoffset The stroke-dashoffset; a constant indicating the offset in pixels of the first dash along the stroke; defaults to zero.
#' @param strokeLinecap The stroke-linecap; a constant specifying how to cap stroked paths, such as *butt*, *round*, or *square*.
#' @param strokeLinejoin The stroke-linejoin; a constant specifying how to join stroked paths, such as *bevel*, *miter*, *miter-clip*, or *round*.
#' @param strokeMiterlimit The stroke-miterlimit; a constant number specifying how to limit the length of *miter* joins on stroked paths.
#' @param strokeOpacity The stroke-opacity; a constant between 0 and 1, or a channel typically bound to the *opacity* scale.
#' @param strokeWidth The stroke-width; a constant number in pixels, or a channel.
#' @param target The target; a constant string specifying the target window (_e.g._, *_blank*) for clickable links; used in conjunction with the **href** option.
#' @param tension The tension option only has an effect on bundle, cardinal and Catmull–Rom splines (*bundle*, *cardinal*, *cardinal-open*, *cardinal-closed*, *catmull-rom*, *catmull-rom-open*, and *catmull-rom-closed*).
#' @param tip Whether to generate a tooltip for this mark, and any tip options.
#' @param title The title; a channel specifying accessible, short textual descriptions as strings (possibly with newlines).
#' @param x The horizontal position (or length) channel, typically bound to the *x* scale.
#' @param y The vertical position channel, typically bound to the *y* scale; defaults to the zero-based index of the data 0, 1, 2, ….
#' @param z An optional ordinal channel for grouping data into (possibly stacked) series to be drawn as separate areas; defaults to **fill** if a channel, or **stroke** if a channel.
#' @param ... Additional options or plot-level attributes.
#' @param data_from The name of the data source this mark reads from (see [vg_data()]).
#' @param filter_by A Param/Selection (e.g. from [param()]) to filter this mark's data by.
#' @family mark functions
#' @export
vg_delaunay_link <- function(spec = NULL, ariaDescription = vg_unset, ariaHidden = vg_unset, ariaLabel = vg_unset, channels = vg_unset, clip = vg_unset, curve = vg_unset, dx = vg_unset, dy = vg_unset, facet = vg_unset, facetAnchor = vg_unset, fill = vg_unset, fillOpacity = vg_unset, filter = vg_unset, fx = vg_unset, fy = vg_unset, href = vg_unset, imageFilter = vg_unset, margin = vg_unset, marginBottom = vg_unset, marginLeft = vg_unset, marginRight = vg_unset, marginTop = vg_unset, marker = vg_unset, markerEnd = vg_unset, markerMid = vg_unset, markerStart = vg_unset, mixBlendMode = vg_unset, opacity = vg_unset, paintOrder = vg_unset, pointerEvents = vg_unset, reverse = vg_unset, select = vg_unset, shapeRendering = vg_unset, sort = vg_unset, stroke = vg_unset, strokeDasharray = vg_unset, strokeDashoffset = vg_unset, strokeLinecap = vg_unset, strokeLinejoin = vg_unset, strokeMiterlimit = vg_unset, strokeOpacity = vg_unset, strokeWidth = vg_unset, target = vg_unset, tension = vg_unset, tip = vg_unset, title = vg_unset, x = vg_unset, y = vg_unset, z = vg_unset, ..., data_from = vg_unset, filter_by = vg_unset) {
  vg_mark_(spec, "delaunayLink", ariaDescription = ariaDescription, ariaHidden = ariaHidden, ariaLabel = ariaLabel, channels = channels, clip = clip, curve = curve, dx = dx, dy = dy, facet = facet, facetAnchor = facetAnchor, fill = fill, fillOpacity = fillOpacity, filter = filter, fx = fx, fy = fy, href = href, imageFilter = imageFilter, margin = margin, marginBottom = marginBottom, marginLeft = marginLeft, marginRight = marginRight, marginTop = marginTop, marker = marker, markerEnd = markerEnd, markerMid = markerMid, markerStart = markerStart, mixBlendMode = mixBlendMode, opacity = opacity, paintOrder = paintOrder, pointerEvents = pointerEvents, reverse = reverse, select = select, shapeRendering = shapeRendering, sort = sort, stroke = stroke, strokeDasharray = strokeDasharray, strokeDashoffset = strokeDashoffset, strokeLinecap = strokeLinecap, strokeLinejoin = strokeLinejoin, strokeMiterlimit = strokeMiterlimit, strokeOpacity = strokeOpacity, strokeWidth = strokeWidth, target = target, tension = tension, tip = tip, title = title, x = x, y = y, z = z, ..., data_from = data_from, filter_by = filter_by)
}

#' The delaunayMesh mark.
#'
#' @param spec A plot fragment or `vgspec` to add this mark to, or `NULL` to start a new plot with just this mark.
#' @param ariaDescription The aria-description; a constant textual description.
#' @param ariaHidden The aria-hidden state; a constant indicating whether the element is exposed to an accessibility API.
#' @param ariaLabel The aria-label; a channel specifying short textual labels representing the value in the accessibility tree.
#' @param channels Additional named channels, for example to include in a tooltip.
#' @param clip How to clip the mark; one of: - *frame* or true - clip to the plot’s frame (inner area) - *sphere* - clip to the projected sphere (*e.g.*, front hemisphere) - null or false - do not clip The *sphere* clip option requires a geographic projection.
#' @param curve The curve (interpolation) method for connecting adjacent points.
#' @param dx The horizontal offset in pixels; a constant option.
#' @param dy The vertical offset in pixels; a constant option.
#' @param facet Whether to enable or disable faceting; one of: - *auto* (default) - automatically determine if this mark should be faceted - *include* (or true) - draw the subset of the mark’s data in the current facet - *exclude* - draw the subset of the mark’s data *not* in the current facet - *super* - draw this mark in a single frame that covers all facets - null (or false) - repeat this mark’s data across all facets (*i.e.*, no faceting) When a mark uses *super* faceting, it is not allowed to use position scales (*x*, *y*, *fx*, or *fy*); *super* faceting is intended for decorations, such as labels and legends.
#' @param facetAnchor How to place the mark with respect to facets; one of: - null (default for most marks) - display the mark in each non-empty facet - *top*, *right*, *bottom*, or *left* - display the mark only in facets on the given side - *top-empty*, *right-empty*, *bottom-empty*, or *left-empty* (default for axis marks) - display the mark only in facets that have empty space on the given side: either the margin, or an empty facet - *empty* - display the mark in empty facets only
#' @param fill The fill; a constant CSS color string, or a channel typically bound to the *color* scale.
#' @param fillOpacity The fill-opacity; a constant number between 0 and 1, or a channel typically bound to the *opacity* scale.
#' @param filter Applies a transform to filter the mark’s index according to the given channel values; only truthy values are retained.
#' @param fx The horizontal facet position channel, for mark-level faceting, bound to the *fx* scale.
#' @param fy The vertical facet position channel, for mark-level faceting, bound to the *fy* scale.
#' @param href The href; a channel specifying URLs for clickable links.
#' @param imageFilter A CSS filter; a constant string used to adjust the rendering of images, such as *blur(5px)*.
#' @param margin Shorthand to set the same default for all four mark margins: **marginTop**, **marginRight**, **marginBottom**, and **marginLeft**; typically defaults to 0, except for axis marks.
#' @param marginBottom The mark’s bottom margin; the minimum distance in pixels between the bottom edges of the inner and outer plot area.
#' @param marginLeft The mark’s left margin; the minimum distance in pixels between the left edges of the inner and outer plot area.
#' @param marginRight The mark’s right margin; the minimum distance in pixels between the right edges of the mark’s inner and outer plot area.
#' @param marginTop The mark’s top margin; the minimum distance in pixels between the top edges of the inner and outer plot area.
#' @param marker Shorthand to set the same default for markerStart, markerMid, and markerEnd; one of: - a marker name such as *arrow* or *circle* - *none* (default) - no marker * true - alias for *circle-fill* * false or null - alias for *none*
#' @param markerEnd The marker for the ending point of a line segment; one of: - a marker name such as *arrow* or *circle* * *none* (default) - no marker * true - alias for *circle-fill* * false or null - alias for *none*
#' @param markerMid The marker for any middle (interior) points of a line segment.
#' @param markerStart The marker for the starting point of a line segment; one of: - a marker name such as *arrow* or *circle* * *none* (default) - no marker * true - alias for *circle-fill* * false or null - alias for *none*
#' @param mixBlendMode The mix-blend-mode; a constant string specifying how to blend content such as *multiply*.
#' @param opacity The opacity; a constant between 0 and 1, or a channel typically bound to the *opacity* scale.
#' @param paintOrder The paint-order; a constant string specifying the order in which the **fill**, **stroke**, and any markers are drawn; defaults to *normal*, which draws the fill, then stroke, then markers; defaults to *stroke* for the text mark to create a “halo” around text to improve legibility.
#' @param pointerEvents The pointer-events property; a constant string such as *none*.
#' @param reverse Applies a transform to reverse the order of the mark’s index, say for reverse input order.
#' @param select Applies a filter transform after data is loaded to highlight selected values only.
#' @param shapeRendering The shape-rendering; a constant string such as *crispEdges*.
#' @param sort Either applies a transform to sort the mark’s index by the specified channel values, or imputes ordinal scale domains from this mark’s channels.
#' @param stroke The stroke; a constant CSS color string, or a channel typically bound to the *color* scale.
#' @param strokeDasharray The stroke-dasharray; a constant number indicating the length in pixels of alternating dashes and gaps, or a constant string of numbers separated by spaces or commas (_e.g._, *10 2* for dashes of 10 pixels separated by gaps of 2 pixels), or *none* (the default) for no dashing : https://developer.mozilla.org/en-US/docs/Web/SVG/Attribute/stroke-dasharray
#' @param strokeDashoffset The stroke-dashoffset; a constant indicating the offset in pixels of the first dash along the stroke; defaults to zero.
#' @param strokeLinecap The stroke-linecap; a constant specifying how to cap stroked paths, such as *butt*, *round*, or *square*.
#' @param strokeLinejoin The stroke-linejoin; a constant specifying how to join stroked paths, such as *bevel*, *miter*, *miter-clip*, or *round*.
#' @param strokeMiterlimit The stroke-miterlimit; a constant number specifying how to limit the length of *miter* joins on stroked paths.
#' @param strokeOpacity The stroke-opacity; a constant between 0 and 1, or a channel typically bound to the *opacity* scale.
#' @param strokeWidth The stroke-width; a constant number in pixels, or a channel.
#' @param target The target; a constant string specifying the target window (_e.g._, *_blank*) for clickable links; used in conjunction with the **href** option.
#' @param tension The tension option only has an effect on bundle, cardinal and Catmull–Rom splines (*bundle*, *cardinal*, *cardinal-open*, *cardinal-closed*, *catmull-rom*, *catmull-rom-open*, and *catmull-rom-closed*).
#' @param tip Whether to generate a tooltip for this mark, and any tip options.
#' @param title The title; a channel specifying accessible, short textual descriptions as strings (possibly with newlines).
#' @param x The horizontal position (or length) channel, typically bound to the *x* scale.
#' @param y The vertical position channel, typically bound to the *y* scale; defaults to the zero-based index of the data 0, 1, 2, ….
#' @param z An optional ordinal channel for grouping data into (possibly stacked) series to be drawn as separate areas; defaults to **fill** if a channel, or **stroke** if a channel.
#' @param ... Additional options or plot-level attributes.
#' @param data_from The name of the data source this mark reads from (see [vg_data()]).
#' @param filter_by A Param/Selection (e.g. from [param()]) to filter this mark's data by.
#' @family mark functions
#' @export
vg_delaunay_mesh <- function(spec = NULL, ariaDescription = vg_unset, ariaHidden = vg_unset, ariaLabel = vg_unset, channels = vg_unset, clip = vg_unset, curve = vg_unset, dx = vg_unset, dy = vg_unset, facet = vg_unset, facetAnchor = vg_unset, fill = vg_unset, fillOpacity = vg_unset, filter = vg_unset, fx = vg_unset, fy = vg_unset, href = vg_unset, imageFilter = vg_unset, margin = vg_unset, marginBottom = vg_unset, marginLeft = vg_unset, marginRight = vg_unset, marginTop = vg_unset, marker = vg_unset, markerEnd = vg_unset, markerMid = vg_unset, markerStart = vg_unset, mixBlendMode = vg_unset, opacity = vg_unset, paintOrder = vg_unset, pointerEvents = vg_unset, reverse = vg_unset, select = vg_unset, shapeRendering = vg_unset, sort = vg_unset, stroke = vg_unset, strokeDasharray = vg_unset, strokeDashoffset = vg_unset, strokeLinecap = vg_unset, strokeLinejoin = vg_unset, strokeMiterlimit = vg_unset, strokeOpacity = vg_unset, strokeWidth = vg_unset, target = vg_unset, tension = vg_unset, tip = vg_unset, title = vg_unset, x = vg_unset, y = vg_unset, z = vg_unset, ..., data_from = vg_unset, filter_by = vg_unset) {
  vg_mark_(spec, "delaunayMesh", ariaDescription = ariaDescription, ariaHidden = ariaHidden, ariaLabel = ariaLabel, channels = channels, clip = clip, curve = curve, dx = dx, dy = dy, facet = facet, facetAnchor = facetAnchor, fill = fill, fillOpacity = fillOpacity, filter = filter, fx = fx, fy = fy, href = href, imageFilter = imageFilter, margin = margin, marginBottom = marginBottom, marginLeft = marginLeft, marginRight = marginRight, marginTop = marginTop, marker = marker, markerEnd = markerEnd, markerMid = markerMid, markerStart = markerStart, mixBlendMode = mixBlendMode, opacity = opacity, paintOrder = paintOrder, pointerEvents = pointerEvents, reverse = reverse, select = select, shapeRendering = shapeRendering, sort = sort, stroke = stroke, strokeDasharray = strokeDasharray, strokeDashoffset = strokeDashoffset, strokeLinecap = strokeLinecap, strokeLinejoin = strokeLinejoin, strokeMiterlimit = strokeMiterlimit, strokeOpacity = strokeOpacity, strokeWidth = strokeWidth, target = target, tension = tension, tip = tip, title = title, x = x, y = y, z = z, ..., data_from = data_from, filter_by = filter_by)
}

#' The denseLine mark.
#'
#' @param spec A plot fragment or `vgspec` to add this mark to, or `NULL` to start a new plot with just this mark.
#' @param ariaDescription The aria-description; a constant textual description.
#' @param ariaHidden The aria-hidden state; a constant indicating whether the element is exposed to an accessibility API.
#' @param ariaLabel The aria-label; a channel specifying short textual labels representing the value in the accessibility tree.
#' @param bandwidth The kernel density bandwidth for smoothing, in pixels.
#' @param channels Additional named channels, for example to include in a tooltip.
#' @param clip How to clip the mark; one of: - *frame* or true - clip to the plot’s frame (inner area) - *sphere* - clip to the projected sphere (*e.g.*, front hemisphere) - null or false - do not clip The *sphere* clip option requires a geographic projection.
#' @param dx The horizontal offset in pixels; a constant option.
#' @param dy The vertical offset in pixels; a constant option.
#' @param facet Whether to enable or disable faceting; one of: - *auto* (default) - automatically determine if this mark should be faceted - *include* (or true) - draw the subset of the mark’s data in the current facet - *exclude* - draw the subset of the mark’s data *not* in the current facet - *super* - draw this mark in a single frame that covers all facets - null (or false) - repeat this mark’s data across all facets (*i.e.*, no faceting) When a mark uses *super* faceting, it is not allowed to use position scales (*x*, *y*, *fx*, or *fy*); *super* faceting is intended for decorations, such as labels and legends.
#' @param facetAnchor How to place the mark with respect to facets; one of: - null (default for most marks) - display the mark in each non-empty facet - *top*, *right*, *bottom*, or *left* - display the mark only in facets on the given side - *top-empty*, *right-empty*, *bottom-empty*, or *left-empty* (default for axis marks) - display the mark only in facets that have empty space on the given side: either the margin, or an empty facet - *empty* - display the mark in empty facets only
#' @param fill The fill; a constant CSS color string, or a channel typically bound to the *color* scale.
#' @param fillOpacity The fill-opacity; a constant number between 0 and 1, or a channel typically bound to the *opacity* scale.
#' @param filter Applies a transform to filter the mark’s index according to the given channel values; only truthy values are retained.
#' @param fx The horizontal facet position channel, for mark-level faceting, bound to the *fx* scale.
#' @param fy The vertical facet position channel, for mark-level faceting, bound to the *fy* scale.
#' @param height The height (number of rows) of the grid, in actual pixels.
#' @param href The href; a channel specifying URLs for clickable links.
#' @param imageFilter A CSS filter; a constant string used to adjust the rendering of images, such as *blur(5px)*.
#' @param imageRendering The image-rendering attribute; defaults to *auto* (bilinear).
#' @param interpolate The spatial interpolation method; one of: - *none* - do not perform interpolation (the default), maps samples to single bins - *linear* - apply proportional linear interpolation across adjacent bins - *nearest* - assign each pixel to the closest sample’s value (Voronoi diagram) - *barycentric* - apply barycentric interpolation over the Delaunay triangulation - *random-walk* - apply a random walk from each pixel, stopping when near a sample
#' @param margin Shorthand to set the same default for all four mark margins: **marginTop**, **marginRight**, **marginBottom**, and **marginLeft**; typically defaults to 0, except for axis marks.
#' @param marginBottom The mark’s bottom margin; the minimum distance in pixels between the bottom edges of the inner and outer plot area.
#' @param marginLeft The mark’s left margin; the minimum distance in pixels between the left edges of the inner and outer plot area.
#' @param marginRight The mark’s right margin; the minimum distance in pixels between the right edges of the mark’s inner and outer plot area.
#' @param marginTop The mark’s top margin; the minimum distance in pixels between the top edges of the inner and outer plot area.
#' @param mixBlendMode The mix-blend-mode; a constant string specifying how to blend content such as *multiply*.
#' @param normalize Flag to perform approximate arc length normalization of line segments to prevent artifacts due to overcounting steep lines.
#' @param opacity The opacity; a constant between 0 and 1, or a channel typically bound to the *opacity* scale.
#' @param pad The bin padding, one of 1 (default) to include extra padding for the final bin, or 0 to make the bins flush with the maximum domain value.
#' @param paintOrder The paint-order; a constant string specifying the order in which the **fill**, **stroke**, and any markers are drawn; defaults to *normal*, which draws the fill, then stroke, then markers; defaults to *stroke* for the text mark to create a “halo” around text to improve legibility.
#' @param pixelSize The effective screen size of a raster pixel, used to determine the height and width of the raster from the frame’s dimensions; defaults to 1.
#' @param pointerEvents The pointer-events property; a constant string such as *none*.
#' @param reverse Applies a transform to reverse the order of the mark’s index, say for reverse input order.
#' @param select Applies a filter transform after data is loaded to highlight selected values only.
#' @param shapeRendering The shape-rendering; a constant string such as *crispEdges*.
#' @param sort Either applies a transform to sort the mark’s index by the specified channel values, or imputes ordinal scale domains from this mark’s channels.
#' @param stroke The stroke; a constant CSS color string, or a channel typically bound to the *color* scale.
#' @param strokeDasharray The stroke-dasharray; a constant number indicating the length in pixels of alternating dashes and gaps, or a constant string of numbers separated by spaces or commas (_e.g._, *10 2* for dashes of 10 pixels separated by gaps of 2 pixels), or *none* (the default) for no dashing : https://developer.mozilla.org/en-US/docs/Web/SVG/Attribute/stroke-dasharray
#' @param strokeDashoffset The stroke-dashoffset; a constant indicating the offset in pixels of the first dash along the stroke; defaults to zero.
#' @param strokeLinecap The stroke-linecap; a constant specifying how to cap stroked paths, such as *butt*, *round*, or *square*.
#' @param strokeLinejoin The stroke-linejoin; a constant specifying how to join stroked paths, such as *bevel*, *miter*, *miter-clip*, or *round*.
#' @param strokeMiterlimit The stroke-miterlimit; a constant number specifying how to limit the length of *miter* joins on stroked paths.
#' @param strokeOpacity The stroke-opacity; a constant between 0 and 1, or a channel typically bound to the *opacity* scale.
#' @param strokeWidth The stroke-width; a constant number in pixels, or a channel.
#' @param target The target; a constant string specifying the target window (_e.g._, *_blank*) for clickable links; used in conjunction with the **href** option.
#' @param tip Whether to generate a tooltip for this mark, and any tip options.
#' @param title The title; a channel specifying accessible, short textual descriptions as strings (possibly with newlines).
#' @param width The width (number of columns) of the grid, in actual pixels.
#' @param x The horizontal position (or length) channel, typically bound to the *x* scale.
#' @param y The vertical position channel, typically bound to the *y* scale; defaults to the zero-based index of the data 0, 1, 2, ….
#' @param z An optional ordinal channel for grouping data into (possibly stacked) series to be drawn as separate areas; defaults to **fill** if a channel, or **stroke** if a channel.
#' @param ... Additional options or plot-level attributes.
#' @param data_from The name of the data source this mark reads from (see [vg_data()]).
#' @param filter_by A Param/Selection (e.g. from [param()]) to filter this mark's data by.
#' @family mark functions
#' @export
vg_dense_line <- function(spec = NULL, ariaDescription = vg_unset, ariaHidden = vg_unset, ariaLabel = vg_unset, bandwidth = vg_unset, channels = vg_unset, clip = vg_unset, dx = vg_unset, dy = vg_unset, facet = vg_unset, facetAnchor = vg_unset, fill = vg_unset, fillOpacity = vg_unset, filter = vg_unset, fx = vg_unset, fy = vg_unset, height = vg_unset, href = vg_unset, imageFilter = vg_unset, imageRendering = vg_unset, interpolate = vg_unset, margin = vg_unset, marginBottom = vg_unset, marginLeft = vg_unset, marginRight = vg_unset, marginTop = vg_unset, mixBlendMode = vg_unset, normalize = vg_unset, opacity = vg_unset, pad = vg_unset, paintOrder = vg_unset, pixelSize = vg_unset, pointerEvents = vg_unset, reverse = vg_unset, select = vg_unset, shapeRendering = vg_unset, sort = vg_unset, stroke = vg_unset, strokeDasharray = vg_unset, strokeDashoffset = vg_unset, strokeLinecap = vg_unset, strokeLinejoin = vg_unset, strokeMiterlimit = vg_unset, strokeOpacity = vg_unset, strokeWidth = vg_unset, target = vg_unset, tip = vg_unset, title = vg_unset, width = vg_unset, x = vg_unset, y = vg_unset, z = vg_unset, ..., data_from = vg_unset, filter_by = vg_unset) {
  vg_mark_(spec, "denseLine", ariaDescription = ariaDescription, ariaHidden = ariaHidden, ariaLabel = ariaLabel, bandwidth = bandwidth, channels = channels, clip = clip, dx = dx, dy = dy, facet = facet, facetAnchor = facetAnchor, fill = fill, fillOpacity = fillOpacity, filter = filter, fx = fx, fy = fy, height = height, href = href, imageFilter = imageFilter, imageRendering = imageRendering, interpolate = interpolate, margin = margin, marginBottom = marginBottom, marginLeft = marginLeft, marginRight = marginRight, marginTop = marginTop, mixBlendMode = mixBlendMode, normalize = normalize, opacity = opacity, pad = pad, paintOrder = paintOrder, pixelSize = pixelSize, pointerEvents = pointerEvents, reverse = reverse, select = select, shapeRendering = shapeRendering, sort = sort, stroke = stroke, strokeDasharray = strokeDasharray, strokeDashoffset = strokeDashoffset, strokeLinecap = strokeLinecap, strokeLinejoin = strokeLinejoin, strokeMiterlimit = strokeMiterlimit, strokeOpacity = strokeOpacity, strokeWidth = strokeWidth, target = target, tip = tip, title = title, width = width, x = x, y = y, z = z, ..., data_from = data_from, filter_by = filter_by)
}

#' The density mark for 2D densities.
#'
#' @param spec A plot fragment or `vgspec` to add this mark to, or `NULL` to start a new plot with just this mark.
#' @param ariaDescription The aria-description; a constant textual description.
#' @param ariaHidden The aria-hidden state; a constant indicating whether the element is exposed to an accessibility API.
#' @param ariaLabel The aria-label; a channel specifying short textual labels representing the value in the accessibility tree.
#' @param bandwidth The kernel density bandwidth for smoothing, in pixels.
#' @param channels Additional named channels, for example to include in a tooltip.
#' @param clip How to clip the mark; one of: - *frame* or true - clip to the plot’s frame (inner area) - *sphere* - clip to the projected sphere (*e.g.*, front hemisphere) - null or false - do not clip The *sphere* clip option requires a geographic projection.
#' @param dx The horizontal offset in pixels; a constant option.
#' @param dy The vertical offset in pixels; a constant option.
#' @param facet Whether to enable or disable faceting; one of: - *auto* (default) - automatically determine if this mark should be faceted - *include* (or true) - draw the subset of the mark’s data in the current facet - *exclude* - draw the subset of the mark’s data *not* in the current facet - *super* - draw this mark in a single frame that covers all facets - null (or false) - repeat this mark’s data across all facets (*i.e.*, no faceting) When a mark uses *super* faceting, it is not allowed to use position scales (*x*, *y*, *fx*, or *fy*); *super* faceting is intended for decorations, such as labels and legends.
#' @param facetAnchor How to place the mark with respect to facets; one of: - null (default for most marks) - display the mark in each non-empty facet - *top*, *right*, *bottom*, or *left* - display the mark only in facets on the given side - *top-empty*, *right-empty*, *bottom-empty*, or *left-empty* (default for axis marks) - display the mark only in facets that have empty space on the given side: either the margin, or an empty facet - *empty* - display the mark in empty facets only
#' @param fill The fill; a constant CSS color string, or a channel typically bound to the *color* scale.
#' @param fillOpacity The fill-opacity; a constant number between 0 and 1, or a channel typically bound to the *opacity* scale.
#' @param filter Applies a transform to filter the mark’s index according to the given channel values; only truthy values are retained.
#' @param fontFamily The font-family; a constant; defaults to the plot’s font family, which is typically *system-ui*.
#' @param fontSize The font size in pixels; either a constant or a channel; defaults to the plot’s font size, which is typically 10.
#' @param fontStyle The font style; a constant; defaults to the plot’s font style, which is typically *normal*.
#' @param fontVariant The font variant; a constant; if the **text** channel contains numbers or dates, defaults to *tabular-nums* to facilitate comparing numbers; otherwise defaults to the plot’s font style, which is typically *normal*.
#' @param fontWeight The font weight; a constant; defaults to the plot’s font weight, which is typically *normal*.
#' @param frameAnchor The frame anchor specifies defaults for **x** and **y**, along with **textAnchor** and **lineAnchor**, based on the plot’s frame; it may be one of the four sides (*top*, *right*, *bottom*, *left*), one of the four corners (*top-left*, *top-right*, *bottom-right*, *bottom-left*), or the *middle* of the frame.
#' @param fx The horizontal facet position channel, for mark-level faceting, bound to the *fx* scale.
#' @param fy The vertical facet position channel, for mark-level faceting, bound to the *fy* scale.
#' @param height The height (number of rows) of the grid, in actual pixels.
#' @param href The href; a channel specifying URLs for clickable links.
#' @param imageFilter A CSS filter; a constant string used to adjust the rendering of images, such as *blur(5px)*.
#' @param interpolate The spatial interpolation method; one of: - *none* - do not perform interpolation (the default), maps samples to single bins - *linear* - apply proportional linear interpolation across adjacent bins - *nearest* - assign each pixel to the closest sample’s value (Voronoi diagram) - *barycentric* - apply barycentric interpolation over the Delaunay triangulation - *random-walk* - apply a random walk from each pixel, stopping when near a sample
#' @param lineHeight The line height in ems; defaults to 1.
#' @param lineWidth The line width in ems (e.g., 10 for about 20 characters); defaults to infinity, disabling wrapping and clipping.
#' @param margin Shorthand to set the same default for all four mark margins: **marginTop**, **marginRight**, **marginBottom**, and **marginLeft**; typically defaults to 0, except for axis marks.
#' @param marginBottom The mark’s bottom margin; the minimum distance in pixels between the bottom edges of the inner and outer plot area.
#' @param marginLeft The mark’s left margin; the minimum distance in pixels between the left edges of the inner and outer plot area.
#' @param marginRight The mark’s right margin; the minimum distance in pixels between the right edges of the mark’s inner and outer plot area.
#' @param marginTop The mark’s top margin; the minimum distance in pixels between the top edges of the inner and outer plot area.
#' @param mixBlendMode The mix-blend-mode; a constant string specifying how to blend content such as *multiply*.
#' @param monospace If true, changes the default **fontFamily** to *monospace*, and uses simplified monospaced text metrics calculations.
#' @param opacity The opacity; a constant between 0 and 1, or a channel typically bound to the *opacity* scale.
#' @param pad The bin padding, one of 1 (default) to include extra padding for the final bin, or 0 to make the bins flush with the maximum domain value.
#' @param paintOrder The paint-order; a constant string specifying the order in which the **fill**, **stroke**, and any markers are drawn; defaults to *normal*, which draws the fill, then stroke, then markers; defaults to *stroke* for the text mark to create a “halo” around text to improve legibility.
#' @param pixelSize The effective screen size of a raster pixel, used to determine the height and width of the raster from the frame’s dimensions; defaults to 1.
#' @param pointerEvents The pointer-events property; a constant string such as *none*.
#' @param r The radius of dots; either a channel or constant.
#' @param reverse Applies a transform to reverse the order of the mark’s index, say for reverse input order.
#' @param rotate The rotation angle in degrees clockwise; a constant or a channel; defaults to 0°.
#' @param select Applies a filter transform after data is loaded to highlight selected values only.
#' @param shapeRendering The shape-rendering; a constant string such as *crispEdges*.
#' @param sort Either applies a transform to sort the mark’s index by the specified channel values, or imputes ordinal scale domains from this mark’s channels.
#' @param stroke The stroke; a constant CSS color string, or a channel typically bound to the *color* scale.
#' @param strokeDasharray The stroke-dasharray; a constant number indicating the length in pixels of alternating dashes and gaps, or a constant string of numbers separated by spaces or commas (_e.g._, *10 2* for dashes of 10 pixels separated by gaps of 2 pixels), or *none* (the default) for no dashing : https://developer.mozilla.org/en-US/docs/Web/SVG/Attribute/stroke-dasharray
#' @param strokeDashoffset The stroke-dashoffset; a constant indicating the offset in pixels of the first dash along the stroke; defaults to zero.
#' @param strokeLinecap The stroke-linecap; a constant specifying how to cap stroked paths, such as *butt*, *round*, or *square*.
#' @param strokeLinejoin The stroke-linejoin; a constant specifying how to join stroked paths, such as *bevel*, *miter*, *miter-clip*, or *round*.
#' @param strokeMiterlimit The stroke-miterlimit; a constant number specifying how to limit the length of *miter* joins on stroked paths.
#' @param strokeOpacity The stroke-opacity; a constant between 0 and 1, or a channel typically bound to the *opacity* scale.
#' @param strokeWidth The stroke-width; a constant number in pixels, or a channel.
#' @param symbol The categorical symbol; either a channel or a constant.
#' @param target The target; a constant string specifying the target window (_e.g._, *_blank*) for clickable links; used in conjunction with the **href** option.
#' @param textAnchor The text anchor controls how text is aligned (typically horizontally) relative to its anchor point; it is one of *start*, *end*, or *middle*.
#' @param textOverflow How truncate (or wrap) lines of text longer than the given **lineWidth**; one of: - null (default) - preserve overflowing characters (and wrap if needed) - *clip* or *clip-end* - remove characters from the end - *clip-start* - remove characters from the start - *ellipsis* or *ellipsis-end* - replace characters from the end with an ellipsis (…) - *ellipsis-start* - replace characters from the start with an ellipsis (…) - *ellipsis-middle* - replace characters from the middle with an ellipsis (…) If no **title** was specified, if text requires truncation, a title containing the non-truncated text will be implicitly added.
#' @param tip Whether to generate a tooltip for this mark, and any tip options.
#' @param title The title; a channel specifying accessible, short textual descriptions as strings (possibly with newlines).
#' @param type The basic mark type to use to render 2D density values.
#' @param width The width (number of columns) of the grid, in actual pixels.
#' @param x The horizontal position (or length) channel, typically bound to the *x* scale.
#' @param y The vertical position channel, typically bound to the *y* scale; defaults to the zero-based index of the data 0, 1, 2, ….
#' @param z An optional ordinal channel for grouping data into (possibly stacked) series to be drawn as separate areas; defaults to **fill** if a channel, or **stroke** if a channel.
#' @param ... Additional options or plot-level attributes.
#' @param data_from The name of the data source this mark reads from (see [vg_data()]).
#' @param filter_by A Param/Selection (e.g. from [param()]) to filter this mark's data by.
#' @family mark functions
#' @export
vg_density <- function(spec = NULL, ariaDescription = vg_unset, ariaHidden = vg_unset, ariaLabel = vg_unset, bandwidth = vg_unset, channels = vg_unset, clip = vg_unset, dx = vg_unset, dy = vg_unset, facet = vg_unset, facetAnchor = vg_unset, fill = vg_unset, fillOpacity = vg_unset, filter = vg_unset, fontFamily = vg_unset, fontSize = vg_unset, fontStyle = vg_unset, fontVariant = vg_unset, fontWeight = vg_unset, frameAnchor = vg_unset, fx = vg_unset, fy = vg_unset, height = vg_unset, href = vg_unset, imageFilter = vg_unset, interpolate = vg_unset, lineHeight = vg_unset, lineWidth = vg_unset, margin = vg_unset, marginBottom = vg_unset, marginLeft = vg_unset, marginRight = vg_unset, marginTop = vg_unset, mixBlendMode = vg_unset, monospace = vg_unset, opacity = vg_unset, pad = vg_unset, paintOrder = vg_unset, pixelSize = vg_unset, pointerEvents = vg_unset, r = vg_unset, reverse = vg_unset, rotate = vg_unset, select = vg_unset, shapeRendering = vg_unset, sort = vg_unset, stroke = vg_unset, strokeDasharray = vg_unset, strokeDashoffset = vg_unset, strokeLinecap = vg_unset, strokeLinejoin = vg_unset, strokeMiterlimit = vg_unset, strokeOpacity = vg_unset, strokeWidth = vg_unset, symbol = vg_unset, target = vg_unset, textAnchor = vg_unset, textOverflow = vg_unset, tip = vg_unset, title = vg_unset, type = vg_unset, width = vg_unset, x = vg_unset, y = vg_unset, z = vg_unset, ..., data_from = vg_unset, filter_by = vg_unset) {
  vg_mark_(spec, "density", ariaDescription = ariaDescription, ariaHidden = ariaHidden, ariaLabel = ariaLabel, bandwidth = bandwidth, channels = channels, clip = clip, dx = dx, dy = dy, facet = facet, facetAnchor = facetAnchor, fill = fill, fillOpacity = fillOpacity, filter = filter, fontFamily = fontFamily, fontSize = fontSize, fontStyle = fontStyle, fontVariant = fontVariant, fontWeight = fontWeight, frameAnchor = frameAnchor, fx = fx, fy = fy, height = height, href = href, imageFilter = imageFilter, interpolate = interpolate, lineHeight = lineHeight, lineWidth = lineWidth, margin = margin, marginBottom = marginBottom, marginLeft = marginLeft, marginRight = marginRight, marginTop = marginTop, mixBlendMode = mixBlendMode, monospace = monospace, opacity = opacity, pad = pad, paintOrder = paintOrder, pixelSize = pixelSize, pointerEvents = pointerEvents, r = r, reverse = reverse, rotate = rotate, select = select, shapeRendering = shapeRendering, sort = sort, stroke = stroke, strokeDasharray = strokeDasharray, strokeDashoffset = strokeDashoffset, strokeLinecap = strokeLinecap, strokeLinejoin = strokeLinejoin, strokeMiterlimit = strokeMiterlimit, strokeOpacity = strokeOpacity, strokeWidth = strokeWidth, symbol = symbol, target = target, textAnchor = textAnchor, textOverflow = textOverflow, tip = tip, title = title, type = type, width = width, x = x, y = y, z = z, ..., data_from = data_from, filter_by = filter_by)
}

#' The densityX mark.
#'
#' @param spec A plot fragment or `vgspec` to add this mark to, or `NULL` to start a new plot with just this mark.
#' @param ariaDescription The aria-description; a constant textual description.
#' @param ariaHidden The aria-hidden state; a constant indicating whether the element is exposed to an accessibility API.
#' @param ariaLabel The aria-label; a channel specifying short textual labels representing the value in the accessibility tree.
#' @param bandwidth The kernel density bandwidth for smoothing, in pixels.
#' @param bins The number of bins over which to discretize the data prior to smoothing.
#' @param channels Additional named channels, for example to include in a tooltip.
#' @param clip How to clip the mark; one of: - *frame* or true - clip to the plot’s frame (inner area) - *sphere* - clip to the projected sphere (*e.g.*, front hemisphere) - null or false - do not clip The *sphere* clip option requires a geographic projection.
#' @param curve The curve (interpolation) method for connecting adjacent points.
#' @param dx The horizontal offset in pixels; a constant option.
#' @param dy The vertical offset in pixels; a constant option.
#' @param facet Whether to enable or disable faceting; one of: - *auto* (default) - automatically determine if this mark should be faceted - *include* (or true) - draw the subset of the mark’s data in the current facet - *exclude* - draw the subset of the mark’s data *not* in the current facet - *super* - draw this mark in a single frame that covers all facets - null (or false) - repeat this mark’s data across all facets (*i.e.*, no faceting) When a mark uses *super* faceting, it is not allowed to use position scales (*x*, *y*, *fx*, or *fy*); *super* faceting is intended for decorations, such as labels and legends.
#' @param facetAnchor How to place the mark with respect to facets; one of: - null (default for most marks) - display the mark in each non-empty facet - *top*, *right*, *bottom*, or *left* - display the mark only in facets on the given side - *top-empty*, *right-empty*, *bottom-empty*, or *left-empty* (default for axis marks) - display the mark only in facets that have empty space on the given side: either the margin, or an empty facet - *empty* - display the mark in empty facets only
#' @param fill The fill; a constant CSS color string, or a channel typically bound to the *color* scale.
#' @param fillOpacity The fill-opacity; a constant number between 0 and 1, or a channel typically bound to the *opacity* scale.
#' @param filter Applies a transform to filter the mark’s index according to the given channel values; only truthy values are retained.
#' @param fx The horizontal facet position channel, for mark-level faceting, bound to the *fx* scale.
#' @param fy The vertical facet position channel, for mark-level faceting, bound to the *fy* scale.
#' @param href The href; a channel specifying URLs for clickable links.
#' @param imageFilter A CSS filter; a constant string used to adjust the rendering of images, such as *blur(5px)*.
#' @param margin Shorthand to set the same default for all four mark margins: **marginTop**, **marginRight**, **marginBottom**, and **marginLeft**; typically defaults to 0, except for axis marks.
#' @param marginBottom The mark’s bottom margin; the minimum distance in pixels between the bottom edges of the inner and outer plot area.
#' @param marginLeft The mark’s left margin; the minimum distance in pixels between the left edges of the inner and outer plot area.
#' @param marginRight The mark’s right margin; the minimum distance in pixels between the right edges of the mark’s inner and outer plot area.
#' @param marginTop The mark’s top margin; the minimum distance in pixels between the top edges of the inner and outer plot area.
#' @param mixBlendMode The mix-blend-mode; a constant string specifying how to blend content such as *multiply*.
#' @param normalize Flag to perform approximate arc length normalization of line segments to prevent artifacts due to overcounting steep lines.
#' @param offset After stacking, an optional **offset** can be applied to translate and scale stacks, say to produce a streamgraph; defaults to null for a zero baseline (**y** = 0 for stackY, and **x** = 0 for stackX).
#' @param opacity The opacity; a constant between 0 and 1, or a channel typically bound to the *opacity* scale.
#' @param order The order in which stacks are layered; one of: - null (default) for input order - a named stack order method such as *inside-out* or *sum* - a field name, for natural order of the corresponding values - a function of data, for natural order of the corresponding values - an array of explicit **z** values in the desired order If the *wiggle* **offset** is used, as for a streamgraph, the default changes to *inside-out*.
#' @param paintOrder The paint-order; a constant string specifying the order in which the **fill**, **stroke**, and any markers are drawn; defaults to *normal*, which draws the fill, then stroke, then markers; defaults to *stroke* for the text mark to create a “halo” around text to improve legibility.
#' @param pointerEvents The pointer-events property; a constant string such as *none*.
#' @param reverse Applies a transform to reverse the order of the mark’s index, say for reverse input order.
#' @param select Applies a filter transform after data is loaded to highlight selected values only.
#' @param shapeRendering The shape-rendering; a constant string such as *crispEdges*.
#' @param sort Either applies a transform to sort the mark’s index by the specified channel values, or imputes ordinal scale domains from this mark’s channels.
#' @param stack Flag indicating if densities should be stacked.
#' @param stroke The stroke; a constant CSS color string, or a channel typically bound to the *color* scale.
#' @param strokeDasharray The stroke-dasharray; a constant number indicating the length in pixels of alternating dashes and gaps, or a constant string of numbers separated by spaces or commas (_e.g._, *10 2* for dashes of 10 pixels separated by gaps of 2 pixels), or *none* (the default) for no dashing : https://developer.mozilla.org/en-US/docs/Web/SVG/Attribute/stroke-dasharray
#' @param strokeDashoffset The stroke-dashoffset; a constant indicating the offset in pixels of the first dash along the stroke; defaults to zero.
#' @param strokeLinecap The stroke-linecap; a constant specifying how to cap stroked paths, such as *butt*, *round*, or *square*.
#' @param strokeLinejoin The stroke-linejoin; a constant specifying how to join stroked paths, such as *bevel*, *miter*, *miter-clip*, or *round*.
#' @param strokeMiterlimit The stroke-miterlimit; a constant number specifying how to limit the length of *miter* joins on stroked paths.
#' @param strokeOpacity The stroke-opacity; a constant between 0 and 1, or a channel typically bound to the *opacity* scale.
#' @param strokeWidth The stroke-width; a constant number in pixels, or a channel.
#' @param target The target; a constant string specifying the target window (_e.g._, *_blank*) for clickable links; used in conjunction with the **href** option.
#' @param tension The tension option only has an effect on bundle, cardinal and Catmull–Rom splines (*bundle*, *cardinal*, *cardinal-open*, *cardinal-closed*, *catmull-rom*, *catmull-rom-open*, and *catmull-rom-closed*).
#' @param tip Whether to generate a tooltip for this mark, and any tip options.
#' @param title The title; a channel specifying accessible, short textual descriptions as strings (possibly with newlines).
#' @param type The basic mark type to use to render 2D density values.
#' @param y The vertical position channel, typically bound to the *y* scale; defaults to the zero-based index of the data 0, 1, 2, ….
#' @param z An optional ordinal channel for grouping data into (possibly stacked) series to be drawn as separate areas; defaults to **fill** if a channel, or **stroke** if a channel.
#' @param marker Shorthand to set the same default for markerStart, markerMid, and markerEnd; one of: - a marker name such as *arrow* or *circle* - *none* (default) - no marker * true - alias for *circle-fill* * false or null - alias for *none*
#' @param markerEnd The marker for the ending point of a line segment; one of: - a marker name such as *arrow* or *circle* * *none* (default) - no marker * true - alias for *circle-fill* * false or null - alias for *none*
#' @param markerMid The marker for any middle (interior) points of a line segment.
#' @param markerStart The marker for the starting point of a line segment; one of: - a marker name such as *arrow* or *circle* * *none* (default) - no marker * true - alias for *circle-fill* * false or null - alias for *none*
#' @param frameAnchor The frame anchor specifies defaults for **x** and **y**, along with **textAnchor** and **lineAnchor**, based on the plot’s frame; it may be one of the four sides (*top*, *right*, *bottom*, *left*), one of the four corners (*top-left*, *top-right*, *bottom-right*, *bottom-left*), or the *middle* of the frame.
#' @param r The radius of dots; either a channel or constant.
#' @param rotate The rotation angle in degrees clockwise; a constant or a channel; defaults to 0°.
#' @param symbol The categorical symbol; either a channel or a constant.
#' @param fontFamily The font-family; a constant; defaults to the plot’s font family, which is typically *system-ui*.
#' @param fontSize The font size in pixels; either a constant or a channel; defaults to the plot’s font size, which is typically 10.
#' @param fontStyle The font style; a constant; defaults to the plot’s font style, which is typically *normal*.
#' @param fontVariant The font variant; a constant; if the **text** channel contains numbers or dates, defaults to *tabular-nums* to facilitate comparing numbers; otherwise defaults to the plot’s font style, which is typically *normal*.
#' @param fontWeight The font weight; a constant; defaults to the plot’s font weight, which is typically *normal*.
#' @param lineAnchor The line anchor controls how text is aligned (typically vertically) relative to its anchor point; it is one of *top*, *bottom*, or *middle*.
#' @param lineHeight The line height in ems; defaults to 1.
#' @param lineWidth The line width in ems (e.g., 10 for about 20 characters); defaults to infinity, disabling wrapping and clipping.
#' @param monospace If true, changes the default **fontFamily** to *monospace*, and uses simplified monospaced text metrics calculations.
#' @param text The text contents channel, possibly with line breaks (\\n, \\r\\n, or \\r).
#' @param textAnchor The text anchor controls how text is aligned (typically horizontally) relative to its anchor point; it is one of *start*, *end*, or *middle*.
#' @param textOverflow How truncate (or wrap) lines of text longer than the given **lineWidth**; one of: - null (default) - preserve overflowing characters (and wrap if needed) - *clip* or *clip-end* - remove characters from the end - *clip-start* - remove characters from the start - *ellipsis* or *ellipsis-end* - replace characters from the end with an ellipsis (…) - *ellipsis-start* - replace characters from the start with an ellipsis (…) - *ellipsis-middle* - replace characters from the middle with an ellipsis (…) If no **title** was specified, if text requires truncation, a title containing the non-truncated text will be implicitly added.
#' @param ... Additional options or plot-level attributes.
#' @param data_from The name of the data source this mark reads from (see [vg_data()]).
#' @param filter_by A Param/Selection (e.g. from [param()]) to filter this mark's data by.
#' @family mark functions
#' @export
vg_density_x <- function(spec = NULL, ariaDescription = vg_unset, ariaHidden = vg_unset, ariaLabel = vg_unset, bandwidth = vg_unset, bins = vg_unset, channels = vg_unset, clip = vg_unset, curve = vg_unset, dx = vg_unset, dy = vg_unset, facet = vg_unset, facetAnchor = vg_unset, fill = vg_unset, fillOpacity = vg_unset, filter = vg_unset, fx = vg_unset, fy = vg_unset, href = vg_unset, imageFilter = vg_unset, margin = vg_unset, marginBottom = vg_unset, marginLeft = vg_unset, marginRight = vg_unset, marginTop = vg_unset, mixBlendMode = vg_unset, normalize = vg_unset, offset = vg_unset, opacity = vg_unset, order = vg_unset, paintOrder = vg_unset, pointerEvents = vg_unset, reverse = vg_unset, select = vg_unset, shapeRendering = vg_unset, sort = vg_unset, stack = vg_unset, stroke = vg_unset, strokeDasharray = vg_unset, strokeDashoffset = vg_unset, strokeLinecap = vg_unset, strokeLinejoin = vg_unset, strokeMiterlimit = vg_unset, strokeOpacity = vg_unset, strokeWidth = vg_unset, target = vg_unset, tension = vg_unset, tip = vg_unset, title = vg_unset, type = vg_unset, y = vg_unset, z = vg_unset, marker = vg_unset, markerEnd = vg_unset, markerMid = vg_unset, markerStart = vg_unset, frameAnchor = vg_unset, r = vg_unset, rotate = vg_unset, symbol = vg_unset, fontFamily = vg_unset, fontSize = vg_unset, fontStyle = vg_unset, fontVariant = vg_unset, fontWeight = vg_unset, lineAnchor = vg_unset, lineHeight = vg_unset, lineWidth = vg_unset, monospace = vg_unset, text = vg_unset, textAnchor = vg_unset, textOverflow = vg_unset, ..., data_from = vg_unset, filter_by = vg_unset) {
  vg_mark_(spec, "densityX", ariaDescription = ariaDescription, ariaHidden = ariaHidden, ariaLabel = ariaLabel, bandwidth = bandwidth, bins = bins, channels = channels, clip = clip, curve = curve, dx = dx, dy = dy, facet = facet, facetAnchor = facetAnchor, fill = fill, fillOpacity = fillOpacity, filter = filter, fx = fx, fy = fy, href = href, imageFilter = imageFilter, margin = margin, marginBottom = marginBottom, marginLeft = marginLeft, marginRight = marginRight, marginTop = marginTop, mixBlendMode = mixBlendMode, normalize = normalize, offset = offset, opacity = opacity, order = order, paintOrder = paintOrder, pointerEvents = pointerEvents, reverse = reverse, select = select, shapeRendering = shapeRendering, sort = sort, stack = stack, stroke = stroke, strokeDasharray = strokeDasharray, strokeDashoffset = strokeDashoffset, strokeLinecap = strokeLinecap, strokeLinejoin = strokeLinejoin, strokeMiterlimit = strokeMiterlimit, strokeOpacity = strokeOpacity, strokeWidth = strokeWidth, target = target, tension = tension, tip = tip, title = title, type = type, y = y, z = z, marker = marker, markerEnd = markerEnd, markerMid = markerMid, markerStart = markerStart, frameAnchor = frameAnchor, r = r, rotate = rotate, symbol = symbol, fontFamily = fontFamily, fontSize = fontSize, fontStyle = fontStyle, fontVariant = fontVariant, fontWeight = fontWeight, lineAnchor = lineAnchor, lineHeight = lineHeight, lineWidth = lineWidth, monospace = monospace, text = text, textAnchor = textAnchor, textOverflow = textOverflow, ..., data_from = data_from, filter_by = filter_by)
}

#' The densityY mark.
#'
#' @param spec A plot fragment or `vgspec` to add this mark to, or `NULL` to start a new plot with just this mark.
#' @param ariaDescription The aria-description; a constant textual description.
#' @param ariaHidden The aria-hidden state; a constant indicating whether the element is exposed to an accessibility API.
#' @param ariaLabel The aria-label; a channel specifying short textual labels representing the value in the accessibility tree.
#' @param bandwidth The kernel density bandwidth for smoothing, in pixels.
#' @param bins The number of bins over which to discretize the data prior to smoothing.
#' @param channels Additional named channels, for example to include in a tooltip.
#' @param clip How to clip the mark; one of: - *frame* or true - clip to the plot’s frame (inner area) - *sphere* - clip to the projected sphere (*e.g.*, front hemisphere) - null or false - do not clip The *sphere* clip option requires a geographic projection.
#' @param curve The curve (interpolation) method for connecting adjacent points.
#' @param dx The horizontal offset in pixels; a constant option.
#' @param dy The vertical offset in pixels; a constant option.
#' @param facet Whether to enable or disable faceting; one of: - *auto* (default) - automatically determine if this mark should be faceted - *include* (or true) - draw the subset of the mark’s data in the current facet - *exclude* - draw the subset of the mark’s data *not* in the current facet - *super* - draw this mark in a single frame that covers all facets - null (or false) - repeat this mark’s data across all facets (*i.e.*, no faceting) When a mark uses *super* faceting, it is not allowed to use position scales (*x*, *y*, *fx*, or *fy*); *super* faceting is intended for decorations, such as labels and legends.
#' @param facetAnchor How to place the mark with respect to facets; one of: - null (default for most marks) - display the mark in each non-empty facet - *top*, *right*, *bottom*, or *left* - display the mark only in facets on the given side - *top-empty*, *right-empty*, *bottom-empty*, or *left-empty* (default for axis marks) - display the mark only in facets that have empty space on the given side: either the margin, or an empty facet - *empty* - display the mark in empty facets only
#' @param fill The fill; a constant CSS color string, or a channel typically bound to the *color* scale.
#' @param fillOpacity The fill-opacity; a constant number between 0 and 1, or a channel typically bound to the *opacity* scale.
#' @param filter Applies a transform to filter the mark’s index according to the given channel values; only truthy values are retained.
#' @param fx The horizontal facet position channel, for mark-level faceting, bound to the *fx* scale.
#' @param fy The vertical facet position channel, for mark-level faceting, bound to the *fy* scale.
#' @param href The href; a channel specifying URLs for clickable links.
#' @param imageFilter A CSS filter; a constant string used to adjust the rendering of images, such as *blur(5px)*.
#' @param margin Shorthand to set the same default for all four mark margins: **marginTop**, **marginRight**, **marginBottom**, and **marginLeft**; typically defaults to 0, except for axis marks.
#' @param marginBottom The mark’s bottom margin; the minimum distance in pixels between the bottom edges of the inner and outer plot area.
#' @param marginLeft The mark’s left margin; the minimum distance in pixels between the left edges of the inner and outer plot area.
#' @param marginRight The mark’s right margin; the minimum distance in pixels between the right edges of the mark’s inner and outer plot area.
#' @param marginTop The mark’s top margin; the minimum distance in pixels between the top edges of the inner and outer plot area.
#' @param mixBlendMode The mix-blend-mode; a constant string specifying how to blend content such as *multiply*.
#' @param normalize Flag to perform approximate arc length normalization of line segments to prevent artifacts due to overcounting steep lines.
#' @param offset After stacking, an optional **offset** can be applied to translate and scale stacks, say to produce a streamgraph; defaults to null for a zero baseline (**y** = 0 for stackY, and **x** = 0 for stackX).
#' @param opacity The opacity; a constant between 0 and 1, or a channel typically bound to the *opacity* scale.
#' @param order The order in which stacks are layered; one of: - null (default) for input order - a named stack order method such as *inside-out* or *sum* - a field name, for natural order of the corresponding values - a function of data, for natural order of the corresponding values - an array of explicit **z** values in the desired order If the *wiggle* **offset** is used, as for a streamgraph, the default changes to *inside-out*.
#' @param paintOrder The paint-order; a constant string specifying the order in which the **fill**, **stroke**, and any markers are drawn; defaults to *normal*, which draws the fill, then stroke, then markers; defaults to *stroke* for the text mark to create a “halo” around text to improve legibility.
#' @param pointerEvents The pointer-events property; a constant string such as *none*.
#' @param reverse Applies a transform to reverse the order of the mark’s index, say for reverse input order.
#' @param select Applies a filter transform after data is loaded to highlight selected values only.
#' @param shapeRendering The shape-rendering; a constant string such as *crispEdges*.
#' @param sort Either applies a transform to sort the mark’s index by the specified channel values, or imputes ordinal scale domains from this mark’s channels.
#' @param stack Flag indicating if densities should be stacked.
#' @param stroke The stroke; a constant CSS color string, or a channel typically bound to the *color* scale.
#' @param strokeDasharray The stroke-dasharray; a constant number indicating the length in pixels of alternating dashes and gaps, or a constant string of numbers separated by spaces or commas (_e.g._, *10 2* for dashes of 10 pixels separated by gaps of 2 pixels), or *none* (the default) for no dashing : https://developer.mozilla.org/en-US/docs/Web/SVG/Attribute/stroke-dasharray
#' @param strokeDashoffset The stroke-dashoffset; a constant indicating the offset in pixels of the first dash along the stroke; defaults to zero.
#' @param strokeLinecap The stroke-linecap; a constant specifying how to cap stroked paths, such as *butt*, *round*, or *square*.
#' @param strokeLinejoin The stroke-linejoin; a constant specifying how to join stroked paths, such as *bevel*, *miter*, *miter-clip*, or *round*.
#' @param strokeMiterlimit The stroke-miterlimit; a constant number specifying how to limit the length of *miter* joins on stroked paths.
#' @param strokeOpacity The stroke-opacity; a constant between 0 and 1, or a channel typically bound to the *opacity* scale.
#' @param strokeWidth The stroke-width; a constant number in pixels, or a channel.
#' @param target The target; a constant string specifying the target window (_e.g._, *_blank*) for clickable links; used in conjunction with the **href** option.
#' @param tension The tension option only has an effect on bundle, cardinal and Catmull–Rom splines (*bundle*, *cardinal*, *cardinal-open*, *cardinal-closed*, *catmull-rom*, *catmull-rom-open*, and *catmull-rom-closed*).
#' @param tip Whether to generate a tooltip for this mark, and any tip options.
#' @param title The title; a channel specifying accessible, short textual descriptions as strings (possibly with newlines).
#' @param type The basic mark type to use to render 2D density values.
#' @param x The horizontal position (or length) channel, typically bound to the *x* scale.
#' @param z An optional ordinal channel for grouping data into (possibly stacked) series to be drawn as separate areas; defaults to **fill** if a channel, or **stroke** if a channel.
#' @param marker Shorthand to set the same default for markerStart, markerMid, and markerEnd; one of: - a marker name such as *arrow* or *circle* - *none* (default) - no marker * true - alias for *circle-fill* * false or null - alias for *none*
#' @param markerEnd The marker for the ending point of a line segment; one of: - a marker name such as *arrow* or *circle* * *none* (default) - no marker * true - alias for *circle-fill* * false or null - alias for *none*
#' @param markerMid The marker for any middle (interior) points of a line segment.
#' @param markerStart The marker for the starting point of a line segment; one of: - a marker name such as *arrow* or *circle* * *none* (default) - no marker * true - alias for *circle-fill* * false or null - alias for *none*
#' @param frameAnchor The frame anchor specifies defaults for **x** and **y**, along with **textAnchor** and **lineAnchor**, based on the plot’s frame; it may be one of the four sides (*top*, *right*, *bottom*, *left*), one of the four corners (*top-left*, *top-right*, *bottom-right*, *bottom-left*), or the *middle* of the frame.
#' @param r The radius of dots; either a channel or constant.
#' @param rotate The rotation angle in degrees clockwise; a constant or a channel; defaults to 0°.
#' @param symbol The categorical symbol; either a channel or a constant.
#' @param fontFamily The font-family; a constant; defaults to the plot’s font family, which is typically *system-ui*.
#' @param fontSize The font size in pixels; either a constant or a channel; defaults to the plot’s font size, which is typically 10.
#' @param fontStyle The font style; a constant; defaults to the plot’s font style, which is typically *normal*.
#' @param fontVariant The font variant; a constant; if the **text** channel contains numbers or dates, defaults to *tabular-nums* to facilitate comparing numbers; otherwise defaults to the plot’s font style, which is typically *normal*.
#' @param fontWeight The font weight; a constant; defaults to the plot’s font weight, which is typically *normal*.
#' @param lineAnchor The line anchor controls how text is aligned (typically vertically) relative to its anchor point; it is one of *top*, *bottom*, or *middle*.
#' @param lineHeight The line height in ems; defaults to 1.
#' @param lineWidth The line width in ems (e.g., 10 for about 20 characters); defaults to infinity, disabling wrapping and clipping.
#' @param monospace If true, changes the default **fontFamily** to *monospace*, and uses simplified monospaced text metrics calculations.
#' @param text The text contents channel, possibly with line breaks (\\n, \\r\\n, or \\r).
#' @param textAnchor The text anchor controls how text is aligned (typically horizontally) relative to its anchor point; it is one of *start*, *end*, or *middle*.
#' @param textOverflow How truncate (or wrap) lines of text longer than the given **lineWidth**; one of: - null (default) - preserve overflowing characters (and wrap if needed) - *clip* or *clip-end* - remove characters from the end - *clip-start* - remove characters from the start - *ellipsis* or *ellipsis-end* - replace characters from the end with an ellipsis (…) - *ellipsis-start* - replace characters from the start with an ellipsis (…) - *ellipsis-middle* - replace characters from the middle with an ellipsis (…) If no **title** was specified, if text requires truncation, a title containing the non-truncated text will be implicitly added.
#' @param ... Additional options or plot-level attributes.
#' @param data_from The name of the data source this mark reads from (see [vg_data()]).
#' @param filter_by A Param/Selection (e.g. from [param()]) to filter this mark's data by.
#' @family mark functions
#' @export
vg_density_y <- function(spec = NULL, ariaDescription = vg_unset, ariaHidden = vg_unset, ariaLabel = vg_unset, bandwidth = vg_unset, bins = vg_unset, channels = vg_unset, clip = vg_unset, curve = vg_unset, dx = vg_unset, dy = vg_unset, facet = vg_unset, facetAnchor = vg_unset, fill = vg_unset, fillOpacity = vg_unset, filter = vg_unset, fx = vg_unset, fy = vg_unset, href = vg_unset, imageFilter = vg_unset, margin = vg_unset, marginBottom = vg_unset, marginLeft = vg_unset, marginRight = vg_unset, marginTop = vg_unset, mixBlendMode = vg_unset, normalize = vg_unset, offset = vg_unset, opacity = vg_unset, order = vg_unset, paintOrder = vg_unset, pointerEvents = vg_unset, reverse = vg_unset, select = vg_unset, shapeRendering = vg_unset, sort = vg_unset, stack = vg_unset, stroke = vg_unset, strokeDasharray = vg_unset, strokeDashoffset = vg_unset, strokeLinecap = vg_unset, strokeLinejoin = vg_unset, strokeMiterlimit = vg_unset, strokeOpacity = vg_unset, strokeWidth = vg_unset, target = vg_unset, tension = vg_unset, tip = vg_unset, title = vg_unset, type = vg_unset, x = vg_unset, z = vg_unset, marker = vg_unset, markerEnd = vg_unset, markerMid = vg_unset, markerStart = vg_unset, frameAnchor = vg_unset, r = vg_unset, rotate = vg_unset, symbol = vg_unset, fontFamily = vg_unset, fontSize = vg_unset, fontStyle = vg_unset, fontVariant = vg_unset, fontWeight = vg_unset, lineAnchor = vg_unset, lineHeight = vg_unset, lineWidth = vg_unset, monospace = vg_unset, text = vg_unset, textAnchor = vg_unset, textOverflow = vg_unset, ..., data_from = vg_unset, filter_by = vg_unset) {
  vg_mark_(spec, "densityY", ariaDescription = ariaDescription, ariaHidden = ariaHidden, ariaLabel = ariaLabel, bandwidth = bandwidth, bins = bins, channels = channels, clip = clip, curve = curve, dx = dx, dy = dy, facet = facet, facetAnchor = facetAnchor, fill = fill, fillOpacity = fillOpacity, filter = filter, fx = fx, fy = fy, href = href, imageFilter = imageFilter, margin = margin, marginBottom = marginBottom, marginLeft = marginLeft, marginRight = marginRight, marginTop = marginTop, mixBlendMode = mixBlendMode, normalize = normalize, offset = offset, opacity = opacity, order = order, paintOrder = paintOrder, pointerEvents = pointerEvents, reverse = reverse, select = select, shapeRendering = shapeRendering, sort = sort, stack = stack, stroke = stroke, strokeDasharray = strokeDasharray, strokeDashoffset = strokeDashoffset, strokeLinecap = strokeLinecap, strokeLinejoin = strokeLinejoin, strokeMiterlimit = strokeMiterlimit, strokeOpacity = strokeOpacity, strokeWidth = strokeWidth, target = target, tension = tension, tip = tip, title = title, type = type, x = x, z = z, marker = marker, markerEnd = markerEnd, markerMid = markerMid, markerStart = markerStart, frameAnchor = frameAnchor, r = r, rotate = rotate, symbol = symbol, fontFamily = fontFamily, fontSize = fontSize, fontStyle = fontStyle, fontVariant = fontVariant, fontWeight = fontWeight, lineAnchor = lineAnchor, lineHeight = lineHeight, lineWidth = lineWidth, monospace = monospace, text = text, textAnchor = textAnchor, textOverflow = textOverflow, ..., data_from = data_from, filter_by = filter_by)
}

#' The dot mark.
#'
#' @param spec A plot fragment or `vgspec` to add this mark to, or `NULL` to start a new plot with just this mark.
#' @param ariaDescription The aria-description; a constant textual description.
#' @param ariaHidden The aria-hidden state; a constant indicating whether the element is exposed to an accessibility API.
#' @param ariaLabel The aria-label; a channel specifying short textual labels representing the value in the accessibility tree.
#' @param channels Additional named channels, for example to include in a tooltip.
#' @param clip How to clip the mark; one of: - *frame* or true - clip to the plot’s frame (inner area) - *sphere* - clip to the projected sphere (*e.g.*, front hemisphere) - null or false - do not clip The *sphere* clip option requires a geographic projection.
#' @param dx The horizontal offset in pixels; a constant option.
#' @param dy The vertical offset in pixels; a constant option.
#' @param facet Whether to enable or disable faceting; one of: - *auto* (default) - automatically determine if this mark should be faceted - *include* (or true) - draw the subset of the mark’s data in the current facet - *exclude* - draw the subset of the mark’s data *not* in the current facet - *super* - draw this mark in a single frame that covers all facets - null (or false) - repeat this mark’s data across all facets (*i.e.*, no faceting) When a mark uses *super* faceting, it is not allowed to use position scales (*x*, *y*, *fx*, or *fy*); *super* faceting is intended for decorations, such as labels and legends.
#' @param facetAnchor How to place the mark with respect to facets; one of: - null (default for most marks) - display the mark in each non-empty facet - *top*, *right*, *bottom*, or *left* - display the mark only in facets on the given side - *top-empty*, *right-empty*, *bottom-empty*, or *left-empty* (default for axis marks) - display the mark only in facets that have empty space on the given side: either the margin, or an empty facet - *empty* - display the mark in empty facets only
#' @param fill The fill; a constant CSS color string, or a channel typically bound to the *color* scale.
#' @param fillOpacity The fill-opacity; a constant number between 0 and 1, or a channel typically bound to the *opacity* scale.
#' @param filter Applies a transform to filter the mark’s index according to the given channel values; only truthy values are retained.
#' @param frameAnchor The frame anchor specifies defaults for **x** and **y**, along with **textAnchor** and **lineAnchor**, based on the plot’s frame; it may be one of the four sides (*top*, *right*, *bottom*, *left*), one of the four corners (*top-left*, *top-right*, *bottom-right*, *bottom-left*), or the *middle* of the frame.
#' @param fx The horizontal facet position channel, for mark-level faceting, bound to the *fx* scale.
#' @param fy The vertical facet position channel, for mark-level faceting, bound to the *fy* scale.
#' @param href The href; a channel specifying URLs for clickable links.
#' @param imageFilter A CSS filter; a constant string used to adjust the rendering of images, such as *blur(5px)*.
#' @param margin Shorthand to set the same default for all four mark margins: **marginTop**, **marginRight**, **marginBottom**, and **marginLeft**; typically defaults to 0, except for axis marks.
#' @param marginBottom The mark’s bottom margin; the minimum distance in pixels between the bottom edges of the inner and outer plot area.
#' @param marginLeft The mark’s left margin; the minimum distance in pixels between the left edges of the inner and outer plot area.
#' @param marginRight The mark’s right margin; the minimum distance in pixels between the right edges of the mark’s inner and outer plot area.
#' @param marginTop The mark’s top margin; the minimum distance in pixels between the top edges of the inner and outer plot area.
#' @param mixBlendMode The mix-blend-mode; a constant string specifying how to blend content such as *multiply*.
#' @param opacity The opacity; a constant between 0 and 1, or a channel typically bound to the *opacity* scale.
#' @param paintOrder The paint-order; a constant string specifying the order in which the **fill**, **stroke**, and any markers are drawn; defaults to *normal*, which draws the fill, then stroke, then markers; defaults to *stroke* for the text mark to create a “halo” around text to improve legibility.
#' @param pointerEvents The pointer-events property; a constant string such as *none*.
#' @param r The radius of dots; either a channel or constant.
#' @param reverse Applies a transform to reverse the order of the mark’s index, say for reverse input order.
#' @param rotate The rotation angle in degrees clockwise; a constant or a channel; defaults to 0°.
#' @param select Applies a filter transform after data is loaded to highlight selected values only.
#' @param shapeRendering The shape-rendering; a constant string such as *crispEdges*.
#' @param sort Either applies a transform to sort the mark’s index by the specified channel values, or imputes ordinal scale domains from this mark’s channels.
#' @param stroke The stroke; a constant CSS color string, or a channel typically bound to the *color* scale.
#' @param strokeDasharray The stroke-dasharray; a constant number indicating the length in pixels of alternating dashes and gaps, or a constant string of numbers separated by spaces or commas (_e.g._, *10 2* for dashes of 10 pixels separated by gaps of 2 pixels), or *none* (the default) for no dashing : https://developer.mozilla.org/en-US/docs/Web/SVG/Attribute/stroke-dasharray
#' @param strokeDashoffset The stroke-dashoffset; a constant indicating the offset in pixels of the first dash along the stroke; defaults to zero.
#' @param strokeLinecap The stroke-linecap; a constant specifying how to cap stroked paths, such as *butt*, *round*, or *square*.
#' @param strokeLinejoin The stroke-linejoin; a constant specifying how to join stroked paths, such as *bevel*, *miter*, *miter-clip*, or *round*.
#' @param strokeMiterlimit The stroke-miterlimit; a constant number specifying how to limit the length of *miter* joins on stroked paths.
#' @param strokeOpacity The stroke-opacity; a constant between 0 and 1, or a channel typically bound to the *opacity* scale.
#' @param strokeWidth The stroke-width; a constant number in pixels, or a channel.
#' @param symbol The categorical symbol; either a channel or a constant.
#' @param target The target; a constant string specifying the target window (_e.g._, *_blank*) for clickable links; used in conjunction with the **href** option.
#' @param tip Whether to generate a tooltip for this mark, and any tip options.
#' @param title The title; a channel specifying accessible, short textual descriptions as strings (possibly with newlines).
#' @param x The horizontal position (or length) channel, typically bound to the *x* scale.
#' @param y The vertical position channel, typically bound to the *y* scale; defaults to the zero-based index of the data 0, 1, 2, ….
#' @param z An optional ordinal channel for grouping data into (possibly stacked) series to be drawn as separate areas; defaults to **fill** if a channel, or **stroke** if a channel.
#' @param ... Additional options or plot-level attributes.
#' @param data_from The name of the data source this mark reads from (see [vg_data()]).
#' @param filter_by A Param/Selection (e.g. from [param()]) to filter this mark's data by.
#' @family mark functions
#' @export
vg_dot <- function(spec = NULL, ariaDescription = vg_unset, ariaHidden = vg_unset, ariaLabel = vg_unset, channels = vg_unset, clip = vg_unset, dx = vg_unset, dy = vg_unset, facet = vg_unset, facetAnchor = vg_unset, fill = vg_unset, fillOpacity = vg_unset, filter = vg_unset, frameAnchor = vg_unset, fx = vg_unset, fy = vg_unset, href = vg_unset, imageFilter = vg_unset, margin = vg_unset, marginBottom = vg_unset, marginLeft = vg_unset, marginRight = vg_unset, marginTop = vg_unset, mixBlendMode = vg_unset, opacity = vg_unset, paintOrder = vg_unset, pointerEvents = vg_unset, r = vg_unset, reverse = vg_unset, rotate = vg_unset, select = vg_unset, shapeRendering = vg_unset, sort = vg_unset, stroke = vg_unset, strokeDasharray = vg_unset, strokeDashoffset = vg_unset, strokeLinecap = vg_unset, strokeLinejoin = vg_unset, strokeMiterlimit = vg_unset, strokeOpacity = vg_unset, strokeWidth = vg_unset, symbol = vg_unset, target = vg_unset, tip = vg_unset, title = vg_unset, x = vg_unset, y = vg_unset, z = vg_unset, ..., data_from = vg_unset, filter_by = vg_unset) {
  vg_mark_(spec, "dot", ariaDescription = ariaDescription, ariaHidden = ariaHidden, ariaLabel = ariaLabel, channels = channels, clip = clip, dx = dx, dy = dy, facet = facet, facetAnchor = facetAnchor, fill = fill, fillOpacity = fillOpacity, filter = filter, frameAnchor = frameAnchor, fx = fx, fy = fy, href = href, imageFilter = imageFilter, margin = margin, marginBottom = marginBottom, marginLeft = marginLeft, marginRight = marginRight, marginTop = marginTop, mixBlendMode = mixBlendMode, opacity = opacity, paintOrder = paintOrder, pointerEvents = pointerEvents, r = r, reverse = reverse, rotate = rotate, select = select, shapeRendering = shapeRendering, sort = sort, stroke = stroke, strokeDasharray = strokeDasharray, strokeDashoffset = strokeDashoffset, strokeLinecap = strokeLinecap, strokeLinejoin = strokeLinejoin, strokeMiterlimit = strokeMiterlimit, strokeOpacity = strokeOpacity, strokeWidth = strokeWidth, symbol = symbol, target = target, tip = tip, title = title, x = x, y = y, z = z, ..., data_from = data_from, filter_by = filter_by)
}

#' The dotX mark.
#'
#' @param spec A plot fragment or `vgspec` to add this mark to, or `NULL` to start a new plot with just this mark.
#' @param ariaDescription The aria-description; a constant textual description.
#' @param ariaHidden The aria-hidden state; a constant indicating whether the element is exposed to an accessibility API.
#' @param ariaLabel The aria-label; a channel specifying short textual labels representing the value in the accessibility tree.
#' @param channels Additional named channels, for example to include in a tooltip.
#' @param clip How to clip the mark; one of: - *frame* or true - clip to the plot’s frame (inner area) - *sphere* - clip to the projected sphere (*e.g.*, front hemisphere) - null or false - do not clip The *sphere* clip option requires a geographic projection.
#' @param dx The horizontal offset in pixels; a constant option.
#' @param dy The vertical offset in pixels; a constant option.
#' @param facet Whether to enable or disable faceting; one of: - *auto* (default) - automatically determine if this mark should be faceted - *include* (or true) - draw the subset of the mark’s data in the current facet - *exclude* - draw the subset of the mark’s data *not* in the current facet - *super* - draw this mark in a single frame that covers all facets - null (or false) - repeat this mark’s data across all facets (*i.e.*, no faceting) When a mark uses *super* faceting, it is not allowed to use position scales (*x*, *y*, *fx*, or *fy*); *super* faceting is intended for decorations, such as labels and legends.
#' @param facetAnchor How to place the mark with respect to facets; one of: - null (default for most marks) - display the mark in each non-empty facet - *top*, *right*, *bottom*, or *left* - display the mark only in facets on the given side - *top-empty*, *right-empty*, *bottom-empty*, or *left-empty* (default for axis marks) - display the mark only in facets that have empty space on the given side: either the margin, or an empty facet - *empty* - display the mark in empty facets only
#' @param fill The fill; a constant CSS color string, or a channel typically bound to the *color* scale.
#' @param fillOpacity The fill-opacity; a constant number between 0 and 1, or a channel typically bound to the *opacity* scale.
#' @param filter Applies a transform to filter the mark’s index according to the given channel values; only truthy values are retained.
#' @param frameAnchor The frame anchor specifies defaults for **x** and **y**, along with **textAnchor** and **lineAnchor**, based on the plot’s frame; it may be one of the four sides (*top*, *right*, *bottom*, *left*), one of the four corners (*top-left*, *top-right*, *bottom-right*, *bottom-left*), or the *middle* of the frame.
#' @param fx The horizontal facet position channel, for mark-level faceting, bound to the *fx* scale.
#' @param fy The vertical facet position channel, for mark-level faceting, bound to the *fy* scale.
#' @param href The href; a channel specifying URLs for clickable links.
#' @param imageFilter A CSS filter; a constant string used to adjust the rendering of images, such as *blur(5px)*.
#' @param interval Enforces uniformity for data at regular intervals, such as integer values or daily samples.
#' @param margin Shorthand to set the same default for all four mark margins: **marginTop**, **marginRight**, **marginBottom**, and **marginLeft**; typically defaults to 0, except for axis marks.
#' @param marginBottom The mark’s bottom margin; the minimum distance in pixels between the bottom edges of the inner and outer plot area.
#' @param marginLeft The mark’s left margin; the minimum distance in pixels between the left edges of the inner and outer plot area.
#' @param marginRight The mark’s right margin; the minimum distance in pixels between the right edges of the mark’s inner and outer plot area.
#' @param marginTop The mark’s top margin; the minimum distance in pixels between the top edges of the inner and outer plot area.
#' @param mixBlendMode The mix-blend-mode; a constant string specifying how to blend content such as *multiply*.
#' @param opacity The opacity; a constant between 0 and 1, or a channel typically bound to the *opacity* scale.
#' @param paintOrder The paint-order; a constant string specifying the order in which the **fill**, **stroke**, and any markers are drawn; defaults to *normal*, which draws the fill, then stroke, then markers; defaults to *stroke* for the text mark to create a “halo” around text to improve legibility.
#' @param pointerEvents The pointer-events property; a constant string such as *none*.
#' @param r The radius of dots; either a channel or constant.
#' @param reverse Applies a transform to reverse the order of the mark’s index, say for reverse input order.
#' @param rotate The rotation angle in degrees clockwise; a constant or a channel; defaults to 0°.
#' @param select Applies a filter transform after data is loaded to highlight selected values only.
#' @param shapeRendering The shape-rendering; a constant string such as *crispEdges*.
#' @param sort Either applies a transform to sort the mark’s index by the specified channel values, or imputes ordinal scale domains from this mark’s channels.
#' @param stroke The stroke; a constant CSS color string, or a channel typically bound to the *color* scale.
#' @param strokeDasharray The stroke-dasharray; a constant number indicating the length in pixels of alternating dashes and gaps, or a constant string of numbers separated by spaces or commas (_e.g._, *10 2* for dashes of 10 pixels separated by gaps of 2 pixels), or *none* (the default) for no dashing : https://developer.mozilla.org/en-US/docs/Web/SVG/Attribute/stroke-dasharray
#' @param strokeDashoffset The stroke-dashoffset; a constant indicating the offset in pixels of the first dash along the stroke; defaults to zero.
#' @param strokeLinecap The stroke-linecap; a constant specifying how to cap stroked paths, such as *butt*, *round*, or *square*.
#' @param strokeLinejoin The stroke-linejoin; a constant specifying how to join stroked paths, such as *bevel*, *miter*, *miter-clip*, or *round*.
#' @param strokeMiterlimit The stroke-miterlimit; a constant number specifying how to limit the length of *miter* joins on stroked paths.
#' @param strokeOpacity The stroke-opacity; a constant between 0 and 1, or a channel typically bound to the *opacity* scale.
#' @param strokeWidth The stroke-width; a constant number in pixels, or a channel.
#' @param symbol The categorical symbol; either a channel or a constant.
#' @param target The target; a constant string specifying the target window (_e.g._, *_blank*) for clickable links; used in conjunction with the **href** option.
#' @param tip Whether to generate a tooltip for this mark, and any tip options.
#' @param title The title; a channel specifying accessible, short textual descriptions as strings (possibly with newlines).
#' @param x The horizontal position (or length) channel, typically bound to the *x* scale.
#' @param y The vertical position channel, typically bound to the *y* scale; defaults to the zero-based index of the data 0, 1, 2, ….
#' @param z An optional ordinal channel for grouping data into (possibly stacked) series to be drawn as separate areas; defaults to **fill** if a channel, or **stroke** if a channel.
#' @param ... Additional options or plot-level attributes.
#' @param data_from The name of the data source this mark reads from (see [vg_data()]).
#' @param filter_by A Param/Selection (e.g. from [param()]) to filter this mark's data by.
#' @family mark functions
#' @export
vg_dot_x <- function(spec = NULL, ariaDescription = vg_unset, ariaHidden = vg_unset, ariaLabel = vg_unset, channels = vg_unset, clip = vg_unset, dx = vg_unset, dy = vg_unset, facet = vg_unset, facetAnchor = vg_unset, fill = vg_unset, fillOpacity = vg_unset, filter = vg_unset, frameAnchor = vg_unset, fx = vg_unset, fy = vg_unset, href = vg_unset, imageFilter = vg_unset, interval = vg_unset, margin = vg_unset, marginBottom = vg_unset, marginLeft = vg_unset, marginRight = vg_unset, marginTop = vg_unset, mixBlendMode = vg_unset, opacity = vg_unset, paintOrder = vg_unset, pointerEvents = vg_unset, r = vg_unset, reverse = vg_unset, rotate = vg_unset, select = vg_unset, shapeRendering = vg_unset, sort = vg_unset, stroke = vg_unset, strokeDasharray = vg_unset, strokeDashoffset = vg_unset, strokeLinecap = vg_unset, strokeLinejoin = vg_unset, strokeMiterlimit = vg_unset, strokeOpacity = vg_unset, strokeWidth = vg_unset, symbol = vg_unset, target = vg_unset, tip = vg_unset, title = vg_unset, x = vg_unset, y = vg_unset, z = vg_unset, ..., data_from = vg_unset, filter_by = vg_unset) {
  vg_mark_(spec, "dotX", ariaDescription = ariaDescription, ariaHidden = ariaHidden, ariaLabel = ariaLabel, channels = channels, clip = clip, dx = dx, dy = dy, facet = facet, facetAnchor = facetAnchor, fill = fill, fillOpacity = fillOpacity, filter = filter, frameAnchor = frameAnchor, fx = fx, fy = fy, href = href, imageFilter = imageFilter, interval = interval, margin = margin, marginBottom = marginBottom, marginLeft = marginLeft, marginRight = marginRight, marginTop = marginTop, mixBlendMode = mixBlendMode, opacity = opacity, paintOrder = paintOrder, pointerEvents = pointerEvents, r = r, reverse = reverse, rotate = rotate, select = select, shapeRendering = shapeRendering, sort = sort, stroke = stroke, strokeDasharray = strokeDasharray, strokeDashoffset = strokeDashoffset, strokeLinecap = strokeLinecap, strokeLinejoin = strokeLinejoin, strokeMiterlimit = strokeMiterlimit, strokeOpacity = strokeOpacity, strokeWidth = strokeWidth, symbol = symbol, target = target, tip = tip, title = title, x = x, y = y, z = z, ..., data_from = data_from, filter_by = filter_by)
}

#' The dotY mark.
#'
#' @param spec A plot fragment or `vgspec` to add this mark to, or `NULL` to start a new plot with just this mark.
#' @param ariaDescription The aria-description; a constant textual description.
#' @param ariaHidden The aria-hidden state; a constant indicating whether the element is exposed to an accessibility API.
#' @param ariaLabel The aria-label; a channel specifying short textual labels representing the value in the accessibility tree.
#' @param channels Additional named channels, for example to include in a tooltip.
#' @param clip How to clip the mark; one of: - *frame* or true - clip to the plot’s frame (inner area) - *sphere* - clip to the projected sphere (*e.g.*, front hemisphere) - null or false - do not clip The *sphere* clip option requires a geographic projection.
#' @param dx The horizontal offset in pixels; a constant option.
#' @param dy The vertical offset in pixels; a constant option.
#' @param facet Whether to enable or disable faceting; one of: - *auto* (default) - automatically determine if this mark should be faceted - *include* (or true) - draw the subset of the mark’s data in the current facet - *exclude* - draw the subset of the mark’s data *not* in the current facet - *super* - draw this mark in a single frame that covers all facets - null (or false) - repeat this mark’s data across all facets (*i.e.*, no faceting) When a mark uses *super* faceting, it is not allowed to use position scales (*x*, *y*, *fx*, or *fy*); *super* faceting is intended for decorations, such as labels and legends.
#' @param facetAnchor How to place the mark with respect to facets; one of: - null (default for most marks) - display the mark in each non-empty facet - *top*, *right*, *bottom*, or *left* - display the mark only in facets on the given side - *top-empty*, *right-empty*, *bottom-empty*, or *left-empty* (default for axis marks) - display the mark only in facets that have empty space on the given side: either the margin, or an empty facet - *empty* - display the mark in empty facets only
#' @param fill The fill; a constant CSS color string, or a channel typically bound to the *color* scale.
#' @param fillOpacity The fill-opacity; a constant number between 0 and 1, or a channel typically bound to the *opacity* scale.
#' @param filter Applies a transform to filter the mark’s index according to the given channel values; only truthy values are retained.
#' @param frameAnchor The frame anchor specifies defaults for **x** and **y**, along with **textAnchor** and **lineAnchor**, based on the plot’s frame; it may be one of the four sides (*top*, *right*, *bottom*, *left*), one of the four corners (*top-left*, *top-right*, *bottom-right*, *bottom-left*), or the *middle* of the frame.
#' @param fx The horizontal facet position channel, for mark-level faceting, bound to the *fx* scale.
#' @param fy The vertical facet position channel, for mark-level faceting, bound to the *fy* scale.
#' @param href The href; a channel specifying URLs for clickable links.
#' @param imageFilter A CSS filter; a constant string used to adjust the rendering of images, such as *blur(5px)*.
#' @param interval Enforces uniformity for data at regular intervals, such as integer values or daily samples.
#' @param margin Shorthand to set the same default for all four mark margins: **marginTop**, **marginRight**, **marginBottom**, and **marginLeft**; typically defaults to 0, except for axis marks.
#' @param marginBottom The mark’s bottom margin; the minimum distance in pixels between the bottom edges of the inner and outer plot area.
#' @param marginLeft The mark’s left margin; the minimum distance in pixels between the left edges of the inner and outer plot area.
#' @param marginRight The mark’s right margin; the minimum distance in pixels between the right edges of the mark’s inner and outer plot area.
#' @param marginTop The mark’s top margin; the minimum distance in pixels between the top edges of the inner and outer plot area.
#' @param mixBlendMode The mix-blend-mode; a constant string specifying how to blend content such as *multiply*.
#' @param opacity The opacity; a constant between 0 and 1, or a channel typically bound to the *opacity* scale.
#' @param paintOrder The paint-order; a constant string specifying the order in which the **fill**, **stroke**, and any markers are drawn; defaults to *normal*, which draws the fill, then stroke, then markers; defaults to *stroke* for the text mark to create a “halo” around text to improve legibility.
#' @param pointerEvents The pointer-events property; a constant string such as *none*.
#' @param r The radius of dots; either a channel or constant.
#' @param reverse Applies a transform to reverse the order of the mark’s index, say for reverse input order.
#' @param rotate The rotation angle in degrees clockwise; a constant or a channel; defaults to 0°.
#' @param select Applies a filter transform after data is loaded to highlight selected values only.
#' @param shapeRendering The shape-rendering; a constant string such as *crispEdges*.
#' @param sort Either applies a transform to sort the mark’s index by the specified channel values, or imputes ordinal scale domains from this mark’s channels.
#' @param stroke The stroke; a constant CSS color string, or a channel typically bound to the *color* scale.
#' @param strokeDasharray The stroke-dasharray; a constant number indicating the length in pixels of alternating dashes and gaps, or a constant string of numbers separated by spaces or commas (_e.g._, *10 2* for dashes of 10 pixels separated by gaps of 2 pixels), or *none* (the default) for no dashing : https://developer.mozilla.org/en-US/docs/Web/SVG/Attribute/stroke-dasharray
#' @param strokeDashoffset The stroke-dashoffset; a constant indicating the offset in pixels of the first dash along the stroke; defaults to zero.
#' @param strokeLinecap The stroke-linecap; a constant specifying how to cap stroked paths, such as *butt*, *round*, or *square*.
#' @param strokeLinejoin The stroke-linejoin; a constant specifying how to join stroked paths, such as *bevel*, *miter*, *miter-clip*, or *round*.
#' @param strokeMiterlimit The stroke-miterlimit; a constant number specifying how to limit the length of *miter* joins on stroked paths.
#' @param strokeOpacity The stroke-opacity; a constant between 0 and 1, or a channel typically bound to the *opacity* scale.
#' @param strokeWidth The stroke-width; a constant number in pixels, or a channel.
#' @param symbol The categorical symbol; either a channel or a constant.
#' @param target The target; a constant string specifying the target window (_e.g._, *_blank*) for clickable links; used in conjunction with the **href** option.
#' @param tip Whether to generate a tooltip for this mark, and any tip options.
#' @param title The title; a channel specifying accessible, short textual descriptions as strings (possibly with newlines).
#' @param x The horizontal position (or length) channel, typically bound to the *x* scale.
#' @param y The vertical position channel, typically bound to the *y* scale; defaults to the zero-based index of the data 0, 1, 2, ….
#' @param z An optional ordinal channel for grouping data into (possibly stacked) series to be drawn as separate areas; defaults to **fill** if a channel, or **stroke** if a channel.
#' @param ... Additional options or plot-level attributes.
#' @param data_from The name of the data source this mark reads from (see [vg_data()]).
#' @param filter_by A Param/Selection (e.g. from [param()]) to filter this mark's data by.
#' @family mark functions
#' @export
vg_dot_y <- function(spec = NULL, ariaDescription = vg_unset, ariaHidden = vg_unset, ariaLabel = vg_unset, channels = vg_unset, clip = vg_unset, dx = vg_unset, dy = vg_unset, facet = vg_unset, facetAnchor = vg_unset, fill = vg_unset, fillOpacity = vg_unset, filter = vg_unset, frameAnchor = vg_unset, fx = vg_unset, fy = vg_unset, href = vg_unset, imageFilter = vg_unset, interval = vg_unset, margin = vg_unset, marginBottom = vg_unset, marginLeft = vg_unset, marginRight = vg_unset, marginTop = vg_unset, mixBlendMode = vg_unset, opacity = vg_unset, paintOrder = vg_unset, pointerEvents = vg_unset, r = vg_unset, reverse = vg_unset, rotate = vg_unset, select = vg_unset, shapeRendering = vg_unset, sort = vg_unset, stroke = vg_unset, strokeDasharray = vg_unset, strokeDashoffset = vg_unset, strokeLinecap = vg_unset, strokeLinejoin = vg_unset, strokeMiterlimit = vg_unset, strokeOpacity = vg_unset, strokeWidth = vg_unset, symbol = vg_unset, target = vg_unset, tip = vg_unset, title = vg_unset, x = vg_unset, y = vg_unset, z = vg_unset, ..., data_from = vg_unset, filter_by = vg_unset) {
  vg_mark_(spec, "dotY", ariaDescription = ariaDescription, ariaHidden = ariaHidden, ariaLabel = ariaLabel, channels = channels, clip = clip, dx = dx, dy = dy, facet = facet, facetAnchor = facetAnchor, fill = fill, fillOpacity = fillOpacity, filter = filter, frameAnchor = frameAnchor, fx = fx, fy = fy, href = href, imageFilter = imageFilter, interval = interval, margin = margin, marginBottom = marginBottom, marginLeft = marginLeft, marginRight = marginRight, marginTop = marginTop, mixBlendMode = mixBlendMode, opacity = opacity, paintOrder = paintOrder, pointerEvents = pointerEvents, r = r, reverse = reverse, rotate = rotate, select = select, shapeRendering = shapeRendering, sort = sort, stroke = stroke, strokeDasharray = strokeDasharray, strokeDashoffset = strokeDashoffset, strokeLinecap = strokeLinecap, strokeLinejoin = strokeLinejoin, strokeMiterlimit = strokeMiterlimit, strokeOpacity = strokeOpacity, strokeWidth = strokeWidth, symbol = symbol, target = target, tip = tip, title = title, x = x, y = y, z = z, ..., data_from = data_from, filter_by = filter_by)
}

#' The errorbarX mark.
#'
#' @param spec A plot fragment or `vgspec` to add this mark to, or `NULL` to start a new plot with just this mark.
#' @param ariaDescription The aria-description; a constant textual description.
#' @param ariaHidden The aria-hidden state; a constant indicating whether the element is exposed to an accessibility API.
#' @param ariaLabel The aria-label; a channel specifying short textual labels representing the value in the accessibility tree.
#' @param channels Additional named channels, for example to include in a tooltip.
#' @param ci The confidence interval in (0, 1); defaults to 0.95.
#' @param clip How to clip the mark; one of: - *frame* or true - clip to the plot’s frame (inner area) - *sphere* - clip to the projected sphere (*e.g.*, front hemisphere) - null or false - do not clip The *sphere* clip option requires a geographic projection.
#' @param dx The horizontal offset in pixels; a constant option.
#' @param dy The vertical offset in pixels; a constant option.
#' @param facet Whether to enable or disable faceting; one of: - *auto* (default) - automatically determine if this mark should be faceted - *include* (or true) - draw the subset of the mark’s data in the current facet - *exclude* - draw the subset of the mark’s data *not* in the current facet - *super* - draw this mark in a single frame that covers all facets - null (or false) - repeat this mark’s data across all facets (*i.e.*, no faceting) When a mark uses *super* faceting, it is not allowed to use position scales (*x*, *y*, *fx*, or *fy*); *super* faceting is intended for decorations, such as labels and legends.
#' @param facetAnchor How to place the mark with respect to facets; one of: - null (default for most marks) - display the mark in each non-empty facet - *top*, *right*, *bottom*, or *left* - display the mark only in facets on the given side - *top-empty*, *right-empty*, *bottom-empty*, or *left-empty* (default for axis marks) - display the mark only in facets that have empty space on the given side: either the margin, or an empty facet - *empty* - display the mark in empty facets only
#' @param fill The fill; a constant CSS color string, or a channel typically bound to the *color* scale.
#' @param fillOpacity The fill-opacity; a constant number between 0 and 1, or a channel typically bound to the *opacity* scale.
#' @param filter Applies a transform to filter the mark’s index according to the given channel values; only truthy values are retained.
#' @param fx The horizontal facet position channel, for mark-level faceting, bound to the *fx* scale.
#' @param fy The vertical facet position channel, for mark-level faceting, bound to the *fy* scale.
#' @param href The href; a channel specifying URLs for clickable links.
#' @param imageFilter A CSS filter; a constant string used to adjust the rendering of images, such as *blur(5px)*.
#' @param margin Shorthand to set the same default for all four mark margins: **marginTop**, **marginRight**, **marginBottom**, and **marginLeft**; typically defaults to 0, except for axis marks.
#' @param marginBottom The mark’s bottom margin; the minimum distance in pixels between the bottom edges of the inner and outer plot area.
#' @param marginLeft The mark’s left margin; the minimum distance in pixels between the left edges of the inner and outer plot area.
#' @param marginRight The mark’s right margin; the minimum distance in pixels between the right edges of the mark’s inner and outer plot area.
#' @param marginTop The mark’s top margin; the minimum distance in pixels between the top edges of the inner and outer plot area.
#' @param marker Shorthand to set the same default for markerStart, markerMid, and markerEnd; one of: - a marker name such as *arrow* or *circle* - *none* (default) - no marker * true - alias for *circle-fill* * false or null - alias for *none*
#' @param markerEnd The marker for the ending point of a line segment; one of: - a marker name such as *arrow* or *circle* * *none* (default) - no marker * true - alias for *circle-fill* * false or null - alias for *none*
#' @param markerMid The marker for any middle (interior) points of a line segment.
#' @param markerStart The marker for the starting point of a line segment; one of: - a marker name such as *arrow* or *circle* * *none* (default) - no marker * true - alias for *circle-fill* * false or null - alias for *none*
#' @param mixBlendMode The mix-blend-mode; a constant string specifying how to blend content such as *multiply*.
#' @param opacity The opacity; a constant between 0 and 1, or a channel typically bound to the *opacity* scale.
#' @param paintOrder The paint-order; a constant string specifying the order in which the **fill**, **stroke**, and any markers are drawn; defaults to *normal*, which draws the fill, then stroke, then markers; defaults to *stroke* for the text mark to create a “halo” around text to improve legibility.
#' @param pointerEvents The pointer-events property; a constant string such as *none*.
#' @param reverse Applies a transform to reverse the order of the mark’s index, say for reverse input order.
#' @param select Applies a filter transform after data is loaded to highlight selected values only.
#' @param shapeRendering The shape-rendering; a constant string such as *crispEdges*.
#' @param sort Either applies a transform to sort the mark’s index by the specified channel values, or imputes ordinal scale domains from this mark’s channels.
#' @param stroke The stroke; a constant CSS color string, or a channel typically bound to the *color* scale.
#' @param strokeDasharray The stroke-dasharray; a constant number indicating the length in pixels of alternating dashes and gaps, or a constant string of numbers separated by spaces or commas (_e.g._, *10 2* for dashes of 10 pixels separated by gaps of 2 pixels), or *none* (the default) for no dashing : https://developer.mozilla.org/en-US/docs/Web/SVG/Attribute/stroke-dasharray
#' @param strokeDashoffset The stroke-dashoffset; a constant indicating the offset in pixels of the first dash along the stroke; defaults to zero.
#' @param strokeLinecap The stroke-linecap; a constant specifying how to cap stroked paths, such as *butt*, *round*, or *square*.
#' @param strokeLinejoin The stroke-linejoin; a constant specifying how to join stroked paths, such as *bevel*, *miter*, *miter-clip*, or *round*.
#' @param strokeMiterlimit The stroke-miterlimit; a constant number specifying how to limit the length of *miter* joins on stroked paths.
#' @param strokeOpacity The stroke-opacity; a constant between 0 and 1, or a channel typically bound to the *opacity* scale.
#' @param strokeWidth The stroke-width; a constant number in pixels, or a channel.
#' @param target The target; a constant string specifying the target window (_e.g._, *_blank*) for clickable links; used in conjunction with the **href** option.
#' @param tip Whether to generate a tooltip for this mark, and any tip options.
#' @param title The title; a channel specifying accessible, short textual descriptions as strings (possibly with newlines).
#' @param x The horizontal position (or length) channel, typically bound to the *x* scale.
#' @param y The vertical position channel, typically bound to the *y* scale; defaults to the zero-based index of the data 0, 1, 2, ….
#' @param z An optional ordinal channel for grouping data into (possibly stacked) series to be drawn as separate areas; defaults to **fill** if a channel, or **stroke** if a channel.
#' @param ... Additional options or plot-level attributes.
#' @param data_from The name of the data source this mark reads from (see [vg_data()]).
#' @param filter_by A Param/Selection (e.g. from [param()]) to filter this mark's data by.
#' @family mark functions
#' @export
vg_errorbar_x <- function(spec = NULL, ariaDescription = vg_unset, ariaHidden = vg_unset, ariaLabel = vg_unset, channels = vg_unset, ci = vg_unset, clip = vg_unset, dx = vg_unset, dy = vg_unset, facet = vg_unset, facetAnchor = vg_unset, fill = vg_unset, fillOpacity = vg_unset, filter = vg_unset, fx = vg_unset, fy = vg_unset, href = vg_unset, imageFilter = vg_unset, margin = vg_unset, marginBottom = vg_unset, marginLeft = vg_unset, marginRight = vg_unset, marginTop = vg_unset, marker = vg_unset, markerEnd = vg_unset, markerMid = vg_unset, markerStart = vg_unset, mixBlendMode = vg_unset, opacity = vg_unset, paintOrder = vg_unset, pointerEvents = vg_unset, reverse = vg_unset, select = vg_unset, shapeRendering = vg_unset, sort = vg_unset, stroke = vg_unset, strokeDasharray = vg_unset, strokeDashoffset = vg_unset, strokeLinecap = vg_unset, strokeLinejoin = vg_unset, strokeMiterlimit = vg_unset, strokeOpacity = vg_unset, strokeWidth = vg_unset, target = vg_unset, tip = vg_unset, title = vg_unset, x = vg_unset, y = vg_unset, z = vg_unset, ..., data_from = vg_unset, filter_by = vg_unset) {
  vg_mark_(spec, "errorbarX", ariaDescription = ariaDescription, ariaHidden = ariaHidden, ariaLabel = ariaLabel, channels = channels, ci = ci, clip = clip, dx = dx, dy = dy, facet = facet, facetAnchor = facetAnchor, fill = fill, fillOpacity = fillOpacity, filter = filter, fx = fx, fy = fy, href = href, imageFilter = imageFilter, margin = margin, marginBottom = marginBottom, marginLeft = marginLeft, marginRight = marginRight, marginTop = marginTop, marker = marker, markerEnd = markerEnd, markerMid = markerMid, markerStart = markerStart, mixBlendMode = mixBlendMode, opacity = opacity, paintOrder = paintOrder, pointerEvents = pointerEvents, reverse = reverse, select = select, shapeRendering = shapeRendering, sort = sort, stroke = stroke, strokeDasharray = strokeDasharray, strokeDashoffset = strokeDashoffset, strokeLinecap = strokeLinecap, strokeLinejoin = strokeLinejoin, strokeMiterlimit = strokeMiterlimit, strokeOpacity = strokeOpacity, strokeWidth = strokeWidth, target = target, tip = tip, title = title, x = x, y = y, z = z, ..., data_from = data_from, filter_by = filter_by)
}

#' The errorbarY mark.
#'
#' @param spec A plot fragment or `vgspec` to add this mark to, or `NULL` to start a new plot with just this mark.
#' @param ariaDescription The aria-description; a constant textual description.
#' @param ariaHidden The aria-hidden state; a constant indicating whether the element is exposed to an accessibility API.
#' @param ariaLabel The aria-label; a channel specifying short textual labels representing the value in the accessibility tree.
#' @param channels Additional named channels, for example to include in a tooltip.
#' @param ci The confidence interval in (0, 1); defaults to 0.95.
#' @param clip How to clip the mark; one of: - *frame* or true - clip to the plot’s frame (inner area) - *sphere* - clip to the projected sphere (*e.g.*, front hemisphere) - null or false - do not clip The *sphere* clip option requires a geographic projection.
#' @param dx The horizontal offset in pixels; a constant option.
#' @param dy The vertical offset in pixels; a constant option.
#' @param facet Whether to enable or disable faceting; one of: - *auto* (default) - automatically determine if this mark should be faceted - *include* (or true) - draw the subset of the mark’s data in the current facet - *exclude* - draw the subset of the mark’s data *not* in the current facet - *super* - draw this mark in a single frame that covers all facets - null (or false) - repeat this mark’s data across all facets (*i.e.*, no faceting) When a mark uses *super* faceting, it is not allowed to use position scales (*x*, *y*, *fx*, or *fy*); *super* faceting is intended for decorations, such as labels and legends.
#' @param facetAnchor How to place the mark with respect to facets; one of: - null (default for most marks) - display the mark in each non-empty facet - *top*, *right*, *bottom*, or *left* - display the mark only in facets on the given side - *top-empty*, *right-empty*, *bottom-empty*, or *left-empty* (default for axis marks) - display the mark only in facets that have empty space on the given side: either the margin, or an empty facet - *empty* - display the mark in empty facets only
#' @param fill The fill; a constant CSS color string, or a channel typically bound to the *color* scale.
#' @param fillOpacity The fill-opacity; a constant number between 0 and 1, or a channel typically bound to the *opacity* scale.
#' @param filter Applies a transform to filter the mark’s index according to the given channel values; only truthy values are retained.
#' @param fx The horizontal facet position channel, for mark-level faceting, bound to the *fx* scale.
#' @param fy The vertical facet position channel, for mark-level faceting, bound to the *fy* scale.
#' @param href The href; a channel specifying URLs for clickable links.
#' @param imageFilter A CSS filter; a constant string used to adjust the rendering of images, such as *blur(5px)*.
#' @param margin Shorthand to set the same default for all four mark margins: **marginTop**, **marginRight**, **marginBottom**, and **marginLeft**; typically defaults to 0, except for axis marks.
#' @param marginBottom The mark’s bottom margin; the minimum distance in pixels between the bottom edges of the inner and outer plot area.
#' @param marginLeft The mark’s left margin; the minimum distance in pixels between the left edges of the inner and outer plot area.
#' @param marginRight The mark’s right margin; the minimum distance in pixels between the right edges of the mark’s inner and outer plot area.
#' @param marginTop The mark’s top margin; the minimum distance in pixels between the top edges of the inner and outer plot area.
#' @param marker Shorthand to set the same default for markerStart, markerMid, and markerEnd; one of: - a marker name such as *arrow* or *circle* - *none* (default) - no marker * true - alias for *circle-fill* * false or null - alias for *none*
#' @param markerEnd The marker for the ending point of a line segment; one of: - a marker name such as *arrow* or *circle* * *none* (default) - no marker * true - alias for *circle-fill* * false or null - alias for *none*
#' @param markerMid The marker for any middle (interior) points of a line segment.
#' @param markerStart The marker for the starting point of a line segment; one of: - a marker name such as *arrow* or *circle* * *none* (default) - no marker * true - alias for *circle-fill* * false or null - alias for *none*
#' @param mixBlendMode The mix-blend-mode; a constant string specifying how to blend content such as *multiply*.
#' @param opacity The opacity; a constant between 0 and 1, or a channel typically bound to the *opacity* scale.
#' @param paintOrder The paint-order; a constant string specifying the order in which the **fill**, **stroke**, and any markers are drawn; defaults to *normal*, which draws the fill, then stroke, then markers; defaults to *stroke* for the text mark to create a “halo” around text to improve legibility.
#' @param pointerEvents The pointer-events property; a constant string such as *none*.
#' @param reverse Applies a transform to reverse the order of the mark’s index, say for reverse input order.
#' @param select Applies a filter transform after data is loaded to highlight selected values only.
#' @param shapeRendering The shape-rendering; a constant string such as *crispEdges*.
#' @param sort Either applies a transform to sort the mark’s index by the specified channel values, or imputes ordinal scale domains from this mark’s channels.
#' @param stroke The stroke; a constant CSS color string, or a channel typically bound to the *color* scale.
#' @param strokeDasharray The stroke-dasharray; a constant number indicating the length in pixels of alternating dashes and gaps, or a constant string of numbers separated by spaces or commas (_e.g._, *10 2* for dashes of 10 pixels separated by gaps of 2 pixels), or *none* (the default) for no dashing : https://developer.mozilla.org/en-US/docs/Web/SVG/Attribute/stroke-dasharray
#' @param strokeDashoffset The stroke-dashoffset; a constant indicating the offset in pixels of the first dash along the stroke; defaults to zero.
#' @param strokeLinecap The stroke-linecap; a constant specifying how to cap stroked paths, such as *butt*, *round*, or *square*.
#' @param strokeLinejoin The stroke-linejoin; a constant specifying how to join stroked paths, such as *bevel*, *miter*, *miter-clip*, or *round*.
#' @param strokeMiterlimit The stroke-miterlimit; a constant number specifying how to limit the length of *miter* joins on stroked paths.
#' @param strokeOpacity The stroke-opacity; a constant between 0 and 1, or a channel typically bound to the *opacity* scale.
#' @param strokeWidth The stroke-width; a constant number in pixels, or a channel.
#' @param target The target; a constant string specifying the target window (_e.g._, *_blank*) for clickable links; used in conjunction with the **href** option.
#' @param tip Whether to generate a tooltip for this mark, and any tip options.
#' @param title The title; a channel specifying accessible, short textual descriptions as strings (possibly with newlines).
#' @param x The horizontal position (or length) channel, typically bound to the *x* scale.
#' @param y The vertical position channel, typically bound to the *y* scale; defaults to the zero-based index of the data 0, 1, 2, ….
#' @param z An optional ordinal channel for grouping data into (possibly stacked) series to be drawn as separate areas; defaults to **fill** if a channel, or **stroke** if a channel.
#' @param ... Additional options or plot-level attributes.
#' @param data_from The name of the data source this mark reads from (see [vg_data()]).
#' @param filter_by A Param/Selection (e.g. from [param()]) to filter this mark's data by.
#' @family mark functions
#' @export
vg_errorbar_y <- function(spec = NULL, ariaDescription = vg_unset, ariaHidden = vg_unset, ariaLabel = vg_unset, channels = vg_unset, ci = vg_unset, clip = vg_unset, dx = vg_unset, dy = vg_unset, facet = vg_unset, facetAnchor = vg_unset, fill = vg_unset, fillOpacity = vg_unset, filter = vg_unset, fx = vg_unset, fy = vg_unset, href = vg_unset, imageFilter = vg_unset, margin = vg_unset, marginBottom = vg_unset, marginLeft = vg_unset, marginRight = vg_unset, marginTop = vg_unset, marker = vg_unset, markerEnd = vg_unset, markerMid = vg_unset, markerStart = vg_unset, mixBlendMode = vg_unset, opacity = vg_unset, paintOrder = vg_unset, pointerEvents = vg_unset, reverse = vg_unset, select = vg_unset, shapeRendering = vg_unset, sort = vg_unset, stroke = vg_unset, strokeDasharray = vg_unset, strokeDashoffset = vg_unset, strokeLinecap = vg_unset, strokeLinejoin = vg_unset, strokeMiterlimit = vg_unset, strokeOpacity = vg_unset, strokeWidth = vg_unset, target = vg_unset, tip = vg_unset, title = vg_unset, x = vg_unset, y = vg_unset, z = vg_unset, ..., data_from = vg_unset, filter_by = vg_unset) {
  vg_mark_(spec, "errorbarY", ariaDescription = ariaDescription, ariaHidden = ariaHidden, ariaLabel = ariaLabel, channels = channels, ci = ci, clip = clip, dx = dx, dy = dy, facet = facet, facetAnchor = facetAnchor, fill = fill, fillOpacity = fillOpacity, filter = filter, fx = fx, fy = fy, href = href, imageFilter = imageFilter, margin = margin, marginBottom = marginBottom, marginLeft = marginLeft, marginRight = marginRight, marginTop = marginTop, marker = marker, markerEnd = markerEnd, markerMid = markerMid, markerStart = markerStart, mixBlendMode = mixBlendMode, opacity = opacity, paintOrder = paintOrder, pointerEvents = pointerEvents, reverse = reverse, select = select, shapeRendering = shapeRendering, sort = sort, stroke = stroke, strokeDasharray = strokeDasharray, strokeDashoffset = strokeDashoffset, strokeLinecap = strokeLinecap, strokeLinejoin = strokeLinejoin, strokeMiterlimit = strokeMiterlimit, strokeOpacity = strokeOpacity, strokeWidth = strokeWidth, target = target, tip = tip, title = title, x = x, y = y, z = z, ..., data_from = data_from, filter_by = filter_by)
}

#' The frame mark.
#'
#' @param spec A plot fragment or `vgspec` to add this mark to, or `NULL` to start a new plot with just this mark.
#' @param anchor The side of the frame on which to place the axis: *top* or *bottom* for horizontal axes (axisX and axisFx) and their associated vertical grids (gridX and gridFx), or *left* or *right* for vertical axes (axisY and axisFY) and their associated horizontal grids (gridY and gridFy).
#' @param ariaDescription The aria-description; a constant textual description.
#' @param ariaHidden The aria-hidden state; a constant indicating whether the element is exposed to an accessibility API.
#' @param ariaLabel The aria-label; a channel specifying short textual labels representing the value in the accessibility tree.
#' @param channels Additional named channels, for example to include in a tooltip.
#' @param clip How to clip the mark; one of: - *frame* or true - clip to the plot’s frame (inner area) - *sphere* - clip to the projected sphere (*e.g.*, front hemisphere) - null or false - do not clip The *sphere* clip option requires a geographic projection.
#' @param dx The horizontal offset in pixels; a constant option.
#' @param dy The vertical offset in pixels; a constant option.
#' @param facet Whether to enable or disable faceting; one of: - *auto* (default) - automatically determine if this mark should be faceted - *include* (or true) - draw the subset of the mark’s data in the current facet - *exclude* - draw the subset of the mark’s data *not* in the current facet - *super* - draw this mark in a single frame that covers all facets - null (or false) - repeat this mark’s data across all facets (*i.e.*, no faceting) When a mark uses *super* faceting, it is not allowed to use position scales (*x*, *y*, *fx*, or *fy*); *super* faceting is intended for decorations, such as labels and legends.
#' @param facetAnchor How to place the mark with respect to facets; one of: - null (default for most marks) - display the mark in each non-empty facet - *top*, *right*, *bottom*, or *left* - display the mark only in facets on the given side - *top-empty*, *right-empty*, *bottom-empty*, or *left-empty* (default for axis marks) - display the mark only in facets that have empty space on the given side: either the margin, or an empty facet - *empty* - display the mark in empty facets only
#' @param fill The fill; a constant CSS color string, or a channel typically bound to the *color* scale.
#' @param fillOpacity The fill-opacity; a constant number between 0 and 1, or a channel typically bound to the *opacity* scale.
#' @param filter Applies a transform to filter the mark’s index according to the given channel values; only truthy values are retained.
#' @param fx The horizontal facet position channel, for mark-level faceting, bound to the *fx* scale.
#' @param fy The vertical facet position channel, for mark-level faceting, bound to the *fy* scale.
#' @param href The href; a channel specifying URLs for clickable links.
#' @param imageFilter A CSS filter; a constant string used to adjust the rendering of images, such as *blur(5px)*.
#' @param inset Shorthand to set the same default for **insetStart** and **insetEnd**.
#' @param insetBottom Insets the bottom edge by the specified number of pixels.
#' @param insetLeft Insets the left edge by the specified number of pixels.
#' @param insetRight Insets the right edge by the specified number of pixels.
#' @param insetTop Insets the top edge by the specified number of pixels.
#' @param margin Shorthand to set the same default for all four mark margins: **marginTop**, **marginRight**, **marginBottom**, and **marginLeft**; typically defaults to 0, except for axis marks.
#' @param marginBottom The mark’s bottom margin; the minimum distance in pixels between the bottom edges of the inner and outer plot area.
#' @param marginLeft The mark’s left margin; the minimum distance in pixels between the left edges of the inner and outer plot area.
#' @param marginRight The mark’s right margin; the minimum distance in pixels between the right edges of the mark’s inner and outer plot area.
#' @param marginTop The mark’s top margin; the minimum distance in pixels between the top edges of the inner and outer plot area.
#' @param mixBlendMode The mix-blend-mode; a constant string specifying how to blend content such as *multiply*.
#' @param opacity The opacity; a constant between 0 and 1, or a channel typically bound to the *opacity* scale.
#' @param paintOrder The paint-order; a constant string specifying the order in which the **fill**, **stroke**, and any markers are drawn; defaults to *normal*, which draws the fill, then stroke, then markers; defaults to *stroke* for the text mark to create a “halo” around text to improve legibility.
#' @param pointerEvents The pointer-events property; a constant string such as *none*.
#' @param reverse Applies a transform to reverse the order of the mark’s index, say for reverse input order.
#' @param rx The rounded corner *x*-radius, either in pixels or as a percentage of the rect width.
#' @param ry The rounded corner *y*-radius, either in pixels or as a percentage of the rect height.
#' @param select Applies a filter transform after data is loaded to highlight selected values only.
#' @param shapeRendering The shape-rendering; a constant string such as *crispEdges*.
#' @param sort Either applies a transform to sort the mark’s index by the specified channel values, or imputes ordinal scale domains from this mark’s channels.
#' @param stroke The stroke; a constant CSS color string, or a channel typically bound to the *color* scale.
#' @param strokeDasharray The stroke-dasharray; a constant number indicating the length in pixels of alternating dashes and gaps, or a constant string of numbers separated by spaces or commas (_e.g._, *10 2* for dashes of 10 pixels separated by gaps of 2 pixels), or *none* (the default) for no dashing : https://developer.mozilla.org/en-US/docs/Web/SVG/Attribute/stroke-dasharray
#' @param strokeDashoffset The stroke-dashoffset; a constant indicating the offset in pixels of the first dash along the stroke; defaults to zero.
#' @param strokeLinecap The stroke-linecap; a constant specifying how to cap stroked paths, such as *butt*, *round*, or *square*.
#' @param strokeLinejoin The stroke-linejoin; a constant specifying how to join stroked paths, such as *bevel*, *miter*, *miter-clip*, or *round*.
#' @param strokeMiterlimit The stroke-miterlimit; a constant number specifying how to limit the length of *miter* joins on stroked paths.
#' @param strokeOpacity The stroke-opacity; a constant between 0 and 1, or a channel typically bound to the *opacity* scale.
#' @param strokeWidth The stroke-width; a constant number in pixels, or a channel.
#' @param target The target; a constant string specifying the target window (_e.g._, *_blank*) for clickable links; used in conjunction with the **href** option.
#' @param tip Whether to generate a tooltip for this mark, and any tip options.
#' @param title The title; a channel specifying accessible, short textual descriptions as strings (possibly with newlines).
#' @param ... Additional options or plot-level attributes.
#' @param data_from The name of the data source this mark reads from (see [vg_data()]).
#' @param filter_by A Param/Selection (e.g. from [param()]) to filter this mark's data by.
#' @family mark functions
#' @export
vg_frame <- function(spec = NULL, anchor = vg_unset, ariaDescription = vg_unset, ariaHidden = vg_unset, ariaLabel = vg_unset, channels = vg_unset, clip = vg_unset, dx = vg_unset, dy = vg_unset, facet = vg_unset, facetAnchor = vg_unset, fill = vg_unset, fillOpacity = vg_unset, filter = vg_unset, fx = vg_unset, fy = vg_unset, href = vg_unset, imageFilter = vg_unset, inset = vg_unset, insetBottom = vg_unset, insetLeft = vg_unset, insetRight = vg_unset, insetTop = vg_unset, margin = vg_unset, marginBottom = vg_unset, marginLeft = vg_unset, marginRight = vg_unset, marginTop = vg_unset, mixBlendMode = vg_unset, opacity = vg_unset, paintOrder = vg_unset, pointerEvents = vg_unset, reverse = vg_unset, rx = vg_unset, ry = vg_unset, select = vg_unset, shapeRendering = vg_unset, sort = vg_unset, stroke = vg_unset, strokeDasharray = vg_unset, strokeDashoffset = vg_unset, strokeLinecap = vg_unset, strokeLinejoin = vg_unset, strokeMiterlimit = vg_unset, strokeOpacity = vg_unset, strokeWidth = vg_unset, target = vg_unset, tip = vg_unset, title = vg_unset, ..., data_from = vg_unset, filter_by = vg_unset) {
  vg_mark_(spec, "frame", anchor = anchor, ariaDescription = ariaDescription, ariaHidden = ariaHidden, ariaLabel = ariaLabel, channels = channels, clip = clip, dx = dx, dy = dy, facet = facet, facetAnchor = facetAnchor, fill = fill, fillOpacity = fillOpacity, filter = filter, fx = fx, fy = fy, href = href, imageFilter = imageFilter, inset = inset, insetBottom = insetBottom, insetLeft = insetLeft, insetRight = insetRight, insetTop = insetTop, margin = margin, marginBottom = marginBottom, marginLeft = marginLeft, marginRight = marginRight, marginTop = marginTop, mixBlendMode = mixBlendMode, opacity = opacity, paintOrder = paintOrder, pointerEvents = pointerEvents, reverse = reverse, rx = rx, ry = ry, select = select, shapeRendering = shapeRendering, sort = sort, stroke = stroke, strokeDasharray = strokeDasharray, strokeDashoffset = strokeDashoffset, strokeLinecap = strokeLinecap, strokeLinejoin = strokeLinejoin, strokeMiterlimit = strokeMiterlimit, strokeOpacity = strokeOpacity, strokeWidth = strokeWidth, target = target, tip = tip, title = title, ..., data_from = data_from, filter_by = filter_by)
}

#' The geo mark.
#'
#' @param spec A plot fragment or `vgspec` to add this mark to, or `NULL` to start a new plot with just this mark.
#' @param ariaDescription The aria-description; a constant textual description.
#' @param ariaHidden The aria-hidden state; a constant indicating whether the element is exposed to an accessibility API.
#' @param ariaLabel The aria-label; a channel specifying short textual labels representing the value in the accessibility tree.
#' @param channels Additional named channels, for example to include in a tooltip.
#' @param clip How to clip the mark; one of: - *frame* or true - clip to the plot’s frame (inner area) - *sphere* - clip to the projected sphere (*e.g.*, front hemisphere) - null or false - do not clip The *sphere* clip option requires a geographic projection.
#' @param dx The horizontal offset in pixels; a constant option.
#' @param dy The vertical offset in pixels; a constant option.
#' @param facet Whether to enable or disable faceting; one of: - *auto* (default) - automatically determine if this mark should be faceted - *include* (or true) - draw the subset of the mark’s data in the current facet - *exclude* - draw the subset of the mark’s data *not* in the current facet - *super* - draw this mark in a single frame that covers all facets - null (or false) - repeat this mark’s data across all facets (*i.e.*, no faceting) When a mark uses *super* faceting, it is not allowed to use position scales (*x*, *y*, *fx*, or *fy*); *super* faceting is intended for decorations, such as labels and legends.
#' @param facetAnchor How to place the mark with respect to facets; one of: - null (default for most marks) - display the mark in each non-empty facet - *top*, *right*, *bottom*, or *left* - display the mark only in facets on the given side - *top-empty*, *right-empty*, *bottom-empty*, or *left-empty* (default for axis marks) - display the mark only in facets that have empty space on the given side: either the margin, or an empty facet - *empty* - display the mark in empty facets only
#' @param fill The fill; a constant CSS color string, or a channel typically bound to the *color* scale.
#' @param fillOpacity The fill-opacity; a constant number between 0 and 1, or a channel typically bound to the *opacity* scale.
#' @param filter Applies a transform to filter the mark’s index according to the given channel values; only truthy values are retained.
#' @param fx The horizontal facet position channel, for mark-level faceting, bound to the *fx* scale.
#' @param fy The vertical facet position channel, for mark-level faceting, bound to the *fy* scale.
#' @param geometry A required channel for the geometry to render; defaults to identity, assuming *data* is a GeoJSON object or an iterable of GeoJSON objects.
#' @param href The href; a channel specifying URLs for clickable links.
#' @param imageFilter A CSS filter; a constant string used to adjust the rendering of images, such as *blur(5px)*.
#' @param margin Shorthand to set the same default for all four mark margins: **marginTop**, **marginRight**, **marginBottom**, and **marginLeft**; typically defaults to 0, except for axis marks.
#' @param marginBottom The mark’s bottom margin; the minimum distance in pixels between the bottom edges of the inner and outer plot area.
#' @param marginLeft The mark’s left margin; the minimum distance in pixels between the left edges of the inner and outer plot area.
#' @param marginRight The mark’s right margin; the minimum distance in pixels between the right edges of the mark’s inner and outer plot area.
#' @param marginTop The mark’s top margin; the minimum distance in pixels between the top edges of the inner and outer plot area.
#' @param mixBlendMode The mix-blend-mode; a constant string specifying how to blend content such as *multiply*.
#' @param opacity The opacity; a constant between 0 and 1, or a channel typically bound to the *opacity* scale.
#' @param paintOrder The paint-order; a constant string specifying the order in which the **fill**, **stroke**, and any markers are drawn; defaults to *normal*, which draws the fill, then stroke, then markers; defaults to *stroke* for the text mark to create a “halo” around text to improve legibility.
#' @param pointerEvents The pointer-events property; a constant string such as *none*.
#' @param r The radius of dots; either a channel or constant.
#' @param reverse Applies a transform to reverse the order of the mark’s index, say for reverse input order.
#' @param select Applies a filter transform after data is loaded to highlight selected values only.
#' @param shapeRendering The shape-rendering; a constant string such as *crispEdges*.
#' @param sort Either applies a transform to sort the mark’s index by the specified channel values, or imputes ordinal scale domains from this mark’s channels.
#' @param stroke The stroke; a constant CSS color string, or a channel typically bound to the *color* scale.
#' @param strokeDasharray The stroke-dasharray; a constant number indicating the length in pixels of alternating dashes and gaps, or a constant string of numbers separated by spaces or commas (_e.g._, *10 2* for dashes of 10 pixels separated by gaps of 2 pixels), or *none* (the default) for no dashing : https://developer.mozilla.org/en-US/docs/Web/SVG/Attribute/stroke-dasharray
#' @param strokeDashoffset The stroke-dashoffset; a constant indicating the offset in pixels of the first dash along the stroke; defaults to zero.
#' @param strokeLinecap The stroke-linecap; a constant specifying how to cap stroked paths, such as *butt*, *round*, or *square*.
#' @param strokeLinejoin The stroke-linejoin; a constant specifying how to join stroked paths, such as *bevel*, *miter*, *miter-clip*, or *round*.
#' @param strokeMiterlimit The stroke-miterlimit; a constant number specifying how to limit the length of *miter* joins on stroked paths.
#' @param strokeOpacity The stroke-opacity; a constant between 0 and 1, or a channel typically bound to the *opacity* scale.
#' @param strokeWidth The stroke-width; a constant number in pixels, or a channel.
#' @param target The target; a constant string specifying the target window (_e.g._, *_blank*) for clickable links; used in conjunction with the **href** option.
#' @param tip Whether to generate a tooltip for this mark, and any tip options.
#' @param title The title; a channel specifying accessible, short textual descriptions as strings (possibly with newlines).
#' @param ... Additional options or plot-level attributes.
#' @param data_from The name of the data source this mark reads from (see [vg_data()]).
#' @param filter_by A Param/Selection (e.g. from [param()]) to filter this mark's data by.
#' @family mark functions
#' @export
vg_geo <- function(spec = NULL, ariaDescription = vg_unset, ariaHidden = vg_unset, ariaLabel = vg_unset, channels = vg_unset, clip = vg_unset, dx = vg_unset, dy = vg_unset, facet = vg_unset, facetAnchor = vg_unset, fill = vg_unset, fillOpacity = vg_unset, filter = vg_unset, fx = vg_unset, fy = vg_unset, geometry = vg_unset, href = vg_unset, imageFilter = vg_unset, margin = vg_unset, marginBottom = vg_unset, marginLeft = vg_unset, marginRight = vg_unset, marginTop = vg_unset, mixBlendMode = vg_unset, opacity = vg_unset, paintOrder = vg_unset, pointerEvents = vg_unset, r = vg_unset, reverse = vg_unset, select = vg_unset, shapeRendering = vg_unset, sort = vg_unset, stroke = vg_unset, strokeDasharray = vg_unset, strokeDashoffset = vg_unset, strokeLinecap = vg_unset, strokeLinejoin = vg_unset, strokeMiterlimit = vg_unset, strokeOpacity = vg_unset, strokeWidth = vg_unset, target = vg_unset, tip = vg_unset, title = vg_unset, ..., data_from = vg_unset, filter_by = vg_unset) {
  vg_mark_(spec, "geo", ariaDescription = ariaDescription, ariaHidden = ariaHidden, ariaLabel = ariaLabel, channels = channels, clip = clip, dx = dx, dy = dy, facet = facet, facetAnchor = facetAnchor, fill = fill, fillOpacity = fillOpacity, filter = filter, fx = fx, fy = fy, geometry = geometry, href = href, imageFilter = imageFilter, margin = margin, marginBottom = marginBottom, marginLeft = marginLeft, marginRight = marginRight, marginTop = marginTop, mixBlendMode = mixBlendMode, opacity = opacity, paintOrder = paintOrder, pointerEvents = pointerEvents, r = r, reverse = reverse, select = select, shapeRendering = shapeRendering, sort = sort, stroke = stroke, strokeDasharray = strokeDasharray, strokeDashoffset = strokeDashoffset, strokeLinecap = strokeLinecap, strokeLinejoin = strokeLinejoin, strokeMiterlimit = strokeMiterlimit, strokeOpacity = strokeOpacity, strokeWidth = strokeWidth, target = target, tip = tip, title = title, ..., data_from = data_from, filter_by = filter_by)
}

#' The graticule mark.
#'
#' @param spec A plot fragment or `vgspec` to add this mark to, or `NULL` to start a new plot with just this mark.
#' @param ariaDescription The aria-description; a constant textual description.
#' @param ariaHidden The aria-hidden state; a constant indicating whether the element is exposed to an accessibility API.
#' @param ariaLabel The aria-label; a channel specifying short textual labels representing the value in the accessibility tree.
#' @param channels Additional named channels, for example to include in a tooltip.
#' @param clip How to clip the mark; one of: - *frame* or true - clip to the plot’s frame (inner area) - *sphere* - clip to the projected sphere (*e.g.*, front hemisphere) - null or false - do not clip The *sphere* clip option requires a geographic projection.
#' @param dx The horizontal offset in pixels; a constant option.
#' @param dy The vertical offset in pixels; a constant option.
#' @param facet Whether to enable or disable faceting; one of: - *auto* (default) - automatically determine if this mark should be faceted - *include* (or true) - draw the subset of the mark’s data in the current facet - *exclude* - draw the subset of the mark’s data *not* in the current facet - *super* - draw this mark in a single frame that covers all facets - null (or false) - repeat this mark’s data across all facets (*i.e.*, no faceting) When a mark uses *super* faceting, it is not allowed to use position scales (*x*, *y*, *fx*, or *fy*); *super* faceting is intended for decorations, such as labels and legends.
#' @param facetAnchor How to place the mark with respect to facets; one of: - null (default for most marks) - display the mark in each non-empty facet - *top*, *right*, *bottom*, or *left* - display the mark only in facets on the given side - *top-empty*, *right-empty*, *bottom-empty*, or *left-empty* (default for axis marks) - display the mark only in facets that have empty space on the given side: either the margin, or an empty facet - *empty* - display the mark in empty facets only
#' @param fill The fill; a constant CSS color string, or a channel typically bound to the *color* scale.
#' @param fillOpacity The fill-opacity; a constant number between 0 and 1, or a channel typically bound to the *opacity* scale.
#' @param filter Applies a transform to filter the mark’s index according to the given channel values; only truthy values are retained.
#' @param fx The horizontal facet position channel, for mark-level faceting, bound to the *fx* scale.
#' @param fy The vertical facet position channel, for mark-level faceting, bound to the *fy* scale.
#' @param href The href; a channel specifying URLs for clickable links.
#' @param imageFilter A CSS filter; a constant string used to adjust the rendering of images, such as *blur(5px)*.
#' @param margin Shorthand to set the same default for all four mark margins: **marginTop**, **marginRight**, **marginBottom**, and **marginLeft**; typically defaults to 0, except for axis marks.
#' @param marginBottom The mark’s bottom margin; the minimum distance in pixels between the bottom edges of the inner and outer plot area.
#' @param marginLeft The mark’s left margin; the minimum distance in pixels between the left edges of the inner and outer plot area.
#' @param marginRight The mark’s right margin; the minimum distance in pixels between the right edges of the mark’s inner and outer plot area.
#' @param marginTop The mark’s top margin; the minimum distance in pixels between the top edges of the inner and outer plot area.
#' @param mixBlendMode The mix-blend-mode; a constant string specifying how to blend content such as *multiply*.
#' @param opacity The opacity; a constant between 0 and 1, or a channel typically bound to the *opacity* scale.
#' @param paintOrder The paint-order; a constant string specifying the order in which the **fill**, **stroke**, and any markers are drawn; defaults to *normal*, which draws the fill, then stroke, then markers; defaults to *stroke* for the text mark to create a “halo” around text to improve legibility.
#' @param pointerEvents The pointer-events property; a constant string such as *none*.
#' @param reverse Applies a transform to reverse the order of the mark’s index, say for reverse input order.
#' @param select Applies a filter transform after data is loaded to highlight selected values only.
#' @param shapeRendering The shape-rendering; a constant string such as *crispEdges*.
#' @param sort Either applies a transform to sort the mark’s index by the specified channel values, or imputes ordinal scale domains from this mark’s channels.
#' @param stroke The stroke; a constant CSS color string, or a channel typically bound to the *color* scale.
#' @param strokeDasharray The stroke-dasharray; a constant number indicating the length in pixels of alternating dashes and gaps, or a constant string of numbers separated by spaces or commas (_e.g._, *10 2* for dashes of 10 pixels separated by gaps of 2 pixels), or *none* (the default) for no dashing : https://developer.mozilla.org/en-US/docs/Web/SVG/Attribute/stroke-dasharray
#' @param strokeDashoffset The stroke-dashoffset; a constant indicating the offset in pixels of the first dash along the stroke; defaults to zero.
#' @param strokeLinecap The stroke-linecap; a constant specifying how to cap stroked paths, such as *butt*, *round*, or *square*.
#' @param strokeLinejoin The stroke-linejoin; a constant specifying how to join stroked paths, such as *bevel*, *miter*, *miter-clip*, or *round*.
#' @param strokeMiterlimit The stroke-miterlimit; a constant number specifying how to limit the length of *miter* joins on stroked paths.
#' @param strokeOpacity The stroke-opacity; a constant between 0 and 1, or a channel typically bound to the *opacity* scale.
#' @param strokeWidth The stroke-width; a constant number in pixels, or a channel.
#' @param target The target; a constant string specifying the target window (_e.g._, *_blank*) for clickable links; used in conjunction with the **href** option.
#' @param tip Whether to generate a tooltip for this mark, and any tip options.
#' @param title The title; a channel specifying accessible, short textual descriptions as strings (possibly with newlines).
#' @param ... Additional options or plot-level attributes.
#' @param data_from The name of the data source this mark reads from (see [vg_data()]).
#' @param filter_by A Param/Selection (e.g. from [param()]) to filter this mark's data by.
#' @family mark functions
#' @export
vg_graticule <- function(spec = NULL, ariaDescription = vg_unset, ariaHidden = vg_unset, ariaLabel = vg_unset, channels = vg_unset, clip = vg_unset, dx = vg_unset, dy = vg_unset, facet = vg_unset, facetAnchor = vg_unset, fill = vg_unset, fillOpacity = vg_unset, filter = vg_unset, fx = vg_unset, fy = vg_unset, href = vg_unset, imageFilter = vg_unset, margin = vg_unset, marginBottom = vg_unset, marginLeft = vg_unset, marginRight = vg_unset, marginTop = vg_unset, mixBlendMode = vg_unset, opacity = vg_unset, paintOrder = vg_unset, pointerEvents = vg_unset, reverse = vg_unset, select = vg_unset, shapeRendering = vg_unset, sort = vg_unset, stroke = vg_unset, strokeDasharray = vg_unset, strokeDashoffset = vg_unset, strokeLinecap = vg_unset, strokeLinejoin = vg_unset, strokeMiterlimit = vg_unset, strokeOpacity = vg_unset, strokeWidth = vg_unset, target = vg_unset, tip = vg_unset, title = vg_unset, ..., data_from = vg_unset, filter_by = vg_unset) {
  vg_mark_(spec, "graticule", ariaDescription = ariaDescription, ariaHidden = ariaHidden, ariaLabel = ariaLabel, channels = channels, clip = clip, dx = dx, dy = dy, facet = facet, facetAnchor = facetAnchor, fill = fill, fillOpacity = fillOpacity, filter = filter, fx = fx, fy = fy, href = href, imageFilter = imageFilter, margin = margin, marginBottom = marginBottom, marginLeft = marginLeft, marginRight = marginRight, marginTop = marginTop, mixBlendMode = mixBlendMode, opacity = opacity, paintOrder = paintOrder, pointerEvents = pointerEvents, reverse = reverse, select = select, shapeRendering = shapeRendering, sort = sort, stroke = stroke, strokeDasharray = strokeDasharray, strokeDashoffset = strokeDashoffset, strokeLinecap = strokeLinecap, strokeLinejoin = strokeLinejoin, strokeMiterlimit = strokeMiterlimit, strokeOpacity = strokeOpacity, strokeWidth = strokeWidth, target = target, tip = tip, title = title, ..., data_from = data_from, filter_by = filter_by)
}

#' The gridFx mark.
#'
#' @param spec A plot fragment or `vgspec` to add this mark to, or `NULL` to start a new plot with just this mark.
#' @param anchor The side of the frame on which to place the axis: *top* or *bottom* for horizontal axes (axisX and axisFx) and their associated vertical grids (gridX and gridFx), or *left* or *right* for vertical axes (axisY and axisFY) and their associated horizontal grids (gridY and gridFy).
#' @param ariaDescription The aria-description; a constant textual description.
#' @param ariaHidden The aria-hidden state; a constant indicating whether the element is exposed to an accessibility API.
#' @param ariaLabel The aria-label; a channel specifying short textual labels representing the value in the accessibility tree.
#' @param channels Additional named channels, for example to include in a tooltip.
#' @param clip How to clip the mark; one of: - *frame* or true - clip to the plot’s frame (inner area) - *sphere* - clip to the projected sphere (*e.g.*, front hemisphere) - null or false - do not clip The *sphere* clip option requires a geographic projection.
#' @param color A shorthand for setting both **fill** and **stroke**; affects the stroke of tick vectors and grid rules, and the fill of tick texts and axis label texts; defaults to *currentColor*.
#' @param dx The horizontal offset in pixels; a constant option.
#' @param dy The vertical offset in pixels; a constant option.
#' @param facet Whether to enable or disable faceting; one of: - *auto* (default) - automatically determine if this mark should be faceted - *include* (or true) - draw the subset of the mark’s data in the current facet - *exclude* - draw the subset of the mark’s data *not* in the current facet - *super* - draw this mark in a single frame that covers all facets - null (or false) - repeat this mark’s data across all facets (*i.e.*, no faceting) When a mark uses *super* faceting, it is not allowed to use position scales (*x*, *y*, *fx*, or *fy*); *super* faceting is intended for decorations, such as labels and legends.
#' @param facetAnchor How to place the mark with respect to facets; one of: - null (default for most marks) - display the mark in each non-empty facet - *top*, *right*, *bottom*, or *left* - display the mark only in facets on the given side - *top-empty*, *right-empty*, *bottom-empty*, or *left-empty* (default for axis marks) - display the mark only in facets that have empty space on the given side: either the margin, or an empty facet - *empty* - display the mark in empty facets only
#' @param fill The fill; a constant CSS color string, or a channel typically bound to the *color* scale.
#' @param fillOpacity The fill-opacity; a constant number between 0 and 1, or a channel typically bound to the *opacity* scale.
#' @param filter Applies a transform to filter the mark’s index according to the given channel values; only truthy values are retained.
#' @param fx The horizontal facet position channel, for mark-level faceting, bound to the *fx* scale.
#' @param fy The vertical facet position channel, for mark-level faceting, bound to the *fy* scale.
#' @param href The href; a channel specifying URLs for clickable links.
#' @param imageFilter A CSS filter; a constant string used to adjust the rendering of images, such as *blur(5px)*.
#' @param inset Shorthand to set the same default for **insetStart** and **insetEnd**.
#' @param insetBottom Insets the bottom edge by the specified number of pixels.
#' @param insetTop Insets the top edge by the specified number of pixels.
#' @param interval Enforces uniformity for data at regular intervals, such as integer values or daily samples.
#' @param margin Shorthand to set the same default for all four mark margins: **marginTop**, **marginRight**, **marginBottom**, and **marginLeft**; typically defaults to 0, except for axis marks.
#' @param marginBottom The mark’s bottom margin; the minimum distance in pixels between the bottom edges of the inner and outer plot area.
#' @param marginLeft The mark’s left margin; the minimum distance in pixels between the left edges of the inner and outer plot area.
#' @param marginRight The mark’s right margin; the minimum distance in pixels between the right edges of the mark’s inner and outer plot area.
#' @param marginTop The mark’s top margin; the minimum distance in pixels between the top edges of the inner and outer plot area.
#' @param marker Shorthand to set the same default for markerStart, markerMid, and markerEnd; one of: - a marker name such as *arrow* or *circle* - *none* (default) - no marker * true - alias for *circle-fill* * false or null - alias for *none*
#' @param markerEnd The marker for the ending point of a line segment; one of: - a marker name such as *arrow* or *circle* * *none* (default) - no marker * true - alias for *circle-fill* * false or null - alias for *none*
#' @param markerMid The marker for any middle (interior) points of a line segment.
#' @param markerStart The marker for the starting point of a line segment; one of: - a marker name such as *arrow* or *circle* * *none* (default) - no marker * true - alias for *circle-fill* * false or null - alias for *none*
#' @param mixBlendMode The mix-blend-mode; a constant string specifying how to blend content such as *multiply*.
#' @param opacity The opacity; a constant between 0 and 1, or a channel typically bound to the *opacity* scale.
#' @param paintOrder The paint-order; a constant string specifying the order in which the **fill**, **stroke**, and any markers are drawn; defaults to *normal*, which draws the fill, then stroke, then markers; defaults to *stroke* for the text mark to create a “halo” around text to improve legibility.
#' @param pointerEvents The pointer-events property; a constant string such as *none*.
#' @param reverse Applies a transform to reverse the order of the mark’s index, say for reverse input order.
#' @param select Applies a filter transform after data is loaded to highlight selected values only.
#' @param shapeRendering The shape-rendering; a constant string such as *crispEdges*.
#' @param sort Either applies a transform to sort the mark’s index by the specified channel values, or imputes ordinal scale domains from this mark’s channels.
#' @param stroke The stroke; a constant CSS color string, or a channel typically bound to the *color* scale.
#' @param strokeDasharray The stroke-dasharray; a constant number indicating the length in pixels of alternating dashes and gaps, or a constant string of numbers separated by spaces or commas (_e.g._, *10 2* for dashes of 10 pixels separated by gaps of 2 pixels), or *none* (the default) for no dashing : https://developer.mozilla.org/en-US/docs/Web/SVG/Attribute/stroke-dasharray
#' @param strokeDashoffset The stroke-dashoffset; a constant indicating the offset in pixels of the first dash along the stroke; defaults to zero.
#' @param strokeLinecap The stroke-linecap; a constant specifying how to cap stroked paths, such as *butt*, *round*, or *square*.
#' @param strokeLinejoin The stroke-linejoin; a constant specifying how to join stroked paths, such as *bevel*, *miter*, *miter-clip*, or *round*.
#' @param strokeMiterlimit The stroke-miterlimit; a constant number specifying how to limit the length of *miter* joins on stroked paths.
#' @param strokeOpacity The stroke-opacity; a constant between 0 and 1, or a channel typically bound to the *opacity* scale.
#' @param strokeWidth The stroke-width; a constant number in pixels, or a channel.
#' @param target The target; a constant string specifying the target window (_e.g._, *_blank*) for clickable links; used in conjunction with the **href** option.
#' @param tickSpacing The desired approximate spacing between adjacent axis ticks, affecting the default **ticks**; defaults to 80 pixels for *x* and *fx*, and 35 pixels for *y* and *fy*.
#' @param ticks The desired approximate number of axis ticks, or an explicit array of tick values, or an interval such as *day* or *month*.
#' @param tip Whether to generate a tooltip for this mark, and any tip options.
#' @param title The title; a channel specifying accessible, short textual descriptions as strings (possibly with newlines).
#' @param x The horizontal position (or length) channel, typically bound to the *x* scale.
#' @param y The vertical position channel, typically bound to the *y* scale; defaults to the zero-based index of the data 0, 1, 2, ….
#' @param y1 The required primary (starting, often bottom) vertical position channel, representing the area’s baseline, typically bound to the *y* scale.
#' @param y2 The optional secondary (ending, often top) vertical position channel, representing the area’s topline, typically bound to the *y* scale; if not specified, **y1** is used.
#' @param ... Additional options or plot-level attributes.
#' @param data_from The name of the data source this mark reads from (see [vg_data()]).
#' @param filter_by A Param/Selection (e.g. from [param()]) to filter this mark's data by.
#' @family mark functions
#' @export
vg_grid_fx <- function(spec = NULL, anchor = vg_unset, ariaDescription = vg_unset, ariaHidden = vg_unset, ariaLabel = vg_unset, channels = vg_unset, clip = vg_unset, color = vg_unset, dx = vg_unset, dy = vg_unset, facet = vg_unset, facetAnchor = vg_unset, fill = vg_unset, fillOpacity = vg_unset, filter = vg_unset, fx = vg_unset, fy = vg_unset, href = vg_unset, imageFilter = vg_unset, inset = vg_unset, insetBottom = vg_unset, insetTop = vg_unset, interval = vg_unset, margin = vg_unset, marginBottom = vg_unset, marginLeft = vg_unset, marginRight = vg_unset, marginTop = vg_unset, marker = vg_unset, markerEnd = vg_unset, markerMid = vg_unset, markerStart = vg_unset, mixBlendMode = vg_unset, opacity = vg_unset, paintOrder = vg_unset, pointerEvents = vg_unset, reverse = vg_unset, select = vg_unset, shapeRendering = vg_unset, sort = vg_unset, stroke = vg_unset, strokeDasharray = vg_unset, strokeDashoffset = vg_unset, strokeLinecap = vg_unset, strokeLinejoin = vg_unset, strokeMiterlimit = vg_unset, strokeOpacity = vg_unset, strokeWidth = vg_unset, target = vg_unset, tickSpacing = vg_unset, ticks = vg_unset, tip = vg_unset, title = vg_unset, x = vg_unset, y = vg_unset, y1 = vg_unset, y2 = vg_unset, ..., data_from = vg_unset, filter_by = vg_unset) {
  vg_mark_(spec, "gridFx", anchor = anchor, ariaDescription = ariaDescription, ariaHidden = ariaHidden, ariaLabel = ariaLabel, channels = channels, clip = clip, color = color, dx = dx, dy = dy, facet = facet, facetAnchor = facetAnchor, fill = fill, fillOpacity = fillOpacity, filter = filter, fx = fx, fy = fy, href = href, imageFilter = imageFilter, inset = inset, insetBottom = insetBottom, insetTop = insetTop, interval = interval, margin = margin, marginBottom = marginBottom, marginLeft = marginLeft, marginRight = marginRight, marginTop = marginTop, marker = marker, markerEnd = markerEnd, markerMid = markerMid, markerStart = markerStart, mixBlendMode = mixBlendMode, opacity = opacity, paintOrder = paintOrder, pointerEvents = pointerEvents, reverse = reverse, select = select, shapeRendering = shapeRendering, sort = sort, stroke = stroke, strokeDasharray = strokeDasharray, strokeDashoffset = strokeDashoffset, strokeLinecap = strokeLinecap, strokeLinejoin = strokeLinejoin, strokeMiterlimit = strokeMiterlimit, strokeOpacity = strokeOpacity, strokeWidth = strokeWidth, target = target, tickSpacing = tickSpacing, ticks = ticks, tip = tip, title = title, x = x, y = y, y1 = y1, y2 = y2, ..., data_from = data_from, filter_by = filter_by)
}

#' The gridFy mark.
#'
#' @param spec A plot fragment or `vgspec` to add this mark to, or `NULL` to start a new plot with just this mark.
#' @param anchor The side of the frame on which to place the axis: *top* or *bottom* for horizontal axes (axisX and axisFx) and their associated vertical grids (gridX and gridFx), or *left* or *right* for vertical axes (axisY and axisFY) and their associated horizontal grids (gridY and gridFy).
#' @param ariaDescription The aria-description; a constant textual description.
#' @param ariaHidden The aria-hidden state; a constant indicating whether the element is exposed to an accessibility API.
#' @param ariaLabel The aria-label; a channel specifying short textual labels representing the value in the accessibility tree.
#' @param channels Additional named channels, for example to include in a tooltip.
#' @param clip How to clip the mark; one of: - *frame* or true - clip to the plot’s frame (inner area) - *sphere* - clip to the projected sphere (*e.g.*, front hemisphere) - null or false - do not clip The *sphere* clip option requires a geographic projection.
#' @param color A shorthand for setting both **fill** and **stroke**; affects the stroke of tick vectors and grid rules, and the fill of tick texts and axis label texts; defaults to *currentColor*.
#' @param dx The horizontal offset in pixels; a constant option.
#' @param dy The vertical offset in pixels; a constant option.
#' @param facet Whether to enable or disable faceting; one of: - *auto* (default) - automatically determine if this mark should be faceted - *include* (or true) - draw the subset of the mark’s data in the current facet - *exclude* - draw the subset of the mark’s data *not* in the current facet - *super* - draw this mark in a single frame that covers all facets - null (or false) - repeat this mark’s data across all facets (*i.e.*, no faceting) When a mark uses *super* faceting, it is not allowed to use position scales (*x*, *y*, *fx*, or *fy*); *super* faceting is intended for decorations, such as labels and legends.
#' @param facetAnchor How to place the mark with respect to facets; one of: - null (default for most marks) - display the mark in each non-empty facet - *top*, *right*, *bottom*, or *left* - display the mark only in facets on the given side - *top-empty*, *right-empty*, *bottom-empty*, or *left-empty* (default for axis marks) - display the mark only in facets that have empty space on the given side: either the margin, or an empty facet - *empty* - display the mark in empty facets only
#' @param fill The fill; a constant CSS color string, or a channel typically bound to the *color* scale.
#' @param fillOpacity The fill-opacity; a constant number between 0 and 1, or a channel typically bound to the *opacity* scale.
#' @param filter Applies a transform to filter the mark’s index according to the given channel values; only truthy values are retained.
#' @param fx The horizontal facet position channel, for mark-level faceting, bound to the *fx* scale.
#' @param fy The vertical facet position channel, for mark-level faceting, bound to the *fy* scale.
#' @param href The href; a channel specifying URLs for clickable links.
#' @param imageFilter A CSS filter; a constant string used to adjust the rendering of images, such as *blur(5px)*.
#' @param inset Shorthand to set the same default for **insetStart** and **insetEnd**.
#' @param insetLeft Insets the left edge by the specified number of pixels.
#' @param insetRight Insets the right edge by the specified number of pixels.
#' @param interval Enforces uniformity for data at regular intervals, such as integer values or daily samples.
#' @param margin Shorthand to set the same default for all four mark margins: **marginTop**, **marginRight**, **marginBottom**, and **marginLeft**; typically defaults to 0, except for axis marks.
#' @param marginBottom The mark’s bottom margin; the minimum distance in pixels between the bottom edges of the inner and outer plot area.
#' @param marginLeft The mark’s left margin; the minimum distance in pixels between the left edges of the inner and outer plot area.
#' @param marginRight The mark’s right margin; the minimum distance in pixels between the right edges of the mark’s inner and outer plot area.
#' @param marginTop The mark’s top margin; the minimum distance in pixels between the top edges of the inner and outer plot area.
#' @param marker Shorthand to set the same default for markerStart, markerMid, and markerEnd; one of: - a marker name such as *arrow* or *circle* - *none* (default) - no marker * true - alias for *circle-fill* * false or null - alias for *none*
#' @param markerEnd The marker for the ending point of a line segment; one of: - a marker name such as *arrow* or *circle* * *none* (default) - no marker * true - alias for *circle-fill* * false or null - alias for *none*
#' @param markerMid The marker for any middle (interior) points of a line segment.
#' @param markerStart The marker for the starting point of a line segment; one of: - a marker name such as *arrow* or *circle* * *none* (default) - no marker * true - alias for *circle-fill* * false or null - alias for *none*
#' @param mixBlendMode The mix-blend-mode; a constant string specifying how to blend content such as *multiply*.
#' @param opacity The opacity; a constant between 0 and 1, or a channel typically bound to the *opacity* scale.
#' @param paintOrder The paint-order; a constant string specifying the order in which the **fill**, **stroke**, and any markers are drawn; defaults to *normal*, which draws the fill, then stroke, then markers; defaults to *stroke* for the text mark to create a “halo” around text to improve legibility.
#' @param pointerEvents The pointer-events property; a constant string such as *none*.
#' @param reverse Applies a transform to reverse the order of the mark’s index, say for reverse input order.
#' @param select Applies a filter transform after data is loaded to highlight selected values only.
#' @param shapeRendering The shape-rendering; a constant string such as *crispEdges*.
#' @param sort Either applies a transform to sort the mark’s index by the specified channel values, or imputes ordinal scale domains from this mark’s channels.
#' @param stroke The stroke; a constant CSS color string, or a channel typically bound to the *color* scale.
#' @param strokeDasharray The stroke-dasharray; a constant number indicating the length in pixels of alternating dashes and gaps, or a constant string of numbers separated by spaces or commas (_e.g._, *10 2* for dashes of 10 pixels separated by gaps of 2 pixels), or *none* (the default) for no dashing : https://developer.mozilla.org/en-US/docs/Web/SVG/Attribute/stroke-dasharray
#' @param strokeDashoffset The stroke-dashoffset; a constant indicating the offset in pixels of the first dash along the stroke; defaults to zero.
#' @param strokeLinecap The stroke-linecap; a constant specifying how to cap stroked paths, such as *butt*, *round*, or *square*.
#' @param strokeLinejoin The stroke-linejoin; a constant specifying how to join stroked paths, such as *bevel*, *miter*, *miter-clip*, or *round*.
#' @param strokeMiterlimit The stroke-miterlimit; a constant number specifying how to limit the length of *miter* joins on stroked paths.
#' @param strokeOpacity The stroke-opacity; a constant between 0 and 1, or a channel typically bound to the *opacity* scale.
#' @param strokeWidth The stroke-width; a constant number in pixels, or a channel.
#' @param target The target; a constant string specifying the target window (_e.g._, *_blank*) for clickable links; used in conjunction with the **href** option.
#' @param tickSpacing The desired approximate spacing between adjacent axis ticks, affecting the default **ticks**; defaults to 80 pixels for *x* and *fx*, and 35 pixels for *y* and *fy*.
#' @param ticks The desired approximate number of axis ticks, or an explicit array of tick values, or an interval such as *day* or *month*.
#' @param tip Whether to generate a tooltip for this mark, and any tip options.
#' @param title The title; a channel specifying accessible, short textual descriptions as strings (possibly with newlines).
#' @param x The horizontal position (or length) channel, typically bound to the *x* scale.
#' @param x1 The required primary (starting, often left) horizontal position channel, representing the area’s baseline, typically bound to the *x* scale.
#' @param x2 The optional secondary (ending, often right) horizontal position channel, representing the area’s topline, typically bound to the *x* scale; if not specified, **x1** is used.
#' @param y The vertical position channel, typically bound to the *y* scale; defaults to the zero-based index of the data 0, 1, 2, ….
#' @param ... Additional options or plot-level attributes.
#' @param data_from The name of the data source this mark reads from (see [vg_data()]).
#' @param filter_by A Param/Selection (e.g. from [param()]) to filter this mark's data by.
#' @family mark functions
#' @export
vg_grid_fy <- function(spec = NULL, anchor = vg_unset, ariaDescription = vg_unset, ariaHidden = vg_unset, ariaLabel = vg_unset, channels = vg_unset, clip = vg_unset, color = vg_unset, dx = vg_unset, dy = vg_unset, facet = vg_unset, facetAnchor = vg_unset, fill = vg_unset, fillOpacity = vg_unset, filter = vg_unset, fx = vg_unset, fy = vg_unset, href = vg_unset, imageFilter = vg_unset, inset = vg_unset, insetLeft = vg_unset, insetRight = vg_unset, interval = vg_unset, margin = vg_unset, marginBottom = vg_unset, marginLeft = vg_unset, marginRight = vg_unset, marginTop = vg_unset, marker = vg_unset, markerEnd = vg_unset, markerMid = vg_unset, markerStart = vg_unset, mixBlendMode = vg_unset, opacity = vg_unset, paintOrder = vg_unset, pointerEvents = vg_unset, reverse = vg_unset, select = vg_unset, shapeRendering = vg_unset, sort = vg_unset, stroke = vg_unset, strokeDasharray = vg_unset, strokeDashoffset = vg_unset, strokeLinecap = vg_unset, strokeLinejoin = vg_unset, strokeMiterlimit = vg_unset, strokeOpacity = vg_unset, strokeWidth = vg_unset, target = vg_unset, tickSpacing = vg_unset, ticks = vg_unset, tip = vg_unset, title = vg_unset, x = vg_unset, x1 = vg_unset, x2 = vg_unset, y = vg_unset, ..., data_from = vg_unset, filter_by = vg_unset) {
  vg_mark_(spec, "gridFy", anchor = anchor, ariaDescription = ariaDescription, ariaHidden = ariaHidden, ariaLabel = ariaLabel, channels = channels, clip = clip, color = color, dx = dx, dy = dy, facet = facet, facetAnchor = facetAnchor, fill = fill, fillOpacity = fillOpacity, filter = filter, fx = fx, fy = fy, href = href, imageFilter = imageFilter, inset = inset, insetLeft = insetLeft, insetRight = insetRight, interval = interval, margin = margin, marginBottom = marginBottom, marginLeft = marginLeft, marginRight = marginRight, marginTop = marginTop, marker = marker, markerEnd = markerEnd, markerMid = markerMid, markerStart = markerStart, mixBlendMode = mixBlendMode, opacity = opacity, paintOrder = paintOrder, pointerEvents = pointerEvents, reverse = reverse, select = select, shapeRendering = shapeRendering, sort = sort, stroke = stroke, strokeDasharray = strokeDasharray, strokeDashoffset = strokeDashoffset, strokeLinecap = strokeLinecap, strokeLinejoin = strokeLinejoin, strokeMiterlimit = strokeMiterlimit, strokeOpacity = strokeOpacity, strokeWidth = strokeWidth, target = target, tickSpacing = tickSpacing, ticks = ticks, tip = tip, title = title, x = x, x1 = x1, x2 = x2, y = y, ..., data_from = data_from, filter_by = filter_by)
}

#' The gridX mark.
#'
#' @param spec A plot fragment or `vgspec` to add this mark to, or `NULL` to start a new plot with just this mark.
#' @param anchor The side of the frame on which to place the axis: *top* or *bottom* for horizontal axes (axisX and axisFx) and their associated vertical grids (gridX and gridFx), or *left* or *right* for vertical axes (axisY and axisFY) and their associated horizontal grids (gridY and gridFy).
#' @param ariaDescription The aria-description; a constant textual description.
#' @param ariaHidden The aria-hidden state; a constant indicating whether the element is exposed to an accessibility API.
#' @param ariaLabel The aria-label; a channel specifying short textual labels representing the value in the accessibility tree.
#' @param channels Additional named channels, for example to include in a tooltip.
#' @param clip How to clip the mark; one of: - *frame* or true - clip to the plot’s frame (inner area) - *sphere* - clip to the projected sphere (*e.g.*, front hemisphere) - null or false - do not clip The *sphere* clip option requires a geographic projection.
#' @param color A shorthand for setting both **fill** and **stroke**; affects the stroke of tick vectors and grid rules, and the fill of tick texts and axis label texts; defaults to *currentColor*.
#' @param dx The horizontal offset in pixels; a constant option.
#' @param dy The vertical offset in pixels; a constant option.
#' @param facet Whether to enable or disable faceting; one of: - *auto* (default) - automatically determine if this mark should be faceted - *include* (or true) - draw the subset of the mark’s data in the current facet - *exclude* - draw the subset of the mark’s data *not* in the current facet - *super* - draw this mark in a single frame that covers all facets - null (or false) - repeat this mark’s data across all facets (*i.e.*, no faceting) When a mark uses *super* faceting, it is not allowed to use position scales (*x*, *y*, *fx*, or *fy*); *super* faceting is intended for decorations, such as labels and legends.
#' @param facetAnchor How to place the mark with respect to facets; one of: - null (default for most marks) - display the mark in each non-empty facet - *top*, *right*, *bottom*, or *left* - display the mark only in facets on the given side - *top-empty*, *right-empty*, *bottom-empty*, or *left-empty* (default for axis marks) - display the mark only in facets that have empty space on the given side: either the margin, or an empty facet - *empty* - display the mark in empty facets only
#' @param fill The fill; a constant CSS color string, or a channel typically bound to the *color* scale.
#' @param fillOpacity The fill-opacity; a constant number between 0 and 1, or a channel typically bound to the *opacity* scale.
#' @param filter Applies a transform to filter the mark’s index according to the given channel values; only truthy values are retained.
#' @param fx The horizontal facet position channel, for mark-level faceting, bound to the *fx* scale.
#' @param fy The vertical facet position channel, for mark-level faceting, bound to the *fy* scale.
#' @param href The href; a channel specifying URLs for clickable links.
#' @param imageFilter A CSS filter; a constant string used to adjust the rendering of images, such as *blur(5px)*.
#' @param inset Shorthand to set the same default for **insetStart** and **insetEnd**.
#' @param insetBottom Insets the bottom edge by the specified number of pixels.
#' @param insetTop Insets the top edge by the specified number of pixels.
#' @param interval Enforces uniformity for data at regular intervals, such as integer values or daily samples.
#' @param margin Shorthand to set the same default for all four mark margins: **marginTop**, **marginRight**, **marginBottom**, and **marginLeft**; typically defaults to 0, except for axis marks.
#' @param marginBottom The mark’s bottom margin; the minimum distance in pixels between the bottom edges of the inner and outer plot area.
#' @param marginLeft The mark’s left margin; the minimum distance in pixels between the left edges of the inner and outer plot area.
#' @param marginRight The mark’s right margin; the minimum distance in pixels between the right edges of the mark’s inner and outer plot area.
#' @param marginTop The mark’s top margin; the minimum distance in pixels between the top edges of the inner and outer plot area.
#' @param marker Shorthand to set the same default for markerStart, markerMid, and markerEnd; one of: - a marker name such as *arrow* or *circle* - *none* (default) - no marker * true - alias for *circle-fill* * false or null - alias for *none*
#' @param markerEnd The marker for the ending point of a line segment; one of: - a marker name such as *arrow* or *circle* * *none* (default) - no marker * true - alias for *circle-fill* * false or null - alias for *none*
#' @param markerMid The marker for any middle (interior) points of a line segment.
#' @param markerStart The marker for the starting point of a line segment; one of: - a marker name such as *arrow* or *circle* * *none* (default) - no marker * true - alias for *circle-fill* * false or null - alias for *none*
#' @param mixBlendMode The mix-blend-mode; a constant string specifying how to blend content such as *multiply*.
#' @param opacity The opacity; a constant between 0 and 1, or a channel typically bound to the *opacity* scale.
#' @param paintOrder The paint-order; a constant string specifying the order in which the **fill**, **stroke**, and any markers are drawn; defaults to *normal*, which draws the fill, then stroke, then markers; defaults to *stroke* for the text mark to create a “halo” around text to improve legibility.
#' @param pointerEvents The pointer-events property; a constant string such as *none*.
#' @param reverse Applies a transform to reverse the order of the mark’s index, say for reverse input order.
#' @param select Applies a filter transform after data is loaded to highlight selected values only.
#' @param shapeRendering The shape-rendering; a constant string such as *crispEdges*.
#' @param sort Either applies a transform to sort the mark’s index by the specified channel values, or imputes ordinal scale domains from this mark’s channels.
#' @param stroke The stroke; a constant CSS color string, or a channel typically bound to the *color* scale.
#' @param strokeDasharray The stroke-dasharray; a constant number indicating the length in pixels of alternating dashes and gaps, or a constant string of numbers separated by spaces or commas (_e.g._, *10 2* for dashes of 10 pixels separated by gaps of 2 pixels), or *none* (the default) for no dashing : https://developer.mozilla.org/en-US/docs/Web/SVG/Attribute/stroke-dasharray
#' @param strokeDashoffset The stroke-dashoffset; a constant indicating the offset in pixels of the first dash along the stroke; defaults to zero.
#' @param strokeLinecap The stroke-linecap; a constant specifying how to cap stroked paths, such as *butt*, *round*, or *square*.
#' @param strokeLinejoin The stroke-linejoin; a constant specifying how to join stroked paths, such as *bevel*, *miter*, *miter-clip*, or *round*.
#' @param strokeMiterlimit The stroke-miterlimit; a constant number specifying how to limit the length of *miter* joins on stroked paths.
#' @param strokeOpacity The stroke-opacity; a constant between 0 and 1, or a channel typically bound to the *opacity* scale.
#' @param strokeWidth The stroke-width; a constant number in pixels, or a channel.
#' @param target The target; a constant string specifying the target window (_e.g._, *_blank*) for clickable links; used in conjunction with the **href** option.
#' @param tickSpacing The desired approximate spacing between adjacent axis ticks, affecting the default **ticks**; defaults to 80 pixels for *x* and *fx*, and 35 pixels for *y* and *fy*.
#' @param ticks The desired approximate number of axis ticks, or an explicit array of tick values, or an interval such as *day* or *month*.
#' @param tip Whether to generate a tooltip for this mark, and any tip options.
#' @param title The title; a channel specifying accessible, short textual descriptions as strings (possibly with newlines).
#' @param x The horizontal position (or length) channel, typically bound to the *x* scale.
#' @param y The vertical position channel, typically bound to the *y* scale; defaults to the zero-based index of the data 0, 1, 2, ….
#' @param y1 The required primary (starting, often bottom) vertical position channel, representing the area’s baseline, typically bound to the *y* scale.
#' @param y2 The optional secondary (ending, often top) vertical position channel, representing the area’s topline, typically bound to the *y* scale; if not specified, **y1** is used.
#' @param ... Additional options or plot-level attributes.
#' @param data_from The name of the data source this mark reads from (see [vg_data()]).
#' @param filter_by A Param/Selection (e.g. from [param()]) to filter this mark's data by.
#' @family mark functions
#' @export
vg_grid_x <- function(spec = NULL, anchor = vg_unset, ariaDescription = vg_unset, ariaHidden = vg_unset, ariaLabel = vg_unset, channels = vg_unset, clip = vg_unset, color = vg_unset, dx = vg_unset, dy = vg_unset, facet = vg_unset, facetAnchor = vg_unset, fill = vg_unset, fillOpacity = vg_unset, filter = vg_unset, fx = vg_unset, fy = vg_unset, href = vg_unset, imageFilter = vg_unset, inset = vg_unset, insetBottom = vg_unset, insetTop = vg_unset, interval = vg_unset, margin = vg_unset, marginBottom = vg_unset, marginLeft = vg_unset, marginRight = vg_unset, marginTop = vg_unset, marker = vg_unset, markerEnd = vg_unset, markerMid = vg_unset, markerStart = vg_unset, mixBlendMode = vg_unset, opacity = vg_unset, paintOrder = vg_unset, pointerEvents = vg_unset, reverse = vg_unset, select = vg_unset, shapeRendering = vg_unset, sort = vg_unset, stroke = vg_unset, strokeDasharray = vg_unset, strokeDashoffset = vg_unset, strokeLinecap = vg_unset, strokeLinejoin = vg_unset, strokeMiterlimit = vg_unset, strokeOpacity = vg_unset, strokeWidth = vg_unset, target = vg_unset, tickSpacing = vg_unset, ticks = vg_unset, tip = vg_unset, title = vg_unset, x = vg_unset, y = vg_unset, y1 = vg_unset, y2 = vg_unset, ..., data_from = vg_unset, filter_by = vg_unset) {
  vg_mark_(spec, "gridX", anchor = anchor, ariaDescription = ariaDescription, ariaHidden = ariaHidden, ariaLabel = ariaLabel, channels = channels, clip = clip, color = color, dx = dx, dy = dy, facet = facet, facetAnchor = facetAnchor, fill = fill, fillOpacity = fillOpacity, filter = filter, fx = fx, fy = fy, href = href, imageFilter = imageFilter, inset = inset, insetBottom = insetBottom, insetTop = insetTop, interval = interval, margin = margin, marginBottom = marginBottom, marginLeft = marginLeft, marginRight = marginRight, marginTop = marginTop, marker = marker, markerEnd = markerEnd, markerMid = markerMid, markerStart = markerStart, mixBlendMode = mixBlendMode, opacity = opacity, paintOrder = paintOrder, pointerEvents = pointerEvents, reverse = reverse, select = select, shapeRendering = shapeRendering, sort = sort, stroke = stroke, strokeDasharray = strokeDasharray, strokeDashoffset = strokeDashoffset, strokeLinecap = strokeLinecap, strokeLinejoin = strokeLinejoin, strokeMiterlimit = strokeMiterlimit, strokeOpacity = strokeOpacity, strokeWidth = strokeWidth, target = target, tickSpacing = tickSpacing, ticks = ticks, tip = tip, title = title, x = x, y = y, y1 = y1, y2 = y2, ..., data_from = data_from, filter_by = filter_by)
}

#' The gridY mark.
#'
#' @param spec A plot fragment or `vgspec` to add this mark to, or `NULL` to start a new plot with just this mark.
#' @param anchor The side of the frame on which to place the axis: *top* or *bottom* for horizontal axes (axisX and axisFx) and their associated vertical grids (gridX and gridFx), or *left* or *right* for vertical axes (axisY and axisFY) and their associated horizontal grids (gridY and gridFy).
#' @param ariaDescription The aria-description; a constant textual description.
#' @param ariaHidden The aria-hidden state; a constant indicating whether the element is exposed to an accessibility API.
#' @param ariaLabel The aria-label; a channel specifying short textual labels representing the value in the accessibility tree.
#' @param channels Additional named channels, for example to include in a tooltip.
#' @param clip How to clip the mark; one of: - *frame* or true - clip to the plot’s frame (inner area) - *sphere* - clip to the projected sphere (*e.g.*, front hemisphere) - null or false - do not clip The *sphere* clip option requires a geographic projection.
#' @param color A shorthand for setting both **fill** and **stroke**; affects the stroke of tick vectors and grid rules, and the fill of tick texts and axis label texts; defaults to *currentColor*.
#' @param dx The horizontal offset in pixels; a constant option.
#' @param dy The vertical offset in pixels; a constant option.
#' @param facet Whether to enable or disable faceting; one of: - *auto* (default) - automatically determine if this mark should be faceted - *include* (or true) - draw the subset of the mark’s data in the current facet - *exclude* - draw the subset of the mark’s data *not* in the current facet - *super* - draw this mark in a single frame that covers all facets - null (or false) - repeat this mark’s data across all facets (*i.e.*, no faceting) When a mark uses *super* faceting, it is not allowed to use position scales (*x*, *y*, *fx*, or *fy*); *super* faceting is intended for decorations, such as labels and legends.
#' @param facetAnchor How to place the mark with respect to facets; one of: - null (default for most marks) - display the mark in each non-empty facet - *top*, *right*, *bottom*, or *left* - display the mark only in facets on the given side - *top-empty*, *right-empty*, *bottom-empty*, or *left-empty* (default for axis marks) - display the mark only in facets that have empty space on the given side: either the margin, or an empty facet - *empty* - display the mark in empty facets only
#' @param fill The fill; a constant CSS color string, or a channel typically bound to the *color* scale.
#' @param fillOpacity The fill-opacity; a constant number between 0 and 1, or a channel typically bound to the *opacity* scale.
#' @param filter Applies a transform to filter the mark’s index according to the given channel values; only truthy values are retained.
#' @param fx The horizontal facet position channel, for mark-level faceting, bound to the *fx* scale.
#' @param fy The vertical facet position channel, for mark-level faceting, bound to the *fy* scale.
#' @param href The href; a channel specifying URLs for clickable links.
#' @param imageFilter A CSS filter; a constant string used to adjust the rendering of images, such as *blur(5px)*.
#' @param inset Shorthand to set the same default for **insetStart** and **insetEnd**.
#' @param insetLeft Insets the left edge by the specified number of pixels.
#' @param insetRight Insets the right edge by the specified number of pixels.
#' @param interval Enforces uniformity for data at regular intervals, such as integer values or daily samples.
#' @param margin Shorthand to set the same default for all four mark margins: **marginTop**, **marginRight**, **marginBottom**, and **marginLeft**; typically defaults to 0, except for axis marks.
#' @param marginBottom The mark’s bottom margin; the minimum distance in pixels between the bottom edges of the inner and outer plot area.
#' @param marginLeft The mark’s left margin; the minimum distance in pixels between the left edges of the inner and outer plot area.
#' @param marginRight The mark’s right margin; the minimum distance in pixels between the right edges of the mark’s inner and outer plot area.
#' @param marginTop The mark’s top margin; the minimum distance in pixels between the top edges of the inner and outer plot area.
#' @param marker Shorthand to set the same default for markerStart, markerMid, and markerEnd; one of: - a marker name such as *arrow* or *circle* - *none* (default) - no marker * true - alias for *circle-fill* * false or null - alias for *none*
#' @param markerEnd The marker for the ending point of a line segment; one of: - a marker name such as *arrow* or *circle* * *none* (default) - no marker * true - alias for *circle-fill* * false or null - alias for *none*
#' @param markerMid The marker for any middle (interior) points of a line segment.
#' @param markerStart The marker for the starting point of a line segment; one of: - a marker name such as *arrow* or *circle* * *none* (default) - no marker * true - alias for *circle-fill* * false or null - alias for *none*
#' @param mixBlendMode The mix-blend-mode; a constant string specifying how to blend content such as *multiply*.
#' @param opacity The opacity; a constant between 0 and 1, or a channel typically bound to the *opacity* scale.
#' @param paintOrder The paint-order; a constant string specifying the order in which the **fill**, **stroke**, and any markers are drawn; defaults to *normal*, which draws the fill, then stroke, then markers; defaults to *stroke* for the text mark to create a “halo” around text to improve legibility.
#' @param pointerEvents The pointer-events property; a constant string such as *none*.
#' @param reverse Applies a transform to reverse the order of the mark’s index, say for reverse input order.
#' @param select Applies a filter transform after data is loaded to highlight selected values only.
#' @param shapeRendering The shape-rendering; a constant string such as *crispEdges*.
#' @param sort Either applies a transform to sort the mark’s index by the specified channel values, or imputes ordinal scale domains from this mark’s channels.
#' @param stroke The stroke; a constant CSS color string, or a channel typically bound to the *color* scale.
#' @param strokeDasharray The stroke-dasharray; a constant number indicating the length in pixels of alternating dashes and gaps, or a constant string of numbers separated by spaces or commas (_e.g._, *10 2* for dashes of 10 pixels separated by gaps of 2 pixels), or *none* (the default) for no dashing : https://developer.mozilla.org/en-US/docs/Web/SVG/Attribute/stroke-dasharray
#' @param strokeDashoffset The stroke-dashoffset; a constant indicating the offset in pixels of the first dash along the stroke; defaults to zero.
#' @param strokeLinecap The stroke-linecap; a constant specifying how to cap stroked paths, such as *butt*, *round*, or *square*.
#' @param strokeLinejoin The stroke-linejoin; a constant specifying how to join stroked paths, such as *bevel*, *miter*, *miter-clip*, or *round*.
#' @param strokeMiterlimit The stroke-miterlimit; a constant number specifying how to limit the length of *miter* joins on stroked paths.
#' @param strokeOpacity The stroke-opacity; a constant between 0 and 1, or a channel typically bound to the *opacity* scale.
#' @param strokeWidth The stroke-width; a constant number in pixels, or a channel.
#' @param target The target; a constant string specifying the target window (_e.g._, *_blank*) for clickable links; used in conjunction with the **href** option.
#' @param tickSpacing The desired approximate spacing between adjacent axis ticks, affecting the default **ticks**; defaults to 80 pixels for *x* and *fx*, and 35 pixels for *y* and *fy*.
#' @param ticks The desired approximate number of axis ticks, or an explicit array of tick values, or an interval such as *day* or *month*.
#' @param tip Whether to generate a tooltip for this mark, and any tip options.
#' @param title The title; a channel specifying accessible, short textual descriptions as strings (possibly with newlines).
#' @param x The horizontal position (or length) channel, typically bound to the *x* scale.
#' @param x1 The required primary (starting, often left) horizontal position channel, representing the area’s baseline, typically bound to the *x* scale.
#' @param x2 The optional secondary (ending, often right) horizontal position channel, representing the area’s topline, typically bound to the *x* scale; if not specified, **x1** is used.
#' @param y The vertical position channel, typically bound to the *y* scale; defaults to the zero-based index of the data 0, 1, 2, ….
#' @param ... Additional options or plot-level attributes.
#' @param data_from The name of the data source this mark reads from (see [vg_data()]).
#' @param filter_by A Param/Selection (e.g. from [param()]) to filter this mark's data by.
#' @family mark functions
#' @export
vg_grid_y <- function(spec = NULL, anchor = vg_unset, ariaDescription = vg_unset, ariaHidden = vg_unset, ariaLabel = vg_unset, channels = vg_unset, clip = vg_unset, color = vg_unset, dx = vg_unset, dy = vg_unset, facet = vg_unset, facetAnchor = vg_unset, fill = vg_unset, fillOpacity = vg_unset, filter = vg_unset, fx = vg_unset, fy = vg_unset, href = vg_unset, imageFilter = vg_unset, inset = vg_unset, insetLeft = vg_unset, insetRight = vg_unset, interval = vg_unset, margin = vg_unset, marginBottom = vg_unset, marginLeft = vg_unset, marginRight = vg_unset, marginTop = vg_unset, marker = vg_unset, markerEnd = vg_unset, markerMid = vg_unset, markerStart = vg_unset, mixBlendMode = vg_unset, opacity = vg_unset, paintOrder = vg_unset, pointerEvents = vg_unset, reverse = vg_unset, select = vg_unset, shapeRendering = vg_unset, sort = vg_unset, stroke = vg_unset, strokeDasharray = vg_unset, strokeDashoffset = vg_unset, strokeLinecap = vg_unset, strokeLinejoin = vg_unset, strokeMiterlimit = vg_unset, strokeOpacity = vg_unset, strokeWidth = vg_unset, target = vg_unset, tickSpacing = vg_unset, ticks = vg_unset, tip = vg_unset, title = vg_unset, x = vg_unset, x1 = vg_unset, x2 = vg_unset, y = vg_unset, ..., data_from = vg_unset, filter_by = vg_unset) {
  vg_mark_(spec, "gridY", anchor = anchor, ariaDescription = ariaDescription, ariaHidden = ariaHidden, ariaLabel = ariaLabel, channels = channels, clip = clip, color = color, dx = dx, dy = dy, facet = facet, facetAnchor = facetAnchor, fill = fill, fillOpacity = fillOpacity, filter = filter, fx = fx, fy = fy, href = href, imageFilter = imageFilter, inset = inset, insetLeft = insetLeft, insetRight = insetRight, interval = interval, margin = margin, marginBottom = marginBottom, marginLeft = marginLeft, marginRight = marginRight, marginTop = marginTop, marker = marker, markerEnd = markerEnd, markerMid = markerMid, markerStart = markerStart, mixBlendMode = mixBlendMode, opacity = opacity, paintOrder = paintOrder, pointerEvents = pointerEvents, reverse = reverse, select = select, shapeRendering = shapeRendering, sort = sort, stroke = stroke, strokeDasharray = strokeDasharray, strokeDashoffset = strokeDashoffset, strokeLinecap = strokeLinecap, strokeLinejoin = strokeLinejoin, strokeMiterlimit = strokeMiterlimit, strokeOpacity = strokeOpacity, strokeWidth = strokeWidth, target = target, tickSpacing = tickSpacing, ticks = ticks, tip = tip, title = title, x = x, x1 = x1, x2 = x2, y = y, ..., data_from = data_from, filter_by = filter_by)
}

#' The heatmap mark.
#'
#' @param spec A plot fragment or `vgspec` to add this mark to, or `NULL` to start a new plot with just this mark.
#' @param ariaDescription The aria-description; a constant textual description.
#' @param ariaHidden The aria-hidden state; a constant indicating whether the element is exposed to an accessibility API.
#' @param ariaLabel The aria-label; a channel specifying short textual labels representing the value in the accessibility tree.
#' @param bandwidth The kernel density bandwidth for smoothing, in pixels.
#' @param channels Additional named channels, for example to include in a tooltip.
#' @param clip How to clip the mark; one of: - *frame* or true - clip to the plot’s frame (inner area) - *sphere* - clip to the projected sphere (*e.g.*, front hemisphere) - null or false - do not clip The *sphere* clip option requires a geographic projection.
#' @param dx The horizontal offset in pixels; a constant option.
#' @param dy The vertical offset in pixels; a constant option.
#' @param facet Whether to enable or disable faceting; one of: - *auto* (default) - automatically determine if this mark should be faceted - *include* (or true) - draw the subset of the mark’s data in the current facet - *exclude* - draw the subset of the mark’s data *not* in the current facet - *super* - draw this mark in a single frame that covers all facets - null (or false) - repeat this mark’s data across all facets (*i.e.*, no faceting) When a mark uses *super* faceting, it is not allowed to use position scales (*x*, *y*, *fx*, or *fy*); *super* faceting is intended for decorations, such as labels and legends.
#' @param facetAnchor How to place the mark with respect to facets; one of: - null (default for most marks) - display the mark in each non-empty facet - *top*, *right*, *bottom*, or *left* - display the mark only in facets on the given side - *top-empty*, *right-empty*, *bottom-empty*, or *left-empty* (default for axis marks) - display the mark only in facets that have empty space on the given side: either the margin, or an empty facet - *empty* - display the mark in empty facets only
#' @param fill The fill; a constant CSS color string, or a channel typically bound to the *color* scale.
#' @param fillOpacity The fill-opacity; a constant number between 0 and 1, or a channel typically bound to the *opacity* scale.
#' @param filter Applies a transform to filter the mark’s index according to the given channel values; only truthy values are retained.
#' @param fx The horizontal facet position channel, for mark-level faceting, bound to the *fx* scale.
#' @param fy The vertical facet position channel, for mark-level faceting, bound to the *fy* scale.
#' @param height The height (number of rows) of the grid, in actual pixels.
#' @param href The href; a channel specifying URLs for clickable links.
#' @param imageFilter A CSS filter; a constant string used to adjust the rendering of images, such as *blur(5px)*.
#' @param imageRendering The image-rendering attribute; defaults to *auto* (bilinear).
#' @param interpolate The spatial interpolation method; one of: - *none* - do not perform interpolation (the default), maps samples to single bins - *linear* - apply proportional linear interpolation across adjacent bins - *nearest* - assign each pixel to the closest sample’s value (Voronoi diagram) - *barycentric* - apply barycentric interpolation over the Delaunay triangulation - *random-walk* - apply a random walk from each pixel, stopping when near a sample
#' @param margin Shorthand to set the same default for all four mark margins: **marginTop**, **marginRight**, **marginBottom**, and **marginLeft**; typically defaults to 0, except for axis marks.
#' @param marginBottom The mark’s bottom margin; the minimum distance in pixels between the bottom edges of the inner and outer plot area.
#' @param marginLeft The mark’s left margin; the minimum distance in pixels between the left edges of the inner and outer plot area.
#' @param marginRight The mark’s right margin; the minimum distance in pixels between the right edges of the mark’s inner and outer plot area.
#' @param marginTop The mark’s top margin; the minimum distance in pixels between the top edges of the inner and outer plot area.
#' @param mixBlendMode The mix-blend-mode; a constant string specifying how to blend content such as *multiply*.
#' @param opacity The opacity; a constant between 0 and 1, or a channel typically bound to the *opacity* scale.
#' @param pad The bin padding, one of 1 (default) to include extra padding for the final bin, or 0 to make the bins flush with the maximum domain value.
#' @param paintOrder The paint-order; a constant string specifying the order in which the **fill**, **stroke**, and any markers are drawn; defaults to *normal*, which draws the fill, then stroke, then markers; defaults to *stroke* for the text mark to create a “halo” around text to improve legibility.
#' @param pixelSize The effective screen size of a raster pixel, used to determine the height and width of the raster from the frame’s dimensions; defaults to 1.
#' @param pointerEvents The pointer-events property; a constant string such as *none*.
#' @param reverse Applies a transform to reverse the order of the mark’s index, say for reverse input order.
#' @param select Applies a filter transform after data is loaded to highlight selected values only.
#' @param shapeRendering The shape-rendering; a constant string such as *crispEdges*.
#' @param sort Either applies a transform to sort the mark’s index by the specified channel values, or imputes ordinal scale domains from this mark’s channels.
#' @param stroke The stroke; a constant CSS color string, or a channel typically bound to the *color* scale.
#' @param strokeDasharray The stroke-dasharray; a constant number indicating the length in pixels of alternating dashes and gaps, or a constant string of numbers separated by spaces or commas (_e.g._, *10 2* for dashes of 10 pixels separated by gaps of 2 pixels), or *none* (the default) for no dashing : https://developer.mozilla.org/en-US/docs/Web/SVG/Attribute/stroke-dasharray
#' @param strokeDashoffset The stroke-dashoffset; a constant indicating the offset in pixels of the first dash along the stroke; defaults to zero.
#' @param strokeLinecap The stroke-linecap; a constant specifying how to cap stroked paths, such as *butt*, *round*, or *square*.
#' @param strokeLinejoin The stroke-linejoin; a constant specifying how to join stroked paths, such as *bevel*, *miter*, *miter-clip*, or *round*.
#' @param strokeMiterlimit The stroke-miterlimit; a constant number specifying how to limit the length of *miter* joins on stroked paths.
#' @param strokeOpacity The stroke-opacity; a constant between 0 and 1, or a channel typically bound to the *opacity* scale.
#' @param strokeWidth The stroke-width; a constant number in pixels, or a channel.
#' @param target The target; a constant string specifying the target window (_e.g._, *_blank*) for clickable links; used in conjunction with the **href** option.
#' @param tip Whether to generate a tooltip for this mark, and any tip options.
#' @param title The title; a channel specifying accessible, short textual descriptions as strings (possibly with newlines).
#' @param width The width (number of columns) of the grid, in actual pixels.
#' @param x The horizontal position (or length) channel, typically bound to the *x* scale.
#' @param y The vertical position channel, typically bound to the *y* scale; defaults to the zero-based index of the data 0, 1, 2, ….
#' @param ... Additional options or plot-level attributes.
#' @param data_from The name of the data source this mark reads from (see [vg_data()]).
#' @param filter_by A Param/Selection (e.g. from [param()]) to filter this mark's data by.
#' @family mark functions
#' @export
vg_heatmap <- function(spec = NULL, ariaDescription = vg_unset, ariaHidden = vg_unset, ariaLabel = vg_unset, bandwidth = vg_unset, channels = vg_unset, clip = vg_unset, dx = vg_unset, dy = vg_unset, facet = vg_unset, facetAnchor = vg_unset, fill = vg_unset, fillOpacity = vg_unset, filter = vg_unset, fx = vg_unset, fy = vg_unset, height = vg_unset, href = vg_unset, imageFilter = vg_unset, imageRendering = vg_unset, interpolate = vg_unset, margin = vg_unset, marginBottom = vg_unset, marginLeft = vg_unset, marginRight = vg_unset, marginTop = vg_unset, mixBlendMode = vg_unset, opacity = vg_unset, pad = vg_unset, paintOrder = vg_unset, pixelSize = vg_unset, pointerEvents = vg_unset, reverse = vg_unset, select = vg_unset, shapeRendering = vg_unset, sort = vg_unset, stroke = vg_unset, strokeDasharray = vg_unset, strokeDashoffset = vg_unset, strokeLinecap = vg_unset, strokeLinejoin = vg_unset, strokeMiterlimit = vg_unset, strokeOpacity = vg_unset, strokeWidth = vg_unset, target = vg_unset, tip = vg_unset, title = vg_unset, width = vg_unset, x = vg_unset, y = vg_unset, ..., data_from = vg_unset, filter_by = vg_unset) {
  vg_mark_(spec, "heatmap", ariaDescription = ariaDescription, ariaHidden = ariaHidden, ariaLabel = ariaLabel, bandwidth = bandwidth, channels = channels, clip = clip, dx = dx, dy = dy, facet = facet, facetAnchor = facetAnchor, fill = fill, fillOpacity = fillOpacity, filter = filter, fx = fx, fy = fy, height = height, href = href, imageFilter = imageFilter, imageRendering = imageRendering, interpolate = interpolate, margin = margin, marginBottom = marginBottom, marginLeft = marginLeft, marginRight = marginRight, marginTop = marginTop, mixBlendMode = mixBlendMode, opacity = opacity, pad = pad, paintOrder = paintOrder, pixelSize = pixelSize, pointerEvents = pointerEvents, reverse = reverse, select = select, shapeRendering = shapeRendering, sort = sort, stroke = stroke, strokeDasharray = strokeDasharray, strokeDashoffset = strokeDashoffset, strokeLinecap = strokeLinecap, strokeLinejoin = strokeLinejoin, strokeMiterlimit = strokeMiterlimit, strokeOpacity = strokeOpacity, strokeWidth = strokeWidth, target = target, tip = tip, title = title, width = width, x = x, y = y, ..., data_from = data_from, filter_by = filter_by)
}

#' The hexagon mark.
#'
#' @param spec A plot fragment or `vgspec` to add this mark to, or `NULL` to start a new plot with just this mark.
#' @param ariaDescription The aria-description; a constant textual description.
#' @param ariaHidden The aria-hidden state; a constant indicating whether the element is exposed to an accessibility API.
#' @param ariaLabel The aria-label; a channel specifying short textual labels representing the value in the accessibility tree.
#' @param channels Additional named channels, for example to include in a tooltip.
#' @param clip How to clip the mark; one of: - *frame* or true - clip to the plot’s frame (inner area) - *sphere* - clip to the projected sphere (*e.g.*, front hemisphere) - null or false - do not clip The *sphere* clip option requires a geographic projection.
#' @param dx The horizontal offset in pixels; a constant option.
#' @param dy The vertical offset in pixels; a constant option.
#' @param facet Whether to enable or disable faceting; one of: - *auto* (default) - automatically determine if this mark should be faceted - *include* (or true) - draw the subset of the mark’s data in the current facet - *exclude* - draw the subset of the mark’s data *not* in the current facet - *super* - draw this mark in a single frame that covers all facets - null (or false) - repeat this mark’s data across all facets (*i.e.*, no faceting) When a mark uses *super* faceting, it is not allowed to use position scales (*x*, *y*, *fx*, or *fy*); *super* faceting is intended for decorations, such as labels and legends.
#' @param facetAnchor How to place the mark with respect to facets; one of: - null (default for most marks) - display the mark in each non-empty facet - *top*, *right*, *bottom*, or *left* - display the mark only in facets on the given side - *top-empty*, *right-empty*, *bottom-empty*, or *left-empty* (default for axis marks) - display the mark only in facets that have empty space on the given side: either the margin, or an empty facet - *empty* - display the mark in empty facets only
#' @param fill The fill; a constant CSS color string, or a channel typically bound to the *color* scale.
#' @param fillOpacity The fill-opacity; a constant number between 0 and 1, or a channel typically bound to the *opacity* scale.
#' @param filter Applies a transform to filter the mark’s index according to the given channel values; only truthy values are retained.
#' @param frameAnchor The frame anchor specifies defaults for **x** and **y**, along with **textAnchor** and **lineAnchor**, based on the plot’s frame; it may be one of the four sides (*top*, *right*, *bottom*, *left*), one of the four corners (*top-left*, *top-right*, *bottom-right*, *bottom-left*), or the *middle* of the frame.
#' @param fx The horizontal facet position channel, for mark-level faceting, bound to the *fx* scale.
#' @param fy The vertical facet position channel, for mark-level faceting, bound to the *fy* scale.
#' @param href The href; a channel specifying URLs for clickable links.
#' @param imageFilter A CSS filter; a constant string used to adjust the rendering of images, such as *blur(5px)*.
#' @param margin Shorthand to set the same default for all four mark margins: **marginTop**, **marginRight**, **marginBottom**, and **marginLeft**; typically defaults to 0, except for axis marks.
#' @param marginBottom The mark’s bottom margin; the minimum distance in pixels between the bottom edges of the inner and outer plot area.
#' @param marginLeft The mark’s left margin; the minimum distance in pixels between the left edges of the inner and outer plot area.
#' @param marginRight The mark’s right margin; the minimum distance in pixels between the right edges of the mark’s inner and outer plot area.
#' @param marginTop The mark’s top margin; the minimum distance in pixels between the top edges of the inner and outer plot area.
#' @param mixBlendMode The mix-blend-mode; a constant string specifying how to blend content such as *multiply*.
#' @param opacity The opacity; a constant between 0 and 1, or a channel typically bound to the *opacity* scale.
#' @param paintOrder The paint-order; a constant string specifying the order in which the **fill**, **stroke**, and any markers are drawn; defaults to *normal*, which draws the fill, then stroke, then markers; defaults to *stroke* for the text mark to create a “halo” around text to improve legibility.
#' @param pointerEvents The pointer-events property; a constant string such as *none*.
#' @param r The radius of dots; either a channel or constant.
#' @param reverse Applies a transform to reverse the order of the mark’s index, say for reverse input order.
#' @param rotate The rotation angle in degrees clockwise; a constant or a channel; defaults to 0°.
#' @param select Applies a filter transform after data is loaded to highlight selected values only.
#' @param shapeRendering The shape-rendering; a constant string such as *crispEdges*.
#' @param sort Either applies a transform to sort the mark’s index by the specified channel values, or imputes ordinal scale domains from this mark’s channels.
#' @param stroke The stroke; a constant CSS color string, or a channel typically bound to the *color* scale.
#' @param strokeDasharray The stroke-dasharray; a constant number indicating the length in pixels of alternating dashes and gaps, or a constant string of numbers separated by spaces or commas (_e.g._, *10 2* for dashes of 10 pixels separated by gaps of 2 pixels), or *none* (the default) for no dashing : https://developer.mozilla.org/en-US/docs/Web/SVG/Attribute/stroke-dasharray
#' @param strokeDashoffset The stroke-dashoffset; a constant indicating the offset in pixels of the first dash along the stroke; defaults to zero.
#' @param strokeLinecap The stroke-linecap; a constant specifying how to cap stroked paths, such as *butt*, *round*, or *square*.
#' @param strokeLinejoin The stroke-linejoin; a constant specifying how to join stroked paths, such as *bevel*, *miter*, *miter-clip*, or *round*.
#' @param strokeMiterlimit The stroke-miterlimit; a constant number specifying how to limit the length of *miter* joins on stroked paths.
#' @param strokeOpacity The stroke-opacity; a constant between 0 and 1, or a channel typically bound to the *opacity* scale.
#' @param strokeWidth The stroke-width; a constant number in pixels, or a channel.
#' @param symbol The categorical symbol; either a channel or a constant.
#' @param target The target; a constant string specifying the target window (_e.g._, *_blank*) for clickable links; used in conjunction with the **href** option.
#' @param tip Whether to generate a tooltip for this mark, and any tip options.
#' @param title The title; a channel specifying accessible, short textual descriptions as strings (possibly with newlines).
#' @param x The horizontal position (or length) channel, typically bound to the *x* scale.
#' @param y The vertical position channel, typically bound to the *y* scale; defaults to the zero-based index of the data 0, 1, 2, ….
#' @param z An optional ordinal channel for grouping data into (possibly stacked) series to be drawn as separate areas; defaults to **fill** if a channel, or **stroke** if a channel.
#' @param ... Additional options or plot-level attributes.
#' @param data_from The name of the data source this mark reads from (see [vg_data()]).
#' @param filter_by A Param/Selection (e.g. from [param()]) to filter this mark's data by.
#' @family mark functions
#' @export
vg_hexagon <- function(spec = NULL, ariaDescription = vg_unset, ariaHidden = vg_unset, ariaLabel = vg_unset, channels = vg_unset, clip = vg_unset, dx = vg_unset, dy = vg_unset, facet = vg_unset, facetAnchor = vg_unset, fill = vg_unset, fillOpacity = vg_unset, filter = vg_unset, frameAnchor = vg_unset, fx = vg_unset, fy = vg_unset, href = vg_unset, imageFilter = vg_unset, margin = vg_unset, marginBottom = vg_unset, marginLeft = vg_unset, marginRight = vg_unset, marginTop = vg_unset, mixBlendMode = vg_unset, opacity = vg_unset, paintOrder = vg_unset, pointerEvents = vg_unset, r = vg_unset, reverse = vg_unset, rotate = vg_unset, select = vg_unset, shapeRendering = vg_unset, sort = vg_unset, stroke = vg_unset, strokeDasharray = vg_unset, strokeDashoffset = vg_unset, strokeLinecap = vg_unset, strokeLinejoin = vg_unset, strokeMiterlimit = vg_unset, strokeOpacity = vg_unset, strokeWidth = vg_unset, symbol = vg_unset, target = vg_unset, tip = vg_unset, title = vg_unset, x = vg_unset, y = vg_unset, z = vg_unset, ..., data_from = vg_unset, filter_by = vg_unset) {
  vg_mark_(spec, "hexagon", ariaDescription = ariaDescription, ariaHidden = ariaHidden, ariaLabel = ariaLabel, channels = channels, clip = clip, dx = dx, dy = dy, facet = facet, facetAnchor = facetAnchor, fill = fill, fillOpacity = fillOpacity, filter = filter, frameAnchor = frameAnchor, fx = fx, fy = fy, href = href, imageFilter = imageFilter, margin = margin, marginBottom = marginBottom, marginLeft = marginLeft, marginRight = marginRight, marginTop = marginTop, mixBlendMode = mixBlendMode, opacity = opacity, paintOrder = paintOrder, pointerEvents = pointerEvents, r = r, reverse = reverse, rotate = rotate, select = select, shapeRendering = shapeRendering, sort = sort, stroke = stroke, strokeDasharray = strokeDasharray, strokeDashoffset = strokeDashoffset, strokeLinecap = strokeLinecap, strokeLinejoin = strokeLinejoin, strokeMiterlimit = strokeMiterlimit, strokeOpacity = strokeOpacity, strokeWidth = strokeWidth, symbol = symbol, target = target, tip = tip, title = title, x = x, y = y, z = z, ..., data_from = data_from, filter_by = filter_by)
}

#' The hexbin mark.
#'
#' @param spec A plot fragment or `vgspec` to add this mark to, or `NULL` to start a new plot with just this mark.
#' @param ariaDescription The aria-description; a constant textual description.
#' @param ariaHidden The aria-hidden state; a constant indicating whether the element is exposed to an accessibility API.
#' @param ariaLabel The aria-label; a channel specifying short textual labels representing the value in the accessibility tree.
#' @param binWidth The distance between centers of neighboring hexagons, in pixels; defaults to 20.
#' @param channels Additional named channels, for example to include in a tooltip.
#' @param clip How to clip the mark; one of: - *frame* or true - clip to the plot’s frame (inner area) - *sphere* - clip to the projected sphere (*e.g.*, front hemisphere) - null or false - do not clip The *sphere* clip option requires a geographic projection.
#' @param dx The horizontal offset in pixels; a constant option.
#' @param dy The vertical offset in pixels; a constant option.
#' @param facet Whether to enable or disable faceting; one of: - *auto* (default) - automatically determine if this mark should be faceted - *include* (or true) - draw the subset of the mark’s data in the current facet - *exclude* - draw the subset of the mark’s data *not* in the current facet - *super* - draw this mark in a single frame that covers all facets - null (or false) - repeat this mark’s data across all facets (*i.e.*, no faceting) When a mark uses *super* faceting, it is not allowed to use position scales (*x*, *y*, *fx*, or *fy*); *super* faceting is intended for decorations, such as labels and legends.
#' @param facetAnchor How to place the mark with respect to facets; one of: - null (default for most marks) - display the mark in each non-empty facet - *top*, *right*, *bottom*, or *left* - display the mark only in facets on the given side - *top-empty*, *right-empty*, *bottom-empty*, or *left-empty* (default for axis marks) - display the mark only in facets that have empty space on the given side: either the margin, or an empty facet - *empty* - display the mark in empty facets only
#' @param fill The fill; a constant CSS color string, or a channel typically bound to the *color* scale.
#' @param fillOpacity The fill-opacity; a constant number between 0 and 1, or a channel typically bound to the *opacity* scale.
#' @param filter Applies a transform to filter the mark’s index according to the given channel values; only truthy values are retained.
#' @param fontFamily The font-family; a constant; defaults to the plot’s font family, which is typically *system-ui*.
#' @param fontSize The font size in pixels; either a constant or a channel; defaults to the plot’s font size, which is typically 10.
#' @param fontStyle The font style; a constant; defaults to the plot’s font style, which is typically *normal*.
#' @param fontVariant The font variant; a constant; if the **text** channel contains numbers or dates, defaults to *tabular-nums* to facilitate comparing numbers; otherwise defaults to the plot’s font style, which is typically *normal*.
#' @param fontWeight The font weight; a constant; defaults to the plot’s font weight, which is typically *normal*.
#' @param frameAnchor The frame anchor specifies defaults for **x** and **y**, along with **textAnchor** and **lineAnchor**, based on the plot’s frame; it may be one of the four sides (*top*, *right*, *bottom*, *left*), one of the four corners (*top-left*, *top-right*, *bottom-right*, *bottom-left*), or the *middle* of the frame.
#' @param fx The horizontal facet position channel, for mark-level faceting, bound to the *fx* scale.
#' @param fy The vertical facet position channel, for mark-level faceting, bound to the *fy* scale.
#' @param href The href; a channel specifying URLs for clickable links.
#' @param imageFilter A CSS filter; a constant string used to adjust the rendering of images, such as *blur(5px)*.
#' @param lineHeight The line height in ems; defaults to 1.
#' @param lineWidth The line width in ems (e.g., 10 for about 20 characters); defaults to infinity, disabling wrapping and clipping.
#' @param margin Shorthand to set the same default for all four mark margins: **marginTop**, **marginRight**, **marginBottom**, and **marginLeft**; typically defaults to 0, except for axis marks.
#' @param marginBottom The mark’s bottom margin; the minimum distance in pixels between the bottom edges of the inner and outer plot area.
#' @param marginLeft The mark’s left margin; the minimum distance in pixels between the left edges of the inner and outer plot area.
#' @param marginRight The mark’s right margin; the minimum distance in pixels between the right edges of the mark’s inner and outer plot area.
#' @param marginTop The mark’s top margin; the minimum distance in pixels between the top edges of the inner and outer plot area.
#' @param mixBlendMode The mix-blend-mode; a constant string specifying how to blend content such as *multiply*.
#' @param monospace If true, changes the default **fontFamily** to *monospace*, and uses simplified monospaced text metrics calculations.
#' @param opacity The opacity; a constant between 0 and 1, or a channel typically bound to the *opacity* scale.
#' @param paintOrder The paint-order; a constant string specifying the order in which the **fill**, **stroke**, and any markers are drawn; defaults to *normal*, which draws the fill, then stroke, then markers; defaults to *stroke* for the text mark to create a “halo” around text to improve legibility.
#' @param pointerEvents The pointer-events property; a constant string such as *none*.
#' @param r The radius of dots; either a channel or constant.
#' @param reverse Applies a transform to reverse the order of the mark’s index, say for reverse input order.
#' @param rotate The rotation angle in degrees clockwise; a constant or a channel; defaults to 0°.
#' @param select Applies a filter transform after data is loaded to highlight selected values only.
#' @param shapeRendering The shape-rendering; a constant string such as *crispEdges*.
#' @param sort Either applies a transform to sort the mark’s index by the specified channel values, or imputes ordinal scale domains from this mark’s channels.
#' @param stroke The stroke; a constant CSS color string, or a channel typically bound to the *color* scale.
#' @param strokeDasharray The stroke-dasharray; a constant number indicating the length in pixels of alternating dashes and gaps, or a constant string of numbers separated by spaces or commas (_e.g._, *10 2* for dashes of 10 pixels separated by gaps of 2 pixels), or *none* (the default) for no dashing : https://developer.mozilla.org/en-US/docs/Web/SVG/Attribute/stroke-dasharray
#' @param strokeDashoffset The stroke-dashoffset; a constant indicating the offset in pixels of the first dash along the stroke; defaults to zero.
#' @param strokeLinecap The stroke-linecap; a constant specifying how to cap stroked paths, such as *butt*, *round*, or *square*.
#' @param strokeLinejoin The stroke-linejoin; a constant specifying how to join stroked paths, such as *bevel*, *miter*, *miter-clip*, or *round*.
#' @param strokeMiterlimit The stroke-miterlimit; a constant number specifying how to limit the length of *miter* joins on stroked paths.
#' @param strokeOpacity The stroke-opacity; a constant between 0 and 1, or a channel typically bound to the *opacity* scale.
#' @param strokeWidth The stroke-width; a constant number in pixels, or a channel.
#' @param symbol The categorical symbol; either a channel or a constant.
#' @param target The target; a constant string specifying the target window (_e.g._, *_blank*) for clickable links; used in conjunction with the **href** option.
#' @param textAnchor The text anchor controls how text is aligned (typically horizontally) relative to its anchor point; it is one of *start*, *end*, or *middle*.
#' @param textOverflow How truncate (or wrap) lines of text longer than the given **lineWidth**; one of: - null (default) - preserve overflowing characters (and wrap if needed) - *clip* or *clip-end* - remove characters from the end - *clip-start* - remove characters from the start - *ellipsis* or *ellipsis-end* - replace characters from the end with an ellipsis (…) - *ellipsis-start* - replace characters from the start with an ellipsis (…) - *ellipsis-middle* - replace characters from the middle with an ellipsis (…) If no **title** was specified, if text requires truncation, a title containing the non-truncated text will be implicitly added.
#' @param tip Whether to generate a tooltip for this mark, and any tip options.
#' @param title The title; a channel specifying accessible, short textual descriptions as strings (possibly with newlines).
#' @param type The basic mark type to use to render 2D density values.
#' @param x The horizontal position (or length) channel, typically bound to the *x* scale.
#' @param y The vertical position channel, typically bound to the *y* scale; defaults to the zero-based index of the data 0, 1, 2, ….
#' @param z An optional ordinal channel for grouping data into (possibly stacked) series to be drawn as separate areas; defaults to **fill** if a channel, or **stroke** if a channel.
#' @param ... Additional options or plot-level attributes.
#' @param data_from The name of the data source this mark reads from (see [vg_data()]).
#' @param filter_by A Param/Selection (e.g. from [param()]) to filter this mark's data by.
#' @family mark functions
#' @export
vg_hexbin <- function(spec = NULL, ariaDescription = vg_unset, ariaHidden = vg_unset, ariaLabel = vg_unset, binWidth = vg_unset, channels = vg_unset, clip = vg_unset, dx = vg_unset, dy = vg_unset, facet = vg_unset, facetAnchor = vg_unset, fill = vg_unset, fillOpacity = vg_unset, filter = vg_unset, fontFamily = vg_unset, fontSize = vg_unset, fontStyle = vg_unset, fontVariant = vg_unset, fontWeight = vg_unset, frameAnchor = vg_unset, fx = vg_unset, fy = vg_unset, href = vg_unset, imageFilter = vg_unset, lineHeight = vg_unset, lineWidth = vg_unset, margin = vg_unset, marginBottom = vg_unset, marginLeft = vg_unset, marginRight = vg_unset, marginTop = vg_unset, mixBlendMode = vg_unset, monospace = vg_unset, opacity = vg_unset, paintOrder = vg_unset, pointerEvents = vg_unset, r = vg_unset, reverse = vg_unset, rotate = vg_unset, select = vg_unset, shapeRendering = vg_unset, sort = vg_unset, stroke = vg_unset, strokeDasharray = vg_unset, strokeDashoffset = vg_unset, strokeLinecap = vg_unset, strokeLinejoin = vg_unset, strokeMiterlimit = vg_unset, strokeOpacity = vg_unset, strokeWidth = vg_unset, symbol = vg_unset, target = vg_unset, textAnchor = vg_unset, textOverflow = vg_unset, tip = vg_unset, title = vg_unset, type = vg_unset, x = vg_unset, y = vg_unset, z = vg_unset, ..., data_from = vg_unset, filter_by = vg_unset) {
  vg_mark_(spec, "hexbin", ariaDescription = ariaDescription, ariaHidden = ariaHidden, ariaLabel = ariaLabel, binWidth = binWidth, channels = channels, clip = clip, dx = dx, dy = dy, facet = facet, facetAnchor = facetAnchor, fill = fill, fillOpacity = fillOpacity, filter = filter, fontFamily = fontFamily, fontSize = fontSize, fontStyle = fontStyle, fontVariant = fontVariant, fontWeight = fontWeight, frameAnchor = frameAnchor, fx = fx, fy = fy, href = href, imageFilter = imageFilter, lineHeight = lineHeight, lineWidth = lineWidth, margin = margin, marginBottom = marginBottom, marginLeft = marginLeft, marginRight = marginRight, marginTop = marginTop, mixBlendMode = mixBlendMode, monospace = monospace, opacity = opacity, paintOrder = paintOrder, pointerEvents = pointerEvents, r = r, reverse = reverse, rotate = rotate, select = select, shapeRendering = shapeRendering, sort = sort, stroke = stroke, strokeDasharray = strokeDasharray, strokeDashoffset = strokeDashoffset, strokeLinecap = strokeLinecap, strokeLinejoin = strokeLinejoin, strokeMiterlimit = strokeMiterlimit, strokeOpacity = strokeOpacity, strokeWidth = strokeWidth, symbol = symbol, target = target, textAnchor = textAnchor, textOverflow = textOverflow, tip = tip, title = title, type = type, x = x, y = y, z = z, ..., data_from = data_from, filter_by = filter_by)
}

#' The hexgrid mark.
#'
#' @param spec A plot fragment or `vgspec` to add this mark to, or `NULL` to start a new plot with just this mark.
#' @param ariaDescription The aria-description; a constant textual description.
#' @param ariaHidden The aria-hidden state; a constant indicating whether the element is exposed to an accessibility API.
#' @param ariaLabel The aria-label; a channel specifying short textual labels representing the value in the accessibility tree.
#' @param binWidth The distance between centers of neighboring hexagons, in pixels; defaults to 20.
#' @param channels Additional named channels, for example to include in a tooltip.
#' @param clip How to clip the mark; one of: - *frame* or true - clip to the plot’s frame (inner area) - *sphere* - clip to the projected sphere (*e.g.*, front hemisphere) - null or false - do not clip The *sphere* clip option requires a geographic projection.
#' @param dx The horizontal offset in pixels; a constant option.
#' @param dy The vertical offset in pixels; a constant option.
#' @param facet Whether to enable or disable faceting; one of: - *auto* (default) - automatically determine if this mark should be faceted - *include* (or true) - draw the subset of the mark’s data in the current facet - *exclude* - draw the subset of the mark’s data *not* in the current facet - *super* - draw this mark in a single frame that covers all facets - null (or false) - repeat this mark’s data across all facets (*i.e.*, no faceting) When a mark uses *super* faceting, it is not allowed to use position scales (*x*, *y*, *fx*, or *fy*); *super* faceting is intended for decorations, such as labels and legends.
#' @param facetAnchor How to place the mark with respect to facets; one of: - null (default for most marks) - display the mark in each non-empty facet - *top*, *right*, *bottom*, or *left* - display the mark only in facets on the given side - *top-empty*, *right-empty*, *bottom-empty*, or *left-empty* (default for axis marks) - display the mark only in facets that have empty space on the given side: either the margin, or an empty facet - *empty* - display the mark in empty facets only
#' @param fill The fill; a constant CSS color string, or a channel typically bound to the *color* scale.
#' @param fillOpacity The fill-opacity; a constant number between 0 and 1, or a channel typically bound to the *opacity* scale.
#' @param filter Applies a transform to filter the mark’s index according to the given channel values; only truthy values are retained.
#' @param fx The horizontal facet position channel, for mark-level faceting, bound to the *fx* scale.
#' @param fy The vertical facet position channel, for mark-level faceting, bound to the *fy* scale.
#' @param href The href; a channel specifying URLs for clickable links.
#' @param imageFilter A CSS filter; a constant string used to adjust the rendering of images, such as *blur(5px)*.
#' @param margin Shorthand to set the same default for all four mark margins: **marginTop**, **marginRight**, **marginBottom**, and **marginLeft**; typically defaults to 0, except for axis marks.
#' @param marginBottom The mark’s bottom margin; the minimum distance in pixels between the bottom edges of the inner and outer plot area.
#' @param marginLeft The mark’s left margin; the minimum distance in pixels between the left edges of the inner and outer plot area.
#' @param marginRight The mark’s right margin; the minimum distance in pixels between the right edges of the mark’s inner and outer plot area.
#' @param marginTop The mark’s top margin; the minimum distance in pixels between the top edges of the inner and outer plot area.
#' @param mixBlendMode The mix-blend-mode; a constant string specifying how to blend content such as *multiply*.
#' @param opacity The opacity; a constant between 0 and 1, or a channel typically bound to the *opacity* scale.
#' @param paintOrder The paint-order; a constant string specifying the order in which the **fill**, **stroke**, and any markers are drawn; defaults to *normal*, which draws the fill, then stroke, then markers; defaults to *stroke* for the text mark to create a “halo” around text to improve legibility.
#' @param pointerEvents The pointer-events property; a constant string such as *none*.
#' @param reverse Applies a transform to reverse the order of the mark’s index, say for reverse input order.
#' @param select Applies a filter transform after data is loaded to highlight selected values only.
#' @param shapeRendering The shape-rendering; a constant string such as *crispEdges*.
#' @param sort Either applies a transform to sort the mark’s index by the specified channel values, or imputes ordinal scale domains from this mark’s channels.
#' @param stroke The stroke; a constant CSS color string, or a channel typically bound to the *color* scale.
#' @param strokeDasharray The stroke-dasharray; a constant number indicating the length in pixels of alternating dashes and gaps, or a constant string of numbers separated by spaces or commas (_e.g._, *10 2* for dashes of 10 pixels separated by gaps of 2 pixels), or *none* (the default) for no dashing : https://developer.mozilla.org/en-US/docs/Web/SVG/Attribute/stroke-dasharray
#' @param strokeDashoffset The stroke-dashoffset; a constant indicating the offset in pixels of the first dash along the stroke; defaults to zero.
#' @param strokeLinecap The stroke-linecap; a constant specifying how to cap stroked paths, such as *butt*, *round*, or *square*.
#' @param strokeLinejoin The stroke-linejoin; a constant specifying how to join stroked paths, such as *bevel*, *miter*, *miter-clip*, or *round*.
#' @param strokeMiterlimit The stroke-miterlimit; a constant number specifying how to limit the length of *miter* joins on stroked paths.
#' @param strokeOpacity The stroke-opacity; a constant between 0 and 1, or a channel typically bound to the *opacity* scale.
#' @param strokeWidth The stroke-width; a constant number in pixels, or a channel.
#' @param target The target; a constant string specifying the target window (_e.g._, *_blank*) for clickable links; used in conjunction with the **href** option.
#' @param tip Whether to generate a tooltip for this mark, and any tip options.
#' @param title The title; a channel specifying accessible, short textual descriptions as strings (possibly with newlines).
#' @param ... Additional options or plot-level attributes.
#' @param data_from The name of the data source this mark reads from (see [vg_data()]).
#' @param filter_by A Param/Selection (e.g. from [param()]) to filter this mark's data by.
#' @family mark functions
#' @export
vg_hexgrid <- function(spec = NULL, ariaDescription = vg_unset, ariaHidden = vg_unset, ariaLabel = vg_unset, binWidth = vg_unset, channels = vg_unset, clip = vg_unset, dx = vg_unset, dy = vg_unset, facet = vg_unset, facetAnchor = vg_unset, fill = vg_unset, fillOpacity = vg_unset, filter = vg_unset, fx = vg_unset, fy = vg_unset, href = vg_unset, imageFilter = vg_unset, margin = vg_unset, marginBottom = vg_unset, marginLeft = vg_unset, marginRight = vg_unset, marginTop = vg_unset, mixBlendMode = vg_unset, opacity = vg_unset, paintOrder = vg_unset, pointerEvents = vg_unset, reverse = vg_unset, select = vg_unset, shapeRendering = vg_unset, sort = vg_unset, stroke = vg_unset, strokeDasharray = vg_unset, strokeDashoffset = vg_unset, strokeLinecap = vg_unset, strokeLinejoin = vg_unset, strokeMiterlimit = vg_unset, strokeOpacity = vg_unset, strokeWidth = vg_unset, target = vg_unset, tip = vg_unset, title = vg_unset, ..., data_from = vg_unset, filter_by = vg_unset) {
  vg_mark_(spec, "hexgrid", ariaDescription = ariaDescription, ariaHidden = ariaHidden, ariaLabel = ariaLabel, binWidth = binWidth, channels = channels, clip = clip, dx = dx, dy = dy, facet = facet, facetAnchor = facetAnchor, fill = fill, fillOpacity = fillOpacity, filter = filter, fx = fx, fy = fy, href = href, imageFilter = imageFilter, margin = margin, marginBottom = marginBottom, marginLeft = marginLeft, marginRight = marginRight, marginTop = marginTop, mixBlendMode = mixBlendMode, opacity = opacity, paintOrder = paintOrder, pointerEvents = pointerEvents, reverse = reverse, select = select, shapeRendering = shapeRendering, sort = sort, stroke = stroke, strokeDasharray = strokeDasharray, strokeDashoffset = strokeDashoffset, strokeLinecap = strokeLinecap, strokeLinejoin = strokeLinejoin, strokeMiterlimit = strokeMiterlimit, strokeOpacity = strokeOpacity, strokeWidth = strokeWidth, target = target, tip = tip, title = title, ..., data_from = data_from, filter_by = filter_by)
}

#' The hull mark.
#'
#' @param spec A plot fragment or `vgspec` to add this mark to, or `NULL` to start a new plot with just this mark.
#' @param ariaDescription The aria-description; a constant textual description.
#' @param ariaHidden The aria-hidden state; a constant indicating whether the element is exposed to an accessibility API.
#' @param ariaLabel The aria-label; a channel specifying short textual labels representing the value in the accessibility tree.
#' @param channels Additional named channels, for example to include in a tooltip.
#' @param clip How to clip the mark; one of: - *frame* or true - clip to the plot’s frame (inner area) - *sphere* - clip to the projected sphere (*e.g.*, front hemisphere) - null or false - do not clip The *sphere* clip option requires a geographic projection.
#' @param curve The curve (interpolation) method for connecting adjacent points.
#' @param dx The horizontal offset in pixels; a constant option.
#' @param dy The vertical offset in pixels; a constant option.
#' @param facet Whether to enable or disable faceting; one of: - *auto* (default) - automatically determine if this mark should be faceted - *include* (or true) - draw the subset of the mark’s data in the current facet - *exclude* - draw the subset of the mark’s data *not* in the current facet - *super* - draw this mark in a single frame that covers all facets - null (or false) - repeat this mark’s data across all facets (*i.e.*, no faceting) When a mark uses *super* faceting, it is not allowed to use position scales (*x*, *y*, *fx*, or *fy*); *super* faceting is intended for decorations, such as labels and legends.
#' @param facetAnchor How to place the mark with respect to facets; one of: - null (default for most marks) - display the mark in each non-empty facet - *top*, *right*, *bottom*, or *left* - display the mark only in facets on the given side - *top-empty*, *right-empty*, *bottom-empty*, or *left-empty* (default for axis marks) - display the mark only in facets that have empty space on the given side: either the margin, or an empty facet - *empty* - display the mark in empty facets only
#' @param fill The fill; a constant CSS color string, or a channel typically bound to the *color* scale.
#' @param fillOpacity The fill-opacity; a constant number between 0 and 1, or a channel typically bound to the *opacity* scale.
#' @param filter Applies a transform to filter the mark’s index according to the given channel values; only truthy values are retained.
#' @param fx The horizontal facet position channel, for mark-level faceting, bound to the *fx* scale.
#' @param fy The vertical facet position channel, for mark-level faceting, bound to the *fy* scale.
#' @param href The href; a channel specifying URLs for clickable links.
#' @param imageFilter A CSS filter; a constant string used to adjust the rendering of images, such as *blur(5px)*.
#' @param margin Shorthand to set the same default for all four mark margins: **marginTop**, **marginRight**, **marginBottom**, and **marginLeft**; typically defaults to 0, except for axis marks.
#' @param marginBottom The mark’s bottom margin; the minimum distance in pixels between the bottom edges of the inner and outer plot area.
#' @param marginLeft The mark’s left margin; the minimum distance in pixels between the left edges of the inner and outer plot area.
#' @param marginRight The mark’s right margin; the minimum distance in pixels between the right edges of the mark’s inner and outer plot area.
#' @param marginTop The mark’s top margin; the minimum distance in pixels between the top edges of the inner and outer plot area.
#' @param marker Shorthand to set the same default for markerStart, markerMid, and markerEnd; one of: - a marker name such as *arrow* or *circle* - *none* (default) - no marker * true - alias for *circle-fill* * false or null - alias for *none*
#' @param markerEnd The marker for the ending point of a line segment; one of: - a marker name such as *arrow* or *circle* * *none* (default) - no marker * true - alias for *circle-fill* * false or null - alias for *none*
#' @param markerMid The marker for any middle (interior) points of a line segment.
#' @param markerStart The marker for the starting point of a line segment; one of: - a marker name such as *arrow* or *circle* * *none* (default) - no marker * true - alias for *circle-fill* * false or null - alias for *none*
#' @param mixBlendMode The mix-blend-mode; a constant string specifying how to blend content such as *multiply*.
#' @param opacity The opacity; a constant between 0 and 1, or a channel typically bound to the *opacity* scale.
#' @param paintOrder The paint-order; a constant string specifying the order in which the **fill**, **stroke**, and any markers are drawn; defaults to *normal*, which draws the fill, then stroke, then markers; defaults to *stroke* for the text mark to create a “halo” around text to improve legibility.
#' @param pointerEvents The pointer-events property; a constant string such as *none*.
#' @param reverse Applies a transform to reverse the order of the mark’s index, say for reverse input order.
#' @param select Applies a filter transform after data is loaded to highlight selected values only.
#' @param shapeRendering The shape-rendering; a constant string such as *crispEdges*.
#' @param sort Either applies a transform to sort the mark’s index by the specified channel values, or imputes ordinal scale domains from this mark’s channels.
#' @param stroke The stroke; a constant CSS color string, or a channel typically bound to the *color* scale.
#' @param strokeDasharray The stroke-dasharray; a constant number indicating the length in pixels of alternating dashes and gaps, or a constant string of numbers separated by spaces or commas (_e.g._, *10 2* for dashes of 10 pixels separated by gaps of 2 pixels), or *none* (the default) for no dashing : https://developer.mozilla.org/en-US/docs/Web/SVG/Attribute/stroke-dasharray
#' @param strokeDashoffset The stroke-dashoffset; a constant indicating the offset in pixels of the first dash along the stroke; defaults to zero.
#' @param strokeLinecap The stroke-linecap; a constant specifying how to cap stroked paths, such as *butt*, *round*, or *square*.
#' @param strokeLinejoin The stroke-linejoin; a constant specifying how to join stroked paths, such as *bevel*, *miter*, *miter-clip*, or *round*.
#' @param strokeMiterlimit The stroke-miterlimit; a constant number specifying how to limit the length of *miter* joins on stroked paths.
#' @param strokeOpacity The stroke-opacity; a constant between 0 and 1, or a channel typically bound to the *opacity* scale.
#' @param strokeWidth The stroke-width; a constant number in pixels, or a channel.
#' @param target The target; a constant string specifying the target window (_e.g._, *_blank*) for clickable links; used in conjunction with the **href** option.
#' @param tension The tension option only has an effect on bundle, cardinal and Catmull–Rom splines (*bundle*, *cardinal*, *cardinal-open*, *cardinal-closed*, *catmull-rom*, *catmull-rom-open*, and *catmull-rom-closed*).
#' @param tip Whether to generate a tooltip for this mark, and any tip options.
#' @param title The title; a channel specifying accessible, short textual descriptions as strings (possibly with newlines).
#' @param x The horizontal position (or length) channel, typically bound to the *x* scale.
#' @param y The vertical position channel, typically bound to the *y* scale; defaults to the zero-based index of the data 0, 1, 2, ….
#' @param z An optional ordinal channel for grouping data into (possibly stacked) series to be drawn as separate areas; defaults to **fill** if a channel, or **stroke** if a channel.
#' @param ... Additional options or plot-level attributes.
#' @param data_from The name of the data source this mark reads from (see [vg_data()]).
#' @param filter_by A Param/Selection (e.g. from [param()]) to filter this mark's data by.
#' @family mark functions
#' @export
vg_hull <- function(spec = NULL, ariaDescription = vg_unset, ariaHidden = vg_unset, ariaLabel = vg_unset, channels = vg_unset, clip = vg_unset, curve = vg_unset, dx = vg_unset, dy = vg_unset, facet = vg_unset, facetAnchor = vg_unset, fill = vg_unset, fillOpacity = vg_unset, filter = vg_unset, fx = vg_unset, fy = vg_unset, href = vg_unset, imageFilter = vg_unset, margin = vg_unset, marginBottom = vg_unset, marginLeft = vg_unset, marginRight = vg_unset, marginTop = vg_unset, marker = vg_unset, markerEnd = vg_unset, markerMid = vg_unset, markerStart = vg_unset, mixBlendMode = vg_unset, opacity = vg_unset, paintOrder = vg_unset, pointerEvents = vg_unset, reverse = vg_unset, select = vg_unset, shapeRendering = vg_unset, sort = vg_unset, stroke = vg_unset, strokeDasharray = vg_unset, strokeDashoffset = vg_unset, strokeLinecap = vg_unset, strokeLinejoin = vg_unset, strokeMiterlimit = vg_unset, strokeOpacity = vg_unset, strokeWidth = vg_unset, target = vg_unset, tension = vg_unset, tip = vg_unset, title = vg_unset, x = vg_unset, y = vg_unset, z = vg_unset, ..., data_from = vg_unset, filter_by = vg_unset) {
  vg_mark_(spec, "hull", ariaDescription = ariaDescription, ariaHidden = ariaHidden, ariaLabel = ariaLabel, channels = channels, clip = clip, curve = curve, dx = dx, dy = dy, facet = facet, facetAnchor = facetAnchor, fill = fill, fillOpacity = fillOpacity, filter = filter, fx = fx, fy = fy, href = href, imageFilter = imageFilter, margin = margin, marginBottom = marginBottom, marginLeft = marginLeft, marginRight = marginRight, marginTop = marginTop, marker = marker, markerEnd = markerEnd, markerMid = markerMid, markerStart = markerStart, mixBlendMode = mixBlendMode, opacity = opacity, paintOrder = paintOrder, pointerEvents = pointerEvents, reverse = reverse, select = select, shapeRendering = shapeRendering, sort = sort, stroke = stroke, strokeDasharray = strokeDasharray, strokeDashoffset = strokeDashoffset, strokeLinecap = strokeLinecap, strokeLinejoin = strokeLinejoin, strokeMiterlimit = strokeMiterlimit, strokeOpacity = strokeOpacity, strokeWidth = strokeWidth, target = target, tension = tension, tip = tip, title = title, x = x, y = y, z = z, ..., data_from = data_from, filter_by = filter_by)
}

#' The `image` mark.
#'
#' @param spec A plot fragment or `vgspec` to add this mark to, or `NULL` to start a new plot with just this mark.
#' @param ariaDescription The aria-description; a constant textual description.
#' @param ariaHidden The aria-hidden state; a constant indicating whether the element is exposed to an accessibility API.
#' @param ariaLabel The aria-label; a channel specifying short textual labels representing the value in the accessibility tree.
#' @param channels Additional named channels, for example to include in a tooltip.
#' @param clip How to clip the mark; one of: - *frame* or true - clip to the plot’s frame (inner area) - *sphere* - clip to the projected sphere (*e.g.*, front hemisphere) - null or false - do not clip The *sphere* clip option requires a geographic projection.
#' @param crossOrigin The cross-origin behavior.
#' @param dx The horizontal offset in pixels; a constant option.
#' @param dy The vertical offset in pixels; a constant option.
#' @param facet Whether to enable or disable faceting; one of: - *auto* (default) - automatically determine if this mark should be faceted - *include* (or true) - draw the subset of the mark’s data in the current facet - *exclude* - draw the subset of the mark’s data *not* in the current facet - *super* - draw this mark in a single frame that covers all facets - null (or false) - repeat this mark’s data across all facets (*i.e.*, no faceting) When a mark uses *super* faceting, it is not allowed to use position scales (*x*, *y*, *fx*, or *fy*); *super* faceting is intended for decorations, such as labels and legends.
#' @param facetAnchor How to place the mark with respect to facets; one of: - null (default for most marks) - display the mark in each non-empty facet - *top*, *right*, *bottom*, or *left* - display the mark only in facets on the given side - *top-empty*, *right-empty*, *bottom-empty*, or *left-empty* (default for axis marks) - display the mark only in facets that have empty space on the given side: either the margin, or an empty facet - *empty* - display the mark in empty facets only
#' @param fill The fill; a constant CSS color string, or a channel typically bound to the *color* scale.
#' @param fillOpacity The fill-opacity; a constant number between 0 and 1, or a channel typically bound to the *opacity* scale.
#' @param filter Applies a transform to filter the mark’s index according to the given channel values; only truthy values are retained.
#' @param frameAnchor The frame anchor specifies defaults for **x** and **y**, along with **textAnchor** and **lineAnchor**, based on the plot’s frame; it may be one of the four sides (*top*, *right*, *bottom*, *left*), one of the four corners (*top-left*, *top-right*, *bottom-right*, *bottom-left*), or the *middle* of the frame.
#' @param fx The horizontal facet position channel, for mark-level faceting, bound to the *fx* scale.
#' @param fy The vertical facet position channel, for mark-level faceting, bound to the *fy* scale.
#' @param height The height (number of rows) of the grid, in actual pixels.
#' @param href The href; a channel specifying URLs for clickable links.
#' @param imageFilter A CSS filter; a constant string used to adjust the rendering of images, such as *blur(5px)*.
#' @param imageRendering The image-rendering attribute; defaults to *auto* (bilinear).
#' @param margin Shorthand to set the same default for all four mark margins: **marginTop**, **marginRight**, **marginBottom**, and **marginLeft**; typically defaults to 0, except for axis marks.
#' @param marginBottom The mark’s bottom margin; the minimum distance in pixels between the bottom edges of the inner and outer plot area.
#' @param marginLeft The mark’s left margin; the minimum distance in pixels between the left edges of the inner and outer plot area.
#' @param marginRight The mark’s right margin; the minimum distance in pixels between the right edges of the mark’s inner and outer plot area.
#' @param marginTop The mark’s top margin; the minimum distance in pixels between the top edges of the inner and outer plot area.
#' @param mixBlendMode The mix-blend-mode; a constant string specifying how to blend content such as *multiply*.
#' @param opacity The opacity; a constant between 0 and 1, or a channel typically bound to the *opacity* scale.
#' @param paintOrder The paint-order; a constant string specifying the order in which the **fill**, **stroke**, and any markers are drawn; defaults to *normal*, which draws the fill, then stroke, then markers; defaults to *stroke* for the text mark to create a “halo” around text to improve legibility.
#' @param pointerEvents The pointer-events property; a constant string such as *none*.
#' @param preserveAspectRatio The image aspect ratio; defaults to *xMidYMid meet*.
#' @param r The radius of dots; either a channel or constant.
#' @param reverse Applies a transform to reverse the order of the mark’s index, say for reverse input order.
#' @param rotate The rotation angle in degrees clockwise; a constant or a channel; defaults to 0°.
#' @param select Applies a filter transform after data is loaded to highlight selected values only.
#' @param shapeRendering The shape-rendering; a constant string such as *crispEdges*.
#' @param sort Either applies a transform to sort the mark’s index by the specified channel values, or imputes ordinal scale domains from this mark’s channels.
#' @param src The required image URL (or relative path).
#' @param stroke The stroke; a constant CSS color string, or a channel typically bound to the *color* scale.
#' @param strokeDasharray The stroke-dasharray; a constant number indicating the length in pixels of alternating dashes and gaps, or a constant string of numbers separated by spaces or commas (_e.g._, *10 2* for dashes of 10 pixels separated by gaps of 2 pixels), or *none* (the default) for no dashing : https://developer.mozilla.org/en-US/docs/Web/SVG/Attribute/stroke-dasharray
#' @param strokeDashoffset The stroke-dashoffset; a constant indicating the offset in pixels of the first dash along the stroke; defaults to zero.
#' @param strokeLinecap The stroke-linecap; a constant specifying how to cap stroked paths, such as *butt*, *round*, or *square*.
#' @param strokeLinejoin The stroke-linejoin; a constant specifying how to join stroked paths, such as *bevel*, *miter*, *miter-clip*, or *round*.
#' @param strokeMiterlimit The stroke-miterlimit; a constant number specifying how to limit the length of *miter* joins on stroked paths.
#' @param strokeOpacity The stroke-opacity; a constant between 0 and 1, or a channel typically bound to the *opacity* scale.
#' @param strokeWidth The stroke-width; a constant number in pixels, or a channel.
#' @param target The target; a constant string specifying the target window (_e.g._, *_blank*) for clickable links; used in conjunction with the **href** option.
#' @param tip Whether to generate a tooltip for this mark, and any tip options.
#' @param title The title; a channel specifying accessible, short textual descriptions as strings (possibly with newlines).
#' @param width The width (number of columns) of the grid, in actual pixels.
#' @param x The horizontal position (or length) channel, typically bound to the *x* scale.
#' @param y The vertical position channel, typically bound to the *y* scale; defaults to the zero-based index of the data 0, 1, 2, ….
#' @param ... Additional options or plot-level attributes.
#' @param data_from The name of the data source this mark reads from (see [vg_data()]).
#' @param filter_by A Param/Selection (e.g. from [param()]) to filter this mark's data by.
#' @family mark functions
#' @export
vg_image <- function(spec = NULL, ariaDescription = vg_unset, ariaHidden = vg_unset, ariaLabel = vg_unset, channels = vg_unset, clip = vg_unset, crossOrigin = vg_unset, dx = vg_unset, dy = vg_unset, facet = vg_unset, facetAnchor = vg_unset, fill = vg_unset, fillOpacity = vg_unset, filter = vg_unset, frameAnchor = vg_unset, fx = vg_unset, fy = vg_unset, height = vg_unset, href = vg_unset, imageFilter = vg_unset, imageRendering = vg_unset, margin = vg_unset, marginBottom = vg_unset, marginLeft = vg_unset, marginRight = vg_unset, marginTop = vg_unset, mixBlendMode = vg_unset, opacity = vg_unset, paintOrder = vg_unset, pointerEvents = vg_unset, preserveAspectRatio = vg_unset, r = vg_unset, reverse = vg_unset, rotate = vg_unset, select = vg_unset, shapeRendering = vg_unset, sort = vg_unset, src = vg_unset, stroke = vg_unset, strokeDasharray = vg_unset, strokeDashoffset = vg_unset, strokeLinecap = vg_unset, strokeLinejoin = vg_unset, strokeMiterlimit = vg_unset, strokeOpacity = vg_unset, strokeWidth = vg_unset, target = vg_unset, tip = vg_unset, title = vg_unset, width = vg_unset, x = vg_unset, y = vg_unset, ..., data_from = vg_unset, filter_by = vg_unset) {
  vg_mark_(spec, "image", ariaDescription = ariaDescription, ariaHidden = ariaHidden, ariaLabel = ariaLabel, channels = channels, clip = clip, crossOrigin = crossOrigin, dx = dx, dy = dy, facet = facet, facetAnchor = facetAnchor, fill = fill, fillOpacity = fillOpacity, filter = filter, frameAnchor = frameAnchor, fx = fx, fy = fy, height = height, href = href, imageFilter = imageFilter, imageRendering = imageRendering, margin = margin, marginBottom = marginBottom, marginLeft = marginLeft, marginRight = marginRight, marginTop = marginTop, mixBlendMode = mixBlendMode, opacity = opacity, paintOrder = paintOrder, pointerEvents = pointerEvents, preserveAspectRatio = preserveAspectRatio, r = r, reverse = reverse, rotate = rotate, select = select, shapeRendering = shapeRendering, sort = sort, src = src, stroke = stroke, strokeDasharray = strokeDasharray, strokeDashoffset = strokeDashoffset, strokeLinecap = strokeLinecap, strokeLinejoin = strokeLinejoin, strokeMiterlimit = strokeMiterlimit, strokeOpacity = strokeOpacity, strokeWidth = strokeWidth, target = target, tip = tip, title = title, width = width, x = x, y = y, ..., data_from = data_from, filter_by = filter_by)
}

#' The line mark.
#'
#' @param spec A plot fragment or `vgspec` to add this mark to, or `NULL` to start a new plot with just this mark.
#' @param ariaDescription The aria-description; a constant textual description.
#' @param ariaHidden The aria-hidden state; a constant indicating whether the element is exposed to an accessibility API.
#' @param ariaLabel The aria-label; a channel specifying short textual labels representing the value in the accessibility tree.
#' @param channels Additional named channels, for example to include in a tooltip.
#' @param clip How to clip the mark; one of: - *frame* or true - clip to the plot’s frame (inner area) - *sphere* - clip to the projected sphere (*e.g.*, front hemisphere) - null or false - do not clip The *sphere* clip option requires a geographic projection.
#' @param curve The curve (interpolation) method for connecting adjacent points.
#' @param dx The horizontal offset in pixels; a constant option.
#' @param dy The vertical offset in pixels; a constant option.
#' @param facet Whether to enable or disable faceting; one of: - *auto* (default) - automatically determine if this mark should be faceted - *include* (or true) - draw the subset of the mark’s data in the current facet - *exclude* - draw the subset of the mark’s data *not* in the current facet - *super* - draw this mark in a single frame that covers all facets - null (or false) - repeat this mark’s data across all facets (*i.e.*, no faceting) When a mark uses *super* faceting, it is not allowed to use position scales (*x*, *y*, *fx*, or *fy*); *super* faceting is intended for decorations, such as labels and legends.
#' @param facetAnchor How to place the mark with respect to facets; one of: - null (default for most marks) - display the mark in each non-empty facet - *top*, *right*, *bottom*, or *left* - display the mark only in facets on the given side - *top-empty*, *right-empty*, *bottom-empty*, or *left-empty* (default for axis marks) - display the mark only in facets that have empty space on the given side: either the margin, or an empty facet - *empty* - display the mark in empty facets only
#' @param fill The fill; a constant CSS color string, or a channel typically bound to the *color* scale.
#' @param fillOpacity The fill-opacity; a constant number between 0 and 1, or a channel typically bound to the *opacity* scale.
#' @param filter Applies a transform to filter the mark’s index according to the given channel values; only truthy values are retained.
#' @param fx The horizontal facet position channel, for mark-level faceting, bound to the *fx* scale.
#' @param fy The vertical facet position channel, for mark-level faceting, bound to the *fy* scale.
#' @param href The href; a channel specifying URLs for clickable links.
#' @param imageFilter A CSS filter; a constant string used to adjust the rendering of images, such as *blur(5px)*.
#' @param margin Shorthand to set the same default for all four mark margins: **marginTop**, **marginRight**, **marginBottom**, and **marginLeft**; typically defaults to 0, except for axis marks.
#' @param marginBottom The mark’s bottom margin; the minimum distance in pixels between the bottom edges of the inner and outer plot area.
#' @param marginLeft The mark’s left margin; the minimum distance in pixels between the left edges of the inner and outer plot area.
#' @param marginRight The mark’s right margin; the minimum distance in pixels between the right edges of the mark’s inner and outer plot area.
#' @param marginTop The mark’s top margin; the minimum distance in pixels between the top edges of the inner and outer plot area.
#' @param marker Shorthand to set the same default for markerStart, markerMid, and markerEnd; one of: - a marker name such as *arrow* or *circle* - *none* (default) - no marker * true - alias for *circle-fill* * false or null - alias for *none*
#' @param markerEnd The marker for the ending point of a line segment; one of: - a marker name such as *arrow* or *circle* * *none* (default) - no marker * true - alias for *circle-fill* * false or null - alias for *none*
#' @param markerMid The marker for any middle (interior) points of a line segment.
#' @param markerStart The marker for the starting point of a line segment; one of: - a marker name such as *arrow* or *circle* * *none* (default) - no marker * true - alias for *circle-fill* * false or null - alias for *none*
#' @param mixBlendMode The mix-blend-mode; a constant string specifying how to blend content such as *multiply*.
#' @param opacity The opacity; a constant between 0 and 1, or a channel typically bound to the *opacity* scale.
#' @param paintOrder The paint-order; a constant string specifying the order in which the **fill**, **stroke**, and any markers are drawn; defaults to *normal*, which draws the fill, then stroke, then markers; defaults to *stroke* for the text mark to create a “halo” around text to improve legibility.
#' @param pointerEvents The pointer-events property; a constant string such as *none*.
#' @param reverse Applies a transform to reverse the order of the mark’s index, say for reverse input order.
#' @param select Applies a filter transform after data is loaded to highlight selected values only.
#' @param shapeRendering The shape-rendering; a constant string such as *crispEdges*.
#' @param sort Either applies a transform to sort the mark’s index by the specified channel values, or imputes ordinal scale domains from this mark’s channels.
#' @param stroke The stroke; a constant CSS color string, or a channel typically bound to the *color* scale.
#' @param strokeDasharray The stroke-dasharray; a constant number indicating the length in pixels of alternating dashes and gaps, or a constant string of numbers separated by spaces or commas (_e.g._, *10 2* for dashes of 10 pixels separated by gaps of 2 pixels), or *none* (the default) for no dashing : https://developer.mozilla.org/en-US/docs/Web/SVG/Attribute/stroke-dasharray
#' @param strokeDashoffset The stroke-dashoffset; a constant indicating the offset in pixels of the first dash along the stroke; defaults to zero.
#' @param strokeLinecap The stroke-linecap; a constant specifying how to cap stroked paths, such as *butt*, *round*, or *square*.
#' @param strokeLinejoin The stroke-linejoin; a constant specifying how to join stroked paths, such as *bevel*, *miter*, *miter-clip*, or *round*.
#' @param strokeMiterlimit The stroke-miterlimit; a constant number specifying how to limit the length of *miter* joins on stroked paths.
#' @param strokeOpacity The stroke-opacity; a constant between 0 and 1, or a channel typically bound to the *opacity* scale.
#' @param strokeWidth The stroke-width; a constant number in pixels, or a channel.
#' @param target The target; a constant string specifying the target window (_e.g._, *_blank*) for clickable links; used in conjunction with the **href** option.
#' @param tension The tension option only has an effect on bundle, cardinal and Catmull–Rom splines (*bundle*, *cardinal*, *cardinal-open*, *cardinal-closed*, *catmull-rom*, *catmull-rom-open*, and *catmull-rom-closed*).
#' @param tip Whether to generate a tooltip for this mark, and any tip options.
#' @param title The title; a channel specifying accessible, short textual descriptions as strings (possibly with newlines).
#' @param x The horizontal position (or length) channel, typically bound to the *x* scale.
#' @param y The vertical position channel, typically bound to the *y* scale; defaults to the zero-based index of the data 0, 1, 2, ….
#' @param z An optional ordinal channel for grouping data into (possibly stacked) series to be drawn as separate areas; defaults to **fill** if a channel, or **stroke** if a channel.
#' @param ... Additional options or plot-level attributes.
#' @param data_from The name of the data source this mark reads from (see [vg_data()]).
#' @param filter_by A Param/Selection (e.g. from [param()]) to filter this mark's data by.
#' @family mark functions
#' @export
vg_line <- function(spec = NULL, ariaDescription = vg_unset, ariaHidden = vg_unset, ariaLabel = vg_unset, channels = vg_unset, clip = vg_unset, curve = vg_unset, dx = vg_unset, dy = vg_unset, facet = vg_unset, facetAnchor = vg_unset, fill = vg_unset, fillOpacity = vg_unset, filter = vg_unset, fx = vg_unset, fy = vg_unset, href = vg_unset, imageFilter = vg_unset, margin = vg_unset, marginBottom = vg_unset, marginLeft = vg_unset, marginRight = vg_unset, marginTop = vg_unset, marker = vg_unset, markerEnd = vg_unset, markerMid = vg_unset, markerStart = vg_unset, mixBlendMode = vg_unset, opacity = vg_unset, paintOrder = vg_unset, pointerEvents = vg_unset, reverse = vg_unset, select = vg_unset, shapeRendering = vg_unset, sort = vg_unset, stroke = vg_unset, strokeDasharray = vg_unset, strokeDashoffset = vg_unset, strokeLinecap = vg_unset, strokeLinejoin = vg_unset, strokeMiterlimit = vg_unset, strokeOpacity = vg_unset, strokeWidth = vg_unset, target = vg_unset, tension = vg_unset, tip = vg_unset, title = vg_unset, x = vg_unset, y = vg_unset, z = vg_unset, ..., data_from = vg_unset, filter_by = vg_unset) {
  vg_mark_(spec, "line", ariaDescription = ariaDescription, ariaHidden = ariaHidden, ariaLabel = ariaLabel, channels = channels, clip = clip, curve = curve, dx = dx, dy = dy, facet = facet, facetAnchor = facetAnchor, fill = fill, fillOpacity = fillOpacity, filter = filter, fx = fx, fy = fy, href = href, imageFilter = imageFilter, margin = margin, marginBottom = marginBottom, marginLeft = marginLeft, marginRight = marginRight, marginTop = marginTop, marker = marker, markerEnd = markerEnd, markerMid = markerMid, markerStart = markerStart, mixBlendMode = mixBlendMode, opacity = opacity, paintOrder = paintOrder, pointerEvents = pointerEvents, reverse = reverse, select = select, shapeRendering = shapeRendering, sort = sort, stroke = stroke, strokeDasharray = strokeDasharray, strokeDashoffset = strokeDashoffset, strokeLinecap = strokeLinecap, strokeLinejoin = strokeLinejoin, strokeMiterlimit = strokeMiterlimit, strokeOpacity = strokeOpacity, strokeWidth = strokeWidth, target = target, tension = tension, tip = tip, title = title, x = x, y = y, z = z, ..., data_from = data_from, filter_by = filter_by)
}

#' The lineX mark.
#'
#' @param spec A plot fragment or `vgspec` to add this mark to, or `NULL` to start a new plot with just this mark.
#' @param ariaDescription The aria-description; a constant textual description.
#' @param ariaHidden The aria-hidden state; a constant indicating whether the element is exposed to an accessibility API.
#' @param ariaLabel The aria-label; a channel specifying short textual labels representing the value in the accessibility tree.
#' @param channels Additional named channels, for example to include in a tooltip.
#' @param clip How to clip the mark; one of: - *frame* or true - clip to the plot’s frame (inner area) - *sphere* - clip to the projected sphere (*e.g.*, front hemisphere) - null or false - do not clip The *sphere* clip option requires a geographic projection.
#' @param curve The curve (interpolation) method for connecting adjacent points.
#' @param dx The horizontal offset in pixels; a constant option.
#' @param dy The vertical offset in pixels; a constant option.
#' @param facet Whether to enable or disable faceting; one of: - *auto* (default) - automatically determine if this mark should be faceted - *include* (or true) - draw the subset of the mark’s data in the current facet - *exclude* - draw the subset of the mark’s data *not* in the current facet - *super* - draw this mark in a single frame that covers all facets - null (or false) - repeat this mark’s data across all facets (*i.e.*, no faceting) When a mark uses *super* faceting, it is not allowed to use position scales (*x*, *y*, *fx*, or *fy*); *super* faceting is intended for decorations, such as labels and legends.
#' @param facetAnchor How to place the mark with respect to facets; one of: - null (default for most marks) - display the mark in each non-empty facet - *top*, *right*, *bottom*, or *left* - display the mark only in facets on the given side - *top-empty*, *right-empty*, *bottom-empty*, or *left-empty* (default for axis marks) - display the mark only in facets that have empty space on the given side: either the margin, or an empty facet - *empty* - display the mark in empty facets only
#' @param fill The fill; a constant CSS color string, or a channel typically bound to the *color* scale.
#' @param fillOpacity The fill-opacity; a constant number between 0 and 1, or a channel typically bound to the *opacity* scale.
#' @param filter Applies a transform to filter the mark’s index according to the given channel values; only truthy values are retained.
#' @param fx The horizontal facet position channel, for mark-level faceting, bound to the *fx* scale.
#' @param fy The vertical facet position channel, for mark-level faceting, bound to the *fy* scale.
#' @param href The href; a channel specifying URLs for clickable links.
#' @param imageFilter A CSS filter; a constant string used to adjust the rendering of images, such as *blur(5px)*.
#' @param margin Shorthand to set the same default for all four mark margins: **marginTop**, **marginRight**, **marginBottom**, and **marginLeft**; typically defaults to 0, except for axis marks.
#' @param marginBottom The mark’s bottom margin; the minimum distance in pixels between the bottom edges of the inner and outer plot area.
#' @param marginLeft The mark’s left margin; the minimum distance in pixels between the left edges of the inner and outer plot area.
#' @param marginRight The mark’s right margin; the minimum distance in pixels between the right edges of the mark’s inner and outer plot area.
#' @param marginTop The mark’s top margin; the minimum distance in pixels between the top edges of the inner and outer plot area.
#' @param marker Shorthand to set the same default for markerStart, markerMid, and markerEnd; one of: - a marker name such as *arrow* or *circle* - *none* (default) - no marker * true - alias for *circle-fill* * false or null - alias for *none*
#' @param markerEnd The marker for the ending point of a line segment; one of: - a marker name such as *arrow* or *circle* * *none* (default) - no marker * true - alias for *circle-fill* * false or null - alias for *none*
#' @param markerMid The marker for any middle (interior) points of a line segment.
#' @param markerStart The marker for the starting point of a line segment; one of: - a marker name such as *arrow* or *circle* * *none* (default) - no marker * true - alias for *circle-fill* * false or null - alias for *none*
#' @param mixBlendMode The mix-blend-mode; a constant string specifying how to blend content such as *multiply*.
#' @param opacity The opacity; a constant between 0 and 1, or a channel typically bound to the *opacity* scale.
#' @param paintOrder The paint-order; a constant string specifying the order in which the **fill**, **stroke**, and any markers are drawn; defaults to *normal*, which draws the fill, then stroke, then markers; defaults to *stroke* for the text mark to create a “halo” around text to improve legibility.
#' @param pointerEvents The pointer-events property; a constant string such as *none*.
#' @param reverse Applies a transform to reverse the order of the mark’s index, say for reverse input order.
#' @param select Applies a filter transform after data is loaded to highlight selected values only.
#' @param shapeRendering The shape-rendering; a constant string such as *crispEdges*.
#' @param sort Either applies a transform to sort the mark’s index by the specified channel values, or imputes ordinal scale domains from this mark’s channels.
#' @param stroke The stroke; a constant CSS color string, or a channel typically bound to the *color* scale.
#' @param strokeDasharray The stroke-dasharray; a constant number indicating the length in pixels of alternating dashes and gaps, or a constant string of numbers separated by spaces or commas (_e.g._, *10 2* for dashes of 10 pixels separated by gaps of 2 pixels), or *none* (the default) for no dashing : https://developer.mozilla.org/en-US/docs/Web/SVG/Attribute/stroke-dasharray
#' @param strokeDashoffset The stroke-dashoffset; a constant indicating the offset in pixels of the first dash along the stroke; defaults to zero.
#' @param strokeLinecap The stroke-linecap; a constant specifying how to cap stroked paths, such as *butt*, *round*, or *square*.
#' @param strokeLinejoin The stroke-linejoin; a constant specifying how to join stroked paths, such as *bevel*, *miter*, *miter-clip*, or *round*.
#' @param strokeMiterlimit The stroke-miterlimit; a constant number specifying how to limit the length of *miter* joins on stroked paths.
#' @param strokeOpacity The stroke-opacity; a constant between 0 and 1, or a channel typically bound to the *opacity* scale.
#' @param strokeWidth The stroke-width; a constant number in pixels, or a channel.
#' @param target The target; a constant string specifying the target window (_e.g._, *_blank*) for clickable links; used in conjunction with the **href** option.
#' @param tension The tension option only has an effect on bundle, cardinal and Catmull–Rom splines (*bundle*, *cardinal*, *cardinal-open*, *cardinal-closed*, *catmull-rom*, *catmull-rom-open*, and *catmull-rom-closed*).
#' @param tip Whether to generate a tooltip for this mark, and any tip options.
#' @param title The title; a channel specifying accessible, short textual descriptions as strings (possibly with newlines).
#' @param x The horizontal position (or length) channel, typically bound to the *x* scale.
#' @param y The vertical position channel, typically bound to the *y* scale; defaults to the zero-based index of the data 0, 1, 2, ….
#' @param z An optional ordinal channel for grouping data into (possibly stacked) series to be drawn as separate areas; defaults to **fill** if a channel, or **stroke** if a channel.
#' @param ... Additional options or plot-level attributes.
#' @param data_from The name of the data source this mark reads from (see [vg_data()]).
#' @param filter_by A Param/Selection (e.g. from [param()]) to filter this mark's data by.
#' @family mark functions
#' @export
vg_line_x <- function(spec = NULL, ariaDescription = vg_unset, ariaHidden = vg_unset, ariaLabel = vg_unset, channels = vg_unset, clip = vg_unset, curve = vg_unset, dx = vg_unset, dy = vg_unset, facet = vg_unset, facetAnchor = vg_unset, fill = vg_unset, fillOpacity = vg_unset, filter = vg_unset, fx = vg_unset, fy = vg_unset, href = vg_unset, imageFilter = vg_unset, margin = vg_unset, marginBottom = vg_unset, marginLeft = vg_unset, marginRight = vg_unset, marginTop = vg_unset, marker = vg_unset, markerEnd = vg_unset, markerMid = vg_unset, markerStart = vg_unset, mixBlendMode = vg_unset, opacity = vg_unset, paintOrder = vg_unset, pointerEvents = vg_unset, reverse = vg_unset, select = vg_unset, shapeRendering = vg_unset, sort = vg_unset, stroke = vg_unset, strokeDasharray = vg_unset, strokeDashoffset = vg_unset, strokeLinecap = vg_unset, strokeLinejoin = vg_unset, strokeMiterlimit = vg_unset, strokeOpacity = vg_unset, strokeWidth = vg_unset, target = vg_unset, tension = vg_unset, tip = vg_unset, title = vg_unset, x = vg_unset, y = vg_unset, z = vg_unset, ..., data_from = vg_unset, filter_by = vg_unset) {
  vg_mark_(spec, "lineX", ariaDescription = ariaDescription, ariaHidden = ariaHidden, ariaLabel = ariaLabel, channels = channels, clip = clip, curve = curve, dx = dx, dy = dy, facet = facet, facetAnchor = facetAnchor, fill = fill, fillOpacity = fillOpacity, filter = filter, fx = fx, fy = fy, href = href, imageFilter = imageFilter, margin = margin, marginBottom = marginBottom, marginLeft = marginLeft, marginRight = marginRight, marginTop = marginTop, marker = marker, markerEnd = markerEnd, markerMid = markerMid, markerStart = markerStart, mixBlendMode = mixBlendMode, opacity = opacity, paintOrder = paintOrder, pointerEvents = pointerEvents, reverse = reverse, select = select, shapeRendering = shapeRendering, sort = sort, stroke = stroke, strokeDasharray = strokeDasharray, strokeDashoffset = strokeDashoffset, strokeLinecap = strokeLinecap, strokeLinejoin = strokeLinejoin, strokeMiterlimit = strokeMiterlimit, strokeOpacity = strokeOpacity, strokeWidth = strokeWidth, target = target, tension = tension, tip = tip, title = title, x = x, y = y, z = z, ..., data_from = data_from, filter_by = filter_by)
}

#' The lineY mark.
#'
#' @param spec A plot fragment or `vgspec` to add this mark to, or `NULL` to start a new plot with just this mark.
#' @param ariaDescription The aria-description; a constant textual description.
#' @param ariaHidden The aria-hidden state; a constant indicating whether the element is exposed to an accessibility API.
#' @param ariaLabel The aria-label; a channel specifying short textual labels representing the value in the accessibility tree.
#' @param channels Additional named channels, for example to include in a tooltip.
#' @param clip How to clip the mark; one of: - *frame* or true - clip to the plot’s frame (inner area) - *sphere* - clip to the projected sphere (*e.g.*, front hemisphere) - null or false - do not clip The *sphere* clip option requires a geographic projection.
#' @param curve The curve (interpolation) method for connecting adjacent points.
#' @param dx The horizontal offset in pixels; a constant option.
#' @param dy The vertical offset in pixels; a constant option.
#' @param facet Whether to enable or disable faceting; one of: - *auto* (default) - automatically determine if this mark should be faceted - *include* (or true) - draw the subset of the mark’s data in the current facet - *exclude* - draw the subset of the mark’s data *not* in the current facet - *super* - draw this mark in a single frame that covers all facets - null (or false) - repeat this mark’s data across all facets (*i.e.*, no faceting) When a mark uses *super* faceting, it is not allowed to use position scales (*x*, *y*, *fx*, or *fy*); *super* faceting is intended for decorations, such as labels and legends.
#' @param facetAnchor How to place the mark with respect to facets; one of: - null (default for most marks) - display the mark in each non-empty facet - *top*, *right*, *bottom*, or *left* - display the mark only in facets on the given side - *top-empty*, *right-empty*, *bottom-empty*, or *left-empty* (default for axis marks) - display the mark only in facets that have empty space on the given side: either the margin, or an empty facet - *empty* - display the mark in empty facets only
#' @param fill The fill; a constant CSS color string, or a channel typically bound to the *color* scale.
#' @param fillOpacity The fill-opacity; a constant number between 0 and 1, or a channel typically bound to the *opacity* scale.
#' @param filter Applies a transform to filter the mark’s index according to the given channel values; only truthy values are retained.
#' @param fx The horizontal facet position channel, for mark-level faceting, bound to the *fx* scale.
#' @param fy The vertical facet position channel, for mark-level faceting, bound to the *fy* scale.
#' @param href The href; a channel specifying URLs for clickable links.
#' @param imageFilter A CSS filter; a constant string used to adjust the rendering of images, such as *blur(5px)*.
#' @param margin Shorthand to set the same default for all four mark margins: **marginTop**, **marginRight**, **marginBottom**, and **marginLeft**; typically defaults to 0, except for axis marks.
#' @param marginBottom The mark’s bottom margin; the minimum distance in pixels between the bottom edges of the inner and outer plot area.
#' @param marginLeft The mark’s left margin; the minimum distance in pixels between the left edges of the inner and outer plot area.
#' @param marginRight The mark’s right margin; the minimum distance in pixels between the right edges of the mark’s inner and outer plot area.
#' @param marginTop The mark’s top margin; the minimum distance in pixels between the top edges of the inner and outer plot area.
#' @param marker Shorthand to set the same default for markerStart, markerMid, and markerEnd; one of: - a marker name such as *arrow* or *circle* - *none* (default) - no marker * true - alias for *circle-fill* * false or null - alias for *none*
#' @param markerEnd The marker for the ending point of a line segment; one of: - a marker name such as *arrow* or *circle* * *none* (default) - no marker * true - alias for *circle-fill* * false or null - alias for *none*
#' @param markerMid The marker for any middle (interior) points of a line segment.
#' @param markerStart The marker for the starting point of a line segment; one of: - a marker name such as *arrow* or *circle* * *none* (default) - no marker * true - alias for *circle-fill* * false or null - alias for *none*
#' @param mixBlendMode The mix-blend-mode; a constant string specifying how to blend content such as *multiply*.
#' @param opacity The opacity; a constant between 0 and 1, or a channel typically bound to the *opacity* scale.
#' @param paintOrder The paint-order; a constant string specifying the order in which the **fill**, **stroke**, and any markers are drawn; defaults to *normal*, which draws the fill, then stroke, then markers; defaults to *stroke* for the text mark to create a “halo” around text to improve legibility.
#' @param pointerEvents The pointer-events property; a constant string such as *none*.
#' @param reverse Applies a transform to reverse the order of the mark’s index, say for reverse input order.
#' @param select Applies a filter transform after data is loaded to highlight selected values only.
#' @param shapeRendering The shape-rendering; a constant string such as *crispEdges*.
#' @param sort Either applies a transform to sort the mark’s index by the specified channel values, or imputes ordinal scale domains from this mark’s channels.
#' @param stroke The stroke; a constant CSS color string, or a channel typically bound to the *color* scale.
#' @param strokeDasharray The stroke-dasharray; a constant number indicating the length in pixels of alternating dashes and gaps, or a constant string of numbers separated by spaces or commas (_e.g._, *10 2* for dashes of 10 pixels separated by gaps of 2 pixels), or *none* (the default) for no dashing : https://developer.mozilla.org/en-US/docs/Web/SVG/Attribute/stroke-dasharray
#' @param strokeDashoffset The stroke-dashoffset; a constant indicating the offset in pixels of the first dash along the stroke; defaults to zero.
#' @param strokeLinecap The stroke-linecap; a constant specifying how to cap stroked paths, such as *butt*, *round*, or *square*.
#' @param strokeLinejoin The stroke-linejoin; a constant specifying how to join stroked paths, such as *bevel*, *miter*, *miter-clip*, or *round*.
#' @param strokeMiterlimit The stroke-miterlimit; a constant number specifying how to limit the length of *miter* joins on stroked paths.
#' @param strokeOpacity The stroke-opacity; a constant between 0 and 1, or a channel typically bound to the *opacity* scale.
#' @param strokeWidth The stroke-width; a constant number in pixels, or a channel.
#' @param target The target; a constant string specifying the target window (_e.g._, *_blank*) for clickable links; used in conjunction with the **href** option.
#' @param tension The tension option only has an effect on bundle, cardinal and Catmull–Rom splines (*bundle*, *cardinal*, *cardinal-open*, *cardinal-closed*, *catmull-rom*, *catmull-rom-open*, and *catmull-rom-closed*).
#' @param tip Whether to generate a tooltip for this mark, and any tip options.
#' @param title The title; a channel specifying accessible, short textual descriptions as strings (possibly with newlines).
#' @param x The horizontal position (or length) channel, typically bound to the *x* scale.
#' @param y The vertical position channel, typically bound to the *y* scale; defaults to the zero-based index of the data 0, 1, 2, ….
#' @param z An optional ordinal channel for grouping data into (possibly stacked) series to be drawn as separate areas; defaults to **fill** if a channel, or **stroke** if a channel.
#' @param ... Additional options or plot-level attributes.
#' @param data_from The name of the data source this mark reads from (see [vg_data()]).
#' @param filter_by A Param/Selection (e.g. from [param()]) to filter this mark's data by.
#' @family mark functions
#' @export
vg_line_y <- function(spec = NULL, ariaDescription = vg_unset, ariaHidden = vg_unset, ariaLabel = vg_unset, channels = vg_unset, clip = vg_unset, curve = vg_unset, dx = vg_unset, dy = vg_unset, facet = vg_unset, facetAnchor = vg_unset, fill = vg_unset, fillOpacity = vg_unset, filter = vg_unset, fx = vg_unset, fy = vg_unset, href = vg_unset, imageFilter = vg_unset, margin = vg_unset, marginBottom = vg_unset, marginLeft = vg_unset, marginRight = vg_unset, marginTop = vg_unset, marker = vg_unset, markerEnd = vg_unset, markerMid = vg_unset, markerStart = vg_unset, mixBlendMode = vg_unset, opacity = vg_unset, paintOrder = vg_unset, pointerEvents = vg_unset, reverse = vg_unset, select = vg_unset, shapeRendering = vg_unset, sort = vg_unset, stroke = vg_unset, strokeDasharray = vg_unset, strokeDashoffset = vg_unset, strokeLinecap = vg_unset, strokeLinejoin = vg_unset, strokeMiterlimit = vg_unset, strokeOpacity = vg_unset, strokeWidth = vg_unset, target = vg_unset, tension = vg_unset, tip = vg_unset, title = vg_unset, x = vg_unset, y = vg_unset, z = vg_unset, ..., data_from = vg_unset, filter_by = vg_unset) {
  vg_mark_(spec, "lineY", ariaDescription = ariaDescription, ariaHidden = ariaHidden, ariaLabel = ariaLabel, channels = channels, clip = clip, curve = curve, dx = dx, dy = dy, facet = facet, facetAnchor = facetAnchor, fill = fill, fillOpacity = fillOpacity, filter = filter, fx = fx, fy = fy, href = href, imageFilter = imageFilter, margin = margin, marginBottom = marginBottom, marginLeft = marginLeft, marginRight = marginRight, marginTop = marginTop, marker = marker, markerEnd = markerEnd, markerMid = markerMid, markerStart = markerStart, mixBlendMode = mixBlendMode, opacity = opacity, paintOrder = paintOrder, pointerEvents = pointerEvents, reverse = reverse, select = select, shapeRendering = shapeRendering, sort = sort, stroke = stroke, strokeDasharray = strokeDasharray, strokeDashoffset = strokeDashoffset, strokeLinecap = strokeLinecap, strokeLinejoin = strokeLinejoin, strokeMiterlimit = strokeMiterlimit, strokeOpacity = strokeOpacity, strokeWidth = strokeWidth, target = target, tension = tension, tip = tip, title = title, x = x, y = y, z = z, ..., data_from = data_from, filter_by = filter_by)
}

#' The link mark.
#'
#' @param spec A plot fragment or `vgspec` to add this mark to, or `NULL` to start a new plot with just this mark.
#' @param ariaDescription The aria-description; a constant textual description.
#' @param ariaHidden The aria-hidden state; a constant indicating whether the element is exposed to an accessibility API.
#' @param ariaLabel The aria-label; a channel specifying short textual labels representing the value in the accessibility tree.
#' @param channels Additional named channels, for example to include in a tooltip.
#' @param clip How to clip the mark; one of: - *frame* or true - clip to the plot’s frame (inner area) - *sphere* - clip to the projected sphere (*e.g.*, front hemisphere) - null or false - do not clip The *sphere* clip option requires a geographic projection.
#' @param curve The curve (interpolation) method for connecting adjacent points.
#' @param dx The horizontal offset in pixels; a constant option.
#' @param dy The vertical offset in pixels; a constant option.
#' @param facet Whether to enable or disable faceting; one of: - *auto* (default) - automatically determine if this mark should be faceted - *include* (or true) - draw the subset of the mark’s data in the current facet - *exclude* - draw the subset of the mark’s data *not* in the current facet - *super* - draw this mark in a single frame that covers all facets - null (or false) - repeat this mark’s data across all facets (*i.e.*, no faceting) When a mark uses *super* faceting, it is not allowed to use position scales (*x*, *y*, *fx*, or *fy*); *super* faceting is intended for decorations, such as labels and legends.
#' @param facetAnchor How to place the mark with respect to facets; one of: - null (default for most marks) - display the mark in each non-empty facet - *top*, *right*, *bottom*, or *left* - display the mark only in facets on the given side - *top-empty*, *right-empty*, *bottom-empty*, or *left-empty* (default for axis marks) - display the mark only in facets that have empty space on the given side: either the margin, or an empty facet - *empty* - display the mark in empty facets only
#' @param fill The fill; a constant CSS color string, or a channel typically bound to the *color* scale.
#' @param fillOpacity The fill-opacity; a constant number between 0 and 1, or a channel typically bound to the *opacity* scale.
#' @param filter Applies a transform to filter the mark’s index according to the given channel values; only truthy values are retained.
#' @param fx The horizontal facet position channel, for mark-level faceting, bound to the *fx* scale.
#' @param fy The vertical facet position channel, for mark-level faceting, bound to the *fy* scale.
#' @param href The href; a channel specifying URLs for clickable links.
#' @param imageFilter A CSS filter; a constant string used to adjust the rendering of images, such as *blur(5px)*.
#' @param margin Shorthand to set the same default for all four mark margins: **marginTop**, **marginRight**, **marginBottom**, and **marginLeft**; typically defaults to 0, except for axis marks.
#' @param marginBottom The mark’s bottom margin; the minimum distance in pixels between the bottom edges of the inner and outer plot area.
#' @param marginLeft The mark’s left margin; the minimum distance in pixels between the left edges of the inner and outer plot area.
#' @param marginRight The mark’s right margin; the minimum distance in pixels between the right edges of the mark’s inner and outer plot area.
#' @param marginTop The mark’s top margin; the minimum distance in pixels between the top edges of the inner and outer plot area.
#' @param marker Shorthand to set the same default for markerStart, markerMid, and markerEnd; one of: - a marker name such as *arrow* or *circle* - *none* (default) - no marker * true - alias for *circle-fill* * false or null - alias for *none*
#' @param markerEnd The marker for the ending point of a line segment; one of: - a marker name such as *arrow* or *circle* * *none* (default) - no marker * true - alias for *circle-fill* * false or null - alias for *none*
#' @param markerMid The marker for any middle (interior) points of a line segment.
#' @param markerStart The marker for the starting point of a line segment; one of: - a marker name such as *arrow* or *circle* * *none* (default) - no marker * true - alias for *circle-fill* * false or null - alias for *none*
#' @param mixBlendMode The mix-blend-mode; a constant string specifying how to blend content such as *multiply*.
#' @param opacity The opacity; a constant between 0 and 1, or a channel typically bound to the *opacity* scale.
#' @param paintOrder The paint-order; a constant string specifying the order in which the **fill**, **stroke**, and any markers are drawn; defaults to *normal*, which draws the fill, then stroke, then markers; defaults to *stroke* for the text mark to create a “halo” around text to improve legibility.
#' @param pointerEvents The pointer-events property; a constant string such as *none*.
#' @param reverse Applies a transform to reverse the order of the mark’s index, say for reverse input order.
#' @param select Applies a filter transform after data is loaded to highlight selected values only.
#' @param shapeRendering The shape-rendering; a constant string such as *crispEdges*.
#' @param sort Either applies a transform to sort the mark’s index by the specified channel values, or imputes ordinal scale domains from this mark’s channels.
#' @param stroke The stroke; a constant CSS color string, or a channel typically bound to the *color* scale.
#' @param strokeDasharray The stroke-dasharray; a constant number indicating the length in pixels of alternating dashes and gaps, or a constant string of numbers separated by spaces or commas (_e.g._, *10 2* for dashes of 10 pixels separated by gaps of 2 pixels), or *none* (the default) for no dashing : https://developer.mozilla.org/en-US/docs/Web/SVG/Attribute/stroke-dasharray
#' @param strokeDashoffset The stroke-dashoffset; a constant indicating the offset in pixels of the first dash along the stroke; defaults to zero.
#' @param strokeLinecap The stroke-linecap; a constant specifying how to cap stroked paths, such as *butt*, *round*, or *square*.
#' @param strokeLinejoin The stroke-linejoin; a constant specifying how to join stroked paths, such as *bevel*, *miter*, *miter-clip*, or *round*.
#' @param strokeMiterlimit The stroke-miterlimit; a constant number specifying how to limit the length of *miter* joins on stroked paths.
#' @param strokeOpacity The stroke-opacity; a constant between 0 and 1, or a channel typically bound to the *opacity* scale.
#' @param strokeWidth The stroke-width; a constant number in pixels, or a channel.
#' @param target The target; a constant string specifying the target window (_e.g._, *_blank*) for clickable links; used in conjunction with the **href** option.
#' @param tension The tension option only has an effect on bundle, cardinal and Catmull–Rom splines (*bundle*, *cardinal*, *cardinal-open*, *cardinal-closed*, *catmull-rom*, *catmull-rom-open*, and *catmull-rom-closed*).
#' @param tip Whether to generate a tooltip for this mark, and any tip options.
#' @param title The title; a channel specifying accessible, short textual descriptions as strings (possibly with newlines).
#' @param x The horizontal position (or length) channel, typically bound to the *x* scale.
#' @param x1 The required primary (starting, often left) horizontal position channel, representing the area’s baseline, typically bound to the *x* scale.
#' @param x2 The optional secondary (ending, often right) horizontal position channel, representing the area’s topline, typically bound to the *x* scale; if not specified, **x1** is used.
#' @param y The vertical position channel, typically bound to the *y* scale; defaults to the zero-based index of the data 0, 1, 2, ….
#' @param y1 The required primary (starting, often bottom) vertical position channel, representing the area’s baseline, typically bound to the *y* scale.
#' @param y2 The optional secondary (ending, often top) vertical position channel, representing the area’s topline, typically bound to the *y* scale; if not specified, **y1** is used.
#' @param ... Additional options or plot-level attributes.
#' @param data_from The name of the data source this mark reads from (see [vg_data()]).
#' @param filter_by A Param/Selection (e.g. from [param()]) to filter this mark's data by.
#' @family mark functions
#' @export
vg_link <- function(spec = NULL, ariaDescription = vg_unset, ariaHidden = vg_unset, ariaLabel = vg_unset, channels = vg_unset, clip = vg_unset, curve = vg_unset, dx = vg_unset, dy = vg_unset, facet = vg_unset, facetAnchor = vg_unset, fill = vg_unset, fillOpacity = vg_unset, filter = vg_unset, fx = vg_unset, fy = vg_unset, href = vg_unset, imageFilter = vg_unset, margin = vg_unset, marginBottom = vg_unset, marginLeft = vg_unset, marginRight = vg_unset, marginTop = vg_unset, marker = vg_unset, markerEnd = vg_unset, markerMid = vg_unset, markerStart = vg_unset, mixBlendMode = vg_unset, opacity = vg_unset, paintOrder = vg_unset, pointerEvents = vg_unset, reverse = vg_unset, select = vg_unset, shapeRendering = vg_unset, sort = vg_unset, stroke = vg_unset, strokeDasharray = vg_unset, strokeDashoffset = vg_unset, strokeLinecap = vg_unset, strokeLinejoin = vg_unset, strokeMiterlimit = vg_unset, strokeOpacity = vg_unset, strokeWidth = vg_unset, target = vg_unset, tension = vg_unset, tip = vg_unset, title = vg_unset, x = vg_unset, x1 = vg_unset, x2 = vg_unset, y = vg_unset, y1 = vg_unset, y2 = vg_unset, ..., data_from = vg_unset, filter_by = vg_unset) {
  vg_mark_(spec, "link", ariaDescription = ariaDescription, ariaHidden = ariaHidden, ariaLabel = ariaLabel, channels = channels, clip = clip, curve = curve, dx = dx, dy = dy, facet = facet, facetAnchor = facetAnchor, fill = fill, fillOpacity = fillOpacity, filter = filter, fx = fx, fy = fy, href = href, imageFilter = imageFilter, margin = margin, marginBottom = marginBottom, marginLeft = marginLeft, marginRight = marginRight, marginTop = marginTop, marker = marker, markerEnd = markerEnd, markerMid = markerMid, markerStart = markerStart, mixBlendMode = mixBlendMode, opacity = opacity, paintOrder = paintOrder, pointerEvents = pointerEvents, reverse = reverse, select = select, shapeRendering = shapeRendering, sort = sort, stroke = stroke, strokeDasharray = strokeDasharray, strokeDashoffset = strokeDashoffset, strokeLinecap = strokeLinecap, strokeLinejoin = strokeLinejoin, strokeMiterlimit = strokeMiterlimit, strokeOpacity = strokeOpacity, strokeWidth = strokeWidth, target = target, tension = tension, tip = tip, title = title, x = x, x1 = x1, x2 = x2, y = y, y1 = y1, y2 = y2, ..., data_from = data_from, filter_by = filter_by)
}

#' The raster mark.
#'
#' @param spec A plot fragment or `vgspec` to add this mark to, or `NULL` to start a new plot with just this mark.
#' @param ariaDescription The aria-description; a constant textual description.
#' @param ariaHidden The aria-hidden state; a constant indicating whether the element is exposed to an accessibility API.
#' @param ariaLabel The aria-label; a channel specifying short textual labels representing the value in the accessibility tree.
#' @param bandwidth The kernel density bandwidth for smoothing, in pixels.
#' @param channels Additional named channels, for example to include in a tooltip.
#' @param clip How to clip the mark; one of: - *frame* or true - clip to the plot’s frame (inner area) - *sphere* - clip to the projected sphere (*e.g.*, front hemisphere) - null or false - do not clip The *sphere* clip option requires a geographic projection.
#' @param dx The horizontal offset in pixels; a constant option.
#' @param dy The vertical offset in pixels; a constant option.
#' @param facet Whether to enable or disable faceting; one of: - *auto* (default) - automatically determine if this mark should be faceted - *include* (or true) - draw the subset of the mark’s data in the current facet - *exclude* - draw the subset of the mark’s data *not* in the current facet - *super* - draw this mark in a single frame that covers all facets - null (or false) - repeat this mark’s data across all facets (*i.e.*, no faceting) When a mark uses *super* faceting, it is not allowed to use position scales (*x*, *y*, *fx*, or *fy*); *super* faceting is intended for decorations, such as labels and legends.
#' @param facetAnchor How to place the mark with respect to facets; one of: - null (default for most marks) - display the mark in each non-empty facet - *top*, *right*, *bottom*, or *left* - display the mark only in facets on the given side - *top-empty*, *right-empty*, *bottom-empty*, or *left-empty* (default for axis marks) - display the mark only in facets that have empty space on the given side: either the margin, or an empty facet - *empty* - display the mark in empty facets only
#' @param fill The fill; a constant CSS color string, or a channel typically bound to the *color* scale.
#' @param fillOpacity The fill-opacity; a constant number between 0 and 1, or a channel typically bound to the *opacity* scale.
#' @param filter Applies a transform to filter the mark’s index according to the given channel values; only truthy values are retained.
#' @param fx The horizontal facet position channel, for mark-level faceting, bound to the *fx* scale.
#' @param fy The vertical facet position channel, for mark-level faceting, bound to the *fy* scale.
#' @param height The height (number of rows) of the grid, in actual pixels.
#' @param href The href; a channel specifying URLs for clickable links.
#' @param imageFilter A CSS filter; a constant string used to adjust the rendering of images, such as *blur(5px)*.
#' @param imageRendering The image-rendering attribute; defaults to *auto* (bilinear).
#' @param interpolate The spatial interpolation method; one of: - *none* - do not perform interpolation (the default), maps samples to single bins - *linear* - apply proportional linear interpolation across adjacent bins - *nearest* - assign each pixel to the closest sample’s value (Voronoi diagram) - *barycentric* - apply barycentric interpolation over the Delaunay triangulation - *random-walk* - apply a random walk from each pixel, stopping when near a sample
#' @param margin Shorthand to set the same default for all four mark margins: **marginTop**, **marginRight**, **marginBottom**, and **marginLeft**; typically defaults to 0, except for axis marks.
#' @param marginBottom The mark’s bottom margin; the minimum distance in pixels between the bottom edges of the inner and outer plot area.
#' @param marginLeft The mark’s left margin; the minimum distance in pixels between the left edges of the inner and outer plot area.
#' @param marginRight The mark’s right margin; the minimum distance in pixels between the right edges of the mark’s inner and outer plot area.
#' @param marginTop The mark’s top margin; the minimum distance in pixels between the top edges of the inner and outer plot area.
#' @param mixBlendMode The mix-blend-mode; a constant string specifying how to blend content such as *multiply*.
#' @param opacity The opacity; a constant between 0 and 1, or a channel typically bound to the *opacity* scale.
#' @param pad The bin padding, one of 1 (default) to include extra padding for the final bin, or 0 to make the bins flush with the maximum domain value.
#' @param paintOrder The paint-order; a constant string specifying the order in which the **fill**, **stroke**, and any markers are drawn; defaults to *normal*, which draws the fill, then stroke, then markers; defaults to *stroke* for the text mark to create a “halo” around text to improve legibility.
#' @param pixelSize The effective screen size of a raster pixel, used to determine the height and width of the raster from the frame’s dimensions; defaults to 1.
#' @param pointerEvents The pointer-events property; a constant string such as *none*.
#' @param reverse Applies a transform to reverse the order of the mark’s index, say for reverse input order.
#' @param select Applies a filter transform after data is loaded to highlight selected values only.
#' @param shapeRendering The shape-rendering; a constant string such as *crispEdges*.
#' @param sort Either applies a transform to sort the mark’s index by the specified channel values, or imputes ordinal scale domains from this mark’s channels.
#' @param stroke The stroke; a constant CSS color string, or a channel typically bound to the *color* scale.
#' @param strokeDasharray The stroke-dasharray; a constant number indicating the length in pixels of alternating dashes and gaps, or a constant string of numbers separated by spaces or commas (_e.g._, *10 2* for dashes of 10 pixels separated by gaps of 2 pixels), or *none* (the default) for no dashing : https://developer.mozilla.org/en-US/docs/Web/SVG/Attribute/stroke-dasharray
#' @param strokeDashoffset The stroke-dashoffset; a constant indicating the offset in pixels of the first dash along the stroke; defaults to zero.
#' @param strokeLinecap The stroke-linecap; a constant specifying how to cap stroked paths, such as *butt*, *round*, or *square*.
#' @param strokeLinejoin The stroke-linejoin; a constant specifying how to join stroked paths, such as *bevel*, *miter*, *miter-clip*, or *round*.
#' @param strokeMiterlimit The stroke-miterlimit; a constant number specifying how to limit the length of *miter* joins on stroked paths.
#' @param strokeOpacity The stroke-opacity; a constant between 0 and 1, or a channel typically bound to the *opacity* scale.
#' @param strokeWidth The stroke-width; a constant number in pixels, or a channel.
#' @param target The target; a constant string specifying the target window (_e.g._, *_blank*) for clickable links; used in conjunction with the **href** option.
#' @param tip Whether to generate a tooltip for this mark, and any tip options.
#' @param title The title; a channel specifying accessible, short textual descriptions as strings (possibly with newlines).
#' @param width The width (number of columns) of the grid, in actual pixels.
#' @param x The horizontal position (or length) channel, typically bound to the *x* scale.
#' @param y The vertical position channel, typically bound to the *y* scale; defaults to the zero-based index of the data 0, 1, 2, ….
#' @param ... Additional options or plot-level attributes.
#' @param data_from The name of the data source this mark reads from (see [vg_data()]).
#' @param filter_by A Param/Selection (e.g. from [param()]) to filter this mark's data by.
#' @family mark functions
#' @export
vg_raster <- function(spec = NULL, ariaDescription = vg_unset, ariaHidden = vg_unset, ariaLabel = vg_unset, bandwidth = vg_unset, channels = vg_unset, clip = vg_unset, dx = vg_unset, dy = vg_unset, facet = vg_unset, facetAnchor = vg_unset, fill = vg_unset, fillOpacity = vg_unset, filter = vg_unset, fx = vg_unset, fy = vg_unset, height = vg_unset, href = vg_unset, imageFilter = vg_unset, imageRendering = vg_unset, interpolate = vg_unset, margin = vg_unset, marginBottom = vg_unset, marginLeft = vg_unset, marginRight = vg_unset, marginTop = vg_unset, mixBlendMode = vg_unset, opacity = vg_unset, pad = vg_unset, paintOrder = vg_unset, pixelSize = vg_unset, pointerEvents = vg_unset, reverse = vg_unset, select = vg_unset, shapeRendering = vg_unset, sort = vg_unset, stroke = vg_unset, strokeDasharray = vg_unset, strokeDashoffset = vg_unset, strokeLinecap = vg_unset, strokeLinejoin = vg_unset, strokeMiterlimit = vg_unset, strokeOpacity = vg_unset, strokeWidth = vg_unset, target = vg_unset, tip = vg_unset, title = vg_unset, width = vg_unset, x = vg_unset, y = vg_unset, ..., data_from = vg_unset, filter_by = vg_unset) {
  vg_mark_(spec, "raster", ariaDescription = ariaDescription, ariaHidden = ariaHidden, ariaLabel = ariaLabel, bandwidth = bandwidth, channels = channels, clip = clip, dx = dx, dy = dy, facet = facet, facetAnchor = facetAnchor, fill = fill, fillOpacity = fillOpacity, filter = filter, fx = fx, fy = fy, height = height, href = href, imageFilter = imageFilter, imageRendering = imageRendering, interpolate = interpolate, margin = margin, marginBottom = marginBottom, marginLeft = marginLeft, marginRight = marginRight, marginTop = marginTop, mixBlendMode = mixBlendMode, opacity = opacity, pad = pad, paintOrder = paintOrder, pixelSize = pixelSize, pointerEvents = pointerEvents, reverse = reverse, select = select, shapeRendering = shapeRendering, sort = sort, stroke = stroke, strokeDasharray = strokeDasharray, strokeDashoffset = strokeDashoffset, strokeLinecap = strokeLinecap, strokeLinejoin = strokeLinejoin, strokeMiterlimit = strokeMiterlimit, strokeOpacity = strokeOpacity, strokeWidth = strokeWidth, target = target, tip = tip, title = title, width = width, x = x, y = y, ..., data_from = data_from, filter_by = filter_by)
}

#' The rasterTile mark.
#'
#' @param spec A plot fragment or `vgspec` to add this mark to, or `NULL` to start a new plot with just this mark.
#' @param ariaDescription The aria-description; a constant textual description.
#' @param ariaHidden The aria-hidden state; a constant indicating whether the element is exposed to an accessibility API.
#' @param ariaLabel The aria-label; a channel specifying short textual labels representing the value in the accessibility tree.
#' @param bandwidth The kernel density bandwidth for smoothing, in pixels.
#' @param channels Additional named channels, for example to include in a tooltip.
#' @param clip How to clip the mark; one of: - *frame* or true - clip to the plot’s frame (inner area) - *sphere* - clip to the projected sphere (*e.g.*, front hemisphere) - null or false - do not clip The *sphere* clip option requires a geographic projection.
#' @param dx The horizontal offset in pixels; a constant option.
#' @param dy The vertical offset in pixels; a constant option.
#' @param facet Whether to enable or disable faceting; one of: - *auto* (default) - automatically determine if this mark should be faceted - *include* (or true) - draw the subset of the mark’s data in the current facet - *exclude* - draw the subset of the mark’s data *not* in the current facet - *super* - draw this mark in a single frame that covers all facets - null (or false) - repeat this mark’s data across all facets (*i.e.*, no faceting) When a mark uses *super* faceting, it is not allowed to use position scales (*x*, *y*, *fx*, or *fy*); *super* faceting is intended for decorations, such as labels and legends.
#' @param facetAnchor How to place the mark with respect to facets; one of: - null (default for most marks) - display the mark in each non-empty facet - *top*, *right*, *bottom*, or *left* - display the mark only in facets on the given side - *top-empty*, *right-empty*, *bottom-empty*, or *left-empty* (default for axis marks) - display the mark only in facets that have empty space on the given side: either the margin, or an empty facet - *empty* - display the mark in empty facets only
#' @param fill The fill; a constant CSS color string, or a channel typically bound to the *color* scale.
#' @param fillOpacity The fill-opacity; a constant number between 0 and 1, or a channel typically bound to the *opacity* scale.
#' @param filter Applies a transform to filter the mark’s index according to the given channel values; only truthy values are retained.
#' @param fx The horizontal facet position channel, for mark-level faceting, bound to the *fx* scale.
#' @param fy The vertical facet position channel, for mark-level faceting, bound to the *fy* scale.
#' @param height The height (number of rows) of the grid, in actual pixels.
#' @param href The href; a channel specifying URLs for clickable links.
#' @param imageFilter A CSS filter; a constant string used to adjust the rendering of images, such as *blur(5px)*.
#' @param imageRendering The image-rendering attribute; defaults to *auto* (bilinear).
#' @param interpolate The spatial interpolation method; one of: - *none* - do not perform interpolation (the default), maps samples to single bins - *linear* - apply proportional linear interpolation across adjacent bins - *nearest* - assign each pixel to the closest sample’s value (Voronoi diagram) - *barycentric* - apply barycentric interpolation over the Delaunay triangulation - *random-walk* - apply a random walk from each pixel, stopping when near a sample
#' @param margin Shorthand to set the same default for all four mark margins: **marginTop**, **marginRight**, **marginBottom**, and **marginLeft**; typically defaults to 0, except for axis marks.
#' @param marginBottom The mark’s bottom margin; the minimum distance in pixels between the bottom edges of the inner and outer plot area.
#' @param marginLeft The mark’s left margin; the minimum distance in pixels between the left edges of the inner and outer plot area.
#' @param marginRight The mark’s right margin; the minimum distance in pixels between the right edges of the mark’s inner and outer plot area.
#' @param marginTop The mark’s top margin; the minimum distance in pixels between the top edges of the inner and outer plot area.
#' @param mixBlendMode The mix-blend-mode; a constant string specifying how to blend content such as *multiply*.
#' @param opacity The opacity; a constant between 0 and 1, or a channel typically bound to the *opacity* scale.
#' @param origin The coordinates of the tile origin in the **x** and **y** data domains.
#' @param pad The bin padding, one of 1 (default) to include extra padding for the final bin, or 0 to make the bins flush with the maximum domain value.
#' @param paintOrder The paint-order; a constant string specifying the order in which the **fill**, **stroke**, and any markers are drawn; defaults to *normal*, which draws the fill, then stroke, then markers; defaults to *stroke* for the text mark to create a “halo” around text to improve legibility.
#' @param pixelSize The effective screen size of a raster pixel, used to determine the height and width of the raster from the frame’s dimensions; defaults to 1.
#' @param pointerEvents The pointer-events property; a constant string such as *none*.
#' @param reverse Applies a transform to reverse the order of the mark’s index, say for reverse input order.
#' @param select Applies a filter transform after data is loaded to highlight selected values only.
#' @param shapeRendering The shape-rendering; a constant string such as *crispEdges*.
#' @param sort Either applies a transform to sort the mark’s index by the specified channel values, or imputes ordinal scale domains from this mark’s channels.
#' @param stroke The stroke; a constant CSS color string, or a channel typically bound to the *color* scale.
#' @param strokeDasharray The stroke-dasharray; a constant number indicating the length in pixels of alternating dashes and gaps, or a constant string of numbers separated by spaces or commas (_e.g._, *10 2* for dashes of 10 pixels separated by gaps of 2 pixels), or *none* (the default) for no dashing : https://developer.mozilla.org/en-US/docs/Web/SVG/Attribute/stroke-dasharray
#' @param strokeDashoffset The stroke-dashoffset; a constant indicating the offset in pixels of the first dash along the stroke; defaults to zero.
#' @param strokeLinecap The stroke-linecap; a constant specifying how to cap stroked paths, such as *butt*, *round*, or *square*.
#' @param strokeLinejoin The stroke-linejoin; a constant specifying how to join stroked paths, such as *bevel*, *miter*, *miter-clip*, or *round*.
#' @param strokeMiterlimit The stroke-miterlimit; a constant number specifying how to limit the length of *miter* joins on stroked paths.
#' @param strokeOpacity The stroke-opacity; a constant between 0 and 1, or a channel typically bound to the *opacity* scale.
#' @param strokeWidth The stroke-width; a constant number in pixels, or a channel.
#' @param target The target; a constant string specifying the target window (_e.g._, *_blank*) for clickable links; used in conjunction with the **href** option.
#' @param tip Whether to generate a tooltip for this mark, and any tip options.
#' @param title The title; a channel specifying accessible, short textual descriptions as strings (possibly with newlines).
#' @param width The width (number of columns) of the grid, in actual pixels.
#' @param x The horizontal position (or length) channel, typically bound to the *x* scale.
#' @param y The vertical position channel, typically bound to the *y* scale; defaults to the zero-based index of the data 0, 1, 2, ….
#' @param ... Additional options or plot-level attributes.
#' @param data_from The name of the data source this mark reads from (see [vg_data()]).
#' @param filter_by A Param/Selection (e.g. from [param()]) to filter this mark's data by.
#' @family mark functions
#' @export
vg_raster_tile <- function(spec = NULL, ariaDescription = vg_unset, ariaHidden = vg_unset, ariaLabel = vg_unset, bandwidth = vg_unset, channels = vg_unset, clip = vg_unset, dx = vg_unset, dy = vg_unset, facet = vg_unset, facetAnchor = vg_unset, fill = vg_unset, fillOpacity = vg_unset, filter = vg_unset, fx = vg_unset, fy = vg_unset, height = vg_unset, href = vg_unset, imageFilter = vg_unset, imageRendering = vg_unset, interpolate = vg_unset, margin = vg_unset, marginBottom = vg_unset, marginLeft = vg_unset, marginRight = vg_unset, marginTop = vg_unset, mixBlendMode = vg_unset, opacity = vg_unset, origin = vg_unset, pad = vg_unset, paintOrder = vg_unset, pixelSize = vg_unset, pointerEvents = vg_unset, reverse = vg_unset, select = vg_unset, shapeRendering = vg_unset, sort = vg_unset, stroke = vg_unset, strokeDasharray = vg_unset, strokeDashoffset = vg_unset, strokeLinecap = vg_unset, strokeLinejoin = vg_unset, strokeMiterlimit = vg_unset, strokeOpacity = vg_unset, strokeWidth = vg_unset, target = vg_unset, tip = vg_unset, title = vg_unset, width = vg_unset, x = vg_unset, y = vg_unset, ..., data_from = vg_unset, filter_by = vg_unset) {
  vg_mark_(spec, "rasterTile", ariaDescription = ariaDescription, ariaHidden = ariaHidden, ariaLabel = ariaLabel, bandwidth = bandwidth, channels = channels, clip = clip, dx = dx, dy = dy, facet = facet, facetAnchor = facetAnchor, fill = fill, fillOpacity = fillOpacity, filter = filter, fx = fx, fy = fy, height = height, href = href, imageFilter = imageFilter, imageRendering = imageRendering, interpolate = interpolate, margin = margin, marginBottom = marginBottom, marginLeft = marginLeft, marginRight = marginRight, marginTop = marginTop, mixBlendMode = mixBlendMode, opacity = opacity, origin = origin, pad = pad, paintOrder = paintOrder, pixelSize = pixelSize, pointerEvents = pointerEvents, reverse = reverse, select = select, shapeRendering = shapeRendering, sort = sort, stroke = stroke, strokeDasharray = strokeDasharray, strokeDashoffset = strokeDashoffset, strokeLinecap = strokeLinecap, strokeLinejoin = strokeLinejoin, strokeMiterlimit = strokeMiterlimit, strokeOpacity = strokeOpacity, strokeWidth = strokeWidth, target = target, tip = tip, title = title, width = width, x = x, y = y, ..., data_from = data_from, filter_by = filter_by)
}

#' The rect mark.
#'
#' @param spec A plot fragment or `vgspec` to add this mark to, or `NULL` to start a new plot with just this mark.
#' @param ariaDescription The aria-description; a constant textual description.
#' @param ariaHidden The aria-hidden state; a constant indicating whether the element is exposed to an accessibility API.
#' @param ariaLabel The aria-label; a channel specifying short textual labels representing the value in the accessibility tree.
#' @param channels Additional named channels, for example to include in a tooltip.
#' @param clip How to clip the mark; one of: - *frame* or true - clip to the plot’s frame (inner area) - *sphere* - clip to the projected sphere (*e.g.*, front hemisphere) - null or false - do not clip The *sphere* clip option requires a geographic projection.
#' @param dx The horizontal offset in pixels; a constant option.
#' @param dy The vertical offset in pixels; a constant option.
#' @param facet Whether to enable or disable faceting; one of: - *auto* (default) - automatically determine if this mark should be faceted - *include* (or true) - draw the subset of the mark’s data in the current facet - *exclude* - draw the subset of the mark’s data *not* in the current facet - *super* - draw this mark in a single frame that covers all facets - null (or false) - repeat this mark’s data across all facets (*i.e.*, no faceting) When a mark uses *super* faceting, it is not allowed to use position scales (*x*, *y*, *fx*, or *fy*); *super* faceting is intended for decorations, such as labels and legends.
#' @param facetAnchor How to place the mark with respect to facets; one of: - null (default for most marks) - display the mark in each non-empty facet - *top*, *right*, *bottom*, or *left* - display the mark only in facets on the given side - *top-empty*, *right-empty*, *bottom-empty*, or *left-empty* (default for axis marks) - display the mark only in facets that have empty space on the given side: either the margin, or an empty facet - *empty* - display the mark in empty facets only
#' @param fill The fill; a constant CSS color string, or a channel typically bound to the *color* scale.
#' @param fillOpacity The fill-opacity; a constant number between 0 and 1, or a channel typically bound to the *opacity* scale.
#' @param filter Applies a transform to filter the mark’s index according to the given channel values; only truthy values are retained.
#' @param fx The horizontal facet position channel, for mark-level faceting, bound to the *fx* scale.
#' @param fy The vertical facet position channel, for mark-level faceting, bound to the *fy* scale.
#' @param href The href; a channel specifying URLs for clickable links.
#' @param imageFilter A CSS filter; a constant string used to adjust the rendering of images, such as *blur(5px)*.
#' @param inset Shorthand to set the same default for **insetStart** and **insetEnd**.
#' @param insetBottom Insets the bottom edge by the specified number of pixels.
#' @param insetLeft Insets the left edge by the specified number of pixels.
#' @param insetRight Insets the right edge by the specified number of pixels.
#' @param insetTop Insets the top edge by the specified number of pixels.
#' @param interval Enforces uniformity for data at regular intervals, such as integer values or daily samples.
#' @param margin Shorthand to set the same default for all four mark margins: **marginTop**, **marginRight**, **marginBottom**, and **marginLeft**; typically defaults to 0, except for axis marks.
#' @param marginBottom The mark’s bottom margin; the minimum distance in pixels between the bottom edges of the inner and outer plot area.
#' @param marginLeft The mark’s left margin; the minimum distance in pixels between the left edges of the inner and outer plot area.
#' @param marginRight The mark’s right margin; the minimum distance in pixels between the right edges of the mark’s inner and outer plot area.
#' @param marginTop The mark’s top margin; the minimum distance in pixels between the top edges of the inner and outer plot area.
#' @param mixBlendMode The mix-blend-mode; a constant string specifying how to blend content such as *multiply*.
#' @param offset After stacking, an optional **offset** can be applied to translate and scale stacks, say to produce a streamgraph; defaults to null for a zero baseline (**y** = 0 for stackY, and **x** = 0 for stackX).
#' @param opacity The opacity; a constant between 0 and 1, or a channel typically bound to the *opacity* scale.
#' @param order The order in which stacks are layered; one of: - null (default) for input order - a named stack order method such as *inside-out* or *sum* - a field name, for natural order of the corresponding values - a function of data, for natural order of the corresponding values - an array of explicit **z** values in the desired order If the *wiggle* **offset** is used, as for a streamgraph, the default changes to *inside-out*.
#' @param paintOrder The paint-order; a constant string specifying the order in which the **fill**, **stroke**, and any markers are drawn; defaults to *normal*, which draws the fill, then stroke, then markers; defaults to *stroke* for the text mark to create a “halo” around text to improve legibility.
#' @param pointerEvents The pointer-events property; a constant string such as *none*.
#' @param reverse Applies a transform to reverse the order of the mark’s index, say for reverse input order.
#' @param rx The rounded corner *x*-radius, either in pixels or as a percentage of the rect width.
#' @param ry The rounded corner *y*-radius, either in pixels or as a percentage of the rect height.
#' @param select Applies a filter transform after data is loaded to highlight selected values only.
#' @param shapeRendering The shape-rendering; a constant string such as *crispEdges*.
#' @param sort Either applies a transform to sort the mark’s index by the specified channel values, or imputes ordinal scale domains from this mark’s channels.
#' @param stroke The stroke; a constant CSS color string, or a channel typically bound to the *color* scale.
#' @param strokeDasharray The stroke-dasharray; a constant number indicating the length in pixels of alternating dashes and gaps, or a constant string of numbers separated by spaces or commas (_e.g._, *10 2* for dashes of 10 pixels separated by gaps of 2 pixels), or *none* (the default) for no dashing : https://developer.mozilla.org/en-US/docs/Web/SVG/Attribute/stroke-dasharray
#' @param strokeDashoffset The stroke-dashoffset; a constant indicating the offset in pixels of the first dash along the stroke; defaults to zero.
#' @param strokeLinecap The stroke-linecap; a constant specifying how to cap stroked paths, such as *butt*, *round*, or *square*.
#' @param strokeLinejoin The stroke-linejoin; a constant specifying how to join stroked paths, such as *bevel*, *miter*, *miter-clip*, or *round*.
#' @param strokeMiterlimit The stroke-miterlimit; a constant number specifying how to limit the length of *miter* joins on stroked paths.
#' @param strokeOpacity The stroke-opacity; a constant between 0 and 1, or a channel typically bound to the *opacity* scale.
#' @param strokeWidth The stroke-width; a constant number in pixels, or a channel.
#' @param target The target; a constant string specifying the target window (_e.g._, *_blank*) for clickable links; used in conjunction with the **href** option.
#' @param tip Whether to generate a tooltip for this mark, and any tip options.
#' @param title The title; a channel specifying accessible, short textual descriptions as strings (possibly with newlines).
#' @param x The horizontal position (or length) channel, typically bound to the *x* scale.
#' @param x1 The required primary (starting, often left) horizontal position channel, representing the area’s baseline, typically bound to the *x* scale.
#' @param x2 The optional secondary (ending, often right) horizontal position channel, representing the area’s topline, typically bound to the *x* scale; if not specified, **x1** is used.
#' @param y The vertical position channel, typically bound to the *y* scale; defaults to the zero-based index of the data 0, 1, 2, ….
#' @param y1 The required primary (starting, often bottom) vertical position channel, representing the area’s baseline, typically bound to the *y* scale.
#' @param y2 The optional secondary (ending, often top) vertical position channel, representing the area’s topline, typically bound to the *y* scale; if not specified, **y1** is used.
#' @param z An optional ordinal channel for grouping data into (possibly stacked) series to be drawn as separate areas; defaults to **fill** if a channel, or **stroke** if a channel.
#' @param ... Additional options or plot-level attributes.
#' @param data_from The name of the data source this mark reads from (see [vg_data()]).
#' @param filter_by A Param/Selection (e.g. from [param()]) to filter this mark's data by.
#' @family mark functions
#' @export
vg_rect <- function(spec = NULL, ariaDescription = vg_unset, ariaHidden = vg_unset, ariaLabel = vg_unset, channels = vg_unset, clip = vg_unset, dx = vg_unset, dy = vg_unset, facet = vg_unset, facetAnchor = vg_unset, fill = vg_unset, fillOpacity = vg_unset, filter = vg_unset, fx = vg_unset, fy = vg_unset, href = vg_unset, imageFilter = vg_unset, inset = vg_unset, insetBottom = vg_unset, insetLeft = vg_unset, insetRight = vg_unset, insetTop = vg_unset, interval = vg_unset, margin = vg_unset, marginBottom = vg_unset, marginLeft = vg_unset, marginRight = vg_unset, marginTop = vg_unset, mixBlendMode = vg_unset, offset = vg_unset, opacity = vg_unset, order = vg_unset, paintOrder = vg_unset, pointerEvents = vg_unset, reverse = vg_unset, rx = vg_unset, ry = vg_unset, select = vg_unset, shapeRendering = vg_unset, sort = vg_unset, stroke = vg_unset, strokeDasharray = vg_unset, strokeDashoffset = vg_unset, strokeLinecap = vg_unset, strokeLinejoin = vg_unset, strokeMiterlimit = vg_unset, strokeOpacity = vg_unset, strokeWidth = vg_unset, target = vg_unset, tip = vg_unset, title = vg_unset, x = vg_unset, x1 = vg_unset, x2 = vg_unset, y = vg_unset, y1 = vg_unset, y2 = vg_unset, z = vg_unset, ..., data_from = vg_unset, filter_by = vg_unset) {
  vg_mark_(spec, "rect", ariaDescription = ariaDescription, ariaHidden = ariaHidden, ariaLabel = ariaLabel, channels = channels, clip = clip, dx = dx, dy = dy, facet = facet, facetAnchor = facetAnchor, fill = fill, fillOpacity = fillOpacity, filter = filter, fx = fx, fy = fy, href = href, imageFilter = imageFilter, inset = inset, insetBottom = insetBottom, insetLeft = insetLeft, insetRight = insetRight, insetTop = insetTop, interval = interval, margin = margin, marginBottom = marginBottom, marginLeft = marginLeft, marginRight = marginRight, marginTop = marginTop, mixBlendMode = mixBlendMode, offset = offset, opacity = opacity, order = order, paintOrder = paintOrder, pointerEvents = pointerEvents, reverse = reverse, rx = rx, ry = ry, select = select, shapeRendering = shapeRendering, sort = sort, stroke = stroke, strokeDasharray = strokeDasharray, strokeDashoffset = strokeDashoffset, strokeLinecap = strokeLinecap, strokeLinejoin = strokeLinejoin, strokeMiterlimit = strokeMiterlimit, strokeOpacity = strokeOpacity, strokeWidth = strokeWidth, target = target, tip = tip, title = title, x = x, x1 = x1, x2 = x2, y = y, y1 = y1, y2 = y2, z = z, ..., data_from = data_from, filter_by = filter_by)
}

#' The rectX mark.
#'
#' @param spec A plot fragment or `vgspec` to add this mark to, or `NULL` to start a new plot with just this mark.
#' @param ariaDescription The aria-description; a constant textual description.
#' @param ariaHidden The aria-hidden state; a constant indicating whether the element is exposed to an accessibility API.
#' @param ariaLabel The aria-label; a channel specifying short textual labels representing the value in the accessibility tree.
#' @param channels Additional named channels, for example to include in a tooltip.
#' @param clip How to clip the mark; one of: - *frame* or true - clip to the plot’s frame (inner area) - *sphere* - clip to the projected sphere (*e.g.*, front hemisphere) - null or false - do not clip The *sphere* clip option requires a geographic projection.
#' @param dx The horizontal offset in pixels; a constant option.
#' @param dy The vertical offset in pixels; a constant option.
#' @param facet Whether to enable or disable faceting; one of: - *auto* (default) - automatically determine if this mark should be faceted - *include* (or true) - draw the subset of the mark’s data in the current facet - *exclude* - draw the subset of the mark’s data *not* in the current facet - *super* - draw this mark in a single frame that covers all facets - null (or false) - repeat this mark’s data across all facets (*i.e.*, no faceting) When a mark uses *super* faceting, it is not allowed to use position scales (*x*, *y*, *fx*, or *fy*); *super* faceting is intended for decorations, such as labels and legends.
#' @param facetAnchor How to place the mark with respect to facets; one of: - null (default for most marks) - display the mark in each non-empty facet - *top*, *right*, *bottom*, or *left* - display the mark only in facets on the given side - *top-empty*, *right-empty*, *bottom-empty*, or *left-empty* (default for axis marks) - display the mark only in facets that have empty space on the given side: either the margin, or an empty facet - *empty* - display the mark in empty facets only
#' @param fill The fill; a constant CSS color string, or a channel typically bound to the *color* scale.
#' @param fillOpacity The fill-opacity; a constant number between 0 and 1, or a channel typically bound to the *opacity* scale.
#' @param filter Applies a transform to filter the mark’s index according to the given channel values; only truthy values are retained.
#' @param fx The horizontal facet position channel, for mark-level faceting, bound to the *fx* scale.
#' @param fy The vertical facet position channel, for mark-level faceting, bound to the *fy* scale.
#' @param href The href; a channel specifying URLs for clickable links.
#' @param imageFilter A CSS filter; a constant string used to adjust the rendering of images, such as *blur(5px)*.
#' @param inset Shorthand to set the same default for **insetStart** and **insetEnd**.
#' @param insetBottom Insets the bottom edge by the specified number of pixels.
#' @param insetLeft Insets the left edge by the specified number of pixels.
#' @param insetRight Insets the right edge by the specified number of pixels.
#' @param insetTop Insets the top edge by the specified number of pixels.
#' @param interval Enforces uniformity for data at regular intervals, such as integer values or daily samples.
#' @param margin Shorthand to set the same default for all four mark margins: **marginTop**, **marginRight**, **marginBottom**, and **marginLeft**; typically defaults to 0, except for axis marks.
#' @param marginBottom The mark’s bottom margin; the minimum distance in pixels between the bottom edges of the inner and outer plot area.
#' @param marginLeft The mark’s left margin; the minimum distance in pixels between the left edges of the inner and outer plot area.
#' @param marginRight The mark’s right margin; the minimum distance in pixels between the right edges of the mark’s inner and outer plot area.
#' @param marginTop The mark’s top margin; the minimum distance in pixels between the top edges of the inner and outer plot area.
#' @param mixBlendMode The mix-blend-mode; a constant string specifying how to blend content such as *multiply*.
#' @param offset After stacking, an optional **offset** can be applied to translate and scale stacks, say to produce a streamgraph; defaults to null for a zero baseline (**y** = 0 for stackY, and **x** = 0 for stackX).
#' @param opacity The opacity; a constant between 0 and 1, or a channel typically bound to the *opacity* scale.
#' @param order The order in which stacks are layered; one of: - null (default) for input order - a named stack order method such as *inside-out* or *sum* - a field name, for natural order of the corresponding values - a function of data, for natural order of the corresponding values - an array of explicit **z** values in the desired order If the *wiggle* **offset** is used, as for a streamgraph, the default changes to *inside-out*.
#' @param paintOrder The paint-order; a constant string specifying the order in which the **fill**, **stroke**, and any markers are drawn; defaults to *normal*, which draws the fill, then stroke, then markers; defaults to *stroke* for the text mark to create a “halo” around text to improve legibility.
#' @param pointerEvents The pointer-events property; a constant string such as *none*.
#' @param reverse Applies a transform to reverse the order of the mark’s index, say for reverse input order.
#' @param rx The rounded corner *x*-radius, either in pixels or as a percentage of the rect width.
#' @param ry The rounded corner *y*-radius, either in pixels or as a percentage of the rect height.
#' @param select Applies a filter transform after data is loaded to highlight selected values only.
#' @param shapeRendering The shape-rendering; a constant string such as *crispEdges*.
#' @param sort Either applies a transform to sort the mark’s index by the specified channel values, or imputes ordinal scale domains from this mark’s channels.
#' @param stroke The stroke; a constant CSS color string, or a channel typically bound to the *color* scale.
#' @param strokeDasharray The stroke-dasharray; a constant number indicating the length in pixels of alternating dashes and gaps, or a constant string of numbers separated by spaces or commas (_e.g._, *10 2* for dashes of 10 pixels separated by gaps of 2 pixels), or *none* (the default) for no dashing : https://developer.mozilla.org/en-US/docs/Web/SVG/Attribute/stroke-dasharray
#' @param strokeDashoffset The stroke-dashoffset; a constant indicating the offset in pixels of the first dash along the stroke; defaults to zero.
#' @param strokeLinecap The stroke-linecap; a constant specifying how to cap stroked paths, such as *butt*, *round*, or *square*.
#' @param strokeLinejoin The stroke-linejoin; a constant specifying how to join stroked paths, such as *bevel*, *miter*, *miter-clip*, or *round*.
#' @param strokeMiterlimit The stroke-miterlimit; a constant number specifying how to limit the length of *miter* joins on stroked paths.
#' @param strokeOpacity The stroke-opacity; a constant between 0 and 1, or a channel typically bound to the *opacity* scale.
#' @param strokeWidth The stroke-width; a constant number in pixels, or a channel.
#' @param target The target; a constant string specifying the target window (_e.g._, *_blank*) for clickable links; used in conjunction with the **href** option.
#' @param tip Whether to generate a tooltip for this mark, and any tip options.
#' @param title The title; a channel specifying accessible, short textual descriptions as strings (possibly with newlines).
#' @param x The horizontal position (or length) channel, typically bound to the *x* scale.
#' @param x1 The required primary (starting, often left) horizontal position channel, representing the area’s baseline, typically bound to the *x* scale.
#' @param x2 The optional secondary (ending, often right) horizontal position channel, representing the area’s topline, typically bound to the *x* scale; if not specified, **x1** is used.
#' @param y The vertical position channel, typically bound to the *y* scale; defaults to the zero-based index of the data 0, 1, 2, ….
#' @param y1 The required primary (starting, often bottom) vertical position channel, representing the area’s baseline, typically bound to the *y* scale.
#' @param y2 The optional secondary (ending, often top) vertical position channel, representing the area’s topline, typically bound to the *y* scale; if not specified, **y1** is used.
#' @param z An optional ordinal channel for grouping data into (possibly stacked) series to be drawn as separate areas; defaults to **fill** if a channel, or **stroke** if a channel.
#' @param ... Additional options or plot-level attributes.
#' @param data_from The name of the data source this mark reads from (see [vg_data()]).
#' @param filter_by A Param/Selection (e.g. from [param()]) to filter this mark's data by.
#' @family mark functions
#' @export
vg_rect_x <- function(spec = NULL, ariaDescription = vg_unset, ariaHidden = vg_unset, ariaLabel = vg_unset, channels = vg_unset, clip = vg_unset, dx = vg_unset, dy = vg_unset, facet = vg_unset, facetAnchor = vg_unset, fill = vg_unset, fillOpacity = vg_unset, filter = vg_unset, fx = vg_unset, fy = vg_unset, href = vg_unset, imageFilter = vg_unset, inset = vg_unset, insetBottom = vg_unset, insetLeft = vg_unset, insetRight = vg_unset, insetTop = vg_unset, interval = vg_unset, margin = vg_unset, marginBottom = vg_unset, marginLeft = vg_unset, marginRight = vg_unset, marginTop = vg_unset, mixBlendMode = vg_unset, offset = vg_unset, opacity = vg_unset, order = vg_unset, paintOrder = vg_unset, pointerEvents = vg_unset, reverse = vg_unset, rx = vg_unset, ry = vg_unset, select = vg_unset, shapeRendering = vg_unset, sort = vg_unset, stroke = vg_unset, strokeDasharray = vg_unset, strokeDashoffset = vg_unset, strokeLinecap = vg_unset, strokeLinejoin = vg_unset, strokeMiterlimit = vg_unset, strokeOpacity = vg_unset, strokeWidth = vg_unset, target = vg_unset, tip = vg_unset, title = vg_unset, x = vg_unset, x1 = vg_unset, x2 = vg_unset, y = vg_unset, y1 = vg_unset, y2 = vg_unset, z = vg_unset, ..., data_from = vg_unset, filter_by = vg_unset) {
  vg_mark_(spec, "rectX", ariaDescription = ariaDescription, ariaHidden = ariaHidden, ariaLabel = ariaLabel, channels = channels, clip = clip, dx = dx, dy = dy, facet = facet, facetAnchor = facetAnchor, fill = fill, fillOpacity = fillOpacity, filter = filter, fx = fx, fy = fy, href = href, imageFilter = imageFilter, inset = inset, insetBottom = insetBottom, insetLeft = insetLeft, insetRight = insetRight, insetTop = insetTop, interval = interval, margin = margin, marginBottom = marginBottom, marginLeft = marginLeft, marginRight = marginRight, marginTop = marginTop, mixBlendMode = mixBlendMode, offset = offset, opacity = opacity, order = order, paintOrder = paintOrder, pointerEvents = pointerEvents, reverse = reverse, rx = rx, ry = ry, select = select, shapeRendering = shapeRendering, sort = sort, stroke = stroke, strokeDasharray = strokeDasharray, strokeDashoffset = strokeDashoffset, strokeLinecap = strokeLinecap, strokeLinejoin = strokeLinejoin, strokeMiterlimit = strokeMiterlimit, strokeOpacity = strokeOpacity, strokeWidth = strokeWidth, target = target, tip = tip, title = title, x = x, x1 = x1, x2 = x2, y = y, y1 = y1, y2 = y2, z = z, ..., data_from = data_from, filter_by = filter_by)
}

#' The rectY mark.
#'
#' @param spec A plot fragment or `vgspec` to add this mark to, or `NULL` to start a new plot with just this mark.
#' @param ariaDescription The aria-description; a constant textual description.
#' @param ariaHidden The aria-hidden state; a constant indicating whether the element is exposed to an accessibility API.
#' @param ariaLabel The aria-label; a channel specifying short textual labels representing the value in the accessibility tree.
#' @param channels Additional named channels, for example to include in a tooltip.
#' @param clip How to clip the mark; one of: - *frame* or true - clip to the plot’s frame (inner area) - *sphere* - clip to the projected sphere (*e.g.*, front hemisphere) - null or false - do not clip The *sphere* clip option requires a geographic projection.
#' @param dx The horizontal offset in pixels; a constant option.
#' @param dy The vertical offset in pixels; a constant option.
#' @param facet Whether to enable or disable faceting; one of: - *auto* (default) - automatically determine if this mark should be faceted - *include* (or true) - draw the subset of the mark’s data in the current facet - *exclude* - draw the subset of the mark’s data *not* in the current facet - *super* - draw this mark in a single frame that covers all facets - null (or false) - repeat this mark’s data across all facets (*i.e.*, no faceting) When a mark uses *super* faceting, it is not allowed to use position scales (*x*, *y*, *fx*, or *fy*); *super* faceting is intended for decorations, such as labels and legends.
#' @param facetAnchor How to place the mark with respect to facets; one of: - null (default for most marks) - display the mark in each non-empty facet - *top*, *right*, *bottom*, or *left* - display the mark only in facets on the given side - *top-empty*, *right-empty*, *bottom-empty*, or *left-empty* (default for axis marks) - display the mark only in facets that have empty space on the given side: either the margin, or an empty facet - *empty* - display the mark in empty facets only
#' @param fill The fill; a constant CSS color string, or a channel typically bound to the *color* scale.
#' @param fillOpacity The fill-opacity; a constant number between 0 and 1, or a channel typically bound to the *opacity* scale.
#' @param filter Applies a transform to filter the mark’s index according to the given channel values; only truthy values are retained.
#' @param fx The horizontal facet position channel, for mark-level faceting, bound to the *fx* scale.
#' @param fy The vertical facet position channel, for mark-level faceting, bound to the *fy* scale.
#' @param href The href; a channel specifying URLs for clickable links.
#' @param imageFilter A CSS filter; a constant string used to adjust the rendering of images, such as *blur(5px)*.
#' @param inset Shorthand to set the same default for **insetStart** and **insetEnd**.
#' @param insetBottom Insets the bottom edge by the specified number of pixels.
#' @param insetLeft Insets the left edge by the specified number of pixels.
#' @param insetRight Insets the right edge by the specified number of pixels.
#' @param insetTop Insets the top edge by the specified number of pixels.
#' @param interval Enforces uniformity for data at regular intervals, such as integer values or daily samples.
#' @param margin Shorthand to set the same default for all four mark margins: **marginTop**, **marginRight**, **marginBottom**, and **marginLeft**; typically defaults to 0, except for axis marks.
#' @param marginBottom The mark’s bottom margin; the minimum distance in pixels between the bottom edges of the inner and outer plot area.
#' @param marginLeft The mark’s left margin; the minimum distance in pixels between the left edges of the inner and outer plot area.
#' @param marginRight The mark’s right margin; the minimum distance in pixels between the right edges of the mark’s inner and outer plot area.
#' @param marginTop The mark’s top margin; the minimum distance in pixels between the top edges of the inner and outer plot area.
#' @param mixBlendMode The mix-blend-mode; a constant string specifying how to blend content such as *multiply*.
#' @param offset After stacking, an optional **offset** can be applied to translate and scale stacks, say to produce a streamgraph; defaults to null for a zero baseline (**y** = 0 for stackY, and **x** = 0 for stackX).
#' @param opacity The opacity; a constant between 0 and 1, or a channel typically bound to the *opacity* scale.
#' @param order The order in which stacks are layered; one of: - null (default) for input order - a named stack order method such as *inside-out* or *sum* - a field name, for natural order of the corresponding values - a function of data, for natural order of the corresponding values - an array of explicit **z** values in the desired order If the *wiggle* **offset** is used, as for a streamgraph, the default changes to *inside-out*.
#' @param paintOrder The paint-order; a constant string specifying the order in which the **fill**, **stroke**, and any markers are drawn; defaults to *normal*, which draws the fill, then stroke, then markers; defaults to *stroke* for the text mark to create a “halo” around text to improve legibility.
#' @param pointerEvents The pointer-events property; a constant string such as *none*.
#' @param reverse Applies a transform to reverse the order of the mark’s index, say for reverse input order.
#' @param rx The rounded corner *x*-radius, either in pixels or as a percentage of the rect width.
#' @param ry The rounded corner *y*-radius, either in pixels or as a percentage of the rect height.
#' @param select Applies a filter transform after data is loaded to highlight selected values only.
#' @param shapeRendering The shape-rendering; a constant string such as *crispEdges*.
#' @param sort Either applies a transform to sort the mark’s index by the specified channel values, or imputes ordinal scale domains from this mark’s channels.
#' @param stroke The stroke; a constant CSS color string, or a channel typically bound to the *color* scale.
#' @param strokeDasharray The stroke-dasharray; a constant number indicating the length in pixels of alternating dashes and gaps, or a constant string of numbers separated by spaces or commas (_e.g._, *10 2* for dashes of 10 pixels separated by gaps of 2 pixels), or *none* (the default) for no dashing : https://developer.mozilla.org/en-US/docs/Web/SVG/Attribute/stroke-dasharray
#' @param strokeDashoffset The stroke-dashoffset; a constant indicating the offset in pixels of the first dash along the stroke; defaults to zero.
#' @param strokeLinecap The stroke-linecap; a constant specifying how to cap stroked paths, such as *butt*, *round*, or *square*.
#' @param strokeLinejoin The stroke-linejoin; a constant specifying how to join stroked paths, such as *bevel*, *miter*, *miter-clip*, or *round*.
#' @param strokeMiterlimit The stroke-miterlimit; a constant number specifying how to limit the length of *miter* joins on stroked paths.
#' @param strokeOpacity The stroke-opacity; a constant between 0 and 1, or a channel typically bound to the *opacity* scale.
#' @param strokeWidth The stroke-width; a constant number in pixels, or a channel.
#' @param target The target; a constant string specifying the target window (_e.g._, *_blank*) for clickable links; used in conjunction with the **href** option.
#' @param tip Whether to generate a tooltip for this mark, and any tip options.
#' @param title The title; a channel specifying accessible, short textual descriptions as strings (possibly with newlines).
#' @param x The horizontal position (or length) channel, typically bound to the *x* scale.
#' @param x1 The required primary (starting, often left) horizontal position channel, representing the area’s baseline, typically bound to the *x* scale.
#' @param x2 The optional secondary (ending, often right) horizontal position channel, representing the area’s topline, typically bound to the *x* scale; if not specified, **x1** is used.
#' @param y The vertical position channel, typically bound to the *y* scale; defaults to the zero-based index of the data 0, 1, 2, ….
#' @param y1 The required primary (starting, often bottom) vertical position channel, representing the area’s baseline, typically bound to the *y* scale.
#' @param y2 The optional secondary (ending, often top) vertical position channel, representing the area’s topline, typically bound to the *y* scale; if not specified, **y1** is used.
#' @param z An optional ordinal channel for grouping data into (possibly stacked) series to be drawn as separate areas; defaults to **fill** if a channel, or **stroke** if a channel.
#' @param ... Additional options or plot-level attributes.
#' @param data_from The name of the data source this mark reads from (see [vg_data()]).
#' @param filter_by A Param/Selection (e.g. from [param()]) to filter this mark's data by.
#' @family mark functions
#' @export
vg_rect_y <- function(spec = NULL, ariaDescription = vg_unset, ariaHidden = vg_unset, ariaLabel = vg_unset, channels = vg_unset, clip = vg_unset, dx = vg_unset, dy = vg_unset, facet = vg_unset, facetAnchor = vg_unset, fill = vg_unset, fillOpacity = vg_unset, filter = vg_unset, fx = vg_unset, fy = vg_unset, href = vg_unset, imageFilter = vg_unset, inset = vg_unset, insetBottom = vg_unset, insetLeft = vg_unset, insetRight = vg_unset, insetTop = vg_unset, interval = vg_unset, margin = vg_unset, marginBottom = vg_unset, marginLeft = vg_unset, marginRight = vg_unset, marginTop = vg_unset, mixBlendMode = vg_unset, offset = vg_unset, opacity = vg_unset, order = vg_unset, paintOrder = vg_unset, pointerEvents = vg_unset, reverse = vg_unset, rx = vg_unset, ry = vg_unset, select = vg_unset, shapeRendering = vg_unset, sort = vg_unset, stroke = vg_unset, strokeDasharray = vg_unset, strokeDashoffset = vg_unset, strokeLinecap = vg_unset, strokeLinejoin = vg_unset, strokeMiterlimit = vg_unset, strokeOpacity = vg_unset, strokeWidth = vg_unset, target = vg_unset, tip = vg_unset, title = vg_unset, x = vg_unset, x1 = vg_unset, x2 = vg_unset, y = vg_unset, y1 = vg_unset, y2 = vg_unset, z = vg_unset, ..., data_from = vg_unset, filter_by = vg_unset) {
  vg_mark_(spec, "rectY", ariaDescription = ariaDescription, ariaHidden = ariaHidden, ariaLabel = ariaLabel, channels = channels, clip = clip, dx = dx, dy = dy, facet = facet, facetAnchor = facetAnchor, fill = fill, fillOpacity = fillOpacity, filter = filter, fx = fx, fy = fy, href = href, imageFilter = imageFilter, inset = inset, insetBottom = insetBottom, insetLeft = insetLeft, insetRight = insetRight, insetTop = insetTop, interval = interval, margin = margin, marginBottom = marginBottom, marginLeft = marginLeft, marginRight = marginRight, marginTop = marginTop, mixBlendMode = mixBlendMode, offset = offset, opacity = opacity, order = order, paintOrder = paintOrder, pointerEvents = pointerEvents, reverse = reverse, rx = rx, ry = ry, select = select, shapeRendering = shapeRendering, sort = sort, stroke = stroke, strokeDasharray = strokeDasharray, strokeDashoffset = strokeDashoffset, strokeLinecap = strokeLinecap, strokeLinejoin = strokeLinejoin, strokeMiterlimit = strokeMiterlimit, strokeOpacity = strokeOpacity, strokeWidth = strokeWidth, target = target, tip = tip, title = title, x = x, x1 = x1, x2 = x2, y = y, y1 = y1, y2 = y2, z = z, ..., data_from = data_from, filter_by = filter_by)
}

#' The regressionY mark.
#'
#' @param spec A plot fragment or `vgspec` to add this mark to, or `NULL` to start a new plot with just this mark.
#' @param ariaDescription The aria-description; a constant textual description.
#' @param ariaHidden The aria-hidden state; a constant indicating whether the element is exposed to an accessibility API.
#' @param ariaLabel The aria-label; a channel specifying short textual labels representing the value in the accessibility tree.
#' @param channels Additional named channels, for example to include in a tooltip.
#' @param ci The confidence interval in (0, 1); defaults to 0.95.
#' @param clip How to clip the mark; one of: - *frame* or true - clip to the plot’s frame (inner area) - *sphere* - clip to the projected sphere (*e.g.*, front hemisphere) - null or false - do not clip The *sphere* clip option requires a geographic projection.
#' @param dx The horizontal offset in pixels; a constant option.
#' @param dy The vertical offset in pixels; a constant option.
#' @param facet Whether to enable or disable faceting; one of: - *auto* (default) - automatically determine if this mark should be faceted - *include* (or true) - draw the subset of the mark’s data in the current facet - *exclude* - draw the subset of the mark’s data *not* in the current facet - *super* - draw this mark in a single frame that covers all facets - null (or false) - repeat this mark’s data across all facets (*i.e.*, no faceting) When a mark uses *super* faceting, it is not allowed to use position scales (*x*, *y*, *fx*, or *fy*); *super* faceting is intended for decorations, such as labels and legends.
#' @param facetAnchor How to place the mark with respect to facets; one of: - null (default for most marks) - display the mark in each non-empty facet - *top*, *right*, *bottom*, or *left* - display the mark only in facets on the given side - *top-empty*, *right-empty*, *bottom-empty*, or *left-empty* (default for axis marks) - display the mark only in facets that have empty space on the given side: either the margin, or an empty facet - *empty* - display the mark in empty facets only
#' @param fill The fill; a constant CSS color string, or a channel typically bound to the *color* scale.
#' @param fillOpacity The fill-opacity; a constant number between 0 and 1, or a channel typically bound to the *opacity* scale.
#' @param filter Applies a transform to filter the mark’s index according to the given channel values; only truthy values are retained.
#' @param fx The horizontal facet position channel, for mark-level faceting, bound to the *fx* scale.
#' @param fy The vertical facet position channel, for mark-level faceting, bound to the *fy* scale.
#' @param href The href; a channel specifying URLs for clickable links.
#' @param imageFilter A CSS filter; a constant string used to adjust the rendering of images, such as *blur(5px)*.
#' @param margin Shorthand to set the same default for all four mark margins: **marginTop**, **marginRight**, **marginBottom**, and **marginLeft**; typically defaults to 0, except for axis marks.
#' @param marginBottom The mark’s bottom margin; the minimum distance in pixels between the bottom edges of the inner and outer plot area.
#' @param marginLeft The mark’s left margin; the minimum distance in pixels between the left edges of the inner and outer plot area.
#' @param marginRight The mark’s right margin; the minimum distance in pixels between the right edges of the mark’s inner and outer plot area.
#' @param marginTop The mark’s top margin; the minimum distance in pixels between the top edges of the inner and outer plot area.
#' @param mixBlendMode The mix-blend-mode; a constant string specifying how to blend content such as *multiply*.
#' @param opacity The opacity; a constant between 0 and 1, or a channel typically bound to the *opacity* scale.
#' @param paintOrder The paint-order; a constant string specifying the order in which the **fill**, **stroke**, and any markers are drawn; defaults to *normal*, which draws the fill, then stroke, then markers; defaults to *stroke* for the text mark to create a “halo” around text to improve legibility.
#' @param pointerEvents The pointer-events property; a constant string such as *none*.
#' @param precision The distance in pixels between samples of the confidence band; defaults to 4.
#' @param reverse Applies a transform to reverse the order of the mark’s index, say for reverse input order.
#' @param select Applies a filter transform after data is loaded to highlight selected values only.
#' @param shapeRendering The shape-rendering; a constant string such as *crispEdges*.
#' @param sort Either applies a transform to sort the mark’s index by the specified channel values, or imputes ordinal scale domains from this mark’s channels.
#' @param stroke The stroke; a constant CSS color string, or a channel typically bound to the *color* scale.
#' @param strokeDasharray The stroke-dasharray; a constant number indicating the length in pixels of alternating dashes and gaps, or a constant string of numbers separated by spaces or commas (_e.g._, *10 2* for dashes of 10 pixels separated by gaps of 2 pixels), or *none* (the default) for no dashing : https://developer.mozilla.org/en-US/docs/Web/SVG/Attribute/stroke-dasharray
#' @param strokeDashoffset The stroke-dashoffset; a constant indicating the offset in pixels of the first dash along the stroke; defaults to zero.
#' @param strokeLinecap The stroke-linecap; a constant specifying how to cap stroked paths, such as *butt*, *round*, or *square*.
#' @param strokeLinejoin The stroke-linejoin; a constant specifying how to join stroked paths, such as *bevel*, *miter*, *miter-clip*, or *round*.
#' @param strokeMiterlimit The stroke-miterlimit; a constant number specifying how to limit the length of *miter* joins on stroked paths.
#' @param strokeOpacity The stroke-opacity; a constant between 0 and 1, or a channel typically bound to the *opacity* scale.
#' @param strokeWidth The stroke-width; a constant number in pixels, or a channel.
#' @param target The target; a constant string specifying the target window (_e.g._, *_blank*) for clickable links; used in conjunction with the **href** option.
#' @param tip Whether to generate a tooltip for this mark, and any tip options.
#' @param title The title; a channel specifying accessible, short textual descriptions as strings (possibly with newlines).
#' @param x The horizontal position (or length) channel, typically bound to the *x* scale.
#' @param y The vertical position channel, typically bound to the *y* scale; defaults to the zero-based index of the data 0, 1, 2, ….
#' @param z An optional ordinal channel for grouping data into (possibly stacked) series to be drawn as separate areas; defaults to **fill** if a channel, or **stroke** if a channel.
#' @param ... Additional options or plot-level attributes.
#' @param data_from The name of the data source this mark reads from (see [vg_data()]).
#' @param filter_by A Param/Selection (e.g. from [param()]) to filter this mark's data by.
#' @family mark functions
#' @export
vg_regression_y <- function(spec = NULL, ariaDescription = vg_unset, ariaHidden = vg_unset, ariaLabel = vg_unset, channels = vg_unset, ci = vg_unset, clip = vg_unset, dx = vg_unset, dy = vg_unset, facet = vg_unset, facetAnchor = vg_unset, fill = vg_unset, fillOpacity = vg_unset, filter = vg_unset, fx = vg_unset, fy = vg_unset, href = vg_unset, imageFilter = vg_unset, margin = vg_unset, marginBottom = vg_unset, marginLeft = vg_unset, marginRight = vg_unset, marginTop = vg_unset, mixBlendMode = vg_unset, opacity = vg_unset, paintOrder = vg_unset, pointerEvents = vg_unset, precision = vg_unset, reverse = vg_unset, select = vg_unset, shapeRendering = vg_unset, sort = vg_unset, stroke = vg_unset, strokeDasharray = vg_unset, strokeDashoffset = vg_unset, strokeLinecap = vg_unset, strokeLinejoin = vg_unset, strokeMiterlimit = vg_unset, strokeOpacity = vg_unset, strokeWidth = vg_unset, target = vg_unset, tip = vg_unset, title = vg_unset, x = vg_unset, y = vg_unset, z = vg_unset, ..., data_from = vg_unset, filter_by = vg_unset) {
  vg_mark_(spec, "regressionY", ariaDescription = ariaDescription, ariaHidden = ariaHidden, ariaLabel = ariaLabel, channels = channels, ci = ci, clip = clip, dx = dx, dy = dy, facet = facet, facetAnchor = facetAnchor, fill = fill, fillOpacity = fillOpacity, filter = filter, fx = fx, fy = fy, href = href, imageFilter = imageFilter, margin = margin, marginBottom = marginBottom, marginLeft = marginLeft, marginRight = marginRight, marginTop = marginTop, mixBlendMode = mixBlendMode, opacity = opacity, paintOrder = paintOrder, pointerEvents = pointerEvents, precision = precision, reverse = reverse, select = select, shapeRendering = shapeRendering, sort = sort, stroke = stroke, strokeDasharray = strokeDasharray, strokeDashoffset = strokeDashoffset, strokeLinecap = strokeLinecap, strokeLinejoin = strokeLinejoin, strokeMiterlimit = strokeMiterlimit, strokeOpacity = strokeOpacity, strokeWidth = strokeWidth, target = target, tip = tip, title = title, x = x, y = y, z = z, ..., data_from = data_from, filter_by = filter_by)
}

#' The ruleX mark.
#'
#' @param spec A plot fragment or `vgspec` to add this mark to, or `NULL` to start a new plot with just this mark.
#' @param ariaDescription The aria-description; a constant textual description.
#' @param ariaHidden The aria-hidden state; a constant indicating whether the element is exposed to an accessibility API.
#' @param ariaLabel The aria-label; a channel specifying short textual labels representing the value in the accessibility tree.
#' @param channels Additional named channels, for example to include in a tooltip.
#' @param clip How to clip the mark; one of: - *frame* or true - clip to the plot’s frame (inner area) - *sphere* - clip to the projected sphere (*e.g.*, front hemisphere) - null or false - do not clip The *sphere* clip option requires a geographic projection.
#' @param dx The horizontal offset in pixels; a constant option.
#' @param dy The vertical offset in pixels; a constant option.
#' @param facet Whether to enable or disable faceting; one of: - *auto* (default) - automatically determine if this mark should be faceted - *include* (or true) - draw the subset of the mark’s data in the current facet - *exclude* - draw the subset of the mark’s data *not* in the current facet - *super* - draw this mark in a single frame that covers all facets - null (or false) - repeat this mark’s data across all facets (*i.e.*, no faceting) When a mark uses *super* faceting, it is not allowed to use position scales (*x*, *y*, *fx*, or *fy*); *super* faceting is intended for decorations, such as labels and legends.
#' @param facetAnchor How to place the mark with respect to facets; one of: - null (default for most marks) - display the mark in each non-empty facet - *top*, *right*, *bottom*, or *left* - display the mark only in facets on the given side - *top-empty*, *right-empty*, *bottom-empty*, or *left-empty* (default for axis marks) - display the mark only in facets that have empty space on the given side: either the margin, or an empty facet - *empty* - display the mark in empty facets only
#' @param fill The fill; a constant CSS color string, or a channel typically bound to the *color* scale.
#' @param fillOpacity The fill-opacity; a constant number between 0 and 1, or a channel typically bound to the *opacity* scale.
#' @param filter Applies a transform to filter the mark’s index according to the given channel values; only truthy values are retained.
#' @param fx The horizontal facet position channel, for mark-level faceting, bound to the *fx* scale.
#' @param fy The vertical facet position channel, for mark-level faceting, bound to the *fy* scale.
#' @param href The href; a channel specifying URLs for clickable links.
#' @param imageFilter A CSS filter; a constant string used to adjust the rendering of images, such as *blur(5px)*.
#' @param inset Shorthand to set the same default for **insetStart** and **insetEnd**.
#' @param insetBottom Insets the bottom edge by the specified number of pixels.
#' @param insetTop Insets the top edge by the specified number of pixels.
#' @param interval Enforces uniformity for data at regular intervals, such as integer values or daily samples.
#' @param margin Shorthand to set the same default for all four mark margins: **marginTop**, **marginRight**, **marginBottom**, and **marginLeft**; typically defaults to 0, except for axis marks.
#' @param marginBottom The mark’s bottom margin; the minimum distance in pixels between the bottom edges of the inner and outer plot area.
#' @param marginLeft The mark’s left margin; the minimum distance in pixels between the left edges of the inner and outer plot area.
#' @param marginRight The mark’s right margin; the minimum distance in pixels between the right edges of the mark’s inner and outer plot area.
#' @param marginTop The mark’s top margin; the minimum distance in pixels between the top edges of the inner and outer plot area.
#' @param marker Shorthand to set the same default for markerStart, markerMid, and markerEnd; one of: - a marker name such as *arrow* or *circle* - *none* (default) - no marker * true - alias for *circle-fill* * false or null - alias for *none*
#' @param markerEnd The marker for the ending point of a line segment; one of: - a marker name such as *arrow* or *circle* * *none* (default) - no marker * true - alias for *circle-fill* * false or null - alias for *none*
#' @param markerMid The marker for any middle (interior) points of a line segment.
#' @param markerStart The marker for the starting point of a line segment; one of: - a marker name such as *arrow* or *circle* * *none* (default) - no marker * true - alias for *circle-fill* * false or null - alias for *none*
#' @param mixBlendMode The mix-blend-mode; a constant string specifying how to blend content such as *multiply*.
#' @param opacity The opacity; a constant between 0 and 1, or a channel typically bound to the *opacity* scale.
#' @param paintOrder The paint-order; a constant string specifying the order in which the **fill**, **stroke**, and any markers are drawn; defaults to *normal*, which draws the fill, then stroke, then markers; defaults to *stroke* for the text mark to create a “halo” around text to improve legibility.
#' @param pointerEvents The pointer-events property; a constant string such as *none*.
#' @param reverse Applies a transform to reverse the order of the mark’s index, say for reverse input order.
#' @param select Applies a filter transform after data is loaded to highlight selected values only.
#' @param shapeRendering The shape-rendering; a constant string such as *crispEdges*.
#' @param sort Either applies a transform to sort the mark’s index by the specified channel values, or imputes ordinal scale domains from this mark’s channels.
#' @param stroke The stroke; a constant CSS color string, or a channel typically bound to the *color* scale.
#' @param strokeDasharray The stroke-dasharray; a constant number indicating the length in pixels of alternating dashes and gaps, or a constant string of numbers separated by spaces or commas (_e.g._, *10 2* for dashes of 10 pixels separated by gaps of 2 pixels), or *none* (the default) for no dashing : https://developer.mozilla.org/en-US/docs/Web/SVG/Attribute/stroke-dasharray
#' @param strokeDashoffset The stroke-dashoffset; a constant indicating the offset in pixels of the first dash along the stroke; defaults to zero.
#' @param strokeLinecap The stroke-linecap; a constant specifying how to cap stroked paths, such as *butt*, *round*, or *square*.
#' @param strokeLinejoin The stroke-linejoin; a constant specifying how to join stroked paths, such as *bevel*, *miter*, *miter-clip*, or *round*.
#' @param strokeMiterlimit The stroke-miterlimit; a constant number specifying how to limit the length of *miter* joins on stroked paths.
#' @param strokeOpacity The stroke-opacity; a constant between 0 and 1, or a channel typically bound to the *opacity* scale.
#' @param strokeWidth The stroke-width; a constant number in pixels, or a channel.
#' @param target The target; a constant string specifying the target window (_e.g._, *_blank*) for clickable links; used in conjunction with the **href** option.
#' @param tip Whether to generate a tooltip for this mark, and any tip options.
#' @param title The title; a channel specifying accessible, short textual descriptions as strings (possibly with newlines).
#' @param x The horizontal position (or length) channel, typically bound to the *x* scale.
#' @param y The vertical position channel, typically bound to the *y* scale; defaults to the zero-based index of the data 0, 1, 2, ….
#' @param y1 The required primary (starting, often bottom) vertical position channel, representing the area’s baseline, typically bound to the *y* scale.
#' @param y2 The optional secondary (ending, often top) vertical position channel, representing the area’s topline, typically bound to the *y* scale; if not specified, **y1** is used.
#' @param ... Additional options or plot-level attributes.
#' @param data_from The name of the data source this mark reads from (see [vg_data()]).
#' @param filter_by A Param/Selection (e.g. from [param()]) to filter this mark's data by.
#' @family mark functions
#' @export
vg_rule_x <- function(spec = NULL, ariaDescription = vg_unset, ariaHidden = vg_unset, ariaLabel = vg_unset, channels = vg_unset, clip = vg_unset, dx = vg_unset, dy = vg_unset, facet = vg_unset, facetAnchor = vg_unset, fill = vg_unset, fillOpacity = vg_unset, filter = vg_unset, fx = vg_unset, fy = vg_unset, href = vg_unset, imageFilter = vg_unset, inset = vg_unset, insetBottom = vg_unset, insetTop = vg_unset, interval = vg_unset, margin = vg_unset, marginBottom = vg_unset, marginLeft = vg_unset, marginRight = vg_unset, marginTop = vg_unset, marker = vg_unset, markerEnd = vg_unset, markerMid = vg_unset, markerStart = vg_unset, mixBlendMode = vg_unset, opacity = vg_unset, paintOrder = vg_unset, pointerEvents = vg_unset, reverse = vg_unset, select = vg_unset, shapeRendering = vg_unset, sort = vg_unset, stroke = vg_unset, strokeDasharray = vg_unset, strokeDashoffset = vg_unset, strokeLinecap = vg_unset, strokeLinejoin = vg_unset, strokeMiterlimit = vg_unset, strokeOpacity = vg_unset, strokeWidth = vg_unset, target = vg_unset, tip = vg_unset, title = vg_unset, x = vg_unset, y = vg_unset, y1 = vg_unset, y2 = vg_unset, ..., data_from = vg_unset, filter_by = vg_unset) {
  vg_mark_(spec, "ruleX", ariaDescription = ariaDescription, ariaHidden = ariaHidden, ariaLabel = ariaLabel, channels = channels, clip = clip, dx = dx, dy = dy, facet = facet, facetAnchor = facetAnchor, fill = fill, fillOpacity = fillOpacity, filter = filter, fx = fx, fy = fy, href = href, imageFilter = imageFilter, inset = inset, insetBottom = insetBottom, insetTop = insetTop, interval = interval, margin = margin, marginBottom = marginBottom, marginLeft = marginLeft, marginRight = marginRight, marginTop = marginTop, marker = marker, markerEnd = markerEnd, markerMid = markerMid, markerStart = markerStart, mixBlendMode = mixBlendMode, opacity = opacity, paintOrder = paintOrder, pointerEvents = pointerEvents, reverse = reverse, select = select, shapeRendering = shapeRendering, sort = sort, stroke = stroke, strokeDasharray = strokeDasharray, strokeDashoffset = strokeDashoffset, strokeLinecap = strokeLinecap, strokeLinejoin = strokeLinejoin, strokeMiterlimit = strokeMiterlimit, strokeOpacity = strokeOpacity, strokeWidth = strokeWidth, target = target, tip = tip, title = title, x = x, y = y, y1 = y1, y2 = y2, ..., data_from = data_from, filter_by = filter_by)
}

#' The ruleY mark.
#'
#' @param spec A plot fragment or `vgspec` to add this mark to, or `NULL` to start a new plot with just this mark.
#' @param ariaDescription The aria-description; a constant textual description.
#' @param ariaHidden The aria-hidden state; a constant indicating whether the element is exposed to an accessibility API.
#' @param ariaLabel The aria-label; a channel specifying short textual labels representing the value in the accessibility tree.
#' @param channels Additional named channels, for example to include in a tooltip.
#' @param clip How to clip the mark; one of: - *frame* or true - clip to the plot’s frame (inner area) - *sphere* - clip to the projected sphere (*e.g.*, front hemisphere) - null or false - do not clip The *sphere* clip option requires a geographic projection.
#' @param dx The horizontal offset in pixels; a constant option.
#' @param dy The vertical offset in pixels; a constant option.
#' @param facet Whether to enable or disable faceting; one of: - *auto* (default) - automatically determine if this mark should be faceted - *include* (or true) - draw the subset of the mark’s data in the current facet - *exclude* - draw the subset of the mark’s data *not* in the current facet - *super* - draw this mark in a single frame that covers all facets - null (or false) - repeat this mark’s data across all facets (*i.e.*, no faceting) When a mark uses *super* faceting, it is not allowed to use position scales (*x*, *y*, *fx*, or *fy*); *super* faceting is intended for decorations, such as labels and legends.
#' @param facetAnchor How to place the mark with respect to facets; one of: - null (default for most marks) - display the mark in each non-empty facet - *top*, *right*, *bottom*, or *left* - display the mark only in facets on the given side - *top-empty*, *right-empty*, *bottom-empty*, or *left-empty* (default for axis marks) - display the mark only in facets that have empty space on the given side: either the margin, or an empty facet - *empty* - display the mark in empty facets only
#' @param fill The fill; a constant CSS color string, or a channel typically bound to the *color* scale.
#' @param fillOpacity The fill-opacity; a constant number between 0 and 1, or a channel typically bound to the *opacity* scale.
#' @param filter Applies a transform to filter the mark’s index according to the given channel values; only truthy values are retained.
#' @param fx The horizontal facet position channel, for mark-level faceting, bound to the *fx* scale.
#' @param fy The vertical facet position channel, for mark-level faceting, bound to the *fy* scale.
#' @param href The href; a channel specifying URLs for clickable links.
#' @param imageFilter A CSS filter; a constant string used to adjust the rendering of images, such as *blur(5px)*.
#' @param inset Shorthand to set the same default for **insetStart** and **insetEnd**.
#' @param insetBottom Insets the bottom edge by the specified number of pixels.
#' @param insetTop Insets the top edge by the specified number of pixels.
#' @param interval Enforces uniformity for data at regular intervals, such as integer values or daily samples.
#' @param margin Shorthand to set the same default for all four mark margins: **marginTop**, **marginRight**, **marginBottom**, and **marginLeft**; typically defaults to 0, except for axis marks.
#' @param marginBottom The mark’s bottom margin; the minimum distance in pixels between the bottom edges of the inner and outer plot area.
#' @param marginLeft The mark’s left margin; the minimum distance in pixels between the left edges of the inner and outer plot area.
#' @param marginRight The mark’s right margin; the minimum distance in pixels between the right edges of the mark’s inner and outer plot area.
#' @param marginTop The mark’s top margin; the minimum distance in pixels between the top edges of the inner and outer plot area.
#' @param marker Shorthand to set the same default for markerStart, markerMid, and markerEnd; one of: - a marker name such as *arrow* or *circle* - *none* (default) - no marker * true - alias for *circle-fill* * false or null - alias for *none*
#' @param markerEnd The marker for the ending point of a line segment; one of: - a marker name such as *arrow* or *circle* * *none* (default) - no marker * true - alias for *circle-fill* * false or null - alias for *none*
#' @param markerMid The marker for any middle (interior) points of a line segment.
#' @param markerStart The marker for the starting point of a line segment; one of: - a marker name such as *arrow* or *circle* * *none* (default) - no marker * true - alias for *circle-fill* * false or null - alias for *none*
#' @param mixBlendMode The mix-blend-mode; a constant string specifying how to blend content such as *multiply*.
#' @param opacity The opacity; a constant between 0 and 1, or a channel typically bound to the *opacity* scale.
#' @param paintOrder The paint-order; a constant string specifying the order in which the **fill**, **stroke**, and any markers are drawn; defaults to *normal*, which draws the fill, then stroke, then markers; defaults to *stroke* for the text mark to create a “halo” around text to improve legibility.
#' @param pointerEvents The pointer-events property; a constant string such as *none*.
#' @param reverse Applies a transform to reverse the order of the mark’s index, say for reverse input order.
#' @param select Applies a filter transform after data is loaded to highlight selected values only.
#' @param shapeRendering The shape-rendering; a constant string such as *crispEdges*.
#' @param sort Either applies a transform to sort the mark’s index by the specified channel values, or imputes ordinal scale domains from this mark’s channels.
#' @param stroke The stroke; a constant CSS color string, or a channel typically bound to the *color* scale.
#' @param strokeDasharray The stroke-dasharray; a constant number indicating the length in pixels of alternating dashes and gaps, or a constant string of numbers separated by spaces or commas (_e.g._, *10 2* for dashes of 10 pixels separated by gaps of 2 pixels), or *none* (the default) for no dashing : https://developer.mozilla.org/en-US/docs/Web/SVG/Attribute/stroke-dasharray
#' @param strokeDashoffset The stroke-dashoffset; a constant indicating the offset in pixels of the first dash along the stroke; defaults to zero.
#' @param strokeLinecap The stroke-linecap; a constant specifying how to cap stroked paths, such as *butt*, *round*, or *square*.
#' @param strokeLinejoin The stroke-linejoin; a constant specifying how to join stroked paths, such as *bevel*, *miter*, *miter-clip*, or *round*.
#' @param strokeMiterlimit The stroke-miterlimit; a constant number specifying how to limit the length of *miter* joins on stroked paths.
#' @param strokeOpacity The stroke-opacity; a constant between 0 and 1, or a channel typically bound to the *opacity* scale.
#' @param strokeWidth The stroke-width; a constant number in pixels, or a channel.
#' @param target The target; a constant string specifying the target window (_e.g._, *_blank*) for clickable links; used in conjunction with the **href** option.
#' @param tip Whether to generate a tooltip for this mark, and any tip options.
#' @param title The title; a channel specifying accessible, short textual descriptions as strings (possibly with newlines).
#' @param x The horizontal position (or length) channel, typically bound to the *x* scale.
#' @param y The vertical position channel, typically bound to the *y* scale; defaults to the zero-based index of the data 0, 1, 2, ….
#' @param y1 The required primary (starting, often bottom) vertical position channel, representing the area’s baseline, typically bound to the *y* scale.
#' @param y2 The optional secondary (ending, often top) vertical position channel, representing the area’s topline, typically bound to the *y* scale; if not specified, **y1** is used.
#' @param ... Additional options or plot-level attributes.
#' @param data_from The name of the data source this mark reads from (see [vg_data()]).
#' @param filter_by A Param/Selection (e.g. from [param()]) to filter this mark's data by.
#' @family mark functions
#' @export
vg_rule_y <- function(spec = NULL, ariaDescription = vg_unset, ariaHidden = vg_unset, ariaLabel = vg_unset, channels = vg_unset, clip = vg_unset, dx = vg_unset, dy = vg_unset, facet = vg_unset, facetAnchor = vg_unset, fill = vg_unset, fillOpacity = vg_unset, filter = vg_unset, fx = vg_unset, fy = vg_unset, href = vg_unset, imageFilter = vg_unset, inset = vg_unset, insetBottom = vg_unset, insetTop = vg_unset, interval = vg_unset, margin = vg_unset, marginBottom = vg_unset, marginLeft = vg_unset, marginRight = vg_unset, marginTop = vg_unset, marker = vg_unset, markerEnd = vg_unset, markerMid = vg_unset, markerStart = vg_unset, mixBlendMode = vg_unset, opacity = vg_unset, paintOrder = vg_unset, pointerEvents = vg_unset, reverse = vg_unset, select = vg_unset, shapeRendering = vg_unset, sort = vg_unset, stroke = vg_unset, strokeDasharray = vg_unset, strokeDashoffset = vg_unset, strokeLinecap = vg_unset, strokeLinejoin = vg_unset, strokeMiterlimit = vg_unset, strokeOpacity = vg_unset, strokeWidth = vg_unset, target = vg_unset, tip = vg_unset, title = vg_unset, x = vg_unset, y = vg_unset, y1 = vg_unset, y2 = vg_unset, ..., data_from = vg_unset, filter_by = vg_unset) {
  vg_mark_(spec, "ruleY", ariaDescription = ariaDescription, ariaHidden = ariaHidden, ariaLabel = ariaLabel, channels = channels, clip = clip, dx = dx, dy = dy, facet = facet, facetAnchor = facetAnchor, fill = fill, fillOpacity = fillOpacity, filter = filter, fx = fx, fy = fy, href = href, imageFilter = imageFilter, inset = inset, insetBottom = insetBottom, insetTop = insetTop, interval = interval, margin = margin, marginBottom = marginBottom, marginLeft = marginLeft, marginRight = marginRight, marginTop = marginTop, marker = marker, markerEnd = markerEnd, markerMid = markerMid, markerStart = markerStart, mixBlendMode = mixBlendMode, opacity = opacity, paintOrder = paintOrder, pointerEvents = pointerEvents, reverse = reverse, select = select, shapeRendering = shapeRendering, sort = sort, stroke = stroke, strokeDasharray = strokeDasharray, strokeDashoffset = strokeDashoffset, strokeLinecap = strokeLinecap, strokeLinejoin = strokeLinejoin, strokeMiterlimit = strokeMiterlimit, strokeOpacity = strokeOpacity, strokeWidth = strokeWidth, target = target, tip = tip, title = title, x = x, y = y, y1 = y1, y2 = y2, ..., data_from = data_from, filter_by = filter_by)
}

#' The sphere mark.
#'
#' @param spec A plot fragment or `vgspec` to add this mark to, or `NULL` to start a new plot with just this mark.
#' @param ariaDescription The aria-description; a constant textual description.
#' @param ariaHidden The aria-hidden state; a constant indicating whether the element is exposed to an accessibility API.
#' @param ariaLabel The aria-label; a channel specifying short textual labels representing the value in the accessibility tree.
#' @param channels Additional named channels, for example to include in a tooltip.
#' @param clip How to clip the mark; one of: - *frame* or true - clip to the plot’s frame (inner area) - *sphere* - clip to the projected sphere (*e.g.*, front hemisphere) - null or false - do not clip The *sphere* clip option requires a geographic projection.
#' @param dx The horizontal offset in pixels; a constant option.
#' @param dy The vertical offset in pixels; a constant option.
#' @param facet Whether to enable or disable faceting; one of: - *auto* (default) - automatically determine if this mark should be faceted - *include* (or true) - draw the subset of the mark’s data in the current facet - *exclude* - draw the subset of the mark’s data *not* in the current facet - *super* - draw this mark in a single frame that covers all facets - null (or false) - repeat this mark’s data across all facets (*i.e.*, no faceting) When a mark uses *super* faceting, it is not allowed to use position scales (*x*, *y*, *fx*, or *fy*); *super* faceting is intended for decorations, such as labels and legends.
#' @param facetAnchor How to place the mark with respect to facets; one of: - null (default for most marks) - display the mark in each non-empty facet - *top*, *right*, *bottom*, or *left* - display the mark only in facets on the given side - *top-empty*, *right-empty*, *bottom-empty*, or *left-empty* (default for axis marks) - display the mark only in facets that have empty space on the given side: either the margin, or an empty facet - *empty* - display the mark in empty facets only
#' @param fill The fill; a constant CSS color string, or a channel typically bound to the *color* scale.
#' @param fillOpacity The fill-opacity; a constant number between 0 and 1, or a channel typically bound to the *opacity* scale.
#' @param filter Applies a transform to filter the mark’s index according to the given channel values; only truthy values are retained.
#' @param fx The horizontal facet position channel, for mark-level faceting, bound to the *fx* scale.
#' @param fy The vertical facet position channel, for mark-level faceting, bound to the *fy* scale.
#' @param href The href; a channel specifying URLs for clickable links.
#' @param imageFilter A CSS filter; a constant string used to adjust the rendering of images, such as *blur(5px)*.
#' @param margin Shorthand to set the same default for all four mark margins: **marginTop**, **marginRight**, **marginBottom**, and **marginLeft**; typically defaults to 0, except for axis marks.
#' @param marginBottom The mark’s bottom margin; the minimum distance in pixels between the bottom edges of the inner and outer plot area.
#' @param marginLeft The mark’s left margin; the minimum distance in pixels between the left edges of the inner and outer plot area.
#' @param marginRight The mark’s right margin; the minimum distance in pixels between the right edges of the mark’s inner and outer plot area.
#' @param marginTop The mark’s top margin; the minimum distance in pixels between the top edges of the inner and outer plot area.
#' @param mixBlendMode The mix-blend-mode; a constant string specifying how to blend content such as *multiply*.
#' @param opacity The opacity; a constant between 0 and 1, or a channel typically bound to the *opacity* scale.
#' @param paintOrder The paint-order; a constant string specifying the order in which the **fill**, **stroke**, and any markers are drawn; defaults to *normal*, which draws the fill, then stroke, then markers; defaults to *stroke* for the text mark to create a “halo” around text to improve legibility.
#' @param pointerEvents The pointer-events property; a constant string such as *none*.
#' @param reverse Applies a transform to reverse the order of the mark’s index, say for reverse input order.
#' @param select Applies a filter transform after data is loaded to highlight selected values only.
#' @param shapeRendering The shape-rendering; a constant string such as *crispEdges*.
#' @param sort Either applies a transform to sort the mark’s index by the specified channel values, or imputes ordinal scale domains from this mark’s channels.
#' @param stroke The stroke; a constant CSS color string, or a channel typically bound to the *color* scale.
#' @param strokeDasharray The stroke-dasharray; a constant number indicating the length in pixels of alternating dashes and gaps, or a constant string of numbers separated by spaces or commas (_e.g._, *10 2* for dashes of 10 pixels separated by gaps of 2 pixels), or *none* (the default) for no dashing : https://developer.mozilla.org/en-US/docs/Web/SVG/Attribute/stroke-dasharray
#' @param strokeDashoffset The stroke-dashoffset; a constant indicating the offset in pixels of the first dash along the stroke; defaults to zero.
#' @param strokeLinecap The stroke-linecap; a constant specifying how to cap stroked paths, such as *butt*, *round*, or *square*.
#' @param strokeLinejoin The stroke-linejoin; a constant specifying how to join stroked paths, such as *bevel*, *miter*, *miter-clip*, or *round*.
#' @param strokeMiterlimit The stroke-miterlimit; a constant number specifying how to limit the length of *miter* joins on stroked paths.
#' @param strokeOpacity The stroke-opacity; a constant between 0 and 1, or a channel typically bound to the *opacity* scale.
#' @param strokeWidth The stroke-width; a constant number in pixels, or a channel.
#' @param target The target; a constant string specifying the target window (_e.g._, *_blank*) for clickable links; used in conjunction with the **href** option.
#' @param tip Whether to generate a tooltip for this mark, and any tip options.
#' @param title The title; a channel specifying accessible, short textual descriptions as strings (possibly with newlines).
#' @param ... Additional options or plot-level attributes.
#' @param data_from The name of the data source this mark reads from (see [vg_data()]).
#' @param filter_by A Param/Selection (e.g. from [param()]) to filter this mark's data by.
#' @family mark functions
#' @export
vg_sphere <- function(spec = NULL, ariaDescription = vg_unset, ariaHidden = vg_unset, ariaLabel = vg_unset, channels = vg_unset, clip = vg_unset, dx = vg_unset, dy = vg_unset, facet = vg_unset, facetAnchor = vg_unset, fill = vg_unset, fillOpacity = vg_unset, filter = vg_unset, fx = vg_unset, fy = vg_unset, href = vg_unset, imageFilter = vg_unset, margin = vg_unset, marginBottom = vg_unset, marginLeft = vg_unset, marginRight = vg_unset, marginTop = vg_unset, mixBlendMode = vg_unset, opacity = vg_unset, paintOrder = vg_unset, pointerEvents = vg_unset, reverse = vg_unset, select = vg_unset, shapeRendering = vg_unset, sort = vg_unset, stroke = vg_unset, strokeDasharray = vg_unset, strokeDashoffset = vg_unset, strokeLinecap = vg_unset, strokeLinejoin = vg_unset, strokeMiterlimit = vg_unset, strokeOpacity = vg_unset, strokeWidth = vg_unset, target = vg_unset, tip = vg_unset, title = vg_unset, ..., data_from = vg_unset, filter_by = vg_unset) {
  vg_mark_(spec, "sphere", ariaDescription = ariaDescription, ariaHidden = ariaHidden, ariaLabel = ariaLabel, channels = channels, clip = clip, dx = dx, dy = dy, facet = facet, facetAnchor = facetAnchor, fill = fill, fillOpacity = fillOpacity, filter = filter, fx = fx, fy = fy, href = href, imageFilter = imageFilter, margin = margin, marginBottom = marginBottom, marginLeft = marginLeft, marginRight = marginRight, marginTop = marginTop, mixBlendMode = mixBlendMode, opacity = opacity, paintOrder = paintOrder, pointerEvents = pointerEvents, reverse = reverse, select = select, shapeRendering = shapeRendering, sort = sort, stroke = stroke, strokeDasharray = strokeDasharray, strokeDashoffset = strokeDashoffset, strokeLinecap = strokeLinecap, strokeLinejoin = strokeLinejoin, strokeMiterlimit = strokeMiterlimit, strokeOpacity = strokeOpacity, strokeWidth = strokeWidth, target = target, tip = tip, title = title, ..., data_from = data_from, filter_by = filter_by)
}

#' The spike mark.
#'
#' @param spec A plot fragment or `vgspec` to add this mark to, or `NULL` to start a new plot with just this mark.
#' @param anchor The side of the frame on which to place the axis: *top* or *bottom* for horizontal axes (axisX and axisFx) and their associated vertical grids (gridX and gridFx), or *left* or *right* for vertical axes (axisY and axisFY) and their associated horizontal grids (gridY and gridFy).
#' @param ariaDescription The aria-description; a constant textual description.
#' @param ariaHidden The aria-hidden state; a constant indicating whether the element is exposed to an accessibility API.
#' @param ariaLabel The aria-label; a channel specifying short textual labels representing the value in the accessibility tree.
#' @param channels Additional named channels, for example to include in a tooltip.
#' @param clip How to clip the mark; one of: - *frame* or true - clip to the plot’s frame (inner area) - *sphere* - clip to the projected sphere (*e.g.*, front hemisphere) - null or false - do not clip The *sphere* clip option requires a geographic projection.
#' @param dx The horizontal offset in pixels; a constant option.
#' @param dy The vertical offset in pixels; a constant option.
#' @param facet Whether to enable or disable faceting; one of: - *auto* (default) - automatically determine if this mark should be faceted - *include* (or true) - draw the subset of the mark’s data in the current facet - *exclude* - draw the subset of the mark’s data *not* in the current facet - *super* - draw this mark in a single frame that covers all facets - null (or false) - repeat this mark’s data across all facets (*i.e.*, no faceting) When a mark uses *super* faceting, it is not allowed to use position scales (*x*, *y*, *fx*, or *fy*); *super* faceting is intended for decorations, such as labels and legends.
#' @param facetAnchor How to place the mark with respect to facets; one of: - null (default for most marks) - display the mark in each non-empty facet - *top*, *right*, *bottom*, or *left* - display the mark only in facets on the given side - *top-empty*, *right-empty*, *bottom-empty*, or *left-empty* (default for axis marks) - display the mark only in facets that have empty space on the given side: either the margin, or an empty facet - *empty* - display the mark in empty facets only
#' @param fill The fill; a constant CSS color string, or a channel typically bound to the *color* scale.
#' @param fillOpacity The fill-opacity; a constant number between 0 and 1, or a channel typically bound to the *opacity* scale.
#' @param filter Applies a transform to filter the mark’s index according to the given channel values; only truthy values are retained.
#' @param frameAnchor The frame anchor specifies defaults for **x** and **y**, along with **textAnchor** and **lineAnchor**, based on the plot’s frame; it may be one of the four sides (*top*, *right*, *bottom*, *left*), one of the four corners (*top-left*, *top-right*, *bottom-right*, *bottom-left*), or the *middle* of the frame.
#' @param fx The horizontal facet position channel, for mark-level faceting, bound to the *fx* scale.
#' @param fy The vertical facet position channel, for mark-level faceting, bound to the *fy* scale.
#' @param href The href; a channel specifying URLs for clickable links.
#' @param imageFilter A CSS filter; a constant string used to adjust the rendering of images, such as *blur(5px)*.
#' @param length The vector’s length; either an optional channel bound to the *length* scale or a constant number in pixels.
#' @param margin Shorthand to set the same default for all four mark margins: **marginTop**, **marginRight**, **marginBottom**, and **marginLeft**; typically defaults to 0, except for axis marks.
#' @param marginBottom The mark’s bottom margin; the minimum distance in pixels between the bottom edges of the inner and outer plot area.
#' @param marginLeft The mark’s left margin; the minimum distance in pixels between the left edges of the inner and outer plot area.
#' @param marginRight The mark’s right margin; the minimum distance in pixels between the right edges of the mark’s inner and outer plot area.
#' @param marginTop The mark’s top margin; the minimum distance in pixels between the top edges of the inner and outer plot area.
#' @param mixBlendMode The mix-blend-mode; a constant string specifying how to blend content such as *multiply*.
#' @param opacity The opacity; a constant between 0 and 1, or a channel typically bound to the *opacity* scale.
#' @param paintOrder The paint-order; a constant string specifying the order in which the **fill**, **stroke**, and any markers are drawn; defaults to *normal*, which draws the fill, then stroke, then markers; defaults to *stroke* for the text mark to create a “halo” around text to improve legibility.
#' @param pointerEvents The pointer-events property; a constant string such as *none*.
#' @param r The radius of dots; either a channel or constant.
#' @param reverse Applies a transform to reverse the order of the mark’s index, say for reverse input order.
#' @param rotate The rotation angle in degrees clockwise; a constant or a channel; defaults to 0°.
#' @param select Applies a filter transform after data is loaded to highlight selected values only.
#' @param shape The shape of the vector; a constant.
#' @param shapeRendering The shape-rendering; a constant string such as *crispEdges*.
#' @param sort Either applies a transform to sort the mark’s index by the specified channel values, or imputes ordinal scale domains from this mark’s channels.
#' @param stroke The stroke; a constant CSS color string, or a channel typically bound to the *color* scale.
#' @param strokeDasharray The stroke-dasharray; a constant number indicating the length in pixels of alternating dashes and gaps, or a constant string of numbers separated by spaces or commas (_e.g._, *10 2* for dashes of 10 pixels separated by gaps of 2 pixels), or *none* (the default) for no dashing : https://developer.mozilla.org/en-US/docs/Web/SVG/Attribute/stroke-dasharray
#' @param strokeDashoffset The stroke-dashoffset; a constant indicating the offset in pixels of the first dash along the stroke; defaults to zero.
#' @param strokeLinecap The stroke-linecap; a constant specifying how to cap stroked paths, such as *butt*, *round*, or *square*.
#' @param strokeLinejoin The stroke-linejoin; a constant specifying how to join stroked paths, such as *bevel*, *miter*, *miter-clip*, or *round*.
#' @param strokeMiterlimit The stroke-miterlimit; a constant number specifying how to limit the length of *miter* joins on stroked paths.
#' @param strokeOpacity The stroke-opacity; a constant between 0 and 1, or a channel typically bound to the *opacity* scale.
#' @param strokeWidth The stroke-width; a constant number in pixels, or a channel.
#' @param target The target; a constant string specifying the target window (_e.g._, *_blank*) for clickable links; used in conjunction with the **href** option.
#' @param tip Whether to generate a tooltip for this mark, and any tip options.
#' @param title The title; a channel specifying accessible, short textual descriptions as strings (possibly with newlines).
#' @param x The horizontal position (or length) channel, typically bound to the *x* scale.
#' @param y The vertical position channel, typically bound to the *y* scale; defaults to the zero-based index of the data 0, 1, 2, ….
#' @param ... Additional options or plot-level attributes.
#' @param data_from The name of the data source this mark reads from (see [vg_data()]).
#' @param filter_by A Param/Selection (e.g. from [param()]) to filter this mark's data by.
#' @family mark functions
#' @export
vg_spike <- function(spec = NULL, anchor = vg_unset, ariaDescription = vg_unset, ariaHidden = vg_unset, ariaLabel = vg_unset, channels = vg_unset, clip = vg_unset, dx = vg_unset, dy = vg_unset, facet = vg_unset, facetAnchor = vg_unset, fill = vg_unset, fillOpacity = vg_unset, filter = vg_unset, frameAnchor = vg_unset, fx = vg_unset, fy = vg_unset, href = vg_unset, imageFilter = vg_unset, length = vg_unset, margin = vg_unset, marginBottom = vg_unset, marginLeft = vg_unset, marginRight = vg_unset, marginTop = vg_unset, mixBlendMode = vg_unset, opacity = vg_unset, paintOrder = vg_unset, pointerEvents = vg_unset, r = vg_unset, reverse = vg_unset, rotate = vg_unset, select = vg_unset, shape = vg_unset, shapeRendering = vg_unset, sort = vg_unset, stroke = vg_unset, strokeDasharray = vg_unset, strokeDashoffset = vg_unset, strokeLinecap = vg_unset, strokeLinejoin = vg_unset, strokeMiterlimit = vg_unset, strokeOpacity = vg_unset, strokeWidth = vg_unset, target = vg_unset, tip = vg_unset, title = vg_unset, x = vg_unset, y = vg_unset, ..., data_from = vg_unset, filter_by = vg_unset) {
  vg_mark_(spec, "spike", anchor = anchor, ariaDescription = ariaDescription, ariaHidden = ariaHidden, ariaLabel = ariaLabel, channels = channels, clip = clip, dx = dx, dy = dy, facet = facet, facetAnchor = facetAnchor, fill = fill, fillOpacity = fillOpacity, filter = filter, frameAnchor = frameAnchor, fx = fx, fy = fy, href = href, imageFilter = imageFilter, length = length, margin = margin, marginBottom = marginBottom, marginLeft = marginLeft, marginRight = marginRight, marginTop = marginTop, mixBlendMode = mixBlendMode, opacity = opacity, paintOrder = paintOrder, pointerEvents = pointerEvents, r = r, reverse = reverse, rotate = rotate, select = select, shape = shape, shapeRendering = shapeRendering, sort = sort, stroke = stroke, strokeDasharray = strokeDasharray, strokeDashoffset = strokeDashoffset, strokeLinecap = strokeLinecap, strokeLinejoin = strokeLinejoin, strokeMiterlimit = strokeMiterlimit, strokeOpacity = strokeOpacity, strokeWidth = strokeWidth, target = target, tip = tip, title = title, x = x, y = y, ..., data_from = data_from, filter_by = filter_by)
}

#' The text mark.
#'
#' @param spec A plot fragment or `vgspec` to add this mark to, or `NULL` to start a new plot with just this mark.
#' @param ariaDescription The aria-description; a constant textual description.
#' @param ariaHidden The aria-hidden state; a constant indicating whether the element is exposed to an accessibility API.
#' @param ariaLabel The aria-label; a channel specifying short textual labels representing the value in the accessibility tree.
#' @param channels Additional named channels, for example to include in a tooltip.
#' @param clip How to clip the mark; one of: - *frame* or true - clip to the plot’s frame (inner area) - *sphere* - clip to the projected sphere (*e.g.*, front hemisphere) - null or false - do not clip The *sphere* clip option requires a geographic projection.
#' @param dx The horizontal offset in pixels; a constant option.
#' @param dy The vertical offset in pixels; a constant option.
#' @param facet Whether to enable or disable faceting; one of: - *auto* (default) - automatically determine if this mark should be faceted - *include* (or true) - draw the subset of the mark’s data in the current facet - *exclude* - draw the subset of the mark’s data *not* in the current facet - *super* - draw this mark in a single frame that covers all facets - null (or false) - repeat this mark’s data across all facets (*i.e.*, no faceting) When a mark uses *super* faceting, it is not allowed to use position scales (*x*, *y*, *fx*, or *fy*); *super* faceting is intended for decorations, such as labels and legends.
#' @param facetAnchor How to place the mark with respect to facets; one of: - null (default for most marks) - display the mark in each non-empty facet - *top*, *right*, *bottom*, or *left* - display the mark only in facets on the given side - *top-empty*, *right-empty*, *bottom-empty*, or *left-empty* (default for axis marks) - display the mark only in facets that have empty space on the given side: either the margin, or an empty facet - *empty* - display the mark in empty facets only
#' @param fill The fill; a constant CSS color string, or a channel typically bound to the *color* scale.
#' @param fillOpacity The fill-opacity; a constant number between 0 and 1, or a channel typically bound to the *opacity* scale.
#' @param filter Applies a transform to filter the mark’s index according to the given channel values; only truthy values are retained.
#' @param fontFamily The font-family; a constant; defaults to the plot’s font family, which is typically *system-ui*.
#' @param fontSize The font size in pixels; either a constant or a channel; defaults to the plot’s font size, which is typically 10.
#' @param fontStyle The font style; a constant; defaults to the plot’s font style, which is typically *normal*.
#' @param fontVariant The font variant; a constant; if the **text** channel contains numbers or dates, defaults to *tabular-nums* to facilitate comparing numbers; otherwise defaults to the plot’s font style, which is typically *normal*.
#' @param fontWeight The font weight; a constant; defaults to the plot’s font weight, which is typically *normal*.
#' @param frameAnchor The frame anchor specifies defaults for **x** and **y**, along with **textAnchor** and **lineAnchor**, based on the plot’s frame; it may be one of the four sides (*top*, *right*, *bottom*, *left*), one of the four corners (*top-left*, *top-right*, *bottom-right*, *bottom-left*), or the *middle* of the frame.
#' @param fx The horizontal facet position channel, for mark-level faceting, bound to the *fx* scale.
#' @param fy The vertical facet position channel, for mark-level faceting, bound to the *fy* scale.
#' @param href The href; a channel specifying URLs for clickable links.
#' @param imageFilter A CSS filter; a constant string used to adjust the rendering of images, such as *blur(5px)*.
#' @param lineAnchor The line anchor controls how text is aligned (typically vertically) relative to its anchor point; it is one of *top*, *bottom*, or *middle*.
#' @param lineHeight The line height in ems; defaults to 1.
#' @param lineWidth The line width in ems (e.g., 10 for about 20 characters); defaults to infinity, disabling wrapping and clipping.
#' @param margin Shorthand to set the same default for all four mark margins: **marginTop**, **marginRight**, **marginBottom**, and **marginLeft**; typically defaults to 0, except for axis marks.
#' @param marginBottom The mark’s bottom margin; the minimum distance in pixels between the bottom edges of the inner and outer plot area.
#' @param marginLeft The mark’s left margin; the minimum distance in pixels between the left edges of the inner and outer plot area.
#' @param marginRight The mark’s right margin; the minimum distance in pixels between the right edges of the mark’s inner and outer plot area.
#' @param marginTop The mark’s top margin; the minimum distance in pixels between the top edges of the inner and outer plot area.
#' @param mixBlendMode The mix-blend-mode; a constant string specifying how to blend content such as *multiply*.
#' @param monospace If true, changes the default **fontFamily** to *monospace*, and uses simplified monospaced text metrics calculations.
#' @param opacity The opacity; a constant between 0 and 1, or a channel typically bound to the *opacity* scale.
#' @param paintOrder The paint-order; a constant string specifying the order in which the **fill**, **stroke**, and any markers are drawn; defaults to *normal*, which draws the fill, then stroke, then markers; defaults to *stroke* for the text mark to create a “halo” around text to improve legibility.
#' @param pointerEvents The pointer-events property; a constant string such as *none*.
#' @param reverse Applies a transform to reverse the order of the mark’s index, say for reverse input order.
#' @param rotate The rotation angle in degrees clockwise; a constant or a channel; defaults to 0°.
#' @param select Applies a filter transform after data is loaded to highlight selected values only.
#' @param shapeRendering The shape-rendering; a constant string such as *crispEdges*.
#' @param sort Either applies a transform to sort the mark’s index by the specified channel values, or imputes ordinal scale domains from this mark’s channels.
#' @param stroke The stroke; a constant CSS color string, or a channel typically bound to the *color* scale.
#' @param strokeDasharray The stroke-dasharray; a constant number indicating the length in pixels of alternating dashes and gaps, or a constant string of numbers separated by spaces or commas (_e.g._, *10 2* for dashes of 10 pixels separated by gaps of 2 pixels), or *none* (the default) for no dashing : https://developer.mozilla.org/en-US/docs/Web/SVG/Attribute/stroke-dasharray
#' @param strokeDashoffset The stroke-dashoffset; a constant indicating the offset in pixels of the first dash along the stroke; defaults to zero.
#' @param strokeLinecap The stroke-linecap; a constant specifying how to cap stroked paths, such as *butt*, *round*, or *square*.
#' @param strokeLinejoin The stroke-linejoin; a constant specifying how to join stroked paths, such as *bevel*, *miter*, *miter-clip*, or *round*.
#' @param strokeMiterlimit The stroke-miterlimit; a constant number specifying how to limit the length of *miter* joins on stroked paths.
#' @param strokeOpacity The stroke-opacity; a constant between 0 and 1, or a channel typically bound to the *opacity* scale.
#' @param strokeWidth The stroke-width; a constant number in pixels, or a channel.
#' @param target The target; a constant string specifying the target window (_e.g._, *_blank*) for clickable links; used in conjunction with the **href** option.
#' @param text The text contents channel, possibly with line breaks (\\n, \\r\\n, or \\r).
#' @param textAnchor The text anchor controls how text is aligned (typically horizontally) relative to its anchor point; it is one of *start*, *end*, or *middle*.
#' @param textOverflow How truncate (or wrap) lines of text longer than the given **lineWidth**; one of: - null (default) - preserve overflowing characters (and wrap if needed) - *clip* or *clip-end* - remove characters from the end - *clip-start* - remove characters from the start - *ellipsis* or *ellipsis-end* - replace characters from the end with an ellipsis (…) - *ellipsis-start* - replace characters from the start with an ellipsis (…) - *ellipsis-middle* - replace characters from the middle with an ellipsis (…) If no **title** was specified, if text requires truncation, a title containing the non-truncated text will be implicitly added.
#' @param tip Whether to generate a tooltip for this mark, and any tip options.
#' @param title The title; a channel specifying accessible, short textual descriptions as strings (possibly with newlines).
#' @param x The horizontal position (or length) channel, typically bound to the *x* scale.
#' @param y The vertical position channel, typically bound to the *y* scale; defaults to the zero-based index of the data 0, 1, 2, ….
#' @param z An optional ordinal channel for grouping data into (possibly stacked) series to be drawn as separate areas; defaults to **fill** if a channel, or **stroke** if a channel.
#' @param ... Additional options or plot-level attributes.
#' @param data_from The name of the data source this mark reads from (see [vg_data()]).
#' @param filter_by A Param/Selection (e.g. from [param()]) to filter this mark's data by.
#' @family mark functions
#' @export
vg_text <- function(spec = NULL, ariaDescription = vg_unset, ariaHidden = vg_unset, ariaLabel = vg_unset, channels = vg_unset, clip = vg_unset, dx = vg_unset, dy = vg_unset, facet = vg_unset, facetAnchor = vg_unset, fill = vg_unset, fillOpacity = vg_unset, filter = vg_unset, fontFamily = vg_unset, fontSize = vg_unset, fontStyle = vg_unset, fontVariant = vg_unset, fontWeight = vg_unset, frameAnchor = vg_unset, fx = vg_unset, fy = vg_unset, href = vg_unset, imageFilter = vg_unset, lineAnchor = vg_unset, lineHeight = vg_unset, lineWidth = vg_unset, margin = vg_unset, marginBottom = vg_unset, marginLeft = vg_unset, marginRight = vg_unset, marginTop = vg_unset, mixBlendMode = vg_unset, monospace = vg_unset, opacity = vg_unset, paintOrder = vg_unset, pointerEvents = vg_unset, reverse = vg_unset, rotate = vg_unset, select = vg_unset, shapeRendering = vg_unset, sort = vg_unset, stroke = vg_unset, strokeDasharray = vg_unset, strokeDashoffset = vg_unset, strokeLinecap = vg_unset, strokeLinejoin = vg_unset, strokeMiterlimit = vg_unset, strokeOpacity = vg_unset, strokeWidth = vg_unset, target = vg_unset, text = vg_unset, textAnchor = vg_unset, textOverflow = vg_unset, tip = vg_unset, title = vg_unset, x = vg_unset, y = vg_unset, z = vg_unset, ..., data_from = vg_unset, filter_by = vg_unset) {
  vg_mark_(spec, "text", ariaDescription = ariaDescription, ariaHidden = ariaHidden, ariaLabel = ariaLabel, channels = channels, clip = clip, dx = dx, dy = dy, facet = facet, facetAnchor = facetAnchor, fill = fill, fillOpacity = fillOpacity, filter = filter, fontFamily = fontFamily, fontSize = fontSize, fontStyle = fontStyle, fontVariant = fontVariant, fontWeight = fontWeight, frameAnchor = frameAnchor, fx = fx, fy = fy, href = href, imageFilter = imageFilter, lineAnchor = lineAnchor, lineHeight = lineHeight, lineWidth = lineWidth, margin = margin, marginBottom = marginBottom, marginLeft = marginLeft, marginRight = marginRight, marginTop = marginTop, mixBlendMode = mixBlendMode, monospace = monospace, opacity = opacity, paintOrder = paintOrder, pointerEvents = pointerEvents, reverse = reverse, rotate = rotate, select = select, shapeRendering = shapeRendering, sort = sort, stroke = stroke, strokeDasharray = strokeDasharray, strokeDashoffset = strokeDashoffset, strokeLinecap = strokeLinecap, strokeLinejoin = strokeLinejoin, strokeMiterlimit = strokeMiterlimit, strokeOpacity = strokeOpacity, strokeWidth = strokeWidth, target = target, text = text, textAnchor = textAnchor, textOverflow = textOverflow, tip = tip, title = title, x = x, y = y, z = z, ..., data_from = data_from, filter_by = filter_by)
}

#' The textX mark.
#'
#' @param spec A plot fragment or `vgspec` to add this mark to, or `NULL` to start a new plot with just this mark.
#' @param ariaDescription The aria-description; a constant textual description.
#' @param ariaHidden The aria-hidden state; a constant indicating whether the element is exposed to an accessibility API.
#' @param ariaLabel The aria-label; a channel specifying short textual labels representing the value in the accessibility tree.
#' @param channels Additional named channels, for example to include in a tooltip.
#' @param clip How to clip the mark; one of: - *frame* or true - clip to the plot’s frame (inner area) - *sphere* - clip to the projected sphere (*e.g.*, front hemisphere) - null or false - do not clip The *sphere* clip option requires a geographic projection.
#' @param dx The horizontal offset in pixels; a constant option.
#' @param dy The vertical offset in pixels; a constant option.
#' @param facet Whether to enable or disable faceting; one of: - *auto* (default) - automatically determine if this mark should be faceted - *include* (or true) - draw the subset of the mark’s data in the current facet - *exclude* - draw the subset of the mark’s data *not* in the current facet - *super* - draw this mark in a single frame that covers all facets - null (or false) - repeat this mark’s data across all facets (*i.e.*, no faceting) When a mark uses *super* faceting, it is not allowed to use position scales (*x*, *y*, *fx*, or *fy*); *super* faceting is intended for decorations, such as labels and legends.
#' @param facetAnchor How to place the mark with respect to facets; one of: - null (default for most marks) - display the mark in each non-empty facet - *top*, *right*, *bottom*, or *left* - display the mark only in facets on the given side - *top-empty*, *right-empty*, *bottom-empty*, or *left-empty* (default for axis marks) - display the mark only in facets that have empty space on the given side: either the margin, or an empty facet - *empty* - display the mark in empty facets only
#' @param fill The fill; a constant CSS color string, or a channel typically bound to the *color* scale.
#' @param fillOpacity The fill-opacity; a constant number between 0 and 1, or a channel typically bound to the *opacity* scale.
#' @param filter Applies a transform to filter the mark’s index according to the given channel values; only truthy values are retained.
#' @param fontFamily The font-family; a constant; defaults to the plot’s font family, which is typically *system-ui*.
#' @param fontSize The font size in pixels; either a constant or a channel; defaults to the plot’s font size, which is typically 10.
#' @param fontStyle The font style; a constant; defaults to the plot’s font style, which is typically *normal*.
#' @param fontVariant The font variant; a constant; if the **text** channel contains numbers or dates, defaults to *tabular-nums* to facilitate comparing numbers; otherwise defaults to the plot’s font style, which is typically *normal*.
#' @param fontWeight The font weight; a constant; defaults to the plot’s font weight, which is typically *normal*.
#' @param frameAnchor The frame anchor specifies defaults for **x** and **y**, along with **textAnchor** and **lineAnchor**, based on the plot’s frame; it may be one of the four sides (*top*, *right*, *bottom*, *left*), one of the four corners (*top-left*, *top-right*, *bottom-right*, *bottom-left*), or the *middle* of the frame.
#' @param fx The horizontal facet position channel, for mark-level faceting, bound to the *fx* scale.
#' @param fy The vertical facet position channel, for mark-level faceting, bound to the *fy* scale.
#' @param href The href; a channel specifying URLs for clickable links.
#' @param imageFilter A CSS filter; a constant string used to adjust the rendering of images, such as *blur(5px)*.
#' @param interval Enforces uniformity for data at regular intervals, such as integer values or daily samples.
#' @param lineAnchor The line anchor controls how text is aligned (typically vertically) relative to its anchor point; it is one of *top*, *bottom*, or *middle*.
#' @param lineHeight The line height in ems; defaults to 1.
#' @param lineWidth The line width in ems (e.g., 10 for about 20 characters); defaults to infinity, disabling wrapping and clipping.
#' @param margin Shorthand to set the same default for all four mark margins: **marginTop**, **marginRight**, **marginBottom**, and **marginLeft**; typically defaults to 0, except for axis marks.
#' @param marginBottom The mark’s bottom margin; the minimum distance in pixels between the bottom edges of the inner and outer plot area.
#' @param marginLeft The mark’s left margin; the minimum distance in pixels between the left edges of the inner and outer plot area.
#' @param marginRight The mark’s right margin; the minimum distance in pixels between the right edges of the mark’s inner and outer plot area.
#' @param marginTop The mark’s top margin; the minimum distance in pixels between the top edges of the inner and outer plot area.
#' @param mixBlendMode The mix-blend-mode; a constant string specifying how to blend content such as *multiply*.
#' @param monospace If true, changes the default **fontFamily** to *monospace*, and uses simplified monospaced text metrics calculations.
#' @param opacity The opacity; a constant between 0 and 1, or a channel typically bound to the *opacity* scale.
#' @param paintOrder The paint-order; a constant string specifying the order in which the **fill**, **stroke**, and any markers are drawn; defaults to *normal*, which draws the fill, then stroke, then markers; defaults to *stroke* for the text mark to create a “halo” around text to improve legibility.
#' @param pointerEvents The pointer-events property; a constant string such as *none*.
#' @param reverse Applies a transform to reverse the order of the mark’s index, say for reverse input order.
#' @param rotate The rotation angle in degrees clockwise; a constant or a channel; defaults to 0°.
#' @param select Applies a filter transform after data is loaded to highlight selected values only.
#' @param shapeRendering The shape-rendering; a constant string such as *crispEdges*.
#' @param sort Either applies a transform to sort the mark’s index by the specified channel values, or imputes ordinal scale domains from this mark’s channels.
#' @param stroke The stroke; a constant CSS color string, or a channel typically bound to the *color* scale.
#' @param strokeDasharray The stroke-dasharray; a constant number indicating the length in pixels of alternating dashes and gaps, or a constant string of numbers separated by spaces or commas (_e.g._, *10 2* for dashes of 10 pixels separated by gaps of 2 pixels), or *none* (the default) for no dashing : https://developer.mozilla.org/en-US/docs/Web/SVG/Attribute/stroke-dasharray
#' @param strokeDashoffset The stroke-dashoffset; a constant indicating the offset in pixels of the first dash along the stroke; defaults to zero.
#' @param strokeLinecap The stroke-linecap; a constant specifying how to cap stroked paths, such as *butt*, *round*, or *square*.
#' @param strokeLinejoin The stroke-linejoin; a constant specifying how to join stroked paths, such as *bevel*, *miter*, *miter-clip*, or *round*.
#' @param strokeMiterlimit The stroke-miterlimit; a constant number specifying how to limit the length of *miter* joins on stroked paths.
#' @param strokeOpacity The stroke-opacity; a constant between 0 and 1, or a channel typically bound to the *opacity* scale.
#' @param strokeWidth The stroke-width; a constant number in pixels, or a channel.
#' @param target The target; a constant string specifying the target window (_e.g._, *_blank*) for clickable links; used in conjunction with the **href** option.
#' @param text The text contents channel, possibly with line breaks (\\n, \\r\\n, or \\r).
#' @param textAnchor The text anchor controls how text is aligned (typically horizontally) relative to its anchor point; it is one of *start*, *end*, or *middle*.
#' @param textOverflow How truncate (or wrap) lines of text longer than the given **lineWidth**; one of: - null (default) - preserve overflowing characters (and wrap if needed) - *clip* or *clip-end* - remove characters from the end - *clip-start* - remove characters from the start - *ellipsis* or *ellipsis-end* - replace characters from the end with an ellipsis (…) - *ellipsis-start* - replace characters from the start with an ellipsis (…) - *ellipsis-middle* - replace characters from the middle with an ellipsis (…) If no **title** was specified, if text requires truncation, a title containing the non-truncated text will be implicitly added.
#' @param tip Whether to generate a tooltip for this mark, and any tip options.
#' @param title The title; a channel specifying accessible, short textual descriptions as strings (possibly with newlines).
#' @param x The horizontal position (or length) channel, typically bound to the *x* scale.
#' @param y The vertical position channel, typically bound to the *y* scale; defaults to the zero-based index of the data 0, 1, 2, ….
#' @param z An optional ordinal channel for grouping data into (possibly stacked) series to be drawn as separate areas; defaults to **fill** if a channel, or **stroke** if a channel.
#' @param ... Additional options or plot-level attributes.
#' @param data_from The name of the data source this mark reads from (see [vg_data()]).
#' @param filter_by A Param/Selection (e.g. from [param()]) to filter this mark's data by.
#' @family mark functions
#' @export
vg_text_x <- function(spec = NULL, ariaDescription = vg_unset, ariaHidden = vg_unset, ariaLabel = vg_unset, channels = vg_unset, clip = vg_unset, dx = vg_unset, dy = vg_unset, facet = vg_unset, facetAnchor = vg_unset, fill = vg_unset, fillOpacity = vg_unset, filter = vg_unset, fontFamily = vg_unset, fontSize = vg_unset, fontStyle = vg_unset, fontVariant = vg_unset, fontWeight = vg_unset, frameAnchor = vg_unset, fx = vg_unset, fy = vg_unset, href = vg_unset, imageFilter = vg_unset, interval = vg_unset, lineAnchor = vg_unset, lineHeight = vg_unset, lineWidth = vg_unset, margin = vg_unset, marginBottom = vg_unset, marginLeft = vg_unset, marginRight = vg_unset, marginTop = vg_unset, mixBlendMode = vg_unset, monospace = vg_unset, opacity = vg_unset, paintOrder = vg_unset, pointerEvents = vg_unset, reverse = vg_unset, rotate = vg_unset, select = vg_unset, shapeRendering = vg_unset, sort = vg_unset, stroke = vg_unset, strokeDasharray = vg_unset, strokeDashoffset = vg_unset, strokeLinecap = vg_unset, strokeLinejoin = vg_unset, strokeMiterlimit = vg_unset, strokeOpacity = vg_unset, strokeWidth = vg_unset, target = vg_unset, text = vg_unset, textAnchor = vg_unset, textOverflow = vg_unset, tip = vg_unset, title = vg_unset, x = vg_unset, y = vg_unset, z = vg_unset, ..., data_from = vg_unset, filter_by = vg_unset) {
  vg_mark_(spec, "textX", ariaDescription = ariaDescription, ariaHidden = ariaHidden, ariaLabel = ariaLabel, channels = channels, clip = clip, dx = dx, dy = dy, facet = facet, facetAnchor = facetAnchor, fill = fill, fillOpacity = fillOpacity, filter = filter, fontFamily = fontFamily, fontSize = fontSize, fontStyle = fontStyle, fontVariant = fontVariant, fontWeight = fontWeight, frameAnchor = frameAnchor, fx = fx, fy = fy, href = href, imageFilter = imageFilter, interval = interval, lineAnchor = lineAnchor, lineHeight = lineHeight, lineWidth = lineWidth, margin = margin, marginBottom = marginBottom, marginLeft = marginLeft, marginRight = marginRight, marginTop = marginTop, mixBlendMode = mixBlendMode, monospace = monospace, opacity = opacity, paintOrder = paintOrder, pointerEvents = pointerEvents, reverse = reverse, rotate = rotate, select = select, shapeRendering = shapeRendering, sort = sort, stroke = stroke, strokeDasharray = strokeDasharray, strokeDashoffset = strokeDashoffset, strokeLinecap = strokeLinecap, strokeLinejoin = strokeLinejoin, strokeMiterlimit = strokeMiterlimit, strokeOpacity = strokeOpacity, strokeWidth = strokeWidth, target = target, text = text, textAnchor = textAnchor, textOverflow = textOverflow, tip = tip, title = title, x = x, y = y, z = z, ..., data_from = data_from, filter_by = filter_by)
}

#' The textY mark.
#'
#' @param spec A plot fragment or `vgspec` to add this mark to, or `NULL` to start a new plot with just this mark.
#' @param ariaDescription The aria-description; a constant textual description.
#' @param ariaHidden The aria-hidden state; a constant indicating whether the element is exposed to an accessibility API.
#' @param ariaLabel The aria-label; a channel specifying short textual labels representing the value in the accessibility tree.
#' @param channels Additional named channels, for example to include in a tooltip.
#' @param clip How to clip the mark; one of: - *frame* or true - clip to the plot’s frame (inner area) - *sphere* - clip to the projected sphere (*e.g.*, front hemisphere) - null or false - do not clip The *sphere* clip option requires a geographic projection.
#' @param dx The horizontal offset in pixels; a constant option.
#' @param dy The vertical offset in pixels; a constant option.
#' @param facet Whether to enable or disable faceting; one of: - *auto* (default) - automatically determine if this mark should be faceted - *include* (or true) - draw the subset of the mark’s data in the current facet - *exclude* - draw the subset of the mark’s data *not* in the current facet - *super* - draw this mark in a single frame that covers all facets - null (or false) - repeat this mark’s data across all facets (*i.e.*, no faceting) When a mark uses *super* faceting, it is not allowed to use position scales (*x*, *y*, *fx*, or *fy*); *super* faceting is intended for decorations, such as labels and legends.
#' @param facetAnchor How to place the mark with respect to facets; one of: - null (default for most marks) - display the mark in each non-empty facet - *top*, *right*, *bottom*, or *left* - display the mark only in facets on the given side - *top-empty*, *right-empty*, *bottom-empty*, or *left-empty* (default for axis marks) - display the mark only in facets that have empty space on the given side: either the margin, or an empty facet - *empty* - display the mark in empty facets only
#' @param fill The fill; a constant CSS color string, or a channel typically bound to the *color* scale.
#' @param fillOpacity The fill-opacity; a constant number between 0 and 1, or a channel typically bound to the *opacity* scale.
#' @param filter Applies a transform to filter the mark’s index according to the given channel values; only truthy values are retained.
#' @param fontFamily The font-family; a constant; defaults to the plot’s font family, which is typically *system-ui*.
#' @param fontSize The font size in pixels; either a constant or a channel; defaults to the plot’s font size, which is typically 10.
#' @param fontStyle The font style; a constant; defaults to the plot’s font style, which is typically *normal*.
#' @param fontVariant The font variant; a constant; if the **text** channel contains numbers or dates, defaults to *tabular-nums* to facilitate comparing numbers; otherwise defaults to the plot’s font style, which is typically *normal*.
#' @param fontWeight The font weight; a constant; defaults to the plot’s font weight, which is typically *normal*.
#' @param frameAnchor The frame anchor specifies defaults for **x** and **y**, along with **textAnchor** and **lineAnchor**, based on the plot’s frame; it may be one of the four sides (*top*, *right*, *bottom*, *left*), one of the four corners (*top-left*, *top-right*, *bottom-right*, *bottom-left*), or the *middle* of the frame.
#' @param fx The horizontal facet position channel, for mark-level faceting, bound to the *fx* scale.
#' @param fy The vertical facet position channel, for mark-level faceting, bound to the *fy* scale.
#' @param href The href; a channel specifying URLs for clickable links.
#' @param imageFilter A CSS filter; a constant string used to adjust the rendering of images, such as *blur(5px)*.
#' @param interval Enforces uniformity for data at regular intervals, such as integer values or daily samples.
#' @param lineAnchor The line anchor controls how text is aligned (typically vertically) relative to its anchor point; it is one of *top*, *bottom*, or *middle*.
#' @param lineHeight The line height in ems; defaults to 1.
#' @param lineWidth The line width in ems (e.g., 10 for about 20 characters); defaults to infinity, disabling wrapping and clipping.
#' @param margin Shorthand to set the same default for all four mark margins: **marginTop**, **marginRight**, **marginBottom**, and **marginLeft**; typically defaults to 0, except for axis marks.
#' @param marginBottom The mark’s bottom margin; the minimum distance in pixels between the bottom edges of the inner and outer plot area.
#' @param marginLeft The mark’s left margin; the minimum distance in pixels between the left edges of the inner and outer plot area.
#' @param marginRight The mark’s right margin; the minimum distance in pixels between the right edges of the mark’s inner and outer plot area.
#' @param marginTop The mark’s top margin; the minimum distance in pixels between the top edges of the inner and outer plot area.
#' @param mixBlendMode The mix-blend-mode; a constant string specifying how to blend content such as *multiply*.
#' @param monospace If true, changes the default **fontFamily** to *monospace*, and uses simplified monospaced text metrics calculations.
#' @param opacity The opacity; a constant between 0 and 1, or a channel typically bound to the *opacity* scale.
#' @param paintOrder The paint-order; a constant string specifying the order in which the **fill**, **stroke**, and any markers are drawn; defaults to *normal*, which draws the fill, then stroke, then markers; defaults to *stroke* for the text mark to create a “halo” around text to improve legibility.
#' @param pointerEvents The pointer-events property; a constant string such as *none*.
#' @param reverse Applies a transform to reverse the order of the mark’s index, say for reverse input order.
#' @param rotate The rotation angle in degrees clockwise; a constant or a channel; defaults to 0°.
#' @param select Applies a filter transform after data is loaded to highlight selected values only.
#' @param shapeRendering The shape-rendering; a constant string such as *crispEdges*.
#' @param sort Either applies a transform to sort the mark’s index by the specified channel values, or imputes ordinal scale domains from this mark’s channels.
#' @param stroke The stroke; a constant CSS color string, or a channel typically bound to the *color* scale.
#' @param strokeDasharray The stroke-dasharray; a constant number indicating the length in pixels of alternating dashes and gaps, or a constant string of numbers separated by spaces or commas (_e.g._, *10 2* for dashes of 10 pixels separated by gaps of 2 pixels), or *none* (the default) for no dashing : https://developer.mozilla.org/en-US/docs/Web/SVG/Attribute/stroke-dasharray
#' @param strokeDashoffset The stroke-dashoffset; a constant indicating the offset in pixels of the first dash along the stroke; defaults to zero.
#' @param strokeLinecap The stroke-linecap; a constant specifying how to cap stroked paths, such as *butt*, *round*, or *square*.
#' @param strokeLinejoin The stroke-linejoin; a constant specifying how to join stroked paths, such as *bevel*, *miter*, *miter-clip*, or *round*.
#' @param strokeMiterlimit The stroke-miterlimit; a constant number specifying how to limit the length of *miter* joins on stroked paths.
#' @param strokeOpacity The stroke-opacity; a constant between 0 and 1, or a channel typically bound to the *opacity* scale.
#' @param strokeWidth The stroke-width; a constant number in pixels, or a channel.
#' @param target The target; a constant string specifying the target window (_e.g._, *_blank*) for clickable links; used in conjunction with the **href** option.
#' @param text The text contents channel, possibly with line breaks (\\n, \\r\\n, or \\r).
#' @param textAnchor The text anchor controls how text is aligned (typically horizontally) relative to its anchor point; it is one of *start*, *end*, or *middle*.
#' @param textOverflow How truncate (or wrap) lines of text longer than the given **lineWidth**; one of: - null (default) - preserve overflowing characters (and wrap if needed) - *clip* or *clip-end* - remove characters from the end - *clip-start* - remove characters from the start - *ellipsis* or *ellipsis-end* - replace characters from the end with an ellipsis (…) - *ellipsis-start* - replace characters from the start with an ellipsis (…) - *ellipsis-middle* - replace characters from the middle with an ellipsis (…) If no **title** was specified, if text requires truncation, a title containing the non-truncated text will be implicitly added.
#' @param tip Whether to generate a tooltip for this mark, and any tip options.
#' @param title The title; a channel specifying accessible, short textual descriptions as strings (possibly with newlines).
#' @param x The horizontal position (or length) channel, typically bound to the *x* scale.
#' @param y The vertical position channel, typically bound to the *y* scale; defaults to the zero-based index of the data 0, 1, 2, ….
#' @param z An optional ordinal channel for grouping data into (possibly stacked) series to be drawn as separate areas; defaults to **fill** if a channel, or **stroke** if a channel.
#' @param ... Additional options or plot-level attributes.
#' @param data_from The name of the data source this mark reads from (see [vg_data()]).
#' @param filter_by A Param/Selection (e.g. from [param()]) to filter this mark's data by.
#' @family mark functions
#' @export
vg_text_y <- function(spec = NULL, ariaDescription = vg_unset, ariaHidden = vg_unset, ariaLabel = vg_unset, channels = vg_unset, clip = vg_unset, dx = vg_unset, dy = vg_unset, facet = vg_unset, facetAnchor = vg_unset, fill = vg_unset, fillOpacity = vg_unset, filter = vg_unset, fontFamily = vg_unset, fontSize = vg_unset, fontStyle = vg_unset, fontVariant = vg_unset, fontWeight = vg_unset, frameAnchor = vg_unset, fx = vg_unset, fy = vg_unset, href = vg_unset, imageFilter = vg_unset, interval = vg_unset, lineAnchor = vg_unset, lineHeight = vg_unset, lineWidth = vg_unset, margin = vg_unset, marginBottom = vg_unset, marginLeft = vg_unset, marginRight = vg_unset, marginTop = vg_unset, mixBlendMode = vg_unset, monospace = vg_unset, opacity = vg_unset, paintOrder = vg_unset, pointerEvents = vg_unset, reverse = vg_unset, rotate = vg_unset, select = vg_unset, shapeRendering = vg_unset, sort = vg_unset, stroke = vg_unset, strokeDasharray = vg_unset, strokeDashoffset = vg_unset, strokeLinecap = vg_unset, strokeLinejoin = vg_unset, strokeMiterlimit = vg_unset, strokeOpacity = vg_unset, strokeWidth = vg_unset, target = vg_unset, text = vg_unset, textAnchor = vg_unset, textOverflow = vg_unset, tip = vg_unset, title = vg_unset, x = vg_unset, y = vg_unset, z = vg_unset, ..., data_from = vg_unset, filter_by = vg_unset) {
  vg_mark_(spec, "textY", ariaDescription = ariaDescription, ariaHidden = ariaHidden, ariaLabel = ariaLabel, channels = channels, clip = clip, dx = dx, dy = dy, facet = facet, facetAnchor = facetAnchor, fill = fill, fillOpacity = fillOpacity, filter = filter, fontFamily = fontFamily, fontSize = fontSize, fontStyle = fontStyle, fontVariant = fontVariant, fontWeight = fontWeight, frameAnchor = frameAnchor, fx = fx, fy = fy, href = href, imageFilter = imageFilter, interval = interval, lineAnchor = lineAnchor, lineHeight = lineHeight, lineWidth = lineWidth, margin = margin, marginBottom = marginBottom, marginLeft = marginLeft, marginRight = marginRight, marginTop = marginTop, mixBlendMode = mixBlendMode, monospace = monospace, opacity = opacity, paintOrder = paintOrder, pointerEvents = pointerEvents, reverse = reverse, rotate = rotate, select = select, shapeRendering = shapeRendering, sort = sort, stroke = stroke, strokeDasharray = strokeDasharray, strokeDashoffset = strokeDashoffset, strokeLinecap = strokeLinecap, strokeLinejoin = strokeLinejoin, strokeMiterlimit = strokeMiterlimit, strokeOpacity = strokeOpacity, strokeWidth = strokeWidth, target = target, text = text, textAnchor = textAnchor, textOverflow = textOverflow, tip = tip, title = title, x = x, y = y, z = z, ..., data_from = data_from, filter_by = filter_by)
}

#' The tickX mark.
#'
#' @param spec A plot fragment or `vgspec` to add this mark to, or `NULL` to start a new plot with just this mark.
#' @param ariaDescription The aria-description; a constant textual description.
#' @param ariaHidden The aria-hidden state; a constant indicating whether the element is exposed to an accessibility API.
#' @param ariaLabel The aria-label; a channel specifying short textual labels representing the value in the accessibility tree.
#' @param channels Additional named channels, for example to include in a tooltip.
#' @param clip How to clip the mark; one of: - *frame* or true - clip to the plot’s frame (inner area) - *sphere* - clip to the projected sphere (*e.g.*, front hemisphere) - null or false - do not clip The *sphere* clip option requires a geographic projection.
#' @param dx The horizontal offset in pixels; a constant option.
#' @param dy The vertical offset in pixels; a constant option.
#' @param facet Whether to enable or disable faceting; one of: - *auto* (default) - automatically determine if this mark should be faceted - *include* (or true) - draw the subset of the mark’s data in the current facet - *exclude* - draw the subset of the mark’s data *not* in the current facet - *super* - draw this mark in a single frame that covers all facets - null (or false) - repeat this mark’s data across all facets (*i.e.*, no faceting) When a mark uses *super* faceting, it is not allowed to use position scales (*x*, *y*, *fx*, or *fy*); *super* faceting is intended for decorations, such as labels and legends.
#' @param facetAnchor How to place the mark with respect to facets; one of: - null (default for most marks) - display the mark in each non-empty facet - *top*, *right*, *bottom*, or *left* - display the mark only in facets on the given side - *top-empty*, *right-empty*, *bottom-empty*, or *left-empty* (default for axis marks) - display the mark only in facets that have empty space on the given side: either the margin, or an empty facet - *empty* - display the mark in empty facets only
#' @param fill The fill; a constant CSS color string, or a channel typically bound to the *color* scale.
#' @param fillOpacity The fill-opacity; a constant number between 0 and 1, or a channel typically bound to the *opacity* scale.
#' @param filter Applies a transform to filter the mark’s index according to the given channel values; only truthy values are retained.
#' @param fx The horizontal facet position channel, for mark-level faceting, bound to the *fx* scale.
#' @param fy The vertical facet position channel, for mark-level faceting, bound to the *fy* scale.
#' @param href The href; a channel specifying URLs for clickable links.
#' @param imageFilter A CSS filter; a constant string used to adjust the rendering of images, such as *blur(5px)*.
#' @param inset Shorthand to set the same default for **insetStart** and **insetEnd**.
#' @param insetBottom Insets the bottom edge by the specified number of pixels.
#' @param insetTop Insets the top edge by the specified number of pixels.
#' @param margin Shorthand to set the same default for all four mark margins: **marginTop**, **marginRight**, **marginBottom**, and **marginLeft**; typically defaults to 0, except for axis marks.
#' @param marginBottom The mark’s bottom margin; the minimum distance in pixels between the bottom edges of the inner and outer plot area.
#' @param marginLeft The mark’s left margin; the minimum distance in pixels between the left edges of the inner and outer plot area.
#' @param marginRight The mark’s right margin; the minimum distance in pixels between the right edges of the mark’s inner and outer plot area.
#' @param marginTop The mark’s top margin; the minimum distance in pixels between the top edges of the inner and outer plot area.
#' @param marker Shorthand to set the same default for markerStart, markerMid, and markerEnd; one of: - a marker name such as *arrow* or *circle* - *none* (default) - no marker * true - alias for *circle-fill* * false or null - alias for *none*
#' @param markerEnd The marker for the ending point of a line segment; one of: - a marker name such as *arrow* or *circle* * *none* (default) - no marker * true - alias for *circle-fill* * false or null - alias for *none*
#' @param markerMid The marker for any middle (interior) points of a line segment.
#' @param markerStart The marker for the starting point of a line segment; one of: - a marker name such as *arrow* or *circle* * *none* (default) - no marker * true - alias for *circle-fill* * false or null - alias for *none*
#' @param mixBlendMode The mix-blend-mode; a constant string specifying how to blend content such as *multiply*.
#' @param opacity The opacity; a constant between 0 and 1, or a channel typically bound to the *opacity* scale.
#' @param paintOrder The paint-order; a constant string specifying the order in which the **fill**, **stroke**, and any markers are drawn; defaults to *normal*, which draws the fill, then stroke, then markers; defaults to *stroke* for the text mark to create a “halo” around text to improve legibility.
#' @param pointerEvents The pointer-events property; a constant string such as *none*.
#' @param reverse Applies a transform to reverse the order of the mark’s index, say for reverse input order.
#' @param select Applies a filter transform after data is loaded to highlight selected values only.
#' @param shapeRendering The shape-rendering; a constant string such as *crispEdges*.
#' @param sort Either applies a transform to sort the mark’s index by the specified channel values, or imputes ordinal scale domains from this mark’s channels.
#' @param stroke The stroke; a constant CSS color string, or a channel typically bound to the *color* scale.
#' @param strokeDasharray The stroke-dasharray; a constant number indicating the length in pixels of alternating dashes and gaps, or a constant string of numbers separated by spaces or commas (_e.g._, *10 2* for dashes of 10 pixels separated by gaps of 2 pixels), or *none* (the default) for no dashing : https://developer.mozilla.org/en-US/docs/Web/SVG/Attribute/stroke-dasharray
#' @param strokeDashoffset The stroke-dashoffset; a constant indicating the offset in pixels of the first dash along the stroke; defaults to zero.
#' @param strokeLinecap The stroke-linecap; a constant specifying how to cap stroked paths, such as *butt*, *round*, or *square*.
#' @param strokeLinejoin The stroke-linejoin; a constant specifying how to join stroked paths, such as *bevel*, *miter*, *miter-clip*, or *round*.
#' @param strokeMiterlimit The stroke-miterlimit; a constant number specifying how to limit the length of *miter* joins on stroked paths.
#' @param strokeOpacity The stroke-opacity; a constant between 0 and 1, or a channel typically bound to the *opacity* scale.
#' @param strokeWidth The stroke-width; a constant number in pixels, or a channel.
#' @param target The target; a constant string specifying the target window (_e.g._, *_blank*) for clickable links; used in conjunction with the **href** option.
#' @param tip Whether to generate a tooltip for this mark, and any tip options.
#' @param title The title; a channel specifying accessible, short textual descriptions as strings (possibly with newlines).
#' @param x The horizontal position (or length) channel, typically bound to the *x* scale.
#' @param y The vertical position channel, typically bound to the *y* scale; defaults to the zero-based index of the data 0, 1, 2, ….
#' @param ... Additional options or plot-level attributes.
#' @param data_from The name of the data source this mark reads from (see [vg_data()]).
#' @param filter_by A Param/Selection (e.g. from [param()]) to filter this mark's data by.
#' @family mark functions
#' @export
vg_tick_x <- function(spec = NULL, ariaDescription = vg_unset, ariaHidden = vg_unset, ariaLabel = vg_unset, channels = vg_unset, clip = vg_unset, dx = vg_unset, dy = vg_unset, facet = vg_unset, facetAnchor = vg_unset, fill = vg_unset, fillOpacity = vg_unset, filter = vg_unset, fx = vg_unset, fy = vg_unset, href = vg_unset, imageFilter = vg_unset, inset = vg_unset, insetBottom = vg_unset, insetTop = vg_unset, margin = vg_unset, marginBottom = vg_unset, marginLeft = vg_unset, marginRight = vg_unset, marginTop = vg_unset, marker = vg_unset, markerEnd = vg_unset, markerMid = vg_unset, markerStart = vg_unset, mixBlendMode = vg_unset, opacity = vg_unset, paintOrder = vg_unset, pointerEvents = vg_unset, reverse = vg_unset, select = vg_unset, shapeRendering = vg_unset, sort = vg_unset, stroke = vg_unset, strokeDasharray = vg_unset, strokeDashoffset = vg_unset, strokeLinecap = vg_unset, strokeLinejoin = vg_unset, strokeMiterlimit = vg_unset, strokeOpacity = vg_unset, strokeWidth = vg_unset, target = vg_unset, tip = vg_unset, title = vg_unset, x = vg_unset, y = vg_unset, ..., data_from = vg_unset, filter_by = vg_unset) {
  vg_mark_(spec, "tickX", ariaDescription = ariaDescription, ariaHidden = ariaHidden, ariaLabel = ariaLabel, channels = channels, clip = clip, dx = dx, dy = dy, facet = facet, facetAnchor = facetAnchor, fill = fill, fillOpacity = fillOpacity, filter = filter, fx = fx, fy = fy, href = href, imageFilter = imageFilter, inset = inset, insetBottom = insetBottom, insetTop = insetTop, margin = margin, marginBottom = marginBottom, marginLeft = marginLeft, marginRight = marginRight, marginTop = marginTop, marker = marker, markerEnd = markerEnd, markerMid = markerMid, markerStart = markerStart, mixBlendMode = mixBlendMode, opacity = opacity, paintOrder = paintOrder, pointerEvents = pointerEvents, reverse = reverse, select = select, shapeRendering = shapeRendering, sort = sort, stroke = stroke, strokeDasharray = strokeDasharray, strokeDashoffset = strokeDashoffset, strokeLinecap = strokeLinecap, strokeLinejoin = strokeLinejoin, strokeMiterlimit = strokeMiterlimit, strokeOpacity = strokeOpacity, strokeWidth = strokeWidth, target = target, tip = tip, title = title, x = x, y = y, ..., data_from = data_from, filter_by = filter_by)
}

#' The tickY mark.
#'
#' @param spec A plot fragment or `vgspec` to add this mark to, or `NULL` to start a new plot with just this mark.
#' @param ariaDescription The aria-description; a constant textual description.
#' @param ariaHidden The aria-hidden state; a constant indicating whether the element is exposed to an accessibility API.
#' @param ariaLabel The aria-label; a channel specifying short textual labels representing the value in the accessibility tree.
#' @param channels Additional named channels, for example to include in a tooltip.
#' @param clip How to clip the mark; one of: - *frame* or true - clip to the plot’s frame (inner area) - *sphere* - clip to the projected sphere (*e.g.*, front hemisphere) - null or false - do not clip The *sphere* clip option requires a geographic projection.
#' @param dx The horizontal offset in pixels; a constant option.
#' @param dy The vertical offset in pixels; a constant option.
#' @param facet Whether to enable or disable faceting; one of: - *auto* (default) - automatically determine if this mark should be faceted - *include* (or true) - draw the subset of the mark’s data in the current facet - *exclude* - draw the subset of the mark’s data *not* in the current facet - *super* - draw this mark in a single frame that covers all facets - null (or false) - repeat this mark’s data across all facets (*i.e.*, no faceting) When a mark uses *super* faceting, it is not allowed to use position scales (*x*, *y*, *fx*, or *fy*); *super* faceting is intended for decorations, such as labels and legends.
#' @param facetAnchor How to place the mark with respect to facets; one of: - null (default for most marks) - display the mark in each non-empty facet - *top*, *right*, *bottom*, or *left* - display the mark only in facets on the given side - *top-empty*, *right-empty*, *bottom-empty*, or *left-empty* (default for axis marks) - display the mark only in facets that have empty space on the given side: either the margin, or an empty facet - *empty* - display the mark in empty facets only
#' @param fill The fill; a constant CSS color string, or a channel typically bound to the *color* scale.
#' @param fillOpacity The fill-opacity; a constant number between 0 and 1, or a channel typically bound to the *opacity* scale.
#' @param filter Applies a transform to filter the mark’s index according to the given channel values; only truthy values are retained.
#' @param fx The horizontal facet position channel, for mark-level faceting, bound to the *fx* scale.
#' @param fy The vertical facet position channel, for mark-level faceting, bound to the *fy* scale.
#' @param href The href; a channel specifying URLs for clickable links.
#' @param imageFilter A CSS filter; a constant string used to adjust the rendering of images, such as *blur(5px)*.
#' @param inset Shorthand to set the same default for **insetStart** and **insetEnd**.
#' @param insetLeft Insets the left edge by the specified number of pixels.
#' @param insetRight Insets the right edge by the specified number of pixels.
#' @param margin Shorthand to set the same default for all four mark margins: **marginTop**, **marginRight**, **marginBottom**, and **marginLeft**; typically defaults to 0, except for axis marks.
#' @param marginBottom The mark’s bottom margin; the minimum distance in pixels between the bottom edges of the inner and outer plot area.
#' @param marginLeft The mark’s left margin; the minimum distance in pixels between the left edges of the inner and outer plot area.
#' @param marginRight The mark’s right margin; the minimum distance in pixels between the right edges of the mark’s inner and outer plot area.
#' @param marginTop The mark’s top margin; the minimum distance in pixels between the top edges of the inner and outer plot area.
#' @param marker Shorthand to set the same default for markerStart, markerMid, and markerEnd; one of: - a marker name such as *arrow* or *circle* - *none* (default) - no marker * true - alias for *circle-fill* * false or null - alias for *none*
#' @param markerEnd The marker for the ending point of a line segment; one of: - a marker name such as *arrow* or *circle* * *none* (default) - no marker * true - alias for *circle-fill* * false or null - alias for *none*
#' @param markerMid The marker for any middle (interior) points of a line segment.
#' @param markerStart The marker for the starting point of a line segment; one of: - a marker name such as *arrow* or *circle* * *none* (default) - no marker * true - alias for *circle-fill* * false or null - alias for *none*
#' @param mixBlendMode The mix-blend-mode; a constant string specifying how to blend content such as *multiply*.
#' @param opacity The opacity; a constant between 0 and 1, or a channel typically bound to the *opacity* scale.
#' @param paintOrder The paint-order; a constant string specifying the order in which the **fill**, **stroke**, and any markers are drawn; defaults to *normal*, which draws the fill, then stroke, then markers; defaults to *stroke* for the text mark to create a “halo” around text to improve legibility.
#' @param pointerEvents The pointer-events property; a constant string such as *none*.
#' @param reverse Applies a transform to reverse the order of the mark’s index, say for reverse input order.
#' @param select Applies a filter transform after data is loaded to highlight selected values only.
#' @param shapeRendering The shape-rendering; a constant string such as *crispEdges*.
#' @param sort Either applies a transform to sort the mark’s index by the specified channel values, or imputes ordinal scale domains from this mark’s channels.
#' @param stroke The stroke; a constant CSS color string, or a channel typically bound to the *color* scale.
#' @param strokeDasharray The stroke-dasharray; a constant number indicating the length in pixels of alternating dashes and gaps, or a constant string of numbers separated by spaces or commas (_e.g._, *10 2* for dashes of 10 pixels separated by gaps of 2 pixels), or *none* (the default) for no dashing : https://developer.mozilla.org/en-US/docs/Web/SVG/Attribute/stroke-dasharray
#' @param strokeDashoffset The stroke-dashoffset; a constant indicating the offset in pixels of the first dash along the stroke; defaults to zero.
#' @param strokeLinecap The stroke-linecap; a constant specifying how to cap stroked paths, such as *butt*, *round*, or *square*.
#' @param strokeLinejoin The stroke-linejoin; a constant specifying how to join stroked paths, such as *bevel*, *miter*, *miter-clip*, or *round*.
#' @param strokeMiterlimit The stroke-miterlimit; a constant number specifying how to limit the length of *miter* joins on stroked paths.
#' @param strokeOpacity The stroke-opacity; a constant between 0 and 1, or a channel typically bound to the *opacity* scale.
#' @param strokeWidth The stroke-width; a constant number in pixels, or a channel.
#' @param target The target; a constant string specifying the target window (_e.g._, *_blank*) for clickable links; used in conjunction with the **href** option.
#' @param tip Whether to generate a tooltip for this mark, and any tip options.
#' @param title The title; a channel specifying accessible, short textual descriptions as strings (possibly with newlines).
#' @param x The horizontal position (or length) channel, typically bound to the *x* scale.
#' @param y The vertical position channel, typically bound to the *y* scale; defaults to the zero-based index of the data 0, 1, 2, ….
#' @param ... Additional options or plot-level attributes.
#' @param data_from The name of the data source this mark reads from (see [vg_data()]).
#' @param filter_by A Param/Selection (e.g. from [param()]) to filter this mark's data by.
#' @family mark functions
#' @export
vg_tick_y <- function(spec = NULL, ariaDescription = vg_unset, ariaHidden = vg_unset, ariaLabel = vg_unset, channels = vg_unset, clip = vg_unset, dx = vg_unset, dy = vg_unset, facet = vg_unset, facetAnchor = vg_unset, fill = vg_unset, fillOpacity = vg_unset, filter = vg_unset, fx = vg_unset, fy = vg_unset, href = vg_unset, imageFilter = vg_unset, inset = vg_unset, insetLeft = vg_unset, insetRight = vg_unset, margin = vg_unset, marginBottom = vg_unset, marginLeft = vg_unset, marginRight = vg_unset, marginTop = vg_unset, marker = vg_unset, markerEnd = vg_unset, markerMid = vg_unset, markerStart = vg_unset, mixBlendMode = vg_unset, opacity = vg_unset, paintOrder = vg_unset, pointerEvents = vg_unset, reverse = vg_unset, select = vg_unset, shapeRendering = vg_unset, sort = vg_unset, stroke = vg_unset, strokeDasharray = vg_unset, strokeDashoffset = vg_unset, strokeLinecap = vg_unset, strokeLinejoin = vg_unset, strokeMiterlimit = vg_unset, strokeOpacity = vg_unset, strokeWidth = vg_unset, target = vg_unset, tip = vg_unset, title = vg_unset, x = vg_unset, y = vg_unset, ..., data_from = vg_unset, filter_by = vg_unset) {
  vg_mark_(spec, "tickY", ariaDescription = ariaDescription, ariaHidden = ariaHidden, ariaLabel = ariaLabel, channels = channels, clip = clip, dx = dx, dy = dy, facet = facet, facetAnchor = facetAnchor, fill = fill, fillOpacity = fillOpacity, filter = filter, fx = fx, fy = fy, href = href, imageFilter = imageFilter, inset = inset, insetLeft = insetLeft, insetRight = insetRight, margin = margin, marginBottom = marginBottom, marginLeft = marginLeft, marginRight = marginRight, marginTop = marginTop, marker = marker, markerEnd = markerEnd, markerMid = markerMid, markerStart = markerStart, mixBlendMode = mixBlendMode, opacity = opacity, paintOrder = paintOrder, pointerEvents = pointerEvents, reverse = reverse, select = select, shapeRendering = shapeRendering, sort = sort, stroke = stroke, strokeDasharray = strokeDasharray, strokeDashoffset = strokeDashoffset, strokeLinecap = strokeLinecap, strokeLinejoin = strokeLinejoin, strokeMiterlimit = strokeMiterlimit, strokeOpacity = strokeOpacity, strokeWidth = strokeWidth, target = target, tip = tip, title = title, x = x, y = y, ..., data_from = data_from, filter_by = filter_by)
}

#' The vector mark.
#'
#' @param spec A plot fragment or `vgspec` to add this mark to, or `NULL` to start a new plot with just this mark.
#' @param anchor The side of the frame on which to place the axis: *top* or *bottom* for horizontal axes (axisX and axisFx) and their associated vertical grids (gridX and gridFx), or *left* or *right* for vertical axes (axisY and axisFY) and their associated horizontal grids (gridY and gridFy).
#' @param ariaDescription The aria-description; a constant textual description.
#' @param ariaHidden The aria-hidden state; a constant indicating whether the element is exposed to an accessibility API.
#' @param ariaLabel The aria-label; a channel specifying short textual labels representing the value in the accessibility tree.
#' @param channels Additional named channels, for example to include in a tooltip.
#' @param clip How to clip the mark; one of: - *frame* or true - clip to the plot’s frame (inner area) - *sphere* - clip to the projected sphere (*e.g.*, front hemisphere) - null or false - do not clip The *sphere* clip option requires a geographic projection.
#' @param dx The horizontal offset in pixels; a constant option.
#' @param dy The vertical offset in pixels; a constant option.
#' @param facet Whether to enable or disable faceting; one of: - *auto* (default) - automatically determine if this mark should be faceted - *include* (or true) - draw the subset of the mark’s data in the current facet - *exclude* - draw the subset of the mark’s data *not* in the current facet - *super* - draw this mark in a single frame that covers all facets - null (or false) - repeat this mark’s data across all facets (*i.e.*, no faceting) When a mark uses *super* faceting, it is not allowed to use position scales (*x*, *y*, *fx*, or *fy*); *super* faceting is intended for decorations, such as labels and legends.
#' @param facetAnchor How to place the mark with respect to facets; one of: - null (default for most marks) - display the mark in each non-empty facet - *top*, *right*, *bottom*, or *left* - display the mark only in facets on the given side - *top-empty*, *right-empty*, *bottom-empty*, or *left-empty* (default for axis marks) - display the mark only in facets that have empty space on the given side: either the margin, or an empty facet - *empty* - display the mark in empty facets only
#' @param fill The fill; a constant CSS color string, or a channel typically bound to the *color* scale.
#' @param fillOpacity The fill-opacity; a constant number between 0 and 1, or a channel typically bound to the *opacity* scale.
#' @param filter Applies a transform to filter the mark’s index according to the given channel values; only truthy values are retained.
#' @param frameAnchor The frame anchor specifies defaults for **x** and **y**, along with **textAnchor** and **lineAnchor**, based on the plot’s frame; it may be one of the four sides (*top*, *right*, *bottom*, *left*), one of the four corners (*top-left*, *top-right*, *bottom-right*, *bottom-left*), or the *middle* of the frame.
#' @param fx The horizontal facet position channel, for mark-level faceting, bound to the *fx* scale.
#' @param fy The vertical facet position channel, for mark-level faceting, bound to the *fy* scale.
#' @param href The href; a channel specifying URLs for clickable links.
#' @param imageFilter A CSS filter; a constant string used to adjust the rendering of images, such as *blur(5px)*.
#' @param length The vector’s length; either an optional channel bound to the *length* scale or a constant number in pixels.
#' @param margin Shorthand to set the same default for all four mark margins: **marginTop**, **marginRight**, **marginBottom**, and **marginLeft**; typically defaults to 0, except for axis marks.
#' @param marginBottom The mark’s bottom margin; the minimum distance in pixels between the bottom edges of the inner and outer plot area.
#' @param marginLeft The mark’s left margin; the minimum distance in pixels between the left edges of the inner and outer plot area.
#' @param marginRight The mark’s right margin; the minimum distance in pixels between the right edges of the mark’s inner and outer plot area.
#' @param marginTop The mark’s top margin; the minimum distance in pixels between the top edges of the inner and outer plot area.
#' @param mixBlendMode The mix-blend-mode; a constant string specifying how to blend content such as *multiply*.
#' @param opacity The opacity; a constant between 0 and 1, or a channel typically bound to the *opacity* scale.
#' @param paintOrder The paint-order; a constant string specifying the order in which the **fill**, **stroke**, and any markers are drawn; defaults to *normal*, which draws the fill, then stroke, then markers; defaults to *stroke* for the text mark to create a “halo” around text to improve legibility.
#' @param pointerEvents The pointer-events property; a constant string such as *none*.
#' @param r The radius of dots; either a channel or constant.
#' @param reverse Applies a transform to reverse the order of the mark’s index, say for reverse input order.
#' @param rotate The rotation angle in degrees clockwise; a constant or a channel; defaults to 0°.
#' @param select Applies a filter transform after data is loaded to highlight selected values only.
#' @param shape The shape of the vector; a constant.
#' @param shapeRendering The shape-rendering; a constant string such as *crispEdges*.
#' @param sort Either applies a transform to sort the mark’s index by the specified channel values, or imputes ordinal scale domains from this mark’s channels.
#' @param stroke The stroke; a constant CSS color string, or a channel typically bound to the *color* scale.
#' @param strokeDasharray The stroke-dasharray; a constant number indicating the length in pixels of alternating dashes and gaps, or a constant string of numbers separated by spaces or commas (_e.g._, *10 2* for dashes of 10 pixels separated by gaps of 2 pixels), or *none* (the default) for no dashing : https://developer.mozilla.org/en-US/docs/Web/SVG/Attribute/stroke-dasharray
#' @param strokeDashoffset The stroke-dashoffset; a constant indicating the offset in pixels of the first dash along the stroke; defaults to zero.
#' @param strokeLinecap The stroke-linecap; a constant specifying how to cap stroked paths, such as *butt*, *round*, or *square*.
#' @param strokeLinejoin The stroke-linejoin; a constant specifying how to join stroked paths, such as *bevel*, *miter*, *miter-clip*, or *round*.
#' @param strokeMiterlimit The stroke-miterlimit; a constant number specifying how to limit the length of *miter* joins on stroked paths.
#' @param strokeOpacity The stroke-opacity; a constant between 0 and 1, or a channel typically bound to the *opacity* scale.
#' @param strokeWidth The stroke-width; a constant number in pixels, or a channel.
#' @param target The target; a constant string specifying the target window (_e.g._, *_blank*) for clickable links; used in conjunction with the **href** option.
#' @param tip Whether to generate a tooltip for this mark, and any tip options.
#' @param title The title; a channel specifying accessible, short textual descriptions as strings (possibly with newlines).
#' @param x The horizontal position (or length) channel, typically bound to the *x* scale.
#' @param y The vertical position channel, typically bound to the *y* scale; defaults to the zero-based index of the data 0, 1, 2, ….
#' @param ... Additional options or plot-level attributes.
#' @param data_from The name of the data source this mark reads from (see [vg_data()]).
#' @param filter_by A Param/Selection (e.g. from [param()]) to filter this mark's data by.
#' @family mark functions
#' @export
vg_vector <- function(spec = NULL, anchor = vg_unset, ariaDescription = vg_unset, ariaHidden = vg_unset, ariaLabel = vg_unset, channels = vg_unset, clip = vg_unset, dx = vg_unset, dy = vg_unset, facet = vg_unset, facetAnchor = vg_unset, fill = vg_unset, fillOpacity = vg_unset, filter = vg_unset, frameAnchor = vg_unset, fx = vg_unset, fy = vg_unset, href = vg_unset, imageFilter = vg_unset, length = vg_unset, margin = vg_unset, marginBottom = vg_unset, marginLeft = vg_unset, marginRight = vg_unset, marginTop = vg_unset, mixBlendMode = vg_unset, opacity = vg_unset, paintOrder = vg_unset, pointerEvents = vg_unset, r = vg_unset, reverse = vg_unset, rotate = vg_unset, select = vg_unset, shape = vg_unset, shapeRendering = vg_unset, sort = vg_unset, stroke = vg_unset, strokeDasharray = vg_unset, strokeDashoffset = vg_unset, strokeLinecap = vg_unset, strokeLinejoin = vg_unset, strokeMiterlimit = vg_unset, strokeOpacity = vg_unset, strokeWidth = vg_unset, target = vg_unset, tip = vg_unset, title = vg_unset, x = vg_unset, y = vg_unset, ..., data_from = vg_unset, filter_by = vg_unset) {
  vg_mark_(spec, "vector", anchor = anchor, ariaDescription = ariaDescription, ariaHidden = ariaHidden, ariaLabel = ariaLabel, channels = channels, clip = clip, dx = dx, dy = dy, facet = facet, facetAnchor = facetAnchor, fill = fill, fillOpacity = fillOpacity, filter = filter, frameAnchor = frameAnchor, fx = fx, fy = fy, href = href, imageFilter = imageFilter, length = length, margin = margin, marginBottom = marginBottom, marginLeft = marginLeft, marginRight = marginRight, marginTop = marginTop, mixBlendMode = mixBlendMode, opacity = opacity, paintOrder = paintOrder, pointerEvents = pointerEvents, r = r, reverse = reverse, rotate = rotate, select = select, shape = shape, shapeRendering = shapeRendering, sort = sort, stroke = stroke, strokeDasharray = strokeDasharray, strokeDashoffset = strokeDashoffset, strokeLinecap = strokeLinecap, strokeLinejoin = strokeLinejoin, strokeMiterlimit = strokeMiterlimit, strokeOpacity = strokeOpacity, strokeWidth = strokeWidth, target = target, tip = tip, title = title, x = x, y = y, ..., data_from = data_from, filter_by = filter_by)
}

#' The vectorX mark.
#'
#' @param spec A plot fragment or `vgspec` to add this mark to, or `NULL` to start a new plot with just this mark.
#' @param anchor The side of the frame on which to place the axis: *top* or *bottom* for horizontal axes (axisX and axisFx) and their associated vertical grids (gridX and gridFx), or *left* or *right* for vertical axes (axisY and axisFY) and their associated horizontal grids (gridY and gridFy).
#' @param ariaDescription The aria-description; a constant textual description.
#' @param ariaHidden The aria-hidden state; a constant indicating whether the element is exposed to an accessibility API.
#' @param ariaLabel The aria-label; a channel specifying short textual labels representing the value in the accessibility tree.
#' @param channels Additional named channels, for example to include in a tooltip.
#' @param clip How to clip the mark; one of: - *frame* or true - clip to the plot’s frame (inner area) - *sphere* - clip to the projected sphere (*e.g.*, front hemisphere) - null or false - do not clip The *sphere* clip option requires a geographic projection.
#' @param dx The horizontal offset in pixels; a constant option.
#' @param dy The vertical offset in pixels; a constant option.
#' @param facet Whether to enable or disable faceting; one of: - *auto* (default) - automatically determine if this mark should be faceted - *include* (or true) - draw the subset of the mark’s data in the current facet - *exclude* - draw the subset of the mark’s data *not* in the current facet - *super* - draw this mark in a single frame that covers all facets - null (or false) - repeat this mark’s data across all facets (*i.e.*, no faceting) When a mark uses *super* faceting, it is not allowed to use position scales (*x*, *y*, *fx*, or *fy*); *super* faceting is intended for decorations, such as labels and legends.
#' @param facetAnchor How to place the mark with respect to facets; one of: - null (default for most marks) - display the mark in each non-empty facet - *top*, *right*, *bottom*, or *left* - display the mark only in facets on the given side - *top-empty*, *right-empty*, *bottom-empty*, or *left-empty* (default for axis marks) - display the mark only in facets that have empty space on the given side: either the margin, or an empty facet - *empty* - display the mark in empty facets only
#' @param fill The fill; a constant CSS color string, or a channel typically bound to the *color* scale.
#' @param fillOpacity The fill-opacity; a constant number between 0 and 1, or a channel typically bound to the *opacity* scale.
#' @param filter Applies a transform to filter the mark’s index according to the given channel values; only truthy values are retained.
#' @param frameAnchor The frame anchor specifies defaults for **x** and **y**, along with **textAnchor** and **lineAnchor**, based on the plot’s frame; it may be one of the four sides (*top*, *right*, *bottom*, *left*), one of the four corners (*top-left*, *top-right*, *bottom-right*, *bottom-left*), or the *middle* of the frame.
#' @param fx The horizontal facet position channel, for mark-level faceting, bound to the *fx* scale.
#' @param fy The vertical facet position channel, for mark-level faceting, bound to the *fy* scale.
#' @param href The href; a channel specifying URLs for clickable links.
#' @param imageFilter A CSS filter; a constant string used to adjust the rendering of images, such as *blur(5px)*.
#' @param length The vector’s length; either an optional channel bound to the *length* scale or a constant number in pixels.
#' @param margin Shorthand to set the same default for all four mark margins: **marginTop**, **marginRight**, **marginBottom**, and **marginLeft**; typically defaults to 0, except for axis marks.
#' @param marginBottom The mark’s bottom margin; the minimum distance in pixels between the bottom edges of the inner and outer plot area.
#' @param marginLeft The mark’s left margin; the minimum distance in pixels between the left edges of the inner and outer plot area.
#' @param marginRight The mark’s right margin; the minimum distance in pixels between the right edges of the mark’s inner and outer plot area.
#' @param marginTop The mark’s top margin; the minimum distance in pixels between the top edges of the inner and outer plot area.
#' @param mixBlendMode The mix-blend-mode; a constant string specifying how to blend content such as *multiply*.
#' @param opacity The opacity; a constant between 0 and 1, or a channel typically bound to the *opacity* scale.
#' @param paintOrder The paint-order; a constant string specifying the order in which the **fill**, **stroke**, and any markers are drawn; defaults to *normal*, which draws the fill, then stroke, then markers; defaults to *stroke* for the text mark to create a “halo” around text to improve legibility.
#' @param pointerEvents The pointer-events property; a constant string such as *none*.
#' @param r The radius of dots; either a channel or constant.
#' @param reverse Applies a transform to reverse the order of the mark’s index, say for reverse input order.
#' @param rotate The rotation angle in degrees clockwise; a constant or a channel; defaults to 0°.
#' @param select Applies a filter transform after data is loaded to highlight selected values only.
#' @param shape The shape of the vector; a constant.
#' @param shapeRendering The shape-rendering; a constant string such as *crispEdges*.
#' @param sort Either applies a transform to sort the mark’s index by the specified channel values, or imputes ordinal scale domains from this mark’s channels.
#' @param stroke The stroke; a constant CSS color string, or a channel typically bound to the *color* scale.
#' @param strokeDasharray The stroke-dasharray; a constant number indicating the length in pixels of alternating dashes and gaps, or a constant string of numbers separated by spaces or commas (_e.g._, *10 2* for dashes of 10 pixels separated by gaps of 2 pixels), or *none* (the default) for no dashing : https://developer.mozilla.org/en-US/docs/Web/SVG/Attribute/stroke-dasharray
#' @param strokeDashoffset The stroke-dashoffset; a constant indicating the offset in pixels of the first dash along the stroke; defaults to zero.
#' @param strokeLinecap The stroke-linecap; a constant specifying how to cap stroked paths, such as *butt*, *round*, or *square*.
#' @param strokeLinejoin The stroke-linejoin; a constant specifying how to join stroked paths, such as *bevel*, *miter*, *miter-clip*, or *round*.
#' @param strokeMiterlimit The stroke-miterlimit; a constant number specifying how to limit the length of *miter* joins on stroked paths.
#' @param strokeOpacity The stroke-opacity; a constant between 0 and 1, or a channel typically bound to the *opacity* scale.
#' @param strokeWidth The stroke-width; a constant number in pixels, or a channel.
#' @param target The target; a constant string specifying the target window (_e.g._, *_blank*) for clickable links; used in conjunction with the **href** option.
#' @param tip Whether to generate a tooltip for this mark, and any tip options.
#' @param title The title; a channel specifying accessible, short textual descriptions as strings (possibly with newlines).
#' @param x The horizontal position (or length) channel, typically bound to the *x* scale.
#' @param y The vertical position channel, typically bound to the *y* scale; defaults to the zero-based index of the data 0, 1, 2, ….
#' @param ... Additional options or plot-level attributes.
#' @param data_from The name of the data source this mark reads from (see [vg_data()]).
#' @param filter_by A Param/Selection (e.g. from [param()]) to filter this mark's data by.
#' @family mark functions
#' @export
vg_vector_x <- function(spec = NULL, anchor = vg_unset, ariaDescription = vg_unset, ariaHidden = vg_unset, ariaLabel = vg_unset, channels = vg_unset, clip = vg_unset, dx = vg_unset, dy = vg_unset, facet = vg_unset, facetAnchor = vg_unset, fill = vg_unset, fillOpacity = vg_unset, filter = vg_unset, frameAnchor = vg_unset, fx = vg_unset, fy = vg_unset, href = vg_unset, imageFilter = vg_unset, length = vg_unset, margin = vg_unset, marginBottom = vg_unset, marginLeft = vg_unset, marginRight = vg_unset, marginTop = vg_unset, mixBlendMode = vg_unset, opacity = vg_unset, paintOrder = vg_unset, pointerEvents = vg_unset, r = vg_unset, reverse = vg_unset, rotate = vg_unset, select = vg_unset, shape = vg_unset, shapeRendering = vg_unset, sort = vg_unset, stroke = vg_unset, strokeDasharray = vg_unset, strokeDashoffset = vg_unset, strokeLinecap = vg_unset, strokeLinejoin = vg_unset, strokeMiterlimit = vg_unset, strokeOpacity = vg_unset, strokeWidth = vg_unset, target = vg_unset, tip = vg_unset, title = vg_unset, x = vg_unset, y = vg_unset, ..., data_from = vg_unset, filter_by = vg_unset) {
  vg_mark_(spec, "vectorX", anchor = anchor, ariaDescription = ariaDescription, ariaHidden = ariaHidden, ariaLabel = ariaLabel, channels = channels, clip = clip, dx = dx, dy = dy, facet = facet, facetAnchor = facetAnchor, fill = fill, fillOpacity = fillOpacity, filter = filter, frameAnchor = frameAnchor, fx = fx, fy = fy, href = href, imageFilter = imageFilter, length = length, margin = margin, marginBottom = marginBottom, marginLeft = marginLeft, marginRight = marginRight, marginTop = marginTop, mixBlendMode = mixBlendMode, opacity = opacity, paintOrder = paintOrder, pointerEvents = pointerEvents, r = r, reverse = reverse, rotate = rotate, select = select, shape = shape, shapeRendering = shapeRendering, sort = sort, stroke = stroke, strokeDasharray = strokeDasharray, strokeDashoffset = strokeDashoffset, strokeLinecap = strokeLinecap, strokeLinejoin = strokeLinejoin, strokeMiterlimit = strokeMiterlimit, strokeOpacity = strokeOpacity, strokeWidth = strokeWidth, target = target, tip = tip, title = title, x = x, y = y, ..., data_from = data_from, filter_by = filter_by)
}

#' The vectorY mark.
#'
#' @param spec A plot fragment or `vgspec` to add this mark to, or `NULL` to start a new plot with just this mark.
#' @param anchor The side of the frame on which to place the axis: *top* or *bottom* for horizontal axes (axisX and axisFx) and their associated vertical grids (gridX and gridFx), or *left* or *right* for vertical axes (axisY and axisFY) and their associated horizontal grids (gridY and gridFy).
#' @param ariaDescription The aria-description; a constant textual description.
#' @param ariaHidden The aria-hidden state; a constant indicating whether the element is exposed to an accessibility API.
#' @param ariaLabel The aria-label; a channel specifying short textual labels representing the value in the accessibility tree.
#' @param channels Additional named channels, for example to include in a tooltip.
#' @param clip How to clip the mark; one of: - *frame* or true - clip to the plot’s frame (inner area) - *sphere* - clip to the projected sphere (*e.g.*, front hemisphere) - null or false - do not clip The *sphere* clip option requires a geographic projection.
#' @param dx The horizontal offset in pixels; a constant option.
#' @param dy The vertical offset in pixels; a constant option.
#' @param facet Whether to enable or disable faceting; one of: - *auto* (default) - automatically determine if this mark should be faceted - *include* (or true) - draw the subset of the mark’s data in the current facet - *exclude* - draw the subset of the mark’s data *not* in the current facet - *super* - draw this mark in a single frame that covers all facets - null (or false) - repeat this mark’s data across all facets (*i.e.*, no faceting) When a mark uses *super* faceting, it is not allowed to use position scales (*x*, *y*, *fx*, or *fy*); *super* faceting is intended for decorations, such as labels and legends.
#' @param facetAnchor How to place the mark with respect to facets; one of: - null (default for most marks) - display the mark in each non-empty facet - *top*, *right*, *bottom*, or *left* - display the mark only in facets on the given side - *top-empty*, *right-empty*, *bottom-empty*, or *left-empty* (default for axis marks) - display the mark only in facets that have empty space on the given side: either the margin, or an empty facet - *empty* - display the mark in empty facets only
#' @param fill The fill; a constant CSS color string, or a channel typically bound to the *color* scale.
#' @param fillOpacity The fill-opacity; a constant number between 0 and 1, or a channel typically bound to the *opacity* scale.
#' @param filter Applies a transform to filter the mark’s index according to the given channel values; only truthy values are retained.
#' @param frameAnchor The frame anchor specifies defaults for **x** and **y**, along with **textAnchor** and **lineAnchor**, based on the plot’s frame; it may be one of the four sides (*top*, *right*, *bottom*, *left*), one of the four corners (*top-left*, *top-right*, *bottom-right*, *bottom-left*), or the *middle* of the frame.
#' @param fx The horizontal facet position channel, for mark-level faceting, bound to the *fx* scale.
#' @param fy The vertical facet position channel, for mark-level faceting, bound to the *fy* scale.
#' @param href The href; a channel specifying URLs for clickable links.
#' @param imageFilter A CSS filter; a constant string used to adjust the rendering of images, such as *blur(5px)*.
#' @param length The vector’s length; either an optional channel bound to the *length* scale or a constant number in pixels.
#' @param margin Shorthand to set the same default for all four mark margins: **marginTop**, **marginRight**, **marginBottom**, and **marginLeft**; typically defaults to 0, except for axis marks.
#' @param marginBottom The mark’s bottom margin; the minimum distance in pixels between the bottom edges of the inner and outer plot area.
#' @param marginLeft The mark’s left margin; the minimum distance in pixels between the left edges of the inner and outer plot area.
#' @param marginRight The mark’s right margin; the minimum distance in pixels between the right edges of the mark’s inner and outer plot area.
#' @param marginTop The mark’s top margin; the minimum distance in pixels between the top edges of the inner and outer plot area.
#' @param mixBlendMode The mix-blend-mode; a constant string specifying how to blend content such as *multiply*.
#' @param opacity The opacity; a constant between 0 and 1, or a channel typically bound to the *opacity* scale.
#' @param paintOrder The paint-order; a constant string specifying the order in which the **fill**, **stroke**, and any markers are drawn; defaults to *normal*, which draws the fill, then stroke, then markers; defaults to *stroke* for the text mark to create a “halo” around text to improve legibility.
#' @param pointerEvents The pointer-events property; a constant string such as *none*.
#' @param r The radius of dots; either a channel or constant.
#' @param reverse Applies a transform to reverse the order of the mark’s index, say for reverse input order.
#' @param rotate The rotation angle in degrees clockwise; a constant or a channel; defaults to 0°.
#' @param select Applies a filter transform after data is loaded to highlight selected values only.
#' @param shape The shape of the vector; a constant.
#' @param shapeRendering The shape-rendering; a constant string such as *crispEdges*.
#' @param sort Either applies a transform to sort the mark’s index by the specified channel values, or imputes ordinal scale domains from this mark’s channels.
#' @param stroke The stroke; a constant CSS color string, or a channel typically bound to the *color* scale.
#' @param strokeDasharray The stroke-dasharray; a constant number indicating the length in pixels of alternating dashes and gaps, or a constant string of numbers separated by spaces or commas (_e.g._, *10 2* for dashes of 10 pixels separated by gaps of 2 pixels), or *none* (the default) for no dashing : https://developer.mozilla.org/en-US/docs/Web/SVG/Attribute/stroke-dasharray
#' @param strokeDashoffset The stroke-dashoffset; a constant indicating the offset in pixels of the first dash along the stroke; defaults to zero.
#' @param strokeLinecap The stroke-linecap; a constant specifying how to cap stroked paths, such as *butt*, *round*, or *square*.
#' @param strokeLinejoin The stroke-linejoin; a constant specifying how to join stroked paths, such as *bevel*, *miter*, *miter-clip*, or *round*.
#' @param strokeMiterlimit The stroke-miterlimit; a constant number specifying how to limit the length of *miter* joins on stroked paths.
#' @param strokeOpacity The stroke-opacity; a constant between 0 and 1, or a channel typically bound to the *opacity* scale.
#' @param strokeWidth The stroke-width; a constant number in pixels, or a channel.
#' @param target The target; a constant string specifying the target window (_e.g._, *_blank*) for clickable links; used in conjunction with the **href** option.
#' @param tip Whether to generate a tooltip for this mark, and any tip options.
#' @param title The title; a channel specifying accessible, short textual descriptions as strings (possibly with newlines).
#' @param x The horizontal position (or length) channel, typically bound to the *x* scale.
#' @param y The vertical position channel, typically bound to the *y* scale; defaults to the zero-based index of the data 0, 1, 2, ….
#' @param ... Additional options or plot-level attributes.
#' @param data_from The name of the data source this mark reads from (see [vg_data()]).
#' @param filter_by A Param/Selection (e.g. from [param()]) to filter this mark's data by.
#' @family mark functions
#' @export
vg_vector_y <- function(spec = NULL, anchor = vg_unset, ariaDescription = vg_unset, ariaHidden = vg_unset, ariaLabel = vg_unset, channels = vg_unset, clip = vg_unset, dx = vg_unset, dy = vg_unset, facet = vg_unset, facetAnchor = vg_unset, fill = vg_unset, fillOpacity = vg_unset, filter = vg_unset, frameAnchor = vg_unset, fx = vg_unset, fy = vg_unset, href = vg_unset, imageFilter = vg_unset, length = vg_unset, margin = vg_unset, marginBottom = vg_unset, marginLeft = vg_unset, marginRight = vg_unset, marginTop = vg_unset, mixBlendMode = vg_unset, opacity = vg_unset, paintOrder = vg_unset, pointerEvents = vg_unset, r = vg_unset, reverse = vg_unset, rotate = vg_unset, select = vg_unset, shape = vg_unset, shapeRendering = vg_unset, sort = vg_unset, stroke = vg_unset, strokeDasharray = vg_unset, strokeDashoffset = vg_unset, strokeLinecap = vg_unset, strokeLinejoin = vg_unset, strokeMiterlimit = vg_unset, strokeOpacity = vg_unset, strokeWidth = vg_unset, target = vg_unset, tip = vg_unset, title = vg_unset, x = vg_unset, y = vg_unset, ..., data_from = vg_unset, filter_by = vg_unset) {
  vg_mark_(spec, "vectorY", anchor = anchor, ariaDescription = ariaDescription, ariaHidden = ariaHidden, ariaLabel = ariaLabel, channels = channels, clip = clip, dx = dx, dy = dy, facet = facet, facetAnchor = facetAnchor, fill = fill, fillOpacity = fillOpacity, filter = filter, frameAnchor = frameAnchor, fx = fx, fy = fy, href = href, imageFilter = imageFilter, length = length, margin = margin, marginBottom = marginBottom, marginLeft = marginLeft, marginRight = marginRight, marginTop = marginTop, mixBlendMode = mixBlendMode, opacity = opacity, paintOrder = paintOrder, pointerEvents = pointerEvents, r = r, reverse = reverse, rotate = rotate, select = select, shape = shape, shapeRendering = shapeRendering, sort = sort, stroke = stroke, strokeDasharray = strokeDasharray, strokeDashoffset = strokeDashoffset, strokeLinecap = strokeLinecap, strokeLinejoin = strokeLinejoin, strokeMiterlimit = strokeMiterlimit, strokeOpacity = strokeOpacity, strokeWidth = strokeWidth, target = target, tip = tip, title = title, x = x, y = y, ..., data_from = data_from, filter_by = filter_by)
}

#' The voronoi mark.
#'
#' @param spec A plot fragment or `vgspec` to add this mark to, or `NULL` to start a new plot with just this mark.
#' @param ariaDescription The aria-description; a constant textual description.
#' @param ariaHidden The aria-hidden state; a constant indicating whether the element is exposed to an accessibility API.
#' @param ariaLabel The aria-label; a channel specifying short textual labels representing the value in the accessibility tree.
#' @param channels Additional named channels, for example to include in a tooltip.
#' @param clip How to clip the mark; one of: - *frame* or true - clip to the plot’s frame (inner area) - *sphere* - clip to the projected sphere (*e.g.*, front hemisphere) - null or false - do not clip The *sphere* clip option requires a geographic projection.
#' @param curve The curve (interpolation) method for connecting adjacent points.
#' @param dx The horizontal offset in pixels; a constant option.
#' @param dy The vertical offset in pixels; a constant option.
#' @param facet Whether to enable or disable faceting; one of: - *auto* (default) - automatically determine if this mark should be faceted - *include* (or true) - draw the subset of the mark’s data in the current facet - *exclude* - draw the subset of the mark’s data *not* in the current facet - *super* - draw this mark in a single frame that covers all facets - null (or false) - repeat this mark’s data across all facets (*i.e.*, no faceting) When a mark uses *super* faceting, it is not allowed to use position scales (*x*, *y*, *fx*, or *fy*); *super* faceting is intended for decorations, such as labels and legends.
#' @param facetAnchor How to place the mark with respect to facets; one of: - null (default for most marks) - display the mark in each non-empty facet - *top*, *right*, *bottom*, or *left* - display the mark only in facets on the given side - *top-empty*, *right-empty*, *bottom-empty*, or *left-empty* (default for axis marks) - display the mark only in facets that have empty space on the given side: either the margin, or an empty facet - *empty* - display the mark in empty facets only
#' @param fill The fill; a constant CSS color string, or a channel typically bound to the *color* scale.
#' @param fillOpacity The fill-opacity; a constant number between 0 and 1, or a channel typically bound to the *opacity* scale.
#' @param filter Applies a transform to filter the mark’s index according to the given channel values; only truthy values are retained.
#' @param fx The horizontal facet position channel, for mark-level faceting, bound to the *fx* scale.
#' @param fy The vertical facet position channel, for mark-level faceting, bound to the *fy* scale.
#' @param href The href; a channel specifying URLs for clickable links.
#' @param imageFilter A CSS filter; a constant string used to adjust the rendering of images, such as *blur(5px)*.
#' @param margin Shorthand to set the same default for all four mark margins: **marginTop**, **marginRight**, **marginBottom**, and **marginLeft**; typically defaults to 0, except for axis marks.
#' @param marginBottom The mark’s bottom margin; the minimum distance in pixels between the bottom edges of the inner and outer plot area.
#' @param marginLeft The mark’s left margin; the minimum distance in pixels between the left edges of the inner and outer plot area.
#' @param marginRight The mark’s right margin; the minimum distance in pixels between the right edges of the mark’s inner and outer plot area.
#' @param marginTop The mark’s top margin; the minimum distance in pixels between the top edges of the inner and outer plot area.
#' @param marker Shorthand to set the same default for markerStart, markerMid, and markerEnd; one of: - a marker name such as *arrow* or *circle* - *none* (default) - no marker * true - alias for *circle-fill* * false or null - alias for *none*
#' @param markerEnd The marker for the ending point of a line segment; one of: - a marker name such as *arrow* or *circle* * *none* (default) - no marker * true - alias for *circle-fill* * false or null - alias for *none*
#' @param markerMid The marker for any middle (interior) points of a line segment.
#' @param markerStart The marker for the starting point of a line segment; one of: - a marker name such as *arrow* or *circle* * *none* (default) - no marker * true - alias for *circle-fill* * false or null - alias for *none*
#' @param mixBlendMode The mix-blend-mode; a constant string specifying how to blend content such as *multiply*.
#' @param opacity The opacity; a constant between 0 and 1, or a channel typically bound to the *opacity* scale.
#' @param paintOrder The paint-order; a constant string specifying the order in which the **fill**, **stroke**, and any markers are drawn; defaults to *normal*, which draws the fill, then stroke, then markers; defaults to *stroke* for the text mark to create a “halo” around text to improve legibility.
#' @param pointerEvents The pointer-events property; a constant string such as *none*.
#' @param reverse Applies a transform to reverse the order of the mark’s index, say for reverse input order.
#' @param select Applies a filter transform after data is loaded to highlight selected values only.
#' @param shapeRendering The shape-rendering; a constant string such as *crispEdges*.
#' @param sort Either applies a transform to sort the mark’s index by the specified channel values, or imputes ordinal scale domains from this mark’s channels.
#' @param stroke The stroke; a constant CSS color string, or a channel typically bound to the *color* scale.
#' @param strokeDasharray The stroke-dasharray; a constant number indicating the length in pixels of alternating dashes and gaps, or a constant string of numbers separated by spaces or commas (_e.g._, *10 2* for dashes of 10 pixels separated by gaps of 2 pixels), or *none* (the default) for no dashing : https://developer.mozilla.org/en-US/docs/Web/SVG/Attribute/stroke-dasharray
#' @param strokeDashoffset The stroke-dashoffset; a constant indicating the offset in pixels of the first dash along the stroke; defaults to zero.
#' @param strokeLinecap The stroke-linecap; a constant specifying how to cap stroked paths, such as *butt*, *round*, or *square*.
#' @param strokeLinejoin The stroke-linejoin; a constant specifying how to join stroked paths, such as *bevel*, *miter*, *miter-clip*, or *round*.
#' @param strokeMiterlimit The stroke-miterlimit; a constant number specifying how to limit the length of *miter* joins on stroked paths.
#' @param strokeOpacity The stroke-opacity; a constant between 0 and 1, or a channel typically bound to the *opacity* scale.
#' @param strokeWidth The stroke-width; a constant number in pixels, or a channel.
#' @param target The target; a constant string specifying the target window (_e.g._, *_blank*) for clickable links; used in conjunction with the **href** option.
#' @param tension The tension option only has an effect on bundle, cardinal and Catmull–Rom splines (*bundle*, *cardinal*, *cardinal-open*, *cardinal-closed*, *catmull-rom*, *catmull-rom-open*, and *catmull-rom-closed*).
#' @param tip Whether to generate a tooltip for this mark, and any tip options.
#' @param title The title; a channel specifying accessible, short textual descriptions as strings (possibly with newlines).
#' @param x The horizontal position (or length) channel, typically bound to the *x* scale.
#' @param y The vertical position channel, typically bound to the *y* scale; defaults to the zero-based index of the data 0, 1, 2, ….
#' @param z An optional ordinal channel for grouping data into (possibly stacked) series to be drawn as separate areas; defaults to **fill** if a channel, or **stroke** if a channel.
#' @param ... Additional options or plot-level attributes.
#' @param data_from The name of the data source this mark reads from (see [vg_data()]).
#' @param filter_by A Param/Selection (e.g. from [param()]) to filter this mark's data by.
#' @family mark functions
#' @export
vg_voronoi <- function(spec = NULL, ariaDescription = vg_unset, ariaHidden = vg_unset, ariaLabel = vg_unset, channels = vg_unset, clip = vg_unset, curve = vg_unset, dx = vg_unset, dy = vg_unset, facet = vg_unset, facetAnchor = vg_unset, fill = vg_unset, fillOpacity = vg_unset, filter = vg_unset, fx = vg_unset, fy = vg_unset, href = vg_unset, imageFilter = vg_unset, margin = vg_unset, marginBottom = vg_unset, marginLeft = vg_unset, marginRight = vg_unset, marginTop = vg_unset, marker = vg_unset, markerEnd = vg_unset, markerMid = vg_unset, markerStart = vg_unset, mixBlendMode = vg_unset, opacity = vg_unset, paintOrder = vg_unset, pointerEvents = vg_unset, reverse = vg_unset, select = vg_unset, shapeRendering = vg_unset, sort = vg_unset, stroke = vg_unset, strokeDasharray = vg_unset, strokeDashoffset = vg_unset, strokeLinecap = vg_unset, strokeLinejoin = vg_unset, strokeMiterlimit = vg_unset, strokeOpacity = vg_unset, strokeWidth = vg_unset, target = vg_unset, tension = vg_unset, tip = vg_unset, title = vg_unset, x = vg_unset, y = vg_unset, z = vg_unset, ..., data_from = vg_unset, filter_by = vg_unset) {
  vg_mark_(spec, "voronoi", ariaDescription = ariaDescription, ariaHidden = ariaHidden, ariaLabel = ariaLabel, channels = channels, clip = clip, curve = curve, dx = dx, dy = dy, facet = facet, facetAnchor = facetAnchor, fill = fill, fillOpacity = fillOpacity, filter = filter, fx = fx, fy = fy, href = href, imageFilter = imageFilter, margin = margin, marginBottom = marginBottom, marginLeft = marginLeft, marginRight = marginRight, marginTop = marginTop, marker = marker, markerEnd = markerEnd, markerMid = markerMid, markerStart = markerStart, mixBlendMode = mixBlendMode, opacity = opacity, paintOrder = paintOrder, pointerEvents = pointerEvents, reverse = reverse, select = select, shapeRendering = shapeRendering, sort = sort, stroke = stroke, strokeDasharray = strokeDasharray, strokeDashoffset = strokeDashoffset, strokeLinecap = strokeLinecap, strokeLinejoin = strokeLinejoin, strokeMiterlimit = strokeMiterlimit, strokeOpacity = strokeOpacity, strokeWidth = strokeWidth, target = target, tension = tension, tip = tip, title = title, x = x, y = y, z = z, ..., data_from = data_from, filter_by = filter_by)
}

#' The voronoiMesh mark.
#'
#' @param spec A plot fragment or `vgspec` to add this mark to, or `NULL` to start a new plot with just this mark.
#' @param ariaDescription The aria-description; a constant textual description.
#' @param ariaHidden The aria-hidden state; a constant indicating whether the element is exposed to an accessibility API.
#' @param ariaLabel The aria-label; a channel specifying short textual labels representing the value in the accessibility tree.
#' @param channels Additional named channels, for example to include in a tooltip.
#' @param clip How to clip the mark; one of: - *frame* or true - clip to the plot’s frame (inner area) - *sphere* - clip to the projected sphere (*e.g.*, front hemisphere) - null or false - do not clip The *sphere* clip option requires a geographic projection.
#' @param curve The curve (interpolation) method for connecting adjacent points.
#' @param dx The horizontal offset in pixels; a constant option.
#' @param dy The vertical offset in pixels; a constant option.
#' @param facet Whether to enable or disable faceting; one of: - *auto* (default) - automatically determine if this mark should be faceted - *include* (or true) - draw the subset of the mark’s data in the current facet - *exclude* - draw the subset of the mark’s data *not* in the current facet - *super* - draw this mark in a single frame that covers all facets - null (or false) - repeat this mark’s data across all facets (*i.e.*, no faceting) When a mark uses *super* faceting, it is not allowed to use position scales (*x*, *y*, *fx*, or *fy*); *super* faceting is intended for decorations, such as labels and legends.
#' @param facetAnchor How to place the mark with respect to facets; one of: - null (default for most marks) - display the mark in each non-empty facet - *top*, *right*, *bottom*, or *left* - display the mark only in facets on the given side - *top-empty*, *right-empty*, *bottom-empty*, or *left-empty* (default for axis marks) - display the mark only in facets that have empty space on the given side: either the margin, or an empty facet - *empty* - display the mark in empty facets only
#' @param fill The fill; a constant CSS color string, or a channel typically bound to the *color* scale.
#' @param fillOpacity The fill-opacity; a constant number between 0 and 1, or a channel typically bound to the *opacity* scale.
#' @param filter Applies a transform to filter the mark’s index according to the given channel values; only truthy values are retained.
#' @param fx The horizontal facet position channel, for mark-level faceting, bound to the *fx* scale.
#' @param fy The vertical facet position channel, for mark-level faceting, bound to the *fy* scale.
#' @param href The href; a channel specifying URLs for clickable links.
#' @param imageFilter A CSS filter; a constant string used to adjust the rendering of images, such as *blur(5px)*.
#' @param margin Shorthand to set the same default for all four mark margins: **marginTop**, **marginRight**, **marginBottom**, and **marginLeft**; typically defaults to 0, except for axis marks.
#' @param marginBottom The mark’s bottom margin; the minimum distance in pixels between the bottom edges of the inner and outer plot area.
#' @param marginLeft The mark’s left margin; the minimum distance in pixels between the left edges of the inner and outer plot area.
#' @param marginRight The mark’s right margin; the minimum distance in pixels between the right edges of the mark’s inner and outer plot area.
#' @param marginTop The mark’s top margin; the minimum distance in pixels between the top edges of the inner and outer plot area.
#' @param marker Shorthand to set the same default for markerStart, markerMid, and markerEnd; one of: - a marker name such as *arrow* or *circle* - *none* (default) - no marker * true - alias for *circle-fill* * false or null - alias for *none*
#' @param markerEnd The marker for the ending point of a line segment; one of: - a marker name such as *arrow* or *circle* * *none* (default) - no marker * true - alias for *circle-fill* * false or null - alias for *none*
#' @param markerMid The marker for any middle (interior) points of a line segment.
#' @param markerStart The marker for the starting point of a line segment; one of: - a marker name such as *arrow* or *circle* * *none* (default) - no marker * true - alias for *circle-fill* * false or null - alias for *none*
#' @param mixBlendMode The mix-blend-mode; a constant string specifying how to blend content such as *multiply*.
#' @param opacity The opacity; a constant between 0 and 1, or a channel typically bound to the *opacity* scale.
#' @param paintOrder The paint-order; a constant string specifying the order in which the **fill**, **stroke**, and any markers are drawn; defaults to *normal*, which draws the fill, then stroke, then markers; defaults to *stroke* for the text mark to create a “halo” around text to improve legibility.
#' @param pointerEvents The pointer-events property; a constant string such as *none*.
#' @param reverse Applies a transform to reverse the order of the mark’s index, say for reverse input order.
#' @param select Applies a filter transform after data is loaded to highlight selected values only.
#' @param shapeRendering The shape-rendering; a constant string such as *crispEdges*.
#' @param sort Either applies a transform to sort the mark’s index by the specified channel values, or imputes ordinal scale domains from this mark’s channels.
#' @param stroke The stroke; a constant CSS color string, or a channel typically bound to the *color* scale.
#' @param strokeDasharray The stroke-dasharray; a constant number indicating the length in pixels of alternating dashes and gaps, or a constant string of numbers separated by spaces or commas (_e.g._, *10 2* for dashes of 10 pixels separated by gaps of 2 pixels), or *none* (the default) for no dashing : https://developer.mozilla.org/en-US/docs/Web/SVG/Attribute/stroke-dasharray
#' @param strokeDashoffset The stroke-dashoffset; a constant indicating the offset in pixels of the first dash along the stroke; defaults to zero.
#' @param strokeLinecap The stroke-linecap; a constant specifying how to cap stroked paths, such as *butt*, *round*, or *square*.
#' @param strokeLinejoin The stroke-linejoin; a constant specifying how to join stroked paths, such as *bevel*, *miter*, *miter-clip*, or *round*.
#' @param strokeMiterlimit The stroke-miterlimit; a constant number specifying how to limit the length of *miter* joins on stroked paths.
#' @param strokeOpacity The stroke-opacity; a constant between 0 and 1, or a channel typically bound to the *opacity* scale.
#' @param strokeWidth The stroke-width; a constant number in pixels, or a channel.
#' @param target The target; a constant string specifying the target window (_e.g._, *_blank*) for clickable links; used in conjunction with the **href** option.
#' @param tension The tension option only has an effect on bundle, cardinal and Catmull–Rom splines (*bundle*, *cardinal*, *cardinal-open*, *cardinal-closed*, *catmull-rom*, *catmull-rom-open*, and *catmull-rom-closed*).
#' @param tip Whether to generate a tooltip for this mark, and any tip options.
#' @param title The title; a channel specifying accessible, short textual descriptions as strings (possibly with newlines).
#' @param x The horizontal position (or length) channel, typically bound to the *x* scale.
#' @param y The vertical position channel, typically bound to the *y* scale; defaults to the zero-based index of the data 0, 1, 2, ….
#' @param z An optional ordinal channel for grouping data into (possibly stacked) series to be drawn as separate areas; defaults to **fill** if a channel, or **stroke** if a channel.
#' @param ... Additional options or plot-level attributes.
#' @param data_from The name of the data source this mark reads from (see [vg_data()]).
#' @param filter_by A Param/Selection (e.g. from [param()]) to filter this mark's data by.
#' @family mark functions
#' @export
vg_voronoi_mesh <- function(spec = NULL, ariaDescription = vg_unset, ariaHidden = vg_unset, ariaLabel = vg_unset, channels = vg_unset, clip = vg_unset, curve = vg_unset, dx = vg_unset, dy = vg_unset, facet = vg_unset, facetAnchor = vg_unset, fill = vg_unset, fillOpacity = vg_unset, filter = vg_unset, fx = vg_unset, fy = vg_unset, href = vg_unset, imageFilter = vg_unset, margin = vg_unset, marginBottom = vg_unset, marginLeft = vg_unset, marginRight = vg_unset, marginTop = vg_unset, marker = vg_unset, markerEnd = vg_unset, markerMid = vg_unset, markerStart = vg_unset, mixBlendMode = vg_unset, opacity = vg_unset, paintOrder = vg_unset, pointerEvents = vg_unset, reverse = vg_unset, select = vg_unset, shapeRendering = vg_unset, sort = vg_unset, stroke = vg_unset, strokeDasharray = vg_unset, strokeDashoffset = vg_unset, strokeLinecap = vg_unset, strokeLinejoin = vg_unset, strokeMiterlimit = vg_unset, strokeOpacity = vg_unset, strokeWidth = vg_unset, target = vg_unset, tension = vg_unset, tip = vg_unset, title = vg_unset, x = vg_unset, y = vg_unset, z = vg_unset, ..., data_from = vg_unset, filter_by = vg_unset) {
  vg_mark_(spec, "voronoiMesh", ariaDescription = ariaDescription, ariaHidden = ariaHidden, ariaLabel = ariaLabel, channels = channels, clip = clip, curve = curve, dx = dx, dy = dy, facet = facet, facetAnchor = facetAnchor, fill = fill, fillOpacity = fillOpacity, filter = filter, fx = fx, fy = fy, href = href, imageFilter = imageFilter, margin = margin, marginBottom = marginBottom, marginLeft = marginLeft, marginRight = marginRight, marginTop = marginTop, marker = marker, markerEnd = markerEnd, markerMid = markerMid, markerStart = markerStart, mixBlendMode = mixBlendMode, opacity = opacity, paintOrder = paintOrder, pointerEvents = pointerEvents, reverse = reverse, select = select, shapeRendering = shapeRendering, sort = sort, stroke = stroke, strokeDasharray = strokeDasharray, strokeDashoffset = strokeDashoffset, strokeLinecap = strokeLinecap, strokeLinejoin = strokeLinejoin, strokeMiterlimit = strokeMiterlimit, strokeOpacity = strokeOpacity, strokeWidth = strokeWidth, target = target, tension = tension, tip = tip, title = title, x = x, y = y, z = z, ..., data_from = data_from, filter_by = filter_by)
}

#' The waffleX mark.
#'
#' @param spec A plot fragment or `vgspec` to add this mark to, or `NULL` to start a new plot with just this mark.
#' @param ariaDescription The aria-description; a constant textual description.
#' @param ariaHidden The aria-hidden state; a constant indicating whether the element is exposed to an accessibility API.
#' @param ariaLabel The aria-label; a channel specifying short textual labels representing the value in the accessibility tree.
#' @param channels Additional named channels, for example to include in a tooltip.
#' @param clip How to clip the mark; one of: - *frame* or true - clip to the plot’s frame (inner area) - *sphere* - clip to the projected sphere (*e.g.*, front hemisphere) - null or false - do not clip The *sphere* clip option requires a geographic projection.
#' @param dx The horizontal offset in pixels; a constant option.
#' @param dy The vertical offset in pixels; a constant option.
#' @param facet Whether to enable or disable faceting; one of: - *auto* (default) - automatically determine if this mark should be faceted - *include* (or true) - draw the subset of the mark’s data in the current facet - *exclude* - draw the subset of the mark’s data *not* in the current facet - *super* - draw this mark in a single frame that covers all facets - null (or false) - repeat this mark’s data across all facets (*i.e.*, no faceting) When a mark uses *super* faceting, it is not allowed to use position scales (*x*, *y*, *fx*, or *fy*); *super* faceting is intended for decorations, such as labels and legends.
#' @param facetAnchor How to place the mark with respect to facets; one of: - null (default for most marks) - display the mark in each non-empty facet - *top*, *right*, *bottom*, or *left* - display the mark only in facets on the given side - *top-empty*, *right-empty*, *bottom-empty*, or *left-empty* (default for axis marks) - display the mark only in facets that have empty space on the given side: either the margin, or an empty facet - *empty* - display the mark in empty facets only
#' @param fill The fill; a constant CSS color string, or a channel typically bound to the *color* scale.
#' @param fillOpacity The fill-opacity; a constant number between 0 and 1, or a channel typically bound to the *opacity* scale.
#' @param filter Applies a transform to filter the mark’s index according to the given channel values; only truthy values are retained.
#' @param fx The horizontal facet position channel, for mark-level faceting, bound to the *fx* scale.
#' @param fy The vertical facet position channel, for mark-level faceting, bound to the *fy* scale.
#' @param gap The gap in pixels between cells; defaults to 1.
#' @param href The href; a channel specifying URLs for clickable links.
#' @param imageFilter A CSS filter; a constant string used to adjust the rendering of images, such as *blur(5px)*.
#' @param inset Shorthand to set the same default for **insetStart** and **insetEnd**.
#' @param insetBottom Insets the bottom edge by the specified number of pixels.
#' @param insetLeft Insets the left edge by the specified number of pixels.
#' @param insetRight Insets the right edge by the specified number of pixels.
#' @param insetTop Insets the top edge by the specified number of pixels.
#' @param interval Enforces uniformity for data at regular intervals, such as integer values or daily samples.
#' @param margin Shorthand to set the same default for all four mark margins: **marginTop**, **marginRight**, **marginBottom**, and **marginLeft**; typically defaults to 0, except for axis marks.
#' @param marginBottom The mark’s bottom margin; the minimum distance in pixels between the bottom edges of the inner and outer plot area.
#' @param marginLeft The mark’s left margin; the minimum distance in pixels between the left edges of the inner and outer plot area.
#' @param marginRight The mark’s right margin; the minimum distance in pixels between the right edges of the mark’s inner and outer plot area.
#' @param marginTop The mark’s top margin; the minimum distance in pixels between the top edges of the inner and outer plot area.
#' @param mixBlendMode The mix-blend-mode; a constant string specifying how to blend content such as *multiply*.
#' @param multiple The number of cells per row or column; defaults to undefined for automatic.
#' @param offset After stacking, an optional **offset** can be applied to translate and scale stacks, say to produce a streamgraph; defaults to null for a zero baseline (**y** = 0 for stackY, and **x** = 0 for stackX).
#' @param opacity The opacity; a constant between 0 and 1, or a channel typically bound to the *opacity* scale.
#' @param order The order in which stacks are layered; one of: - null (default) for input order - a named stack order method such as *inside-out* or *sum* - a field name, for natural order of the corresponding values - a function of data, for natural order of the corresponding values - an array of explicit **z** values in the desired order If the *wiggle* **offset** is used, as for a streamgraph, the default changes to *inside-out*.
#' @param paintOrder The paint-order; a constant string specifying the order in which the **fill**, **stroke**, and any markers are drawn; defaults to *normal*, which draws the fill, then stroke, then markers; defaults to *stroke* for the text mark to create a “halo” around text to improve legibility.
#' @param pointerEvents The pointer-events property; a constant string such as *none*.
#' @param reverse Applies a transform to reverse the order of the mark’s index, say for reverse input order.
#' @param round If true, round to integers to avoid partial cells.
#' @param rx The rounded corner *x*-radius, either in pixels or as a percentage of the rect width.
#' @param ry The rounded corner *y*-radius, either in pixels or as a percentage of the rect height.
#' @param select Applies a filter transform after data is loaded to highlight selected values only.
#' @param shapeRendering The shape-rendering; a constant string such as *crispEdges*.
#' @param sort Either applies a transform to sort the mark’s index by the specified channel values, or imputes ordinal scale domains from this mark’s channels.
#' @param stroke The stroke; a constant CSS color string, or a channel typically bound to the *color* scale.
#' @param strokeDasharray The stroke-dasharray; a constant number indicating the length in pixels of alternating dashes and gaps, or a constant string of numbers separated by spaces or commas (_e.g._, *10 2* for dashes of 10 pixels separated by gaps of 2 pixels), or *none* (the default) for no dashing : https://developer.mozilla.org/en-US/docs/Web/SVG/Attribute/stroke-dasharray
#' @param strokeDashoffset The stroke-dashoffset; a constant indicating the offset in pixels of the first dash along the stroke; defaults to zero.
#' @param strokeLinecap The stroke-linecap; a constant specifying how to cap stroked paths, such as *butt*, *round*, or *square*.
#' @param strokeLinejoin The stroke-linejoin; a constant specifying how to join stroked paths, such as *bevel*, *miter*, *miter-clip*, or *round*.
#' @param strokeMiterlimit The stroke-miterlimit; a constant number specifying how to limit the length of *miter* joins on stroked paths.
#' @param strokeOpacity The stroke-opacity; a constant between 0 and 1, or a channel typically bound to the *opacity* scale.
#' @param strokeWidth The stroke-width; a constant number in pixels, or a channel.
#' @param target The target; a constant string specifying the target window (_e.g._, *_blank*) for clickable links; used in conjunction with the **href** option.
#' @param tip Whether to generate a tooltip for this mark, and any tip options.
#' @param title The title; a channel specifying accessible, short textual descriptions as strings (possibly with newlines).
#' @param unit The quantity each cell represents; defaults to 1.
#' @param x The horizontal position (or length) channel, typically bound to the *x* scale.
#' @param x1 The required primary (starting, often left) horizontal position channel, representing the area’s baseline, typically bound to the *x* scale.
#' @param x2 The optional secondary (ending, often right) horizontal position channel, representing the area’s topline, typically bound to the *x* scale; if not specified, **x1** is used.
#' @param y The vertical position channel, typically bound to the *y* scale; defaults to the zero-based index of the data 0, 1, 2, ….
#' @param z An optional ordinal channel for grouping data into (possibly stacked) series to be drawn as separate areas; defaults to **fill** if a channel, or **stroke** if a channel.
#' @param ... Additional options or plot-level attributes.
#' @param data_from The name of the data source this mark reads from (see [vg_data()]).
#' @param filter_by A Param/Selection (e.g. from [param()]) to filter this mark's data by.
#' @family mark functions
#' @export
vg_waffle_x <- function(spec = NULL, ariaDescription = vg_unset, ariaHidden = vg_unset, ariaLabel = vg_unset, channels = vg_unset, clip = vg_unset, dx = vg_unset, dy = vg_unset, facet = vg_unset, facetAnchor = vg_unset, fill = vg_unset, fillOpacity = vg_unset, filter = vg_unset, fx = vg_unset, fy = vg_unset, gap = vg_unset, href = vg_unset, imageFilter = vg_unset, inset = vg_unset, insetBottom = vg_unset, insetLeft = vg_unset, insetRight = vg_unset, insetTop = vg_unset, interval = vg_unset, margin = vg_unset, marginBottom = vg_unset, marginLeft = vg_unset, marginRight = vg_unset, marginTop = vg_unset, mixBlendMode = vg_unset, multiple = vg_unset, offset = vg_unset, opacity = vg_unset, order = vg_unset, paintOrder = vg_unset, pointerEvents = vg_unset, reverse = vg_unset, round = vg_unset, rx = vg_unset, ry = vg_unset, select = vg_unset, shapeRendering = vg_unset, sort = vg_unset, stroke = vg_unset, strokeDasharray = vg_unset, strokeDashoffset = vg_unset, strokeLinecap = vg_unset, strokeLinejoin = vg_unset, strokeMiterlimit = vg_unset, strokeOpacity = vg_unset, strokeWidth = vg_unset, target = vg_unset, tip = vg_unset, title = vg_unset, unit = vg_unset, x = vg_unset, x1 = vg_unset, x2 = vg_unset, y = vg_unset, z = vg_unset, ..., data_from = vg_unset, filter_by = vg_unset) {
  vg_mark_(spec, "waffleX", ariaDescription = ariaDescription, ariaHidden = ariaHidden, ariaLabel = ariaLabel, channels = channels, clip = clip, dx = dx, dy = dy, facet = facet, facetAnchor = facetAnchor, fill = fill, fillOpacity = fillOpacity, filter = filter, fx = fx, fy = fy, gap = gap, href = href, imageFilter = imageFilter, inset = inset, insetBottom = insetBottom, insetLeft = insetLeft, insetRight = insetRight, insetTop = insetTop, interval = interval, margin = margin, marginBottom = marginBottom, marginLeft = marginLeft, marginRight = marginRight, marginTop = marginTop, mixBlendMode = mixBlendMode, multiple = multiple, offset = offset, opacity = opacity, order = order, paintOrder = paintOrder, pointerEvents = pointerEvents, reverse = reverse, round = round, rx = rx, ry = ry, select = select, shapeRendering = shapeRendering, sort = sort, stroke = stroke, strokeDasharray = strokeDasharray, strokeDashoffset = strokeDashoffset, strokeLinecap = strokeLinecap, strokeLinejoin = strokeLinejoin, strokeMiterlimit = strokeMiterlimit, strokeOpacity = strokeOpacity, strokeWidth = strokeWidth, target = target, tip = tip, title = title, unit = unit, x = x, x1 = x1, x2 = x2, y = y, z = z, ..., data_from = data_from, filter_by = filter_by)
}

#' The waffleY mark.
#'
#' @param spec A plot fragment or `vgspec` to add this mark to, or `NULL` to start a new plot with just this mark.
#' @param ariaDescription The aria-description; a constant textual description.
#' @param ariaHidden The aria-hidden state; a constant indicating whether the element is exposed to an accessibility API.
#' @param ariaLabel The aria-label; a channel specifying short textual labels representing the value in the accessibility tree.
#' @param channels Additional named channels, for example to include in a tooltip.
#' @param clip How to clip the mark; one of: - *frame* or true - clip to the plot’s frame (inner area) - *sphere* - clip to the projected sphere (*e.g.*, front hemisphere) - null or false - do not clip The *sphere* clip option requires a geographic projection.
#' @param dx The horizontal offset in pixels; a constant option.
#' @param dy The vertical offset in pixels; a constant option.
#' @param facet Whether to enable or disable faceting; one of: - *auto* (default) - automatically determine if this mark should be faceted - *include* (or true) - draw the subset of the mark’s data in the current facet - *exclude* - draw the subset of the mark’s data *not* in the current facet - *super* - draw this mark in a single frame that covers all facets - null (or false) - repeat this mark’s data across all facets (*i.e.*, no faceting) When a mark uses *super* faceting, it is not allowed to use position scales (*x*, *y*, *fx*, or *fy*); *super* faceting is intended for decorations, such as labels and legends.
#' @param facetAnchor How to place the mark with respect to facets; one of: - null (default for most marks) - display the mark in each non-empty facet - *top*, *right*, *bottom*, or *left* - display the mark only in facets on the given side - *top-empty*, *right-empty*, *bottom-empty*, or *left-empty* (default for axis marks) - display the mark only in facets that have empty space on the given side: either the margin, or an empty facet - *empty* - display the mark in empty facets only
#' @param fill The fill; a constant CSS color string, or a channel typically bound to the *color* scale.
#' @param fillOpacity The fill-opacity; a constant number between 0 and 1, or a channel typically bound to the *opacity* scale.
#' @param filter Applies a transform to filter the mark’s index according to the given channel values; only truthy values are retained.
#' @param fx The horizontal facet position channel, for mark-level faceting, bound to the *fx* scale.
#' @param fy The vertical facet position channel, for mark-level faceting, bound to the *fy* scale.
#' @param gap The gap in pixels between cells; defaults to 1.
#' @param href The href; a channel specifying URLs for clickable links.
#' @param imageFilter A CSS filter; a constant string used to adjust the rendering of images, such as *blur(5px)*.
#' @param inset Shorthand to set the same default for **insetStart** and **insetEnd**.
#' @param insetBottom Insets the bottom edge by the specified number of pixels.
#' @param insetLeft Insets the left edge by the specified number of pixels.
#' @param insetRight Insets the right edge by the specified number of pixels.
#' @param insetTop Insets the top edge by the specified number of pixels.
#' @param interval Enforces uniformity for data at regular intervals, such as integer values or daily samples.
#' @param margin Shorthand to set the same default for all four mark margins: **marginTop**, **marginRight**, **marginBottom**, and **marginLeft**; typically defaults to 0, except for axis marks.
#' @param marginBottom The mark’s bottom margin; the minimum distance in pixels between the bottom edges of the inner and outer plot area.
#' @param marginLeft The mark’s left margin; the minimum distance in pixels between the left edges of the inner and outer plot area.
#' @param marginRight The mark’s right margin; the minimum distance in pixels between the right edges of the mark’s inner and outer plot area.
#' @param marginTop The mark’s top margin; the minimum distance in pixels between the top edges of the inner and outer plot area.
#' @param mixBlendMode The mix-blend-mode; a constant string specifying how to blend content such as *multiply*.
#' @param multiple The number of cells per row or column; defaults to undefined for automatic.
#' @param offset After stacking, an optional **offset** can be applied to translate and scale stacks, say to produce a streamgraph; defaults to null for a zero baseline (**y** = 0 for stackY, and **x** = 0 for stackX).
#' @param opacity The opacity; a constant between 0 and 1, or a channel typically bound to the *opacity* scale.
#' @param order The order in which stacks are layered; one of: - null (default) for input order - a named stack order method such as *inside-out* or *sum* - a field name, for natural order of the corresponding values - a function of data, for natural order of the corresponding values - an array of explicit **z** values in the desired order If the *wiggle* **offset** is used, as for a streamgraph, the default changes to *inside-out*.
#' @param paintOrder The paint-order; a constant string specifying the order in which the **fill**, **stroke**, and any markers are drawn; defaults to *normal*, which draws the fill, then stroke, then markers; defaults to *stroke* for the text mark to create a “halo” around text to improve legibility.
#' @param pointerEvents The pointer-events property; a constant string such as *none*.
#' @param reverse Applies a transform to reverse the order of the mark’s index, say for reverse input order.
#' @param round If true, round to integers to avoid partial cells.
#' @param rx The rounded corner *x*-radius, either in pixels or as a percentage of the rect width.
#' @param ry The rounded corner *y*-radius, either in pixels or as a percentage of the rect height.
#' @param select Applies a filter transform after data is loaded to highlight selected values only.
#' @param shapeRendering The shape-rendering; a constant string such as *crispEdges*.
#' @param sort Either applies a transform to sort the mark’s index by the specified channel values, or imputes ordinal scale domains from this mark’s channels.
#' @param stroke The stroke; a constant CSS color string, or a channel typically bound to the *color* scale.
#' @param strokeDasharray The stroke-dasharray; a constant number indicating the length in pixels of alternating dashes and gaps, or a constant string of numbers separated by spaces or commas (_e.g._, *10 2* for dashes of 10 pixels separated by gaps of 2 pixels), or *none* (the default) for no dashing : https://developer.mozilla.org/en-US/docs/Web/SVG/Attribute/stroke-dasharray
#' @param strokeDashoffset The stroke-dashoffset; a constant indicating the offset in pixels of the first dash along the stroke; defaults to zero.
#' @param strokeLinecap The stroke-linecap; a constant specifying how to cap stroked paths, such as *butt*, *round*, or *square*.
#' @param strokeLinejoin The stroke-linejoin; a constant specifying how to join stroked paths, such as *bevel*, *miter*, *miter-clip*, or *round*.
#' @param strokeMiterlimit The stroke-miterlimit; a constant number specifying how to limit the length of *miter* joins on stroked paths.
#' @param strokeOpacity The stroke-opacity; a constant between 0 and 1, or a channel typically bound to the *opacity* scale.
#' @param strokeWidth The stroke-width; a constant number in pixels, or a channel.
#' @param target The target; a constant string specifying the target window (_e.g._, *_blank*) for clickable links; used in conjunction with the **href** option.
#' @param tip Whether to generate a tooltip for this mark, and any tip options.
#' @param title The title; a channel specifying accessible, short textual descriptions as strings (possibly with newlines).
#' @param unit The quantity each cell represents; defaults to 1.
#' @param x The horizontal position (or length) channel, typically bound to the *x* scale.
#' @param y The vertical position channel, typically bound to the *y* scale; defaults to the zero-based index of the data 0, 1, 2, ….
#' @param y1 The required primary (starting, often bottom) vertical position channel, representing the area’s baseline, typically bound to the *y* scale.
#' @param y2 The optional secondary (ending, often top) vertical position channel, representing the area’s topline, typically bound to the *y* scale; if not specified, **y1** is used.
#' @param z An optional ordinal channel for grouping data into (possibly stacked) series to be drawn as separate areas; defaults to **fill** if a channel, or **stroke** if a channel.
#' @param ... Additional options or plot-level attributes.
#' @param data_from The name of the data source this mark reads from (see [vg_data()]).
#' @param filter_by A Param/Selection (e.g. from [param()]) to filter this mark's data by.
#' @family mark functions
#' @export
vg_waffle_y <- function(spec = NULL, ariaDescription = vg_unset, ariaHidden = vg_unset, ariaLabel = vg_unset, channels = vg_unset, clip = vg_unset, dx = vg_unset, dy = vg_unset, facet = vg_unset, facetAnchor = vg_unset, fill = vg_unset, fillOpacity = vg_unset, filter = vg_unset, fx = vg_unset, fy = vg_unset, gap = vg_unset, href = vg_unset, imageFilter = vg_unset, inset = vg_unset, insetBottom = vg_unset, insetLeft = vg_unset, insetRight = vg_unset, insetTop = vg_unset, interval = vg_unset, margin = vg_unset, marginBottom = vg_unset, marginLeft = vg_unset, marginRight = vg_unset, marginTop = vg_unset, mixBlendMode = vg_unset, multiple = vg_unset, offset = vg_unset, opacity = vg_unset, order = vg_unset, paintOrder = vg_unset, pointerEvents = vg_unset, reverse = vg_unset, round = vg_unset, rx = vg_unset, ry = vg_unset, select = vg_unset, shapeRendering = vg_unset, sort = vg_unset, stroke = vg_unset, strokeDasharray = vg_unset, strokeDashoffset = vg_unset, strokeLinecap = vg_unset, strokeLinejoin = vg_unset, strokeMiterlimit = vg_unset, strokeOpacity = vg_unset, strokeWidth = vg_unset, target = vg_unset, tip = vg_unset, title = vg_unset, unit = vg_unset, x = vg_unset, y = vg_unset, y1 = vg_unset, y2 = vg_unset, z = vg_unset, ..., data_from = vg_unset, filter_by = vg_unset) {
  vg_mark_(spec, "waffleY", ariaDescription = ariaDescription, ariaHidden = ariaHidden, ariaLabel = ariaLabel, channels = channels, clip = clip, dx = dx, dy = dy, facet = facet, facetAnchor = facetAnchor, fill = fill, fillOpacity = fillOpacity, filter = filter, fx = fx, fy = fy, gap = gap, href = href, imageFilter = imageFilter, inset = inset, insetBottom = insetBottom, insetLeft = insetLeft, insetRight = insetRight, insetTop = insetTop, interval = interval, margin = margin, marginBottom = marginBottom, marginLeft = marginLeft, marginRight = marginRight, marginTop = marginTop, mixBlendMode = mixBlendMode, multiple = multiple, offset = offset, opacity = opacity, order = order, paintOrder = paintOrder, pointerEvents = pointerEvents, reverse = reverse, round = round, rx = rx, ry = ry, select = select, shapeRendering = shapeRendering, sort = sort, stroke = stroke, strokeDasharray = strokeDasharray, strokeDashoffset = strokeDashoffset, strokeLinecap = strokeLinecap, strokeLinejoin = strokeLinejoin, strokeMiterlimit = strokeMiterlimit, strokeOpacity = strokeOpacity, strokeWidth = strokeWidth, target = target, tip = tip, title = title, unit = unit, x = x, y = y, y1 = y1, y2 = y2, z = z, ..., data_from = data_from, filter_by = filter_by)
}

